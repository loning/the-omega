\section{附录}

\subsection{可复现性声明}

本文所有正文引用的实验产物均由本论文目录下脚本生成：
\begin{verbatim}
python3 -m pip install -r requirements.txt
python3 scripts/run_all.py
\end{verbatim}
生成的 LaTeX 片段位于 `sections/generated/`，稳定导出图/表（若有）位于 `artifacts/export/`。

