\subsection[Gamma audits]{Gamma audits: proxy weak-field amplitude tests and a direct $\gamma_{\mathrm{dict}}$ calibration (audit)}
\label{app:gamma_crossobs_consistency}

This appendix closes an interface-audit deliverable flagged in \texttt{theory\_closure\_tracker.md}:
a concrete audit of the single-parameter weak-field dictionaries that appear in the overhead-to-lapse and overhead-to-gravity closures.
We separate two logically distinct objects:
\begin{itemize}
\item a \emph{direct} estimate of the dictionary coefficient $\gamma_{\mathrm{dict}}$ in
\[
N=\e^{-\gamma_{\mathrm{dict}}\chi},\qquad
\Phi=-\gamma_{\mathrm{dict}} c^2(\chi-\chi_0),
\]
connecting a reconstructed overhead proxy field $\chi$ to clock-rate (lapse) and weak-field gravitational proxies
(Section~\ref{app:overhead_to_gravity_closure} and Appendix~\ref{app:time_mass_delay_reference}).
\item a \emph{proxy} amplitude parameter $\gamma_{\mathrm{proxy}}$ obtained by compressing standard weak-field constraints
(PPN, redshift tests, and a few compact cosmology summaries) into a single scalar rescaling under explicit assumption footprints.
\end{itemize}
\AuditTag We use the notation $\gamma_{\mathrm{dict}}$ in this appendix to avoid confusion with the PPN parameter $\gamma_{\mathrm{PPN}}$; it denotes the same dictionary coefficient written as $\gamma$ in Section~\ref{app:overhead_to_gravity_closure}.

\paragraph{Scope and status.}
\InterfaceTag The purpose of this module is \emph{not} to import external physics as theorem-level premises.
It is an audit-facing interface object: given declared data channels and a declared reconstruction/matching pipeline,
it produces deterministic point estimates (and declared uncertainty/stability envelopes) for \emph{either} $\gamma_{\mathrm{proxy}}$ or $\gamma_{\mathrm{dict}}$.
\AuditTag We do \emph{not} perform a single joint $\chi^2$ test across proxy constraints and the direct rotation-curve calibration, because they do not estimate the same object unless an additional identification between $\chi$ and the weak-field potentials is supplied and audited.

\paragraph{Reproducible artifact.}
\AuditTag The estimates, stability sweeps, and figure in this appendix are generated by the deterministic script
\texttt{scripts/exp\_gamma\_cross\_observation.py} from a small vendored data subset under
\texttt{data/gamma\_crossobs/}. The script writes the LaTeX fragments
\texttt{sections/generated/gamma\_crossobs\_proxy\_rows.tex},
\texttt{sections/generated/gamma\_crossobs\_proxy\_diagnostics.tex},
\texttt{sections/generated/gamma\_crossobs\_proxy\_stability\_rows.tex},
\texttt{sections/generated/gamma\_crossobs\_direct\_rows.tex},
\texttt{sections/generated/gamma\_crossobs\_direct\_diagnostics.tex},
\texttt{sections/generated/gamma\_crossobs\_direct\_stability\_rows.tex},
and the figures
\texttt{figures/gamma\_crossobs\_proxy.png} and \texttt{figures/gamma\_crossobs\_direct.png}.

\FloatBarrier

\subsubsection[Channel-level estimators]{Channel-level estimators (summary)}
\label{subsec:gamma_crossobs_estimators}

\paragraph{Rotation curves (SPARC).}
\InterfaceTag We implement the one-parameter weighted least-squares estimator in Section~\ref{app:overhead_to_gravity_closure}:
with $y_i=v_i^2$ and $x_i=-c^2 r_i \chi'(r_i)$, fit $y_i=\gamma x_i$ under weights $w_i=\sigma_{y,i}^{-2}$
with $\sigma_{y,i}\approx 2v_i\sigma_i$.
The $\chi$ field is reconstructed from an auxiliary scalar profile (disk surface brightness) using a minimal 1D specialization
of Section~\ref{app:chi_reconstruction_protocol}; the derivative/smoothing rule is declared and audited by a sweep
(Appendix~\ref{app:protocol_to_continuum_error_control}).

\paragraph{Lensing / time-delay / redshift proxy constraints.}
\InterfaceTag For compact vendored cross-check channels, we use published summary parameters as small-footprint audit inputs.
These are treated as matching-layer constraints and are not premises for the folding core.

\subsubsection{Proxy maps (weak-field amplitude compression) and assumption footprints}
\label{subsec:gamma_crossobs_maps}

\paragraph{Notation and scope.}
\InterfaceTag The parameter $\gamma_{\mathrm{proxy}}$ in this subsection is \emph{not} $\gamma_{\mathrm{dict}}$:
it is a single-number summary that compresses standard weak-field constraints into a scalar amplitude proxy under explicitly stated footprints.
\MatchTag Solar-system constraints, lensing-amplitude summaries, and time-delay $H_0$ inferences are not imported as theorem-level premises.
They are used here only as compact, auditable comparison inputs to form proxy estimates for $\gamma_{\mathrm{proxy}}$ under explicit assumption footprints.

\paragraph{Solar-system Shapiro / light-deflection constraints (PPN).}
\MatchTag A solar-system test reports the PPN parameter $\gamma_{\mathrm{PPN}}$ (e.g.\ Cassini Shapiro delay and VLBI deflection constraints)
\cite{BertottiIessTortora2003,FomalontKopeikin2009}.
To compare such constraints to the single-parameter dictionary used here, we adopt the normalization proxy
\[
\widehat\gamma_{\mathrm{proxy}}:=\frac{1+\gamma_{\mathrm{PPN}}}{2},
\]
so that general relativity ($\gamma_{\mathrm{PPN}}=1$) corresponds to $\gamma_{\mathrm{proxy}}=1$.
\AuditTag Assumption footprint: weak-field static regime; a single scalar coefficient governs both the lapse/potential amplitude used in the
dictionary and the dominant Shapiro/deflection amplitude in the PPN limit; deviations are summarized by a one-parameter rescaling proxy.

\paragraph{Solar-system gravitational redshift test.}
\MatchTag Redshift tests often report a fractional amplitude deviation parameter $\alpha$ defined by a measured shift of the form
$\Delta\nu/\nu=(1+\alpha)\Delta U/c^2$ under a standard weak-field dictionary \cite{Delva2018GalileoRedshift}.
We use the compact proxy map
\[
\widehat\gamma_{\mathrm{proxy}}:=1+\alpha,
\]
so $\alpha=0$ maps to $\gamma_{\mathrm{proxy}}=1$.
\AuditTag Assumption footprint: the same weak-field redshift amplitude that is tested by $\alpha$ is identified with the single-coefficient lapse dictionary
used here (up to the declared normalization).

\paragraph{Weak-lensing map proxy (CMB lensing amplitude \texorpdfstring{$A_L$}{A\_L}).}
\MatchTag We use the phenomenological lensing-amplitude parameter $A_L$ reported in Planck-2018 cosmological-parameter fits
as a compact weak-lensing-map proxy \cite{Planck2018Parameters}.
As the baseline map we set
\[
\widehat\gamma_{\mathrm{proxy}}:=\sqrt{A_L},
\]
since Planck parameterizes a rescaling of the lensing power (in a fixed pipeline) as $C_\ell^{\phi\phi}\mapsto A_L\,C_\ell^{\phi\phi}$, while a linear potential-amplitude rescaling would propagate quadratically to the power (see, e.g., standard weak-lensing conventions \cite{BartelmannSchneider2001}).
\AuditTag As a bounded counterfactual, we also report the alternative proxy $\widehat\gamma_{\mathrm{proxy}}:=A_L$ as a sensitivity diagnostic;
both are treated as matching-layer conventions.

\paragraph{Strong-lensing time-delay proxy (\texorpdfstring{$H_0$}{H0} inference).}
\MatchTag We use a published $H_0$ inference from time-delay strong lensing as a compact proxy channel \cite{Wong2019H0LiCOWXIII}.
As a baseline, we map the ratio to a Planck-2018 reference value \cite{Planck2018Parameters} by
\[
\widehat\gamma_{\mathrm{proxy}}:=\frac{H_0^{\mathrm{TD}}}{H_0^{\mathrm{ref}}},
\qquad
H_0^{\mathrm{ref}}:=H_0^{\mathrm{Planck18}}.
\]
\AuditTag Assumption footprint: the time-delay distance scale is treated as inversely proportional to $H_0$ under a fixed background model,
and any additional amplitude rescaling of the delay/potential contribution is summarized by a one-parameter ratio proxy. As a bounded sensitivity diagnostic,
we also report the same ratio using a late-time distance-ladder reference value (SH0ES) \cite{Riess2019SH0ES}.

\paragraph{Rotation curves (SPARC) via reconstructed \texorpdfstring{$\chi(r)$}{chi(r)}.}
\InterfaceTag This is the \emph{direct} dictionary calibration channel in the present audit: we reconstruct a proxy $\widehat\chi(r)$ from a scalar profile and use the WLS
fit $v^2=\gamma_{\mathrm{dict}}(-c^2 r\chi'(r))$ as stated above, producing a point estimate of $\gamma_{\mathrm{dict}}$ together with declared stability sweeps.
\MatchTag The SPARC rotmod files used here are a tiny vendored subset of the public SPARC dataset \cite{LelliMcGaughSchombert2016SPARC}; they serve as compact
audit inputs only, with the protocol-level reconstruction and derivative regularization declared and swept.

\subsubsection{Results and consistency diagnostics}
\label{subsec:gamma_crossobs_results}

\begin{table}[t]
\centering
\caption{Proxy estimates $\widehat\gamma_{\mathrm{proxy}}$ from compact weak-field channels. The ``pull'' is $(\widehat\gamma_{\mathrm{proxy}}-\widehat\gamma_{\mathrm{proxy,joint}})/\sigma$ under the proxy-only inverse-variance joint estimate.}
\label{tab:gamma_proxy_estimates}
\begin{tabular}{llllp{7.2cm}}
\toprule
Channel & Dataset & $\widehat\gamma_{\mathrm{proxy}} \pm \sigma$ & Pull & Notes \\
\midrule
\input{sections/generated/gamma_crossobs_proxy_rows}%
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Proxy-map stability under bounded counterfactual baselines (alternate proxy maps / reference choices).}
\label{tab:gamma_proxy_stability}
\begin{tabular}{llllllp{7.2cm}}
\toprule
Block & Dataset & $\widehat\gamma_{\mathrm{proxy}} \pm \sigma$ & $\gamma_{\min}$ & $\gamma_{\max}$ & $\max|\Delta\gamma|$ & Notes \\
\midrule
\input{sections/generated/gamma_crossobs_proxy_stability_rows}%
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.92\linewidth]{figures/gamma_crossobs_proxy.png}
\caption{Proxy weak-field amplitude summary: vendored $\widehat\gamma_{\mathrm{proxy}}$ estimates with the proxy-only joint estimate shown as a vertical band.}
\label{fig:gamma_crossobs_proxy}
\end{figure}

\paragraph{Proxy consistency diagnostics (baseline proxy maps).}
\AuditTag \input{sections/generated/gamma_crossobs_proxy_diagnostics.tex}

\begin{table}[t]
\centering
\caption{Direct rotation-curve calibration of the dictionary coefficient $\gamma_{\mathrm{dict}}$ using the declared $\widehat\chi(r)$ reconstruction and the WLS fit $v^2=\gamma_{\mathrm{dict}}(-c^2 r\chi'(r))$. The ``pull'' is computed relative to the rotation-curve joint estimate across galaxies.}
\label{tab:gamma_direct_estimates}
\begin{tabular}{llllp{7.2cm}}
\toprule
Channel & Dataset & $\widehat\gamma_{\mathrm{dict}} \pm \sigma$ & Pull & Notes \\
\midrule
\input{sections/generated/gamma_crossobs_direct_rows}%
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Direct calibration stability: counterfactual reconstruction/regularization sweeps for the rotation-curve pipeline and leave-one-galaxy-out joint estimates.}
\label{tab:gamma_direct_stability}
\begin{tabular}{llllllp{7.2cm}}
\toprule
Block & Dataset & $\widehat\gamma_{\mathrm{dict}} \pm \sigma$ & $\gamma_{\min}$ & $\gamma_{\max}$ & $\max|\Delta\gamma|$ & Notes \\
\midrule
\input{sections/generated/gamma_crossobs_direct_stability_rows}%
\end{tabular}
\end{table}

\FloatBarrier

\begin{figure}[t]
\centering
\includegraphics[width=0.92\linewidth]{figures/gamma_crossobs_direct.png}
\caption{Direct rotation-curve calibration: per-galaxy $\widehat\gamma_{\mathrm{dict}}$ estimates and their joint estimate (vertical band).}
\label{fig:gamma_crossobs_direct}
\end{figure}

\paragraph{Direct calibration diagnostics.}
\AuditTag \input{sections/generated/gamma_crossobs_direct_diagnostics.tex}

