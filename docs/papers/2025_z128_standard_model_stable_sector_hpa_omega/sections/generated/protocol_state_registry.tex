\begin{center}
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{p{0.24\textwidth} p{0.18\textwidth} p{0.22\textwidth} p{0.30\textwidth}}
\toprule
output & protocol state & kernel family & closure note \\
\midrule
Joint protocol-state selection (theory-first; $J_\mu$) & selected $(m^\ast,n^\ast,K^\ast)$ (generated) & finite $\mathcal{F}_\mu\subset\mathcal{M}\times\mathcal{N}\times\mathcal{K}$ & One-shot CAP closure by minimizing $J_\mu=\mathrm{MDL}_\mu+\lambda\,\mathrm{Sens}_\mu$; see Appendix~\ref{subsec:tick_cap_joint_key_contract} and \texttt{sections/generated/protocol\_state\_selected.tex}. \\
Electroweak normalization ($\alpha^{-1}(\mu_Z)$, $\sin^2\theta_W(\mu_Z)$) & $(m,n,K^\ast)$ at $\mu_Z$ & finite $\mathcal{K}$ (Appendix~\ref{app:kernel_family_cap_closure}) & Kernel-closed weighted-volume dictionary; CAP tie-break inside $\mathcal{K}$; see Definition~\ref{def:ew_volumes}. \\
Mass spectrum template (depth map and matching-layer $\Delta r$) & anchor $(m,n)=(6,3)$; $K$ optional & kernel-irrelevant for the deterministic depth ansatz & CAP over a bounded integer ansatz class; optional probabilistic readings must declare $K$ (Section~\ref{sec:mass_spectrum_closure}). \\
PMNS closure (mixing angles; bounded family) & anchor $(m,n)=(6,3)$; $K$ optional & kernel-irrelevant for the minimal bounded phase/perm families & CAP over bounded discrete candidates; kernel dependence only enters when forming weighted readouts from refined fibers (Section~\ref{sec:pmns_neutrino_closure}). \\
$\widehat\chi$ reconstruction and $\gamma$ cross-observation audits (SPARC/proxy) & $(m,n,K)$ with 1D specialization (Appendix~\ref{app:gamma_crossobs_consistency}) & finite tempered family in the aggregator (tables in Appendix~\ref{app:generated_tables}) & Direct/proxy separation; bounded counterfactual sweeps include kernel-family sensitivity of the direct pipeline. \\
Cosmology energy-budget interface (occupancy mapping; audit) & $(m,n,K)$ (Appendix~\ref{app:cosmology_resolution_flow}) & baseline uses microstate-pushforward $\mu_m$ when stated & Occupancy-to-energy mapping is an explicit interface assumption; any probabilistic statements are conditional on declared $K$. \\
Protocol RG operator readouts (kernel view; operator audits) & balanced chain $(m,n)=(2n,n)$; $K$ for readout aggregation & readout kernels are explicit when scalar summaries are reported & Operator kernels (transport/uplift) are structural; readout kernels $K$ are weighting rules for scalar summaries (Appendix~\ref{app:protocol_rg_operator_closure}). \\
Global model selection across families (MDL registry; OP4) & not a physical state; a declared hypothesis-space registry & kernel-family scans counted as explicit footprint when applicable & Cross-family look-elsewhere closure inside an explicit registry; finite kernel-family scans treated as explicit search-space enlargement (Appendix~\ref{app:global_model_selection_mdl}). \\
\bottomrule
\end{tabular}
\end{center}