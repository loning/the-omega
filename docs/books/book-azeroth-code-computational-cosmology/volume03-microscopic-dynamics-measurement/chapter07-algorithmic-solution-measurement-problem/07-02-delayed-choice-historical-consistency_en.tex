\section{Loot Roll (Delayed Choice and Historical Consistency)}

\textit{(Loot Roll - Delayed Choice and Historical Consistency)}

\begin{quote}
\textbf{``History is not a read-only file written on hard drive, but a log dynamically generated based on your current query request. The past didn't determine the present; it's when you check the loot table that the system retroactively generates the BOSS's drop history.''}
\end{quote}

In section 7.1, we said measurement is instantiation. This immediately raises a logical question: If you only now decide whether this cat is dead or alive, was it dead or alive in the past hour? Did we change the past?

This section will use \textbf{delayed choice experiments} to demonstrate \textbf{Code of Azeroth}'s most insane historical view: \textbf{History is generated based on queries}.
We will prove that the past is not objectively real, but a \textbf{log} made up on the spot by the system to match current plot.

\subsection{The Illusion of Established History}

Intuition tells us:
\begin{enumerate}
\item The past has happened and is unique.
\item Whether we look or not, the past won't change.
\end{enumerate}

But \textbf{Wheeler's delayed choice experiment} shatters this illusion.
Imagine a photon flying from billions of light-years away. It faces two choices: go left, or go right.
\begin{itemize}
\item If on Earth we decide to measure its path (particle nature), we force it to take a certain path billions of years ago.
\item If on Earth we decide to measure its interference (wave nature), we force it to take both paths simultaneously billions of years ago.
\end{itemize}

\textbf{Key point}: Our decision on Earth (now) determines the photon's behavior billions of years ago (past).

\textbf{Computational explanation}:
If the system stored every second of the photon's trajectory for billions of years, that wastes too much hard drive space.
In our model, the system \textbf{never stored} intermediate process trajectories.
\begin{itemize}
\item \textbf{Intermediate state}: Photon propagates as \textbf{class} form, doesn't occupy coordinate memory.
\item \textbf{Final state}: Only when photon hits your detector on Earth does the system execute instantiation.
\end{itemize}

\subsection{Dynamic Log Generation Algorithm}

In computer science, this is called \textbf{lazy logging}.

\textbf{Definition 7.2.1 (Dynamic History)}

Physical object's historical trajectory $H(t)$ is not a static array, but a \textbf{function}.
\[
H(t) = \text{GenerateHistory}(\text{Now}, \text{Seed})
\]

This is like \textbf{procedural generation} in games.
Have you played those infinite map games? When you look back at the road behind, the game engine generates terrain behind based on current coordinate seed. As long as the generated road connects with the road under your feet, you can't tell if it ``was always there'' or ``just generated.''

Wheeler used a \textbf{``dragon''} metaphor:
\begin{itemize}
\item \textbf{Dragon tail} (light source): Determined.
\item \textbf{Dragon head} (detector): Determined.
\item \textbf{Dragon body} (intermediate process): Is a cloud of \textbf{probability smoke}. The system never calculated how the dragon body flew; only when the dragon head bites does the system casually draw a line connecting them.
\end{itemize}

\subsection{Quantum Erasure: Database Rollback}

\textbf{Quantum erasure experiments} demonstrate the system's \textbf{editing permissions} on history.

We can first measure the photon's path, then \textbf{after} the photon reaches the screen, decide whether to \textbf{``erase''} this information.
\begin{itemize}
\item If retain information: No interference (particle).
\item If erase information: Interference restored (wave).
\end{itemize}

This seems like time reversal physically, but computationally, this is standard \textbf{database transaction}.

\begin{enumerate}
\item \textbf{Write-ahead log}: System records ``path marker'' in cache. At this point, history is in \textbf{Pending} state.
\item \textbf{Rollback}: If we execute erasure, equivalent to sending \texttt{ABORT} command. System deletes cache marker, photon reverts to superposition state, re-renders interference fringes.
\item \textbf{Commit}: If we read information, equivalent to sending \texttt{COMMIT} command. History is locked, forever written to read-only storage.
\end{enumerate}

\textbf{Theorem 7.2.1 (History Mutability Theorem)}

Before information diffuses to the entire server, history is just \textbf{dirty data} in memory, can be rewritten anytime.

\subsection{Why Can't We Resurrect Caesar?}

Since history is generated, why can't we use ``delayed choice'' to make Caesar not die?
Because there's \textbf{consistency checking}.

Generated history must satisfy boundary conditions: \textbf{Logic must be self-consistent}.

\begin{itemize}
\item \textbf{Macroscopic history (hard)}: Caesar's death has been \texttt{COMMIT}ed countless times by countless observers (people, air, photons). Its entanglement network locks the entire server. To rollback it requires entire server rollback, needs administrator privileges.
\item \textbf{Microscopic history (soft)}: Photons in labs only entangle with you. System can easily rewrite their history; no one knows anyway.
\end{itemize}

Therefore, \textbf{we have the divine power to change microscopic history, but are imprisoned by macroscopic history's inertia}.

\subsection{Summary: History Generated for Plot}

This section proved: \textbf{Causality is bidirectional at the computational level.}

\begin{itemize}
\item \textbf{Physical layer (forward)}: Past determines future.
\item \textbf{Computational layer (reverse)}: Current observation \textbf{filters} and \textbf{instantiates} past that matches conditions.
\end{itemize}

We don't live on a one-way street. We live on an \textbf{instant-calculated stage}. The script (history) is generated in real-time to match the actors' (us) current performance.

The reason the past seems determined is because the system, to maintain logical self-consistency, perfectly fills all plot holes.
