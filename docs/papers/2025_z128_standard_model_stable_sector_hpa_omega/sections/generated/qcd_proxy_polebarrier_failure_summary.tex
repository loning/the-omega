\paragraph{Audit summary (proxy $\leftrightarrow$ pole-barrier consistency loop).} \AuditTag We compute two bounded diagnostics on the same finite Wilson-loop magnitudes: (i) an area/perimeter proxy fit with selected $(\widehat\sigma,\widehat\mu)$, and (ii) a Pad\'e pole-barrier sweep counting interior-unit-disk denominator roots. We report a boolean gate summary and a conflict/consistency label without upgrading confinement or mass-gap status.