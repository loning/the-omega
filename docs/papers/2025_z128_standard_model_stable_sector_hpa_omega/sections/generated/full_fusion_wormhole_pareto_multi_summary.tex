\paragraph{Audit summary (wormhole multi-objective frontiers vs cost).} \AuditTag In addition to the scalar gain score, we report 1D cost frontiers for each observable delta: $\Delta\tau_{\mathrm{trap}}$, $\Delta\tau_{\mathrm{wh,trap}}$, $\Delta E_{\mathrm{emit}}$, and the pointer-jump exit rate. Frontier sizes: delay=1, delaywh=1, emit=3, jump=1.