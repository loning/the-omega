# This paper's reproducible scripts are designed to be auditable and deterministic.
# The core combinatorics can run on the Python standard library, while the
# extended pipeline (plots, robustness sampling) uses a small scientific stack.
#
# Tested with Python 3.11+.

numpy==2.1.4
scipy==1.14.1
matplotlib==3.9.3
