\section{Inference ledger: what is implied within the declared input set}
\label{app:inference_ledger}

This appendix answers a compact audit question that arises repeatedly in the HPA--$\Omega$ program:
if one does \emph{not} introduce any additional axioms or free continuous inputs beyond what is explicitly declared in the main text, what physically meaningful conclusions can be inferred, and what is their dependency status?
We summarize the answer as a layered ledger.
The purpose is not to add new claims, but to make explicit which statements are (i) theorem-level consequences of finite combinatorics, (ii) finite protocol constructions, (iii) conditional interface implications under stated dictionaries, and (iv) bounded-complexity closures with reported mismatch factors.
Appendix~\ref{app:tick_cap_derivation} provides the expanded tick\,+\,CAP derivation spine (candidate families, objectives, and tie-breaks); the present ledger is the compact status summary.

\paragraph{Notation for status tags.}
We use five audit-facing tags:
\begin{itemize}
\item \textbf{[Math]} theorem-level statement in the mathematical layer (finite definitions and proofs).
\item \textbf{[Prot]} finite protocol construction (explicit algorithm on a finite model; no continuum limit assumed).
\item \textbf{[Iface]} physical identification statement (a dictionary/closure declaration stated in protocol language).
\item \textbf{[CAP]} bounded-complexity closure (finite candidate family + deterministic tie-break rules).
\item \textbf{[Open]} explicitly recorded open problem (not closed within the declared input set).
\end{itemize}
Matching-layer dictionaries (units, scheme dependence, threshold conventions) are recorded explicitly as such in the main text; they are not treated as new premises here.

\subsection{Declared inputs (minimal list)}
The paper's declared protocol primitives are recorded in Table~\ref{tab:audit_facing_contract} and Figure~\ref{fig:inference_map}.
In the strict tick-only program pursued here, the only \emph{primitive} inputs are:
\begin{itemize}
\item \textbf{Tick} (scan iteration count) as the time variable (Axiom~\ref{ax:readout_sequentiality}; Section~\ref{sec:tick_calculus}).
\item \textbf{CAP} as the universal selection/closure rule within declared finite candidate families (Axiom~\ref{ax:cap}; Definition~\ref{def:bounded_complexity_closure}; Appendix~\ref{app:cap_audit_template}).
\end{itemize}
All other items that appear throughout the paper (golden-branch slope, addressing basis, anchor selection, orientation-class tie-break, phase-register choices, and coupling/CP normalizations) are treated either as theorem-level definitions in the finite model, or as CAP-closed interface components \emph{within explicitly declared finite candidate families with deterministic tie-break rules}; matching-layer reference conventions (PDG/CODATA, scheme/scale) are recorded explicitly and are not counted as premises.

\paragraph{Audit reading.}
Whenever the main text reports a ``closed'' numerical value for a physical quantity, the intended status is \textbf{[Iface]} (a dictionary/normalization choice) together with \textbf{[CAP]} (closure within a stated finite hypothesis class), and any residual deviation from PDG/CODATA conventions is interpreted as a matching-layer factor rather than as a theorem-level claim.

\subsection{External matching inputs inventory (audit; not premises)}
\label{subsec:external_inputs_inventory}

\MatchTag This paper cites standard references and (in a few places) uses compact external numerical targets or small vendored data subsets as \emph{matching-layer} inputs.
They are not used as theorem-level premises for the folding core.
Table~\ref{tab:external_inputs_inventory} records the external inputs, their role, and where they enter.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{p{0.22\textwidth} p{0.18\textwidth} p{0.52\textwidth}}
\toprule
External input & Role in this paper & Where it enters / what it is used for \\
\midrule
\cite{PDGRPP2024} (PDG RPP) &
\MatchTag reference conventions / comparison targets &
SM quantum numbers and benchmark masses/couplings for mismatch reporting (labeling targets; Table~\ref{tab:mass_spectrum}; Table~\ref{tab:quant_summary}; Appendix~\ref{app:closure_audit_details}). \\
\cite{CODATA2022RMP} (CODATA constants) &
\MatchTag reference conventions / comparison targets &
Fundamental constants (e.g.\ $\alpha_{\mathrm{em}}$, $m_e$) for mismatch reporting and unit conventions in the quantitative summary (Table~\ref{tab:quant_summary} and Appendix~\ref{app:closure_audit_details}). \\
\cite{Esteban2020NuFIT} (NuFIT) &
\MatchTag representative oscillation targets &
Representative PMNS mixing targets used only for bounded-scan fit diagnostics (Section~\ref{sec:pmns_neutrino_closure} and Appendix~\ref{app:holonomy_sweeps_extended}). \\
\cite{Planck2018Parameters} (Planck-2018) &
\MatchTag cosmology comparison target &
Baryon fraction $\Omega_{\mathrm{b},0}$ used as a compact target in the discrete energy-budget matching hypothesis (Appendix~\ref{app:cosmology_resolution_flow}); also used as a reference in the bounded proxy maps of the $\gamma$ cross-observation audit (Appendix~\ref{app:gamma_crossobs_consistency}). \\
\cite{LelliMcGaughSchombert2016SPARC} (SPARC) &
\MatchTag vendored data subset for an audit pipeline &
A tiny vendored subset of SPARC rotation-curve mass-model files under \texttt{data/gamma\_crossobs/sparc/} used to exercise the deterministic $\chi$-reconstruction $\to$ WLS fit pipeline for $\gamma_{\mathrm{dict}}$ (Appendix~\ref{app:gamma_crossobs_consistency}). \\
\cite{Wong2019H0LiCOWXIII} (H0LiCOW XIII) &
\MatchTag compact proxy channel &
Published time-delay $H_0$ inference used only as a compact proxy channel for the $\gamma$ cross-observation audit (Appendix~\ref{app:gamma_crossobs_consistency}). \\
\cite{BertottiIessTortora2003,FomalontKopeikin2009,Delva2018GalileoRedshift} &
\MatchTag compact solar-system proxy channels &
Representative solar-system summary constraints (Shapiro/deflection/redshift) used only as compact proxy channels for the $\gamma$ cross-observation audit, with explicit proxy maps and assumption footprints (Appendix~\ref{app:gamma_crossobs_consistency}). \\
\bottomrule
\end{tabular}
\caption{External matching inputs inventory. All items listed here are used as matching-layer targets, reference conventions, or small audit inputs; they are not treated as theorem-level premises for the folding core.}
\label{tab:external_inputs_inventory}
\end{table}

\subsection{Mathematical-layer consequences (no physical identification needed)}
\paragraph{Stable-sector grammar and counts.}
\begin{itemize}
\item \textbf{[Math]} \textbf{Fibonacci admissible counts.}
For every $m\ge 1$, the $\varphi$-admissible set $X_m\subset\Omega_m$ (forbidden substring ``11'') satisfies $|X_m|=F_{m+2}$ (Lemma~\ref{lem:xm_fib}).
In particular, $|X_6|=21$.
\item \textbf{[Math]} \textbf{Canonical $\pi$-split.}
For $m\ge 4$, the wrap-around predicate $D_\pi$ induces a canonical cyclic/boundary split
$X_m=X_m^{\mathrm{cyc}}\sqcup X_m^{\mathrm{bdry}}$ with
$|X_m^{\mathrm{bdry}}|=F_{m-2}$ and $|X_m^{\mathrm{cyc}}|=F_{m+2}-F_{m-2}$
(Proposition~\ref{prop:cyc_bdry_size}).
At $m=6$ this is $21=18\oplus 3$ with explicit boundary words (Corollary~\ref{prop:cyc_bdry_6}).
\item \textbf{[Math]} \textbf{Fold surjectivity.}
The truncation folding map $\mathrm{Fold}_m:\{0,\dots,2^m-1\}\twoheadrightarrow X_m$ is surjective for every $m\ge 1$ (Proposition~\ref{prop:foldm_surjective}), and in particular $\mathrm{Fold}_6:\{0,\dots,63\}\twoheadrightarrow X_6$ (Lemma~\ref{lem:fold6_surjective}).
\item \textbf{[Math]} \textbf{Canonical integer labeling of $X_6$.}
The Zeckendorf-value map $V:X_6\to\{0,\dots,20\}$ is a bijection (Proposition~\ref{prop:V_bijection}).
\end{itemize}

\paragraph{Hilbert-path chirality sign law.}
\begin{itemize}
\item \textbf{[Math]} \textbf{Parity and traversal reversal flip $\chi$.}
For the discrete Hilbert chirality index $\chi$ defined in \eqref{eq:hilbert_chi_def}, any reflection and traversal reversal flip its sign, while orientation-preserving rigid motions preserve it (Proposition~\ref{prop:chi_flip}).
\end{itemize}

\subsection{Finite protocol constructions (computable, no continuum limit assumed)}
\paragraph{Discrete connection/holonomy skeleton at \texorpdfstring{$(m,n)=(6,3)$}{(m,n)=(6,3)}.}
\begin{itemize}
\item \textbf{[Prot]} \textbf{A deterministic $S_4$ edge connection.}
At the $m=6$ anchor, stable-type fibers satisfy $|P(w)|\in\{2,3,4\}$.
Padding to $4$ slots and minimizing Hamming-cost matchings yields a deterministic edge transport $p_{a\to b}\in S_4$ (Lemma~\ref{lem:edge_transport_welldefined}).
\item \textbf{[Prot]} \textbf{Gauge-invariant plaquette signatures.}
Under local relabelings of fiber slots, plaquette holonomy transforms by conjugation, so its $S_4$ cycle type is invariant (Proposition~\ref{prop:cycle_type_gauge_invariant}).
\item \textbf{[Prot]} \textbf{Dyadic phase-lift and a CP-odd invariant.}
Given a bounded low-complexity phase map family $\tau$ and dyadic denominator $\mathrm{denom}=2^p$, the phase-lifted edge transport produces a finite holonomy model with an induced Jarlskog-type invariant $J$ (Section~\ref{subsec:holonomy_phase_lift_cp}).
The role of $\ZZ_{128}$ is explicit at $p=7$ (Remark~\ref{rem:dyadic_phase_register}).
\end{itemize}

\subsection{Interface-level implications (conditional on stated dictionaries)}
\paragraph{Rigid-frame coarse-lock anchor and the vacuum sector.}
\begin{itemize}
\item \textbf{[Iface]} \textbf{Localization admissibility threshold.}
Under the rigid-frame display anchor (Section~\ref{subsec:6dof_lock}), window lengths $m<6$ are sub-admissible for \emph{single-window} coarse rigid-frame display and are treated as protocol-rejected as localized matter within this interface dictionary (Section~\ref{subsubsec:geometric_vacuum}; see also Remark~\ref{rem:6dof_lock_scope} for scope).
\item \textbf{[Iface]} \textbf{Ghost-sector vacuum microstructure at fixed $m$.}
Even at fixed $m$, microstates outside the admissible grammar are protocol-unstable.
At $m=6$, the ghost-sector size is $|\Omega_6\setminus X_6|=64-21=43$ (Section~\ref{app:ghost_sector}).
\end{itemize}

\paragraph{Gauge fields as compensating connections.}
\begin{itemize}
\item \textbf{[Iface]} \textbf{Compensation is forced by finite fibers.}
When stable labels have nontrivial fibers, cross-site comparison requires an additional transport rule; local relabelings imply a gauge redundancy (Proposition~\ref{prop:gauge_compensation}).
\item \textbf{[Iface]+[CAP]} \textbf{Three-factor closure and the SM gauge triple.}
If compensation decomposes into three commuting classes and gauge factors are modeled as compact unitary groups, then CAP-minimal selection within the stated factorization family yields $U(1)\times SU(2)\times SU(3)$ up to finite quotients (Proposition~\ref{prop:channel_to_gauge}).
\end{itemize}

\paragraph{Chirality, antimatter, and CP-sign anchoring.}
\begin{itemize}
\item \textbf{[Iface]} \textbf{Parity as protocol change.}
Under the canonical orientation-bit convention, the only physically distinguishable discrete choice in the $D_4$ layout family is the orientation class detected by $\mathrm{sgn}(\chi)$ (Definition~\ref{def:scl}, Proposition~\ref{prop:orientation_class_minimal}).
\item \textbf{[Iface]} \textbf{Conjugation-as-reversal and antimatter dual.}
Phase conjugation of the scan orbit corresponds to scan reversal up to an initial-phase flip (Lemma~\ref{lem:conjugation_reversal} and Lemma~\ref{lem:conjugation_readout_reversal}), yielding a protocol antimatter dual by word reversal (Definition~\ref{def:word_reversal_dual}).
\item \textbf{[Iface]} \textbf{Chirality-anchored CP sign.}
Within a fixed protocol class and a fixed PDG parameterization, CP-odd sign conventions are anchored by $\mathrm{sgn}(\chi)$ (Definition~\ref{def:chi_cp_sign}).
\end{itemize}

\paragraph{Closed labeling and minimal chiral content.}
\begin{itemize}
\item \textbf{[Iface]+[CAP]} \textbf{Closed $21$-type labeling at the anchor.}
Once one commits to the split semantics (cyclic $\leftrightarrow$ matter multiplets, boundary $\leftrightarrow$ gauge-factor classes) and to the stated deterministic ordering rules, the labeling map $\mathcal{L}_{\mathrm{SM}}$ is uniquely fixed (Theorem~\ref{thm:labeling_unique}).
\item \textbf{[Iface]} \textbf{Anomaly-neutral closure forces a sterile $\nu_R$.}
Closing the cyclic count to $18$ with the smallest anomaly-neutral extension selects a sterile singlet $\nu_R$ with $Y=0$; anomaly cancellation is unchanged by adding $\nu_R$ (Proposition~\ref{prop:anomaly_nur}).
\end{itemize}

\subsection{Quantitative rigidity targets and closures (within stated finite families)}
The following are recorded in the main text as explicit low-complexity targets or bounded-complexity selections, together with mismatch factors and audit context (Appendix~\ref{app:generated_tables}):
\begin{itemize}
\item \textbf{[Iface]+[CAP]} \textbf{Electromagnetic impedance target.}
$\alpha_{\mathrm{em}}^{-1}{}_{\mathrm{geo}}=4\pi^3+\pi^2+\pi$ under the declared primitive phase-space family and serial aggregation dictionary (Theorem~\ref{thm:alpha_three_channel}).
\item \textbf{[Iface]+[CAP]} \textbf{Electroweak volume target.}
$\alpha^{-1}(\mu_Z)=13\pi^2$ and $\sin^2\theta_W(\mu_Z)=3/13$ under the weighted-volume dictionary fixed by discrete invariants (Theorem~\ref{thm:weinberg_angle}).
\item \textbf{[Iface]+[CAP]} \textbf{CP volume/multiplicity target.}
$J_{\mathrm{geo}}=1/(11\pi^7)$ under the declared CP-odd phase-space and multiplicity dictionary (equation \eqref{eq:j_geo} and Proposition~\ref{prop:jarlskog_rigidity_stmt}).
\item \textbf{[CAP]} \textbf{Discrete mixing closures.}
CKM magnitudes (Proposition~\ref{prop:ckm_mixing_rigidity}) and PMNS mixing sines plus bounded-denominator $\delta$ closure (Proposition~\ref{prop:pmns_mixing_rigidity} and Table~\ref{tab:pmns_delta_sweep}).
\item \textbf{[CAP]} \textbf{Mass-depth rigidity.}
Within the bounded integer ansatz, the coefficients $(2,5,1)$ are rigidly selected (Proposition~\ref{prop:rhat_rigidity}).
\item \textbf{[CAP]} \textbf{Resolution staircase calibration.}
Within the bounded family $r_{\mathrm{step}}=k\pi$ ($1\le k\le 10$), the calibration $r_{\mathrm{step}}=2\pi$ is uniquely selected under the stated objectives against fixed reference anchors (the $Z$ scale, and optionally a conservative QCD-scale anchor), which enter only as matching-layer comparison inputs (Proposition~\ref{prop:r_step_2pi}).
\item \textbf{[CAP]} \textbf{Scalar-sector uplift marker.}
The Higgs--$Z$ depth offset closure $\Delta r_{HZ}=2/3$ is selected within the stated bounded rational family (Proposition~\ref{prop:higgs_z_offset_rigidity}), consistent with treating scalar behavior as uplift/coarse-graining dependent rather than as a new $m=6$ stable type (Proposition~\ref{prop:scalar_sector_closed}).
\end{itemize}

\subsection{Continuum representatives: action, field equations, and thermodynamics (frequency-first)}
\label{subsec:ledger_continuum_representatives}

\paragraph{Equivalence semantics and the frequency-first dictionary.}
\begin{itemize}
\item \textbf{[Iface]} \textbf{Physical objects as equivalence classes.}
Section~\ref{app:equivalence_semantics} formalizes the semantic quotients already used throughout the paper (tick-origin shifts, projection-fiber equivalence, local relabelings/gauge, coarse-graining preorder, and action equivalence).
\item \textbf{[Iface]} \textbf{Frequency as a primary derived quantity.}
Frequency in tick units is defined by phase advance $\omega=\Delta\theta/\Delta t$ (Definition~\ref{def:frequency_from_phase}) and tied to mass/energy/temperature dictionaries (Section~\ref{app:equivalence_semantics} and Appendix~\ref{app:time_mass_delay}).
\end{itemize}

\paragraph{CAP-closed continuum action skeleton.}
\begin{itemize}
\item \textbf{[Iface]+[CAP]} \textbf{CAP-minimal covariant action representative.}
Section~\ref{app:cap_continuum_action_closure} records a finite candidate family of local covariant term types and a bounded rational coefficient box, and states a CAP-minimal action skeleton $S_{\mathrm{eff}}$ (Proposition~\ref{prop:cap_minimal_action_skeleton}).
\end{itemize}

\paragraph{Field equations from variation.}
\begin{itemize}
\item \textbf{[Math]} \textbf{Einstein--Yang--Mills equations (given the CAP-selected representative).}
Section~\ref{app:variational_field_equations} records the Euler--Lagrange equations obtained by varying the representative action: Einstein equation with total stress (Theorem~\ref{thm:einstein_equation_total_stress}), Yang--Mills equations (Proposition~\ref{prop:ym_equation}), and the $\chi$-sector amplitude equation (Proposition~\ref{prop:chi_eom}).
\end{itemize}

\paragraph{Thermodynamics and entropic force.}
\begin{itemize}
\item \textbf{[Iface]+[CAP]} \textbf{Entropy as state counting and CAP as free-energy closure.}
Appendix~\ref{app:thermodynamics_from_equivalence} closes entropy/temperature in coarse-graining language and records a CAP free-energy selection template on finite candidate families (Proposition~\ref{prop:cap_free_energy_closure}), together with the entropic-force response definition.
\end{itemize}

\paragraph{Overhead gravity and the \texorpdfstring{$\chi$}{chi} reconstruction protocol.}
\begin{itemize}
\item \textbf{[Iface]} \textbf{Overhead-to-gravity closure (weak field).}
Section~\ref{app:overhead_to_gravity_closure} records the dictionary $\chi\mapsto N\mapsto g_{00}\mapsto \Phi$ and the resulting weak-field source template $\rho_{\mathrm{eff}}\propto -\Delta\chi$, together with a one-parameter fit for $\gamma$.
\item \textbf{[Prot]} \textbf{Executable $\chi(x)$ reconstruction protocol.}
Section~\ref{app:chi_reconstruction_protocol} records a Hilbert-binning $\to$ window-word $\to$ folding-statistics pipeline that reconstructs $\chi(x)$ from data or simulations.
\end{itemize}

\paragraph{Quantum readout and Born probabilities.}
\begin{itemize}
\item \textbf{[Iface]} \textbf{POVM/instrument interface.}
Appendix~\ref{app:quantum_measurement_born} records POVM readout and instrument update rules as the minimal quantum interface for finite observers.
\item \textbf{[Iface]} \textbf{Born-probability rigidity.}
Appendix~\ref{app:quantum_measurement_born} records two closure routes: a counting template aligned with projection-induced degeneracies and the Gleason--Busch uniqueness theorem.
\end{itemize}

\paragraph{Running couplings and cosmology as resolution flow.}
\begin{itemize}
\item \textbf{[Iface]} \textbf{RG dictionary in the $r$ coordinate (self-contained).}
Appendix~\ref{app:running_couplings_resolution_flow} records the $r$-flow form of RG equations and the semantics of threshold matching as discrete uplifts.
\item \textbf{[Iface]} \textbf{Cosmology as resolution initialization/flow.}
Appendix~\ref{app:cosmology_resolution_flow} records the capacity-growth (inflation-like) mechanism from $|X_m|\sim \varphi^m$ and a discrete background energy-budget matching hypothesis.
\end{itemize}

\paragraph{Connection to falsifiability.}
The falsifiability statements P1--P6 in Section~\ref{sec:falsifiability} are phrased entirely in this protocol language:
they depend on the declared interface items above and on audited finite constructions, and they do not add new theorem-level premises beyond the stated input set.
In particular, the staircase-threshold locations in P3 are conditional on the stated $r_{\mathrm{step}}$ calibration and its reference anchors, while P1/P2/P4/P5/P6 do not require that numerical calibration to be stated.

\subsection{Ledger-tagged items beyond the theorem-level core (audit)}
\label{subsec:ledger_open_problems}
The items below are \emph{not} theorem-level consequences of the finite folding core.
Items tagged \textbf{[Open]} remain not closed within the declared input set; items tagged \textbf{[Iface]} and/or \textbf{[CAP]} are closed as interface representatives (and are not used as premises in the folding proofs).
For discussion and pointers, see Section~\ref{subsec:open_problems_audit_tagged}.
\begin{itemize}
  \item \textbf{[Open]} \textbf{Gauge-group candidate family / three-factor dictionary from protocol primitives.}
  The paper closes $U(1)\times SU(2)\times SU(3)$ only conditionally under explicit compactness/factorization assumptions and a declared finite candidate family (Proposition~\ref{prop:channel_to_gauge}).
  What remains open is deriving the candidate family and the three-factor interface dictionary itself from deeper microscopic scan/readout architecture without hand-declaring the admissible family.
  Within the stated family, the minimizer is robust under several natural label choices (Appendix~\ref{app:gauge_complexity_sensitivity} and Proposition~\ref{prop:gauge_label_robustness}).
  \AuditTag Minimal failure point within the declared input set: the present paper records the assumption bundle (G1--G4) explicitly (Section~\ref{subsec:gauge_as_compensation}) and uses it only at the interface layer.
  Deriving (rather than declaring) the admissible candidate family and the channel$\to$redundancy identification from tick-only primitives would require an additional theorem-level microscopic architecture statement beyond the current paper's input set (hence it is tagged \textbf{[Open]} rather than \textbf{[Iface]}).

  \item \textbf{[Iface]} \textbf{OP1 discharged relative to an explicit extended input set (optional routes).}
  \AuditTag If one adopts either the physics-consensus interface inputs of Appendix~\ref{app:physics_consensus_inputs} (in particular Assumption~\ref{ass:consensus_three_factor_gauge}) or the optional microscopic internal-fiber input of Appendix~\ref{app:internal_fiber_g2_optional} (Assumption~\ref{ass:m2star_internal_fiber_g2}), then the ``candidate family'' component is supplied as a declared external/microscopic input rather than a hand-declared abstract hypothesis class.
  Relative to that extended input set, the remaining three-factor identification reduces to the CAP-minimal closure recorded in Proposition~\ref{prop:channel_to_gauge}.
  No equivalence between the two optional routes is asserted; they are alternative interface-level ways to underwrite the same candidate-family stance.

  \item \textbf{[Iface]} \textbf{Uniqueness/inevitability of the folding map (protocol-local closure).}
  Alternative deterministic dyadic$\to X_m$ maps exist at fixed window length and can change fiber statistics (Appendix~\ref{app:fold_family_sensitivity}).
  Appendix~\ref{app:fold_family_uniqueness} records a minimal protocol-local/implementable contract (value consistency together with no-backreaction refinement under dyadic uplift) and proves that this contract uniquely forces the Zeckendorf-truncation family $\mathrm{Fold}_m$ (Theorem~\ref{thm:fold_family_uniqueness}).
  In this sense, the folding-map family introduces no residual choice once one commits to the stated locality/implementability constraints.

  \item \textbf{[Iface]+[CAP]} \textbf{Continuum Yang--Mills/EFT representative from finite holonomy.}
  Appendix~\ref{app:continuum_yang_mills_from_holonomy} records the closure chain from finite holonomy diagnostics (Section~\ref{sec:protocol_connections_holonomy} and Appendix~\ref{app:holonomy_sweeps_extended}) to a CAP-minimal local continuum representative whose gauge sector is governed by $\Tr(F_{\mu\nu}F^{\mu\nu})$ (Section~\ref{app:cap_continuum_action_closure}) and whose Euler--Lagrange equations include the Yang--Mills equations (Section~\ref{app:variational_field_equations}).

  \item \textbf{[CAP]} \textbf{Global model selection / look-elsewhere across families (MDL within a declared registry).}
  Appendix~\ref{app:global_model_selection_mdl} records a prefix-code/MDL audit closure on a declared family registry that includes the paper's mainline candidate families and the already-audited counterfactual/null baselines (Appendix~\ref{app:generated_tables}).
  Within this registry, a deterministic MDL prior is assigned to families and a global mixture bound is computed for fixed mismatch thresholds (Table~\ref{tab:audit_global_mdl_family_registry}).

  \item \textbf{[Iface]} \textbf{RG as a matching dictionary for EFT parameters.}
  Appendix~\ref{app:running_couplings_resolution_flow} records the $r$-coordinate chain-rule dictionary and standard one-loop templates, together with a compact reference pointer for SM $\beta$-functions used only at the matching layer.

  \item \textbf{[Iface]+[CAP]} \textbf{Scalar/Yukawa sector and RG-running closure (protocol-derived).}
  The physical Yukawa content (eigenvalue spectrum and mixing matrices) and one-loop $\beta$-function coefficients are closed as protocol outputs (Appendix~\ref{app:yukawa_beta_protocol_closure}):
  (i) Yukawa eigenvalues follow from the depth template: $y_f/y_e=\varphi^{\widehat r(f)}$ (Proposition~\ref{prop:yukawa_eigenvalue_closure});
  (ii) mixing matrices (CKM/PMNS) are closed via holonomy/phase-lift mechanisms;
  (iii) $\beta$-coefficients $(b_1,b_2,b_3)=(41/6,-19/6,-7)$ follow from representation counting on the closed labeling (Proposition~\ref{prop:beta_from_labeling}).
  \AuditTag The only non-derived inputs are: (a) the VEV $v$ (or equivalently $y_e$) as a matching-layer calibration, and (b) the minimal Higgs count $N_H=1$ as an uplift interface assumption.
  Right-handed rotation matrices are not fixed because they are not physical observables in SM gauge interactions (Remark~\ref{rem:right_rotation_redundancy}).
\end{itemize}

