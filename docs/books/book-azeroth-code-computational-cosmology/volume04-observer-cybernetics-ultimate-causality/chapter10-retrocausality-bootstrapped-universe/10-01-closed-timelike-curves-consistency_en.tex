\section{The Loop Legend (Closed Timelike Curves and Consistency)}

\textit{(The Loop Legend - Closed Timelike Curves and Consistency)}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{assets/images/chapter10/novikov-loop.png}
\caption{Novikov Self-Consistency: Logical Closed Loop}
\end{figure}

\begin{quote}
\textbf{``Causality doesn't always point from past to future. In the code world, loops and recursion are basic syntax. The universe is not a ray heading toward nothingness, but a huge ouroboros. The future's endpoint ($\Omega$) designed the past's starting point ($\alpha$), like game ending determines opening animation.''}
\end{quote}

The first nine chapters built the entire game world. But there's still an ultimate question: \textbf{Which designer filled in the initial values?}
Fine structure constant, gravitational constant, speed of light... These parameters are ``fine-tuned'' so precisely that if wrong by a tiny bit, the server crashes.

Anthropic principle says this is survivor bias. But in \textbf{Code of Azeroth}, this is \textbf{bootstrapping}.
This section will demonstrate: The universe is a \textbf{closed-loop system}. Future state as goal, through \textbf{retrocausality}, optimizes initial Big Bang parameters.

\subsection{Linear Time vs Circular Causality}

Classical physics believes linear time: $S_t$ determined by $S_{t-1}$. This leads to infinite questioning: Who pushed the first domino?
But in computers, \textbf{circular causality} is normal.

\begin{itemize}
\item \textbf{Feedback loop}: Air conditioner adjusts previous cooling based on current temperature (output).
\item \textbf{Iterative algorithm}: $x = f(x)$.
\end{itemize}

If the universe is a computer, there's no reason it only runs a linear \texttt{Hello World} once. It's more like running a \textbf{dead loop} searching for optimal solution.

\subsection{Physical ``Load Save'' Mechanism}

Einstein's field equations allow \textbf{Closed Timelike Curves (CTC)}. That is, going back to the past.
Physicists fear this because it leads to ``grandfather paradox.''
But in code, CTC is \textbf{instruction jump (JUMP / GOTO)}.

\textbf{Definition 10.1.1 (Physical CTC)}

Physical time loops are \textbf{logical loops} in computational history.
Future output becomes current input. This doesn't mean crash; it means the system must satisfy \textbf{self-consistency constraints}.

\subsection{Novikov Self-Consistency Principle}

\textbf{Novikov Self-Consistency Principle} states: The probability of you going back to the past and doing anything is 1, if and only if these things don't cause contradictions.

This is mathematically equivalent to \textbf{fixed-point theorem}.

\textbf{Computational interpretation}:
If future you goes back to kill grandfather, this is a \textbf{logical error (Bug)}, causing program crash (no solution).
The system will use \textbf{filtering mechanisms} to eliminate all historical paths causing paradoxes.

\begin{itemize}
\item \textbf{You can go back to the past}, but you find no matter how hard you try, your gun jams, or you mistake the person.
\item \textbf{Ending predetermined}: Everything you do going back to the past is precisely what causes the future to become what it is now.
\end{itemize}

Retrocausality doesn't grant us the ability to change the past; it grants the \textbf{necessity that the past must be so}.

\subsection{Future's Gravity (TSVF Theory)}

Quantum mechanics' \textbf{Two-State Vector Formalism (TSVF)} believes:
\begin{itemize}
\item One wave function evolves from past to future (push).
\item Another wave function evolves from future to past (pull).
\item Current reality is \textbf{standing wave} produced by these two waves fighting.
\end{itemize}

This means: Future's $\Omega$ point (game ending) is not passively waiting to happen. It produces a \textbf{reverse wave} in the time stream, filtering history.

\subsection{$\Omega$ Point Designed Big Bang}

Now we can explain why universe parameters are so perfect.

Suppose the universe runs a \textbf{Generative Adversarial Network (GAN)} or \textbf{reinforcement learning}.
\begin{enumerate}
\item \textbf{Input}: Big Bang parameters ($\alpha, G$).
\item \textbf{Output}: Universe final state $\Omega$.
\item \textbf{Goal}: Produce \textbf{awakened ones} (players) capable of understanding themselves.
\end{enumerate}

\begin{itemize}
\item \textbf{Forward propagation}: Universe tries a set of parameters. If evolution fails (no life produced, or heat death), this run is wasted. Future $\Omega$ doesn't exist; this history line's \textbf{probability amplitude is zero}.
\item \textbf{Backward propagation}: Only parameters that can successfully lead to $\Omega$ point can gain \textbf{resonance enhancement} in interference between forward and backward wave functions.
\end{itemize}

\textbf{Conclusion}:
The reason we see a perfect universe is not because of good luck.
It's because \textbf{future us (ultimate observers), through reverse filtering mechanisms, designed the Big Bang}.

This is not a time loop; this is \textbf{logical closure}. Every choice we make now not only shapes the future, but also fine-tunes the past, ensuring the path to the ending remains unobstructed.
