\begin{conclusion}[Multiplications on the elliptic curve of the weight spectrum—Latt\`es self-mapping:\texorpdfstring{$x([2]P)$}{x([2]P)} Closed form quartic rational function]\label{con:xi-terminal-zm-elliptic-lattes-doubling}\label{con:xi-terminal-zm-lattes-4division-certificate}
in conclusion \ref{con:xi-terminal-zm-elliptic-normalization} ellipse model
\[
E:\ Y^2=X^3-X+\frac14
\]
Above, remember the multiplication mapping \([2]:E\to E\), and order \(x:E\to\mathbb P^1\) for \(x\)-coordinate function \(x(P)=X\)。
but \(x\circ[2]\) in affine coordinates \(X\) induces a quadruple Latt\`es self-mapping \(\Phi\in\QQ(X)\)：
\[
\boxed{\;
\Phi(X):=x([2]P)=\frac{X^4+2X^2-2X+1}{4X^3-4X+1}\; }.
\]
its denominator
\(
4X^3-4X+1
=4\bigl(X^3-X+\tfrac14\bigr)
=4Y^2
\)
Equivalent to \(E\) of \(2\)-division condition \(Y=0\) (thus exactly \(x\)-coordinate \(2\)-division polynomial).
also,\(\Phi\)’s critical point can be given by \(4\)-division data is fully characterized: note \(E[2]\)、\(E[4]\) are respectively \(2\)-tickling with \(4\)-torsion subgroup, then for affine coordinates \(X\) are strictly equivalent to
\[
\boxed{\;
\Phi'(X)=0
\ \Longleftrightarrow\
\exists\,Q\in E[4]\setminus E[2]\ \text{make}\ X=x(Q)
\ \Longleftrightarrow\
2X^{6}-10X^{4}+10X^{3}-10X^{2}+2X+1=0\; }.
\]
Thus the sixth degree polynomial gives \(x\)-on the coordinate diagram \(4\)-division certificate (equivalent to classic \(4\)-division polynomial \(\psi_4\) remove factors \(2Y\) after \(X\)-part), times Latt\`es The critical structure of dynamics is at \(\QQ[X]\) level is fully auditable.
\end{conclusion}
\begin{proof}[Points of proof]
For short Weierstrass curves \(Y^2=X^3+aX+b\) (here \(a=-1,b=1/4\)), the doubling point formula gives
\[
x([2]P)=\left(\frac{3X^2+a}{2Y}\right)^{\!2}-2X.
\]
Substitute and use \(4Y^2=4X^3-4X+1\) Simplified, we get the following \(\Phi(X)\)。
right \(\Phi=N/D\)（\(N=X^4+2X^2-2X+1\)、\(D=4X^3-4X+1\)) Directly derive the derivative and clear the denominator,
\(
\Phi'(X)=0\iff N'D-ND'=0
\), simplify to obtain the sixth degree polynomial shown.
On the other hand, for a general short Weierstrass curve \(Y^2=X^3+aX+b\), the four-division polynomial satisfies
\[
\psi_4=4Y\bigl(X^6+5aX^4+20bX^3-5a^2X^2-4abX-8b^2-a^3\bigr).
\]
Substitute \((a,b)=(-1,\tfrac14)\) have to
\[
\psi_4(X)=2Y\bigl(2X^{6}-10X^{4}+10X^{3}-10X^{2}+2X+1\bigr),
\]
Therefore, the sixth-order factor describes \(x\bigl(E[4]\setminus E[2]\bigr)\), with Latt\`es critical set consensus.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_lattes\_rational\_points\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Latt\`es self-mapped post-critical portrait: critical value is \texorpdfstring{$x(E[2]\setminus\{O\})$}{x(E[2]-{O})}, and after one step collapses to \texorpdfstring{$\infty$}{infty}]\label{con:xi-terminal-zm-elliptic-lattes-pcf-portrait}
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-lattes-doubling} mark.
but:
\begin{enumerate}
  \item \(\Phi\) The finite set of critical points is
  \[
  \mathrm{Crit}(\Phi)=x\bigl(E[4]\setminus E[2]\bigr).
  \]
  \item \(\Phi\) is strictly equal to
  \[
  \Phi\bigl(\mathrm{Crit}(\Phi)\bigr)=x\bigl(E[2]\setminus\{O\}\bigr),
  \]
  in \(x\bigl(E[2]\setminus\{O\}\bigr)\) is exactly a cubic polynomial \(4X^{3}-4X+1\) of three.
  \item and
  \[
  \Phi\bigl(x(E[2]\setminus\{O\})\bigr)=\{\infty\},
  \qquad
  \Phi(\infty)=\infty.
  \]
\end{enumerate}
therefore \(\Phi\) is a strictly postcritical finite (PCF) map, and its postcritical portrait is
\[
\mathrm{Crit}(\Phi)\ \xrightarrow{\ \Phi\ }\ x(E[2]\setminus\{O\})\ \xrightarrow{\ \Phi\ }\ \{\infty\}\ \xrightarrow{\ \Phi\ }\ \{\infty\},
\]
And the total posterior critical set is \(\{\infty\}\cup x\bigl(E[2]\setminus\{O\}\bigr)\)。
\end{conclusion}
\begin{proof}[Points of proof]
The first point is the conclusion \ref{con:xi-terminal-zm-elliptic-lattes-doubling} Certified.
to any \(Q\in E[4]\setminus E[2]\),have \([2]Q\in E[2]\setminus\{O\}\),thereby
\(
\Phi(x(Q))=x([2]Q)\in x(E[2]\setminus\{O\})
\), get the second point.
The third point consists of \(\Phi(X)=N(X)/D(X)\) and \(D(X)=4X^3-4X+1=4Y^2\) launched: when \(X\in x(E[2]\setminus\{O\})\) corresponds to \(Y=0\), the denominator is zero and the numerator is non-zero, so \(\Phi(X)=\infty\)。
Finally, by \(\deg N=4\)、\(\deg D=3\) have to \(X\to\infty\) hour \(\Phi(X)\sim \frac14X\),thereby \(\Phi(\infty)=\infty\). Certification completed.
\end{proof}

\begin{conclusion}[Latt\`es Integer decomposition of periodic point equation:\texorpdfstring{$\Phi^n(X)=X$}{Phi^n(X)=X} The molecule consists of \texorpdfstring{$(2^n\pm1)$}{(2^n±1)}-division product gives]\label{con:xi-terminal-zm-elliptic-lattes-periodic-division-factorization}
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-lattes-doubling} The Latt\`es self-mapping
\[
\Phi(X)=\frac{X^4+2X^2-2X+1}{4X^3-4X+1}\in\QQ(X).
\]
for each integer \(n\ge 1\),Will \(\Phi^n(X)-X\) written as a reduced fraction
\[
\Phi^n(X)-X=\frac{A_n(X)}{B_n(X)},
\qquad
A_n(X),B_n(X)\in\ZZ[X],\ \gcd(A_n,B_n)=1.
\]
for any odd number \(m\ge 1\),definition \(x\)-coordinate \(m\)-division integer
\[
\Psi_m(X):=\prod_{Q\in E[m]\setminus\{O\}/\{\pm1\}}\bigl(X-x(Q)\bigr)\ \in\ \ZZ[X],
\]
Its zero point set is non-trivial in terms of multiplicity \(m\)-twisted \(x\)-coordinate image (degree is \((m^2-1)/2\)）。
then for all \(n\ge 1\) establish the strict decomposition law
\[
\boxed{\;
A_n(X)=\pm\,\Psi_{2^n-1}(X)\,\Psi_{2^n+1}(X)\; }.
\]
In particular:
\begin{enumerate}
  \item \(n=1\) (fixed point equation):
  \[
  \Phi(X)-X=-\frac{3X^4-6X^2+3X-1}{4X^3-4X+1},
  \qquad
  A_1(X)=-(3X^4-6X^2+3X-1).
  \]
  \item \(n=2\) (two-period point equation):
  \[
  A_2(X)=-(3X^4-6X^2+3X-1)\cdot Q_{12}(X),
  \]
  in
  \[
  \begin{aligned}
  Q_{12}(X)=\;&5X^{12}-62X^{10}+95X^{9}-105X^{8}-60X^{7}+285X^{6}-174X^{5}\\
  &-5X^{4}-5X^{3}+35X^{2}-15X+2.
  \end{aligned}
  \]
  and \(Q_{12}(X)\) The zero point set corresponds to \(5\)-The twisting point is \(x\)-the image on the coordinates (its degree \(12=(5^2-1)/2\) exactly matches the count).
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(\Phi(X)=x([2]P)\)(in \(X=x(P)\)) iteratively satisfies \(\Phi^n(X)=x([2^n]P)\)。
therefore \(\Phi^n(X)=X\) is equivalent to \(x([2^n]P)=x(P)\),that is \([2^n]P=\pm P\)。
then
\[
[2^n]P=P\ \Longrightarrow\ (2^n-1)P=O,
\qquad
[2^n]P=-P\ \Longrightarrow\ (2^n+1)P=O.
\]
On the contrary, if \(P\) for \((2^n-1)\)-twist or \((2^n+1)\)-twist point, then obviously there is \([2^n]P=\pm P\),thereby \(X=x(P)\) for \(\Phi^n(X)=X\) solution.
because \(2^n\pm1\) is an odd number, the above two types of twist points are at \(x\)-Click naturally under the coordinates \(\pm\) identification, so the corresponding unary integer is exactly \(\Psi_{2^n-1}\) and \(\Psi_{2^n+1}\); product of \(A_n\) differs by at most one whole sign.
\par
Two explicit polynomial decompositions (\(n=1,2\)) can be obtained by directly substituting simplification and polynomial division verification; and the number of times is counted \(\deg \Psi_3=4\)、\(\deg \Psi_5=12\) and \(A_1,A_2\) are consistent in degree, so \(Q_{12}\) can be identified as \(\Psi_5\) (to the unit factor).
Certification completed.
\end{proof}

\begin{conclusion}[Latt\`es Arithmetic closure of critical sixth degree polynomials: discriminants, divergent prime sets and octahedral Galois groups]\label{con:xi-terminal-zm-elliptic-lattes-critical-sextic-galois}
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-lattes-doubling} , and write down the critical sixth degree polynomial as
\[
C(X):=2X^{6}-10X^{4}+10X^{3}-10X^{2}+2X+1\in\QQ[X].
\]
but:
\begin{enumerate}
  \item \(C(X)\) exist \(\QQ\) is irreducible.
  \item whose discriminant satisfies the strict identity
  \[
  \boxed{\ \mathrm{Disc}\bigl(C(X)\bigr)=-2^{8}\cdot 37^{5}\ }.
  \]
  \item is set \(L\) for \(C(X)\), then its Galois group (as \(S_6\The transitive subgroup of ) is
  \[
  \boxed{\ \mathrm{Gal}(L/\QQ)\cong S_{4}\times C_{2}\ },
  \]
  That is, the total octahedral group (order \(48\)）。
  \item can be known from the discriminant, and the expansion \(L/\QQ\) is only possible in prime numbers \(2\) and \(37\) diverges at; and by the square class
  \(
  \mathrm{Disc}(C)\equiv -37\ (\mathrm{mod}\ (\QQ^\times)^2)
  \)
  Get the natural quadratic subdomain
  \[
  \boxed{\ \QQ\bigl(\sqrt{\mathrm{Disc}(C)}\bigr)=\QQ(\sqrt{-37})\ \subset\ L\ }.
  \]
  \item mark \(\rho_4:\mathrm{Gal}(\overline\QQ/\QQ)\to \mathrm{Aut}(E[4])\cong GL_2(\ZZ/4\ZZ)\) for \(4\) - Galois representation of the torsion point.
  because \(C(X)=0\) of six characters \(x\bigl(E[4]\setminus E[2]\bigr)\) coordinate image, its split domain \(L\) Equivalent carrying \(\rho_4\) in business \(E[4]/\{\pm 1\}\) (that is, remove the scalar \(\{\pm I\}\));
  Therefore, the image order of this image is \(48\), and with the above \(S_4\times C_2\) are portrayed consistently.
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
Irreducibility, discriminant factorization and Galois groups are calculated by direct symbols; divergent prime sets and quadratic subfields are immediately derived from the prime factors and square classes of the discriminant.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_lattes\_rational\_points\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Quadratic expansion of weight parameters and ellipticalization of discriminant:\(y\) exist \texorpdfstring{$\QQ(X)$}{Q(X)} minimum polynomial on]\label{con:xi-terminal-zm-y-quadratic-extension-minpoly}\label{con:xi-terminal-zm-elliptic-quadratic-discriminant}
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-normalization} The coordinate convention of (\(X=\lambda,\ Y=w/2\),thereby \(y=(2X^2+w-1)/2=X^2+Y-\tfrac12\)）。
Let the weight parameter of the physical main branch be taken as
\[
\boxed{\;
y:=X^2+Y-\frac12\ \in\ \QQ(E)\; }.
\]
but \(y\) exist \(\QQ(X)\) satisfies a strict quadratic minimum polynomial on
\[
\boxed{\;
y^2-(2X^2-1)\,y+X(X-1)^2(X+1)=0\; }.
\]
Its discriminant is exactly
\[
\boxed{\;
(2X^2-1)^2-4X(X-1)^2(X+1)
=4X^3-4X+1
=4Y^2\; }.
\]
Therefore the two-branch conjugate of the weight parameter is inverted by the ellipse \(\iota:(X,Y)\mapsto(X,-Y)\) gives:
\[
y^{\iota}=(2X^2-1)-y
=X^2-Y-\frac12,
\qquad
yy^{\iota}=X(X-1)^2(X+1).
\]
Therefore, the nonlinear coupling of "weight variable-spectral variable" is compressed into a quadratic expansion and its elliptical discriminant at the function domain level, and is completely rigid in structure.
\end{conclusion}
\begin{proof}[Points of proof]
by definition \(Y=y-X^2+\frac12\), substitute into the curve equation \(Y^2=X^3-X+\frac14\) and expand and simplify to obtain the quadratic polynomial shown.
The discriminant identity is verified by direct expansion, using \(4Y^2=4X^3-4X+1\) can be rewritten.
The conjugate and product identities come from the sum of two roots and the product of two roots of a quadratic equation. Certification completed.
\end{proof}

\begin{conclusion}[Linear transformation of weight parameters under double point pullback: critical sixth degree as linear coefficient]\label{con:xi-terminal-zm-elliptic-y-doubling-linear}
in function domain \(\QQ(E)=\QQ(X,y)\), multiplicative automorphism \([2]:E\to E\) for the physical main support weight parameters \(y\) satisfies the strict identity
\[
\boxed{\;
[2]^*(y)=\frac{C(X)\,y+B(X)}{(4X^3-4X+1)^2}\; },
\]
in
\[
C(X)=2X^{6}-10X^{4}+10X^{3}-10X^{2}+2X+1,
\]
\[
B(X)=-X^{8}+7X^{6}-14X^{5}+27X^{4}-9X^{3}-6X^{2}+X+1.
\]
In particular, in the second expansion \(\QQ(E)/\QQ(X)\) of natural base \(\{1,y\}\) Down,\([2]^*\) exist \(y\) is strictly linear;
Its linear coefficient is exactly Latt\`es critical six times \(C(X)\)。
therefore
\[
\boxed{\;
C(X)=0\quad\Longleftrightarrow\quad [2]^*(y)\in \QQ(X)\; } ,
\]
That is, times Latt\The criticality of `es can be interpreted as the algebraic certificate that the quadratic divergence dependence of weight collapses under multiple point pullbacks.
\end{conclusion}
\begin{proof}[Points of proof]
Written by the doubling point formula \([2](X,Y)=(X_2,Y_2)\), and then \(y_2=X_2^2+Y_2-\tfrac12\) is regarded as \(\QQ(X)\)-linear function in \(y\), and in four certificates \(F(X,y)=0\), the coefficients shown can be obtained by simplifying \(C(X)\)、\(B(X)\) and the denominator \((4X^3-4X+1)^2\)。
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_lattes\_rational\_points\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Cubic/cubic rational expression of double point pullback in the weight plane quartic model]\label{con:xi-terminal-zm-elliptic-y-doubling-cubic-fraction}
in conclusion \ref{con:xi-terminal-zm-elliptic-quadratic-discriminant} The first one or four expressions of
\[
\QQ(E)\cong \QQ(y)(X)/(F(X,y)),
\qquad
F(X,y):=X^{4}-X^{3}-(2y+1)X^{2}+X+y(y+1),
\]
down, note \(y_2:=[2]^*(y)\in\QQ(E)\)。
but \(y_2\) in the expansion base \(\{1,X,X^{2},X^{3}\}\) can be written as about \(X\) cubic/cubic fraction:
\[
\boxed{\;
y_2=\frac{\mathcal N(X,y)}{\mathcal D(X,y)}\in \QQ(y)(X)/(F)\;},
\]
in \(\mathcal N,\mathcal D\in\ZZ[y][X]\) are all about \(X\), and can be taken as
\[
\begin{aligned}
\mathcal N(X,y)=\;&(-2y^{2}-10y+1)X^{3}+(2y^{3}-6y^{2}+34y+27)X^{2}\\
&\quad+(2y^{3}+10y^{2}+12y-23)X-(y^{4}+23y^{2}+23y-1),\\[1mm]
\mathcal D(X,y)=\;&(64y+8)X^{3}+(48y^{2}+16y)X^{2}-(16y^{2}+48y+8)X-(32y^{3}+32y^{2}-1).
\end{aligned}
\]
This represents the nonlinear compression of pulling back the doubling point as \(\QQ(y)\) - finite-order fractional expressions on linear basis, thus providing a direct algebraic interface for local diagnosis of divergences/poles.
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-elliptic-y-doubling-linear}，\(y_2=[2]^*(y)\) exist \(\QQ(E)=\QQ(X,y)\) is given as \(\QQ(X)\)-linear fraction.
Think of this expression as \(\QQ(y)(X)/(F)\), and end with \(F(X,y)=0\) right \(X\) by lowering the higher power of ) and then clearing the denominator, we can get a \(X\) times not exceeding \(3\) represents the numerator and denominator;
pair shown \(\mathcal N,\mathcal D\) can be verified by script \texttt{scripts/exp\_fold\_zm\_elliptic\_lattes\_rational\_points\_audit.py} Finish. Certification completed.
\end{proof}

\begin{conclusion}[The rational point structure of weighted elliptic curves:\texorpdfstring{$E(\QQ)_{\mathrm{tors}}=\{O\}$}{E(Q)\_tors={O}}, and the physical point is of infinite order]\label{con:xi-terminal-zm-elliptic-rational-points-denominator-law}
exist \(E:\ Y^2=X^3-X+\frac14\), the rational point group is a trivial group
\[
\boxed{\;E(\QQ)_{\mathrm{tors}}=\{O\}\; }.
\]
Will \(E\) by integer transformation
\[
(\widetilde X,\widetilde Y)=(4X,8Y)
\]
written as an integral model
\[
\widetilde E:\ \widetilde Y^{2}=\widetilde X^{3}-16\widetilde X+16,
\qquad
\Delta(\widetilde E)=2^{12}\cdot 37,
\]
then in \(p\neq 2,37\) has a good reduction; and the point count gives
\[
\card{\widetilde E(\FF_{5})}=8,
\qquad
\card{\widetilde E(\FF_{7})}=9,
\qquad
\gcd(8,9)=1,
\]
thus deducing \(E(\QQ)_{\mathrm{tors}}=\{O\}\)。
In particular, point
\[
P=\left(2,-\frac52\right)\in E(\QQ)
\qquad(\text{Equivalently }-P=(2,\tfrac52))
\]
is an infinite order point and satisfies \(y(P)=1\)(in \(y\) is the conclusion \ref{con:xi-terminal-zm-y-quadratic-extension-minpoly} physical main branch).
In addition, you can directly substitute \(y(-P)=6\)。
and its in \(\FF_{11}\) satisfies the reduction on
\[
\mathrm{ord}\bigl(P\bmod 11\bigr)=17,
\]
thus \(P\) cannot be a scratch point; therefore \(\mathrm{rank}\,E(\QQ)\ge 1\),and \(E(\QQ)\) is an inflexible free Abelian group.
Write
\(
x(nP)=u_n/v_n^2
\)
is in reduced form (\(v_n\in\ZZ_{>0}\)),but \(v_n\) constitutes an elliptic denominator sequence, and is given by
\(
y=X^2+Y-\tfrac12
\)
Obtain the strictly integral scale law:
\[
\boxed{\;
y(nP)\in\QQ,\qquad \mathrm{den}\bigl(y(nP)\bigr)=v_n^{4}\; }.
\]
The first six items can be taken as (list \(v_n\) and \(y(nP)\)）：
\[
\begin{array}{c|cccccc}
n & 1 & 2 & 3 & 4 & 5 & 6\\ \hline
v_n & 1 & 5 & 29 & 65 & 16264 & 2382785\\
y(nP)
& 1
& \dfrac{96}{625}
& \dfrac{2679201}{707281}
& \dfrac{252115165246}{17850625}
& \dfrac{162231294668143481}{69969611497148416}
& \dfrac{2533325779202487675774201}{32235872541947843696250625}
\end{array}
\]
The corresponding root of the spectrum (i.e. \(x(nP)\), the first six terms are
\[
x(P)=2,\quad
x(2P)=\dfrac{21}{25},\quad
x(3P)=\dfrac{1357}{841},\quad
x(4P)=\dfrac{480106}{4225},\quad
x(5P)=\dfrac{683916417}{264517696},\quad
x(6P)=\dfrac{4881674119706}{5677664356225}.
\]
As a result, the "ellipticization of weight free energy" is further promoted to a rational point dynamics that can be directly used for local-global diagnosis: the multiplicative orbit of the same physical point carries a strict fourth power scale in the denominator.
\end{conclusion}
\begin{proof}[Points of proof]
(i) The triviality of torsion points can be determined by the modulus of a finite number of well-reduced primes. \(p\) group-level gcd auditing: injection by reduction
\(
E(\QQ)_{\mathrm{tors}}\hookrightarrow E(\mathbb F_p)
\)
Know \(|E(\QQ)_{\mathrm{tors}}|\) divisible \(\gcd_p \#E(\mathbb F_p)\)；
In the integral model \(\widetilde E\) to select well-reduced primes \(p=5,7\) to get \(\gcd(\#\widetilde E(\FF_5),\#\widetilde E(\FF_7))=\gcd(8,9)=1\),thereby \(E(\QQ)_{\mathrm{tors}}=\{O\}\)。
(ii) by \(\mathrm{ord}(P\bmod 11)=17\) can be directly excluded \(P\) is the deflection point, so \(P\) is an infinite order point,\(\mathrm{rank}\,E(\QQ)\ge 1\)。
(iii) order \(x(nP)=u_n/v_n^2\)、\(Y(nP)=r_n/v_n^3\) is a reduced representation, then
\(
y(nP)=x(nP)^2+Y(nP)-\tfrac12
\)
divisible by the denominator \(v_n^4\)；
The orbit can be verified to have an exact equal sign \(\mathrm{den}(y(nP))=v_n^4\), and the first six items shown can be recycled.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_lattes\_rational\_points\_audit.py}. Certification completed.
\end{proof}

\input{con__xi-terminal-zm-elliptic-mw-height-denominator-growth}

\begin{conclusion}[Ellipse divisible sequences induced by physical base points: strong divisibility and low-order primitive prime factors]\label{con:xi-terminal-zm-elliptic-eds-strong-divisibility}
in conclusion \ref{con:xi-terminal-zm-elliptic-rational-points-denominator-law} integral model of \(\widetilde E\) on, order
\(
\widetilde P:=(8,20)\in\widetilde E(\QQ)
\)
corresponds to \(P\)。
right \(n\ge 1\) Will \(\widetilde X\bigl(n\widetilde P\bigr)\) written as a reduced fraction
\[
\widetilde X\bigl(n\widetilde P\bigr)=\frac{A_n}{B_n^2},
\qquad
A_n,B_n\in\ZZ,\quad \gcd(A_n,B_n)=1,\quad B_n>0,
\]
but \(\bigl(B_n\bigr)_{n\ge 1}\) constitutes an elliptic divisible sequence and satisfies strong divisibility
\[
\gcd(B_m,B_n)=B_{\gcd(m,n)},
\qquad
m\mid n\Longrightarrow B_m\mid B_n.
\]
The first ten items are
\[
\begin{aligned}
B_1&=1,&
B_2&=5,&
B_3&=29,&
B_4&=65,&
B_5&=8132,\\
B_6&=2382785,&
B_7&=398035821,&
B_8&=43244638645,&
B_9&=124106986093951,&
B_{10}&=270525565025400200.
\end{aligned}
\]
And several primitive denominator prime factors have appeared at lower orders, e.g.
\[
29\mid B_3,\qquad 13\mid B_4,\qquad 19\mid B_5,\qquad 17\mid B_9.
\]
in \(\{13,17,19,29\}\) also belong to \(E_8\) Coxeter index set \(\{1,7,11,13,17,19,23,29\}\), thereby giving an auditable exponential prime co-occurrence sample at a very low level.
\end{conclusion}
\begin{proof}[Points of proof]
Strong divisibility and divisibility are general properties of elliptical divisible sequences;
The divisibility relationship between the first ten terms above and the one shown can be expressed by \(\widetilde P\) is directly calculated and placed in \(\ZZ\) decomposition verification.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_lattes\_rational\_points\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Denominator prime factor and module \texorpdfstring{$p$}{p} Strict equivalence of order:\texorpdfstring{$p\mid B_n$}{p|Bn} group theory decoding]\label{con:xi-terminal-zm-elliptic-eds-modp-order-equivalence}
set up \(p\neq 2,37\) for \(\widetilde E\) is a good reduced prime.
Then for any \(n\ge 1\) are strictly equivalent to
\[
p\mid B_n
\ \Longleftrightarrow\
n\widetilde P\equiv O\pmod p
\ \Longleftrightarrow\
\mathrm{ord}_{\widetilde E(\FF_p)}(\widetilde P)\mid n.
\]
therefore \(\bigl(B_n\bigr)\)'s set of prime factors accurately records \(\widetilde P\) in each finite field point group \(\widetilde E(\FF_p)\); any order distribution in \(p\mid B_n\)'s local "module" \(p\) fingerprint" can be equivalently transformed into a pure group theory proposition"\(\mathrm{ord}(\widetilde P\bmod p)\) whether it is divisible \(n\)”。
\end{conclusion}
\begin{proof}[Points of proof]
At a good reduction point,\(\widetilde E(\QQ)\to\widetilde E(\FF_p)\) to the denominator of \(p\)-advanced information has accurate discrimination:\(p\mid B_n\) if and only if \(\widetilde X(n\widetilde P)\) exist \(p\)-A pole appears under progressive assignment, which is equivalent to \(n\widetilde P\) is reduced to a point at infinity \(O\)。
The last equivalence is the definition of the order of elements in a finite abelian group. Certification completed.
\end{proof}

\input{con__xi-terminal-zm-elliptic-badprime-37-periodicity}

\begin{conclusion}[Non-complex multiplication and primitive prime factor theorem: Infinity of primitive prime factors of elliptical divisible sequences]\label{con:xi-terminal-zm-elliptic-eds-primitive-primes-noncm}
in conclusion \ref{con:xi-terminal-zm-elliptic-normalization} given
\(
j(E)=110592/37
\)
non-algebraic integers, so \(E\) without complex multiplication.
Therefore, according to the primitive prime factor theorem of non-complex multiplicative elliptic divisible sequences, there exists a constant \(n_0\) makes for everything \(n>n_0\)，\(B_n\) Contains original prime factor \(p_n\)(Right now \(p_n\mid B_n\) and \(p_n\nmid\prod_{m<n}B_m\)), and by the conclusion \ref{con:xi-terminal-zm-elliptic-eds-modp-order-equivalence} have to
\[
\mathrm{ord}_{\widetilde E(\FF_{p_n})}(\widetilde P)=n.
\]
Therefore, the elliptical structure produces an infinite number of original prime factors in the strict sense, on which \(\widetilde P\) has a preset precise order for subsequent mode cutting \(p\) Fingerprint verification provides a controllable "order-prime" supply mechanism.
\end{conclusion}

\begin{conclusion}[Domain trace representation of the weight partition function: the denominator is only controlled by the Lee--Yang cubic factor]\label{con:xi-terminal-zm-field-trace-representation}
Follow the conclusion \ref{con:xi-terminal-zm-order4-rational} The sign of
\[
\Pi(\lambda,y):=\lambda^4-\lambda^3-(2y+1)\lambda^2+\lambda+y(y+1),
\qquad
P_{\mathrm{LY}}(y):=256y^3+411y^2+165y+32.
\]
remember \(K:=\QQ(y)(\lambda)\)(in \(\lambda\) for \(\Pi(\lambda,y)=0\), and let
\(
s_m(y):=\mathrm{Tr}_{K/\QQ(y)}(\lambda^m)
=\sum_{i=1}^{4}\lambda_i(y)^m
\)
is the sum of powers of four roots (Newton power sum).
then there is a unique element \(u(\lambda)\in K\), which is based on \(\{1,\lambda,\lambda^2,\lambda^3\}\) can be taken as a cubic polynomial
\[
u(\lambda)=a_0(y)+a_1(y)\lambda+a_2(y)\lambda^2+a_3(y)\lambda^3,
\]
makes for everything \(m\ge 0\) has a strict domain trace representation
\[
\boxed{\;
Z_m(y)=\mathrm{Tr}_{K/\QQ(y)}\!\bigl(u(\lambda)\lambda^m\bigr)\; }.
\]
and \(u\) can be explicitly taken as
\[
\boxed{\;
a_0(y)=\frac{-19y^3-75y^2-42y-8}{P_{\mathrm{LY}}(y)},\qquad
a_2(y)=\frac{87y^2+48y+9}{P_{\mathrm{LY}}(y)}\; } ,
\]
\[
\boxed{\;
a_1(y)=\frac{80y^4-20y^3-82y^2-42y-8}{y\,P_{\mathrm{LY}}(y)},\qquad
a_3(y)=\frac{-16y^3+49y^2+31y+8}{y\,P_{\mathrm{LY}}(y)}\; } .
\]
So despite \(Z_m(y)\in\ZZ[y]\) is an integer coefficient polynomial, and its "spectral projection weight" can only be in the function domain
\(
y=0
\)
and
\(
P_{\mathrm{LY}}(y)=0
\)
A pole appears at; this provides a direct proof for the inevitability of the Lee--Yang bifurcation point at the algebraic level.
\end{conclusion}
\begin{proof}[Points of proof]
to the unknown \(u=b_0+b_1\lambda+b_2\lambda^2+b_3\lambda^3\in K\), there is an identity
\[
\mathrm{Tr}(u\lambda^m)=b_0\,s_m+b_1\,s_{m+1}+b_2\,s_{m+2}+b_3\,s_{m+3}.
\]
because \(m\mapsto Z_m(y)\) and \(m\mapsto s_m(y)\) are satisfied by \(\Pi(\lambda,y)\) induces the same fourth-order recursion, so we only need to \(m=0,1,2,3\) can be uniquely determined by matching \((b_0,\dots,b_3)\)；
Solving the linear system and simplifying it gives us the following \(a_i(y)\)。
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_trace\_galois\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Auditable integer identity after clearing the denominator:\texorpdfstring{$yP_{\mathrm{LY}}(y)Z_m$}{yP(y)Z\_m} is the integer coefficient combination of the power sum]\label{con:xi-terminal-zm-integral-identity-power-sums}
in conclusion \ref{con:xi-terminal-zm-field-trace-representation} notation, multiply by \(yP_{\mathrm{LY}}(y)\) can improve the domain trace formula to an integral identity: for all \(m\ge 0\) have
\[
\boxed{\;
yP_{\mathrm{LY}}(y)\,Z_m(y)
=A_0(y)\,s_m(y)+A_1(y)\,s_{m+1}(y)+A_2(y)\,s_{m+2}(y)+A_3(y)\,s_{m+3}(y)\;}
\]
The coefficient polynomial is
\[
\boxed{\;
\begin{aligned}
A_0(y)&=-19y^4-75y^3-42y^2-8y,\\
A_1(y)&=80y^4-20y^3-82y^2-42y-8,\\
A_2(y)&=87y^3+48y^2+9y,\\
A_3(y)&=-16y^3+49y^2+31y+8.
\end{aligned}}
\]
This identity directly connects "weight counting" (left-hand side) and "spectral power sum" (right-hand side) at the level of whole coefficients and condenses all potential analytic obstacles into an explicit algebraic set \(yP_{\mathrm{LY}}(y)=0\)。
\end{conclusion}
\begin{proof}
will conclude \ref{con:xi-terminal-zm-field-trace-representation} of \(a_i(y)\) Substitute
\(
\mathrm{Tr}(u\lambda^m)=a_0 s_m+a_1 s_{m+1}+a_2 s_{m+2}+a_3 s_{m+3}
\), and clear the denominator to get it. Certification completed.
\end{proof}

\begin{conclusion}[Explanation of trace function: geometric origin of fourth-order recursion and constraints of singular point set]\label{con:xi-terminal-zm-trace-pushforward-origin}
in conclusion \ref{con:xi-terminal-zm-elliptic-normalization} Under the elliptic identification of , the normalization of the spectral curve can be regarded as a fixed elliptic curve \(E\) times on \(4\) rational mapping
\(
y\in\QQ(E)
\)。
to any \(y_0\in\CC\setminus\{0,1\}\) and \(P_{\mathrm{LY}}(y_0)\neq 0\),fiber \(y^{-1}(y_0)\subset E(\CC)\) contains exactly four points;
That \(X\)-coordinate sets and quartic equations \(\Pi(\lambda,y_0)=0\) are consistent.
therefore \(\lambda\) as \(y\), the full-branch Riemann surface of the four-valued algebraic function is \(E(\CC)\) (equivalent to conclusion \ref{con:xi-terminal-zm-elliptic-normalization} normalized description of ), and by the ellipse discriminant \(\Delta(E)=37\) anchored by the unification of.
\par
At the functional domain level, expansion \(\QQ(E)/\QQ(y)\) is the number of times \(4\), its primitive element can be taken as \(X=\lambda\in\QQ(E)\)。
for each integer \(m\ge 0\) defines the trace function
\[
T_m(y):=\Tr_{\QQ(E)/\QQ(y)}(X^m)\in\QQ(y).
\]
but \((T_m)_{m\ge 0}\) is satisfied by \(\Pi(\lambda,y)\) determines the fourth-order linear recursion (its characteristic polynomial is \(\Pi\)）：
\[
T_m(y)=T_{m-1}(y)+(2y+1)T_{m-2}(y)-T_{m-3}(y)-y(y+1)T_{m-4}(y)\qquad(m\ge 4).
\]
Furthermore, as \(\mathbb P^1_y\),\(T_m\) has possible poles only at \(y=\infty\)；
Therefore, its singular point set is included in the divergence value set
\(
\{\infty,0,1\}\cup\{P_{\mathrm{LY}}(y)=0\}
\) (see conclusion \ref{con:xi-terminal-zm-elliptic-y-hurwitz-passport}）。
Combined with conclusion \ref{con:xi-terminal-zm-field-trace-representation} The domain trace representation of , the fourth-order recursion can be interpreted as the degree of \(4\) overlay \(\pi_y:E\to\mathbb P^1_y\) on the trace push structure, and the Lee--Yang cubic factors are intrinsically locked in the divergent geometry of this overlay.
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(\Pi(X,y)=0\) exist \(\QQ(E)\), multiply by \(X^{m-4}\) and take \(\Tr_{\QQ(E)/\QQ(y)}\) gives the recursion shown.
And because of \(X\) exist \(E\) only at the infinite point \(O\) has a pole everywhere, and \(y\) exist \(O\) has a fourth-order pole (conclusion \ref{con:xi-terminal-zm-elliptic-normalization}), so \(T_m\) in limited \(y\)-point does not have a pole, so its possible pole is only at \(y=\infty\)。
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Maximum algebraic complexity of spectrum:\texorpdfstring{$\mathrm{Gal}_{\QQ(y)}\!\bigl(\Pi(\lambda,y)\bigr)\cong S_4$}{Gal(Q(y))(Pi)=S4}]\label{con:xi-terminal-zm-generic-galois-s4}
Bundle \(\Pi(\lambda,y)\) is deemed to be about \(\lambda\) of a fourth degree polynomial.
Its cubic resolvent can be taken as
\[
\boxed{\;
\mathscr R(z,y)
=z^3+(1+2y)z^2-(1+4y+4y^2)z-(1+5y+13y^2+8y^3)\; }.
\]
Its discriminant satisfies the strict identity
\[
\boxed{\;
\mathrm{Disc}_{z}\bigl(\mathscr R(\cdot,y)\bigr)
=-y(y-1)\,P_{\mathrm{LY}}(y)\; } ,
\]
And it is completely consistent with the discriminant of the quartic spectral equation:
\[
\boxed{\;
\mathrm{Disc}_{z}\bigl(\mathscr R(\cdot,y)\bigr)
=\mathrm{Disc}_{\lambda}\bigl(\Pi(\cdot,y)\bigr)\; } .
\]
Therefore in \(y\)-line, the set of divergence values ​​covered by the fourth spectrum is exactly \(\{0,1\}\) and \(P_{\mathrm{LY}}(y)=0\); the Lee--Yang cubic factors thus give the irreducible divergence kernel of the discriminant.
In any good specialization (e.g. \(y=2\))Down,
\[
\mathscr R(z,2)=z^{3}+5z^{2}-25z-127
\]
exist \(\QQ\) is irreducible and its discriminant is non-square,
thereby \(\Pi(\lambda,y)\) exist \(\QQ(y)\The general Galois group on ) is
\[
\boxed{\;\mathrm{Gal}\bigl(\Pi/\QQ(y)\bigr)\cong S_4.\;}
\]
Therefore, the quartic equation of the weight spectrum reaches the maximum symmetric complexity in the functional domain sense, and its only quadratic resolution is given by \(-y(y-1)P_{\mathrm{LY}}(y)\) lock, directly connected to the Lee--Yang divergence set.
Equivalently, lineage \(\lambda_i(y)\) gives a full value group on four spectral branches. \(S_4\)-action; therefore there is no algebraic reduction that globally decomposes the spectral coverage into two mutually unmixed quadratic subon covers.
\end{conclusion}
\begin{proof}[Points of proof]
decomposition \(\mathscr R(z,y)\) and the identity of the discriminant can be obtained by direct symbolic calculation, and the conclusion \ref{con:xi-terminal-zm-discriminant-leyang} The discriminant decomposition of is consistent.
If in a specialization \(y=y_0\) Down \(\mathscr R(z,y_0)\) exist \(\QQ[z]\) is irreducible, then \(\mathscr R\) exist \(\QQ(y)[z]\) is irreducible;
For the irreducible quartic, the irreducibility of the cubic decomposition constrains the Galois group to \(\{A_4,S_4\}\)；
The discriminant is non-square exclusion \(A_4\), so it must be \(S_4\)。
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_trace\_galois\_audit.py}. Certification completed.
\end{proof}

\begin{remark}[Hitchin spectrum coverage with \texorpdfstring{$A_3$}{A3} cameral overlay:\texorpdfstring{$S_4\simeq W(A_3)$}{S4=W(A3)} structural explanation]\label{rem:xi-terminal-zm-hitchin-cameral-cover}
in conclusion \ref{con:xi-terminal-zm-generic-galois-s4} The generic symmetry group given \(S_4\) can be understood as a Weyl singularization: at the level of algebraic geometry, the quartic spectrum covers \(\Pi(\lambda,y)=0\)'s Galois closure gives a \(S_4\)-cameral overlay, its group effect is the same as \(A_3\) type Weyl group \(W(A_3)\cong S_4\) isomorphism.
If you will \(\Pi(\lambda,y)=0\) is interpreted as a certain rank \(4\) Higgs field (e.g. \(G=\mathrm{SL}_4\) or \(\mathrm{PGL}_4\)) on the Hitchin basis, then the \(S_4\) Unification corresponds to the Weyl symmetry of the cameral cover;
And the same discriminant kernel \(-y(y-1)P_{\mathrm{LY}}(y)\) then gives the intersection of this slice with the Hitchin discriminant divisor, thereby intrinsically identifying the Lee--Yang divergence set as an explicit coordinateization of spectrally covering odd fibers.
In the physical implementation of geometric Langlands, Hitchin fiberization and its spectral/cameral data are important geometric inputs for describing the vacuum structure and defect operators after topological distortion.\cite{KapustinWitten2006GeometricLanglands}
\end{remark}

\begin{conclusion}[Rational parameter specialization of quartic integer models and discriminants: explicit upper bounds on divergent prime sets]\label{con:xi-terminal-zm-specialization-discriminant-ab}
Take rational parameters \(y_0=a/b\in\QQ\)（\(\gcd(a,b)=1,\ b>0\)), and consider the fourth integer after the denominator
\[
P_{a,b}(\lambda):=b^{2}\Pi(\lambda,a/b)
=b^{2}\lambda^{4}-b^{2}\lambda^{3}-(2ab+b^{2})\lambda^{2}+b^{2}\lambda+a(a+b)\in\ZZ[\lambda].
\]
then it is about \(\lambda\) satisfies the strict closed form
\[
\boxed{\;
\mathrm{Disc}_{\lambda}\bigl(P_{a,b}\bigr)
=-a(a-b)\bigl(256a^{3}+411a^{2}b+165ab^{2}+32b^{3}\bigr)\,b^{7}\; }.
\]
Therefore if and only if \(a=0\)、\(a=b\) or \(256a^{3}+411a^{2}b+165ab^{2}+32b^{3}=0\) when double roots appear;
In other cases,\(P_{a,b}\) exist \(\overline\QQ\) (equivalently: the specialization overlaps at this parameter \'{e}tale）。
Furthermore, if the prime number \(p\) exist \(P_{a,b}\), it must be divisible
\[
2ab(a-b)\bigl(256a^{3}+411a^{2}b+165ab^{2}+32b^{3}\bigr),
\]
Thus the set of divergent primes is contained by the above explicit integers.
Combined with conclusion \ref{con:xi-terminal-zm-generic-galois-s4} Generics \(S_4\) property, it can be known from Hilbert's indefinability theorem: except for thin set parameters, the specialization polynomial \(P_{a,b}\), the split domain Galois group is still \(S_4\)，
thus obtaining a family of \(y_0\in\QQ\) parameterized \(S_4\) family of number fields whose divergence is completely controlled by the same Lee--Yang cubic kernel after clearing the denominator.
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-discriminant-leyang}，
\(
\mathrm{Disc}_{\lambda}\bigl(\Pi(\cdot,y)\bigr)=-y(y-1)P_{\mathrm{LY}}(y)
\)。
Substitute \(y=a/b\) and exploit the homogeneity of the quartic case
\(
\mathrm{Disc}_{\lambda}(c\cdot f)=c^{6}\mathrm{Disc}_{\lambda}(f)
\)
get
\(
\mathrm{Disc}_{\lambda}\bigl(P_{a,b}\bigr)=b^{12}\mathrm{Disc}_{\lambda}\bigl(\Pi(\cdot,a/b)\bigr)
\)，
Simplify to get the closed formula.
The inclusion of different prime sets comes from the general criterion "the different prime numbers of the split field must divide the polynomial discriminant".
The thin set assertion is derived from the general Galois group and Hilbert's incommensurability theorem.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_pi\_specialization\_discriminant\_ab\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}["oval-\texorpdfstring{$S_4$}{S4}—Lee--Yang" Triple Duality: The same discriminant simultaneously governs the normalized divergence and the split-domain quadratic spine]\label{con:xi-terminal-zm-elliptic-s4-leyang-triple-duality}
curve \(\Pi(\lambda,y)=0\) is geometrically normalized to an elliptic curve \(E\)(in conclusion \ref{con:xi-terminal-zm-elliptic-normalization}），
And the same quartic spectral equation is in \(\QQ(y)\) has \(S_4\) Galois Group (Conclusion \ref{con:xi-terminal-zm-generic-galois-s4}）。
Both are represented by the same polynomial at the discriminant level
\[
-y(y-1)\,P_{\mathrm{LY}}(y)
\]
Also dominate:
\begin{enumerate}
  \item as a geometric object which gives the projection \(\pi:E\to\mathbb P^1_y\) of the divergence divisor (see conclusion \ref{con:xi-terminal-zm-leyang-cubic-branch-image} and conclusion \ref{con:xi-terminal-zm-discriminant-leyang}）；
  \item as an algebraic object, which gives \(S_4\) the only quadratic resolution subdomain of the split domain \(\QQ\bigl(\sqrt{-y(y-1)P_{\mathrm{LY}}(y)}\bigr)\)。
\end{enumerate}
Therefore the Lee--Yang cubic factor \(P_{\mathrm{LY}}(y)\) is not an "accidental singularity of the thermodynamic branch", but a common discriminant kernel that simultaneously controls the divergence geometry of elliptical normalization and the quadratic spine of the four-split domain;
Thus, the "analytical criticality" and "Galois resolution" are structurally locked into the same piece of irreducible algebraic data.
\end{conclusion}

\input{con__xi-terminal-zm-elliptic-branchpoint-specializations-genus2-s4}

