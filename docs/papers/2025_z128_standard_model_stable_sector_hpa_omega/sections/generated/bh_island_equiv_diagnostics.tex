\paragraph{BH6 toy diagnostic (finite reconstruction).} \AuditTag This fragment records a finite protocol-native toy in which interior fiber information (the microstate choice within each folding fiber) is encoded into an exterior record as an auxiliary bitstream. The purpose is to illustrate the algebraic pattern ``interior as a reconstructible subalgebra'' without invoking gravity path integrals.
\begin{center}
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{l r}
\toprule
quantity & value \\
\midrule
$m$ (window length) & 6 \\
$L$ (message microstates) & 64 \\
$r_m=\max_w g_m(w)$ & 4 \\
$b=\lceil\log_2 r_m\rceil$ (bits per fiber index) & 2 \\
$\sum_i \log_2 g_m(W_i)$ (interior ambiguity bits) & 98.679700 \\
$L\cdot b$ (recovery bits in the toy) & 128 \\
slack bits $(L\cdot b)-\sum_i\log_2 g_m(W_i)$ & 29.320300 \\
reconstruction success (toy) & PASS \\
\bottomrule
\end{tabular}
\end{center}