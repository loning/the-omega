% (User input window)
%
% Under the logistic kernel chromatography caliber, the curvature probability density \(p\) is not only identifiable, but also has a fully explicit operator expression;
% The same multiplier structure simultaneously enforces universal thresholds for noise amplification, zero-point density, and bit complexity.

proposition \ref{prop:xi-bulk-curvature-logistic-kernel-decomposition}--\ref{prop:xi-bulk-curvature-tomography-identifiability} The finite atomic decomposition and identifiability of the curvature kernel are given;
Under the same notation, the inversion can be further written as an explicit operator, and the universal seal of stability, zero-point density and accuracy budget can be derived from the same multiplier structure.

\begin{proposition}[Exact deconvolution of scale curvature: explicit inverse operator of logistic smoothing]\label{prop:xi-bulk-curvature-exact-deconvolution-operator}
inferring \ref{cor:xi-bulk-curvature-probability-moments} Under the setting of
\[
\nu:=\frac{1}{\kappa}\sum_{j=1}^{\kappa}\delta_{\sigma_j},
\qquad
\phi(s)=\frac{e^{s}}{(1+e^{s})^{2}}=\frac{1}{4\cosh^{2}(s/2)},
\qquad
p=\phi*\nu.
\]
make \(\mathcal D\) for \(\mathcal S'(\RR)\) on the Fourier multiplier operator (in \(\xi=0\) takes the continuous extension value)
\[
\widehat{(\mathcal D f)}(\xi):=\frac{\sinh(\pi\xi)}{\pi\xi}\,\widehat f(\xi).
\]
Then there is a strict identity in the sense of mild distribution
\[
\boxed{\;\mathcal D p=\nu.\;}
\]
and \(\mathcal D\) has a Weierstrass product representation (acting as a Fourier multiplier operator on \(\mathcal S(\RR)\)）
\[
\boxed{\;
\mathcal D=\prod_{n\ge1}\Bigl(1-\frac{1}{n^{2}}\partial_{s}^{2}\Bigr)
=\frac{\sinh\!\bigl(\pi\sqrt{-\partial_{s}^{2}}\bigr)}{\pi\sqrt{-\partial_{s}^{2}}}\; }.
\]
Therefore "scale smoothing \(p=\phi*\nu\)" is not an irreversible noise channel in this system, but is accurately reversible by a fully explicit integral function infinite-order operator.
\end{proposition}
\begin{proof}
by proposition \ref{prop:xi-bulk-curvature-tomography-identifiability} Closed form Fourier transform in
\(
\widehat\phi(\xi)=\pi\xi/\sinh(\pi\xi)
\)
With the convolution theorem, we get
\(
\widehat p(\xi)=\widehat\phi(\xi)\widehat\nu(\xi)
\)。
ride on both sides \(\sinh(\pi\xi)/(\pi\xi)\) to get
\(
\widehat\nu(\xi)=\frac{\sinh(\pi\xi)}{\pi\xi}\widehat p(\xi)
=\widehat{(\mathcal D p)}(\xi)
\),thereby \(\mathcal Dp=\nu\)。
The product representation is given by the classical integral function identity
\(
\sinh(\pi z)/(\pi z)=\prod_{n\ge1}(1+z^{2}/n^{2})
\)
Replacement with Fourier side \(z^{2}\mapsto \xi^{2}\) (equivalent to the physical side \(1+z^{2}/n^{2}\mapsto 1-\partial_{s}^{2}/n^{2}\))get. Certification completed.
\end{proof}

% (User input window)

\begin{corollary}[Modular square holography of scale shape factors: weighted counting of deep differential multisets]\label{cor:xi-bulk-curvature-modsquare-holography}
make
\(
\widehat p(\xi)=\int_{\RR}e^{-i\xi s}p(s)\,\dd s
\)。
Then there is a strict identity
\[
\boxed{\;
\Bigl|\frac{\sinh(\pi\xi)}{\pi\xi}\widehat p(\xi)\Bigr|^2
=|\widehat\nu(\xi)|^2
=\frac{1}{\kappa^2}\sum_{j,k=1}^{\kappa}e^{-i\xi(\sigma_j-\sigma_k)}
=\frac{1}{\kappa^2}\sum_{\Delta\in\mathcal D}m(\Delta)e^{-i\xi\Delta}\; }.
\]
in \(\mathcal D:=\{\sigma_j-\sigma_k\}_{j,k}\) is a differential multiset,\(m(\Delta)\) is its multiplicity.
So in stripping the universal multiplier \(\widehat\phi(\xi)=\pi\xi/\sinh(\pi\xi)\), the modulus squared data \(|\widehat p(\xi)|^2\) is equivalent to the characteristic function of the depth difference spectrum, so that the two-body geometry can be directly audited without recovering the phase.
\end{corollary}
\begin{proof}
by proposition \ref{prop:xi-bulk-curvature-exact-deconvolution-operator} have to
\(
\widehat\nu(\xi)=\frac{\sinh(\pi\xi)}{\pi\xi}\widehat p(\xi)
\)。
Take modulo the square and use
\(
\widehat\nu(\xi)=\kappa^{-1}\sum_{j=1}^{\kappa}e^{-i\xi\sigma_j}
\)
Expand to get it. Certification completed.
\end{proof}

\begin{corollary}[Depth spectrum uniqueness at general positions: rigid inversion of point sets from differential multisets]\label{cor:xi-bulk-curvature-difference-multiset-rigid-inversion}
Let the depth spectrum \(\{\sigma_j\}_{j=1}^{\kappa}\subset\RR\) satisfies general location conditions: except \(0\), differential multisets \(\mathcal D=\{\sigma_j-\sigma_k\}_{j,k}\) does not contain heavy elements, that is, for any \(\Delta\neq 0\) have \(m(\Delta)\in\{0,1\}\)。
Then the differential multiset \((\mathcal D,m)\) uniquely determines the unordered multiset \(\{\sigma_j\}\) without transformation of the overall translation and reflection; and the inversion can be realized by a finite step combination algorithm.
combined inference \ref{cor:xi-bulk-curvature-modsquare-holography}, under this general location assumption, modulo the squared data \(|\widehat p(\xi)|^{2}\) is sufficient to lock the depth spectrum to only \(\RR\rtimes\ZZ_2\) of global symmetry degrees of freedom.
\end{corollary}
\begin{proof}[Points of proof]
This is the standard rigid conclusion of the classic turnpike/Golomb ruler inversion problem: the maximum difference gives the distance between the two endpoints, recursively peeling endpoint-compatible differences from the differential multiset, and eliminating branches under the general position assumption, resulting in a unique solution; translation and reflection are the only residual symmetries. Certification completed.
\end{proof}

\begin{corollary}[Super-resolution barrier constant \texorpdfstring{$\pi$}{pi}: The stable threshold of depth separation is \texorpdfstring{$\exp(-\pi/\Delta)$}{exp(-pi/Delta)}]\label{cor:xi-bulk-curvature-superresolution-pi-barrier}
Let the observed probability density of curvature be \(\widetilde p=p+\eta\), and assume that the Fourier noise is in the bandwidth \(|\xi|\le\Omega\) is satisfied within
\[
\sup_{|\xi|\le\Omega}|\widehat\eta(\xi)|\le\varepsilon.
\]
Let the linear deconvolution output be \(\widetilde\nu:=\mathcal D\widetilde p\)。
by proposition \ref{prop:xi-bulk-curvature-exact-deconvolution-operator} It can be seen from the deconvolution multiplier that under the worst-case noise,
\[
\sup_{|\xi|\le\Omega}\bigl|\widehat{(\widetilde\nu-\nu)}(\xi)\bigr|
\ \gtrsim\
\varepsilon\cdot \frac{\sinh(\pi\Omega)}{\pi\Omega}
\ \asymp\
\varepsilon\,e^{\pi\Omega}.
\]
So if you want to measure the degree of separation on the scale axis \(\Delta\) to stably distinguish atoms (usually requires using \(\Omega\gtrsim 1/\Delta\), the noise budget must satisfy the magnitude
\[
\varepsilon \lesssim \exp\!\Bigl(-\frac{\pi}{\Delta}\Bigr),
\qquad\text{Equivalently}\qquad
\Delta \gtrsim \frac{\pi}{\log(1/\varepsilon)}.
\]
This threshold does not depend on \(\kappa\) and \(\{\sigma_j\}\) geometric details, constants \(\pi\) is the universal barrier constant.
\end{corollary}
\begin{proof}[Points of proof]
Depend on \(\widetilde\nu-\nu=\mathcal D\eta\) and the definition of multiplier,
\(
|\widehat{(\widetilde\nu-\nu)}(\xi)|
=\frac{\sinh(\pi|\xi|)}{\pi|\xi|}\,|\widehat\eta(\xi)|
\)。
Select phase-aligned under noise constraints \(\widehat\eta\) can achieve the worst-case amplification factor \(\sinh(\pi\Omega)/(\pi\Omega)\)。
And then with the resolution-bandwidth reciprocal scale \(\Omega\gtrsim 1/\Delta\) can be merged. Certification completed.
\end{proof}

% (User input window)

\begin{remark}[Double insurmountable boundary: the joint limit of atomic number-separation-precision]\label{rem:xi-bulk-curvature-double-barrier-kappa-separation-precision}
In the deep spectrum recovery problem, the degree of separation \(\Delta_{\min}\) is also subject to two mutually exclusive institutional threshold constraints:
\begin{itemize}
\item \textbf{Volume stability threshold (Gram side)}：
If you want the Hellinger--Gram volume not to undergo cluster collapse, you need to enter the separation threshold of the quasi-orthogonal phase (corollary \ref{cor:xi-hellinger-quasi-orthogonal-phase}）。
On the contrary, once \(\Delta_{\min}\) forced to follow \(\kappa\) is reduced, then the clustering condition number is \(\kappa(\kappa-1)\) Power system explosion (corollary \ref{cor:xi-hellinger-gram-cluster-conditioning-exponent}）。
\item \textbf{Discernible threshold (deconvolution side)}：
in degree of separation \(\Delta_{\min}\), the noise budget must satisfy
\(
\varepsilon\lesssim \exp(-\pi/\Delta_{\min})
\)
(inference \ref{cor:xi-bulk-curvature-superresolution-pi-barrier}）。
at fixed span \(L=\sigma_\kappa-\sigma_1\) under the constraints of \(\Delta_{\min}\le L/(\kappa-1)\), so the noise threshold is further compressed to
\[
\varepsilon\lesssim \exp\!\Bigl(-\frac{\pi(\kappa-1)}{L}\Bigr),
\]
That is, the accuracy cost pair \(\kappa\) grows exponentially.
\end{itemize}
therefore within any limits \(\Delta_{\min}\) follow \(\kappa\) Under the reduced budget caliber, the atomic number improvement and stable recovery cannot simultaneously enter the double limit of arbitrary high rank and arbitrary small error; the feasible region is given by \(\Delta_{\min}\) double-threshold common truncation.
\end{remark}

\begin{proposition}[Pole--two decompositions of integer functions: universal lattice singularity and depth factor of the scale curvature generating function]\label{prop:xi-bulk-curvature-moment-generating-factorization}
Define the generating function (two-sided Laplace transform)
\[
M_p(t):=\int_{\RR}e^{ts}p(s)\,\dd s,
\qquad |\Re t|<1.
\]
then there is strict factorization
\[
\boxed{\;
M_p(t)=\frac{\pi t}{\sin(\pi t)}\,E(t),\qquad
E(t):=\int_{\RR}e^{ts}\,\dd\nu(s)=\frac{1}{\kappa}\sum_{j=1}^{\kappa}e^{t\sigma_j}.\;}
\]
in \(\frac{\pi t}{\sin(\pi t)}\) gives all universal singularities: for every non-zero integer \(n\in\ZZ\setminus\{0\}\) has a simple pole;
and \(E\) is an integral function and carries all depth information.
In particular,
\[
\boxed{\;\frac{\sin(\pi t)}{\pi t}\,M_p(t)=E(t)\;}
\]
is the sum of finite exponents, constituting the analytical fingerprint of the depth spectrum.
\end{proposition}
\begin{proof}
Depend on \(p=\phi*\nu\) and Fubini’s theorem (\(|\Re t|<1\) Guarantee is absolutely accumulable), get
\[
M_p(t)=\int_{\RR}\int_{\RR}e^{t(s+u)}\phi(s)\,\dd s\,\dd\nu(u)
=\Bigl(\int_{\RR}e^{ts}\phi(s)\,\dd s\Bigr)\cdot\Bigl(\int_{\RR}e^{tu}\,\dd\nu(u)\Bigr).
\]
right \(\phi(s)=e^{s}(1+e^{s})^{-2}\), as substitution \(x=e^{s}\)（\(\dd s=\dd x/x\))have to
\[
\int_{\RR}e^{ts}\phi(s)\,\dd s
=\int_{0}^{\infty}\frac{x^{t}}{(1+x)^{2}}\,\dd x
=\mathrm{B}(t+1,1-t)
=\Gamma(t+1)\Gamma(1-t)
=\frac{\pi t}{\sin(\pi t)}.
\]
This results in the factorization shown. Certification completed.
\end{proof}

\begin{remark}[scale \texorpdfstring{$\zeta$}{zeta} Notation of interface: parent function is scale \texorpdfstring{$\zeta$}{zeta} function]\label{rem:xi-bulk-curvature-scale-zeta-notation}
parent function \(M_p(t)\) can also be recorded as scale \(\zeta\) interface
\[
\zeta_{\mathrm{scale}}(t):=\int_{\RR}e^{ts}p(s)\,\dd s=M_p(t),
\qquad
|\Re t|<1.
\]
by proposition \ref{prop:xi-bulk-curvature-moment-generating-factorization}，\(\zeta_{\mathrm{scale}}\) has a unique meromorphic continuation;
All its integer lattice poles are given by the universal factor \(\pi t/\sin(\pi t)\) is carried, and the depth information is completely carried by the integral function \(E(t)\) carry.
\end{remark}

\begin{corollary}[Deep Dirichlet data of integer pole residues: algebraic reconstruction of finite local data]\label{cor:xi-bulk-curvature-residue-dirichlet-data}
in proposition \ref{prop:xi-bulk-curvature-moment-generating-factorization} Under the settings, for any \(n\in\NN\) has an explicit residue formula
\[
\boxed{\;
\operatorname*{Res}_{t=n}M_p(t)=(-1)^n\,n\,E(n),\qquad
\operatorname*{Res}_{t=-n}M_p(t)=(-1)^n\,(-n)\,E(-n).\;}
\]
make \(y_j:=e^{\sigma_j}=\delta_j^{-1}\)。
but
\[
E(n)=\frac{1}{\kappa}\sum_{j=1}^{\kappa}y_j^{n}.
\]
like \(\{y_j\}\) are different from each other, then the finite sequence \(\{E(n)\}_{n=1}^{2\kappa}\) (equivalently,\(\{\operatorname*{Res}_{t=\pm n}M_p(t)\}_{n=1}^{2\kappa}\)) uniquely determines the unordered multiset \(\{y_j\}\), thus uniquely determining \(\{\delta_j\}\)。
\end{corollary}
\begin{proof}
Depend on
\(
\frac{\pi t}{\sin(\pi t)}
\)
exist \(t=n\) the residue is \((-1)^n n\), multiplied by the factorized formula to get the residue formula.
The second part is the classic power sum inversion:\(\{y_j\}\) under different conditions, the power sum sequence satisfies the order of \(\kappa\) linear recurrence;
Depend on \(\{E(n)\}_{n=1}^{2\kappa}\) can construct the Hankel system to solve the recursion coefficients, thus obtaining \(\{y_j\}\) is the first polynomial of the roots and recover \(\{y_j\}\), and then restore \(\{\delta_j=y_j^{-1}\}\). Certification completed.
\end{proof}

% (User input window)

\begin{corollary}[Extremal reconstruction of integer residues: lossless readout of maximum depth and multiplicity]\label{cor:xi-bulk-curvature-residue-extremal-recovery}
inferring \ref{cor:xi-bulk-curvature-residue-dirichlet-data} Under the setting of
\[
R_n:=(-1)^n\,\frac{1}{n}\operatorname*{Res}_{t=n}\zeta_{\mathrm{scale}}(t)=E(n)\qquad(n\in\NN).
\]
Record the maximum depth and its multiplicity as
\[
\sigma_{\max}:=\max_{1\le j\le\kappa}\sigma_j,
\qquad
r_{\max}:=\card\{j:\ \sigma_j=\sigma_{\max}\}.
\]
Then there is a strict limit formula
\[
\boxed{\;
\sigma_{\max}=\lim_{n\to\infty}\frac{1}{n}\log\bigl(\kappa R_n\bigr),\qquad
r_{\max}=\lim_{n\to\infty}\kappa R_n\,e^{-n\sigma_{\max}}\in\NN.\;}
\]
Therefore, the "deepest defect" and its multiplicity can be directly certified from the residue sequence of integer poles without full spectrum inversion.
\end{corollary}
\begin{proof}[Points of proof]
Depend on \(R_n=E(n)=\kappa^{-1}\sum_{j=1}^{\kappa}e^{n\sigma_j}\), separate the maximum depth term to get
\[
\kappa R_n=r_{\max}e^{n\sigma_{\max}}+\sum_{\sigma_j<\sigma_{\max}}e^{n\sigma_j}
=r_{\max}e^{n\sigma_{\max}}\Bigl(1+O(e^{-n(\sigma_{\max}-\sigma_2)})\Bigr),
\]
in \(\sigma_2\) is the second largest depth (if it does not exist, the error is regarded as \(0\)）。
Take the logarithm of both sides and divide by \(n\) to get \(\sigma_{\max}\); then multiply the above equation by \(e^{-n\sigma_{\max}}\) is obtained by taking the limit \(r_{\max}\). Certification completed.
\end{proof}

\begin{remark}[Standard Euler--Möbius primitive ledger: interchange of powers and moments and primitive sequences]\label{rem:xi-bulk-curvature-euler-mobius-ledger}
make \(y_j:=e^{\sigma_j}=\delta_j^{-1}\), and remember the power sum
\(
p_n:=\sum_{j=1}^{\kappa}y_j^{n}=\kappa E(n)
\)（\(n\in\NN\)）。
Introducing formal power series
\[
\mathfrak Z_{\mathrm{scale}}(z)
:=\prod_{j=1}^{\kappa}\frac{1}{1-y_j z}
=\exp\Bigl(\sum_{n\ge 1}\frac{p_n}{n}z^{n}\Bigr)
\qquad(\text{formal meaning}).
\]
and define its primitive coefficients (Möbius inversion)
\[
\pi^{\mathrm{prim}}_n
:=\frac{1}{n}\sum_{d\mid n}\mu(d)\,p_{n/d}
=\frac{1}{n}\sum_{d\mid n}\mu(d)\sum_{j=1}^{\kappa}y_j^{n/d}.
\]
Then there is an equivalent Euler decomposition
\[
\mathfrak Z_{\mathrm{scale}}(z)=\prod_{n\ge 1}\frac{1}{(1-z^{n})^{\pi^{\mathrm{prim}}_n}}
\qquad(\text{formal meaning}),
\]
thereby \(\{E(n)\}\)(or \(\{p_n\}\)) gives the "total moment" ledger, and \(\{\pi^{\mathrm{prim}}_n\}\) gives its Euler--Möbius primitive decomposition; the two can be transformed into each other by finite-step algebraic operations.
\end{remark}

\begin{proposition}[The dominant law of the index of the primitive ledger: the maximum depth of locking the primitive index rate]\label{prop:xi-bulk-curvature-primitive-max-depth-dominance}
Inheritance note \ref{rem:xi-bulk-curvature-euler-mobius-ledger} symbol, and assume
\[
\Lambda:=\max_{1\le j\le\kappa}y_j,\qquad
r:=\card{\{j:\ y_j=\Lambda\}},
\qquad
\Lambda_2:=\max\{y_j:\ y_j<\Lambda\}\ \ (\text{if exists}).
\]
like \(\Lambda>1\), then for \(n\to\infty\) have
\[
\boxed{\;
\pi^{\mathrm{prim}}_n=\frac{r}{n}\Lambda^n
+O\!\Bigl(\frac{1}{n}\Lambda^{n/2}\Bigr)
+O\!\Bigl(\frac{1}{n}\Lambda_2^{\,n}\Bigr)\;}
\]
(The second item is only available in \(n\) appears when it contains non-trivial factors, and is \(\Lambda^{n/2}\) is the worst case magnitude).
Thus the exponential rate is uniquely locked by the maximum depth:
\[
\boxed{\;\lim_{n\to\infty}\frac{1}{n}\log\bigl|n\pi^{\mathrm{prim}}_n\bigr|=\log\Lambda.\;}
\]
\end{proposition}
\begin{proof}
by definition \(n\pi^{\mathrm{prim}}_n=\sum_{d\mid n}\mu(d)\,p_{n/d}\) and \(p_m=\sum_{j}y_j^m\),have to
\[
p_n=r\Lambda^n+O(\Lambda_2^{\,n}),
\]
in \(\Lambda_2\) does not exist, you can \(O(\Lambda_2^{\,n})\) is understood as \(0\)。
On the other hand, for any \(d\mid n\) and \(d\ge 2\) have \(n/d\le n/2\),thereby
\[
|p_{n/d}|\le \sum_{j=1}^{\kappa}y_j^{n/d}\le \kappa\,\Lambda^{n/d}\le \kappa\,\Lambda^{n/2}.
\]
Will \(d=1\) term is separated from the remaining terms to obtain the expansion shown.
Finally, by \(\Lambda>1\) can be known \(\Lambda^{n/2}=o(\Lambda^n)\) and \(\Lambda_2^{\,n}=o(\Lambda^n)\), so \(n\pi^{\mathrm{prim}}_n\) has an exponential growth rate of \(\log\Lambda\). Certification completed.
\end{proof}

\begin{remark}[Hidden Circular Spectral Geometry: Deconvolution Multipliers and Circular Laplacian \texorpdfstring{$\zeta$}{zeta}-determinant ratio]\label{rem:xi-bulk-curvature-circle-zeta-determinant}
make \(L=-\partial_\theta^2\) is a circle \(S^1\) on Laplacian, its canonical score is \(\{n^2\}_{n\ge1}\)。
That \(\zeta\)-normalized determinant ratio satisfies
\[
\frac{\det_\zeta(L+\xi^{2})}{\det_\zeta(L)}
=\prod_{n\ge1}\Bigl(1+\frac{\xi^{2}}{n^{2}}\Bigr)
=\frac{\sinh(\pi|\xi|)}{\pi|\xi|}.
\]
Therefore the proposition \ref{prop:xi-bulk-curvature-exact-deconvolution-operator} The deconvolution multiplier of is exactly equivalent to the circular Laplacian of \(\zeta\)-determinant ratio;
constant \(\pi\) corresponds to the projection of this hidden circular spectrum geometry onto the scale axis.
\end{remark}

\begin{proposition}[Scale coherence kernel and scale volume potential: short-range Vandermonde repulsion and long-range exponential orthogonalization]\label{prop:xi-bulk-curvature-scale-gram-two-phase}
inferring \ref{cor:xi-bulk-curvature-probability-moments} Under the setting of , define the standard coherence kernel
\[
K(\Delta):=\int_{\RR}\phi(s)\phi(s-\Delta)\,\dd s=(\phi*\phi)(\Delta).
\]
but \(K\) is an even function and has a closed form
\[
\boxed{\;
K(\Delta)=\frac{|\Delta|\coth\!\bigl(|\Delta|/2\bigr)-2}{4\sinh^{2}\!\bigl(|\Delta|/2\bigr)}\; } ,
\qquad
K(0)=\frac{1}{6}.
\]
scale spectrum \(\{\sigma_j\}_{j=1}^{\kappa}\) defines Gram matrix
\(
G^{\mathrm{scale}}_{jk}:=K(\sigma_j-\sigma_k)
\)
and scale volume potential
\(
S_{\mathrm{scale}}:=-\log\det G^{\mathrm{scale}}
\)。
Then there exists a limit structure at both ends:
When the minimum separation \(\Delta_{\min}:=\min_{j\neq k}|\sigma_j-\sigma_k|\to\infty\) hour,
\[
\det G^{\mathrm{scale}}=(K(0))^{\kappa}\bigl(1+o(1)\bigr)=\Bigl(\frac16\Bigr)^{\kappa}\bigl(1+o(1)\bigr),
\qquad
S_{\mathrm{scale}}=\kappa\log 6+o(1),
\]
And the core tail satisfies \(K(\Delta)\sim (|\Delta|-2)e^{-|\Delta|}\), so that the non-diagonal coupling is \(e^{-|\Delta|}\) exponential annihilation.
On the other hand, when \(\sigma_j=\sigma_\star+\varepsilon t_j\) and \(\varepsilon\downarrow0\)（\(t_j\) when each pair is different),
\[
S_{\mathrm{scale}}
=\kappa(\kappa-1)\log\frac{1}{\varepsilon}
+2\sum_{1\le j<k\le \kappa}\log\frac{1}{|t_k-t_j|}
+O(1),
\]
Exhibits Vandermonde-type logarithmic repulsion.
\end{proposition}
\begin{proof}
The closed form is calculated by direct integration: for \(\phi(s)=\frac{1}{4\cosh^{2}(s/2)}\) make \(s=2u\), transformed into
\(
K(\Delta)=\frac18\int_{\RR}\frac{1}{\cosh^{2}(u)\cosh^{2}(u-\Delta/2)}\,\dd u
\), and then integrate the hyperbolic function identity to get the expression shown;\(K(0)=\int \phi^2=\frac16\) Depend on \(\int_{\RR}\cosh(u)^{-4}\dd u=\frac43\) get.
\par
long range dependence \(G^{\mathrm{scale}}=K(0)I+O(\max_{j\neq k}K(\sigma_j-\sigma_k))\) is derived from the continuity of the determinant; the tail term is asymptotically derived from the closed form in \(|\Delta|\to\infty\) when expanded.
\par
The short-range phase can be represented by the translational family \(\phi(\cdot-\sigma_\star-\varepsilon t_j)\) exist \(L^2\) middle pair \(\varepsilon\)'s Taylor expansion yields: The family is in \(\varepsilon\to0\) converges to the derivative tower when \(\{\phi^{(r)}(\cdot-\sigma_\star)\}_{r=0}^{\kappa-1}\) of Zhang Cheng;
The first nonzero term of the Gram determinant is given by the confluent Vandermonde structure, so that \(\varepsilon^{\kappa(\kappa-1)}\prod_{j<k}(t_k-t_j)^2\) scale law.
Taking the logarithm gives the expansion shown. Certification completed.
\end{proof}

\begin{proposition}[Analytical fingerprint of depth span: the zero point density of the integer function after pole removal gives the span]\label{prop:xi-bulk-curvature-depth-span-zero-density}
in proposition \ref{prop:xi-bulk-curvature-moment-generating-factorization} Under the setting of
\[
E(t)=\frac{\sin(\pi t)}{\pi t}\,M_p(t)=\frac{1}{\kappa}\sum_{j=1}^{\kappa}e^{t\sigma_j}.
\]
Let the depth span
\(
L:=\max_j\sigma_j-\min_j\sigma_j
\)。
set up \(N_E(R)\) for \(E\) on the disc \(\{t\in\CC:\ |t|\le R\}\), then there is a limit
\[
\boxed{\;\lim_{R\to\infty}\frac{N_E(R)}{R}=\frac{L}{\pi}.\;}
\]
therefore \(L\) can be determined by the parent function \(M_p\) After removing the universal lattice factor, only the integral function \(E\) zero-point density lossless readout.
\end{proposition}
\begin{proof}[Points of proof]
factor \(e^{t\min_j\sigma_j}\) without changing the zero point set, you can \(E\) into the form of \(\sum_{j}c_j e^{a_j t}\) is an exponential polynomial (\(0=a_1<\cdots<a_\kappa=L\)）。
This type of function is a Cartwright type integer function, and its indicator graph is a line segment on the real axis. \([0,L]\), according to Cartwright’s density theorem, the zero point count satisfies \(N_E(R)\sim (L/\pi)R\). Certification completed.
\end{proof}

\begin{corollary}[Gaussian regularization for stable inversion: a universal threshold for the smallest discernible scale]\label{cor:xi-bulk-curvature-gaussian-regularization-threshold}
Introduce Gaussian regularization \(\nu_\sigma:=G_\sigma*\nu\) (Fourier multiplier \(e^{-\sigma^{2}\xi^{2}/2}\)）。
then by proposition \ref{prop:xi-bulk-curvature-exact-deconvolution-operator} Get the linear recovery operator
\[
\widehat{\nu_\sigma}(\xi)=e^{-\sigma^{2}\xi^{2}/2}\frac{\sinh(\pi\xi)}{\pi\xi}\,\widehat p(\xi).
\]
Its noise amplification factor satisfies
\[
\mathcal A_\sigma(\xi):=e^{-\sigma^{2}\xi^{2}/2}\frac{\sinh(\pi|\xi|)}{\pi|\xi|}
\asymp
\exp\!\Bigl(\pi|\xi|-\frac{\sigma^{2}\xi^{2}}{2}\Bigr),
\]
And in \(|\xi|\approx \pi/\sigma^{2}\) reaches the maximum value at \(\asymp \exp(\pi^{2}/(2\sigma^{2}))\)。
Therefore in the worst case Fourier noise budget \(\varepsilon\), if you want the output noise to remain at a constant level (same magnitude budget), you must select
\[
\boxed{\;\sigma\ \gtrsim\ \frac{\pi}{\sqrt{2\log(1/\varepsilon)}}.\;}
\]
\end{corollary}
\begin{proof}
Expressed by the multiplier, it can be seen that the upper bound of the output noise is \(\sup_\xi \mathcal A_\sigma(\xi)\cdot\varepsilon\)。
The maximum value of the exponential term is given by the unary function \(\pi|\xi|-\sigma^{2}\xi^{2}/2\) maximum point \(|\xi|=\pi/\sigma^{2}\) is given, corresponding to the exponential gain \(\exp(\pi^{2}/(2\sigma^{2}))\)；
make \(\varepsilon\exp(\pi^{2}/(2\sigma^{2}))\lesssim 1\) to get the threshold shown. Certification completed.
\end{proof}

\begin{corollary}[Bit complexity lower bound: linear threshold for resolution and precision bits]\label{cor:xi-bulk-curvature-bit-complexity-lower-bound}
Let the observed probability density of curvature be \(\widetilde p=p+\eta\), and assume that it is in the frequency domain bandwidth \(|\xi|\le \Omega\) has \(B\) bit accuracy, that is
\(
\sup_{|\xi|\le\Omega}|\widehat\eta(\xi)|\le 2^{-B}
\)。
make \(\widetilde\nu:=\mathcal D\widetilde p\)。
then by proposition \ref{prop:xi-bulk-curvature-exact-deconvolution-operator} It can be seen that under the worst-case noise
\[
\sup_{|\xi|\le\Omega}\bigl|\widehat{(\widetilde\nu-\nu)}(\xi)\bigr|
\ \gtrsim\
2^{-B}\,\frac{\sinh(\pi\Omega)}{\pi\Omega}
\ \asymp\
2^{-B}e^{\pi\Omega}.
\]
To achieve scale resolution \(\Delta\) (usually required \(\Omega\gtrsim 1/\Delta\)), then the necessary number of digits of precision satisfies the magnitude
\[
\boxed{\;B\ \gtrsim\ \frac{\pi}{\Delta\ln 2}\;}
\qquad(\text{Ignore logarithmic secondary terms}).
\]
\end{corollary}
\begin{proof}[Points of proof]
Select Phase Aligned for Noise Constraints \(\widehat\eta\) can achieve the worst-case amplification factor \(\sinh(\pi\Omega)/(\pi\Omega)\)。
again \(\Omega\gtrsim 1/\Delta\) can be converted to get the digit threshold. Certification completed.
\end{proof}

\begin{remark}[Same-order reachability of barrier constants: Coarse deconvolution and Prony reconstruction give an executable chain]\label{rem:xi-bulk-curvature-superresolution-achievability}
Under finite atomic aperture, once the noise budget satisfies the corollary \ref{cor:xi-bulk-curvature-bit-complexity-lower-bound} The linear bit threshold of
The inversion can be implemented as an explicit executable chain: bandwidth first \(|\xi|\le \Omega\) is applied within the deconvolution multiplier (proposition \ref{prop:xi-bulk-curvature-exact-deconvolution-operator} or its finite-order truncated version, conclusion \ref{con:xi-terminal-scale-kernel-partial-deconvolution-resolution}），
get \(\widehat\nu\) band-limited approximation;
Then, under general position and minimal separation assumptions, low-rank spectral inversion such as standard Prony/Matrix Pencil/ESPRIT can be used to stably recover the atomic positions by finite sampling (and conclusion \ref{con:xi-terminal-atomic-defect-hankel-prony} Hankel--Prony caliber same type).
Therefore the constant \(\pi/(\ln 2)\The linear slope given by ) not only gives the necessary barrier but is also reachable by the construction chain at the same order (the difference only appears in the logarithmic secondary term).
\end{remark}

\begin{remark}[\texorpdfstring{$(q=4)$}{(q=4)} Unified threshold for: Fluctuations, deconvolution and the first visible correction order of two-body volumes]\label{rem:xi-scale-q4-threshold-principle}
Note \ref{rem:xi-bulk-entropy-fermi-grand-canonical} Write the bulk entropy curvature as the Bernoulli sum of variances;
In a regime based on a second-order cumulant (Gaussian approximation), its first systematic deviation is dominated by a fourth-order cumulant.
On the other hand, the conclusion \ref{con:xi-terminal-scale-kernel-gamma-closed-form} The small frequency expansion of shows the deconvolution multiplier \(\log M_N(\xi)\) is first modified by \(\zeta(4)\) gives,
Therefore, in the bandwidth-controlled coarse deconvolution system, the fourth order is also the minimum nonlinear correction order.
Furthermore, the divergence dictionary of the logistic translation family satisfies \(D_{\mathrm{KL}}=4H^2+O(\Delta^4)\)(Note \ref{rem:xi-logistic-fourth-order-closure}），
The short-range expansion of the Hellinger-Gram volume potential is also \(O(\Delta^4)\) term gives the first superquadratic deviation (proposition \ref{prop:xi-logistic-hellinger-gram-determinant}）。
Therefore, in the scale holographic interface of this article, the fourth order constitutes the common closed level of "the first non-negligible structural deviation".
\end{remark}

