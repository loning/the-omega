\paragraph{Audit summary (hydrogen 21cm $\mu\to r\to m_{\mathrm{eff}}$).} \AuditTag We map the 21cm hyperfine splitting energy (a composite low-energy scale) from the vendored miniset to $\mu$ in GeV and then to $r(\mu)=\log(\mu/m_e)/\log\varphi$ using $m_e=\texttt{M\_E\_GEV}$. We report the staircase label $m_{\mathrm{eff}}(\mu)=6+\lfloor r(\mu)/r_{\mathrm{step}}\rfloor$ with $r_{\mathrm{step}}=2\pi$ (Section~\ref{sec:falsifiability_predictions}). Interpretation boundary: $m_{\mathrm{eff}}<6$ is recorded only as a scale label for a composite effect.