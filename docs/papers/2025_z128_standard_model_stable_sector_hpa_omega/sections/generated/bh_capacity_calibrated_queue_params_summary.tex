\paragraph{Capacity-calibrated queue parameters for known black holes (audit).} \AuditTag This fragment combines the matching-layer CAP calibration $M\mapsto(m^\*(M),n^\*(M))$ (Appendix~\ref{app:bh_planck_capacity_calibration}) with the protocol queue model at the selected window length $m^\*(M)$. The addressing scale $n^\*(M)$ is reported as a capacity/screen indicator but is not simulated explicitly here. The queue model is evaluated under the BH-like absorption mode \texttt{cyclic\_only}.