\section{附录 A：记号与层级审计表}
\label{app:audit-table}

\subsection{层级约定}

\begin{itemize}
  \item \textbf{定义：}引入对象、映射与量。
  \item \textbf{可证明定理：}在给定定义与明确假设下可演绎证明。
  \item \textbf{模型类假设：}选择具体实现（如黄金域、尺度相容图）以获得更强结构或可计算性。
  \item \textbf{解释层命题：}将形式对象映射为物理语义或哲学语义，不进入定理链。
\end{itemize}

\subsection{核心对象表}

\begin{itemize}
  \item $L=\ZZ^6$：六维晶格。
  \item $E_{\parallel},E_{\perp}$：三维物理/内部子空间。
  \item $W\subset E_{\perp}$：窗口。
  \item $u\in E_{\perp}$：内部相位（观察者实现参数）。
  \item $\Lambda(u)$：切投影点集。
  \item $\theta_{t+1}=\theta_t+\alpha$：一维旋转。
  \item $w_t\in\{0,1\}$：二值读出机械词。
  \item $X_m^{\mathrm Z}$：无相邻 $1$ 的合法集合。
  \item $\mathrm{Fold}_m$：稳定化核。
  \item $\mathcal{F}_m(x)$：预像纤维。
  \item $h_\mu(T)$：度量熵。
\end{itemize}

