\subsubsection{arity-charge 的大偏差率函数（\texorpdfstring{$\chi$}{chi} 切片）}\label{subsec:chi-ldp}
沿 $\chi$-切片 $\theta(t)=(t,0,-t)$，压力函数 $P_\chi(t)=\log\rho(M_{\theta(t)})$ 给出 $\chi$ 的指数型倾斜族；相应的大偏差率函数可由 Legendre 对偶直接读出。

\begin{remark}[标准定理接口：Gärtner--Ellis]\label{thm:chi-gartner-ellis}
设 $M_{\theta(t)}$ 在 $t$ 的某邻域内原始，且 $P_\chi(t)=\log\rho(M_{\theta(t)})$ 在该邻域可微。
令
\[
I_\chi(\alpha):=\sup_{t\in\RR}\bigl(t\alpha-P_\chi(t)\bigr).
\]
则对倾斜平衡态下的经验均值 $\bar\chi_n:=\chi(\gamma)/n$，大偏差率函数由 $I_\chi$ 给出（Gärtner--Ellis；参见 \cite{DemboZeitouni2010LargeDeviations}）。
并且在典型值 $\bar\alpha=P_\chi'(0)$ 处满足二次近似
\[
I_\chi(\bar\alpha+\delta)=\frac{\delta^2}{2P_\chi''(0)}+o(\delta^2).
\]
\end{remark}

\begin{remark}[与 Abel 有限部常数曲线的同一切片对比]
$P_\chi(t)$ 控制 leading 指数尺度（典型密度、涨落与大偏差），而 $\log\mathfrak{M}_\chi(t)$ 控制 prime-orbit Mertens 主项中的常数级漂移：
\[
\sum_{n\le N}\frac{p_{n,\chi}(t)}{\lambda_\chi(t)^n}=\log N+\gamma+\log\mathfrak{M}_\chi(t)+o(1),
\qquad
\lambda_\chi(t)=e^{P_\chi(t)}.
\]
因此在同一切片上同时记录 $t\mapsto(P_\chi(t),\log\mathfrak{M}_\chi(t))$，可把“指数尺度的代价”（由 $P_\chi$ 给出）与“常数级漂移的再分配”（由 $\log\mathfrak{M}_\chi$ 给出）作为两种互补指纹并列比较。
\end{remark}

\paragraph{内生时间尺度（无外部输入）：\texorpdfstring{$\chi$}{chi} 切片的相关时间}
本小节的 $t$ 参数不仅控制指数倾斜的静态代价（$P_\chi(t)$）与常数级漂移（$\log\mathfrak{M}_\chi(t)$），也同时诱导一条\textbf{纯核内生}的时间尺度：在 $\theta(t)=(t,0,-t)$ 的平衡态下，arity-charge 观测量的时间相关以谱隙速率指数衰减。
为避免与角色投影 $P_\chi$ 的混合时间 $\tau_{\mathrm{mix}}$（注 \ref{rem:pom-chi-mixing}）混淆，这里把该尺度记为 $\tau_{\mathrm{corr}}$。

\begin{remark}[\texorpdfstring{$\chi$}{chi} 切片的相关时间（谱隙口径）]\label{prop:chi-slice-time-correlation}
设真实输入 40 状态核在 $\theta(t)=(t,0,-t)$ 下的加权矩阵为 $M_{\theta(t)}$，并假设在给定 $t$ 的邻域内矩阵族原始。
定义
\[
\lambda_1(t):=\rho\bigl(M_{\theta(t)}\bigr),
\qquad
\Lambda(t):=\max\{ |\lambda|:\ \lambda\in\mathrm{spec}(M_{\theta(t)}),\ |\lambda|<\lambda_1(t)\},
\qquad
\rho_{\mathrm{corr}}(t):=\frac{\Lambda(t)}{\lambda_1(t)}\in[0,1),
\]
以及相关时间
\[
\tau_{\mathrm{corr}}(t):=\frac{1}{-\log \rho_{\mathrm{corr}}(t)}.
\]
对有限状态原始马尔可夫链，中心化有限窗口可观测量的时间相关满足指数衰减包络
\(
|\mathrm{Cov}(\chi_0,\chi_n)|\le C(t)\,\rho_{\mathrm{corr}}(t)^n
\)，其中 $C(t)<\infty$（标准谱隙结论；参见 \cite{LevinPeresWilmer2009MarkovMixing}）。
本文仅使用其“由有限维谱读出指数包络”的可审计接口。
\end{remark}

\input{sections/generated/fig_real_input_40_time_correlation}

\begin{remark}[黄金点的闭式相关时间（真实输入 40 状态核）]\label{cor:chi-slice-t0-golden-tau}
对真实输入 40 状态核在 $t=0$ 的 $\chi$ 切片平衡态，谱隙比满足
\[
\rho_{\mathrm{corr}}(0)=\frac{\Lambda(0)}{\lambda_1(0)}=\varphi^{-1},
\qquad
\tau_{\mathrm{corr}}(0)=\frac{1}{-\log\rho_{\mathrm{corr}}(0)}=\frac{1}{\log\varphi}.
\]
该闭式与图 \ref{fig:real-input-40-time-correlation} 的 $t=0$ 读出一致。
\end{remark}

\paragraph{事件时钟与时间重标定（无外部输入）}
在 $t=0$ 时，平衡态退化为未加权核的 Parry 测度，因此同一条平衡态轨道上可以引入一个\textbf{独立于 $\chi$ 的“事件时钟”}：令 $e_t\in\{0,1\}$ 表示真实输入核在第 $t$ 步输出位（见附录中对输出位势的定义口径），并定义事件时间
\[
T_n:=\sum_{t=0}^{n-1} e_t.
\]
则由输出位势的压力函数 $P_e(\theta)=\log\lambda(e^{\theta})$，其均值密度为 $\mu_e:=\mathbb{E}[e_0]=P_e'(0)$（闭式可审计，见表 \ref{tab:real_input_40_output_potential_cumulants_closed}）。
将命题 \ref{prop:chi-slice-time-correlation} 的“步数时间”相关尺度 $\tau_{\mathrm{corr}}(0)$ 通过 $T_n\approx \mu_e n$ 换元，即得到以事件时间计量的等价尺度 $\tau_{\mathrm{corr}}^{(e)}:=\mu_e\tau_{\mathrm{corr}}(0)$。
表 \ref{tab:real-input-40-event-time-dilation} 给出该换元在 $t=0$ 的数值化读出，作为“内生时间单位选择”对尺度的可审计重标定。

\input{sections/generated/tab_real_input_40_event_time_dilation}

\begin{remark}[事件时钟下的尺度分离放大（恒等式）]\label{cor:chi-event-clock-amplifies-separation}
在 $t=0$ 的 Parry 平衡态下，令事件时钟 $T_n=\sum_{t=0}^{n-1}e_t$，并令 $\mu_e:=\mathbb{E}[e_0]=P_e'(0)$。
将步数时间相关尺度 $\tau_{\mathrm{corr}}(0)$ 重标定为事件时间尺度
\[
\tau_{\mathrm{corr}}^{(e)}:=\mu_e\,\tau_{\mathrm{corr}}(0).
\]
则角色扭曲混合尺度 $\tau_{\mathrm{mix}}$ 与事件时间相关尺度的比值满足恒等式
\[
\frac{\tau_{\mathrm{mix}}}{\tau_{\mathrm{corr}}^{(e)}}
=\frac{\tau_{\mathrm{mix}}}{\tau_{\mathrm{corr}}(0)}\cdot\frac{1}{\mu_e}.
\]
对真实输入 40 状态核，表 \ref{tab:real-input-40-event-clock-vs-tau-mix} 给出在 $((3,3,5))$ 最坏扭曲处该比值的数值读出，显示事件时钟重标定并不能消除角色扭曲的混合瓶颈，反而将两者的分离进一步放大为一个数量级以上的可审计差异。
\end{remark}

\input{sections/generated/tab_real_input_40_event_clock_vs_tau_mix}

\begin{remark}[事件时钟的有限样本偏差证书（二阶矩接口）]\label{prop:chi-event-clock-deviation-certificate}
在 $t=0$ 的 Parry 平衡态下，令 $e_t\in\{0,1\}$ 为输出位，$T_n:=\sum_{t=0}^{n-1}e_t$，并记 $\mu_e:=\mathbb{E}[e_0]$。
将一步输出位视为有限状态 Parry 马尔可夫链上的有界可观测量，则协方差序列
\(
\mathrm{Cov}(e_0,e_k)
\)
与部分和方差
\(
\mathrm{Var}(T_n)
\)
均可由有限维转移矩阵 $P$ 的幂与内积\emph{精确}计算。
进一步地，令
$$
S_n:=\sum_{t=1}^{n}\mathrm{Var}(T_t),\qquad b_n(\delta):=\sqrt{\frac{S_n}{\delta}}.
$$
则由并合界与 Chebyshev 不等式得到可审计的统一偏差证书：对任意 $\delta\in(0,1)$ 与任意 $n\ge 1$，有
$$
\mathbb{P}\Bigl(\max_{1\le t\le n}|T_t-\mu_e t|\ge b_n(\delta)\Bigr)\le \delta.
$$
表 \ref{tab:real-input-40-event-clock-deviation-certificate} 给出对真实输入 40 状态核的 $(\mu_e,\mathrm{Var}(T_n),S_n,b_n)$ 的数值化读出；由于其输入仅为核的有限状态线性代数对象，故该证书在本文口径下可完全再现。
\end{remark}

\begin{remark}[更强指数型证书的外部定理接口]
命题 \ref{prop:chi-event-clock-deviation-certificate} 采用二阶矩与并合给出统一界，其优点是无需任何额外假设（例如可逆性）且全部量可由有限维线性代数闭式审计。
若进一步希望得到与混合强度相关的\emph{指数型}尾界，可将 $T_n$ 视为有限状态马尔可夫链的加性泛函，并调用马尔可夫链浓缩不等式（例如 \cite{Paulin2015MarkovConcentration} 的 McDiarmid 型界；在可逆链情形亦可用最优 Hoeffding 型界 \cite{LeonPerron2004OptimalHoeffding}）。
这些外部结果可把 $|T_n-\mu_e n|$ 与对应击中时间 $\tau_k:=\inf\{n:\,T_n\ge k\}$ 的偏差统一写为谱隙/混合参数控制的显式指数尾，从而把“事件时钟重标定”的误差预算进一步收紧到可与 $\tau_{\mathrm{mix}}$ 同尺度比较的形式。
\end{remark}

\input{sections/generated/tab_real_input_40_event_clock_deviation_certificate}

\begin{remark}[事件时间的粗换时：指数衰减率的 $\mu_e$ 重标定]\label{lem:chi-event-time-rescaling}
在命题 \ref{prop:chi-slice-time-correlation} 的设定下，设某中心化有限窗口可观测量的步数时间相关满足指数包络
$$
\bigl|\mathrm{Cov}(F,\,G\circ\sigma^n)\bigr|\le C\,\rho^n\qquad (n\ge 0),
$$
其中 $\rho\in(0,1)$。
在 $t=0$ 的 Parry 平衡态下引入事件时钟 $T_n=\sum_{s=0}^{n-1}e_s$ 并记 $\mu_e=\mathbb{E}[e_0]>0$。
定义一个仅依赖均值密度的“事件时间近似换时”
$$
\widetilde\tau(k):=\left\lfloor\frac{k}{\mu_e}\right\rfloor\qquad (k\in\mathbb{N}).
$$
则对应的事件时间相关包络满足
$$
\bigl|\mathrm{Cov}(F,\,G\circ\sigma^{\widetilde\tau(k)})\bigr|
\le C\,\rho^{\lfloor k/\mu_e\rfloor}
\le C\,\exp\!\left(-\frac{-\log\rho}{\mu_e}\,k\right).
$$
该结论只使用“步数时间的指数包络”与事件密度 $\mu_e$，从而把步数时间尺度 $\tau_{\mathrm{corr}}=1/(-\log\rho)$ 纯粹地重标定为事件时间尺度 $\tau_{\mathrm{corr}}^{(e)}=\mu_e\tau_{\mathrm{corr}}$。
作为背景：对有限状态可逆马尔可夫链，上述指数包络可由谱隙形式的协方差衰减直接推出（参见 \cite[Chapter~12]{LevinPeresWilmer2009MarkovMixing}）。
\end{remark}

\begin{remark}[击中时间 $\tau(k)$ 的可审计偏差证书（方差型）]
\label{prop:chi-event-clock-hitting-time-certificate}
在 $t=0$ 的 Parry 平衡态下，令 $e_t\in\{0,1\}$ 为输出位、$T_n:=\sum_{t=0}^{n-1}e_t$、$\mu_e:=\mathbb{E}[e_0]>0$，并定义击中时间
$$
\tau(k):=\inf\{n\ge 1:\,T_n\ge k\}\qquad (k\in\mathbb{N}).
$$
对任意 $\delta\in(0,1)$，以及任意固定的 $k\in\mathbb{N}$，定义两侧证书点
$$
n_-(k,\delta):=\max\Bigl\{n\ge 1:\ k-\mu_e n>0\ \text{且}\ \frac{\mathrm{Var}(T_n)}{(k-\mu_e n)^2}\le\delta\Bigr\},
$$
$$
n_+(k,\delta):=\min\Bigl\{n\ge 1:\ \mu_e n-k>0\ \text{且}\ \frac{\mathrm{Var}(T_n)}{(\mu_e n-k)^2}\le\delta\Bigr\}.
$$
则由 Chebyshev 不等式与恒等式 $\mathbb{P}(\tau(k)>n)=\mathbb{P}(T_n<k)$，有
$$
\mathbb{P}\bigl(\tau(k)\le n_-(k,\delta)\bigr)\le\delta,
\qquad
\mathbb{P}\bigl(\tau(k)>n_+(k,\delta)\bigr)\le\delta,
$$
从而由并合界得到
$$
\mathbb{P}\bigl(n_-(k,\delta)<\tau(k)\le n_+(k,\delta)\bigr)\ge 1-2\delta.
$$
表 \ref{tab:real-input-40-hitting-time-deviation-certificate} 给出若干 $k$ 的可复现数值证书。
\end{remark}

\input{sections/generated/tab_real_input_40_hitting_time_deviation_certificate}

\begin{remark}[击中时间 $\tau(k)$ 的精确分位数区间（无近似）]
\label{prop:chi-event-clock-hitting-time-exact-quantiles}
在命题 \ref{prop:chi-event-clock-hitting-time-certificate} 的设定下，固定 $N\ge 1$。
将 $T_n$ 的分布视为有限状态马尔可夫链上有界边可观测量的加性泛函，则对每个 $n\le N$，$T_n$ 的精确分布可由状态空间上的递推
$$
\mathbb{P}(X_{t+1}=j,\,T_{t+1}=m) = \sum_i \mathbb{P}(X_t=i,\,T_t=m-e(i\to j))\,P_{ij}
$$
（并约定 $m-e(i\to j)<0$ 时该项为 $0$）在有限步内完全计算。
因此对任意 $k\in\{1,\dots,N\}$，击中时间 $\tau(k)=\inf\{n\ge 1:\,T_n\ge k\}$ 的分布函数满足恒等式
$$
\mathbb{P}(\tau(k)\le n)=\mathbb{P}(T_n\ge k),
$$
从而可在离散 $n=1,\dots,N$ 上给出任意分位数的\emph{精确}整数解。
表 \ref{tab:real-input-40-hitting-time-exact-quantiles} 报告若干 $k$ 的 $[q_{\mathrm{lo}},q_{\mathrm{hi}}]$ 分位数区间与参照点 $k/\mu_e$ 的最大偏差，作为“事件时间严格换时误差”在本文口径下的可审计证书。
\end{remark}

\input{sections/generated/tab_real_input_40_hitting_time_exact_quantiles}

\begin{remark}[事件时间指数包络的误差预算：从 $k/\mu_e$ 到 $\tau(k)$]
\label{cor:chi-event-time-exponential-envelope-error-budget}
在引理 \ref{lem:chi-event-time-rescaling} 的设定下，取 $t=0$ 且令 $\rho:=\rho_{\mathrm{corr}}(0)=\varphi^{-1}$。
对任意 $k\in\mathbb{N}$，令 $\tau(k)=\inf\{n\ge 1:\,T_n\ge k\}$ 为事件时间的严格换时。
对给定分位数 $0<q_{\mathrm{lo}}<q_{\mathrm{hi}}<1$，令 $n_{\mathrm{lo}}(k),n_{\mathrm{hi}}(k)$ 分别为满足
$$
\mathbb{P}(\tau(k)\le n_{\mathrm{lo}}(k))\ge q_{\mathrm{lo}},
\qquad
\mathbb{P}(\tau(k)\le n_{\mathrm{hi}}(k))\ge q_{\mathrm{hi}}
$$
的最小整数解。
则由 $\rho\in(0,1)$ 的单调性，得到乘性误差预算：在概率至少 $q_{\mathrm{hi}}-q_{\mathrm{lo}}$ 的事件上，
$$
\rho^{\tau(k)}\in\Bigl[\rho^{n_{\mathrm{hi}}(k)},\,\rho^{n_{\mathrm{lo}}(k)}\Bigr]
=\rho^{k/\mu_e}\cdot\Bigl[\rho^{n_{\mathrm{hi}}(k)-k/\mu_e},\,\rho^{n_{\mathrm{lo}}(k)-k/\mu_e}\Bigr].
$$
当把事件阈值取为扭曲混合尺度 $k_{\mathrm{mix}}:=\lceil\mu_e\,\tau_{\mathrm{mix}}\rceil$ 时，上式将“角色扭曲的读出混合瓶颈”与“事件换时误差”在同一指数包络下对齐。
表 \ref{tab:real-input-40-event-time-rescaling-error-budget} 给出 $k_{\mathrm{mix}}$ 及若干参照 $k$ 的精确分位数与对应的乘性误差区间（可复现）。
\end{remark}

\input{sections/generated/tab_real_input_40_event_time_rescaling_error_budget}

\begin{remark}[扭曲混合尺度处的量级对齐：事件换时误差为常数因子]
\label{cor:chi-event-time-envelope-vs-tau-mix}
在推论 \ref{cor:chi-event-time-exponential-envelope-error-budget} 的设定下，取 $k_{\mathrm{mix}}:=\lceil \mu_e\,\tau_{\mathrm{mix}}\rceil$。
令 $n_{\mathrm{lo}}(k),n_{\mathrm{hi}}(k)$ 为 $\tau(k)$ 的 $[q_{\mathrm{lo}},q_{\mathrm{hi}}]$ 精确分位数（命题 \ref{prop:chi-event-clock-hitting-time-exact-quantiles}）。
则在概率至少 $q_{\mathrm{hi}}-q_{\mathrm{lo}}$ 的事件上，成立
$$
\frac{\rho^{\tau(k_{\mathrm{mix}})}}{\rho^{\tau_{\mathrm{mix}}}}
\in\Bigl[\rho^{n_{\mathrm{hi}}(k_{\mathrm{mix}})-\tau_{\mathrm{mix}}},\,\rho^{n_{\mathrm{lo}}(k_{\mathrm{mix}})-\tau_{\mathrm{mix}}}\Bigr].
$$
表 \ref{tab:real-input-40-event-time-envelope-vs-tau-mix} 在 $k_{\mathrm{mix}}$ 的邻域给出上述比值区间的可复现数值读出，显示“事件时钟严格换时”对 $\rho^n$ 型指数包络的扰动在该对齐尺度上仅为常数倍。
因此，角色扭曲混合瓶颈 $\tau_{\mathrm{mix}}$ 与内生相关尺度之间的数量级分离（推论 \ref{cor:chi-event-clock-amplifies-separation}）不能归因于事件换时的不确定性。
\end{remark}

\input{sections/generated/tab_real_input_40_event_time_envelope_vs_tau_mix}

\begin{remark}[分位数调整的事件相关尺度：$\tau_{\mathrm{eff}}^{(e)}(k)$]
\label{prop:chi-event-time-tau-eff}
在引理 \ref{lem:chi-event-time-rescaling} 的设定下，设步数时间相关满足指数包络 $\rho^n$，并记
$$
\tau_{\mathrm{corr}}(0):=\frac{1}{-\log\rho}.
$$
对事件阈值 $k\in\mathbb{N}$ 的击中时间 $\tau(k)$，定义一个与样本路径相关的“事件时间有效相关尺度”
$$
\tau_{\mathrm{eff}}^{(e)}(k):=\frac{k}{\tau(k)}\,\tau_{\mathrm{corr}}(0).
$$
若 $n_{\mathrm{lo}}(k),n_{\mathrm{med}}(k),n_{\mathrm{hi}}(k)$ 分别为 $\tau(k)$ 在 $q_{\mathrm{lo}},0.5,q_{\mathrm{hi}}$ 处的精确分位数，则由 $\tau(k)$ 的单调性立即得到分位数区间
$$
\tau_{\mathrm{eff}}^{(e)}(k)\in\Bigl[\frac{k}{n_{\mathrm{hi}}(k)}\tau_{\mathrm{corr}}(0),\,\frac{k}{n_{\mathrm{lo}}(k)}\tau_{\mathrm{corr}}(0)\Bigr]
$$
在概率至少 $q_{\mathrm{hi}}-q_{\mathrm{lo}}$ 的事件上成立。
\end{remark}

\begin{remark}[对齐到 $k_{\mathrm{mix}}$ 后的稳健分离]
\label{cor:chi-event-time-tau-eff-vs-tau-mix}
在命题 \ref{prop:chi-event-time-tau-eff} 的设定下取 $k_{\mathrm{mix}}:=\lceil\mu_e\tau_{\mathrm{mix}}\rceil$。
表 \ref{tab:real-input-40-event-time-tau-eff-vs-tau-mix} 给出 $k_{\mathrm{mix}}$ 邻域的 $\tau_{\mathrm{eff}}^{(e)}(k)$ 分位数区间，并与 $\tau_{\mathrm{mix}}$ 做比值对照。
该对照在数值上给出一条更强的审计结论：即使允许用 $\tau(k)$ 的分位数对事件时间进行非渐近、概率校准的换时，得到的事件时间相关尺度仍保持在常数阶，无法解释 $\tau_{\mathrm{mix}}$ 所体现的数量级混合瓶颈。
\end{remark}

\input{sections/generated/tab_real_input_40_event_time_tau_eff_vs_tau_mix}

\begin{remark}[期望级证书：$\mathbb{E}(\rho^{\tau(k)})$ 的严格区间]
\label{prop:chi-event-time-rho-tau-expectation}
在推论 \ref{cor:chi-event-time-exponential-envelope-error-budget} 的设定下，固定 $k\in\mathbb{N}$。
令 $F_n(k):=\mathbb{P}(\tau(k)\le n)=\mathbb{P}(T_n\ge k)$（命题 \ref{prop:chi-event-clock-hitting-time-exact-quantiles}），并定义离散概率质量
$$
p_n(k):=\mathbb{P}(\tau(k)=n)=F_n(k)-F_{n-1}(k)\qquad (n\ge 1),
$$
其中约定 $F_0(k)=0$。
则对任意截断 $n_{\max}\ge 1$，有
$$
\sum_{n=1}^{n_{\max}}\rho^n p_n(k)\ \le\ \mathbb{E}(\rho^{\tau(k)})\ \le\ \sum_{n=1}^{n_{\max}}\rho^n p_n(k)\ +\ \rho^{n_{\max}+1}\,\mathbb{P}(\tau(k)>n_{\max}).
$$
该区间完全由有限步递推得到的 $\{F_n(k)\}_{n\le n_{\max}}$ 决定，因而在本文口径下可作为无需外部不等式的“期望级误差预算”。
\end{remark}

\begin{remark}[对齐到 $k_{\mathrm{mix}}$ 的期望级对照]
\label{cor:chi-event-time-rho-tau-expectation-vs-tau-mix}
在命题 \ref{prop:chi-event-time-rho-tau-expectation} 的设定下取 $k_{\mathrm{mix}}:=\lceil\mu_e\tau_{\mathrm{mix}}\rceil$。
表 \ref{tab:real-input-40-event-time-rho-tau-expectation-vs-tau-mix} 给出 $k_{\mathrm{mix}}$ 邻域的 $\mathbb{E}(\rho^{\tau(k)})$ 严格区间，并与 $\rho^{\tau_{\mathrm{mix}}}$ 比较。
该对照在期望层面进一步表明：即使把事件换时的全部分布信息通过 $\mathbb{E}(\rho^{\tau(k)})$ 聚合，$k_{\mathrm{mix}}$ 邻域内的量级仍与 $\rho^{\tau_{\mathrm{mix}}}$ 处于可审计的分离区间，从而角色扭曲混合瓶颈并非换时噪声的产物。
\end{remark}

\input{sections/generated/tab_real_input_40_event_time_rho_tau_expectation_vs_tau_mix}

\begin{remark}[期望有效指数与偏移：$\tau^{(E)}(k)$ 与 $\Delta^{(E)}(k)$]
\label{prop:chi-event-time-tauE-delta}
在命题 \ref{prop:chi-event-time-rho-tau-expectation} 的设定下，假设 $0<\rho<1$。
定义事件换时的期望有效指数
$$
\tau^{(E)}(k):=\frac{\log\mathbb{E}(\rho^{\tau(k)})}{\log\rho},
$$
以及其相对线性重标定 $k/\mu_e$ 的偏移
$$
\Delta^{(E)}(k):=\tau^{(E)}(k)-\frac{k}{\mu_e}.
$$
若已知 $\mathbb{E}(\rho^{\tau(k)})\in[E_{\mathrm{lo}},E_{\mathrm{hi}}]$ 为严格区间，则由 $\log$ 的单调性与 $\log\rho<0$，得到
$$
\tau^{(E)}(k)\in\Bigl[\frac{\log E_{\mathrm{hi}}}{\log\rho},\,\frac{\log E_{\mathrm{lo}}}{\log\rho}\Bigr],
\qquad
\Delta^{(E)}(k)\in\Bigl[\frac{\log E_{\mathrm{hi}}}{\log\rho}-\frac{k}{\mu_e},\,\frac{\log E_{\mathrm{lo}}}{\log\rho}-\frac{k}{\mu_e}\Bigr].
$$
因此 $\Delta^{(E)}(k)$ 可被视为“期望层面”的事件换时偏移证书：它把 $\tau(k)$ 的全分布信息通过 $\mathbb{E}(\rho^{\tau(k)})$ 聚合为一个可审计的有效指数偏移。
\end{remark}

\begin{remark}[对齐到 $k_{\mathrm{mix}}$ 的期望偏移证书]
\label{cor:chi-event-time-tauE-delta-vs-tau-mix}
在命题 \ref{prop:chi-event-time-tauE-delta} 的设定下取 $k_{\mathrm{mix}}:=\lceil\mu_e\tau_{\mathrm{mix}}\rceil$。
表 \ref{tab:real-input-40-event-time-tauE-delta-vs-tau-mix} 给出 $k_{\mathrm{mix}}$ 邻域内的 $\tau^{(E)}(k)$ 与 $\Delta^{(E)}(k)$ 的严格区间。
该对照把“事件时间 $k$ 与步数时间指数包络”之间的换时误差在期望层面压缩为可复现的偏移量，并显示该偏移在对齐尺度处仅为常数阶。
\end{remark}

\input{sections/generated/tab_real_input_40_event_time_tauE_delta_vs_tau_mix}

\paragraph{指数包络的离散审计证书}
在 $t=0$ 的 Parry 平衡态下，脚本输出的自相关序列 $\{\mathrm{Corr}(n)\}_{n=0}^N$ 与谱隙包络 $r^n$（$r=\rho_{\mathrm{corr}}(0)$）允许构造一个完全可复现的离散证书：
\[
C_N:=\max_{0\le n\le N}\frac{|\mathrm{Corr}(n)|}{r^n}.
\]
\input{sections/generated/tab_real_input_40_time_correlation_decay_certificate}
上表给出 $C_N$ 的数值及其达到位置，从而把命题 \ref{prop:chi-slice-time-correlation} 中的“存在常数 $C(0)$”具体化为可审计的上界候选。

\paragraph{尾部包络证书（排除 $n=0$）}
为避免 $n=0$ 所诱导的平凡最大比值（$\mathrm{Corr}(0)=1$），定义尾部证书
\[
C_N^{(1)}:=\max_{1\le n\le N}\frac{|\mathrm{Corr}(n)|}{r^n}.
\]
由定义立即得到：对一切 $1\le n\le N$，均有
\[
|\mathrm{Corr}(n)|\le C_N^{(1)} r^n.
\]
下表给出离散样本上的 $C_N^{(1)}$ 及其达到位置，作为 $n\ge 1$ 尾部指数包络的可审计证书。
\input{sections/generated/tab_real_input_40_time_correlation_decay_certificate_tail}

\paragraph{次主模态的内生相位与振荡周期（可审计）}
命题 \ref{prop:chi-slice-time-correlation} 给出相关的指数包络尺度 $\rho_{\mathrm{corr}}(t)=\Lambda(t)/\lambda_1(t)$。在有限维情形中，该指数衰减通常由一个或一对次主特征值主导：若 $\lambda_2(t)$ 不为正实数，则相关函数除衰减包络外还会携带一个由 $\arg(\lambda_2(t))$ 控制的振荡因子。

\begin{remark}[次主相位与内生振荡周期]
\label{prop:chi-slice-subleading-phase}
在命题 \ref{prop:chi-slice-time-correlation} 的设定下，取
\[
\lambda_1(t):=\rho\bigl(M_{\theta(t)}\bigr)>0,
\qquad
\lambda_2(t)\in\mathrm{spec}(M_{\theta(t)})\setminus\{\lambda_1(t)\}
\]
为满足 $|\lambda_2(t)|=\Lambda(t)$ 的次主特征值（若出现共轭成对则任选其一）。定义
\[
\rho_2(t):=\frac{|\lambda_2(t)|}{\lambda_1(t)}\in[0,1),
\qquad
\omega(t):=\arg\bigl(\lambda_2(t)\bigr)\in(-\pi,\pi],
\]
并在 $\omega(t)\ne 0$ 时定义内生振荡周期
\[
T_{\mathrm{osc}}(t):=\frac{2\pi}{|\omega(t)|}.
\]
则对任意中心化的有限状态可观测量 $f$，其相关函数的谱表示中存在次主项
\[
\left(\frac{\lambda_2(t)}{\lambda_1(t)}\right)^n
=\rho_2(t)^n\,e^{i n\omega(t)},
\]
从而当 $\omega(t)\ne 0$ 时，相关衰减在指数包络 $\rho_2(t)^n$ 上叠加一个内生相位频率 $\omega(t)$；当 $\omega(t)=\pi$（即 $\lambda_2(t)<0$）时出现二步交替振荡。
\end{remark}

\input{sections/generated/fig_real_input_40_time_correlation_phase}
\input{sections/generated/tab_real_input_40_time_correlation_phase}

\begin{remark}[离散相位相区证书：次主模态由实负到复振荡]
\label{prop:chi-slice-phase-windows-certificate}
沿 $\chi$ 切片取离散网格 $t_0<t_1<\cdots<t_N$，并对每个 $t_i$ 记录满足 $|\lambda_2(t_i)|=\Lambda(t_i)$ 的次主特征值 $\lambda_2(t_i)$ 的实部与虚部。
固定阈值 $\varepsilon>0$，定义离散相区标签
\(
\kappa(t_i)\in\{\texttt{neg\_real},\texttt{pos\_real},\texttt{complex}\}
\)
为：若 $|\Im\lambda_2(t_i)|>\varepsilon$ 则 $\kappa(t_i)=\texttt{complex}$；否则按 $\Re\lambda_2(t_i)$ 的符号取 $\texttt{neg\_real}$ 或 $\texttt{pos\_real}$。
则离散序列 $\{\kappa(t_i)\}$ 的极大常值网格段给出一个完全可复现的“内生相位相区”证书：每一段上 $\omega(t)=\arg\lambda_2(t)$ 与 $T_{\mathrm{osc}}(t)=2\pi/|\omega(t)|$ 的取值范围由有限维谱读出直接确定。
表 \ref{tab:real-input-40-time-corr-phase-windows} 给出真实输入 40 状态核在本文默认网格上的相区分解。
\end{remark}

\input{sections/generated/tab_real_input_40_time_correlation_phase_windows}

\paragraph{相关时间与角色扭曲混合时间的分离（可审计对比）}
上一段的 $\tau_{\mathrm{corr}}(t)$ 来自同一切片平衡态内的相关衰减；而注 \ref{rem:pom-chi-mixing} 中的 $\tau_{\mathrm{mix}}$ 则来自角色扭曲后谱半径比 $\rho/\lambda$ 所控制的“读出偏置”指数衰减。
表 \ref{tab:real-input-40-tau-corr-vs-tau-mix} 将二者在 $t=0$ 与 $((3,3,5))$ 最坏扭曲处并列，并进一步给出三条可复核的“对齐”读出：离散网格最近点 $t_\star$、用于线性插值的网格括号 $[t_-,t_+]$，以及在该括号上的线性根估计 $t_0$（可复现）。
此外，若把 $y(t):=\tau_{\mathrm{corr}}(t)-\tau_{\mathrm{mix}}(3,3,5)$ 视作关于 $t$ 的连续函数，则其离散采样的符号变号次数与根区间计数（表 \ref{tab:real-input-40-tau-corr-monotonicity}）给出“交点多重性”的粗证书，并可通过网格加密表 \ref{tab:real-input-40-tau-corr-monotonicity-refinement} 进行稳定性复核。

\begin{remark}[离散变号证书：交点存在性与多重性下界（可审计）]\label{prop:chi-slice-sign-change-certificate}
令
\[
y(t):=\tau_{\mathrm{corr}}(t)-\tau_{\mathrm{mix}}(3,3,5),
\]
并取严格递增的离散网格 $t_0<t_1<\cdots<t_N$。
假设在每个区间 $[t_i,t_{i+1}]$ 上，映射 $t\mapsto \tau_{\mathrm{corr}}(t)$ 连续（从而 $y$ 连续）。
则：
\begin{enumerate}
  \item 若存在 $i$ 使得 $y(t_i)\,y(t_{i+1})<0$，则由介值定理存在 $t\in(t_i,t_{i+1})$ 使 $y(t)=0$，即 $\tau_{\mathrm{corr}}(t)=\tau_{\mathrm{mix}}(3,3,5)$。
  \item 若在离散序列 $\{y(t_i)\}_{i=0}^N$ 中出现 $K$ 个两两不相交的根区间（每个根区间要么对应一次严格变号 $y(t_i)\,y(t_{i+1})<0$，要么对应一段连续的离散零值块 $y(t_i)=\cdots=y(t_j)=0$），则连续函数 $y$ 在对应的 $K$ 个互不相交区间上至少各含一个零点；因此 $y(t)=0$ 的零点集合至少具有 $K$ 个互不相交的连通成分。
\end{enumerate}
表 \ref{tab:real-input-40-tau-corr-monotonicity} 报告的 $y(t)$ 符号变号次数与根区间计数，给出上述结论的可复现“证书化摘要”；表 \ref{tab:real-input-40-tau-corr-monotonicity-refinement} 在网格加密下对这些离散证书做一致性复核。
\end{remark}
\input{sections/generated/tab_real_input_40_tau_corr_vs_tau_mix}
\input{sections/generated/tab_real_input_40_tau_corr_monotonicity}
\input{sections/generated/tab_real_input_40_tau_corr_monotonicity_refinement}
\input{sections/generated/fig_real_input_40_tau_corr_vs_tau_mix}

\paragraph{把 \texorpdfstring{$\log\mathfrak{M}$}{logM} 完全对齐到显式 \texorpdfstring{$\zeta_M$}{zeta\_M}（闭式常数 + 数值）}
对 40 状态真实输入核，附录 \ref{app:real-input-40-abel} 给出
\[
\det(I-zM)=(1-z)^2(1+z)^3(1-3z+z^2)(1+z-z^2),
\]
因此主极点为 $z_\star=\varphi^{-2}$（对应 $\lambda=\varphi^2$）。并且主极点留数常数具有代数闭式
\[
C=\lim_{z\to z_\star}(1-\lambda z)\,\zeta_M(z)=\frac{47+21\sqrt5}{100}\approx 0.9395742753.
\]
将该 $C$ 代入上文的 Möbius--Abel 闭式即可得到 Abel/Hadamard 有限部常数与其导出的 Mertens 常数（数值见附录 \ref{app:real-input-40-finite-part}）：
\[
\log\mathfrak{M}\approx -0.254312661901716,\qquad
\mathfrak{M}\approx 0.7754493106833835,\qquad
\mathsf{M}=\gamma+\log\mathfrak{M}\approx 0.3229030029998169.
\]

\paragraph{可计算流程（与“离散化—正则化—有限部”同构）}
给定一个有限图移位的邻接矩阵 $M$（或加权矩阵族 $M_\theta$），并固定截断阶 $K\ge 2$，可按下述步骤计算有限部常数：
\begin{enumerate}
  \item \textbf{离散化：}由图/转导器得到矩阵 $M$（或 $M_\theta$），并定义 $\zeta(z)=1/\det(I-zM)$（或 $\zeta_\theta(z)=1/\det(I-zM_\theta)$）。
  \item \textbf{主尺度：}计算 Perron--Frobenius 主特征值 $\lambda=\rho(M)$（或 $\lambda(\theta)=\rho(M_\theta)$），并令 $z_\star=1/\lambda$（或 $z_\star(\theta)=1/\lambda(\theta)$）。
  \item \textbf{留数常数：}计算主极点留数常数
  \[
  C:=\lim_{z\to z_\star}(1-\lambda z)\zeta(z)
  \qquad
  (\text{或 }C(\theta):=\lim_{z\to z_\star(\theta)}(1-\lambda(\theta)z)\zeta_\theta(z)).
  \]
  在“简单极点”情形，$C$ 亦可由行列式导数或特征值乘积表达为闭式（见附录 \ref{app:real-input-40-finite-part}）。
  \item \textbf{正则化（Abel 采样）：}对 $k=2,\dots,K$，在收敛圆盘内采样 $\log\zeta(z_\star^k)$（或 $\log\zeta_\theta(z_\star(\theta)^k)$）。
  \item \textbf{有限部：}用 Möbius 尾的截断和近似
  \[
  \log\mathfrak{M}\approx \log C+\sum_{k=2}^{K}\frac{\mu(k)}{k}\log\zeta(z_\star^k)
  \quad
  (\text{或 }\log\mathfrak{M}(\theta)\approx \log C(\theta)+\sum_{k=2}^{K}\frac{\mu(k)}{k}\log\zeta_\theta(z_\star(\theta)^k)).
  \]
  \item \textbf{Mertens 常数输出：}定义
  \[
  \mathsf{M}:=\gamma+\log\mathfrak{M},
  \qquad
  \mathsf{M}(\theta):=\gamma+\log\mathfrak{M}(\theta).
  \]
\end{enumerate}
由于 $|z_\star|<1$，故 $z_\star^k$ 指数衰减，且 $\log\zeta(z)=O(z)$（$z\to 0$）使得 $\log\zeta(z_\star^k)$ 在 $k$ 上快速趋零，从而该流程在数值上具有很强的稳定性与快速收敛性。

\paragraph{orbit--Mertens 乘积（把 $\mathsf{M}$ 变成可对比标量）}
定义长度截断的 prime-orbit Mertens 乘积
\[
\mathcal{P}(N):=\prod_{\substack{\tau\ \mathrm{primitive}\\|\tau|\le N}}\left(1-\lambda^{-|\tau|}\right)^{-1}.
\]
取对数并用 $-\log(1-x)=x+O(x^2)$ 得
\[
\log\mathcal{P}(N)=\sum_{n\le N}\frac{p_n}{\lambda^n}+O(1)
=\log N+\mathsf{M}+o(1),
\]
因此
\[
\mathcal{P}(N)\sim e^{\mathsf{M}}\,N=e^{\gamma}\mathfrak{M}\,N.
\]
若用“轨道范数” $N(\tau):=\lambda^{|\tau|}$ 改写，并令 $x=\lambda^N$（等价于 $N=\log x/\log\lambda$），则得到等价的 $\log x$ 型主项
\[
\prod_{N(\tau)\le x}\left(1-\frac{1}{N(\tau)}\right)^{-1}
\sim \frac{e^{\gamma}\mathfrak{M}}{\log\lambda}\,\log x.
\]

\paragraph{加权接口与 \texorpdfstring{$\log\mathfrak{M}(\theta)$}{logM(theta)} 相图}
对边势向量 $\Phi$（例如附录 \ref{app:real-input-40-logm-theta} 中的 $\varphi_e,\varphi_-,\varphi_2$），定义加权矩阵族 $M_\theta$ 与
\(
\zeta_\theta(z)=1/\det(I-zM_\theta)
\)。
令 $\lambda(\theta)=\rho(M_\theta)$、$z_\star(\theta)=\lambda(\theta)^{-1}$。与上式同口径，定义
\[
P_\theta(z)=\sum_{k\ge 1}\frac{\mu(k)}{k}\log\zeta_\theta(z^k).
\]
\begin{definition}[Abel/Hadamard 有限部常数]\label{def:abel-finite-part}
对有限图移位的加权矩阵族 $M_\theta$，定义 Abel/Hadamard 有限部常数为
\[
\log\mathfrak{M}(\theta):=\mathrm{fp}_{r=1}\Bigl(P_\theta(r/\lambda(\theta))+\log(1-r)\Bigr),
\]
其中 $P_\theta(z)=\sum_{k\ge 1}\frac{\mu(k)}{k}\log\zeta_\theta(z^k)$ 为 Möbius--$\log\zeta$ 变换，$\lambda(\theta)=\rho(M_\theta)$ 为 Perron--Frobenius 主特征值，$\mathrm{fp}_{r=1}$ 表示在 $r=1$ 处的有限部（Laurent 常数项）。
\end{definition}
\begin{proposition}[Abel 有限部的 Möbius--$\zeta$ 闭式]\label{prop:abel-finite-part-mobius-zeta}
在定义 \ref{def:abel-finite-part} 的设定下，Abel 有限部常数具有可计算闭式
\[
\log\mathfrak{M}(\theta)
=\log C(\theta)+\sum_{k\ge 2}\frac{\mu(k)}{k}\log\zeta_\theta\bigl(z_\star(\theta)^k\bigr),
\qquad
C(\theta):=\lim_{z\to z_\star(\theta)}(1-\lambda(\theta)z)\,\zeta_\theta(z),
\]
其中 $C(\theta)$ 为主极点留数常数，$z_\star(\theta)=\lambda(\theta)^{-1}$ 为主极点位置。
\end{proposition}
等价地（与附录 \ref{app:real-input-40-logm-theta} 同口径），亦可写成一行 Möbius--Abel 形式
\[
\log\mathfrak{M}(\theta)=\log C(\theta)-\sum_{m\ge 2}\frac{P_\theta(\lambda(\theta)^{-m})}{m}.
\]
据此定义加权 Mertens 常数
\(
\mathsf{M}(\theta):=\gamma+\log\mathfrak{M}(\theta)
\)，从而把“离散化—正则化—有限部”落实为一条可扫参数的相图接口；数值相图入口脚本见
\texttt{scripts/exp\_sync\_kernel\_real\_input\_40\_logM\_theta.py}。

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.15}
\caption{$\log\mathfrak{M}(\theta)$ 相图的样本点（真实输入 40 状态核；固定 $\theta_-=0$；Möbius 尾截断 $k_{\max}=250$）。}
\label{tab:real-input-40-logM-theta-sample}
\begin{tabular}{@{}ccccc@{}}
\toprule
$\theta_e$ & $\theta_2$ & $\lambda(\theta)$ & $\log\mathfrak{M}(\theta)$ & $\mathsf{M}(\theta)=\gamma+\log\mathfrak{M}(\theta)$ \\
\midrule
$-1.0$ & $-1.0$ & $1.793239048961$ & $-0.495016484263$ & $0.082199180638$ \\
$\ \ 0.0$ & $-1.0$ & $2.490012563421$ & $-0.184629246340$ & $0.392586418561$ \\
$\ \ 1.0$ & $-1.0$ & $3.646591323038$ & $\ \ 0.072379237963$ & $0.649594902864$ \\
$-1.0$ & $\ \ 0.0$ & $1.863254840922$ & $-0.515448376051$ & $0.061767288851$ \\
$\ \ 0.0$ & $\ \ 0.0$ & $2.618033988750$ & $-0.254312661902$ & $0.322903003000$ \\
$\ \ 1.0$ & $\ \ 0.0$ & $3.864122715327$ & $-0.040498237636$ & $0.536717427266$ \\
$-1.0$ & $\ \ 1.0$ & $2.040720410275$ & $-0.542620915409$ & $0.034594749493$ \\
$\ \ 0.0$ & $\ \ 1.0$ & $2.948418905502$ & $-0.375618148304$ & $0.201597516597$ \\
$\ \ 1.0$ & $\ \ 1.0$ & $4.451463505733$ & $-0.259640526249$ & $0.317575138652$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{三维隐含属性的典型密度与协方差密度（\texorpdfstring{$\theta=0$}{theta=0}）}
在三势 $\theta=(\theta_e,\theta_-,\theta_2)$ 下，$P(\theta)=\log\rho(M_\theta)$ 的梯度与 Hessian 给出三条观测量的典型密度与协方差密度（详见附录 \ref{app:real-input-40-logm-theta} 的复现段落）：
\[
\nabla P(0)=\left(\frac{23-7\sqrt5}{20},\frac{5-\sqrt5}{20},\frac{3-\sqrt5}{10}\right)\approx(0.367376,\ 0.138197,\ 0.076393),
\]
\[
\nabla^2P(0)\approx
\begin{pmatrix}
0.048741 & 0.032574 & 0.015964\\
0.032574 & 0.275035 & 0.115735\\
0.015964 & 0.115735 & 0.067331
\end{pmatrix}.
\]
\begin{remark}[三通道二阶耦合的“中介结构”]
由 Hessian 的非对角项可见二阶耦合具有明显层级：
\[
H_{-2}>H_{e-}>H_{e2}.
\]
这意味着“负携带--碰撞”耦合最强，而“输出--碰撞”最弱；输出与负携带的相关性可被理解为在碰撞通道上被放大并重分配的结果。该层级关系是数值可审计的（见附录 \ref{app:real-input-40-logm-theta} 的复现脚本输出）。
\end{remark}

\paragraph{\texorpdfstring{$\log\mathfrak{M}(\theta)$}{logM(theta)} 的二阶展开（常数级指纹）}
在同一加权族下，$\log\mathfrak{M}(\theta)$ 捕获 prime-orbit Mertens 主项中的常数级漂移（相对于 $\log N$ 的平移），可作为独立于压力主尺度 $P(\theta)$ 的细粒度指纹。
表 \ref{tab:real-input-40-logM-theta-taylor} 给出 $\log\mathfrak{M}(\theta)$ 在 $\theta=0$ 的梯度与 Hessian 的可复现数值（二阶差分；Möbius 尾截断固定），便于与 $\nabla P(0),\nabla^2P(0)$ 并列对比。
\input{sections/generated/tab_real_input_40_logM_theta_taylor}

\paragraph{arity-charge 的同一接口并入}
定义 arity-charge
\[
\chi:=\varphi_e-\varphi_2\in\{-1,0,1\}.
\]
更一般地，若把 $\chi$ 作为一条额外势并入加权，则它只引入线性重参数化（无需扩展状态空间）：
\[
P(\theta_e,\theta_-,\theta_2,\theta_\chi)=P(\theta_e+\theta_\chi,\theta_-,\theta_2-\theta_\chi).
\]
在三参数压力 $P(\theta_e,\theta_-,\theta_2)=\log\rho(M_\theta)$ 下，对 $\chi$ 的线性加权对应一维切线
\[
P_\chi(t):=P(t,0,-t).
\]
因此 $\chi$ 的典型密度与方差密度由
\(
P_\chi'(0),P_\chi''(0)
\)
读出，并可与同一族 $\log\mathfrak{M}(\theta)$ 在该方向的切片一起作为“素数影子”的分叉维度；具体数值与可复现计算见附录 \ref{app:real-input-40-logm-theta} 与
\texttt{scripts/exp\_sync\_kernel\_real\_input\_40\_logM\_chi.py}。

\paragraph{\texorpdfstring{$\chi$}{chi} 的一维切片相图（可计算流程）}
固定
\(
\theta(t)=(t,0,-t)
\)
并令
\[
P_\chi(t):=P(\theta(t))=\log\rho(M_{\theta(t)}),
\qquad
\log\mathfrak{M}_\chi(t):=\log\mathfrak{M}(\theta(t)).
\]
对每个 $t$，按上文“可计算流程”的步骤计算 $\lambda(t)=e^{P_\chi(t)}$、$z_\star(t)=\lambda(t)^{-1}$、$C(t)$，并用截断 Möbius 尾近似
\[
\log\mathfrak{M}_\chi(t)\approx \log C(t)+\sum_{k=2}^{K}\frac{\mu(k)}{k}\log\zeta_{\theta(t)}(z_\star(t)^k).
\]
该一维切片在 $t=0$ 的统计量由压力导数给出：
\[
\mathbb{E}[\chi]=P_\chi'(0),\qquad \mathrm{Var}_\infty(\chi)=P_\chi''(0),
\]
并且在真实输入核上（附录 \ref{app:real-input-40-logm-theta}）有闭式与数值
\[
\mathbb{E}[\chi]=\frac{17-5\sqrt5}{20}\approx 0.2909830056,
\qquad
\mathrm{Var}_\infty(\chi)\approx 0.0841438530.
\]
若记三维 Hessian（协方差密度）为 $H=\nabla^2P(0)$，则
\[
\mathrm{Var}_\infty(\chi)=H_{ee}+H_{22}-2H_{e2},
\qquad
\mathrm{Cov}_\infty(\chi,\varphi_-)=H_{e-}-H_{-2}\approx -0.0831619.
\]
\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.15}
\caption{$\chi$-切片相图的样本点：$t\mapsto(\lambda_\chi(t),\log\mathfrak{M}_\chi(t),\mathsf{M}_\chi(t))$（真实输入 40 状态核，$\theta(t)=(t,0,-t)$；Möbius 尾截断 $k_{\max}=250$）。}
\label{tab:real-input-40-logM-chi-sample}
\begin{tabular}{@{}cccc@{}}
\toprule
$t$ & $\lambda_\chi(t)$ & $\log\mathfrak{M}_\chi(t)$ & $\mathsf{M}_\chi(t)=\gamma+\log\mathfrak{M}_\chi(t)$ \\
\midrule
$-1.0$ & $2.040720410275$ & $-0.542620915409$ & $0.034594749493$ \\
$-0.5$ & $2.287538746528$ & $-0.410414288994$ & $0.166801375907$ \\
$\ \ 0.0$ & $2.618033988750$ & $-0.254312661902$ & $0.322903003000$ \\
$\ \ 0.5$ & $3.059600073602$ & $-0.087974328977$ & $0.489241335925$ \\
$\ \ 1.0$ & $3.646591323038$ & $\ \ 0.072379237963$ & $0.649594902864$ \\
\bottomrule
\end{tabular}
\end{table}
% figure generated by scripts/exp_sync_kernel_real_input_40_logM_chi.py
\input{sections/generated/fig_real_input_40_logM_chi}
从而把“操作数不一致”的典型密度/涨落与同一切片上的 Abel 有限部常数曲线 $\log\mathfrak{M}_\chi(t)$ 放在同一相图坐标下比较。该曲线的数值生成入口脚本见
\texttt{scripts/exp\_sync\_kernel\_real\_input\_40\_logM\_chi.py}。
