\subsection{边界词塔：SM 的分辨率分解与 Zeckendorf--Fibonacci 的 GUT 签名}\label{subsec:bdry-tower-zeck-gut}

\paragraph{端点 \texorpdfstring{$(1,1)$}{(1,1)} 纤维作为“边界词”层}
在黄金均值语法（禁止相邻 \(11\)）下，长度 \(m\) 的稳定类型集合为 \(X_m\)，并且端点纤维
\[
X_m^{\mathrm{bdry}}
:=\{u\in X_m:\ u_1=1,\ u_m=1\}
\]
的基数由 Fibonacci 数强制给出。具体地，推论 \ref{cor:parry-golden-three-levels} 给出
\[
|X_m^{\mathrm{bdry}}|
=\#\{u\in X_m:u_1=1,u_m=1\}
=F_{m-2}.
\]
因此每个分辨率 \(m\) 都携带一个离散的“边界模态数”\(d(m):=|X_m^{\mathrm{bdry}}|\)。

\paragraph{边界词三层塔：SM 的 \(12\) 个规范玻色子}
在 \(m=6\) 的锚定接口上，\(X_6\) 的 \(18\oplus 3\) 分解提供了三条边界连接类离散标签；
若把 gauge 自由度从“单层结构”提升为“跨分辨率塔状模态”，则 SM 的玻色子数
\(\,1+3+8=12\,\)
在本体系中出现为一个严格的 Fibonacci 边界计数分解：
\input{sections/generated/eq_boundary_tower_sm12}
这一步并非重复“\(SU(3)\times SU(2)\times U(1)\) 三因子”结论，而是把接口升级到\emph{玻色子计数层级}：
\(U(1)\)、\(SU(2)\)、\(SU(3)\) 的规范玻色子分别位于 \(m=4,6,8\) 三个边界层，
并以边界词本身作为离散标签候选集。

\begin{remark}[色对称的闭层包络来源：三拷贝 Hilbert 空间的基混合冗余]\label{rem:color-as-three-copy-envelope}
上段给出的 \(m=8\) 边界层为“色”玻色子计数提供了离散锚点。
若在闭层机制上进一步满足：内部态空间可分解为三份不相交拷贝
\[
\mathcal{H}=\mathcal{H}_q\oplus\mathcal{H}_q\oplus\mathcal{H}_q,
\]
且可观测统计对这三份拷贝的基变换不敏感，
则“拷贝混合”的冗余群自然为 \(U(3)\)；在去掉总体相位后得到连续包络的 \(SU(3)\)。
在本文的编译口径中，类似的“拷贝并置”已以类型提升的形式出现（例如 moment-kernel 的 \(\mathrm{LIFT}_{G^{\times q}}\) 并置提升），
从而把连续包络对称的来源落回到一个可追踪的离散拷贝结构上。
\end{remark}

\paragraph{Fold6 的 \texorpdfstring{$18\oplus 3$}{18+3} 锚定强迫一个 \texorpdfstring{$B_3/C_3$}{B3/C3} 统一种子}
在 \(m=6\) 的锚定接口上，除去 \(X_6=X_6^{\mathrm{cyc}}\sqcup X_6^{\mathrm{bdry}}\) 的 \(18\oplus 3\) 分解外，
\(X_6^{\mathrm{cyc}}\) 内部还出现一个严格的 \(6+12\) 二层指纹（按 Hamming weight 分层）：
\input{sections/generated/eq_fold6_b3c3_seed}
若把三条端点纤维 \(X_6^{\mathrm{bdry}}\) 理解为三条对易连接方向（Cartan-like），把 \(18\) 个闭词 \(X_6^{\mathrm{cyc}}\) 理解为根方向（charged/root-like），
则 \((3+18)=21\) 的计数与 rank-\(3\) 简单李代数的 Cartan/根分解同型，从而把候选统一李型锁死到 \(B_3\) 或 \(C_3\)（两者互为长短根交换对偶类型）。
更强地，\(6+12\) 的 cyclic 指纹与 \(B_3/C_3\) 根系的“两根长”结构一致：一类长度 \(6\)、另一类长度 \(12\)。
\par
对应的离散对称核为 Weyl 群
\[
W(B_3)\cong W(C_3)\cong (\ZZ_2)^3\rtimes S_3,\qquad |W|=48,
\]
其中 \(S_3\) 可读作三通道/三轴的置换，而 \((\ZZ_2)^3\) 对应每个对易方向上的符号反射（电荷镜像）。
\par
为避免“手工配对”，脚本 \texttt{scripts/exp\_fold6\_b3c3\_seed.py} 输出两种规范化（长短根互换）的根字典表（表 \ref{tab:fold6_b3c3_root_dictionary_b3} 与表 \ref{tab:fold6_b3c3_root_dictionary_c3}；见附录），并写出 JSON 证书 \texttt{artifacts/export/fold6\_b3c3\_seed.json}。

\begin{corollary}[Weyl 刚性二轨道压缩律：任意 Weyl-不变统计坍缩为两标量]\label{cor:fold6-weyl-two-orbit-compression}
取表 \ref{tab:fold6_b3c3_root_dictionary_b3} 给出的 \(B_3\) 规范化根字典
\(\psi_B:X_6^{\mathrm{cyc}}\to \Phi(B_3)\)。
令 \(\Phi_{\mathrm{short}},\Phi_{\mathrm{long}}\subset\Phi(B_3)\) 分别为短根与长根集合，并定义其在 cyclic 扇区中的逆像分块
\[
X_6^{\mathrm{short}}:=\psi_B^{-1}(\Phi_{\mathrm{short}}),\qquad
X_6^{\mathrm{long}}:=\psi_B^{-1}(\Phi_{\mathrm{long}}),
\qquad
X_6^{\mathrm{cyc}}=X_6^{\mathrm{short}}\sqcup X_6^{\mathrm{long}}.
\]
则该分块与 \(\mathrm{wt}=1\) 与 \(\mathrm{wt}\neq 1\) 的指纹分层一致，并满足
\(|X_6^{\mathrm{short}}|=6\)、\(|X_6^{\mathrm{long}}|=12\)（见表 \ref{tab:fold6_b3c3_root_dictionary_b3} 与片段 \texttt{\detokenize{sections/generated/eq_fold6_b3c3_seed.tex}}）。
\par
设 \(f:X_6^{\mathrm{cyc}}\to\RR\) 为一个 \textbf{Weyl-不变}可观测量：对任意 \(u,v\in X_6^{\mathrm{cyc}}\)，若存在 \(w\in W(B_3)\) 使
\(\psi_B(v)=w\cdot \psi_B(u)\)，则 \(f(u)=f(v)\)。
则 \(f\) 在两条 Weyl 轨道上取常值，记
\[
f_{\mathrm{short}}:=f(u)\ (u\in X_6^{\mathrm{short}}),\qquad
f_{\mathrm{long}}:=f(u)\ (u\in X_6^{\mathrm{long}}),
\]
并且 cyclic 平均满足严格恒等式
\[
\frac{1}{|X_6^{\mathrm{cyc}}|}\sum_{u\in X_6^{\mathrm{cyc}}} f(u)
=
\frac{6}{18}\,f_{\mathrm{short}}+\frac{12}{18}\,f_{\mathrm{long}}
=
\frac13 f_{\mathrm{short}}+\frac23 f_{\mathrm{long}}.
\]
\end{corollary}

\begin{proof}
在 \(B_3\) 的标准根系模型中，Weyl 群 \(W(B_3)\cong (\ZZ_2)^3\rtimes S_3\) 作为“带符号的坐标置换”作用于 \(\RR^3\)，从而对短根集合 \(\{\pm e_i\}\) 与长根集合 \(\{\pm e_i\pm e_j\}\) 分别传递；因此 \(\Phi(B_3)\) 恰分解为两条 Weyl 轨道 \(\Phi_{\mathrm{short}}\) 与 \(\Phi_{\mathrm{long}}\)，其基数分别为 \(6\) 与 \(12\)。
由字典 \(\psi_B\) 的双射性，将该两轨道分解拉回 \(X_6^{\mathrm{cyc}}\) 即得 \(X_6^{\mathrm{cyc}}=X_6^{\mathrm{short}}\sqcup X_6^{\mathrm{long}}\)，并有相同的轨道大小。
Weyl-不变性即意味着 \(f\) 在每条轨道上取常值，故求和可按两块分拆，得到所示平均公式。证毕。
\end{proof}

\paragraph{四步平移的规范 uplift 同构}
对任意 \(n\ge 2\)，定义映射
\[
\iota_n: X_n\to X^{\mathrm{bdry}}_{n+4},\qquad \iota_n(v)=10\,v\,01.
\]
则 \(\iota_n\) 是一个规范的双射，其逆映射为“删去首两位与末两位”。证明要点完全构造性：边界词满足首尾为 \(1\)，而在“无相邻 \(11\)”约束下首尾的相邻位必须为 \(0\)，故任意边界词必形如 \(1,0,(\cdots),0,1\)；中间那段自然落在 \(X_n\)。因此计数恒等式
\(|X_{n+4}^{\mathrm{bdry}}|=|X_n|\) 被提升为可计算的结构同构。
\par
在 \(n=6\) 的实例上，这给出
\input{sections/generated/eq_boundary_uplift_m10_isomorphism}
并把 \(m=10\) 的 \(21\) 个边界模态逐一标号为 “\(18\) 个 \(B_3/C_3\) 根方向 + \(3\) 个 Cartan 方向”：对 \(b\in X^{\mathrm{bdry}}_{10}\)，取 \(v=\iota_6^{-1}(b)\in X_6\)，若 \(v\in X_6^{\mathrm{cyc}}\) 则用表 \ref{tab:fold6_b3c3_root_dictionary_b3} 赋根向量，若 \(v\in X_6^{\mathrm{bdry}}\) 则记为 Cartan 方向。该字典由脚本 \texttt{\detokenize{scripts/exp_boundary_uplift_m10_b3c3_dictionary.py}} 自动生成，完整 \(21\) 词清单见表 \ref{tab:boundary_uplift_m10_dictionary}。
\input{sections/generated/tab_boundary_uplift_m10_dictionary}

\paragraph{\texorpdfstring{$B_3/C_3$}{B3/C3} 根方向的 \texorpdfstring{$6+6+6$}{6+6+6} 刚性分裂}
表 \ref{tab:fold6_b3c3_root_dictionary_b3} 给出的 \(B_3\) 根向量不仅呈现粗分裂 \(6+12\)（短根/长根），其 \(12\) 个长根还进一步刚性分解为两套互不相交的 \(A_2\) 根系：
\[
\Phi_{\mathrm{long}}=\Phi_{A_2(+)}\sqcup\Phi_{A_2(-)},\qquad
\Phi_{A_2(+)}=\{\pm(e_1+e_2),\pm(e_1+e_3),\pm(e_2+e_3)\},
\]
\[
\Phi_{A_2(-)}=\{\pm(e_1-e_2),\pm(e_1-e_3),\pm(e_2-e_3)\}.
\]
其中 \(A_2(+)\) 与 \(A_2(-)\) 的区分仅依赖“两个非零分量同号/异号”的符号结构，因而不依赖额外选择；这把 \(X_6^{\mathrm{cyc}}\) 进一步刚性分裂为
\[
X_6^{\mathrm{cyc}}=X_6^{\mathrm{short}}\ \sqcup\ X_6^{A_2(+)}\ \sqcup\ X_6^{A_2(-)},
\qquad 18=6+6+6,
\]
其显式词表与根向量对应已经并入表 \ref{tab:boundary_uplift_m10_dictionary} 的 uplift 字典中（由上述脚本自动生成）。
结合上一段的“四步平移的规范 uplift 同构”，这一分裂把 \(m=10\) 的边界接口固定为
\(
6\ (\text{short})\ +\ 6\ (A_2(+))\ +\ 6\ (A_2(-))\ +\ 3\ (\text{Cartan})
\)
的四块结构，为后续谱隙对齐或分块耦合分析提供硬指标。

\paragraph{各向同性点的统一判据与 CAP-II 常数组合标定}
注 \ref{rem:sp6-minimal-continuous-envelope} 已将 \(\Sigma(\theta)=\nabla^2P(\theta)\) 的各向同性扫描作为 \(C_3=\mathrm{Sp}(6)\) 连续包络的可审计入口。
将“四步平移的规范 uplift 同构”给出的 uplift 字典与“根方向的 \(6+6+6\) 刚性分裂”给出的根字典同该扫描联立，可给出一个更强的统一判据：在某个“统一点” \(\theta^*\) 上，根向量的有效长度应只依赖根型（短/长），而不依赖具体方向；因此在 \(\theta^*\) 附近，\(m=10\) 的 \(21\) 个边界模态应呈现最简分裂
\[
12\ (\text{长根，含 }A_2(+)\oplus A_2(-))\ \oplus\ 6\ (\text{短根})\ \oplus\ 3\ (\text{Cartan}),
\]
并且在度量 \(g=\Sigma(\theta)^{-1}\) 下满足
\[
R_{ls}(\theta):=\sqrt{\frac{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{\mathrm{long}}}}{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{\mathrm{short}}}}}
\approx \sqrt{2},\qquad
R_{pm}(\theta):=\sqrt{\frac{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{A_2(+)}}}{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{A_2(-)}}}}
\approx 1.
\]
脚本 \texttt{\detokenize{scripts/exp_sp6_isotropy_root_split_ratio.py}} 直接读取各向同性扫描输出
\texttt{\detokenize{artifacts/export/sync_kernel_weighted_sp6_isotropy_scan.json}} 并计算 \(R_{ls},R_{pm}\)，从而把上述判据落到可复核的数值输出上。
\par
进一步，结合附录定义 \ref{def:appH0-lapse-proxy} 的主标量 \(u=\log(\kappa/\kappa_0)\) 与 CAP-II 的 Fisher-amplitude 场（为避免与本文的 arity-charge \(\chi\) 混淆，记为 \(\chi_F\)），
若取 \(\rho=\rho_0 e^{p u}\) 与 \(\chi_F=\sqrt{\rho}\)，则
\[
|\nabla\chi_F|^2=\rho_0\,\frac{p^2}{4}\,e^{p u}\,|\nabla u|^2.
\]
在各向同性点 \(\Sigma(\theta^*)\simeq\sigma^2 I\) 时，二阶涨落给出的二次型规模即由 \(\sigma^2\) 读出，从而提供一个可计算的常数组合标定：
\(
\lambda_F\,\rho_0\,\frac{p^2}{4}\ \approx\ \sigma^2
\)
（\(\sigma^2\) 由 \(\Sigma(\theta^*)\) 的特征值给出）。这把“统一判据”与 CAP-II 的有效标度直接绑定为同一个可审计对象。

\paragraph{\texorpdfstring{$C_6$}{C6} 轨道刚性：从内禀环旋转分块到 Pati--Salam 型 Levi 骨架候选}
上段使用 \(\,X_6=X_6^{\mathrm{cyc}}\sqcup X_6^{\mathrm{bdry}}\) 的 \(18\oplus 3\) 计数接口来锁定 rank-\(3\) 的简单李型种子。
同一窗口 \(m=6\) 还携带一个与“根长短”无关、而仅由\emph{环结构}诱导的内禀对称动作：令
\[
\rho(u_1u_2u_3u_4u_5u_6):=u_2u_3u_4u_5u_6u_1,
\]
则 \(\rho\) 生成 \(C_6\) 的环旋转作用。
由于 \(X_6^{\mathrm{cyc}}\) 等价于“无相邻 \(11\)”并满足 \(u_1u_6=0\) 的闭词子集，故 \(C_6\) 作用在 \(X_6^{\mathrm{cyc}}\) 上封闭。
该作用的轨道分解在 window-6 上是刚性的：
\input{sections/generated/eq_window6_c6_orbit_decomposition}
\input{sections/generated/tab_window6_c6_orbit_decomposition}
从置换表示的观点看，轨道多重集合 \(\{1,2,3,6,6\}\) 还唯一确定了复置换模 \(\CC[X_6^{\mathrm{cyc}}]\) 的 \(C_6\)-模同构类型及其线性角色重数（即环旋转的 Fourier 签名）：
\input{sections/generated/eq_window6_c6_character_decomposition}
因此对任意可观测量 \(f:X_6^{\mathrm{cyc}}\to\CC\)，其在环旋转 \(\rho\) 下的六个本征模态 \(\chi_k\) 的可出现维数由向量 \((m_k)\) 刚性锁定。
\par
该 Fourier 签名由 orbit 数据确定，因而与上文的 \(B_3/C_3\) 根字典（weight 分层与 \(|W|=48\) 的轨道核验）构成两套相互独立的刚性不变量：任意生成元对齐或观测通道对齐需同时兼容两类约束。
\par
注意上式最后一行给出一种按 orbit 尺寸阈值形成的 \(15\oplus 3\) 细分，并且其 \(3\) 维小块仅支持 \(k\in\{0,3\}\) 的 Fourier 模态；而为构造与 Pati--Salam 伴随分块逐轨道对齐的字典，下段将采用另一种以\emph{唯一三元轨道}为锚的 \(15\oplus 3\) 细分。
因此 cyclic 扇区不仅给出 \(18\) 维“根式”接口，还在 orbit 层面强迫出一组可审计的 \(C_6\)-不变分块。
令 \(\mathcal{O}(w):=\{\rho^k(w):k=0,1,\dots,5\}\subseteq X_6^{\mathrm{cyc}}\)。
由表 \ref{tab:window6_c6_orbit_decomposition}，存在唯一的三元轨道 \(\mathcal{O}(\texttt{001001})\)，并且其余四条轨道的并为一个 \(15\) 维块：
\[
X^{\mathrm{cyc},3}_6:=\mathcal{O}(\texttt{001001}),\qquad
X^{\mathrm{cyc},15}_6:=\mathcal{O}(\texttt{000000})\sqcup \mathcal{O}(\texttt{010101})\sqcup \mathcal{O}(\texttt{000001})\sqcup \mathcal{O}(\texttt{000101}).
\]
则 \(|X^{\mathrm{cyc},15}_6|=15\)、\(|X^{\mathrm{cyc},3}_6|=3\)。
结合 \(|X_6^{\mathrm{bdry}}|=3\)，得到一个完全由 \(m=6\) 内禀动作导出的三块维数骨架
\[
21=15\oplus 3\oplus 3.
\]
在“只允许紧半单李代数作连续包络”的最小化口径下，该维数分块将候选 Levi 骨架锁定为
\[
\boxed{\;\mathfrak g_{6}^{\mathrm{Levi}}\ \cong\ \mathfrak{su}(4)\oplus\mathfrak{su}(2)_L\oplus\mathfrak{su}(2)_R\;}\qquad(\dim=15+3+3=21),
\]
即 Pati--Salam 型统一骨架（此处作为纯结构候选，不引入额外指称）。
在该骨架下，表 \ref{tab:window6_c6_orbit_decomposition} 的 orbit 维数可被组织为一个“伴随分块字典”：
\begin{itemize}
  \item \(1\) 维轨道（\texttt{000000}）可与 \(\mathfrak{su}(4)\) 中与 \(\mathfrak{su}(3)\) 可交换的 \(\mathfrak{u}(1)\) 方向对齐；
  \item \(2\) 维轨道（\texttt{010101}）与 \(\mathfrak{su}(3)\) 的 Cartan 子代数对齐（\(\dim=2\)）；
  \item 两条 \(6\) 维轨道分别与 \(\mathfrak{su}(3)\) 的非对角阶梯方向（\(\dim=6\)）与
  \(\mathfrak{su}(4)/(\mathfrak{su}(3)\oplus\mathfrak{u}(1))\) 的桥接方向（\(\mathbf{3}\oplus\bar{\mathbf{3}}\)，\(\dim=6\)）对齐；
  \item cyclic 扇区剩余的 \(3\) 维轨道（\texttt{001001}）与一个 \(\mathfrak{su}(2)_L\) 伴随块对齐；boundary 扇区的 \(3\) 则对应 \(\mathfrak{su}(2)_R\) 伴随块。
\end{itemize}
进一步地，\(15\) 维块内部的 \(1\oplus 2\oplus 6\oplus 6\) 轨道尺寸与
\(
\mathfrak{su}(4)\supset \mathfrak{su}(3)\oplus \mathfrak{u}(1)
\)
的伴随分解在“Cartan/step/bridge”维数层级上精确对齐，为后续在更高分辨率用扭曲谱/异常签名判别两种统一种子（\(B_3/C_3\) vs.\ Levi-\(SU(4)\times SU(2)\times SU(2)\)）提供可审计靶标。

\paragraph{\texorpdfstring{$9+6+3+3$}{9+6+3+3} 的四块分解：把 \texorpdfstring{$21$}{21} 维接口升级为 Pati--Salam 玻色子计数种子}
window-6 的 cyclic 扇区权重直方图为 \(\{0:1,1:6,2:9,3:2\}\)，而 boundary 扇区为三元组 \(X_6^{\mathrm{bdry}}\)。
因此在 \emph{不引入任何新结构} 的前提下，\(X_6\) 还被强迫出一个更细的、完全可枚举审计的四块分解：
\[
X_6
=X^{(2)}_6\ \sqcup\ X^{(1)}_6\ \sqcup\ X^{(L)}_6\ \sqcup\ X^{(R)}_6,
\qquad
21=9+6+3+3,
\]
其中
\(
X^{(2)}_6=\{w\in X_6^{\mathrm{cyc}}:\mathrm{wt}(w)=2\}
\),
\(
X^{(1)}_6=\{w\in X_6^{\mathrm{cyc}}:\mathrm{wt}(w)=1\}
\),
\(
X^{(L)}_6=\{w\in X_6^{\mathrm{cyc}}:\mathrm{wt}(w)\in\{0,3\}\}
\),
\(
X^{(R)}_6=X_6^{\mathrm{bdry}}
\)；
四块的显式词表见表 \ref{tab:window6_patisalam_9633_partition}。
\input{sections/generated/tab_window6_patisalam_9633_partition}
\par
把这 \(21\) 个稳定类型当作“规范生成元标签候选”时，该四块计数在纯维数层级上与 Pati--Salam 的玻色子分解
\[
21=(8+1)+6+3+3
\]
逐块对齐：\(9\) 对应 \(\mathfrak{su}(3)\oplus\mathfrak{u}(1)\) 的非元组计数接口，\(6\) 对应
\(\mathfrak{su}(4)/(\mathfrak{su}(3)\oplus\mathfrak{u}(1))\) 的桥接方向（\(\mathbf{3}\oplus\bar{\mathbf{3}}\)）计数接口，而两组 \(3\) 给出两个 \(\mathfrak{su}(2)\) 伴随块的离散锚（命名上沿用 boundary=\(R\) 的约定：\(X^{(R)}_6\) 作为 \(\mathfrak{su}(2)_R\) 的锚，\(X^{(L)}_6\) 为另一 \(\mathfrak{su}(2)\) 锚）。
注意该四块分解与上文的 orbit-Levi 骨架是兼容的（因为 \(15=(9+6)\)），但它采用的是 weight-layer 对齐；因此其 word-to-generator 的对齐字典一般不要求与 orbit-对齐字典相同。
此处强调：结论是\emph{离散计数与标签层级}的强迫；若要把它提升为“闭合的李代数结构定理”，仍需在本文的协议口径内指定一个可计算 bracket 来源并对闭包作审计（见 \S\ref{subsec:group-unification-audit-pointers} 中关于 \(C_6\) 轨道分解与 \(21=15\oplus 3\oplus 3\) Levi 骨架候选的并列可复核接口）。

\paragraph{boundary 扇区的 \texorpdfstring{$\delta=34$}{δ=34} 双层覆盖：内生 \texorpdfstring{$\ZZ_2$}{Z2} sheet parity}
在 dyadic 读出（附录 \ref{app:fold-multiplicity} 的二进制区间折叠 \(\mathrm{Fold}^{\mathrm{bin}}_6\)）下，
window-6 的 uplift 偏移集合是一个硬算术对象，并且末位为 \(1\) 的类型被强制限制在两层：
\input{sections/generated/eq_fold6_bin_uplift_delta_set}
由于边界词满足 \(u_6=1\)（更强地 \(u_1=u_6=1\)），因此每个 \(w\in X_6^{\mathrm{bdry}}\) 都携带一个规范的二层覆盖
\(
(\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w)=\{V_6(w),\,V_6(w)+34\}
\)
（差值严格为 \(34=F_9\)），并由此诱导出一个无歧义的 \(\ZZ_2\) 分级（sheet parity）：
\[
\pi_{\mathrm{sheet}}(V_6(w)):=0,\qquad \pi_{\mathrm{sheet}}(V_6(w)+34):=1.
\]
该 \(\ZZ_2\) 分级来自 dyadic 读出与 uplift 偏移的硬约束，因而不能由 \(C_6\) 轨道数据或 \(B_3/C_3\) 根字典的 weight 分层反推出；它在协议层提供一条独立的离散选择通道，可与后续的耦合/投影门审计条件并列使用。
进一步，若将 boundary 扇区在 dyadic uplift 上的二层预像视为一个\emph{提升后的边界种子}
\[
\widetilde X_6^{\mathrm{bdry}}
:=\bigsqcup_{w\in X_6^{\mathrm{bdry}}}(\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w),
\]
则 \(\pi_{\mathrm{sheet}}\) 给出一个规范的二层分解
\[
\widetilde X_6^{\mathrm{bdry}}=\widetilde X^{\mathrm{bdry}}_{6,+}\ \sqcup\ \widetilde X^{\mathrm{bdry}}_{6,-},
\qquad
\widetilde X^{\mathrm{bdry}}_{6,+}:=\{n\in\widetilde X_6^{\mathrm{bdry}}:\ \pi_{\mathrm{sheet}}(n)=0\},
\quad
\widetilde X^{\mathrm{bdry}}_{6,-}:=\{n\in\widetilde X_6^{\mathrm{bdry}}:\ \pi_{\mathrm{sheet}}(n)=1\},
\qquad
|\widetilde X^{\mathrm{bdry}}_{6,\pm}|=3,
\]
从而在 parity-graded 口径下，window-6 的 seed 不仅携带 \(21=15\oplus 3\oplus 3\) 的维数骨架，还伴随一个内生的 \(3_+\sqcup 3_-\) 二层通道（由 lift 结构给出，而非由 orbit/weight 结构决定）。
三条边界词的显式 preimage 对见表 \ref{tab:fold6_boundary_sheet_pairs}。
\input{sections/generated/tab_fold6_boundary_sheet_pairs}
作为跨分辨率稳健性检验，在同一 dyadic 折叠口径下，boundary 扇区的 uplift 结构从 \(m=6\) 的二层提升为 \(m=7,8\) 的三层（对应的 \(\delta\) 组合见表 \ref{tab:foldbin_boundary_lift_m6_m8}）。
因此，window-6 的 \(\ZZ_2\) sheet parity 在该读出下是一个\emph{最小窗特有}的硬结构事实；若要把它推广为跨 \(m\) 的残余选择律，需要额外的协议门/投影约束在更高分辨率上重新锁定二层覆盖（该要求本身是可审计、可反驳的）。
\input{sections/generated/tab_foldbin_boundary_lift_m6_m8}

\paragraph{\texorpdfstring{$F_9$}{F9} 门户锁定：\texorpdfstring{$34$}{34} 同时是 window-6 的 uplift 偏移与 \texorpdfstring{$m=6\to 8$}{m=6->8} 共振缺口}
表 \ref{tab:fibonacci_lie_resonance_ladder} 给出下一个稳定类型维数共振点为 \(m=8\)，且
\(
|X_6|=21,\ |X_8|=55
\)。
因此缺口维数严格为
\[
|X_8|-|X_6|=55-21=34=F_9.
\]
结合上段的硬约束 \(w_6=1\Rightarrow \Delta(w)=\{0,34\}\)，得到一个可审计的“门户”事实：
boundary 扇区（更强地，端点纤维 \(X_6^{\mathrm{bdry}}\subset\{w:w_6=1\}\)）在 dyadic uplift 上只能通过 \(\delta=34\) 被配对抬升，
而 \(\delta=34\) 又恰好是从 \(m=6\) 走向下一共振层 \(m=8\) 的维数缺口。
这把“右手/高能/被破缺”之类的解释语义（若读者愿意采用）落回到一个纯离散的锁：\(\delta\) 的可选集合与共振缺口在同一整数 \(34\) 上重合。

\paragraph{尾部可选偏移数的 \texorpdfstring{$4\to 3\to 2$}{4->3->2} 塌缩：分辨率截断诱导的离散破缺开关}
对任意 \(w\in X_6\) 定义 uplift-delta 集与其基数
\[
\Delta(w):=\{\,n-V_6(w): n\in (\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w)\,\},\qquad
\Xi(w):=|\Delta(w)|.
\]
则在 window-6 的有限域 \(n\in\{0,\dots,63\}\) 截断下，\(\Xi(w)\) 被强迫塌缩到三个离散值 \(\{4,3,2\}\)，并且其触发条件仅依赖于末位 \(w_6\) 与阈值 \(V_6(w)\le 8\)（表 \ref{tab:fold6_bin_uplift_choice_collapse} 给出完整词表分区）：
\input{sections/generated/tab_fold6_bin_uplift_choice_collapse}
\par
注意这三个数 \(4,3,2\) 与 \(SU(4),SU(3),SU(2)\) 基本表示的最小内部维数完全一致。
因此“统一与破缺”可在本框架里被读成一个纯组合学的机制原型：\emph{分辨率截断导致尾部可用偏移数减少（内部标签态数塌缩）}，
无需先验写入外加标量场；而 \(\Xi(w)\) 本身即可作为一个可审计的离散序参量（参见 \S\ref{subsec:group-unification-audit-pointers} 中关于 window-6 \(C_6\) 轨道与 dyadic uplift 的审计链条及其 JSON 证书 \texttt{\detokenize{artifacts/export/window6_c6_orbit_patisalam_seed.json}}）。

\begin{remark}[三通道势—响应辛相空间与 \texorpdfstring{$C_3=\mathrm{Sp}(6)$}{C3=Sp(6)} 连续包络]\label{rem:sp6-minimal-continuous-envelope}
在附录 \ref{app:vector-potential} 的向量势口径下，给定三维势参数 \(\theta=(\theta_1,\theta_2,\theta_3)\) 的压力
\(
P(\theta)=\log\rho(B(\theta))
\)
可完全由有限矩阵的谱半径读出。
对每一通道 \(i\in\{1,2,3\}\) 定义响应（密度/流）
\[
J_i(\theta):=\partial_{\theta_i}P(\theta),
\]
则 \((\theta;J)\in\RR^6\) 给出一个自然的“势—响应相空间”，并携带标准辛形式
\[
\omega:=\sum_{i=1}^{3}\dd\theta_i\wedge \dd J_i.
\]
允许三通道混合但保持共轭结构（即保持 \(\omega\)）的最小连续线性群为
\[
\mathrm{Aut}(\omega)=\mathrm{Sp}(6),
\]
它在本文的 \(B_3/C_3\) 离散统一种子中对应 \(C_3\)（长短根对偶类型的“辛侧”）。
更重要的是，该连续包络与 \(m=6\) 的离散锚点在生成元计数上精确对齐：
\[
\dim\mathfrak{sp}(6)=3(2\cdot 3+1)=21=|X_6|.
\]
因此，把 \(m=6\) 的 \(21\) 维计数接口解释为 rank-\(3\) 统一代数的最小连续包络时，\(\mathrm{Sp}(6)\) 是一个由“势—响应”坐标结构内生逼出的候选，而非外加猜测。
对该包络的一个可审计数值检验（通过 \(\nabla^2P(\theta)\) 的“单尺度/各向同性”程度扫描 \((u,v,w)=(e^{\theta_1},e^{\theta_2},e^{\theta_3})\)）见附录注 \ref{rem:sync-kernel-3d-sp6-isotropy-scan} 与表 \ref{tab:sync_kernel_weighted_sp6_isotropy_scan}。
\end{remark}

\paragraph{\texorpdfstring{$\mathrm{Sp}(6)$}{Sp(6)} 的 $9+12$ Cartan 分解：\texorpdfstring{$U(3)$}{U(3)} 紧核与 SM=12 的刚性对齐}
在本节中 \(\mathrm{Sp}(6)\) 以实辛群 \(\mathrm{Sp}(6,\RR)\) 的语境出现，其最大紧子群为 \(U(3)\)，并有标准 Cartan 型分解
\[
\mathfrak{sp}(6,\RR)=\mathfrak{u}(3)\oplus \mathfrak{p},\qquad \dim\mathfrak{u}(3)=9,\ \dim\mathfrak{p}=12.
\]
因此在不引入任何新参数的前提下，\(\dim\mathfrak{sp}(6)=21\) 自带一个刚性的 \(9+12\) 维度劈裂：
\(9\) 维紧核可解释为三通道的基混合/通道旋转自由度，
\(12\) 维非紧补空间在计数上与边界三层塔的 SM 玻色子数 \(1+3+8=12\) 精确对齐。
结合注 \ref{rem:fib-lie-extra-9} 的“\(21-12=9\)”刚性指纹，
该 \(U(3)\) 紧核给出一个内生的“隐藏对称/家族样混合”候选，
并为后续的谱隙对齐分块检验提供自然的 \(9\) vs.\ \(12\) 通道划分（见 \S\ref{subsec:group-unification-spectral-alignment}）。

\paragraph{Zeckendorf--GUT 签名：统一代数 \texorpdfstring{$\leftrightarrow$}{<->} 分辨率层组合}
对任意候选统一李代数 \(\mathfrak g\)，令 \(D:=\dim\mathfrak g\)（即规范玻色子总数）。
以 Zeckendorf 唯一分解写
\[
D=\sum_{k\in S}F_k,\qquad S\ \text{不含相邻指标},
\]
并定义其\textbf{分辨率签名}
\[
\Sigma(\mathfrak g):=\{\,m=k+2:\ k\in S\,\}.
\]
由于 \(F_k=|X^{\mathrm{bdry}}_{k+2}|\)，这等价于
\[
D=\sum_{m\in\Sigma(\mathfrak g)}|X_m^{\mathrm{bdry}}|,
\]
并且由 Zeckendorf 唯一性，\(\Sigma(\mathfrak g)\) 也是\emph{唯一的}（无冗余激活）分辨率层集合。
若把“可观测的 gauge 模态”理解为某些边界层 \(X_m^{\mathrm{bdry}}\) 在协议核/投影门下被激活的子塔，
则 \(\Sigma(\mathfrak g)\) 提供了一套可反驳的字典：只需在所采用的可审计协议中确定哪些 \(m\) 参与了可观测作用，即可把观测落入对应的统一签名。

\input{sections/generated/tab_zeckendorf_gut_signatures}

\begin{corollary}[window-\(6\) uplift 的三项尾部偏移与 GUT 顶层 Zeckendorf 项的离散对应]\label{cor:fold6-tail-offsets-gut-top-terms}
在 window \(m=6\) 上，对任意 \(w\in X_6\) 的 uplift 预像（在有限域截断或 dyadic 读出下）只可能在 Zeckendorf 值
\(
V_6(w)=\sum_{k=1}^{6}w_kF_{k+1}
\)
之上叠加三种非零偏移：
\[
\Delta_{\mathrm{tail}}(6)\subseteq \{F_8,F_9,F_{10}\}=\{21,34,55\}
\]
（见 \cite{Internal2025ResolutionFoldingPhiPiE} 的 \(\mathrm{Fold}_6\) 预像分类；并与本仓库的 dyadic uplift 审计输出一致）。
另一方面，表 \ref{tab:zeckendorf_gut_signatures} 中
\[
24=F_8+F_4,\qquad 45=F_9+F_6+F_4,\qquad 78=F_{10}+F_8+F_3
\]
显示：\(SU(5),SO(10),E_6\) 的 Zeckendorf--GUT 分解的最高 Fibonacci 项分别为 \(F_8,F_9,F_{10}\)。
因此，在仅保留“顶层触发项”的口径下，window-\(6\) 的第一次 uplift 具有一个天然的\textbf{离散三分支}：
选择 \(\,F_8/F_9/F_{10}\,\) 分别与 \(SU(5)/SO(10)/E_6\) 型候选的顶层项对齐。
\end{corollary}

\paragraph{最小预测（离散跳跃而非连续可调）}
一旦接受“规范玻色子 = 边界词塔模态数”的解释，SM 的签名为
\(\Sigma_{\mathrm{SM}}=\{4,6,8\}\)。
那么“超越 SM 的新增规范模态”不再是连续可调参数，而必须以 Fibonacci 边界层作离散跳跃：
最小的上跳新增层为 \(m=9\)（新增 \(F_7=13\) 模态）或 \(m=10\)（新增 \(F_8=21\) 模态）。
其中 \(m=10\) 的 \(21\) 模态允许一个与 \(SU(5)\) 重玻色子计数同型的 \(21-(8+1)=12\) 余量拆分，
为后续把“高层边界塔向低层三塔的投影/补偿”做成算子化闭合提供直接靶标。

\paragraph{家族数 \(N_f\) 与 window-6 uplift 三分支的硬对应：\(F_8/F_9/F_{10}\)}
取每代 \(SU(5)\) 型物质块的标准计数 \(15\) 作为外部输入，并定义总计数
\(
M(N_f):=15N_f
\)。
则对 \(N_f\in\{2,3,4\}\)，\(M(N_f)\) 的 Zeckendorf 分解最高项依次为 \(F_8,F_9,F_{10}\)（表 \ref{tab:window6_family_uplift_lock}）。
结合推论 \ref{cor:fold6-tail-offsets-gut-top-terms} 的“window-6 uplift 离散三分支”对应，
家族数 \(N_f\) 在 window-6 层级被提升为一个\emph{分支选择参数}：
特别地，\(N_f=3\) 精确选中 \(F_9=34\) 分支，从而将 \(SO(10)\) 顶层项对齐的分支在 window-6 上离散锁定为自然统一候选。
\input{sections/generated/tab_window6_family_uplift_lock}
\par
该结论的要点在于：它把 \(N_f\) 从传统的外加输入，转写为本框架内可被 Zeckendorf--uplift 结构审计的离散选择量。

\paragraph{window-6 边界扇区的三通道 singlet 槽位：\(45+3=3\times 16\) 的刚性补全}
window-6 的边界扇区基数被黄金语法刚性固定为 \(|X_6^{\mathrm{bdry}}|=F_4=3\)。
因此在 \(N_f=3\) 时，标准 \(SU(5)\) 物质计数 \(15N_f=45\) 与 boundary 通道数给出一个离散补全恒等式（由脚本生成）：
\input{sections/generated/eq_window6_family_singlet_completion}
其中 \(16\) 可读作 \(SO(10)\) 自旋表示维数（在 \(SU(5)\subset SO(10)\) 嵌入下分解为 \(15\oplus 1\)），
从而该恒等式提供一个可审计的槽位解释：每代“额外的一个 \(SU(5)\) singlet”（例如右手中微子型通道）天然锚定在 boundary sector，而非 cyclic/bulk 稳定态的自由添加。
\par
\emph{可检验推论}：
\begin{enumerate}
  \item 若要求可观测耦合与上文定义的 sheet parity \(\pi_{\mathrm{sheet}}\) 分级相容，则 bulk 与 boundary 的混合项必须经由 \(\delta=34\) 的 sheet 翻转实现，从而给出无需连续微调的离散选择定则（“\(\delta\)-奇偶”）。
  \item 在不提升到更高 window 的前提下，独立 boundary 通道数严格为 \(3\)；因此任何需要多于 \(3\) 个独立 singlet 通道的构造都必须进入 \(m\ge 7\) 的更高分辨率并接受 uplift-tail 结构整体改写（例如 dyadic lift 在 \(m=7,8\) 上不再是二层覆盖，见表 \ref{tab:foldbin_boundary_lift_m6_m8}）。
\end{enumerate}

\paragraph{\texorpdfstring{$X_6$}{X6} 内部的 $1+8+12$ 离散劈裂：\texorpdfstring{$SU(5)$}{SU(5)} 轻/重玻色子字典}
由 dyadic 折叠的 uplift-choice 阈值（表 \ref{tab:fold6_bin_uplift_choice_collapse}），定义
\[
S_{\mathrm{low}}:=\{w\in X_6:\ w_6=0,\ V_6(w)\le 8\},
\]
则 \(|S_{\mathrm{low}}|=9\)，并且其中唯一的零词 \(\texttt{000000}\) 与其余 \(8\) 个非零词自动给出一个
\(
X_6=1\oplus 8\oplus 12
\)
的离散劈裂：
\[
\begin{aligned}
X_6^{\mathrm{zero}}&:=\{\texttt{000000}\},\\
X_6^{\mathrm{light}}&:=\{\texttt{000010},\ \texttt{000100},\ \texttt{001000},\ \texttt{010000},\\
&\qquad \texttt{010100},\ \texttt{100000},\ \texttt{100100},\ \texttt{101000}\},\\
X_6^{\mathrm{heavy}}&:=\{\texttt{000001},\ \texttt{000101},\ \texttt{001001},\ \texttt{010001},\ \texttt{010101},\\
&\qquad \texttt{001010},\ \texttt{010010},\ \texttt{100010},\ \texttt{101010},\ \texttt{100001},\\
&\qquad \texttt{100101},\ \texttt{101001}\}.
\end{aligned}
\]
通过 \(|X_{10}^{\mathrm{bdry}}|=|X_6|\) 的四步平移（注 \ref{rem:fib-lie-resonance-shift-by-4}），
可用标准嵌入 \(u\mapsto 10\,u\,01\) 将该字典抬回 \(m=10\) 的边界层，
从而把 “\(21-(8+1)=12\)” 从计数目标升级为一份可枚举的轻/重玻色子离散字典。

\begin{corollary}[\texorpdfstring{$SU(5)$}{SU(5)} 的 \(21+3\) 计数闭合：从 \(m=6\) 统一种子到 \(24\) 槽位]\label{cor:su5-21-plus-3-closure}
由表 \ref{tab:zeckendorf_gut_signatures}，\(SU(5)\) 的 Zeckendorf 分解为 \(24=F_8+F_4\)，其分辨率签名为
\(\Sigma(SU(5))=\{10,6\}\)。
利用上文的四步 uplift 同构 \(X_n\simeq X_{n+4}^{\mathrm{bdry}}\)，该分解等价地给出一个稳定层计数闭合：
\[
24=|X^{\mathrm{bdry}}_{10}|+|X^{\mathrm{bdry}}_{6}|=|X_{6}|+|X_{2}|=21+3.
\]
其中 \(|X_6|=21\) 的稳定层已携带 \(B_3/C_3\) 统一种子及其 \(9+12\)、\(1+8+12\) 的刚性分块，因而在计数与分块层级上已内生包含“轻扇区 \((8+1)\) 与重余量 \(12\)”的槽位结构；
而 \(|X_2|=3\) 则提供一个与 \(\mathfrak{su}(2)\) 伴随维数同型的最小三模态补块。
因此，在不把 \(SU(5)\) 作为先验主群的口径下，可将 \(SU(5)\) 的 \(24\) 玻色子计数理解为：在 \(m=6\) 的 rank-\(3\) 种子之上进行一次最小的 \(+3\) 闭合（“追加闭合”），而不必改变该 seed 的 \(21\) 维内禀分块结构。
\end{corollary}

\paragraph{重余量 $12$ 的 $8+4$ 两档与熵权重原语}
沿用上文对 dyadic uplift 计数 \(\Xi(w)=|\Delta(w)|\)（亦即 \(d^{\mathrm{bin}}_6(w)\)）的定义，
由表 \ref{tab:fold6_bin_uplift_choice_collapse} 立刻得到：
\[
X_6^{\mathrm{heavy}}=X_{6,2}^{\mathrm{heavy}}\sqcup X_{6,3}^{\mathrm{heavy}},
\qquad |X_{6,2}^{\mathrm{heavy}}|=8,\ |X_{6,3}^{\mathrm{heavy}}|=4,
\]
其中
\(
X_{6,2}^{\mathrm{heavy}}=\{w\in X_6^{\mathrm{heavy}}:w_6=1\}
\)
对应 \(\Xi(w)=2\)，
\(
X_{6,3}^{\mathrm{heavy}}=\{w\in X_6^{\mathrm{heavy}}:w_6=0,\ V_6(w)>8\}
\)
对应 \(\Xi(w)=3\)。
因此重玻色子并非一档，而是内生地分成 \(8\oplus 4\) 两档。
引入熵权重
\[
h(w):=\log \Xi(w)\in\{\log 2,\log 3,\log 4\},
\]
即可将该两档结构直接编码为离散原语；在 cyclic 子集 \(X_6^{\mathrm{cyc}}\) 内部，
\(h(w)\) 进一步切出 \(9\oplus 4\oplus 5\) 的三档分级（对应 \(\Xi=4,3,2\)；仍由表 \ref{tab:fold6_bin_uplift_choice_collapse} 枚举），
为把 \(B_3/C_3\) 的根样方向细化成“加权根系”提供一个可审计通道，并可作为谱隙对齐的分块权重（见 \S\ref{subsec:group-unification-spectral-alignment}）。

\paragraph{bin-fold 退化谱的双尺度统一选择律：SM=12 与 \texorpdfstring{$\dim=55$}{dim=55} 的两路锁定}
对二进制区间折叠 \(\mathrm{Fold}^{\mathrm{bin}}_m:\{0,\dots,2^m-1\}\to X_m\)（定义 \ref{def:fold-bin}），
记退化度（纤维大小）
\[
d^{\mathrm{bin}}_m(w):=\bigl|\mathrm{Fold}^{\mathrm{bin}}_m{}^{-1}(w)\bigr|,\qquad w\in X_m.
\]
在该口径下，可给出一组不依赖外加“分解审美”、仅依赖可枚举退化谱的接口化结论簇：
\begin{enumerate}
  \item \textbf{（SM 的非人为规约：\(12=8+4\) 由 \(m=6\) 的刚性分层读出）}
  由推论 \ref{cor:fold-bin-m6-fiber-hist}，\(m=6\) 时 \(d^{\mathrm{bin}}_6(w)\in\{2,3,4\}\)，并且
  \(\#\{d^{\mathrm{bin}}_6=2\}=8\)、\(\#\{d^{\mathrm{bin}}_6=3\}=4\)、\(\#\{d^{\mathrm{bin}}_6=4\}=9\)。
  定义低退化（最刚性）扇区
  \[
  X^{\mathrm{rig}}_6:=\{w\in X_6:\ d^{\mathrm{bin}}_6(w)\le 3\},
  \]
  则
  \[
  \boxed{\ |X^{\mathrm{rig}}_6|=8+4=12\ }.
  \]
  因而“SM 的可观测规范自由度计数为 \(12\)”在不借助边界三层塔分解的前提下，
  可直接由 bin-fold 的退化谱刚性读出（与本节前述 \(1+3+8\) 边界塔计数接口相互独立）。

  \item \textbf{（暗容量补全的统一维数锚点：\(12+(64-21)=55\)）}
  定义 \(m=6\) 的 ghost microstates 容量
  \[
  H^{\mathrm{bin}}_6:=2^6-|X_6|
  =\sum_{w\in X_6}\bigl(d^{\mathrm{bin}}_6(w)-1\bigr)
  =43.
  \]
  令
  \[
  D^{\mathrm{bin}}_{\mathrm{GUT}}:=|X^{\mathrm{rig}}_6|+H^{\mathrm{bin}}_6,
  \]
  则得到一个完全由 \(m=6\) 退化谱锁定的统一维数候选：
  \[
  \boxed{\ D^{\mathrm{bin}}_{\mathrm{GUT}}=12+43=55\ }.
  \]
  作为维数层的对照，\(55\) 同时等于 \(\dim\mathfrak{so}(11)\) 与 \(\dim\mathfrak{sp}(10)\)；
  因而若以“ghost 容量需被统一后生成元吸收”为口径，则 \(\dim=55\) 构成一个直接可证伪的候选锚点。

  \item \textbf{（统一机制：离散分辨率两级提升，而非连续破缺参数）}
  定义两级刚性提升（尾部固定）嵌入
  \[
  \iota_m:X_m\hookrightarrow X_{m+2},\qquad
  \iota_m(w_1,\dots,w_m):=(w_1,\dots,w_m,0,1).
  \]
  该提升满足：\(\iota_m\) 为规范单射，且其像落在尾部被强制为 \(01\) 的子集，
  因而在“末位为 \(1\) 强制前一位为 \(0\)”的意义下具有最小尾部可变性。
  相应地，统一/破缺可被表述为一条离散的分辨率流：提升 \(\iota_m\) 与截断（删去末二位）互为一侧逆，
  从而把“高能统一态 \(\to\) 低能规约态”的机制落到可计算的提升—截断算子上。

  \item \textbf{（高分辨率退化谱选择器：在 \(m=10\) 再次锁定 \(55\)）}
  对 \(m\in\{8,10,12\}\)，bin-fold 退化度仅落在三个离散值上，并且其直方图在类型层上刚性固定为
  \[
  \begin{aligned}
  m=8:\ &\{3:21,\,5:11,\,6:23\},\\
  m=10:\ &\{5:55,\,8:52,\,9:37\},\\
  m=12:\ &\{8:144,\,12:85,\,13:148\},
  \end{aligned}
  \]
  其可复核输出见表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12}。
  特别地，在 \(m=10\) 时最小退化扇区
  \[
  X^{\min}_{10}:=\Bigl\{w\in X_{10}:\ d^{\mathrm{bin}}_{10}(w)=\min_{u\in X_{10}} d^{\mathrm{bin}}_{10}(u)\Bigr\}
  \]
  满足
  \[
  \boxed{\ |X^{\min}_{10}|=55\ }.
  \]
  因而 \(\dim=55\) 在两条独立读出路径上同时出现：
  一条来自 \(m=6\) 的“SM 低退化规约 \(12\)”与 ghost 容量 \(43\) 的补全，
  另一条来自 \(m=10\) 的高分辨率最刚性扇区计数。
  \input{sections/generated/tab_fold_bin_degeneracy_spectrum_m6_m12}
\end{enumerate}

\paragraph{\(m=10\) 退化谱的例外核扇区：\texorpdfstring{$52=\dim\mathfrak{f}_4$}{52=dim f4}}\label{par:gut-exceptional-kernel-f4}
在表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的 \(m=10\) 行，
bin-fold 退化谱把稳定类型集合 \(X_{10}\) 按退化度 \(d^{\mathrm{bin}}_{10}(w)\) 分解为三扇区：
\[
X_{10}=\mathcal{S}_{10,5}\ \sqcup\ \mathcal{S}_{10,8}\ \sqcup\ \mathcal{S}_{10,9},\qquad
\bigl(|\mathcal{S}_{10,5}|,|\mathcal{S}_{10,8}|,|\mathcal{S}_{10,9}|\bigr)=(55,52,37),
\]
其中 \(\mathcal{S}_{10,d}:=\{w\in X_{10}: d^{\mathrm{bin}}_{10}(w)=d\}\)。
注意 \(|\mathcal{S}_{10,5}|=55\) 为 Fibonacci 数 \(F_{10}\)，与上文 \(\dim=55\) 的两路锁定一致；
而 \(|\mathcal{S}_{10,8}|=52\) 则与简单例外李代数 \(\mathfrak{f}_4\) 的伴随维数满足精确恒等式
\[
\boxed{\ |\mathcal{S}_{10,8}|=52=\dim\mathfrak{f}_4\ }.
\]
在同一三扇区内，\(|\mathcal{S}_{10,5}|=55\)、\(|\mathcal{S}_{10,9}|=37\) 不落在
\(\{\dim\mathfrak{g}:\mathfrak{g}\in\{G_2,F_4,E_6,E_7,E_8\}\}\) 之中，
并且在表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的其余行（\(m\in\{6,8,12\}\)）中并不出现扇区大小为 \(52\) 的分块，
因此 \(m=10\) 的退化谱在维数层内禀地产生一个可被单独指认的“例外核扇区”\(\mathcal{S}_{10,8}\)；
该指认仅依赖可枚举退化谱分解，而不依赖 Zeckendorf--GUT 维数签名字典所预置的候选群表。

\paragraph{例外核 \texorpdfstring{$52$}{52} 与最大纤维极值模 \texorpdfstring{$26$}{26} 的 \texorpdfstring{$E_6$}{E6} 完备化：\texorpdfstring{$52+26=78$}{52+26=78}}\label{par:gut-e6-completion-52-26}
令 \(D_m:=\max_{x\in X_m} d_m(x)\) 为窗口折叠 \(\Fold_m\) 的最大纤维（定理 \ref{thm:pom-max-fiber}）。
由表 \ref{tab:fold_max_fiber_achievers_hiddenbit_metrics} 可复核地读出
\(
D_{13}=26
\)。
将结论 \ref{par:gut-exceptional-kernel-f4} 的例外核扇区维数 \(52\) 与该极值模尺度配对，则得到刚性维数闭合：
\[
\boxed{\ 52+D_{13}=52+26=78=\dim\mathfrak{e}_6\ }.
\]
因此，可提出一个仅依赖既有两条谱数据的离散完备化模板：以 \(m=10\) 的 bin-fold 退化谱选出例外核（\(\dim\mathfrak{f}_4\)），再以 \(m=13\) 的最大纤维极值尺度给出完备模，从而在维数层闭合到 \(\mathfrak{e}_6\) 的伴随维数。

\paragraph{极值模驱动的例外塔增量：与最大纤维谱逐点对齐}\label{par:gut-maxfiber-exceptional-tower}
由表 \ref{tab:fold_max_fiber_achievers_hiddenbit_metrics} 进一步读出
\[
D_{16}=55,\qquad D_{18}=89,
\]
并结合公开的例外李代数伴随维数
\[
\dim\mathfrak{e}_6=78,\qquad \dim\mathfrak{e}_7=133,\qquad \dim\mathfrak{e}_8=248,
\]
得到两条维数增量恒等式（其增量来源可由最大纤维极值模解释）：
\[
\boxed{\ \dim\mathfrak{e}_7-\dim\mathfrak{e}_6=55=D_{16}\ },
\qquad
\boxed{\ \dim\mathfrak{e}_8-\dim\mathfrak{e}_7=115=89+26=D_{18}+D_{13}\ }.
\]
从而例外塔的上升在维数层可被重写为由最大纤维谱所供给的离散增量补全；该表述不依赖具体的群嵌入选择，其输入仅为折叠投影的极值谱与标准维数表。

\paragraph{\(m=10\) 三扇区的统一链骨架：\texorpdfstring{$(55,52,37)$}{(55,52,37)}}\label{par:gut-trisector-55-52-37}
由表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12}，\(m=10\) 的 bin-fold 退化谱在稳定类型层强制出三扇区骨架 \((55,52,37)\)。
结合结论 \ref{par:gut-exceptional-kernel-f4}--\ref{par:gut-maxfiber-exceptional-tower}，可将其中两块作结构性定位：
\begin{itemize}
  \item \(\mathcal{S}_{10,8}\)（扇区大小 \(52\)）：例外核扇区（\(\dim\mathfrak{f}_4\) 锚点）；
  \item \(\mathcal{S}_{10,5}\)（扇区大小 \(55\)）：完备化尺度扇区（与 \(D_{16}=55\) 以及 \(E_6\to E_7\) 的维数增量对齐）；
  \item \(\mathcal{S}_{10,9}\)（扇区大小 \(37\)）：剩余接口扇区；其作为补余接口的维数地位可由 \(D_{18}\) 的同层分解强制定位（接口化结论 \ref{par:gut-d18-endogenous-split}），并可在最小一致性原则下给出一个表示论语义闭合候选 \(\mathfrak{so}(9)\oplus\mathfrak{u}(1)\)（接口化结论 \ref{par:gut-sector37-so9-u1}；审计判据见 \ref{par:gut-foldbin-fold-audit-criteria}）。
\end{itemize}
该三元骨架以退化谱为第一输入，以例外核/极值模的维数闭合为第二输入，从而把“接口扇区”（\(37\)）提升为必须被解释的剩余对象。

\paragraph{接口化结论：\texorpdfstring{$\mathrm{Fold}^{\mathrm{bin}}$}{Foldbin}–\texorpdfstring{$\mathrm{Fold}$}{Fold} 的跨尺度补余恒等式}\label{par:gut-foldbin-fold-complement-identity}
以下接口化结论簇仅以本文既有的可复现枚举谱数据为输入（尤其是表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的退化谱输出与最大纤维闭式接口），
不引入额外外推或解释层候选；行文亦不复述既有段落，仅记录接口断言与其直接推论/核对协议。
对二进制区间折叠 \(\mathrm{Fold}^{\mathrm{bin}}_m:\{0,\dots,2^m-1\}\to X_m\)，记
\(
d^{\mathrm{bin}}_m(w):=\bigl|\mathrm{Fold}^{\mathrm{bin}}_m{}^{-1}(w)\bigr|
\)
并令 \(d_{\min}(m):=\min_{u\in X_m}d^{\mathrm{bin}}_m(u)\)。
对每个 \(d\) 定义退化谱扇区
\(
\mathcal{S}_{m,d}:=\{w\in X_m:\ d^{\mathrm{bin}}_m(w)=d\}
\)。
则在已公开的偶数窗长 \(m\in\{6,8,10,12\}\) 上，bin-fold 的“最刚性扇区”补余容量与窗口折叠 \(\Fold\) 的更高分辨率最大纤维严格锁定为同一量：
\[
\boxed{\ \bigl|X_m\setminus \mathcal{S}_{m,d_{\min}(m)}\bigr|\;=\;D_{2m-2}\ }.
\]
该恒等式构成一条无需外推的交叉审计闭合：左端由表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的退化谱输出直接给出，而右端由最大纤维闭式定理 \ref{thm:pom-max-fiber}（等价地其表格读出）给出；相应核对式已脚本化输出如下：
\input{sections/generated/eq_foldbin_fold_complement_identity_m6_m12}

\paragraph{接口化结论：\texorpdfstring{$D_{18}$}{D18} 的 \texorpdfstring{$m=10$}{m=10} 内生分解与 \texorpdfstring{$37$}{37} 接口扇区的强制定位}\label{par:gut-d18-endogenous-split}
由接口化结论 \ref{par:gut-foldbin-fold-complement-identity} 的 \(m=10\) 特例可得
\(
D_{18}=|X_{10}\setminus\mathcal{S}_{10,5}|
\)。
另一方面，结论 \ref{par:gut-exceptional-kernel-f4} 已给出同层三扇区分解
\(
X_{10}=\mathcal{S}_{10,5}\sqcup\mathcal{S}_{10,8}\sqcup\mathcal{S}_{10,9}
\)
及其精确计数 \((55,52,37)\)，并将 \(|\mathcal{S}_{10,8}|\) 结构性指认为 \(\dim\mathfrak{f}_4\)。
因此得到同层分解恒等式
\[
D_{18}=|\mathcal{S}_{10,8}|+|\mathcal{S}_{10,9}|=52+37,
\qquad
\boxed{\ |\mathcal{S}_{10,9}|\;=\;D_{18}-\dim\mathfrak{f}_4\ }.
\]
换言之，先前作为“外部极值模尺度”读入的 \(D_{18}\) 可等价改写为：\(m=10\) 的退化谱补余在扣除例外核（\(52\)）后的唯一接口维数（\(37\)）。

\paragraph{接口化结论：\texorpdfstring{$37$}{37} 接口扇区的最小一致语义闭合：\texorpdfstring{$\mathfrak{so}(9)\oplus\mathfrak{u}(1)$}{so(9)+u(1)}}\label{par:gut-sector37-so9-u1}
在不引入额外候选群表、仅依赖本文已固化的两条锚点——例外核 \(|\mathcal{S}_{10,8}|=52=\dim\mathfrak{f}_4\)（结论 \ref{par:gut-exceptional-kernel-f4}）与边界层的自旋成对读出模板 \(33=1\oplus 16\times 2\)（并见 \S\ref{subsec:group-unification-audit-pointers} 中“NAP 选择器与 \(m=11\) 的 \(\ZZ_{34}\) Fourier 分解证书”的可复核接口条目）——可给出 \(37\) 扇区的一个最小一致语义闭合方案。
\par
标准李代数事实表明：\(\mathfrak{f}_4\) 含最大子代数 \(\mathfrak{so}(9)\)，并且在 \(\mathfrak{so}(9)\) 下伴随表示满足
\(
52=36\oplus 16
\)
（其中 \(16\) 为自旋型补余）。
若 \(52\) 扇区承担“例外核”，则与其最小相容的接口应当提供一个 \(\mathfrak{so}(9)\) 的 \(36\) 维骨架并额外携带一个尺度生成元，以实现从核到增量块的黏合；
这与接口化结论 \ref{par:gut-d18-endogenous-split} 所强制的
\(
37=36+1
\)
在维数层严格匹配。
因此提出如下闭合候选：
\[
\boxed{\ \mathcal{S}_{10,9}\text{ 的 \(37\) 维接口可解释为 }\mathfrak{so}(9)\oplus\mathfrak{u}(1)\ }.
\]
其中 \(\mathfrak{so}(9)\) 给出“接口骨架”，而 \(\mathfrak{u}(1)\) 承载与标度/端点精度势相容的单生成元方向；自旋型 \(16\) 维方向则在相邻分辨率层上以成对读出模板出现，从而把“例外核—接口—自旋物质”在相邻层上闭合为一致链条。

\paragraph{接口化结论：直接可证伪的统一审计判据——跨尺度补余、同层分解与不可约性}\label{par:gut-foldbin-fold-audit-criteria}
在结论 \ref{par:gut-exceptional-fingerprint} 的“核—模—塔”指纹之外，上述跨尺度补余恒等式与接口闭合提供一组更直接的失败判据；任一失败即否定对应闭合链条：
\begin{enumerate}
  \item \textbf{跨尺度补余审计（偶数窗长）} 对偶数 \(m\ge 6\)，枚举 \(\mathrm{Fold}^{\mathrm{bin}}_m\) 的退化谱并取 \(d_{\min}(m)\)。应满足
  \[
  |X_m|-|\mathcal{S}_{m,d_{\min}(m)}|=D_{2m-2}.
  \]
  本稿在 \(m\in\{6,8,10,12\}\) 上成立（接口化结论 \ref{par:gut-foldbin-fold-complement-identity}）；其在更大 \(m\) 上的稳定性构成新的否定判据。
  \item \textbf{\texorpdfstring{$D_{18}$}{D18} 的同层分解审计（\texorpdfstring{$m=10$}{m=10}）} 仅用 \(m=10\) 行的退化谱输出，应满足
  \[
  |X_{10}\setminus\mathcal{S}_{10,5}|=|\mathcal{S}_{10,8}|+|\mathcal{S}_{10,9}|=D_{18},
  \]
  即 \(52+37=89\)（接口化结论 \ref{par:gut-d18-endogenous-split}）。
  \item \textbf{\texorpdfstring{$37$}{37} 接口的表示论审计（\texorpdfstring{$\mathfrak{so}(9)\oplus\mathfrak{u}(1)$}{so(9)+u(1)} 方案）}
  若将 \(|\mathcal{S}_{10,9}|=37\) 的语义闭合为 \(\mathfrak{so}(9)\oplus\mathfrak{u}(1)\)，则对 \(\mathcal{S}_{10,9}\) 上由体系内“扫描/平移”或“稳定类型半环乘法/扫描算子”诱导的自然线性化作用，其不变子空间结构应出现：
  \begin{itemize}
    \item 一条 \(1\) 维不变方向（对应 \(\mathfrak{u}(1)\)）；
    \item 其余 \(36\) 维部分表现为单一不可约的 \(\mathfrak{so}(9)\) 伴随型骨架。
  \end{itemize}
  任意不可约分解型式与此不符，即否定该语义闭合方案。
\end{enumerate}

\paragraph{小结（从跨窗拼接到同层补余）}
以上四条把原先分属不同窗长的统一拼接（尤其是 \(D_{18}\) 的极值模输入）改写为 \(m=10\) 同层退化谱的补余结构，
并据此将接口扇区 \(37\) 从“残差计数”提升为可定位、可闭合且可证伪的对象。
若继续向更高阶统一延拓，则下一步的硬任务被明确化为：
检验判据 1 在更大偶数 \(m\) 上的稳定性，以及判据 3 的不可约性审计是否通过。

\paragraph{可证伪审计指纹（例外核—极值模—例外塔）}\label{par:gut-exceptional-fingerprint}
若将上述“例外核 + 极值模补全”的解释视为统一链的结构约束，则至少应满足下列三条可审计指纹：
\begin{enumerate}
  \item \textbf{例外核扇区指纹：}在可复核的低分辨率 sweep 中（表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12}），存在扇区大小恰为 \(52\) 的退化谱分块；例如在 \(m=10\) 时 \(|\mathcal{S}_{10,8}|=52\)，并与 \(\dim\mathfrak{f}_4\) 对齐。
  \item \textbf{\texorpdfstring{$E_6$}{E6} 完备化指纹：}最大纤维极值模提供 \(D_{13}=26\)（表 \ref{tab:fold_max_fiber_achievers_hiddenbit_metrics}），使得 \(52+26\) 闭合到 \(\dim\mathfrak{e}_6=78\)；若在更改折叠口径/核族后该尺度不再出现，则“核+模”完备化机制在该版本下失效。
  \item \textbf{例外塔增量指纹：}维数增量需满足
  \(\dim\mathfrak{e}_7-\dim\mathfrak{e}_6=D_{16}\) 与 \(\dim\mathfrak{e}_8-\dim\mathfrak{e}_7=D_{18}+D_{13}\) 的数值对齐（结论 \ref{par:gut-maxfiber-exceptional-tower}），从而将补全从连续微调收束为由极值模离散化的补块规则。
\end{enumerate}

\paragraph{扩展辛相空间强制 \texorpdfstring{$\mathfrak{sp}(10)$}{sp(10)}：\texorpdfstring{$55=12\oplus 9\oplus 24\oplus 10$}{55=12+9+24+10} 的四块统一骨架}\label{par:gut-sp10-four-blocks}
把三通道势—响应辛相空间（注 \ref{rem:sp6-minimal-continuous-envelope}）与二维混合压力的 Legendre--Fenchel 对偶前沿（注 \ref{rem:pom-psi-2d-legendre-front}）并置，可得到一个由五对共轭坐标组织的扩展相空间：取
\[
x:=(\theta_{\varphi},\theta_{\pi},\theta_{e},\ q-1,\ \log u),\qquad
y:=(J_{\varphi},J_{\pi},J_{e},\ \gamma,\ \mathcal{E}),
\]
其中 \((q-1,\gamma)\) 与 \((\log u,\mathcal{E})\) 的共轭配对由二维凸对偶结构
\[
I(\gamma,\mathcal{E})
=\sup_{q\ge 2,\ u\in(0,1)}
\Bigl((q-1)\gamma+(\log u)\mathcal{E}-\Psi(q,u)\Bigr)
\]
给出（注 \ref{rem:pom-psi-2d-legendre-front}）。
其标准辛形式为
\[
\omega_{\mathrm{tot}}
:=\sum_{c\in\{\varphi,\pi,e\}}\dd\theta_{c}\wedge \dd J_{c}
+\dd(q-1)\wedge \dd\gamma
+\dd(\log u)\wedge \dd\mathcal{E}.
\]
在该口径下，保持 \(\omega_{\mathrm{tot}}\) 的最小连续线性包络为 \(\mathrm{Sp}(10,\RR)\)；
因此二进制统一维数锚点 \(D^{\mathrm{bin}}_{\mathrm{GUT}}=55\) 被提升为辛结构所强制的 \(C_5\) 分支：
\[
\boxed{\ \dim\mathfrak{sp}(10,\RR)=55\ }\qquad(\text{与 }\dim\mathfrak{so}(11)=55\text{ 的对照分支相区别}).
\]
\par
按“三通道 \(\oplus\) 两条标度轴”的标准直和分裂，\(\mathfrak{sp}(10,\RR)\) 出现规范的三块分解
\[
\mathfrak{sp}(10,\RR)\ \cong\ \mathfrak{sp}(6,\RR)\ \oplus\ \mathfrak{sp}(4,\RR)\ \oplus\ \mathcal{X},
\qquad
55=21+10+24,
\]
其中交叉块 \(\mathcal{X}\) 的维数严格为 \(24\)。
因此 \(\dim\mathfrak{su}(5)=24\) 在本框架内更自然的角色是“通道（\(\mathfrak{sp}(6)\)）—标度（\(\mathfrak{sp}(4)\)）接口层”的维数槽位，
而 \(\mathrm{Sp}(10)\) 则提供包络层的辛闭包语义。
\par
结合上文关于 \(\mathfrak{sp}(6,\RR)=\mathfrak{u}(3)\oplus\mathfrak{p}\) 的刚性分裂，以及上述 \(21+10+24\) 三块分解，二进制暗容量 \(H^{\mathrm{bin}}_6=43\) 获得一个与分块结构一致的精确归因：
\[
\boxed{\ 43\ =\ 9\ (\mathfrak{u}(3))\ +\ 24\ (\mathcal{X})\ +\ 10\ (\mathfrak{sp}(4,\RR))\ }.
\]
从而维数 \(55\) 的统一槽位在结构层面被锁定为
\[
\boxed{\ 55\ =\ 12\ \oplus\ 9\ \oplus\ 24\ \oplus\ 10\ },
\]
其中 \(12\) 对应于由低退化刚性读出的可见扇区规模，其余 \(43\) 为三块可归因的隐藏容量。
\par
\emph{可检验预测（统一点谱简并签名）}：
在自对偶统一点 \(u=1\)（注 \ref{rem:selfdual-u1-six-branch-lock}）处，
任何以线性化转移/响应算子读出统一槽位的实现（例如通道谱隙耦合族的分块组织，或统一提升算子在通道分解下的块谱组织）
都应呈现与 \(12\oplus 9\oplus 24\oplus 10\) 相容的四块谱结构；
其块多重度在可观测层面应稳定为 \(\{12,9,24,10\}\)，并对核/门的可审计微扰保持鲁棒。
\par
作为通道侧的具体闭合，这一结构把 \(G_{\mathrm{chan}}\) 的连通紧核指向 \(U(3)\)（行列式约束下可取 \(SU(3)\)），
并将“统一”从数值相交提升为结构判据：在统一点处，通道响应张量应恢复 \(U(3)\)-各向同性（与单位阵成比例），
其可审计入口由上文的各向同性点判据与相应扫描表提供。

\paragraph{NAP 选择器：在“非阿贝尔层保留”约束下 \texorpdfstring{$SO(10)$}{SO(10)} 被离散地唯一挑出}
把 \(\{6,8\}\) 视为“非阿贝尔共振层”（对应 \(3\) 与 \(8\) 的可见计数接口），并施加\textbf{非阿贝尔层保留}约束：
向上统一时允许替换/吸收阿贝尔层（\(m=4\)），但要求 \(\{6,8\}\) 作为\emph{独立层}保留在 \(\Sigma(\mathfrak g)\) 中。
在该约束下，对所有简单李代数扫描 \(\Sigma(\mathfrak g)\) 并最小化维数 \(D=\dim\mathfrak g\)，
得到最小解为 \(D_5=\mathfrak{so}(10)\)，且其签名恰为 \(\Sigma=\{6,8,11\}\)。
该选择器的扫描表与“单次离散替换 \(4\to 11\)”的计数等式证书由脚本生成如下：
\input{sections/generated/tab_nap_selector_so10}
\input{sections/generated/eq_nap_selector_so10}
\par
更强地，若要求 \(\{4,6,8\}\) 三层均保持独立（不允许被合并/吸收），则最小简单解跃迁到 \(D_{10}=\mathfrak{so}(20)\)（\(\dim=190\)，\(\Sigma=\{4,6,8,11,14\}\)；见表 \ref{tab:nap_selector_so10} 的 strict 部分），
从而把“阿贝尔层必须被统一重写”提升为一个可审计、可证伪的离散强迫结论。

\paragraph{信息代价选择器 \texorpdfstring{$\Omega$}{Omega}：\texorpdfstring{$SO(10)$}{SO(10)} 的最小维数/最小信息代价双极小}
除最小化维数 \(D=\dim\mathfrak g\) 外，可用分辨率折叠的平均多重度
\[
d_m:=\frac{2^m}{F_{m+2}}
\]
定义一个独立的“信息代价”函子
\[
\Omega(\mathfrak g):=\sum_{m\in\Sigma(\mathfrak g)}\log d_m.
\]
在 NAP 约束 \(\{6,8\}\subseteq\Sigma(\mathfrak g)\) 下，对简单李代数扫描并最小化 \(\Omega\)，最小解仍由 \(SO(10)\) 给出（\(\Omega(SO(10))\approx 4.826\)），且下一候选（例如 \(SO(12)\)）已显著更高（表 \ref{tab:gut_information_cost_selector}）。
同理，在 strict 约束 \(\{4,6,8\}\subseteq\Sigma(\mathfrak g)\) 下最小解跃迁到 \(SO(20)\)，并伴随 \(\Omega\) 的数量级上跳（\(\Omega(SO(20))\approx 8.328\)）。
\input{sections/generated/tab_gut_information_cost_selector}
因此，\(SO(10)\) 分支在本框架内同时是“最小维数”与“最小信息代价”的双重极小解，为其离散选择提供了与维数最小化并行、可审计的第二条独立理由。

\paragraph{\texorpdfstring{$m=11$}{m=11} 电荷层的规范分裂：\texorpdfstring{$34=1\oplus 33$}{34=1+33} 且 \texorpdfstring{$33=(16\ \text{对})+1$}{33=(16 pairs)+1}}
由 \(|X_{11}^{\mathrm{bdry}}|=F_9=34\) 与显式双射 \(X_{11}^{\mathrm{bdry}}\cong X_7\)（\(u=10\,v\,01\)），
可用 Zeckendorf 值域的规范编号 \(j\in\ZZ_{34}\) 为 \(m=11\) 的 \(34\) 个边界模态给出无歧义标签。
在该编号下，平移作用 \(T:e_j\mapsto e_{j+1}\) 的实正则表示强制给出一个规范的 \(1\oplus 33\) 模板：其中 \(Y_0\) 是唯一的 \(T\)-不变方向，其正交补空间维数自动为 \(33\)。
并在该 \(33\) 维正交补内进一步强制 \(33=1\oplus 16\times 2\) 的粗谱并群：\(Y_{17}\) 给出唯一自共轭实方向，其余为 \(16\) 个二维旋转平面：
\input{sections/generated/eq_m11_boundary_z34_decomposition}
该分解只依赖 \(\ZZ_{34}\) 的表示论与规范编号，不依赖任何外加“手工配对”；
因此它为后续把投影门/核谱算子化时的谱裂并群提供一个可审计靶标（特别地，\(34-1=33\) 的重方向计数与 NAP 选择器所确定的 \(m=11\) 电荷层定位在计数层面精确咬合）。
