Exact null enumeration over all $4$-codon subsets $\mathcal{K}\subset\Sigma^3$ ($\binom{64}{4}=635376$). For each $\mathcal{K}$ we maximize the boundary-hit objective $S_{\mathcal{K}}(\mu)=\sum_{c\in\mathcal{K}}\mathbf{1}\{w_\mu(c)\in X_6^{\mathrm{bdry}}\}$ over the $24$ encodings, and record $S_{\max}(\mathcal{K})$ and the argmax multiplicity $M(\mathcal{K})=\#\arg\max_{\mu} S_{\mathcal{K}}(\mu)$. For the biological control set $\{\mathrm{AUG,UAA,UAG,UGA}\}$, we have $S_{\max}=2$ with $M=1$ and the unique maximizer equals $\mu^\ast$.

Under the same uniform $4$-codon subset prior, $\mu^\ast$ is the \emph{unique} maximizer for 9764/635376 subsets (probability 0.015367).
Restricting to the maximal-score class $S_{\max}=2$, $\mu^\ast$ is the unique maximizer for 8637/635376 subsets (probability 0.013594).

Stop-fixed null (more structured): fixing the standard stop set $\{\mathrm{UAA,UAG,UGA}\}$ and varying a single additional codon over the $61$ non-stop choices, the argmax over encodings is unique for 19/61 choices. Among those, the unique argmax equals $\mu^\ast$ for 3/61 choices (namely $\mathrm{AUG}$, $\mathrm{CAC}$, $\mathrm{CAU}$).
