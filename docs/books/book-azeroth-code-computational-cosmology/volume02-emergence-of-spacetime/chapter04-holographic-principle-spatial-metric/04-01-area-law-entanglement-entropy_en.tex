\section{The Boundary Code (Area Law of Entanglement Entropy)}

\textit{(The Boundary Code - Area Law of Entanglement Entropy)}

\begin{quote}
\textbf{``Space is not an empty box containing things, but a web woven from entanglement relationships. The real `data' only exists on two-dimensional surfaces; the three-dimensional world you see is just a holographic projection generated by this data for your convenience.''}
\end{quote}

In Chapter 3, we discussed that light speed is bandwidth limitation. Now, we need to solve a more fundamental problem: How did \textbf{``space''} itself, this game scene, come about?

In old physics, space was a pre-built stage. But in \textbf{Code of Azeroth} theory, this wastes too many resources. If you've played game development, you know you don't need to model the entire Earth; you only need to render the \textbf{texture} layer that players can see.

This section will prove: Macroscopic three-dimensional space is essentially a \textbf{holographic projection} of the underlying quantum bit network. Through the seemingly counterintuitive law of \textbf{area law of entanglement entropy}, we will reveal the universe's greatest secret: \textbf{Volume is illusion; area is real.}

\subsection{Distance Originates from Entanglement: Who Are Friends?}

In geometry, if two points have close coordinates, we say they're close. But in the code world, \textbf{distance is derived}.

\textbf{Definition 4.1.1 (Information Distance)}

The ``distance'' between two objects is determined by their \textbf{mutual information}.

\begin{itemize}
\item If two quantum bits are in \textbf{maximally entangled} state, they are ``adjacent.'' Even if they look separated by a continent on screen, they're next to each other in memory addresses.
\item This view is called the \textbf{ER = EPR conjecture}: Quantum entanglement = wormhole connection.
\end{itemize}

Space is not an empty container, but a huge \textbf{social network graph}. Only after adding friends (entanglement) do you have a ``path'' between you.

\subsection{Area Law: Your Hard Drive Is Full of Textures}

To quantify this relationship, we need to examine the system's \textbf{entropy} (information content).

In thermodynamics, information content is usually thought to be proportional to \textbf{volume} ($S \propto V$). This is intuitive: a box full of books, more books (larger volume), more words.

However, at the bottom layer of the quantum world, we discovered an astonishing Bug---\textbf{Area Law}:

\textbf{Theorem 4.1.1 (Area Law of Entanglement Entropy)}

For a closed region, the maximum information it can contain does not depend on its volume, but on its \textbf{surface area}.

\[
S \propto \text{Area}(\partial A)
\]

\textbf{What does this mean?}

Imagine a huge solid iron ball.
\begin{itemize}
\item If it's a real solid object, describing the state of every atom inside should require huge data (volume law).
\item But physical laws tell us that as long as we describe all data on its \textbf{surface}, everything inside is determined (area law).
\end{itemize}

\textbf{Conclusion}: The interior of this iron ball is actually \textbf{empty}, or \textbf{redundant}. All truly effective data is pasted on the surface. Three-dimensional space is not solid; it's a \textbf{holographic projection}.

\subsection{Tensor Networks and Spatial Rendering}

In nature's code, it uses \textbf{Tensor Networks} to achieve this compression.

This is like \textbf{MERA networks}:
\begin{enumerate}
\item \textbf{Bottom-layer data}: Only exists on boundaries (one-dimensional chains or two-dimensional membranes).
\item \textbf{High-layer structure}: Through entanglement renormalization, extends inward, naturally constructing an additional dimension (spatial depth).
\end{enumerate}

\textbf{Computational corollary}:

The ``curved spacetime'' or ``gravitational field'' we feel is actually a manifestation of \textbf{data compression algorithms} in the bottom-layer code.
\begin{itemize}
\item Data near boundaries represents \textbf{high-resolution details}.
\item Data deep inside represents \textbf{low-resolution outlines}.
\end{itemize}

The ``depth'' of space is essentially the \textbf{logical depth} of computational processing.

\subsection{Ryu-Takayanagi Formula (RT Formula)}

In 2006, Ryu and Takayanagi proposed a formula, which is equivalent to finding the Titans' \textbf{rendering equation}.

\textbf{Formula 4.1.1 (RT Formula)}

\[
S_A = \frac{\text{Area}(\gamma_A)}{4G_N}
\]

This formula tells us:
\begin{enumerate}
\item \textbf{Geometry as entanglement}: Spatial area directly corresponds to the number of entangled bits. If entanglement breaks, space breaks (wormhole disconnects).
\item \textbf{True face of gravitational constant G}: $G$ is not some mysterious constant; it's the \textbf{bit-geometry conversion coefficient}. It defines how many bits of data can render 1 square meter of area.
\end{enumerate}

\subsection{Summary: From Bits to Geometry}

Based on the area law of entanglement entropy, we can draw the final conclusion of \textbf{Code of Azeroth} about space:

The universe is not a pre-existing three-dimensional box.
The universe is a \textbf{quantum bit ocean} defined on two-dimensional boundaries.

To help us players understand, the system uses holographic algorithms to \textbf{render} this two-dimensional data into a three-dimensional image with depth.

The grand Azeroth we inhabit is essentially a \textbf{low-loss compression format} of boundary data. And universal gravitation is the \textbf{geometric deformation} produced when this compression mechanism operates.
