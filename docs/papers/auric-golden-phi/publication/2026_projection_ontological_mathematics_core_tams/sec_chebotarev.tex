\section{Galois groups and secondary terms of collision recurrences}
\label{sec:chebotarev}

The matrix realizations of \Cref{sec:moment-kernel} turn collision counts into
linear recurrences.  For \(q=9,\dots,17\), the corresponding minimal
recurrence polynomials already display a rigid arithmetic and spectral
profile: integral Hankel relations are generated by the minimal polynomial,
irreducibility and full symmetric Galois groups admit explicit mod-\(p\)
certificates, the splitting fields for \(q=12,13,14,15\) are linearly
disjoint, and the second spectral term is controlled by a unique negative real
root.  The exact recurrences used to define the polynomials \(P_q\), together
with the provenance of the certificate tables, are recorded in
Appendix~\ref{app:certification}.

\subsection{Integral Hankel relations}

\begin{definition}[Resonance polynomials and null modes]
\label{def:resonance-polynomials}
For \(q\ge 2\), let \(P_q(\lambda)\in\ZZ[\lambda]\) denote the monic minimal
recurrence polynomial of the sequence \(m\mapsto S_q(m)\), and let
\(d_q:=\deg P_q\).  For \(q=9,\dots,17\), set
\[
  n_q:=2\left\lfloor\frac{q}{2}\right\rfloor+1,
  \qquad
  \Delta_q:=n_q-d_q.
\]
Define the square Hankel block
\[
  H_q:=\bigl[S_q(i+j+2)\bigr]_{0\le i,j\le n_q-1}
\]
and its integral null module
\[
  \mathrm{NullModes}(q):=\ker_{\ZZ}(H_q^{\mathsf T})\subset \ZZ^{n_q}.
\]
Finally, let
\[
  \mathcal M(P_q):=
  \bigl\{\operatorname{coeff}_{<n_q}(Q(\lambda)P_q(\lambda)):
  Q\in\ZZ[\lambda],\ \deg Q<\Delta_q\bigr\}\subset\ZZ^{n_q},
\]
where \(\operatorname{coeff}_{<n_q}\) truncates a polynomial to its first
\(n_q\) coefficients.
\end{definition}

\begin{theorem}[Hankel principalization]
\label{thm:principalization}
For every \(q\in\{9,\dots,17\}\),
\[
  \mathrm{NullModes}(q)=\mathcal M(P_q).
\]
In other words, every integral elimination relation visible in the chosen
Hankel window is a short multiple of the minimal recurrence polynomial.
\end{theorem}

\begin{proof}
Since \(P_q(E)S_q(\cdot+2)=0\), every short multiple \(Q(\lambda)P_q(\lambda)\)
gives an annihilating relation on the full sequence and hence a vector in
\(\mathrm{NullModes}(q)\).  Thus \(\mathcal M(P_q)\subseteq \mathrm{NullModes}(q)\).

By minimality of the recurrence, sufficiently large square Hankel blocks of the
sequence have rank \(d_q\); the chosen range is one in which this rank is
already attained.
Hence
\[
  \rk H_q=d_q,
  \qquad
  \rk_{\ZZ}\mathrm{NullModes}(q)=n_q-d_q=\Delta_q.
\]
On the other hand, \(\mathcal M(P_q)\) is a free \(\ZZ\)-module of rank
\(\Delta_q\), generated by the truncated coefficient vectors of
\(P_q,\lambda P_q,\dots,\lambda^{\Delta_q-1}P_q\).  Since
\(\mathcal M(P_q)\subseteq \mathrm{NullModes}(q)\) and both have the same rank,
they are equal.
\end{proof}

\begin{corollary}[No spurious eliminations]
\label{cor:no-spurious-elimination}
Let \(q\in\{9,\dots,17\}\), and let \(B(\lambda)\in\ZZ[\lambda]\) satisfy
\(\deg B<n_q\).  If its coefficient vector belongs to \(\mathrm{NullModes}(q)\),
then there exists a unique polynomial \(Q(\lambda)\in\ZZ[\lambda]\) with
\(\deg Q<\Delta_q\) such that
\[
  B(\lambda)=Q(\lambda)P_q(\lambda).
\]
In particular, every elimination relation detected in the chosen Hankel block
extends automatically to the full recurrence.
\end{corollary}

\begin{proof}
By \Cref{thm:principalization}, the coefficient vector of \(B\) is the
truncation of \(QP_q\) for some \(Q\) of degree \(<\Delta_q\).  Since
\(\deg(QP_q)<n_q\), truncation does nothing, and uniqueness follows from the
fact that \(P_q\) is monic.
\end{proof}

\begin{corollary}[GCD recovery]
\label{cor:gcd-recovers-minpoly}
Let \(q\in\{9,\dots,17\}\), and choose any \(\ZZ\)-basis
\(\alpha^{(1)},\dots,\alpha^{(\Delta_q)}\) of \(\mathrm{NullModes}(q)\).  Write
\[
  A^{(j)}(\lambda):=\sum_{k=0}^{n_q-1}\alpha^{(j)}_k\lambda^k.
\]
Then
\[
  \gcd\bigl(A^{(1)}(\lambda),\dots,A^{(\Delta_q)}(\lambda)\bigr)=\pm P_q(\lambda).
\]
Thus the minimal recurrence polynomial is uniquely recoverable from any integral
basis of finite Hankel certificates.
\end{corollary}

\begin{proof}
By \Cref{thm:principalization}, each \(A^{(j)}\) is of the form
\(Q^{(j)}P_q\) with \(\deg Q^{(j)}<\Delta_q\), so \(P_q\) is a common divisor.
Conversely, the module \(\mathcal M(P_q)\) contains \(P_q\) itself, because one
may take \(Q\equiv 1\).  Since the \(\alpha^{(j)}\) form a basis of
\(\mathcal M(P_q)\), there are integers \(c_j\) such that
\[
  P_q=\sum_{j=1}^{\Delta_q} c_j A^{(j)}
  =\left(\sum_{j=1}^{\Delta_q} c_jQ^{(j)}\right)P_q.
\]
As \(\ZZ[\lambda]\) is an integral domain, the polynomials \(Q^{(j)}\) generate
the unit ideal.  Hence their greatest common divisor is \(\pm1\), and the gcd of
the \(A^{(j)}\) is exactly \(\pm P_q\).
\end{proof}

\subsection{Irreducibility and full symmetric groups}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{5pt}
\caption{Unramified mod-\(p\) factorization certificates for the resonance
window \(q=9,\dots,17\).  The pattern \((d_q)\) certifies irreducibility and
transitivity, \((d_q-1,1)\) certifies an odd \((d_q-1)\)-cycle, and the final
column supplies a prime cycle of length \(3\), \(5\), or \(7\) for Jordan's
theorem.}
\label{tab:galois-certificates-q9-17}
\begin{tabular}{r r r l r l r l}
\toprule
\(q\) & \(d_q\) & \(p_{\mathrm{irr}}\) & degs & \(p_{(d-1,1)}\) & degs & \(p_{\mathrm{J}}\) & degs\\
\midrule
9  & 7  & 11  & (7)  & 17  & (6,1)   & 13 & (3,2,1,1) \\
10 & 9  & 17  & (9)  & 109 & (8,1)   & 101 & (5,3,1) \\
11 & 9  & 37  & (9)  & 17  & (8,1)   & 19 & (5,4) \\
12 & 13 & 29  & (13) & 17  & (12,1)  & 97 & (7,6) \\
13 & 11 & 29  & (11) & 61  & (10,1)  & 41 & (7,3,1) \\
14 & 13 & 37  & (13) & 47  & (12,1)  & 71 & (7,6) \\
15 & 11 & 17  & (11) & 127 & (10,1)  & 37 & (7,4) \\
16 & 13 & 239 & (13) & 127 & (12,1)  & 19 & (7,2,2,2) \\
17 & 13 & 31  & (13) & 59  & (12,1)  & 23 & (7,6) \\
\bottomrule
\end{tabular}
\end{table}

\begin{proposition}[Irreducibility in the resonance window]
\label{prop:irreducibility-window}
For \(q=9,\dots,17\), the minimal recurrence polynomial \(P_q\) is irreducible
over \(\QQ\).  Its degree sequence is
\[
  (d_9,d_{10},d_{11},d_{12},d_{13},d_{14},d_{15},d_{16},d_{17})
  =(7,9,9,13,11,13,11,13,13).
\]
\end{proposition}

\begin{proof}
The degree data are the orders of the exact recurrences listed in
Table~\ref{tab:appendix-recurrences-q9-17}.  By
\Cref{tab:galois-certificates-q9-17}, for each \(q\) there exists a prime
\(p_{\mathrm{irr}}\) such that \(P_q\bmod p_{\mathrm{irr}}\) is irreducible in
\(\FF_{p_{\mathrm{irr}}}[\lambda]\).  Since \(P_q\) is monic with integer
coefficients, Gauss's lemma implies that \(P_q\) is irreducible over \(\QQ\).
\end{proof}

\begin{lemma}[A transitive group with an \texorpdfstring{$(n-1)$}{(n-1)}-cycle is primitive]
\label{lem:nminus1-cycle-primitive}
Let \(G\le S_n\) be transitive with \(n\ge 3\).  If \(G\) contains an
\((n-1)\)-cycle, then \(G\) is primitive.
\end{lemma}

\begin{proof}
Let \(\tau\in G\) be an \((n-1)\)-cycle, and let \(\alpha\) be its unique fixed
point.  Consider a block \(B\subseteq\{1,\dots,n\}\) for the action of \(G\).

If \(\alpha\in B\), then \(B\setminus\{\alpha\}\) is \(\tau\)-invariant inside the
single \(\tau\)-orbit \(\{1,\dots,n\}\setminus\{\alpha\}\), so either
\(B=\{\alpha\}\) or \(B=\{1,\dots,n\}\).

If \(\alpha\notin B\), choose \(\beta\in B\) and \(g\in G\) with \(g(\beta)=\alpha\),
using transitivity.  Then \(g(B)\) is a block containing \(\alpha\), hence
trivial or all by the previous paragraph.  Therefore \(B\) itself is trivial or
all.
\end{proof}

\begin{theorem}[Full symmetric Galois groups in the resonance window]
\label{thm:galois-sd-window}
For every \(q=9,\dots,17\),
\[
  \Gal(P_q/\QQ)\cong S_{d_q}.
\]
\end{theorem}

\begin{proof}
Fix \(q\in\{9,\dots,17\}\), and let \(G_q:=\Gal(P_q/\QQ)\le S_{d_q}\) in its
natural action on the roots of \(P_q\).

By \Cref{prop:irreducibility-window}, the polynomial \(P_q\) is irreducible, so
\(G_q\) is transitive.  The factorization type \((d_q-1,1)\) from
\Cref{tab:galois-certificates-q9-17} exhibits an element of \(G_q\) that is an
\((d_q-1)\)-cycle, so \Cref{lem:nminus1-cycle-primitive} shows that \(G_q\) is
primitive.

The same table also provides an element of prime cycle length
\[
  \ell_q\in\{3,5,5,7,7,7,7,7,7\},
\]
and in each case \(\ell_q\le d_q-3\).  Jordan's theorem therefore implies that
\(A_{d_q}\subseteq G_q\); see \cite[Thm.~3.3A]{DixonMortimer1996}.

Finally, an \((d_q-1)\)-cycle is odd because \(d_q-1\) is even for all
\(d_q\in\{7,9,11,13\}\).  Hence \(G_q\) is not contained in \(A_{d_q}\).  Since
\(A_{d_q}\subseteq G_q\le S_{d_q}\), we conclude that \(G_q=S_{d_q}\).
\end{proof}

\begin{corollary}[Chebotarev densities]
\label{cor:individual-chebotarev}
For each \(q=9,\dots,17\), the set of unramified primes \(p\) for which
\(P_q\bmod p\) is irreducible in \(\FF_p[\lambda]\) has natural density
\(1/d_q\).
\end{corollary}

\begin{proof}
By \Cref{thm:galois-sd-window}, Frobenius classes are equidistributed in
\(S_{d_q}\).  Irreducibility modulo \(p\) is equivalent to Frobenius being a
\(d_q\)-cycle, and the proportion of \(d_q\)-cycles in \(S_{d_q}\) is
\((d_q-1)!/d_q!=1/d_q\).
\end{proof}

\begin{corollary}[Rational R\'enyi denominators are large]
\label{cor:renyi-rational-denominator}
For \(q\in\{9,\dots,17\}\), set
\[
  D_q:=\frac{q\log 2-\log r_q}{(q-1)\log\varphi}.
\]
If \(D_q=a/b\in\QQ\) in lowest terms, then
\[
  b\ge \left\lceil\frac{d_q}{2}\right\rceil.
\]
In particular, the range \(q=9,\dots,17\) yields the lower bounds
\[
  b\ge 4,5,5,7,6,7,6,7,7
  \qquad (q=9,\dots,17).
\]
\end{corollary}

\begin{proof}
From the definition of \(D_q\),
\[
  r_q = 2^q\varphi^{-(q-1)a/b}\in \QQ(\varphi^{1/b}).
\]
Since \(\QQ(\varphi)=\QQ(\sqrt5)\) is quadratic, the extension
\(\QQ(\varphi^{1/b})/\QQ\) has degree at most \(2b\).  By
\Cref{prop:irreducibility-window}, the algebraic degree of \(r_q\) is \(d_q\).
Therefore \(d_q\le 2b\), which is the claimed lower bound.
\end{proof}

\subsection{Linear disjointness and product densities}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\caption{Discriminant squareclass fingerprints for \(q\in\{12,13,14,15\}\)
using Legendre symbols at the primes \(31,37,43,61\).  After the encoding
\(-1\mapsto 1\), \(+1\mapsto 0\), the resulting \(4\times 4\) matrix has rank
\(4\).}
\label{tab:disc-squareclass-q12-15}
\begin{tabular}{r r r r r}
\toprule
\(q\) & \(\left(\frac{\Disc(P_q)}{31}\right)\) &
\(\left(\frac{\Disc(P_q)}{37}\right)\) &
\(\left(\frac{\Disc(P_q)}{43}\right)\) &
\(\left(\frac{\Disc(P_q)}{61}\right)\) \\
\midrule
12 & -1 & -1 & 1 & 1 \\
13 & 1 & -1 & 1 & -1 \\
14 & -1 & 1 & 1 & 1 \\
15 & -1 & -1 & -1 & 1 \\
\bottomrule
\end{tabular}
\end{table}

\begin{theorem}[Linear disjointness for \texorpdfstring{$q=12,13,14,15$}{q=12,13,14,15}]
\label{thm:linear-disjointness}
Let \(L_q\) be the splitting field of \(P_q\) for
\(q\in\{12,13,14,15\}\).  Then the fields \(L_{12},L_{13},L_{14},L_{15}\) are
linearly disjoint over \(\QQ\), and
\[
  \Gal(L_{12}L_{13}L_{14}L_{15}/\QQ)
  \cong
  S_{13}\times S_{11}\times S_{13}\times S_{11}.
\]
\end{theorem}

\begin{proof}
By \Cref{thm:galois-sd-window}, each \(L_q/\QQ\) has full symmetric Galois
group, so its only nontrivial proper normal subgroup is \(A_{d_q}\).  The
corresponding quadratic subfield is the discriminant field
\(\QQ(\sqrt{\Disc(P_q)})\).

By \Cref{tab:disc-squareclass-q12-15}, the four discriminant square classes are
linearly independent in \(\QQ^\times/(\QQ^\times)^2\).  Hence none of the four
quadratic discriminant fields lies in the compositum of the other three.

Fix \(q\in\{12,13,14,15\}\), and let \(M_q\) be the compositum of the three
remaining splitting fields.  Because both \(L_q/\QQ\) and \(M_q/\QQ\) are
Galois, the intersection \(L_q\cap M_q\) is a normal subextension of
\(L_q/\QQ\).  Therefore \(L_q\cap M_q\) is either \(\QQ\), the discriminant
quadratic field of \(L_q\), or all of \(L_q\).  The last two possibilities are
excluded by squareclass independence.  Thus \(L_q\cap M_q=\QQ\) for each \(q\),
which is exactly linear disjointness.

The direct-product formula for the Galois group follows from the compositum law
for finite Galois extensions with trivial intersections.
\end{proof}

\begin{corollary}[Product Chebotarev densities]
\label{cor:chebotarev-product}
Let \(C_q\subseteq S_{d_q}\) be a conjugacy class for each
\(q\in\{12,13,14,15\}\).  The set of unramified primes \(p\) for which the
Frobenius classes of \(P_{12},P_{13},P_{14},P_{15}\) lie respectively in
\(C_{12},C_{13},C_{14},C_{15}\) has natural density
\[
  \prod_{q\in\{12,13,14,15\}}\frac{\card{C_q}}{\card{S_{d_q}}}.
\]
In particular, the density of primes for which all four polynomials remain
irreducible is
\[
  \frac{1}{13}\cdot\frac{1}{11}\cdot\frac{1}{13}\cdot\frac{1}{11}
  =\frac{1}{20449}.
\]
\end{corollary}

\begin{proof}
Apply the Chebotarev density theorem to the compositum field of
\Cref{thm:linear-disjointness}.  The direct-product Galois group forces exact
independence of Frobenius classes across the four factors.
\end{proof}

\begin{corollary}[Additivity of the logarithmic density]
\label{cor:log-density-additivity}
For conjugacy classes \(C_q\subseteq S_{d_q}\) with \(q\in\{12,13,14,15\}\),
let \(\delta(C_{12},C_{13},C_{14},C_{15})\) denote the density of primes for
which the corresponding Frobenius classes lie in
\(C_{12},C_{13},C_{14},C_{15}\).  Then
\[
  -\log \delta(C_{12},C_{13},C_{14},C_{15})
  =
  \sum_{q\in\{12,13,14,15\}}
  \Bigl(-\log \frac{\card{C_q}}{\card{S_{d_q}}}\Bigr).
\]
In particular, if each \(C_q\) is the class of \(d_q\)-cycles, then
\[
  -\log \delta = \log 20449.
\]
\end{corollary}

\begin{proof}
This is the logarithmic form of \Cref{cor:chebotarev-product}.
\end{proof}

\subsection{A secondary spectral term}

The arithmetic conclusions above can be combined with the root fingerprints of
the exact recurrence polynomials.  The next table records the dominant root,
the outer negative real root, and the largest remaining modulus for the whole
resonance window; Appendix~\ref{app:certification} explains the provenance of
these data.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{5pt}
\caption{Secondary spectral fingerprints for the resonance polynomials
\(P_q\), \(q=9,\dots,17\).  Here \(r_q\) is the Perron root, \(\lambda_q^-<0\)
is the unique root of second-largest modulus, and \(\Theta_q\) is the maximum
modulus among the remaining roots.}
\label{tab:secondary-spectral-q9-17}
\begin{tabular}{r r r r r}
\toprule
\(q\) & \(d_q\) & \(r_q\) & \(\lambda_q^-\) & \(\Theta_q\) \\
\midrule
9  & 7  & 11.778422 & -7.065331  & 5.807034 \\
10 & 9  & 14.771098 & -9.100142  & 7.686459 \\
11 & 9  & 18.535847 & -11.712939 & 10.074255 \\
12 & 13 & 23.273460 & -15.066709 & 13.109656 \\
13 & 11 & 29.237339 & -19.369971 & 16.968007 \\
14 & 13 & 36.747376 & -24.889362 & 21.870525 \\
15 & 11 & 46.207510 & -31.965663 & 28.096490 \\
16 & 13 & 58.127952 & -41.034237 & 35.998554 \\
17 & 13 & 73.153329 & -52.651060 & 46.021996 \\
\bottomrule
\end{tabular}
\end{table}

\begin{theorem}[Alternating second-order law in the resonance window]
\label{thm:alternating-second-order}
For each \(q=9,\dots,17\), let \(\lambda_q^-<0\) and \(\Theta_q\) be as in
\Cref{tab:secondary-spectral-q9-17}.  Then
\[
  \Theta_q<|\lambda_q^-|<r_q.
\]
Moreover there exist real constants \(c_q^+>0\) and \(c_q^-\neq 0\) such that
\[
  S_q(m)=c_q^+\,r_q^m+c_q^-\,(\lambda_q^-)^m+O(\Theta_q^m)
  \qquad (m\to\infty).
\]
Consequently the renormalized error
\[
  E_q(m):=S_q(m)-c_q^+\,r_q^m
\]
is eventually nonzero and has a strict alternating sign law:
\[
  \operatorname{sgn} E_q(m)=(-1)^m\operatorname{sgn}(c_q^-)
  \qquad (m\gg 1).
\]
\end{theorem}

\begin{proof}
Fix \(q\in\{9,\dots,17\}\).  By \Cref{prop:irreducibility-window}, the
polynomial \(P_q\) is irreducible over \(\QQ\).  Since \(\operatorname{char}\QQ=0\),
all roots of \(P_q\) are simple.  Classical linear-recurrence theory therefore
gives an expansion
\[
  S_q(m)=\sum_{j=1}^{d_q} c_{q,j}\lambda_{q,j}^m
\]
for all sufficiently large \(m\), where \(\{\lambda_{q,j}\}\) are the roots of
\(P_q\).

Because \(S_q(m)\in\QQ\) for every \(m\), applying any
\(\sigma\in\Gal(P_q/\QQ)\) to this expansion and using the linear independence of
the sequences \(\lambda_{q,j}^m\) shows that the coefficients are permuted by
Galois:
\[
  \sigma(c_{q,j})=c_{q,\sigma(j)}.
\]
Since \(\Gal(P_q/\QQ)\cong S_{d_q}\) is transitive by
\Cref{thm:galois-sd-window}, either all coefficients \(c_{q,j}\) vanish or none
do.  The sequence \(S_q(m)\) is not identically zero, so every \(c_{q,j}\) is
nonzero.

Now \Cref{tab:secondary-spectral-q9-17} supplies a unique real negative root
\(\lambda_q^-\) of second-largest modulus together with a strict gap
\(\Theta_q<|\lambda_q^-|\).  Grouping all remaining roots into the error term
gives
\[
  S_q(m)=c_q^+\,r_q^m+c_q^-\,(\lambda_q^-)^m+O(\Theta_q^m)
\]
with \(c_q^- \neq 0\).  Since \(S_q(m)>0\) and \(r_q\) strictly dominates the
other moduli, one has \(c_q^+>0\).  The sign law follows because the second term
eventually dominates the \(O(\Theta_q^m)\) remainder.
\end{proof}

\begin{theorem}[Strong multiplicative independence of the Perron roots]
\label{thm:perron-multiplicative-independence}
If integers \(a_{12},a_{13},a_{14},a_{15}\) satisfy
\[
  r_{12}^{a_{12}}r_{13}^{a_{13}}r_{14}^{a_{14}}r_{15}^{a_{15}}\in\QQ^\times,
\]
then
\[
  a_{12}=a_{13}=a_{14}=a_{15}=0.
\]
\end{theorem}

\begin{proof}
Assume that a nontrivial relation exists.  Choose
\(q_0\in\{12,13,14,15\}\) with \(a_{q_0}\neq 0\).  By
\Cref{thm:linear-disjointness},
\[
  \Gal(L_{12}L_{13}L_{14}L_{15}/\QQ)
  \cong
  S_{13}\times S_{11}\times S_{13}\times S_{11}.
\]
Hence one may choose an automorphism \(\sigma\) that acts trivially on the
three factors \(L_q\) with \(q\neq q_0\) and nontrivially on \(L_{q_0}\).
Because \(\Gal(P_{q_0}/\QQ)\cong S_{d_{q_0}}\) acts transitively on the roots of
\(P_{q_0}\), we may arrange that \(\sigma(r_{q_0})\neq r_{q_0}\).

By \Cref{tab:secondary-spectral-q9-17}, every conjugate of \(r_{q_0}\) other
than \(r_{q_0}\) itself has modulus at most \(|\lambda_{q_0}^-|<r_{q_0}\).
Apply \(\sigma\) to the assumed multiplicative relation and cancel the three
fixed factors.  One obtains
\[
  \sigma(r_{q_0})^{a_{q_0}}=r_{q_0}^{a_{q_0}}.
\]
Taking absolute values gives
\[
  |\sigma(r_{q_0})|^{|a_{q_0}|}=r_{q_0}^{|a_{q_0}|},
\]
contradicting \(|\sigma(r_{q_0})|<r_{q_0}\).  Therefore no nontrivial relation
exists.
\end{proof}

\begin{remark}[What remains open]
\label{rem:q16q17}
The full symmetric-group statement already closes for \(q=16\) and \(q=17\).
The next arithmetic target is to extend squareclass independence, linear
disjointness, and the multiplicative-independence law beyond the block
\(q=12,13,14,15\).  On the spectral side, the table above strongly suggests a
persistent negative second root throughout the resonance window, but an
entirely symbolic certification of that pattern would be preferable in a final
version.
\end{remark}
