
\section{Hecke dynamics: the prime skeleton of the discrete layer}
\label{sec:hecke_dynamics_prime}

\subsection{Hecke operators and their algebraic closure}
\label{sec:hecke_operators}

On spaces of modular forms of level~1 and weight~$k$, Hecke operators $T_n$ form a commutative algebra encoding arithmetic symmetry; standard references include \cite{Apostol1990,DiamondShurman2005}. The defining structure is that Hecke operators are indexed by all $n\ge 1$, but the algebra is generated by the operators at primes.

The fundamental multiplication law is
\begin{equation}
\label{eq:hecke_multiplication_law}
T_m T_n = \sum_{d\mid (m,n)} d^{k-1}\,T_{mn/d^2}.
\end{equation}
In particular, if $(m,n)=1$ then $T_{mn}=T_mT_n$, and for prime powers one obtains the recursion
\begin{equation}
\label{eq:hecke_prime_power}
T_{p^{r+1}} = T_p T_{p^r} - p^{k-1} T_{p^{r-1}},\qquad r\ge 1.
\end{equation}

\subsection{Geometric meaning: Hecke correspondences rescale height}
\label{sec:hecke_geometric_scaling}

Beyond the abstract algebra, Hecke operators have a concrete geometric action on functions on $\HH$ via finite sums of explicit fractional-linear maps (double-coset correspondences). For level~$1$ holomorphic modular forms of weight $k$, a standard explicit formula is (see, e.g., \cite{Apostol1990,DiamondShurman2005})
\begin{equation}
\label{eq:hecke_explicit_formula}
(T_n f)(\tau)=n^{k-1}\sum_{\substack{ad=n\\0\le b<d}} d^{-k}\, f\!\left(\frac{a\tau+b}{d}\right)
=\sum_{\substack{ad=n\\0\le b<d}} \frac{a^{k-1}}{d}\, f\!\left(\frac{a\tau+b}{d}\right),
\end{equation}
In particular, for a prime $p$,
\begin{equation}
\label{eq:hecke_prime_formula}
(T_p f)(\tau)=p^{k-1}f(p\tau)+\frac{1}{p}\sum_{b=0}^{p-1} f\!\left(\frac{\tau+b}{p}\right).
\end{equation}
These maps rescale the slice height $y=\Im\tau$ by rational factors:
\begin{equation}
\Im(p\tau)=p\,y,\qquad \Im\!\left(\frac{\tau+b}{p}\right)=\frac{y}{p}.
\end{equation}
Thus primes act as a discrete cross-scale skeleton: one term pushes deeper into the cusp ($y\mapsto p y$) while the other pulls back toward the boundary ($y\mapsto y/p$).

\begin{figure}[H]
\centering
\includegraphics[width=0.98\linewidth]{fig07_hecke_correspondence_rescale_height}
\caption{Hecke correspondence for a prime $p$ (schematic): the explicit formula is a finite sum over fractional-linear maps, with branches that rescale the slice height by $y\mapsto py$ and $y\mapsto y/p$. In the full sum, the action closes on integer-indexed $q$-expansion coefficients.}
\label{fig:hecke_rescale_height}
\end{figure}

\begin{remark}[action on the cusp coordinate]
Under $\tau\mapsto p\tau$ one has $q\mapsto q^p$. Under $\tau\mapsto (\tau+b)/p$, one has $q\mapsto \e^{2\pi \iu b/p}\,q^{1/p}$. In the full Hecke sum~\eqref{eq:hecke_prime_formula} the fractional powers cancel, and $T_p$ preserves the usual $q$-expansion space, which is another way to see why the prime skeleton closes on integer-indexed coefficients.
\end{remark}

\subsection{From double cosets to \texorpdfstring{$q$}{q}-coefficients (explicit closure)}
\label{sec:hecke_on_qexp}

The statement that ``fractional powers cancel'' can be made completely explicit at the coefficient level.

\begin{proposition}[Hecke action on Fourier coefficients]
\label{prop:hecke_on_qexp}
Let $f$ be a holomorphic modular form of weight $k$ with $q$-expansion
\begin{equation}
f(\tau)=\sum_{m\ge 0} a_m q^m,\qquad q=\e^{2\pi\iu\tau}.
\end{equation}
Then $T_n f$ has the $q$-expansion
\begin{equation}
(T_n f)(\tau)=\sum_{\ell\ge 0}\left(\sum_{d\mid(n,\ell)} d^{k-1}\,a_{n\ell/d^2}\right) q^\ell.
\end{equation}
In particular, for a prime $p$,
\begin{equation}
(T_p f)(\tau)=\sum_{\ell\ge 0}\left(a_{p\ell}+p^{k-1}a_{\ell/p}\right)q^\ell,
\end{equation}
where $a_{\ell/p}=0$ if $p\nmid \ell$.
\end{proposition}
\begin{proof}
Insert the $q$-expansion of $f$ into the explicit formula~\eqref{eq:hecke_explicit_formula} and use the root-of-unity sum
\begin{equation}
\sum_{b=0}^{d-1} \e^{2\pi\iu m b/d}
=
\begin{cases}
d, & d\mid m,\\
0, & d\nmid m.
\end{cases}
\end{equation}
Writing $m=d m'$ and collecting coefficients of $q^\ell$ yields the stated divisor sum; see \cite{Apostol1990,DiamondShurman2005} for standard derivations.
\end{proof}

\subsection{Protocol-level meaning of \texorpdfstring{$T_p$}{Tp}: a testable prime-skeleton check}
\label{sec:hecke_protocol_level}

The formal action of $T_p$ on modular observables is completely explicit (Equation~\eqref{eq:hecke_prime_formula}) and induces a closed coefficient-level map (Proposition~\ref{prop:hecke_on_qexp}).
For the present paper's protocol chain, it is useful to separate two notions of ``Hecke acting on the protocol''.
\par
\paragraph{(i) Coefficient-layer closure (used here).}
Once a coefficient sequence $(a_n)$ is recovered (with a certified error budget) from slice sampling, the prime skeleton supplies a \emph{locally checkable} constraint family.
For a prime $p$, define the induced operator on coefficient sequences by
\begin{equation}
\label{eq:hecke_on_coeff_sequence}
\big(\mathcal{T}_p a\big)_\ell := a_{p\ell}+p^{k-1}a_{\ell/p},
\qquad a_{\ell/p}:=0\ \text{if }p\nmid \ell.
\end{equation}
Then $T_p f$ has coefficients $\mathcal{T}_p a$.
If $f$ is a normalized Hecke eigenform, the eigen-relation $T_p f=a_p f$ becomes the coefficient identity
\begin{equation}
\label{eq:hecke_eigen_coeff_identity}
\mathcal{T}_p a = a_p\,a,
\end{equation}
which is an explicit prime-indexed consistency test on the recovered discrete spectrum.
\begin{remark}[normalization convention]
Throughout, ``normalized Hecke eigenform'' means the standard normalization $a_1=1$ in the $q$-expansion. For an eigenform scaled by a nonzero constant, one can always renormalize by dividing the coefficient sequence by $a_1$ before applying the prime-skeleton identities.
\end{remark}
\par
\paragraph{(ii) Implementable channel on instruments (additional structure).}
Interpreting $T_p$ as a physical symmetry acting within a fixed effective observer sector $\hilbert_{\mathrm{eff}}$ requires an explicit interface that maps a modular observable $f$ to an operator-valued readout observable and that can realize the branch mixing $\tau\mapsto p\tau$ and $\tau\mapsto(\tau+b)/p$ operationally.
This is naturally cross-scale (it mixes heights $y\mapsto py$ and $y\mapsto y/p$), and therefore goes beyond the coefficient-level closure used in this paper.
A fully worked finite-resolution model and a discussion of this distinction are recorded in Appendix~\ref{app:hecke_protocol_level}.

\subsection{Quantitative Hecke consistency under certified coefficient errors}
\label{sec:hecke_consistency_quantitative}
The prime-skeleton constraints are most useful when they can be evaluated with a certified tolerance implied by the coefficient-recovery error budgets.
We record a simple deterministic residual bound showing how coefficient-level uncertainty propagates into Hecke-eigen consistency checks.

\begin{definition}[Hecke residual at a prime]
\label{def:hecke_residual}
Fix a weight $k$ and a prime $p$. For a candidate coefficient sequence $b=(b_n)_{n\ge 1}$, define the residual
\begin{equation}
\label{eq:hecke_residual}
R_{p,\ell}(b):=\big(b_{p\ell}+p^{k-1}b_{\ell/p}\big)-b_p b_\ell,
\qquad b_{\ell/p}:=0\ \text{if }p\nmid \ell.
\end{equation}
\end{definition}
\begin{remark}
The residual~\eqref{eq:hecke_residual} tests the normalized Hecke-eigen identity. If one starts from an unnormalized coefficient sequence $a$ with $a_1\neq 0$, apply it to $b_n:=a_n/a_1$.
\end{remark}

\begin{proposition}[residual bound from coefficient error bars]
\label{prop:hecke_residual_bound}
Assume $f$ is a normalized Hecke eigenform so that its true coefficients satisfy
\[
a_{p\ell}+p^{k-1}a_{\ell/p}=a_p a_\ell
\qquad(\ell\ge 1),
\]
where $a_{\ell/p}:=0$ if $p\nmid \ell$,
and let $\widehat a$ be an approximate coefficient sequence. Suppose for a given pair $(p,\ell)$ we have certified bounds
\[
|\widehat a_m-a_m|\le \delta_m
\qquad\text{for }m\in\{p\ell,\ p,\ \ell\}\ \text{and also for }m=\ell/p\ \text{when }p\mid\ell,
\]
with the convention $\delta_{\ell/p}:=0$ if $p\nmid \ell$.
Then the Hecke residual satisfies
\begin{equation}
\label{eq:hecke_residual_bound}
|R_{p,\ell}(\widehat a)|
\le \delta_{p\ell}+p^{k-1}\delta_{\ell/p}+|a_p|\delta_\ell+|a_\ell|\delta_p+\delta_p\delta_\ell.
\end{equation}
\end{proposition}
\begin{proof}
Insert the true identity $a_{p\ell}+p^{k-1}a_{\ell/p}=a_p a_\ell$ into Definition~\ref{def:hecke_residual}:
\[
R_{p,\ell}(\widehat a)
=(\widehat a_{p\ell}-a_{p\ell})+p^{k-1}(\widehat a_{\ell/p}-a_{\ell/p})-(\widehat a_p\widehat a_\ell-a_p a_\ell).
\]
For the product term,
\[
\widehat a_p\widehat a_\ell-a_p a_\ell
=(\widehat a_p-a_p)\widehat a_\ell+a_p(\widehat a_\ell-a_\ell),
\]
so $|\widehat a_p\widehat a_\ell-a_p a_\ell|
\le \delta_p(|a_\ell|+\delta_\ell)+|a_p|\delta_\ell$.
Combining these inequalities yields~\eqref{eq:hecke_residual_bound}.
\end{proof}

\begin{remark}[turning residual bounds into falsifiable fits]
In arithmetic settings, $|a_p|$ admits explicit majorants (e.g.\ Deligne/Ramanujan--Petersson for cuspidal eigenforms; see, e.g., \cite{Deligne1974,Serre1973,DiamondShurman2005}), so~\eqref{eq:hecke_residual_bound} becomes a fully explicit, deterministic tolerance for a Hecke-eigen ``fit'' test.
In the special case where $a_n\in\ZZ$ and coefficients are recovered exactly by rounding (Corollary~\ref{cor:exact_integer_recovery_rounding}), the residual vanishes identically: $R_{p,\ell}(\widehat a)=0$ for all tested primes and indices.
\end{remark}

\subsection{Eigenforms: stable directions and coefficient rigidity}
\label{sec:eigenforms_coeff_rigidity}

If $f$ is a normalized Hecke eigenform with $q$-expansion
\begin{equation}
f(\tau)=\sum_{n\ge 1} a_n q^n,
\end{equation}
then
\begin{equation}
T_n f = a_n f,
\end{equation}
so the $q$-coefficients coincide with Hecke eigenvalues (up to normalization conventions). The Hecke relations immediately imply two rigid constraints on the coefficient spectrum:
\begin{enumerate}
\item \textbf{Coprime multiplicativity.} If $(m,n)=1$, then
\begin{equation}
a_{mn}=a_m a_n.
\end{equation}
\item \textbf{Prime-power recursion.} For any prime $p$ and $r\ge 1$,
\begin{equation}
a_{p^{r+1}} = a_p a_{p^r} - p^{k-1} a_{p^{r-1}}.
\end{equation}
\end{enumerate}
Thus the discrete spectrum is not an arbitrary integer sequence: it is generated and constrained by the prime skeleton.

\subsection{Example: Ramanujan's discriminant and \texorpdfstring{$\tau(n)$}{tau(n)}}
\label{sec:ramanujan_tau_example}

The classical example is the weight-$12$ cusp form
\begin{equation}
\Delta(\tau)=q\prod_{n\ge 1}(1-q^n)^{24}=\sum_{n\ge 1}\tau(n)q^n,
\end{equation}
whose coefficients $\tau(n)$ satisfy the Hecke constraints above (with $k=12$). These relations provide a canonical instance of the prime-generated skeleton at the coefficient level.

\subsection{Euler products and local-to-global closure}
\label{sec:euler_product}

For normalized eigenforms, the Dirichlet series
\begin{equation}
L(s,f)=\sum_{n\ge 1}\frac{a_n}{n^s}
\end{equation}
admits an Euler product factorization
\begin{equation}
L(s,f)=\prod_{p}\left(1-a_p p^{-s}+p^{k-1}p^{-2s}\right)^{-1}.
\end{equation}
This expresses local-to-global closure explicitly: prime-indexed data determine the full spectrum.


