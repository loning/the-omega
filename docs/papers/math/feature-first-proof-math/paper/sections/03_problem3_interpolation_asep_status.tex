\section{Problem 3: Interpolation ASEP/Macdonald Stationary Law}

\textbf{Latest Reviewed Version:} Q3-R7 (FAIL).

\textbf{Current Status.}
The current draft gives a correct conditional construction, but the original unconditional starred statement is still not closed.

\textbf{Submission Record.}

\textbf{Q3-V7 (user submission).}
This version presents a full Metropolis--Hastings construction on $S_n(\lambda)$ with
\[
w(\mu)=F^*_\mu(x;1,t),\quad Z=P^*_\lambda(x;1,t),\quad \pi(\mu)=w(\mu)/Z,
\]
under explicit assumptions
\[
Z>0,\qquad w(\mu)\ge 0\ \forall\mu.
\]
It proves normalization, defines a symmetric proposal, gives case-by-case detailed balance (including zero-weight states), and concludes stationarity.

\textbf{Review Outcome (Q3-R7).}
Still \textbf{FAIL} for original Q3: this is a conditional theorem, not an unconditional closure of the starred problem.

\textbf{Tracking Rule.}
Each newly submitted user version is first appended in this section, then reviewed in `reviews/problem3_review.md`.
