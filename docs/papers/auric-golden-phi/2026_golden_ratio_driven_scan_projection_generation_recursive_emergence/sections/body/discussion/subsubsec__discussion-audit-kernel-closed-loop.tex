% Auto-split to keep each TeX source < 800 lines.

\subsubsection{Closed chain of unified audit protocol: resolution reflection—curvature—fiber spectrum—Hankel normal form—arithmetic rigidity}\label{subsubsec:discussion-audit-kernel-closed-loop}
Based on the "certificate package" perspective in the previous section, this article also includes a set of structural nodes that are more operational-level and can be directly converted into a unified audit protocol.
Together they form a closed chain from canonical slices to unforgeable integer fingerprints:
Resolution reflection (normalized) \(\to\) gauge curvature (no exchange barriers) \(\to\) Fiber spectrum and alias portal (visible phase) \(\to\) Hankel normal form (minimum realization) \(\to\) Arithmetic stiffness and resonance (integer coefficient certificate).
This paragraph only consolidates these nodes into fielded certificates based on the "input-output-verification" caliber, and does not introduce new proof premises.

\paragraph{Resolution reflecton: making canonical selection an idempotent projection}
After fixing the top-level resolution \(M\), the reflecton with resolution \(m\) consists of push forward \(P_{M\to m}\) and fiber uniform lifting \(Q_{M\to m}\), and is defined
\[
\mu^{(m)}:=Q_{M\to m}P_{M\to m}\mu\in\Delta(\Omega_M)
\qquad\text{(Definition \ref{def:pom-resolution-reflector})}.
\]
Idempotence is established by \(P_{M\to m}\circ Q_{M\to m}=\mathrm{id}_{\Delta(X_m)}\) (same definition)
\[
(\mu^{(m)})^{(m)}=\mu^{(m)},
\qquad
P_{M\to m}\mu^{(m)}=P_{M\to m}\mu,
\]
Therefore, "gauge fixing" is not a narrative convention in the scope of this article, but a reviewable operator \(Q_{M\to m}P_{M\to m}\).
In cross-resolution comparisons, reflecting the data to the same gauge slice before talking about invariants avoids spurious regularities induced by coordinate inconsistencies (note \ref{rem:pom-gauge-criterion}; see also Residual additive identities on KL-towers: Theorem \ref{thm:pom-kl-pythagoras-tower}).

\paragraph{Curvature and local non-commutability: auditable signs of constant-level defects}
Non-commutativity between "truncate then fold" and "fold then limit" across resolutions is determined by gauge curvature
\(\mathcal{K}_{m+1\to m}\) (definition \ref{def:pom-gauge-curvature}) is given;
For any bounded observation \(f:X_m\to\RR\), the curvature probability provides strict error bars (Proposition \ref{prop:pom-gauge-curvature-shadow-bound}).
Under the uniform microstate baseline, the exact enumeration of \(m\le 20\) (note \ref{rem:pom-gauge-curvature-audit}; table \ref{tab:fold_truncation_curvature_stats}) presents two types of signs directly related to the audit protocol:
\begin{itemize}
\item \textbf{Constant signs of curvature probability:} \(\mathbb{P}[\mathcal{K}_{m+1\to m}=1]\) approaches \(1/3\) within the enumeration range, suggesting that the obstacle is closer to a constant-level local defect rather than spreading as a macroscopic deviation with the scale;
\item \textbf{Constant signs of Hamming mismatch:} Table \ref{tab:fold_truncation_curvature_stats} reports simultaneously the Hamming distance \(H_m\) output by the two paths, with the mean approaching \(1/2\) within the enumeration range, while \(\mathbb{E}[H_m]/m\) clearly approaches zero.
\end{itemize}
Since \(\mathcal{K}_{m+1\to m}=\mathbf 1[H_m>0]\), we can put
\[
\mathbb{E}[H_m\mid \mathcal{K}_{m+1\to m}=1]
=\frac{\mathbb{E}[H_m]}{\mathbb{P}[\mathcal{K}_{m+1\to m}=1]}
\]
As a purely numerical, falsifiable "conditional mismatch number" audit constant.
Within the \(m\le 20\) enumeration range of table \ref{tab:fold_truncation_curvature_stats}, this ratio is steadily close to \(3/2\), giving a local defect conjecture that can be further verified: the number of typical mismatches when curvature occurs may converge to a constant (rather than growing with \(m\)).

\paragraph{Discrete phase compression of fiber spectra: two-point limit law and alias portal}
For bin-fold, the fiber scale degenerates to a constant factor of the last two states in the exponential main order (theorem \ref{thm:fold-bin-two-state-asymptotic}), and further gives a two-point limit law under uniform dyadic input:
Let \(U_m\sim\mathrm{Unif}\{0,\dots,2^m-1\}\), \(W_m=\mathrm{Fold}^{\mathrm{bin}}_m(U_m)\), then the scaled variable
\(
Z_m=(\varphi/2)^m d^{\mathrm{bin}}_m(W_m)
\)
Distributively converges to a two-point distribution with values ​​\(\{1,\varphi^{-1}\}\) (corollary \ref{cor:fold-bin-two-point-limit-law}).
This conclusion is equivalent to: under a uniform baseline, the "typical fiber scale" is almost only a binary phase certificate, and the error is negligible on the exponential scale.
\par
At the minimum anchor point window-\(6\), the alias structure can be further compressed into a portal-like discrete offset spectrum:
For the boundary sector \(X_6^{\mathrm{bdry}}\), dyadic uplift presents a standard two-layer coverage, with exactly two preimages for each boundary word, and the difference is strictly \(34=F_9\) (table \ref{tab:fold6_boundary_sheet_pairs}; and see the corresponding description in \S\ref{subsubsec:window6-terminal-propositions}).
This type of "fixed-offset two-layer alias portal" compresses fiber spectral nonuniformity from continuous amplitude information into verifiable discrete sheet parity certificates.
\par
The same mechanism also gives a deterministic recalibration between the dyadic cutoff depth and the Fibonacci weight depth: from the definition of \(K(m)\) (definition \ref{def:K-of-m}) and its asymptotic estimate (e.g. \(K(m)=m\log_\varphi 2+O(1)\) given in the proof of theorem \ref{thm:fold-bin-max-fiber-exponent}), it can be seen that
There is an ineliminable linear scale mismatch between the binary cutoff and the Zeckendorf weight depth; this mismatch itself can be viewed as an independent resolution certificate coordinate, used to align the "bit length budget" and "\(\varphi\)-resolution budget" translations.

\paragraph{Hankel normal form:\texorpdfstring{$2d$}{2d} point-locked minimum realization equality protocol for the whole sequence}
For fixed \(q\ge 2\), the Hankel rank \(d\) of the collision moment sequence \(a_n:=S_q(n+2)\) locks the minimum realization dimension, and constructs the Hankel-normal form from the invertible Hankel block with the minimum offset \(\ell_q\)
\(\mathrm{HANKELNF}_q\) (definition \ref{def:pom-hankel-normal-form}).
More strongly, if two implementation paths produce \(S_q,S'_q\) with the same Hankel rank \(d\), then as long as they are equal over a continuous window of length \(2d\), they share the same \(\mathrm{HANKELNF}_q\), thus being completely consistent for all \(m\ge 2\) (Proposition \ref{prop:pom-moment-finite-sampling-decidable}; see also the protocolized formulation of theorem \ref{thm:dephys-hankel-finite-audit}).
Therefore the computable criterion of "mechanism identity" can be reduced to a hard interface with finite samples: once \(d\) and \(\ell_q\) are output as certificates, the verification only needs to check \(2d\) integer samples.

\paragraph{Arithmetic Stiffness and Resonance:\texorpdfstring{$p$}{p}-Progressive Transition, NullMode and Divisor Factorization Certificate}
The Hankel witness block determinant not only provides a rank lower bound (theorem \ref{thm:pom-moment-hankel}), but also carries an unforgeable arithmetic rigid fingerprint.
For example, the stiffness index of \(p=5\) has its first high assignment jump at \(k=5\) (note \ref{rem:pom-hankel-stiffness-p5}), giving a fully internally reviewable arithmetic threshold.
\par
In the resonance window \(q=9,\dots,17\), Hankel rank reduction triggers explicit modal constraints (theorem \ref{thm:pom-moment-resonance}) and can be output offline as an integer NullMode Certificate: For the corresponding Hankel block, the integer basis vectors of \(\ker(H^{\mathsf T})\) are shown in the table \ref{tab:fold_collision_resonance_nullmodes_q9_17}.
Even stronger, these NullMode-induced annihilation polynomials \(A^{(j)}(\lambda)\) and the minimum recursive characteristic polynomial \(P_q(\lambda)\) satisfy the exact integer factorization
\(
A^{(j)}(\lambda)=Q^{(j)}(\lambda)P_q(\lambda)
\)
and the remainder is zero; the corresponding quotient polynomial \(Q^{(j)}\) is itemized and verified as an additional certificate field (table \ref{tab:fold_collision_resonance_nullmodes_primitive_quotient_q9_17}).
This type of "resonance-algebraic quotient" certificate simultaneously develops structural dimensionality reduction and arithmetic divisibility rigidity, forming the most difficult type of integer coefficient fingerprint to be forged in the audit protocol.

\paragraph{Low-frequency compressible structure quantity: harmonic energy proportion as near-coboundary fingerprint}
In the three-dimensional Dirichlet--Mertens constant tensor \(C^{(3,3,5)}\) of the real input \(40\) state kernel (the third axis takes the collision count \(N_2\bmod 5\)),
The drift on the \(\ZZ/5\) axis resolves exactly into the "mean \(+\) two harmonic complex matrices" \(A^{(1)},A^{(2)}\) for each \((a,b)\) (see the output of the generated snippet \ref{tab:real_input_40_arity_dirichlet_mertens_335_N2_summary}).
From this, the proportion of harmonic energy can be defined
\[
\mathcal{E}_1:=\frac{\|A^{(1)}\|_F^2}{\|A^{(1)}\|_F^2+\|A^{(2)}\|_F^2},
\]
And regard \(\mathcal{E}_1\) as the dimensionless fingerprint field of "low-frequency compressible" and near-coboundary structures (the same generated fragment also outputs numerical certificates).
This field, together with the quadratic law audit of small-angle distortion (the consistency of \(\rho/\lambda\) at finite corner points), constitutes a reviewable "low-frequency structure-diffusion slow mode" splicing diagnostic interface.

\paragraph{Audit core interface: unified output from limited samples to fielded certificates}
By organizing the above nodes according to unified interfaces, a reproducible audit kernel protocol template can be obtained:
The input is a limited sample produced by any implementation path (distribution forward, finite segment moment, or finite window enumeration statistics), and the output is a set of fielded certificate objects; any field stable mismatch can be used as a falsification interface for mechanism equivalence.
In the object and script system established in this article, the output can at least include:
\begin{itemize}
\item Resolution reflection slice \(\mu^{(m)}\) and its KL-tower residual ledger (definition \ref{def:pom-resolution-reflector}; theorem \ref{thm:pom-kl-pythagoras-tower});
\item Curvature probability and error bar (definition \ref{def:pom-gauge-curvature}; proposition \ref{prop:pom-gauge-curvature-shadow-bound}; table \ref{tab:fold_truncation_curvature_stats});
\item bin-fold two-point phase and window-\(6\) portal alias offset spectrum (corollary \ref{cor:fold-bin-two-point-limit-law}; table \ref{tab:fold6_boundary_sheet_pairs});
\item Hankel-normal form \(\mathrm{HANKELNF}_q\) and \(2d\) point finite decidable windows (definition \ref{def:pom-hankel-normal-form}; proposition \ref{prop:pom-moment-finite-sampling-decidable});
\item \(p\)-Advanced stiffness index and resonance NullMode/divisor quotient decomposition (note \ref{rem:pom-hankel-stiffness-p5}; table \ref{tab:fold_collision_resonance_nullmodes_q9_17}--\ref{tab:fold_collision_resonance_nullmodes_primitive_quotient_q9_17});
\item Low-frequency structure quantity \(\mathcal{E}_1\) and small-angle quadratic law diagnostic field (fragment \ref{tab:real_input_40_arity_dirichlet_mertens_335_N2_summary}).
\end{itemize}
Therefore, the "audit first, explain later" approach of this article can be implemented as a completely implementation-neutral verification protocol: under fixed readout and baseline standards, any alternative implementation that claims to have the same mechanism must reproduce the same set of certificate fields; otherwise, it can be rejected offline and machine-checkably without entering the details of the internal mechanism.

