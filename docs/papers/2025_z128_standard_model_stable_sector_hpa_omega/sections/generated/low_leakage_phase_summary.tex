\paragraph{Audit summary (low-leakage phase signatures).} \AuditTag Rows are a deterministic illustration in a finite comparison family. The reference threshold is set to $\Gamma_{\mathrm{ref}}=0.001000$ (proxy units). A row is tagged \texttt{low-leakage} iff $\Gamma<\Gamma_{\mathrm{ref}}$.