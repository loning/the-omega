% (User input window)
% 时间：2026-02-16 01:12:09（Asia/Singapore）
% 时间：2026-02-16 01:37:50（Asia/Singapore）
% 时间：2026-02-16 02:05:41（Asia/Singapore）
% 时间：2026-02-16 02:18:37（Asia/Singapore）
%
% In this section, the Hellinger two-body core \(A(\Delta)\) is promoted from closed-form affinity to a geometric object determined by a complete spectrum:
% Its Fourier density is \(\cosh^{-2}(\pi\xi)\), and induces an explicit RKHS with a determinant volume potential;
% Further Toeplitzization under the isometric spectrum gives Szeg\H{o} Limits and CUE representation.

\begin{proposition}[Fourier completely closed form of Hellinger translation kernel:\texorpdfstring{$\cosh^{-2}$}{cosh^{-2}} is the spectral density of deep two-body geometry]\label{prop:xi-hellinger-kernel-fourier-sech2}
Let the scale unimodal kernel
\[
\phi(s)=\frac{1}{4\cosh^{2}(s/2)}\qquad(s\in\RR),
\]
and define the Hellinger affinity core
\[
A(\Delta):=\int_{\RR}\sqrt{\phi(s)\phi(s-\Delta)}\,\dd s
=\frac{|\Delta|}{2\sinh(|\Delta|/2)}\qquad(\Delta\in\RR).
\]
but \(A\in L^{1}(\RR)\cap C^\infty(\RR)\) is an even function.
The Fourier transform convention
\(
\widehat f(\xi):=\int_{\RR}e^{-\mathrm{i}\xi x}f(x)\,\dd x
\)
Below, there is a strict identity
\[
\boxed{\;
\widehat A(\xi)=\int_{\RR}A(\Delta)e^{-\mathrm{i}\xi\Delta}\,\dd\Delta
=\frac{\pi^{2}}{\cosh^{2}(\pi\xi)}\;}
\qquad(\xi\in\RR).
\]
specially
\(
\int_{\RR}A(\Delta)\,\dd\Delta=\widehat A(0)=\pi^{2}
\)。
\end{proposition}
\begin{proof}
make
\(
f(s):=\sqrt{\phi(s)}=\frac{1}{2\cosh(s/2)}
\)。
but
\(
A(\Delta)=\int_{\RR}f(s)f(s-\Delta)\,\dd s
\)
for \(f\), so \(\widehat A(\xi)=|\widehat f(\xi)|^{2}\)。
substitute \(s=2u\) have to
\[
\widehat f(\xi)
=\int_{\RR}\frac{1}{2\cosh(s/2)}e^{-\mathrm{i}\xi s}\,\dd s
=\int_{\RR}\frac{1}{\cosh u}\,e^{-2\mathrm{i}\xi u}\,\dd u
=\frac{\pi}{\cosh(\pi\xi)},
\]
The last step is \(\cosh^{-1}\) of the Fourier closed form.
Therefore \(\widehat A(\xi)=\pi^{2}/\cosh^{2}(\pi\xi)\). Certification completed.
\end{proof}

\input{para__xi-hellinger-green-operator-energy-duality}

\begin{proposition}[RKHSization of depth spectrum:\texorpdfstring{$A$}{A} For strict regeneration and \texorpdfstring{$\det G$}{det G} to evaluate the functor volume]\label{prop:xi-hellinger-kernel-rkhs-volume}
inheritance proposition \ref{prop:xi-hellinger-kernel-fourier-sech2} notation, and notation density
\[
w(\xi):=\widehat A(\xi)=\frac{\pi^2}{\cosh^2(\pi\xi)}.
\]
Define Hilbert space
\[
\mathcal H_{A}
:=\Bigl\{f\in\mathcal S'(\RR):\ \|f\|_{\mathcal H_A}^{2}
:=\frac{1}{2\pi}\int_{\RR}\frac{|\widehat f(\xi)|^{2}}{w(\xi)}\,\dd\xi
=\frac{1}{2\pi}\int_{\RR}|\widehat f(\xi)|^{2}\frac{\cosh^{2}(\pi\xi)}{\pi^{2}}\,\dd\xi<\infty\Bigr\}.
\]
Then for any \(x\in\RR\), evaluation functional \(f\mapsto f(x)\) exist \(\mathcal H_A\) continuously.
make \(k_x(\cdot):=A(\cdot-x)\),but \(k_x\in\mathcal H_A\) and for all \(f\in\mathcal H_A\) has regenerative properties
\[
\boxed{\;f(x)=\langle f,k_x\rangle_{\mathcal H_A}\;},
\qquad
\langle k_x,k_y\rangle_{\mathcal H_A}=A(x-y).
\]
Given depth logarithmic spectrum \(\boldsymbol{\sigma}=(\sigma_1,\dots,\sigma_\kappa)\), define the Gram matrix
\[
G(\boldsymbol{\sigma})_{jk}:=A(\sigma_j-\sigma_k)\qquad(1\le j,k\le\kappa).
\]
but
\[
\boxed{\;
\det G(\boldsymbol{\sigma})
=\mathrm{Vol}^{2}\bigl(\mathrm{span}\{k_{\sigma_1},\dots,k_{\sigma_\kappa}\}\bigr),\qquad
S_{1/2}(\boldsymbol{\sigma}):=-\log\det G(\boldsymbol{\sigma}).\;}
\]
\end{proposition}
\begin{proof}
exist \(\mathcal H_A\) defines the inner product on
\(
\langle f,g\rangle_{\mathcal H_A}:=\frac{1}{2\pi}\int \widehat f(\xi)\overline{\widehat g(\xi)}\,w(\xi)^{-1}\dd\xi
\)。
right \(k_x(\cdot)=A(\cdot-x)\), obtained by Fourier translation \(\widehat{k_x}(\xi)=w(\xi)e^{-\mathrm{i}\xi x}\),thereby \(k_x\in\mathcal H_A\) and
\[
\langle f,k_x\rangle_{\mathcal H_A}
=\frac{1}{2\pi}\int_{\RR}\widehat f(\xi)\,e^{\mathrm{i}\xi x}\,\dd\xi
=f(x),
\]
The last step is the Fourier inversion formula of the Schwartz distribution.
Pick \(f=k_x\) have to \(\langle k_x,k_y\rangle_{\mathcal H_A}=A(x-y)\)。
The last expression is the geometric interpretation of the Gram determinant in Hilbert space:\(\det(\langle v_j,v_k\rangle)=\mathrm{Vol}^{2}(\mathrm{span}\{v_j\})\). Certification completed.
\end{proof}

\input{para__xi-gram-volume-monotonicity-coherence-nearestneighbor}

\begin{proposition}[Exact first-order variation and equilibrium equations of depth volume entropy potential]\label{prop:xi-hellinger-gram-first-variation-equilibrium}
Let the depth logarithmic spectrum \(\mathbf s=(s_1,\dots,s_\kappa)\) are pairwise different, and let the Hellinger--Gram matrix
\[
G(\mathbf s)_{jk}=A(s_j-s_k)\qquad(1\le j,k\le \kappa),
\]
of which the core \(A\) see proposition \ref{prop:xi-hellinger-kernel-fourier-sech2}。
Define depth volume entropy potential
\[
S_{1/2}(\mathbf s):=-\log\det G(\mathbf s).
\]
then for each \(1\le j\le \kappa\) has a strict gradient formula
\[
\boxed{\;
\partial_{s_j}S_{1/2}(\mathbf s)
=-2\sum_{k=1}^{\kappa}\bigl(G(\mathbf s)^{-1}\bigr)_{jk}\,A'(s_j-s_k)\;}
\]
in \(A'(0):=0\), and for \(\Delta\neq 0\),
\[
\boxed{\;
A'(\Delta)
=A(\Delta)\Bigl(\frac{1}{\Delta}-\frac12\coth(\Delta/2)\Bigr)\; }.
\]
because \(S_{1/2}\) translates the whole \(\mathbf s\mapsto \mathbf s+c\mathbf 1\) remains unchanged, any stationary point after the translational degrees of freedom are fixed must satisfy the equilibrium equation
\[
\sum_{k=1}^{\kappa}\bigl(G(\mathbf s)^{-1}\bigr)_{jk}\,A'(s_j-s_k)=0,\qquad j=1,\dots,\kappa.
\]
And force core \(\frac{1}{\Delta}-\frac12\coth(\Delta/2)\) exist \(|\Delta|\to\infty\) and \(\Delta\to 0\) satisfies both ends of
\[
\frac{1}{\Delta}-\frac12\coth(\Delta/2)= -\frac12\,\mathrm{sgn}(\Delta)+O(|\Delta|^{-1}),
\qquad
\frac{1}{\Delta}-\frac12\coth(\Delta/2)=-\frac{\Delta}{12}+O(\Delta^3).
\]
\end{proposition}
\begin{proof}
From the matrix differential identity \(\partial\log\det G=\mathrm{tr}(G^{-1}\partial G)\),have to
\[
\partial_{s_j}\log\det G(\mathbf s)=\mathrm{tr}\bigl(G(\mathbf s)^{-1}\,\partial_{s_j}G(\mathbf s)\bigr).
\]
Notice \(\partial_{s_j}G_{ab}=\delta_{aj}A'(s_j-s_b)-\delta_{bj}A'(s_a-s_j)\),and \(A'\) is an odd function.
then
\[
\partial_{s_j}\log\det G(\mathbf s)
=\sum_{k=1}^{\kappa}\bigl(G^{-1}\bigr)_{jk}A'(s_j-s_k)
+\sum_{k=1}^{\kappa}\bigl(G^{-1}\bigr)_{kj}A'(s_j-s_k)
=2\sum_{k=1}^{\kappa}\bigl(G^{-1}\bigr)_{jk}A'(s_j-s_k),
\]
thus getting shown \(\partial_{s_j}S_{1/2}=-\partial_{s_j}\log\det G\) formula.
right \(\Delta\neq 0\), directly to \(A(\Delta)=\Delta/(2\sinh(\Delta/2))\) Derivative and sorting out
\(
A'(\Delta)=A(\Delta)\bigl(\Delta^{-1}-\tfrac12\coth(\Delta/2)\bigr)
\); then from the odd symmetry order \(A'(0)=0\)。
\par
The equilibrium equations arise from the stationary point condition and translation invariance (equivalent to the constraint \(\sum_j s_j=0\) or take the stationary point with any base point fixed).
Both ends are expanded by \(\coth x=1+O(e^{-2x})\)（\(x\to\infty\))and
\(\coth x=x^{-1}+x/3+O(x^3)\)（\(x\to 0\)) and you get it. Certification completed.
\end{proof}

\begin{remark}[Area law and logarithmic correction of two-body potential:\texorpdfstring{$1/4$}{1/4} long-range reproduction]\label{rem:xi-hellinger-two-body-potential-area-log}
Make the two bodies move \(V(\Delta):=-\log A(\Delta)\)（\(\Delta\ge 0\)）。
then yes \(\Delta>0\) has a strict identity
\[
\boxed{\;
V(\Delta)=\frac{\Delta}{2}-\log\Delta+\log\bigl(1-e^{-\Delta}\bigr)\;}
\]
and therefore
\[
V(\Delta)=\frac{\Delta}{2}-\log\Delta+O(e^{-\Delta})\qquad(\Delta\to\infty).
\]
If you use the physical distance variable instead \(d:=2\Delta\),but
\[
V(d/2)=\frac{d}{4}-\log d+\log 2+O(e^{-d/2})\qquad(d\to\infty),
\]
Among them, the linear term coefficient \(1/4\) appears in strict form in the long-range potential of this core, while the logarithmic term gives an ineliminable secondary correction.
\end{remark}

\begin{proposition}[Strict convexity of deep energy functionals:\texorpdfstring{$-\log A$}{-log A} conditional negative definiteness]\label{prop:xi-hellinger-energy-functional-strict-convexity}
Define a continuous energy functional
\[
\mathcal E(\nu):=\frac12\iint_{\RR\times\RR} -\log A(x-y)\,\dd\nu(x)\dd\nu(y),
\]
in \(\nu\) is a probability measure with finite energy.
Then the core \(\psi(\Delta):=-\log A(\Delta)\) is a (strictly) conditionally negative definite function: for any real coefficient \(c_1,\dots,c_m\) satisfy \(\sum_i c_i=0\) and any point \(x_1,\dots,x_m\in\RR\) have
\[
\sum_{i,j=1}^{m}c_ic_j\,\psi(x_i-x_j)\le 0,
\]
and therefore \(\mathcal E\) is a (strictly) convex functional on a convex set of probability measures.
Under any affine constraints that eliminate the overall translational degrees of freedom (such as a fixed mean),\(\mathcal E\) is unique in its minimal measure.
\end{proposition}
\begin{proof}[Points of proof]
by the Weierstrass product
\(
2\sinh(\Delta/2)=\Delta\prod_{n\ge 1}\bigl(1+\Delta^2/(4\pi^2n^2)\bigr)
\)
have to
\[
A(\Delta)=\prod_{n\ge 1}\Bigl(1+\frac{\Delta^2}{4\pi^2n^2}\Bigr)^{-1}.
\]
each factor \((1+b^2\Delta^2)^{-1}\) is the characteristic function of the central Laplace distribution, so \(A\) is the characteristic function of the sum of a family of independent Laplace variables, and is thus infinitely separable; given by L\'evy--Khintchine theory,\(\psi=-\log A\) is a continuous negative definite function (equivalent to the conditional negative definite inequality described above).
\par
right \(\nu_t=(1-t)\nu_0+t\nu_1\) expand \(\mathcal E(\nu_t)\) available
\(
\mathcal E(\nu_t)=(1-t)\mathcal E(\nu_0)+t\mathcal E(\nu_1)-\frac{t(1-t)}{2}\iint \psi\,\dd(\nu_1-\nu_0)\dd(\nu_1-\nu_0)
\)；
Conditional negative definiteness makes the last term non-negative, thus obtaining convexity. Strictness and uniqueness under constraints are enforced by the equal sign case \(\nu_0=\nu_1\) (or just the overall translation) is launched. Certification completed.
\end{proof}

\begin{theorem}[Deep Hellinger--Gram's strict total positivity and spectral oscillation law]\label{thm:xi-hellinger-gram-strict-total-positivity}
Strictly increasing depth \(s_1<\cdots<s_\kappa\),matrix \(G(\mathbf s)=(A(s_j-s_k))_{1\le j,k\le\kappa}\) is a strictly positive matrix.
therefore:
\begin{itemize}
\All eigenvalues ​​of item are single roots;
\item No. \(r\) The eigenvectors corresponding to large eigenvalues ​​have exactly \(r-1\) times strict sign change;
\item any non-zero vector \(G(\mathbf s)\) The number of sign changes after the action strictly does not increase (the change decreases).
\end{itemize}
\end{theorem}
\begin{proof}[Points of proof]
by proposition \ref{prop:xi-hellinger-kernel-fourier-sech2}, the normalized characteristic function satisfies
\[
\frac{\widehat A(\xi)}{\widehat A(0)}=\mathrm{sech}^{2}(\pi\xi)
=\prod_{n\ge 1}\Bigl(1+\frac{\xi^{2}}{(n-\tfrac12)^{2}}\Bigr)^{-2},
\]
which uses \(\cosh(\pi z)=\prod_{n\ge 1}(1+z^{2}/(n-\tfrac12)^{2})\) of the Weierstrass product.
therefore \(A/\widehat A(0)\) is PF\(_\infty\)（P\'olya frequency) density; by Schoenberg's PF\(_\infty\)\(\Rightarrow\), it can be seen from the strictly positive translation kernel theorem that the kernel \((x,y)\mapsto A(x-y)\) is strictly correct.
make \((x_i)=(y_i)=(s_i)\) to get \(G(\mathbf s)\) is strictly positive.
The fundamental theorem of spectral simplicity, eigenvector oscillation law and decreasing variation property for all positive matrices (Gantmacher--Kre\u{\i}n direct corollary of the decreasing variation/oscillation theorem). Certification completed.
\end{proof}

\begin{corollary}[Chessboard symbolic law of inverse matrices: algebraic certificate of alternating couplings]\label{cor:xi-hellinger-gram-inverse-checkerboard}
In the theorem \ref{thm:xi-hellinger-gram-strict-total-positivity} Under the conditions,\(G(\mathbf s)^{-1}\) has strict checkerboard notation:
\[
\boxed{\;
(-1)^{i+j}\bigl(G(\mathbf s)^{-1}\bigr)_{ij}>0\qquad(1\le i,j\le \kappa).\;}
\]
\end{corollary}
\begin{proof}
This is the sign-regular conclusion for the inverse of a fully positive matrix: the inverse of a non-singular fully positive matrix is ​​a strictly chessboard signed matrix. Certification completed.
\end{proof}

\begin{proposition}[Andreief represents the logarithmic gas with frequency domain:\texorpdfstring{$S_{1/2}$}{S_{1/2}} is the external potential free energy]\label{prop:xi-hellinger-kernel-andreief-loggas}
in proposition \ref{prop:xi-hellinger-kernel-rkhs-volume} Under the setting of , there is a strict integral representation
\[
\boxed{\;
\det G(\boldsymbol{\sigma})
=\frac{1}{\kappa!(2\pi)^{\kappa}}
\int_{\RR^\kappa}
\Bigl(\prod_{r=1}^{\kappa} w(\xi_r)\Bigr)\,
\bigl|\det(e^{-\mathrm{i}\xi_r\sigma_j})_{1\le r,j\le\kappa}\bigr|^{2}
\ \dd\xi_1\cdots \dd\xi_\kappa.\;}
\]
therefore \(S_{1/2}(\boldsymbol{\sigma})\) is equivalent to one-dimensional \(\beta=2\) External potential of the logarithmically interacting system
\(
V(\xi):=-\log w(\xi)=2\log\cosh(\pi\xi)-\log\pi^{2}
\)
Partition function free energy under; depth spectrum \(\boldsymbol{\sigma}\) into the Vandermonde-type volume factor with external phase interpolation.
\end{proposition}
\begin{proof}
by proposition \ref{prop:xi-hellinger-kernel-fourier-sech2} Fourier said,
\[
A(x-y)=\frac{1}{2\pi}\int_{\RR}w(\xi)\,e^{\mathrm{i}\xi(x-y)}\,\dd\xi.
\]
Let the measure \(\dd\mu(\xi):=w(\xi)\dd\xi/(2\pi)\), juxtaposition
\(
F_j(\xi):=e^{-\mathrm{i}\xi\sigma_j}
\)。
but \(G_{jk}=\int F_j(\xi)\overline{F_k(\xi)}\,\dd\mu(\xi)\)。
From the Andreief identity (that is, the integration of the Gram determinant) we get
\[
\det(G_{jk})
=\frac{1}{\kappa!}\int_{\RR^\kappa}\bigl|\det(F_j(\xi_r))_{1\le r,j\le\kappa}\bigr|^{2}\,\dd\mu(\xi_1)\cdots\dd\mu(\xi_\kappa),
\]
into the form shown. The free energy expression is obtained by taking the negative logarithm. Certification completed.
\end{proof}

\begin{theorem}[Hankel sealing of collision constants: under clustering scaling \texorpdfstring{$\det G$}{det G} of confluent Vandermonde factorization]\label{thm:xi-hellinger-gram-collision-hankel-constant}
inheritance proposition \ref{prop:xi-hellinger-kernel-andreief-loggas} , and take any \(\kappa\ge 1\)。
Given two different \(t_1,\dots,t_\kappa\in\RR\) and any \(s_0\in\RR\), consider clustering scaling
\[
\sigma_j=s_0+\varepsilon t_j\qquad(1\le j\le \kappa),\qquad \varepsilon\downarrow 0,
\]
And order \(G(\boldsymbol{\sigma})=(A(\sigma_j-\sigma_k))_{1\le j,k\le\kappa}\)。
then there is a strict asymptotic
\[
\boxed{\;
\det G(\boldsymbol{\sigma})
=\varepsilon^{\kappa(\kappa-1)}
\Bigl(\prod_{j=0}^{\kappa-1}\frac{1}{(j!)^{2}}\Bigr)\,
\mathfrak H_\kappa\,
\prod_{1\le j<k\le\kappa}(t_k-t_j)^{2}\,\bigl(1+o(1)\bigr)\;}
\]
where the universal constant \(\mathfrak H_\kappa\) is the weight function \(w(\xi)=\pi^{2}\mathrm{sech}^{2}(\pi\xi)\) of the normalized Hankel determinant:
\[
\mathfrak H_\kappa:=\det(\mu_{p+q})_{p,q=0}^{\kappa-1},\qquad
\mu_n:=\frac{1}{2\pi}\int_{\RR}\xi^{n}w(\xi)\,\dd\xi.
\]
Therefore, outside of the Vandermonde power law of collision divergence, there is still a dependence only on \(\kappa\) the irreducible volume constant of \(\mathfrak H_\kappa\)。
\end{theorem}
\begin{proof}
by proposition \ref{prop:xi-hellinger-kernel-andreief-loggas} The Andreief representation is with translation invariance,\(\det G(\boldsymbol{\sigma})\) relies only on differences \(\sigma_j-\sigma_k=\varepsilon(t_j-t_k)\),and
\[
\det G(\boldsymbol{\sigma})
=\frac{1}{\kappa!(2\pi)^{\kappa}}
\int_{\RR^\kappa}\Bigl(\prod_{r=1}^{\kappa}w(\xi_r)\Bigr)\,
\bigl|\det(e^{-\mathrm{i}\varepsilon\xi_r t_j})_{1\le r,j\le\kappa}\bigr|^{2}\,\dd\xi_1\cdots\dd\xi_\kappa.
\]
when \(\varepsilon\downarrow 0\), the classic confluent Vandermonde asymptotically gives
\[
\det(e^{-\mathrm{i}\varepsilon\xi_r t_j})_{r,j}
=\varepsilon^{\kappa(\kappa-1)/2}\Bigl(\prod_{j=0}^{\kappa-1}\frac{1}{j!}\Bigr)
\Delta(\boldsymbol{\xi})\,\Delta(\mathbf t)\,\bigl(1+o(1)\bigr),
\]
in \(\Delta(\boldsymbol{\xi})=\prod_{1\le r<\ell\le\kappa}(\xi_\ell-\xi_r)\)、\(\Delta(\mathbf t)=\prod_{1\le j<k\le\kappa}(t_k-t_j)\)。
Taking modulo the square and substituting back, we get
\[
\det G(\boldsymbol{\sigma})
=\varepsilon^{\kappa(\kappa-1)}\Bigl(\prod_{j=0}^{\kappa-1}\frac{1}{(j!)^{2}}\Bigr)\,
\Delta(\mathbf t)^{2}\cdot
\frac{1}{\kappa!}\int_{\RR^\kappa}\Delta(\boldsymbol{\xi})^{2}\,\dd\mu(\xi_1)\cdots\dd\mu(\xi_\kappa)\,\bigl(1+o(1)\bigr),
\]
in \(\dd\mu(\xi)=w(\xi)\dd\xi/(2\pi)\)。
Finally, from the Andreief identity (the matrixed integral of the Vandermonde square) we have
\[
\frac{1}{\kappa!}\int_{\RR^\kappa}\Delta(\boldsymbol{\xi})^{2}\,\dd\mu(\xi_1)\cdots\dd\mu(\xi_\kappa)
=\det(\mu_{p+q})_{p,q=0}^{\kappa-1}=\mathfrak H_\kappa,
\]
This results in the asymptotic shown. Certification completed.
\end{proof}

\begin{corollary}[Minimum singularity exponent of clustered phases: institutional power explosion of condition numbers]\label{cor:xi-hellinger-gram-cluster-conditioning-exponent}
In the theorem \ref{thm:xi-hellinger-gram-collision-hankel-constant} Under the clustering scaling of ,
Existence constant \(c(\mathbf t)>0\) make
\[
\det G(\boldsymbol{\sigma})=c(\mathbf t)\,\varepsilon^{\kappa(\kappa-1)}\bigl(1+o(1)\bigr).
\]
and due to \(\mathrm{tr}(G)=\kappa\), the maximum eigenvalue satisfies \(\lambda_{\max}(G)\le \kappa\)。
So the minimum eigenvalue has a hard upper bound
\[
\lambda_{\min}(G)\le \frac{\det G}{\lambda_{\max}(G)^{\kappa-1}}
\le \kappa^{1-\kappa}\,c(\mathbf t)\,\varepsilon^{\kappa(\kappa-1)}\bigl(1+o(1)\bigr),
\]
Therefore, the spectral condition number satisfies
\[
\boxed{\;
\mathrm{cond}_2(G)=\frac{\lambda_{\max}(G)}{\lambda_{\min}(G)}
\ \ge\ C(\mathbf t)\,\varepsilon^{-\kappa(\kappa-1)}\bigl(1+o(1)\bigr)\;}
\qquad(\varepsilon\downarrow 0),
\]
in \(C(\mathbf t)>0\) is a constant.
Therefore, the numerical ill-posedness of the clustered phase is given by the exponential \(\kappa(\kappa-1)\) strictly locked.
\end{corollary}
\begin{proof}
The product of the determinant and the spectrum gives
\(
\det G=\lambda_{\min}\prod_{r\neq\min}\lambda_r\ge \lambda_{\min}\lambda_{\max}^{\kappa-1}
\)，
Therefore \(\lambda_{\min}\le \det G/\lambda_{\max}^{\kappa-1}\)。
again \(\lambda_{\max}\le \mathrm{tr}(G)=\kappa\), and substitute the theorem \ref{thm:xi-hellinger-gram-collision-hankel-constant} asymptotically, the lower bound of the condition number shown is obtained. Certification completed.
\end{proof}

\begin{proposition}[Arithmeticity of universal Hankel constants:\texorpdfstring{$\mathfrak H_\kappa\in\QQ$}{H_k in Q} And by even \texorpdfstring{$\zeta$}{zeta} value explicit sealing]\label{prop:xi-hellinger-hankel-constant-rationality}
Follow the theorem \ref{thm:xi-hellinger-gram-collision-hankel-constant} mark.
then moment \(\mu_n\) satisfy \(\mu_{2n+1}=0\), and for any \(n\ge 0\) has a strictly closed form
\[
\boxed{\;
\mu_{2n}
=2^{1-2n}\pi^{-2n}(2n)!\,\bigl(1-2^{1-2n}\bigr)\zeta(2n)\in\QQ\; }.
\]
thus for any \(\kappa\ge 1\)，\(\mathfrak H_\kappa=\det(\mu_{p+q})_{p,q=0}^{\kappa-1}\in\QQ\)。
For example
\[
\boxed{\;
\mathfrak H_1=1,\quad
\mathfrak H_2=\frac{1}{12},\quad
\mathfrak H_3=\frac{1}{540},\quad
\mathfrak H_4=\frac{1}{42000},\quad
\mathfrak H_5=\frac{1}{3215625}.\;}
\]
\end{proposition}
\begin{proof}[Points of proof]
The odd-order moments are zero from \(w(\xi)\) of even symmetry.
The dual moment can be given by \(\mathrm{sech}^2\) partial fraction/residue expansion (equivalently, by \(\tanh\) of the lattice pole structure) will
\(\int_{\RR}\xi^{2n}\mathrm{sech}^{2}(\pi\xi)\dd\xi\)
Become Hurwitz \(\zeta\) values, resulting in the \(\zeta(2n)\) closed form; then
\(\zeta(2n)\in \pi^{2n}\QQ\)
roll out \(\mu_{2n}\in\QQ\)。
From the polynomiality of Hankel determinants to moments, we get \(\mathfrak H_\kappa\in\QQ\)。
The values ​​listed are calculated directly from this closed equation. Certification completed.
\end{proof}

\begin{remark}[Law of Arithmetic Bandwidth:\texorpdfstring{$\mathfrak H_\kappa$}{H_k} of \texorpdfstring{$p$}{p}-Advanced rigidity is only supported in \texorpdfstring{$p\le 2\kappa-1$}{p<=2k-1}]\label{rem:xi-hellinger-hankel-padic-bandwidth}
make \(\nu_p(\cdot)\) for \(p\)-advance assignment.
by proposition \ref{prop:xi-hellinger-hankel-constant-rationality} middle \(\mu_{2n}\) of the Bernoulli--von Staudt--Clausen denominator structure:
for any prime number \(p>2\kappa-1\),have \(\nu_p(\mu_0),\dots,\nu_p(\mu_{2\kappa-2})\ge 0\)。
therefore \(\nu_p(\mathfrak H_\kappa)=0\)（\(p>2\kappa-1\)）。
In other words,\(\mathfrak H_\kappa\) of all \(p\)-advanced "ill-conditioned/rigid" can only be represented by a finite set of prime numbers \(\{p:\ p\le 2\kappa-1\}\) contribute.
\end{remark}

\begin{proposition}[Toeplitzization of isometric depth:\texorpdfstring{$\cosh^{-2}$}{cosh^{-2}} Poisson period and symbol]\label{prop:xi-hellinger-toeplitz-symbol-poisson}
like \(\sigma_j=\sigma_0+(j-1)\Delta\)（\(\Delta>0\)),but \(G(\boldsymbol{\sigma})\) is the Toeplitz matrix
\(
G_{jk}=A((j-k)\Delta)
\)。
Its Toeplitz symbol \(\sigma_\Delta:[-\pi,\pi]\to(0,\infty)\) can be written as
\[
\boxed{\;
\sigma_\Delta(\theta)
:=\sum_{n\in\ZZ}A(n\Delta)e^{\mathrm{i}n\theta}
=\frac{1}{\Delta}\sum_{k\in\ZZ}\frac{\pi^{2}}{\cosh^{2}\!\Bigl(\pi\frac{\theta+2\pi k}{\Delta}\Bigr)}\;}
\qquad(\theta\in[-\pi,\pi]).
\]
Equivalently, let \(q:=e^{-\Delta/2}\) and take Jacobi \(\vartheta_4\) function(nome \(q\)）
\[
\vartheta_4(z,q):=\sum_{m\in\ZZ}(-1)^m q^{m^2}e^{2\mathrm{i}mz},
\]
then for all \(\theta\in[-\pi,\pi]\) has a strict identity
\[
\boxed{\;
\sigma_\Delta(\theta)
=1+\Delta\,\partial_\theta^{2}\log\vartheta_4\!\Bigl(\frac{\theta}{2},q\Bigr)
=1+2\Delta\sum_{n\ge 1}\frac{nq^{n}}{1-q^{2n}}\cos(n\theta).\;}
\]
\end{proposition}
\begin{proof}
Toeplitz sex by \(G_{jk}=A(\sigma_j-\sigma_k)=A((j-k)\Delta)\) directly.
The Poisson summation formula is given under this Fourier convention
\(
\sum_{n\in\ZZ}A(n\Delta)e^{\mathrm{i}n\theta}
=\Delta^{-1}\sum_{k\in\ZZ}\widehat A((\theta+2\pi k)/\Delta)
\)。
Substitute proposition \ref{prop:xi-hellinger-kernel-fourier-sech2} of \(\widehat A=w\) obtains the Poisson period sum representation.
\par
On the other hand, by
\(
A(n\Delta)=\frac{n\Delta}{2\sinh(n\Delta/2)}
=n\Delta\,\frac{q^n}{1-q^{2n}}
\)
（\(n\ge 1\)、\(q=e^{-\Delta/2}\)) to get the Fourier series representation
\[
\sigma_\Delta(\theta)=1+2\sum_{n\ge 1}A(n\Delta)\cos(n\theta)
=1+2\Delta\sum_{n\ge 1}\frac{nq^{n}}{1-q^{2n}}\cos(n\theta).
\]
Then by \(\vartheta_4\)'s Jacobi product formula leads to the standard identity
\[
\partial_z^{2}\log\vartheta_4(z,q)
=8\sum_{n\ge 1}\frac{nq^{n}}{1-q^{2n}}\cos(2nz),
\]
Pick \(z=\theta/2\) and use \(\partial_\theta^{2}=(1/4)\partial_z^{2}\) to obtain the closed form of the derivative. Certification completed.
\end{proof}

\begin{remark}[modular dual scale \texorpdfstring{$\Delta\leftrightarrow 2\pi^{2}/\Delta$}{Delta <-> 2pi^2/Delta}: Forced equivalence of Poisson peaking and Fourier decay]\label{rem:xi-hellinger-toeplitz-modular-duality-scale}
in proposition \ref{prop:xi-hellinger-toeplitz-symbol-poisson} Zhong Ling \(\tau:=\mathrm{i}\Delta/(2\pi)\), then nome \(q=e^{\mathrm{i}\pi\tau}=e^{-\Delta/2}\)。
Jacobi transformation \(\tau\mapsto -1/\tau\) export dual name
\[
q_\star:=e^{\mathrm{i}\pi(-1/\tau)}=e^{-2\pi^{2}/\Delta}.
\]
Under this transformation,\(\sigma_\Delta\)'s Fourier series representation and Poisson's periodic sum representation are two equivalent representations of the same θ-structure;
where the scale constant \(2\pi^{2}/\Delta\) is not an approximation trick, but is institutionally forced by the modular duality of the theta-function.
\end{remark}

\begin{proposition}[Inverse matrix localization of isometric Toeplitz phases: exponential decay of the inversion operator capped by bandwidth]\label{prop:xi-hellinger-toeplitz-inverse-localization}
in proposition \ref{prop:xi-hellinger-toeplitz-symbol-poisson} Under the isometric setting of , let
\(
G_\kappa(\Delta)=(A((j-k)\Delta))_{1\le j,k\le \kappa}
\)
And record its symbol as \(\sigma_\Delta\)。
but \(\sigma_\Delta\) in the strip domain \(|\Im\theta|<\Delta/2\) parsed within.
and for any \(0<\varepsilon<\Delta/2\), there is a constant \(C_{\Delta,\varepsilon}>0\) to make everything right \(\kappa\) and \(1\le j,k\le\kappa\) have
\[
\boxed{\;
\bigl|\bigl(G_\kappa(\Delta)^{-1}\bigr)_{jk}\bigr|
\le C_{\Delta,\varepsilon}\,e^{-(\Delta/2-\varepsilon)|j-k|}\;}
\]
Therefore, equidistant depth coding has strict "local reversibility".
Index threshold \(\Delta/2\) Depend on \(\sigma_\Delta\) is closed by the nearest complex singularity distance (see Proposition \ref{prop:xi-hellinger-toeplitz-symbol-poisson} Fourier coefficient decay rate).
\end{proposition}
\begin{proof}[Points of proof]
Depend on \(A(n\Delta)=O(n\Delta\,e^{-n\Delta/2})\) have to \(\sigma_\Delta\)’s Fourier coefficient decays exponentially, so \(\sigma_\Delta\) exist \(|\Im\theta|<\Delta/2\) parsed within.
and due to \(\sigma_\Delta(\theta)>0\)（\(\theta\in[-\pi,\pi]\)) and the analytic function is continuous, any \(0<\varepsilon<\Delta/2\), in the closed band \(|\Im\theta|\le \Delta/2-\varepsilon\) has a consistent lower bound on \(\inf|\sigma_\Delta|>0\)。
therefore \(1/\sigma_\Delta\) is analytic and bounded within this closed band, and its Fourier coefficients are \(e^{-(\Delta/2-\varepsilon)|n|}\) decay.
For infinite Toeplitz operators, the Toeplitz coefficient of the inverse kernel is \(1/\sigma_\Delta\) of the Fourier coefficient; finite truncation \(G_\kappa(\Delta)^{-1}\) is derived from the same Wiener algebraic closure and finite truncation stability. Certification completed.
\end{proof}

\begin{theorem}[Toeplitz thermodynamic limit: isometric volume entropy density sealed by single-symbol integration]\label{thm:xi-hellinger-toeplitz-szego-thermo-limit}
in proposition \ref{prop:xi-hellinger-toeplitz-symbol-poisson} Under the setting of
\(
D_\kappa(\Delta):=\det(G_{jk})_{1\le j,k\le\kappa}
\)。
then there is a limit
\[
\boxed{\;
\lim_{\kappa\to\infty}\frac{1}{\kappa}\log D_\kappa(\Delta)
=\frac{1}{2\pi}\int_{-\pi}^{\pi}\log\sigma_\Delta(\theta)\,\dd\theta\;}
\]
and therefore
\[
\boxed{\;
\lim_{\kappa\to\infty}\frac{1}{\kappa}S_{1/2}(\boldsymbol{\sigma})
=-\frac{1}{2\pi}\int_{-\pi}^{\pi}\log\sigma_\Delta(\theta)\,\dd\theta\;}
\qquad(\sigma_j=\sigma_0+(j-1)\Delta).
\]
\end{theorem}
\begin{proof}
by proposition \ref{prop:xi-hellinger-toeplitz-symbol-poisson}，\(\sigma_\Delta\) exist \([-\pi,\pi]\) is strictly positive and analytical.
Therefore Szeg\H{o} The theorem applies, and the linear limit expression of the Toeplitz determinant is given; the second expression is given by \(S_{1/2}=-\log D_\kappa\) is launched directly. Certification completed.
\end{proof}

\begin{proposition}[Heine--Szeg\H{o} Unitary matrix model: isometric depth volume is CUE external potential partition function]\label{prop:xi-hellinger-toeplitz-heine-szego-cue}
in proposition \ref{prop:xi-hellinger-toeplitz-symbol-poisson} Under the setting of , there is a unitary positive integral expressed
\[
\boxed{\;
D_\kappa(\Delta)
=\frac{1}{(2\pi)^\kappa\kappa!}
\int_{[-\pi,\pi]^\kappa}
\Bigl(\prod_{r=1}^{\kappa}\sigma_\Delta(\theta_r)\Bigr)
\prod_{1\le r<\ell\le\kappa}\bigl|e^{\mathrm{i}\theta_r}-e^{\mathrm{i}\theta_\ell}\bigr|^2\,
\dd\theta_1\cdots\dd\theta_\kappa.\;}
\]
Therefore, under the isometric configuration \(S_{1/2}\) is equivalent to CUE(\(\beta=2\)) Model external potential \(-\log\sigma_\Delta\) the free energy increment under .
\end{proposition}
\begin{proof}
This is the Heine--Szeg between the Toeplitz determinant and the unitary eigenangle integral\H{o} Identity. Certification completed.
\end{proof}

\input{para__xi-hellinger-toeplitz-tail-asymptotics}

