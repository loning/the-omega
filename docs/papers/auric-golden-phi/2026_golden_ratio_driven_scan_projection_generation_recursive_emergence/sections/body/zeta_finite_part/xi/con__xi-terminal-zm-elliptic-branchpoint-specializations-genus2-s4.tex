\begin{conclusion}[Lee--Yang near-square specialization of branch point multiplication:\texorpdfstring{$y=6,21$}{y=6,21} Linear splitting of the square factor of and the quartic spectral equation]\label{con:xi-terminal-zm-branchpoint-doubling-leyang-nearsquare-specializations}
Let Lee--Yang cubic factors
\[
P_{\mathrm{LY}}(y):=256y^{3}+411y^{2}+165y+32.
\]
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-mw-generator-alignment} symbol (\(Q_0=[2]R\)、\(Q_0'=[3]R\) for \(y=0,1\), then the value of its multiple point on the weight coordinate satisfies
\[
y([2]Q_0)=6,\qquad y([2]Q_0')=21.
\]
and a fully explicit "near-square" specialization appears
\[
P_{\mathrm{LY}}(6)=2\cdot 37\cdot 31^{2},\qquad P_{\mathrm{LY}}(21)=4\cdot 11\cdot 241^{2}.
\]
Equivalently, the discriminant kernel extracts full square factors at these two points:
\[
-6\cdot 5\cdot P_{\mathrm{LY}}(6)= -555\cdot(2\cdot 31)^{2},\qquad
-21\cdot 20\cdot P_{\mathrm{LY}}(21)= -1155\cdot(4\cdot 241)^{2}.
\]
At the same time, the quartic spectral equation \(\Pi(\lambda,y)=0\) occurs a linear cleavage on the corresponding fiber:
\[
\Pi(\lambda,6)=(\lambda-2)(\lambda^{3}+\lambda^{2}-11\lambda-21),\qquad
\Pi(\lambda,21)=(\lambda-6)(\lambda^{3}+5\lambda^{2}-13\lambda-77).
\]
The above identity connects "branch point multiplication" and "square decomposition of Lee--Yang discriminant kernel" with two sets of complete explicit integer decompositions, providing the shortest arithmetic entry for subsequent local domain analysis (especially bad prime numbers and square type control).
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-elliptic-mw-generator-alignment} The low-magnification point coordinates of \(y([4]R)\)、\(y([6]R)\) to get \(6,21\); the rest of the decomposition is due to \(\ZZ\) and factor it to get.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_mw\_height\_denominator\_growth\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[The genus of the discriminant double overlay \(2\) model with resolvent's non-divergence triple boost]\label{con:xi-terminal-zm-discriminant-ridge-etale-c3-explicit-model}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes} of genus \(2\) curve
\[
C:\quad w^{2}=-y(y-1)P_{\mathrm{LY}}(y),
\]
And order \(\mathscr R(z,y)\in\ZZ[y][z]\) is the conclusion \ref{con:xi-terminal-zm-generic-galois-s4} The cubic decomposition of
\[
\mathrm{Disc}_{z}\bigl(\mathscr R(\cdot,y)\bigr)=-y(y-1)P_{\mathrm{LY}}(y).
\]
make \(C_{6}\) is an affine curve
\[
w^{2}=-y(y-1)P_{\mathrm{LY}}(y),\qquad \mathscr R(z,y)=0
\]
The normalized smooth projective model of \(\pi:C_{6}\to C\) Depend on \((y,w,z)\mapsto(y,w)\) is given.
but:
\begin{enumerate}
  \item \(\pi\) is a cyclic Galois triple cover, and \(\mathrm{Gal}(C_{6}/C)\cong C_{3}\)；
  \item \(\pi\) is non-divergent everywhere (that is, \'etale override);
  \item is thus obtained by the Riemann--Hurwitz formula \(g(C_{6})=4\)。
\end{enumerate}
Furthermore, composite mapping \(C_{6}\to C\to\mathbb P^1_y\) is normal \(S_3\)-Galois overlay;
Its divergence value set is exactly \(\{0,1,\infty\}\cup\{P_{\mathrm{LY}}(y)=0\text{ of three}\}\), and the order of the lazy group at each of the above divergence values ​​is \(2\)。
The explicit model and conclusions \ref{con:xi-terminal-zm-s4-galois-closure-genus-tower-holomorphic-differentials} Zhongyou \(S_4\) subgroup lattice-induced middleman curve \(C_6=\widetilde X/V_4\) are consistent.
\end{conclusion}
\begin{proof}[Points of proof]
set up \(k=\QQ(y)\), and with \(\mathscr R(z,y)\) defines three expansions \(k\subset k(z)\)。
By conclusion \ref{con:xi-terminal-zm-generic-galois-s4}，\(\mathscr R\) gives its discriminant \(S_3\)-the unique quadratic subexpansion of Galois closure,
And the second sub-expansion is exactly \(k(w)\)(in \(w^2=\mathrm{Disc}_z(\mathscr R)\)), corresponding to the curve \(C\) function domain.
Therefore, the bottom change \(k\subset k(w)\), the Galois group is given by \(S_3\) shrinks to \(A_3\cong C_3\),thereby \(\pi\) is a loop triple coverage.
\par
on the other hand,\(k\subset k(z)\) of all inertia groups (corresponding to \(\mathrm{Disc}_z(\mathscr R)=0\) is generated by transposition,
its and \(A_3\) is empty; therefore, after the second base change, no inert group will appear on the cyclic triple quotient cover, so \(\pi\) is everywhere non-divergent.
Finally, by \(g(C)=2\) and no divergence times \(3\) have to
\(
2g(C_{6})-2=3(2g(C)-2)=6
\), so \(g(C_{6})=4\). Certification completed.
\end{proof}

\begin{conclusion}[The fifth discriminant of the discriminant quadratic ridge curve and the bad reduced prime set:\texorpdfstring{$\{2,3,31,37\}$}{\{2,3,31,37\}} strict decomposition]\label{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes}
make
\[
P_{\mathrm{LY}}(y):=256y^{3}+411y^{2}+165y+32,\qquad
f(y):=-y(y-1)P_{\mathrm{LY}}(y)\in\ZZ[y],
\]
and consider belonging to \(2\) hyperelliptic curve
\[
C:\ w^{2}=f(y).
\]
Then the polynomial discriminant satisfies the strict decomposition
\[
\boxed{\ \mathrm{Disc}_{y}(f)=-2^{20}\cdot 3^{15}\cdot 31^{2}\cdot 37\ }.
\]
Therefore for any prime number \(p\notin\{2,3,31,37\}\), right end \(f(y)\) exist \(\FF_p[y]\) is the square free polynomial, the curve \(C\) exist \(p\) are well reduced.
\end{conclusion}
\begin{proof}[Points of proof]
Factorization of polynomial discriminants is obtained by direct symbolic evaluation.
For odd prime numbers \(p\),Model \(w^2=f(y)\) exist \(\FF_p\) is smooth if and only if \(f\) exist \(\FF_p\) has no repeated roots, which is equivalent to \(\gcd(f,f')=1\), also equivalent to \(p\nmid \mathrm{Disc}_{y}(f)\)。
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_leyang\_discriminant\_ridge\_difference\_square\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Rational torsion upper bounds for Jacobian varieties of discriminant quadratic ridge curves: well-reduced injection and \texorpdfstring{$\gcd_{p}\#J(\FF_{p})=12$}{gcd J(Fp)=12}]\label{con:xi-terminal-zm-discriminant-ridge-jacobian-torsion-upper-bound}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes} of genus \(2\) curve
\[
C:\quad w^{2}=-y(y-1)P_{\mathrm{LY}}(y),
\qquad
J:=\operatorname{Jac}(C).
\]
but:
\begin{enumerate}
  \item consists of rational divergence points \((y,w)=(0,0),(1,0)\) and the point at infinity \(\infty\) gets two nonzero \(2\) - scratching type
  \[
  D_{0}:=[(0,0)-\infty],\qquad D_{1}:=[(1,0)-\infty]\in J(\QQ)[2],
  \]
  and \(D_{0}\neq D_{1}\),therefore \((\ZZ/2\ZZ)^{2}\hookrightarrow J(\QQ)_{\mathrm{tors}}\)。
  \item for any good prime number \(p\notin\{2,3,31,37\}\)，N\The specialization injective of 'eron's model gives
  \[
  J(\QQ)_{\mathrm{tors}}\hookrightarrow J(\FF_{p}),
  \]
  thereby \(\#J(\QQ)_{\mathrm{tors}}\mid \gcd_{p}\#J(\FF_{p})\)。
  by \(p=5,7,11,13,17,19,23,29\) is a set of audit samples, with
  \input{sections/generated/eq_fold_zm_discriminant_ridge_jacobian_torsion_upper_bound_audit}
  thus \(\#J(\QQ)_{\mathrm{tors}}\mid 12\)。
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
For hyperelliptic curves \(w^{2}=f(y)\)（\(\deg f=5\)), the difference between the Weierstrass point and the point at infinity is \(\mathrm{Pic}^{0}\) in \(2\)-scratch;
here \(y=0,1\) is a reasonable point of disagreement, so \(D_{0},D_{1}\in J(\QQ)[2]\) and non-zero.
The difference between the two can be derived from the different supports or equivalently from the basic properties of the divergence spread structure, thus obtaining \((\ZZ/2\ZZ)^2\) injection.
\par
when \(p\) is a good prime number and \(p\neq 2,3\), NaoZi remains single-shot under specialization, so we have \(J(\QQ)_{\mathrm{tors}}\hookrightarrow J(\FF_p)\)。
and \(\#J(\FF_p)=L_p(1)\),in \(L_p(T)\) for \(C/\FF_p\) part of \(L\)-polynomial;
The values ​​listed above can be determined by points \(\#C(\FF_p),\#C(\FF_{p^2})\) Recycle \(L_p\) is strictly calculated.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_discriminant\_ridge\_jacobian\_torsion\_upper\_bound\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Discriminant of quadratic ridge curve \(2\)-torsion split domain and module \(2\) Galois]\label{con:xi-terminal-zm-discriminant-ridge-jacobian-2torsion-field}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes} of genus \(2\) curve
\(
C:\ w^{2}=-y(y-1)P_{\mathrm{LY}}(y)
\)
and its Jacobian variety \(J=\operatorname{Jac}(C)\)。
but:
\begin{enumerate}
  \item \(2\)-torsion splitting domain satisfies
  \[
  \QQ(J[2])=\QQ\bigl(\text{\(P_{\mathrm{LY}}(y)\) all roots of}\bigr),
  \qquad
  \mathrm{Gal}\bigl(\QQ(J[2])/\QQ\bigr)\cong S_{3}.
  \]
  \item the \(S_{3}\)-The only quadratic subdomain of the expansion is
  \[
  \QQ(\sqrt{-111}).
  \]
  \item module \(2\) Galois said
  \[
  \rho_{J,2}:\mathrm{Gal}(\overline{\QQ}/\QQ)\longrightarrow \mathrm{Sp}_{4}(\FF_{2})
  \]
  The image is isomorphic to \(S_{3}\), and can be interpreted as three \(P_{\mathrm{LY}}(y)=0\) of the irrational Weierstrass points (while \(y=0,1,\infty\) The corresponding three points are all rational Weierstrass points).
  \item In particular,
  \[
  J(\QQ)[2]\ \cong\ (\ZZ/2\ZZ)^2,
  \]
  And from the conclusion \ref{con:xi-terminal-zm-discriminant-ridge-jacobian-torsion-upper-bound} of \(D_0,D_1\) generated.
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
For odd degree hyperellipse models \(w^2=f(y)\) (here \(f(y)=-y(y-1)P_{\mathrm{LY}}(y)\)),\(\overline{\QQ}\)-Weierstrass point by \(f(y)=0\) are given by five finite roots and points at infinity, a total of \(6\); and \(J[2]\) is generated by the class Weierstrass Spread.
therefore \(\QQ(J[2])\) are generated from Weierstrass point coordinates.
because \(y=0,1,\infty\) is a rational Weierstrass point, non-trivially generated only from \(P_{\mathrm{LY}}(y)=0\) of three roots, thus \(\QQ(J[2])\) is \(P_{\mathrm{LY}}\) of the split domain.
By conclusion \ref{con:xi-terminal-zm-leyang-cubic-arithmetic}，\(P_{\mathrm{LY}}\) exist \(\QQ\) and its split domain Galois group is \(S_3\), and has a unique quadratic subdomain \(\QQ(\sqrt{-111})\), then we get the first two points.
\par
mold \(2\) express \(\rho_{J,2}\) is compatible with the Galois effect on the six Weierstrass spread classes; since three are located \(P_{\mathrm{LY}}(y)=0\) of Weierstrass points form a \(S_3\)-orbit while the remaining three points are fixed, visible \(\rho_{J,2}\) is isomorphic to the permutation group \(S_3\)。
Finally, from the above \(S_3\)-acting Galois invariants only come from the differences of rational Weierstrass points, it can be seen that \(J(\QQ)[2]\) does not contain additional elements, so \(J(\QQ)[2]\cong(\ZZ/2\ZZ)^2\), and by \(D_0,D_1\) generated. Certification completed.
\end{proof}

\begin{conclusion}[The discriminant quadratic ridge curve is in \texorpdfstring{$\QQ$}{Q} Automorphic rigidity on: no additional symmetries except hyperelliptic involution]\label{con:xi-terminal-zm-discriminant-ridge-autq-rigidity}
Still set \(C:w^{2}=-y(y-1)P_{\mathrm{LY}}(y)\)。
but
\[
\mathrm{Aut}_{\QQ}(C)\ \cong\ \ZZ/2\ZZ,
\]
Its generator is the hyperellipse involution \((y,w)\mapsto (y,-w)\)。
\end{conclusion}
\begin{proof}[Points of proof]
arbitrary \(\QQ\)-automorphism at the base curve \(\PP^{1}_{y}\) and permute the divergence set
\(
\{0,1,\infty\}\cup\{P_{\mathrm{LY}}(y)=0\text{ of three}\}
\)。
because \(\{0,1,\infty\}\) for \(\QQ\)-a set of rational divergence points, the induced Möbius transformation must be stable \(\{0,1,\infty\}\) is one of six standard transformations.
Substitute one by one and compare \(P_{\mathrm{LY}}\) (equivalently, comparing the root set image), it can be seen that: except for identity, none of \(P_{\mathrm{LY}}\) root set.
Therefore, the induced transformation can only be identity, so the automorphism in the coordinate \(y\), so only hyperelliptic involution is possible. Certification completed.
\end{proof}

It can be seen that from the spectral fourth-order polynomial \(\Pi(\lambda,y)\)(theorem \ref{thm:xi-terminal-zm-leyang-elliptic-structure}) the discriminant quadratic ridge curve induced by \(C\) simultaneously render visible at the level of arithmetic geometry \(2\) - and strict rational upper bounds (Conclusion \ref{con:xi-terminal-zm-discriminant-ridge-jacobian-torsion-upper-bound}), and carries a canonical loop three times \'etale overlay (conclusion \ref{con:xi-terminal-zm-discriminant-ridge-etale-c3-explicit-model}), which is in \(K=\QQ(\sqrt{-3})\) can be Kummerized and further exported \(\QQ\)-defined \(3\)-homology (conclusion \ref{con:xi-terminal-zm-discriminant-ridge-3isogeny-descent}）。
Combined with Lee--Yang cubic Chebotarev density law (Conclusion \ref{con:xi-terminal-zm-leyang-cubic-modp-splitting-density}), the visibility of divergence points at the reduction point can be converted into decomposed statistics, thereby connecting the divergence spectrum data with the torturator/cover tower structure in an auditable manner.

\begin{conclusion}[Certificate of the squared difference norm on the quadratic ridge of the discriminant:\texorpdfstring{$Q(y)^2+27w^2=(16A(y)^3)^2$}{Q(y)^2+27w^2=(16A(y)^3)^2}]\label{con:xi-terminal-zm-leyang-discriminant-ridge-diff-square-norm}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes} curve \(C\) and its affine coordinates \((y,w)\)。
make
\[
A(y):=1+2y,\qquad Q(y):=16+69y+219y^{2}+128y^{3}.
\]
then in \(\QQ[y]\) (in fact \(\ZZ[y]\)) two linear identities are simultaneously established in
\[
\boxed{\ Q(y)-16A(y)^3=27y(y-1)\ },\qquad
\boxed{\ Q(y)+16A(y)^3=P_{\mathrm{LY}}(y)\ },
\]
in \(P_{\mathrm{LY}}(y)=256y^3+411y^2+165y+32\) is the conclusion \ref{con:xi-terminal-zm-discriminant-leyang} The Lee--Yang cubic factors.
Thereby automatically deducing the difference square decomposition
\[
\boxed{\ Q(y)^2-256A(y)^6=27y(y-1)P_{\mathrm{LY}}(y)\ }.
\]
Equivalently, in the function domain \(\QQ(C)=\QQ(y,w)\), the norm form is established in
\[
\boxed{\ Q(y)^2+27w^2=(16A(y)^3)^2\ }.
\]
Thus in the second expansion \(\QQ(C)(\sqrt{-3})/\QQ(C)\) on, element
\[
U:=\frac{Q(y)+3w\sqrt{-3}}{16A(y)^3}
\]
Satisfy the strict norm identity
\[
\operatorname{N}_{\QQ(C)(\sqrt{-3})/\QQ(C)}(U)=1.
\]
The norm--\(1\) certificate checks the discriminant \(-y(y-1)P_{\mathrm{LY}}(y)\) is structured as \(\QQ(\sqrt{-3})\) on the quadratic norm expression, thus the conclusion \ref{con:xi-terminal-zm-s4-galois-closure-genus-tower-holomorphic-differentials} The cubic divergence-free coverage of the cycle gives an explicit algebraic entry into the Kummer-type description.
\end{conclusion}
\begin{proof}[Points of proof]
Depend on
\(
16A(y)^3=16(1+2y)^3=16+96y+192y^2+128y^3
\)
and \(Q(y)\) can be obtained by comparing item by item
\(
Q(y)-16A(y)^3=27y^2-27y=27y(y-1)
\)
and
\(
Q(y)+16A(y)^3=256y^3+411y^2+165y+32=P_{\mathrm{LY}}(y)
\)。
Multiplying the two equations yields the squared decomposition of the difference.
Then convert the curve equation \(w^2=-y(y-1)P_{\mathrm{LY}}(y)\) to get the norm form.
The norm identity is given by
\(
\operatorname{N}(Q+3w\sqrt{-3})=Q^2+27w^2
\)
with denominator \((16A^3)^2\) follows immediately.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_leyang\_discriminant\_ridge\_difference\_square\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Chebyshev fiber product structure: on the quadratic ridge curve \(S_3\) Six closures and triple loops \'etale overlay]\label{con:xi-terminal-zm-discriminant-ridge-chebyshev-fiber-product}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-diff-square-norm} notation, and define rational functions
\[
a(y):=\frac{Q(y)}{8A(y)^3}\ \in\ \QQ(y).
\]
Consider a Chebyshev-type cubic map
\[
\psi:\ \PP^1_s\longrightarrow\PP^1_a,\qquad a=s^3-3s,
\]
its about variables \(s\) satisfies the discriminant of
\[
\boxed{\ \mathrm{Disc}_{s}\bigl(s^3-3s-a\bigr)=-27\,(a^2-4)\ }.
\]
By conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-diff-square-norm} The two linear identities of
\[
a(y)-2=\frac{Q(y)-16A(y)^3}{8A(y)^3}=\frac{27y(y-1)}{8A(y)^3},
\qquad
a(y)+2=\frac{Q(y)+16A(y)^3}{8A(y)^3}=\frac{P_{\mathrm{LY}}(y)}{8A(y)^3}.
\]
thereby \(a(y)\) exist \(y\)-Put the divergence set online
\(
\{0,1,\infty\}\cup\{P_{\mathrm{LY}}(y)=0\}
\)
Compressed to \(\{2,-2,\infty\}\subset\PP^1_a\)：
\(a^{-1}(2)=\{y=0,1,\infty\}\)，\(a^{-1}(-2)=\{P_{\mathrm{LY}}(y)=0\}\),and \(a^{-1}(\infty)=\{A(y)=0\}=\{y=-\tfrac12\}\)。
\par
make \(C_6\) is the fiber volume \(C\times_{\PP^1_a}\PP^1_s\) regularization (where \(C\to\PP^1_a\) Depend on \(C\to\PP^1_y\) and \(a(y)\) compounded).
but:
\begin{enumerate}
  \item composite mapping \(C_6\to\PP^1_y\) is the number of times \(6\) of \(S_3\)-Galois overlay, whose inert group consists of transposition (order \(2\) element) generated;
  \The number of item inductions \(3\) overlay \(C_6\to C\) is a cyclic Galois overlay without divergence everywhere (\'etale）；
  \item therefore \(g(C_6)=3(g(C)-1)+1=4\)。
\end{enumerate}
and that \(C_6\to C\) and conclusion \ref{con:xi-terminal-zm-discriminant-ridge-etale-c3-explicit-model} The cubic decomposition formula \(\mathscr R(z,y)=0\) depicts a cycle three times \'etale overlay is consistent.
\end{conclusion}
\begin{proof}[Points of proof]
\(\psi\) exist \(a=\pm 2\) has an exponent at \(2\), in \(a=\infty\) has an exponent at \(3\) total divergence;
On the other hand, it can be seen from the above decomposition: in \(C\to\PP^1_y\) all divergence points (\(y=0,1,\infty\) and \(P_{\mathrm{LY}}(y)=0\), composite mapping \(C\to\PP^1_a\), the divergence indexes are all \(2\), while in \(A(y)=0\) two points \(P_\pm\), the divergence index is \(3\)。
Therefore at each divergence point, the composite map \(C\to\PP^1_a\) and the divergence index of \(\psi\) have the same divergence index; according to Abhyankar's lemma (tamed divergence case), it is known that the three times after pulling back are covered in \(C\), so that \(C_6\to C\) for \'etale loop triple coverage.
its and \(S_3\)-closure correspondence is given by the discriminant identity
\(
\mathrm{Disc}_{s}(s^3-3s-a(y))\sim -y(y-1)P_{\mathrm{LY}}(y)
\)
gives (constant and \(A(y)^6\) only contributes the square factor), then we get \(C_6\to\PP^1_y\) of \(S_3\)-Galois sex.
Finally, according to the Riemann--Hurwitz formula, we get \(g(C_6)=4\). Certification completed.
\end{proof}

\begin{conclusion}[Loop three times without divergence covering Kummer--Cardano normal form and dihedral promotion: explicit generator and decomposition isomorphism]\label{con:xi-terminal-zm-discriminant-ridge-kummer-dihedral}
make \(K:=\QQ(\sqrt{-3})\), and take
\[
\zeta_{3}:=\frac{-1+\sqrt{-3}}{2}.
\]
Follow the conclusion \ref{con:xi-terminal-zm-discriminant-ridge-etale-c3-explicit-model} loop three times \'etale override \(\pi:C_{6}\to C\), and in the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-diff-square-norm} The mark ordered
\[
u:=\frac{Q(y)+3w\sqrt{-3}}{16A(y)^3}\in K(C)^{\times}.
\]
but \(\operatorname{N}_{K(C)/\QQ(C)}(u)=1\), and in \(K\) covering the loop three times can be transformed into Kummer
\[
C_{6,K}:\quad t^{3}=u,
\]
its level \(3\)'s deck transformation is given by \(t\mapsto \zeta_{3}t\) is given.
Further, set
\[
t_{0}:=\frac{2A(y)}{3}\,t
\qquad
\Bigl(\text{thereby }t_{0}^{3}=\frac{Q(y)+3w\sqrt{-3}}{54}\Bigr),
\]
and define
\[
z:=t_{0}+\frac{4A(y)^{2}}{9t_{0}}-\frac{A(y)}{3}\ \in\ K(C_{6,K}).
\]
but \(z\) satisfies the decomposition equation \(\mathscr R(z,y)=0\), thus giving an explicit isomorphism of the function domain
\[
K(C_{6})\otimes_{\QQ}K
=K(y,w,t)\ \cong\ K(y,w,z),
\]
and conclusion \ref{con:xi-terminal-zm-discriminant-ridge-etale-c3-explicit-model} Zhongyou \(\mathscr R(z,y)=0\) defined \(C_{6}\) model is consistent.
In addition, hyperelliptic inversion \(\iota:(y,w)\mapsto(y,-w)\) on this Kummer model is promoted to
\[
(y,w,t)\longmapsto (y,-w,t^{-1}),
\]
Its right \(t_0\) is equivalent to writing \(t_{0}\mapsto \frac{4A(y)^{2}}{9t_{0}}\), and make \(z\) remains unchanged; therefore
\[
K(C_{6,K})^{\langle t\mapsto \zeta_{3}t\rangle}=K(y,w)=K(C)\otimes_{\QQ}K,
\qquad
K(C_{6,K})^{\langle \iota\rangle}=K(y,z).
\]
thus in \(K\) to get an explicit semidirect product action
\[
\bigl\langle\ t\mapsto \zeta_{3}t,\ (y,w,t)\mapsto (y,-w,t^{-1})\ \bigr\rangle
\ \cong\ C_{3}\rtimes C_{2}\ \cong\ D_{6}\ \cong\ S_{3}
\]
At \(C_{6,K}\); this function fixes the bottom coordinate \(y\), and implement it specifically \(C_{6,K}\to\mathbb P^{1}_{y}\) of \(S_{3}\)-normal single value group.
\end{conclusion}
\begin{proof}[Points of proof]
because \(K\) contains a cubic unit root \(\zeta_3\), any number of times \(3\) The domain expansion of the cyclic function is in \(K\) can be expressed by the Kummer equation \(t^{3}=u\) gives (where \(u\in K(C)^{\times}\) unique until multiplied by the cube).
in conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-diff-square-norm} has been constructed to satisfy \(\operatorname{N}_{K(C)/\QQ(C)}(u)=1\) explicit generator \(u\), which is related to \(\pi:C_6\to C\) of \(A_3\)-subexpansion is consistent, thus giving the Kummer normal form shown.
at last,\(\iota\) under the influence \(w\mapsto -w\) make
\(
u\mapsto u^{-1}
\)，
Therefore \(t\mapsto t^{-1}\) gives its boost.
right \(t_0=\frac{2A(y)}{3}t\) and \(z=t_0+\frac{4A(y)^2}{9t_0}-\frac{A(y)}{3}\) is verified by direct substitution of the Cardano cubic solution, and this construction combines the Kummer cube root with the factorized root \(z\) isomorphism explicit.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_trace\_galois\_audit.py}。
make
\(
\sigma:t\mapsto \zeta_3 t
\)
and
\(
\tau:(y,w,t)\mapsto (y,-w,t^{-1})
\)，
then satisfies the relationship \(\tau\sigma\tau=\sigma^{-1}\), thus generating a dihedral group \(D_6\) (order \(6\)）。
Certification completed.
\end{proof}

\begin{conclusion}[Completely localized and explicit divisors of Kummer generators \(3\) - scratch point]\label{con:xi-terminal-zm-discriminant-ridge-kummer-divisor-3torsion}
in conclusion \ref{con:xi-terminal-zm-discriminant-ridge-kummer-dihedral} Under the sign, order
\[
P_{\pm}:=\left(y=-\frac12,\ w=\pm\frac{9}{4}\sqrt{-3}\right)\in C(K),
\qquad
u:=\frac{Q(y)+3w\sqrt{-3}}{16A(y)^3}\in K(C)^{\times},
\qquad
v:=\frac{Q(y)+3w\sqrt{-3}}{Q(y)-3w\sqrt{-3}}\in K(C)^{\times}.
\]
but \(v=u^{2}\), and the principal divisor satisfies the strict identity
\[
\operatorname{div}(u)=3(P_{+}-P_{-}),
\qquad
\operatorname{div}(v)=6(P_{+}-P_{-}),
\]
thereby \(u\) and \(v\) has its zeros and poles supported only at \(\{P_{+},P_{-}\}\) superior.
therefore
\[
\tau:=[P_{+}-P_{-}]\in \mathrm{Pic}^{0}(C_{K})\simeq J(C)(K)
\]
satisfy \(3\tau=0\) and \(\tau\neq 0\),thereby \(\mathrm{ord}(\tau)=3\)。
and due to \(v=u^{2}\) and \(\gcd(2,3)=1\), Kummer expansion \(t^{3}=u\) and \(s^{3}=v\) gives the same \(\mu_{3}\)-twist,
Therefore and conclusion \ref{con:xi-terminal-zm-discriminant-ridge-kummer-dihedral} The cubic coverage of the given cycle is isomorphic in the sense of a functional domain.
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(P_{\mathrm{LY}}(-\tfrac12)=\tfrac{81}{4}\) have to
\(
w^{2}\bigl\vert_{y=-1/2}=-(-\tfrac12)(-\tfrac32)\cdot\tfrac{81}{4}=-\tfrac{243}{16}
\)，
So there \(K=\QQ(\sqrt{-3})\) upper fiber \(y=-\tfrac12\) contains two points \(P_{\pm}\) and \(w(P_{\pm})=\pm\frac{9}{4}\sqrt{-3}\)。
On the other hand, from the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-diff-square-norm} The norm identity of can be obtained
\[
\bigl(Q(y)+3w\sqrt{-3}\bigr)\bigl(Q(y)-3w\sqrt{-3}\bigr)=(16A(y)^{3})^{2}.
\]
at point \(P_{+}\) everywhere \(Q(P_{+})+3w(P_{+})\sqrt{-3}=0\) and \(Q(P_{+})-3w(P_{+})\sqrt{-3}\neq 0\)，
Thus the latter in the local ring \(\mathcal O_{C,P_{+}}\) is the unit, so
\(
\mathrm{ord}_{P_{+}}(Q+3w\sqrt{-3})=\mathrm{ord}_{P_{+}}\!\bigl((16A(y)^{3})^{2}\bigr)=6
\)
(because \(A(y)=1+2y\) exist \(y=-\tfrac12\) is a single zero and the fiber does not diverge).
Same reason in \(P_{-}\) get along well \(\mathrm{ord}_{P_{-}}(Q-3w\sqrt{-3})=6\) and \(\mathrm{ord}_{P_{-}}(Q+3w\sqrt{-3})=0\)。
again \(u=(Q+3w\sqrt{-3})/(16A(y)^{3})\),and \(\mathrm{ord}_{P_{\pm}}(A(y))=1\), so
\(
\operatorname{div}(u)=3(P_{+}-P_{-})
\)；
Depend on \(v=(Q+3w\sqrt{-3})/(Q-3w\sqrt{-3})=u^{2}\) have to \(\operatorname{div}(v)=6(P_{+}-P_{-})\)。
then \(3(P_{+}-P_{-})\) to remove the son for the Lord, so \(3\tau=0\)。
like \(\tau=0\),but \(P_{+}-P_{-}\) is the principal divisor, and it is deduced that there is an extraordinary value function with only one pole \(C\to\PP^1\),thereby \(C\) must be a rational curve;
But the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes} given \(g(C)=2\), contradictory, so \(\tau\neq 0\)。
Finally, by \(v=u^{2}\) and \(\gcd(2,3)=1\) due to \(t^{3}=u\) and \(s^{3}=v\) given by \(\mu_3\)-the twister is the same. Certification completed.
\end{proof}

\begin{conclusion}[explicit \(3\)-Cyclic subgroup descending and \texorpdfstring{$\QQ$}{Q}-definition \(3\) - homologous]\label{con:xi-terminal-zm-discriminant-ridge-3isogeny-descent}
make \(K=\QQ(\sqrt{-3})\), and draw conclusions \ref{con:xi-terminal-zm-discriminant-ridge-kummer-divisor-3torsion} of \(3\)-Tick point \(\tau\in J(C)(K)[3]\setminus\{0\}\) and cyclic subgroups
\[
G:=\langle \tau\rangle=\{0,\pm \tau\}\subset J(C)[3].
\]
because \(\mathrm{Gal}(K/\QQ)\) non-trivial element exchange \(P_{+}\leftrightarrow P_{-}\) thereby inducing \(\tau\mapsto -\tau\), it can be known that the subgroup \(G\) exist \(\mathrm{Gal}(\overline{\QQ}/\QQ)\) is stable under action.
Therefore existence is defined by \(\QQ\) degree \(3\) homologous
\[
\varphi:\ J(C)\longrightarrow J(C)/G,
\]
Its core is exactly \(G\)。
\end{conclusion}
\begin{proof}[Points of proof]
for any domain \(F\) Abelian clusters on \(A/F\), if the finite subgroup scheme \(G\subset A\) exist \(\mathrm{Gal}(\overline F/F)\), then the quotient Abelian cluster \(A/G\) homologous to nature \(A\to A/G\) are available at \(F\) up and down.
here \(F=\QQ\)、\(A=J(C)\), and the above formula \(G\)’s Galois stability is given by \(\tau\mapsto -\tau\) is given immediately; because \(|G|=3\), the number of homologous origins is also \(3\). Certification completed.
\end{proof}

\begin{conclusion}[under schizotypal control \(3\)-twist specialization: the visibility bisection law on well-reduced primes]\label{con:xi-terminal-zm-discriminant-ridge-3torsion-specialization}
set up \(p\notin\{2,3,31,37\}\) is a prime number, so the curve \(C\) exist \(p\) is well reduced (conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes}),and \(p\neq 3\)。
make \(\tau\in J(C)(\QQ(\sqrt{-3}))[3]\setminus\{0\}\) is the conclusion \ref{con:xi-terminal-zm-discriminant-ridge-kummer-divisor-3torsion} of \(3\) - scratch the point.
but:
\begin{enumerate}
  \item if \(p\equiv 1\pmod 3\),but \(\sqrt{-3}\in\FF_{p}\), thus point \(P_{\pm}\) and \(\tau\) are in \(\FF_{p}\) can be specialized on and gives non-zero points
  \[
  \tau_{p}\in J(C)(\FF_{p})[3]\setminus\{0\}.
  \]
  \item if \(p\equiv 2\pmod 3\),but \(\sqrt{-3}\notin\FF_{p}\) but \(\sqrt{-3}\in\FF_{p^{2}}\),therefore \(P_{\pm}\) and \(\tau\) exist \(\FF_{p^{2}}\) and gives non-zero points
  \[
  \tau_{p^{2}}\in J(C)(\FF_{p^{2}})[3]\setminus\{0\}.
  \]
\end{enumerate}
Thus the \(3\)-visibility of twist structures over finite fields is given by quadratic expansion \(\QQ(\sqrt{-3})/\QQ\) of the schizotypal form (i.e. \(p\bmod 3\)) Completely decided.
\end{conclusion}
\begin{proof}[Points of proof]
for well reduced primes \(p\neq 3\)，N\The basic properties of the 'eron model ensure that specialization maps between prime factors and \(p\) is an injective on a relatively prime element; therefore \(\tau\neq 0\) of \(3\)-Flexibility remains non-trivial after reduction.
when \(p\equiv 1\pmod 3\) hour,\(-3\) exist \(\FF_{p}\) is the square, so \(\sqrt{-3}\) and dot \(P_{\pm}\) exist \(\FF_{p}\) can be defined;
when \(p\equiv 2\pmod 3\) hour,\(-3\) exist \(\FF_p\) is not square but lies in \(\FF_{p^2}\) must be a square, so the required domain expansion is exactly \(\FF_{p^2}\). Certification completed.
\end{proof}

\begin{conclusion}[The Jacobian cluster of the discriminant quadratic ridge curve is in \texorpdfstring{$\QQ$}{Q} Above is a simple Abelian surface: irreducible Frobenius polynomial certificate]\label{con:xi-terminal-zm-discriminant-ridge-jacobian-qsimple}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes} of genus \(2\) curve \(C\)。
Correct prime numbers \(p\notin\{2,3,31,37\}\) remember its part \(L\)-polynomial is
\[
L_{p}(T)=1-a_{1}T+a_{2}T^{2}-a_{1}pT^{3}+p^{2}T^{4}.
\]
Then the following local data holds (given \(\#C(\FF_p)\)、\(\#C(\FF_{p^{2}})\) and decomposed form):
\begin{itemize}
  \item \(p=5\)：\(\#C(\FF_{5})=5\)、\(\#C(\FF_{25})=33\),and \(L_{5}(T)=(5T^{2}-3T+1)(5T^{2}+2T+1)\)；
  \item \(p=7\)：\(\#C(\FF_{7})=5\)、\(\#C(\FF_{49})=61\),and \(L_{7}(T)=(7T^{2}-4T+1)(7T^{2}+T+1)\)；
  \item \(p=11\)：\(\#C(\FF_{11})=6\)、\(\#C(\FF_{121})=130\),and \(L_{11}(T)=(11T^{2}+1)(11T^{2}-6T+1)\)；
  \item \(p=13\)：\(\#C(\FF_{13})=14\)、\(\#C(\FF_{169})=166\),and \(L_{13}(T)=169T^{4}-2T^{2}+1\) exist \(\ZZ[T]\) is irreducible;
  \item \(p=17\)：\(\#C(\FF_{17})=15\)、\(\#C(\FF_{289})=337\),and \(L_{17}(T)=289T^{4}-51T^{3}+28T^{2}-3T+1\) exist \(\ZZ[T]\) is irreducible.
\end{itemize}
Depend on \(p=13\)(Also available \(p=17\)) at \(L_{p}(T)\) exist \(\ZZ[T]\) is irreducible and can be deduced:
Jacobian cluster \(J(C)\) exist \(\QQ\) does not exist a non-trivial decomposition (does not contain any \(\QQ\)-ellipse factor), that is \(J(C)\) for \(\QQ\)-Simple Abelian surface.
\end{conclusion}
\begin{proof}[Points of proof]
By point relationship
\(
a_1=p+1-\#C(\FF_p)
\)
and
\(
\#C(\FF_{p^2})=p^2+1-(a_1^2-2a_2)
\)
can be \(\#C(\FF_p)\)、\(\#C(\FF_{p^2})\) only recycling \(L_p(T)\)；
like \(J(C)\) exist \(\QQ\), then for almost all good prime numbers \(p\) whose reduced Frobenius polynomial must be decomposed accordingly, so irreducibility occurs at any good prime number \(L_p(T)\) i.e. exclude \(\QQ\)-break down.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_discriminant\_ridge\_jacobian\_frobenius\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Absolute singleness and terminal homomorphic ring rigidity of discriminant quadratic ridge curve Jacobian varieties]\label{con:xi-terminal-zm-discriminant-ridge-jacobian-absolute-simplicity-endomorphism}
Still a conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes} of genus \(2\) curve \(C\) and its Jacobian variety \(J=\operatorname{Jac}(C)\)。
but:
\begin{enumerate}
  \item \(J\) exist \(\overline{\QQ}\) is absolutely simple.
  \item \[
  \mathrm{End}_{\overline{\QQ}}(J)\ \cong\ \ZZ,
  \qquad\text{Equivalently}\qquad
  \mathrm{End}^0_{\overline{\QQ}}(J):=\mathrm{End}_{\overline{\QQ}}(J)\otimes_{\ZZ}\QQ\ \cong\ \QQ.
  \]
  \item geometry N\'eron--Severi group satisfaction
  \[
  \mathrm{NS}_{\overline{\QQ}}(J)\ \cong\ \ZZ,
  \]
  And generated by the main polarization (theta except subclasses).
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
Find good reduced primes \(p=17,29\)(in conclusion \ref{con:xi-terminal-zm-leyang-discriminant-ridge-polydisc-badprimes}）。
remember \(P_p(x)\) for \(J\) exist \(p\) Frobenius characteristic polynomial (degree \(4\) of Weil polynomial), and let \(\pi_p\) for any of its roots.
script \texttt{scripts/exp\_fold\_zm\_discriminant\_ridge\_jacobian\_endomorphism\_ring\_audit.py} The following reproducible certificate is given:
\input{sections/generated/eq_fold_zm_discriminant_ridge_jacobian_endomorphism_ring_audit}
in \(P_{17},P_{29}\) exist \(\QQ\), and their Galois groups are all dihedral groups \(D_4\) (order \(8\)）。
Known by irreducibility \(J\) exist \(p=17,29\) at \(J_p\) are all simple Abelian surfaces;
Theory by Honda--Tate (here \(\dim J=2\) and \(\deg P_p=4\))have to
\[
\mathrm{End}^0_{\overline{\FF}_{p}}(J_p)\ \cong\ \QQ(\pi_p)\qquad (p=17,29),
\]
for the quadruple CM domain.
\par
further order
\(
u_p:=\pi_p+p/\pi_p
\)。
but \(u_p\)’s minimal polynomial is \(u^2-a_1u+(a_2-2p)=0\)(in \(a_1,a_2\) Depend on \(P_p\) is given), thus \(\QQ(u_{17})=\QQ(\sqrt{33})\)、\(\QQ(u_{29})=\QQ(\sqrt{73})\)；
The two are different from each other.
And because of \(P_{17},P_{29}\) are all Galois closures \(D_4\), the corresponding four CM domains each contain only a unique quadratic subdomain, and this subdomain is its maximum real subdomain;
Therefore \(\QQ(\pi_{17})\cap \QQ(\pi_{29})=\QQ\)。
\par
By the injectivity of well-reduced homomorphic specialization maps (Tate--Faltings-type results),\(\mathrm{End}^0_{\overline{\QQ}}(J)\) can be embedded into each \(\mathrm{End}^0_{\overline{\FF}_{p}}(J_p)\) middle;
then
\[
\mathrm{End}^0_{\overline{\QQ}}(J)\ \hookrightarrow\ \QQ(\pi_{17})\cap \QQ(\pi_{29})=\QQ,
\]
thereby \(\mathrm{End}^0_{\overline{\QQ}}(J)=\QQ\),Right now \(\mathrm{End}_{\overline{\QQ}}(J)\cong\ZZ\)。
This terminal homomorphic algebra is nothing more than trivial idempotent elements, so \(J\) exist \(\overline{\QQ}\) on the absolute order.
at last,\(\mathrm{End}^0_{\overline{\QQ}}(J)=\QQ\), the Rosati invariant part is \(\QQ\),thereby \(\mathrm{NS}_{\overline{\QQ}}(J)\) has a rank of \(1\)；
and \(J\) as a Jacobian carries the natural dominant polarization, so \(\mathrm{NS}_{\overline{\QQ}}(J)\cong\ZZ\) is generated by subclassing theta. Certification completed.
\end{proof}

\begin{conclusion}[\texorpdfstring{$S_4$}{S4} Representation decomposition of properties, subgroup towers and holomorphic differentials of Galois closure curves:\texorpdfstring{$g=16$}{g=16} and \texorpdfstring{$H^{0}(\Omega^{1})$}{H0(Omega1)} 's irreducible spectrum]\label{con:xi-terminal-zm-s4-galois-closure-genus-tower-holomorphic-differentials}
set up \(\widetilde{X}\to\mathbb{P}^{1}_{y}\) is covered four times \(\Pi(\lambda,y)=0\) of \(S_{4}\) Galois closure (\(|S_{4}|=24\)）。
Its divergence cycle type is: in \(y=\infty\) is in the inertia stage \(4\),exist \(y=0,1\) and \(P_{\mathrm{LY}}(y)=0\) The inertia orders at the three roots are all \(2\)。
Obtain closed-form generic numbers from Riemann--Hurwitz formula
\[
g(\widetilde{X})=16.
\]
And there are three structurally rigid middleman curves
\[
\widetilde{X}/A_{4}=C\ \ (g=2),\qquad \widetilde{X}/V_{4}=C_{6}\ \ (g=4),\qquad \widetilde{X}/S_{3}=E\ \ (g=1),
\]
in \(C\) The function domain is \(\QQ\bigl(y,\sqrt{-y(y-1)P_{\mathrm{LY}}(y)}\bigr)\), which can be taken as the discriminant quadratic ridge curve of the above formula;
and \(E\) is the conclusion \ref{con:xi-terminal-zm-elliptic-normalization} elliptic normalized curve.
Since all inertial orders are equal to \(3\) are relatively prime, inducing an undiverged cycle covering three times
\[
\pi:\ C_{6}\longrightarrow C,\qquad \mathrm{Gal}(C_{6}/C)\cong C_{3}.
\]
further,\(\widetilde{X}\) is a holomorphic differential space as \(S_{4}\) - means with complete explicit decomposition
\[
H^{0}(\widetilde{X},\Omega^{1})\ \cong\ 2\cdot \mathrm{sgn}\ \oplus\ V_{2}\ \oplus\ V_{3}\ \oplus\ 3V_{3}',
\]
in \(\mathrm{sgn}\) is a symbolic representation,\(V_{2}\) is the only two-dimensional irreducible representation,\(V_{3},V_{3}'\) are two three-dimensional irreducible representations.
So Jacobi clusters in \(\QQ\) induces decomposition (to \(\QQ\) - homologous meaning)
\[
J(\widetilde{X})\ \sim\ A_{\mathrm{sgn}}\times A_{2}\times A_{3}\times A_{3'},
\]
Their dimensions are respectively \((2,2,3,9)\) (total dimensions \(16\)）。
and \(\mathrm{sgn}\)-equal factor satisfies \(A_{\mathrm{sgn}}\sim J(C)\),at the same time \(J(C_{6})\sim J(C)\times A_{2}\)。
Thus "the simple Abelian surface of the discriminant quadratic ridge" is structurally embedded in \(S_{4}\) in the representation spectrum of Galois closure, it is given that from the Lee--Yang discriminant kernel to the high attribute \(S_{4}\) global arithmetic channel for dynamic closures.
\end{conclusion}
\begin{proof}[Points of proof]
The genus number consists of
\(
g=1+\frac{|S_4|}{2}\bigl(-2+\sum(1-1/e_i)\bigr)
\)
Substitute directly into the inertial order set \(e_\infty=4\), the remaining five points \(e_i=2\) get.
The function domain of the middleman is given by \(S_4\) of the standard subgroup lattice (\(A_4\lhd S_4\)、\(V_4\lhd A_4\)) and the uniqueness of the quadratic subfield of the discriminant are given, and \(C_6\to C\) is determined by the inertia order and \(3\) coprime derivation.
Expressing the decomposition can be expressed by the equation \(\dim H^{0}(\widetilde{X},\Omega^{1})^{H}=g(\widetilde{X}/H)\)(right \(H=S_4,A_4,V_4,S_3\)) combine \(S_4\) role table solves for irreducible multiplicities and is therefore completely rigid.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_s4\_galois\_closure\_representation\_audit.py}. Certification completed.
\end{proof}

