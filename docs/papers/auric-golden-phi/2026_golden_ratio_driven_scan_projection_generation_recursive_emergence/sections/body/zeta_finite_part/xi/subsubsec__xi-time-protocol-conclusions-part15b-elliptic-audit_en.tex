% (User input window)

\paragraph{Auditable derivation skeleton of ellipse unification: double point Latt\`es, quadratic expansion discriminant and quartic spectral splitting domain}
in conclusion \ref{con:xi-terminal-zm-order4-rational}--\ref{con:xi-terminal-zm-discriminant-leyang} The given quartic spectral equation, discriminant decomposition and domain trace implementation,
At the function domain level, the square free part of the discriminant can be \(37\) of elliptic curve coverage.
The following identities can be found in \(\QQ[\lambda]\)、\(\QQ(\lambda)[Y]\) and \(\QQ(y)[\lambda]\), check item by item;
Each derivation step relies only on explicit algebraic identities and finite enumerations, and does not rely on any untraceable external simplification procedures.

\begin{conclusion}[Double point Latt\`es self-mapping and critical sets \(4\)-division certificate]\label{con:xi-terminal-zm-lattes-4division-certificate}
make
\[
\mathcal{E}:\quad Y^{2}=\lambda^{3}-\lambda+\tfrac14
\]
is an elliptic curve of short Weierstrass form, and let \(\Phi:\mathbb{P}^{1}\dashrightarrow\mathbb{P}^{1}\) represents mapping by multiple points \([2]:\mathcal{E}\to\mathcal{E}\) exist \(x\)-Induction on coordinates (Latt\`es) self-mapping:
\[
\Phi(\lambda):=x([2]P)\qquad(P=(\lambda,Y)\in\mathcal{E}).
\]
then there is a strictly closed form
\[
\boxed{\;
\Phi(\lambda)=\frac{\lambda^{4}+2\lambda^{2}-2\lambda+1}{4\lambda^{3}-4\lambda+1}\;}
\]
and satisfy the following auditable structure:
\begin{enumerate}
  \item denominator \(4\lambda^{3}-4\lambda+1\) exactly \(\mathcal{E}\) of \(2\)-division polynomial (equivalent to \(Y=0\), and
  \(
  4Y^{2}=4\lambda^{3}-4\lambda+1
  \)。
  \The critical equation of item is in \(\QQ[\lambda]\) can be reduced to a sixth degree integer:
  \[
  \Phi'(\lambda)=0\quad\Longleftrightarrow\quad
  2\lambda^{6}-10\lambda^{4}+10\lambda^{3}-10\lambda^{2}+2\lambda+1=0.
  \]
  \item order \(\psi_{4}(\lambda,Y)\) for \(\mathcal{E}\) of \(4\)-division polynomial, then there is an identity
  \[
  \boxed{\;
  \frac{\psi_{4}(\lambda,Y)}{2Y}
  =2\lambda^{6}-10\lambda^{4}+10\lambda^{3}-10\lambda^{2}+2\lambda+1\;}
  \]
  thereby
  \(
  \mathrm{Crit}(\Phi)=x\bigl(\mathcal{E}[4]\setminus\mathcal{E}[2]\bigr)
  \)
  (exist \(\mathbb{P}^{1}\) The weight calculated on is \(2\deg\Phi-2=6\)）；
  And its critical value set is
  \[
  \Phi\!\bigl(\mathrm{Crit}(\Phi)\bigr)
  =x\bigl([2](\mathcal{E}[4]\setminus\mathcal{E}[2])\bigr)
  =x\bigl(\mathcal{E}[2]\setminus\{O\}\bigr),
  \]
  That is the denominator \(4\lambda^{3}-4\lambda+1\) of three.
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
For the short Weierstrass form \(Y^{2}=\lambda^{3}+a\lambda+b\), tangent slope
\(
m=\frac{3\lambda^{2}+a}{2Y}
\)
satisfy \(x([2]P)=m^{2}-2\lambda\)。
Will \(m^{2}\) expand and use \(Y^{2}=\lambda^{3}+a\lambda+b\) Eliminate the denominator to get the standard closed form
\[
x([2]P)=\frac{(3\lambda^{2}+a)^{2}-8\lambda(\lambda^{3}+a\lambda+b)}{4(\lambda^{3}+a\lambda+b)}
=\frac{\lambda^{4}-2a\lambda^{2}-8b\lambda+a^{2}}{4\lambda^{3}+4a\lambda+4b}.
\]
right \(a=-1,b=\tfrac14\) and you will get the following \(\Phi\)。
remember
\(
N(\lambda)=\lambda^{4}+2\lambda^{2}-2\lambda+1
\),
\(
D(\lambda)=4\lambda^{3}-4\lambda+1
\),
but \(\Phi=N/D\) and
\(
\Phi'=(N'D-ND')/D^{2}
\)；
Directly simplify to get
\[
N'D-ND'
=2\bigl(2\lambda^{6}-10\lambda^{4}+10\lambda^{3}-10\lambda^{2}+2\lambda+1\bigr),
\]
The critical equation is thus equivalent to the sixth degree integer shown.
at last,\(4\)-division standard identities for polynomials
\[
\psi_{4}(\lambda,Y)=4Y\bigl(\lambda^{6}+5a\lambda^{4}+20b\lambda^{3}-5a^{2}\lambda^{2}-4ab\lambda-8b^{2}-a^{3}\bigr)
\]
Substitute \(a=-1,b=\tfrac14\) to get
\(
\psi_{4}(\lambda,Y)/(2Y)=2\lambda^{6}-10\lambda^{4}+10\lambda^{3}-10\lambda^{2}+2\lambda+1
\)。
because \(\psi_{4}=0\) portray \(\mathcal{E}[4]\setminus\{O\}\),and \(2Y=0\) portray \(\mathcal{E}[2]\setminus\{O\}\)，
Therefore \(\psi_{4}/(2Y)=0\) is equivalent to \(P\in\mathcal{E}[4]\setminus\mathcal{E}[2]\), thus getting \(\mathrm{Crit}(\Phi)\) of \(4\)-division certificate;
The critical value statement comes from \([2](\mathcal{E}[4])=\mathcal{E}[2]\)。
The above identities can all be written by the script \texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py} Review item by item. Certification completed.
\end{proof}

\begin{conclusion}[\texorpdfstring{$\Phi$}{Phi} Complete square decomposition of the critical fiber on the bisection point domain: the pairing law of the quarter-point norm formula and the critical image]\label{con:xi-terminal-zm-lattes-critical-fiber-square-splitting}
make
\[
D(X):=4X^{3}-4X+1,
\]
and take \(\alpha\) for \(D\) of any root (then \((\alpha,0)\in\mathcal{E}[2]\setminus\{O\}\)）。
definition
\[
q_{\alpha}(X):=X^{2}-2\alpha X+(1-2\alpha^{2})\in \QQ(\alpha)[X].
\]
then in \(\QQ(\alpha)[X]\) exists a strict identity in
\[
\bigl(X^{4}+2X^{2}-2X+1\bigr)-\alpha\bigl(4X^{3}-4X+1\bigr)=q_{\alpha}(X)^{2}.
\]
thereby \(\Phi^{-1}(\alpha)\subset\mathbb{P}^{1}\) Chaiyu \(q_{\alpha}\) are given by two roots, and both roots are critical points (at \(\alpha\) are all local divergence indicators on \(2\)）；
that is \(\alpha\)’s full front image is “two critical points, each \(2\)" completely different fibers.
\par
Further, note the critical sixth degree polynomial
\[
C_{\Phi}(X):=2X^{6}-10X^{4}+10X^{3}-10X^{2}+2X+1.
\]
then in \(\QQ[X]\) satisfies the norm identity in
\[
\boxed{\;
C_{\Phi}(X)=2\,\operatorname{Norm}_{\QQ(\alpha)/\QQ}\!\bigl(q_{\alpha}(X)\bigr)\;}
\]
(The right end is for any \(\alpha\) options are true).
therefore \(C_{\Phi}(X)/2\) exist \(\overline{\QQ}\) is decomposed into three groups of quadratic factors, each group corresponding to a non-trivial bisection point \((\alpha,0)\)；
And the two roots of this group are exactly reflected to \(\alpha\) of the pair of critical points.
Equivalently,\(C_{\Phi}(X)/2\) and conclusion \ref{con:xi-terminal-zm-lattes-4division-certificate} of \(\psi_{4}/(2Y)\) consistent,
thereby \(\mathrm{Crit}(\Phi)\) and \(\mathcal{E}[4]\setminus\mathcal{E}[2]\) exist \(\pm 1\) business strictly corresponds.
\end{conclusion}
\begin{proof}[Points of proof]
Write \(\Phi=N/D\),in
\(
N(X)=X^{4}+2X^{2}-2X+1
\)
and
\(
D(X)=4X^{3}-4X+1
\)。
to any \(\alpha\) satisfy \(D(\alpha)=0\),Will \(q_{\alpha}(X)^{2}\) Expand and compare the \(\alpha^{3}\) using the identity \(4\alpha^{3}=4\alpha-1\)(Right now \(D(\alpha)=0\)) elimination, that is, the square decomposition is obtained.
Depend on \(N(X)-\alpha D(X)=q_{\alpha}(X)^{2}\) can be known \(\Phi(X)-\alpha=(q_{\alpha}(X)/D(X))^{2}\), so \(q_{\alpha}\) are all zeros \(\Phi\) exist \(\alpha\) on the double front image point, which is the critical point and the divergence index is \(2\)。
\par
The norm identity comes from the cubic expansion of \(\QQ(\alpha)/\QQ\) take the norm:
\(
\operatorname{Norm}_{\QQ(\alpha)/\QQ}(q_{\alpha}(X))=\prod_{\alpha'}q_{\alpha'}(X)
\),
where the product traverses \(D(\alpha')=0\) of three.
Think of this product as being about the parameters \(\alpha\) can be found in \(\QQ[X]\) to get a sixth degree integer;
and conclusion \ref{con:xi-terminal-zm-lattes-4division-certificate} critical six times \(C_{\Phi}(X)\) Align and check the first term, and get the coefficient constant as \(2\)。
The above equation can be expressed by the script \texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py} Review item by item. Certification completed.
\end{proof}

\begin{conclusion}[Function domain realization of quadratic expansion discriminant of weight coordinates and quartic spectral equation]\label{con:xi-terminal-zm-elliptic-quadratic-discriminant}
on the curve \(\mathcal{E}\) defines the weight function on
\[
y:=\lambda^{2}+Y-\tfrac12\ \in\QQ(\mathcal{E}).
\]
but \(y\) exist \(\QQ(\lambda)\) satisfies the quadratic relation on
\[
\boxed{\;
y^{2}-(2\lambda^{2}-1)y+\lambda(\lambda-1)^{2}(\lambda+1)=0\;}
\]
Its discriminant is
\[
\boxed{\;
(2\lambda^{2}-1)^{2}-4\lambda(\lambda-1)^{2}(\lambda+1)=4\lambda^{3}-4\lambda+1=4Y^{2}\;}
\]
thereby \(\QQ(\mathcal{E})=\QQ(\lambda,Y)\) as \(\QQ(\lambda)\), its discriminant is strictly locked in \(2\)-division polynomial \(4\lambda^{3}-4\lambda+1\) superior.
The conjugate branch is obtained from the Vedic formula of the quadratic relation
\[
y^{\iota}:=(2\lambda^{2}-1)-y=\lambda^{2}-Y-\tfrac12,
\qquad
yy^{\iota}=\lambda(\lambda-1)^{2}(\lambda+1).
\]
therefore \(\QQ(\lambda,Y)=\QQ(\lambda,y)\)。
\par
Think of the above equation as about \(\lambda\) and expand the polynomial of \(\lambda(\lambda-1)^{2}(\lambda+1)=\lambda^{4}-\lambda^{3}-\lambda^{2}+\lambda\), the equivalent quartic relation can be obtained
\[
\boxed{\;
\Pi(\lambda,y)=\lambda^{4}-\lambda^{3}-(2y+1)\lambda^{2}+\lambda+y(y+1)=0\;}
\]
and conclusion \ref{con:xi-terminal-zm-order4-rational} The characteristic equations extracted by linear implementation in are completely consistent.
therefore,\(y\) gives the number of \(4\) coverage \(y:\mathcal{E}\to\mathbb{P}^{1}\), and at infinity \(O\) has a unique pole at \(4\)。
\end{conclusion}
\begin{proof}[Points of proof]
by definition \(Y=y-\lambda^{2}+\tfrac12\), substitute \(\mathcal{E}\) equation and in \(\QQ(\lambda)[y]\), we get the quadratic minimum polynomial.
The discriminant identity is obtained by direct simplification and is related to \(4Y^{2}=4\lambda^{3}-4\lambda+1\) matches.
The conjugate branches and product identities are given directly by Vedic formulas.
Expand the constant term in the quadratic equation as \(\lambda^{4}-\lambda^{3}-\lambda^{2}+\lambda\) and shifting terms, we get \(\Pi(\lambda,y)=0\)。
The extreme order comes from \(y=\lambda^{2}+Y-\tfrac12\) exist \(O\) reason \(\lambda^{2}\) dominant (\(\lambda\) exist \(O\) where the pole order is \(2\)）。
scriptable \texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py} Verify. Certification completed.
\end{proof}

\input{subsubsec__xi-time-protocol-conclusions-part15b-elliptic-audit-y-doubling-affine-cocycle}

\begin{conclusion}[The triviality of torsion groups and the infinite order of rational points: Finite enumeration certificate of good reduction]\label{con:xi-terminal-zm-elliptic-torsion-trivial}
to curve \(\mathcal{E}\) rounding transformation
\[
x':=4\lambda,\qquad y':=8Y,
\]
Get the whole model
\[
\mathcal{E}':\quad {y'}^{2}={x'}^{3}-16x'+16,
\]
Its discriminant is \(\Delta(\mathcal{E}')=2^{12}\cdot 37\)。
Therefore in \(p\in\{3,5\}\) is well reduced, and there is an injective reduction homomorphism of the torsion subgroup
\[
\mathcal{E}(\QQ)_{\mathrm{tors}}\hookrightarrow \mathcal{E}'(\FF_p).
\]
Finite enumeration gives
\[
\#\mathcal{E}'(\FF_{3})=7,\qquad \#\mathcal{E}'(\FF_{5})=8,
\]
thereby \(|\mathcal{E}(\QQ)_{\mathrm{tors}}|\mid\gcd(7,8)=1\),Right now \(\mathcal{E}(\QQ)_{\mathrm{tors}}=\{O\}\)。
In particular, reasonable points \(P=(2,-\tfrac52)\in\mathcal{E}(\QQ)\) is an infinite order point.
\end{conclusion}
\begin{proof}[Points of proof]
Variable substitution gives \(\mathcal{E}'\); the discriminant can be directly calculated according to the short Weierstrass formula \(\Delta(\mathcal{E}')=2^{12}\cdot37\), so \(p=3,5\) good reduction.
The standard conclusion is that the reduced homomorphism at a well reduced place is injective on the torsion subgroup.
right \(p=3,5\) can be directly obtained by finite enumeration; deduced by injectivity \(|\mathcal{E}(\QQ)_{\mathrm{tors}}|\) simultaneously divide \(7\) and \(8\), thus \(1\)。
because \(P\neq O\), so \(P\) is an infinite order. Certification completed.
\end{proof}

\begin{conclusion}[Fourth power denominator scale of rational point orbits and weight coordinates]\label{con:xi-terminal-zm-elliptic-denominator-fourth-power}
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-quadratic-discriminant} weight function \(y=\lambda^{2}+Y-\tfrac12\)。
Take a rational point \(P=(2,-\tfrac52)\in\mathcal{E}(\QQ)\)。
for any integer \(n\ge 1\),Will \(x\)-coordinates written as reduced fractions
\[
\lambda(nP)=\frac{u_n}{v_n^{2}}\qquad(\gcd(u_n,v_n)=1,\ v_n>0).
\]
Then the weight coordinate satisfies the strict denominator law
\[
\mathrm{den}\bigl(y(nP)\bigr)=v_n^{4}.
\]
\end{conclusion}
\begin{proof}[Points of proof]
Take any \(Q=(\lambda,Y)\in\mathcal{E}(\QQ)\) and make it \(x\)-coordinates written as reduced fractions
\(
\lambda=\frac{u}{v^{2}}
\)（\(\gcd(u,v)=1,\ v>0\)）。
From the curve equation
\(
4Y^{2}=4\lambda^{3}-4\lambda+1
\)
get
\[
4Y^{2}=\frac{4u^{3}-4uv^{4}+v^{6}}{v^{6}},
\qquad
Y=\frac{w}{2v^{3}},
\qquad
w^{2}=4u^{3}-4uv^{4}+v^{6}
\]
(in \(w\in\ZZ\)）。
then
\[
y(Q)=\lambda^{2}+Y-\tfrac12
=\frac{u^{2}}{v^{4}}+\frac{w}{2v^{3}}-\tfrac12
=\frac{2u^{2}+wv-v^{4}}{2v^{4}}.
\]
The proband has an even number of molecules: if \(v\) is an odd number, then \(v^{6}\equiv 1\ (\mathrm{mod}\ 4)\) and \(4u^{3}-4uv^{4}\equiv 0\ (\mathrm{mod}\ 4)\)，
Therefore \(w^{2}\equiv 1\ (\mathrm{mod}\ 4)\) thus \(w\) is an odd number; at this time \(wv\) and \(v^{4}\) are all odd numbers, and the difference is an even number, so \(2u^{2}+wv-v^{4}\) is an even number.
like \(v\) is an even number, then \(w^{2}\equiv 0\ (\mathrm{mod}\ 4)\) thus \(w\) is an even number, it also means that the numerator is an even number.
make \(2u^{2}+wv-v^{4}=2n\in\ZZ\),but \(y(Q)=n/v^{4}\)。
\par
Reconfirm \(\gcd(n,v)=1\)。
like \(p\) is an odd prime number and \(p\mid v\),but \(wv\equiv v^{4}\equiv 0\ (\mathrm{mod}\ p)\),thereby
\(
2n\equiv 2u^{2}\not\equiv 0\ (\mathrm{mod}\ p)
\)；
therefore \(p\nmid n\)。
like \(2\mid v\),but \(u\) is an odd number; and from the above formula in module \(8\) There are
\(
w^{2}\equiv 4u^{3}\equiv 4\ (\mathrm{mod}\ 8)
\), so \(w=2k\) and \(k\) is an odd number.
then \(wv\equiv 0\ (\mathrm{mod}\ 4)\)、\(v^{4}\equiv 0\ (\mathrm{mod}\ 4)\),and \(2u^{2}\equiv 2\ (\mathrm{mod}\ 4)\),thereby \(2n\equiv 2\ (\mathrm{mod}\ 4)\),Right now \(n\) is an odd number.
To sum up \(\gcd(n,v)=1\), so \(\mathrm{den}(y(Q))=v^{4}\)。
Pick \(Q=nP\) is the conclusion. Certification completed.
\end{proof}

\begin{conclusion}[Representation of domain traces and clear denominator integer identities: analytical obstacles are compressed into \(yP_{\mathrm{LY}}(y)=0\)]\label{con:xi-terminal-zm-field-trace-denominator-compression}
make \(K:=\QQ(y)(\lambda)\) on the grounds \(\Pi(\lambda,y)=0\) the number of times defined \(4\) expand, and let
\[
s_m(y):=\Tr_{K/\QQ(y)}(\lambda^{m})=\sum_{i=1}^{4}\lambda_i(y)^{m}
\]
is the power sum (\(\lambda_i(y)\) for \(\Pi(\lambda,y)=0\) of the four roots).
Then there is a unique cubic polynomial
\(
u(\lambda)=a_0(y)+a_1(y)\lambda+a_2(y)\lambda^{2}+a_3(y)\lambda^{3}\in K
\)
(coefficient \(a_j(y)\in\QQ(y)\)), such that for all \(m\ge 0\) establishes domain trace representation
\[
\boxed{\;
Z_m(y)=\Tr_{K/\QQ(y)}\bigl(u(\lambda)\lambda^{m}\bigr)\;}
\]
and \(a_j(y)\) all possible poles lie only in \(y=0\) and \(P_{\mathrm{LY}}(y)=0\)。
Equivalently, there exists a polynomial with integer coefficients \(A_0,A_1,A_2,A_3\in\ZZ[y]\) applies to all \(m\ge 0\) Establish clear denominator identities
\[
\boxed{\;
yP_{\mathrm{LY}}(y)\,Z_m(y)
=A_0(y)s_m(y)+A_1(y)s_{m+1}(y)+A_2(y)s_{m+2}(y)+A_3(y)s_{m+3}(y)\;}
\]
thereby \(Z_m(y)\) are collapsed into the same set \(yP_{\mathrm{LY}}(y)=0\)。
\par
Under the above notation, explicit coefficients can be taken
\[
\begin{aligned}
a_0(y)&=-\frac{19y^{3}+75y^{2}+42y+8}{P_{\mathrm{LY}}(y)},\\
a_1(y)&=\frac{2\,(40y^{4}-10y^{3}-41y^{2}-21y-4)}{y\,P_{\mathrm{LY}}(y)},\\
a_2(y)&=\frac{3\,(29y^{2}+16y+3)}{P_{\mathrm{LY}}(y)},\\
a_3(y)&=\frac{-16y^{3}+49y^{2}+31y+8}{y\,P_{\mathrm{LY}}(y)},
\end{aligned}
\qquad
u(\lambda)=a_0(y)+a_1(y)\lambda+a_2(y)\lambda^{2}+a_3(y)\lambda^{3},
\]
and correspondingly
\[
\begin{aligned}
A_0(y)&=-y\,(19y^{3}+75y^{2}+42y+8),\\
A_1(y)&=2(2y+1)\,(20y^{3}-15y^{2}-13y-4),\\
A_2(y)&=3y\,(29y^{2}+16y+3),\\
A_3(y)&=-16y^{3}+49y^{2}+31y+8.
\end{aligned}
\]
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-order4-rational} The generating function of
\(
\sum_{m\ge 0}Z_m(y)t^m=N(t)/D(t)
\),in
\(
N(t)=1+yt-yt^{2}-y^{2}t^{3}
\),and
\[
D(t)=1-t-(2y+1)t^{2}+t^{3}+y(y+1)t^{4}=t^{4}\Pi(1/t,y).
\]
make \(\lambda_1,\dots,\lambda_4\) for \(\Pi(\lambda,y)=0\), then
\(
D(t)=\prod_{i=1}^{4}(1-\lambda_i t)
\), thus the partial fraction decomposition gives \(Z_m(y)=\sum_i c_i(y)\lambda_i(y)^m\)。
exist \(t=1/\lambda_i\) satisfies
\[
\mathrm{Res}_{t=1/\lambda_i}\frac{N(t)}{D(t)}=\frac{N(1/\lambda_i)}{D'(1/\lambda_i)},
\qquad
D'(1/\lambda_i)=-\frac{\partial_\lambda\Pi(\lambda_i,y)}{\lambda_i^{2}},
\]
And if write \(N(t)/D(t)=\sum_i \frac{c_i(y)}{1-\lambda_i t}\),but
\(
\mathrm{Res}_{t=1/\lambda_i}\frac{c_i}{1-\lambda_i t}=-c_i/\lambda_i
\)；
Therefore
\[
c_i(y)=\frac{\lambda_i^{3}N(1/\lambda_i)}{\partial_\lambda\Pi(\lambda_i,y)}.
\]
Therefore in the expansion domain \(K=\QQ(y)(\lambda)\) is preferable
\[
u(\lambda)\equiv \frac{\lambda^{3}N(1/\lambda)}{\partial_\lambda\Pi(\lambda,y)}
=\frac{\lambda^{3}+y\lambda^{2}-y\lambda-y^{2}}{\partial_\lambda\Pi(\lambda,y)}
\pmod{\Pi(\lambda,y)},
\]
put it in \(K\) middle press base \(1,\lambda,\lambda^{2},\lambda^{3}\) The only way to get the conclusion is to get the \(a_0,\dots,a_3\), and by
\(
\Tr_{K/\QQ(y)}(u(\lambda)\lambda^{m})=\sum_i u(\lambda_i)\lambda_i^{m}
\)
Obtain the domain trace representation.
by explicit \(a_j(y)\) It can be seen that its denominator only contains \(y\) and \(P_{\mathrm{LY}}(y)\)；
multiply by \(yP_{\mathrm{LY}}(y)\) and the coefficients are promoted to integer coefficient polynomials \(A_0,\dots,A_3\), and give the clear denominator identity.
The above calculation can be done by script \texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py} Review. Certification completed.
\end{proof}

\begin{conclusion}[Identity of cubic decomposition and discriminant: closed form certificate of resolvent cubic]\label{con:xi-terminal-zm-resolvent-cubic-discriminant-identity}
definition about \(z\) cubic polynomial
\[
R(z,y):=z^{3}+(1+2y)z^{2}-(1+4y+4y^{2})z-(1+5y+13y^{2}+8y^{3})\in\ZZ[y][z].
\]
Then its discriminant satisfies the strict identity
\[
\boxed{\;
\mathrm{Disc}_{z}\bigl(R(\cdot,y)\bigr)
=-y(y-1)P_{\mathrm{LY}}(y)
=\mathrm{Disc}_{\lambda}\bigl(\Pi(\cdot,y)\bigr)\;}
\]
Therefore, the same divergence divisor simultaneously governs the geometric divergence of the quartic spectral equation and its discriminant quadratic resolution subfield.
\end{conclusion}
\begin{proof}[Points of proof]
right \(R(z,y)\) calculate \(z\)-discriminant union in \(\ZZ[y]\), the identity can be obtained by factoring;
and conclusion \ref{con:xi-terminal-zm-discriminant-leyang} of \(\mathrm{Disc}_{\lambda}(\Pi)\) alignment gives the second equal sign.
scriptable \texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py} Verify. Certification completed.
\end{proof}

\begin{conclusion}[The general Galois group of the quartic spectral equation is \(S_{4}\)]\label{con:xi-terminal-zm-galois-s4-from-ramification}
Will \(\Pi(\lambda,y)\) is regarded as \(\QQ(y)\) on \(\lambda\), and let \(R(z,y)\) is the conclusion \ref{con:xi-terminal-zm-resolvent-cubic-discriminant-identity} of resolvent cubic.
then four expansions \(\QQ(y)(\lambda)/\QQ(y)\) is isomorphic to the Galois group \(S_{4}\)。
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-resolvent-cubic-discriminant-identity}，
\(
\mathrm{Disc}_{\lambda}(\Pi)=-y(y-1)P_{\mathrm{LY}}(y)
\)；
intrinsic factor \(y\) The zeroth order at \(1\), so in \(\QQ(y)\) Central Africa Squared.
On the other hand, if \(R(z,y)\) exist \(\QQ(y)\), then as a cubic polynomial it must have roots \(z=f(y)\in\QQ(y)\)。
Pick \(y=2\) get rational roots \(z=f(2)\in\QQ\), which is related to
\[
R(z,2)=z^{3}+5z^{2}-25z-127
\]
No rational roots (rational roots can only be \(\pm1,\pm127\), each substitution is non-zero) contradiction;
Therefore \(R(z,y)\) exist \(\QQ(y)\) is irreducible.
The classic judgment of quartic polynomials points out that its resolvent cubic is irreducible in the base domain and only if the Galois group is \(A_{4}\) or \(S_{4}\)；
Then eliminate it by the discriminant non-square \(A_{4}\),thereby \(\mathrm{Gal}(\Pi/\QQ(y))\cong S_{4}\). Certification completed.
\end{proof}

\begin{conclusion}[Dual role of the same discriminant kernel: geometric divergence divisor and quadratic resolution subfield]\label{con:xi-terminal-zm-discriminant-dual-role}
See \(\Pi(\lambda,y)=0\) for \(y\)-A four-layer covered affine model on a straight line, and note
\(
\Delta(y):=\mathrm{Disc}_{\lambda}(\Pi(\cdot,y))=-y(y-1)P_{\mathrm{LY}}(y)
\)。
but:
\begin{enumerate}
  \item The divergence divisor of this coverage is exactly caused by \(\Delta(y)=0\) given;
  \item by conclusion \ref{con:xi-terminal-zm-galois-s4-from-ramification} of \(\mathrm{Gal}(\Pi/\QQ(y))\cong S_{4}\), the only quadratic resolution subdomain of its split domain is
  \[
  \QQ(y)\bigl(\sqrt{\Delta(y)}\bigr).
  \]
\end{enumerate}
Therefore, the same discriminant kernel \(\Delta(y)\) governs both geometric divergence and algebraic quadratic resolution.
\end{conclusion}
\begin{proof}[Points of proof]
The first point is given by the basic criterion that the divergence point is equivalent to the existence of multiple roots, that is, the discriminant is zero.
The second point is \(S_{4}\) standard structure for expansion: index \(2\) The only normal subgroup of \(A_{4}\), the corresponding secondary subdomain is given by \(\sqrt{\mathrm{Disc}}\) is given.
combine \(\Delta(y)=\mathrm{Disc}_{\lambda}(\Pi)\) is obtained. Certification completed.
\end{proof}

\begin{conclusion}[Total divergence portrait of weight mapping and elimination certificate of Lee--Yang cubic factors]\label{con:xi-terminal-zm-elliptic-ramification-portrait}
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-quadratic-discriminant} notation, and note the Lee--Yang cubic factors
\[
P_{\mathrm{LY}}(y):=256y^{3}+411y^{2}+165y+32\in\ZZ[y].
\]
then cover \(y:\mathcal{E}\to\mathbb{P}^{1}\) is completely explicit:
\begin{enumerate}
  \item \(O\) is the only pole and the pole order is \(4\),therefore \(y=\infty\) is the total divergence point, divergence index \(e_O=4\)。
  \The finite divergence point of item satisfies
  \[
  dy=0\quad\Longleftrightarrow\quad 4\lambda Y+3\lambda^{2}-1=0,
  \]
  and with \(\mathcal{E}\) is equivalent to
  \[
  (\lambda-1)(\lambda+1)\bigl(16\lambda^{3}-9\lambda^{2}+1\bigr)=0.
  \]
  Therefore, the finite divergence point consists of two rational points \((1,-\tfrac12)\)、\((-1,\tfrac12)\) and three conjugate points (whose \(\lambda\)-coordinates are \(16\lambda^{3}-9\lambda^{2}+1=0\), and they are all single-divergence (\(e=2\)）。
  \The set of finite divergence values ​​of item is exactly
  \[
  y(y-1)P_{\mathrm{LY}}(y)=0.
  \]
  in \(y=0\) and \(y=1\) respectively correspond to the above two rational points of disagreement; and \(P_{\mathrm{LY}}(y)=0\) corresponds to three conjugate divergence points.
  This cubic factor can be characterized by an explicit elimination certificate as
  \[
  \boxed{\;
  \mathrm{Res}_{\lambda}\!\Bigl(16\lambda^{3}-9\lambda^{2}+1,\ 4\lambda y-(4\lambda^{3}-3\lambda^{2}-2\lambda+1)\Bigr)
  =-64\,P_{\mathrm{LY}}(y)\;}
  \]
  where the second polynomial comes from the divergence constraint \(4\lambda Y+3\lambda^{2}-1=0\) the identity under \(4\lambda y=4\lambda^{3}-3\lambda^{2}-2\lambda+1\)。
\end{enumerate}
\par
Depend on \(\deg(y)=4\) and \(g(\mathcal{E})=1\), the total divergence must be \(8\)。
in \(O\) exist \(\infty\) Total divergence contribution above \(e_O-1=3\), the remaining five finite divergence values ​​must therefore be single divergence (each contribution \(1\)），
The Hurwitz divergence signature of this coverage is thus \((2,2,2,2,2,4)\)。
\par
Furthermore, two fibers with rational divergence values ​​are in \(\QQ\) fully explicit:
\[
y^{-1}(0)=\Bigl\{(0,\tfrac12),\ (-1,-\tfrac12),\ \underbrace{(1,-\tfrac12)}_{e=2}\Bigr\},
\qquad
y^{-1}(1)=\Bigl\{(2,-\tfrac52),\ (1,\tfrac12),\ \underbrace{(-1,\tfrac12)}_{e=2}\Bigr\},
\]
and
\(
y^{-1}(\infty)=\{\underbrace{O}_{e=4}\}
\)。
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(y=\lambda^{2}+Y-\tfrac12\) have to
\(
dy/d\lambda=2\lambda+(dY/d\lambda)
\)。
And in \(\mathcal{E}\) superior \(2Y(dY/d\lambda)=3\lambda^{2}-1\)，
Therefore \(dy=0\) is equivalent to \((3\lambda^{2}-1)/(2Y)=-2\lambda\),Right now \(4\lambda Y+3\lambda^{2}-1=0\)。
Will \(Y=(1-3\lambda^{2})/(4\lambda)\) Substitute \(\mathcal{E}\) and clear the denominator, we get
\(
(\lambda^{2}-1)(16\lambda^{3}-9\lambda^{2}+1)=0
\)。
Replace two reasonable points of disagreement \(y\) can get the divergence value \(0,1\)。
For cubic factors, use \(4\lambda y=4\lambda^{3}-3\lambda^{2}-2\lambda+1\) and \(16\lambda^{3}-9\lambda^{2}+1=0\) can be obtained by performing concluding elimination as shown \(\mathrm{Res}_{\lambda}\) Certificate.
scriptable \texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py} Verify. Certification completed.
\end{proof}

\input{subsubsec__xi-time-protocol-conclusions-part15b-elliptic-audit-leyang-resy-spectral-decomposition}

\begin{conclusion}[Discriminant locking and bad reduction coupling of Lee--Yang factors:\(37\)]\label{con:xi-terminal-zm-leyang-discriminant-bad-reduction-37}
right \(\mathcal{E}\) for translation \(Y\mapsto Y-\tfrac12\) to get the model
\(
Y^{2}+Y=\lambda^{3}-\lambda
\), the squared free part of its discriminant is \(37\), so the only bad reduced prime number is \(37\)。
In this context, the Lee--Yang cubic factor and the divergent geometry appear rigidly coupled:
\begin{enumerate}
  \item \(P_{\mathrm{LY}}(y)\) exist \(\QQ\) is irreducible on, and
  \[
  \mathrm{Disc}\bigl(P_{\mathrm{LY}}\bigr)=-3^{9}\cdot 31^{2}\cdot 37.
  \]
  \item order \(g(\lambda):=16\lambda^{3}-9\lambda^{2}+1\),but
  \[
  \mathrm{Disc}(g)=-2^{2}\cdot 3^{3}\cdot 37.
  \]
  \item \(g(\lambda)\) and \(2\)-division polynomial \(4\lambda^{3}-4\lambda+1\) in the characteristic \(37\) collapses at:
  \[
  \mathrm{Res}_{\lambda}\bigl(g,\,4\lambda^{3}-4\lambda+1\bigr)=4\cdot 37^{2},
  \]
  and in \(\FF_{37}\) have a common double root \(\lambda\equiv 5\)；
  In the weight coordinates, the collision point corresponds to \(y\equiv 6\)。
  \The item modulo-prime degeneration can also be tracked explicitly at small primes:
  \[
  P_{\mathrm{LY}}(y)\equiv (y-1)^{3}\ (\mathrm{mod}\ 3),\qquad
  P_{\mathrm{LY}}(y)\equiv 8\,(y-6)(y-12)^{2}\ (\mathrm{mod}\ 31),
  \]
  and
  \[
  P_{\mathrm{LY}}(y)\equiv -3\,(y-14)(y-6)^{2}\ (\mathrm{mod}\ 37).
  \]
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
Discriminant, Result and Module \(p\) factorization is calculated by elimination of finite integer coefficients, which can be found in \(\ZZ[y]\)、\(\ZZ[\lambda]\) and \((\ZZ/p\ZZ)[\cdot]\) can be completed directly.
The discrimination of bad reduced primes comes from the squared free part of the discriminant.
The above equation and modular decomposition can be expressed by script \texttt{scripts/exp\_fold\_zm\_elliptic\_audit.py} Review. Certification completed.
\end{proof}

\begin{conclusion}[Group trace identity of the divergence point: the sum of the three conjugate divergence points is a rational point]\label{con:xi-terminal-zm-branchpoint-group-trace}
make \(Q_0:=(1,-\tfrac12)\)、\(Q_0':=(-1,\tfrac12)\) is the conclusion \ref{con:xi-terminal-zm-elliptic-ramification-portrait} Two rational finite divergence points of ,
And order \(Q_1,Q_2,Q_3\in\mathcal{E}(\overline{\QQ})\) to satisfy the simultaneous equations
\[
16\lambda^{3}-9\lambda^{2}+1=0,\qquad 4\lambda Y+3\lambda^{2}-1=0
\]
three points (corresponding to \(P_{\mathrm{LY}}(y)=0\) of three limited divergence points).
Then there is a strict identity under the elliptic curve group law
\[
Q_0+Q_0'+Q_1+Q_2+Q_3=O,
\]
and
\[
\boxed{\;
Q_1+Q_2+Q_3
=-(Q_0+Q_0')
=\bigl(\tfrac14,-\tfrac18\bigr)\in\mathcal{E}(\QQ)\; }.
\]
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-elliptic-ramification-portrait}，
All finite divergence points are exactly \(\mathcal{E}\) and quadratic curve \(4\lambda Y+3\lambda^{2}-1=0\) and the intersection point with the infinity point \(O\) The total number of times given \(6\) cross factor.
Under the embedding of the Weierstrass plane, any quadratic curve with \(\mathcal{E}\) and the intersection divisor of \(6O\) is linearly equivalent, so its sum under the group law is zero;
So get \(Q_0+Q_0'+Q_1+Q_2+Q_3=O\)。
on the other hand,
\(
Q_0+Q_0'=(\tfrac14,\tfrac18)
\)
It is directly calculated from the chord tangent addition formula, and it is obtained by taking the negative \(Q_1+Q_2+Q_3=(\tfrac14,-\tfrac18)\). Certification completed.
\end{proof}

\begin{conclusion}[Tangential slope lock at finite bifurcation point:\(dy=0\) is equivalent to \(m=-2\lambda\)]\label{con:xi-terminal-zm-branch-slope-lock}
exist \(\mathcal{E}\) Note the slope of the tangent line above
\(
m:=(3\lambda^{2}-1)/(2Y)
\)。
Then for finite points (excluding \(O\)) has strict equivalence
\[
dy=0\quad\Longleftrightarrow\quad m=-2\lambda\quad\Longleftrightarrow\quad 4\lambda Y+3\lambda^{2}-1=0.
\]
Therefore, all finite divergence points are exactly \(\mathcal{E}\) and rational quadratic curve \(4\lambda Y+3\lambda^{2}-1=0\) and the conclusion \ref{con:xi-terminal-zm-elliptic-ramification-portrait} The decomposition is exactly the same.
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(y=\lambda^{2}+Y-\tfrac12\) have to
\(
dy/d\lambda=2\lambda+m
\)；
Therefore \(dy=0\) is equivalent to \(m=-2\lambda\)。
multiply by \(2Y\) i.e. get \(4\lambda Y+3\lambda^{2}-1=0\)。
and conclusion \ref{con:xi-terminal-zm-elliptic-ramification-portrait} The substitution of elimination is consistent. Certification completed.
\end{proof}

\input{subsubsec__xi-time-protocol-conclusions-part15b-elliptic-audit-weight-doubling}

