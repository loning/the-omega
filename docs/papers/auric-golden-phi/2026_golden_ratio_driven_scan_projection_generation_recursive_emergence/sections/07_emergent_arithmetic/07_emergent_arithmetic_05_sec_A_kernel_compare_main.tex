\subsection{核对比：零式 + q-场、carry-free 骨架与素数影子}\label{sec:A-kernel-compare-main}
本小节给出一个\textbf{可直接编程并以 BFS 构造状态图}的统一接口，用以对比黄金比例基 $\tau=\frac{1+\sqrt5}{2}$ 上三类典型并行归一化核（$9/13/21$-local；Algorithm I/II/III \cite{SvobodovaAAMPVIIIParallelAddition}，细节规则见 \cite{FrougnyHellerPelantovaSvobodovaParallelAlgorithmsSlides}）。

\subsubsection{统一生成模板：零式 + q-场}
固定 $\beta=\tau$。给定 Laurent 多项式
\[
Z(X)=\sum_{i=-h}^{k} b_i X^i,\qquad Z(\beta)=0,
\]
给定有界整数场 $q=(q_j)_{j\in\ZZ}$（取值于有限集合），定义一次“注入零式”的统一更新
\[
z'_j:=z_j-\sum_{i=-h}^{k} b_i\,q_{j-i}.
\]
因为 $Z(\beta)=0$，故该更新保持值 $\sum_j z'_j\beta^j=\sum_j z_j\beta^j$。

\begin{remark}[“原始场/控制场/迭代”与 \texorpdfstring{$(e,\pi,\varphi)$}{(e, pi, varphi)} 的层次对应]\label{rem:z-q-iter-epi-phi}
上式实现了三类对象的显式解耦：
\begin{itemize}
  \item 系数场 $z=(z_j)$ 承载值不变量 \(\sum_j z_j\beta^j\)，可视为折叠/归一化之前的“原始场”（值纤维上的 lifted 表示）。在本文的黄金比例基情形取 \(\beta=\tau=\varphi\) 时，这个“值层/几何层”的选择与第 \ref{sec:high-dimensional-cut-project} 节的代数无理方向（投影与膨胀）口径同源。
  \item 场 $q=(q_j)$ 决定\emph{注入哪一个零式、以及在何处触发}，因此它在结构上是“控制场”：它本身线性地进入更新，但在进行谱计数/生成函数或扭曲分析时，会自然被提升为指数权重参数（例如加权核中 \(u=e^\theta\) 的倾斜，见附录关于加权同步核的口径）。
  \item “迭代”对应对上述更新/读出算子的复合：无论是把归一化视为有限状态换能器的时间演化，还是把扫描相位空间建模为 \(\TT^n\) 上的平移，其频谱分解都通过紧群角色实现，从而在 Fourier/角色坐标中强制出现 \(e^{2\pi i\langle k,x\rangle}\) 的 \(2\pi\) 归一化（见第 \ref{sec:high-dimensional-cut-project} 节）。
\end{itemize}
因此 \((e,\pi,\varphi)\) 在本文并非“三个并列外加常数”，而分别对应：值/几何提升层（\(\varphi\)）、角色/指数权重层（\((e,\pi)\)）、以及迭代循环层（紧群几何带出的 \(2\pi\)）。
\end{remark}

\paragraph{黄金比例三核只用到两种稀疏 stencil}
Algorithm I/II/III 仅使用下列两种稀疏零式（\cite{SvobodovaAAMPVIIIParallelAddition}；推导与伪代码见 \cite{FrougnyHellerPelantovaSvobodovaParallelAlgorithmsSlides}）：
\[
-\tau^4+7-\tau^{-4}=0\ \Rightarrow\ z'_j=z_j-7q_j+q_{j-4}+q_{j+4}\quad (9\text{-local}),
\]
\[
-\tau^2+3-\tau^{-2}=0\ \Rightarrow\ z'_j=z_j-3q_j+q_{j-2}+q_{j+2}\quad (13/21\text{-local}).
\]
三核的差异只在于 $q$ 的\textbf{取值域}与\textbf{触发策略}：一次（SRZ）、定步迭代（WRZ）、两相消元（A$\circ$B）。

\subsubsection{块级在线化：同余流解耦与最小状态规格（单流）}
由于 stencil 仅耦合 $\pm 4$ 或 $\pm 2$，系统按同余类解耦：$\pm 4$ 对应模 $4$ 的 $m=4$ 条流，$\pm 2$ 对应模 $2$ 的 $m=2$ 条流。每条流都可编译为确定性在线机（固定延迟），并可直接对其\textbf{可达状态空间}做 BFS 构图；全局块级在线机是各流的直积装配（见下）。

\begin{proposition}[单流在线机的最小状态变量（可 BFS）]\label{prop:min-state-spec-flow}
在单流坐标下（强核：$j=4n+r$；弱核：$j=2n+r$），三核可取以下最小状态变量，使得“读入一个输入符号 $\Rightarrow$ 状态更新 + 输出（固定延迟）”为确定性局部规则：
\begin{itemize}
  \item \textbf{$9$-local（强核，SRZ）}：输入 $v_n\in[-10,10]\cap\ZZ$，$q_n\in\{-1,0,1\}$ 由 $v_n$ 唯一决定（把中心位拉回内层字母表）。取状态
  \[
  S^{(9)}_n=(v_{n-1},\,q_{n-2}),
  \]
  则读入 $v_n$ 后即可输出 $z_{n-1}=v_{n-1}-7q_{n-1}+q_{n-2}+q_n$（单流延迟 $1$）。
  \item \textbf{$13$-local（弱核，WRZ，三段微核流水线）}：每段微核取状态
  \(
  (z_{n-1},q_{n-2},q_{n-1})
  \)，三段直积给出
  \[
  S^{(13)}_n=\bigl(S^{(1)}_n,S^{(2)}_n,S^{(3)}_n\bigr),
  \qquad S^{(\ell)}_n=(z^{(\ell)}_{n-1},q^{(\ell)}_{n-2},q^{(\ell)}_{n-1}),
  \]
  单流延迟为 $3$。
  \item \textbf{$21$-local（最小字母表，Algorithm G=A$\circ$B）}：用两相消元（先消 $-2$ 再消 $2$）得到单流延迟 $5$ 的在线机，可取状态
  \[
  S^{(21)}_t=(v_{t-2},v_{t-1};\ q^A_{t-3},q^A_{t-2};\ w_{t-6},w_{t-5},w_{t-4},w_{t-3};\ q^B_{t-6},q^B_{t-5}),
  \]
  读入 $v_t$ 后依次判定 $q^A_{t-1}$、更新 $w_{t-2}$、判定 $q^B_{t-4}$，并输出 $z_{t-5}$。
\end{itemize}
\end{proposition}

\subsubsection{carry-free 骨架的 \texorpdfstring{$\zeta$}{zeta} 分类与 primitive 谱接口}
对任意有限图（SFT）邻接矩阵 $A$，动力学 $\zeta$ 与行列式满足（\cite{BrinStuck2002}，Chapter 3.4）
\[
\zeta_A(z)=\exp\!\left(\sum_{n\ge 1}\frac{|\mathrm{Fix}(\sigma^n)|}{n}z^n\right)=\frac{1}{\det(I-zA)},
\qquad
|\mathrm{Fix}(\sigma^n)|=\Tr(A^n).
\]
primitive 轨道数（按轨道计）由 Möbius 反演提取：
\[
p_n=\frac{1}{n}\sum_{d\mid n}\mu(d)\,\Tr(A^{n/d}).
\]

\begin{theorem}[单流 carry-free 骨架的三分分类]\label{thm:carry-free-zeta-trichotomy}
对 $9/13/21$-local 三核的单流在线机，引入每步“原子活动度”
\[
\kappa(e):=\#\{\text{该步新决定的非零 }q\text{ 事件}\}.
\]
取 $u=0$（仅保留 $\kappa=0$ 的边）得到 carry-free 骨架邻接矩阵 $A_0$。则其最大强连通分量分别与下列三类 SFT 共轭：
\begin{enumerate}
  \item $9$-local：$7$-全移位，$\det(I-zA_0)=1-7z$；
  \item $13$-local：$3$-全移位，$\det(I-zA_0)=1-3z$；
  \item $21$-local：黄金均值 shift（禁子词 $11$），$\det(I-zA_0)=1-z-z^2$。
\end{enumerate}
\end{theorem}

\begin{remark}[可审计证明位置]
定理 \ref{thm:carry-free-zeta-trichotomy} 的证明只依赖 Algorithm S/W/G（含 A/B 判定条件）的显式触发规则；为避免在正文重复展开，证明链已在附录 \ref{app:sync-kernel-A-compare} 的命题 \ref{prop:carry-free-skeleton-three} 给出。
\end{remark}

\begin{corollary}[全局（多流）装配与 carry-free \texorpdfstring{$\zeta$}{zeta} 因子化]\label{cor:global-assembly-zeta}
设单流 carry-free 骨架矩阵为 $A_0^{\mathrm{flow}}$，同余流条数为 $m$（强核 $m=4$，弱核 $m=2$）。则全局块级 carry-free 骨架为张量积
\[
A_0^{\mathrm{glob}}=(A_0^{\mathrm{flow}})^{\otimes m},
\]
因而
\[
\zeta^{\mathrm{glob}}_0(z)=\frac{1}{\det(I-zA_0^{\mathrm{glob}})}.
\]
特别地：
\[
K_9:\ \det(I-zA_0^{\mathrm{glob}})=1-2401z,\qquad
K_{13}:\ \det(I-zA_0^{\mathrm{glob}})=1-9z,
\]
\[
K_{21}:\ \det(I-zA_0^{\mathrm{glob}})=(1+z)^2(1-3z+z^2).
\]
\end{corollary}

\subsubsection{核指纹表与计算接口}
表 \ref{tab:parallel-addition-kernels-fingerprint-main} 汇总了三核在“单流 $\Rightarrow$ 全局块级”装配下的指纹（状态数、延迟、$\kappa$ 均值、carry-free 的 $\det(I-zA_0)$ 与 primitive 谱、以及端点 $\lambda(0),\lambda(1)$）。
\input{sections/generated/tab_parallel_addition_kernels_fingerprint_main}

\subsubsection{端点谱表（\texorpdfstring{$u=0,1$}{u=0,1}）的 primitive 基准}\label{subsec:kernel-endpoint-primitive}
为了得到“零误差、可跨实现核对”的最硬基准，我们把三核的可比指纹进一步收敛到两端点：
\begin{itemize}
  \item $u=0$：carry-free 骨架（仅保留 $\kappa=0$ 边），其 $p_n$ 由 $A_0=M_K(0)$ 的闭式迹/反演得到；
  \item $u=1$：忽略 carry 强度（无权），在本文的单流编译口径下满足 $\Tr(M_K(1)^n)=|B|^n$，因此 $p_n$ 为 full shift 的闭式 Möbius 反演。
\end{itemize}
表 \ref{tab:parallel-addition-kernels-endpoint-primitive} 给出三核在两端点的单流与全局块级 $p_n$（$n\le 20$），作为后续比较中间 $u\in(0,1)$ 分层谱的基准底座。
\input{sections/generated/tab_parallel_addition_kernels_endpoint_primitive}

\begin{corollary}[\texorpdfstring{$u=1$}{u=1} 的无权退化：仅看输入规模的 full shift 基线]\label{cor:par-add-u1-degeneracy}
在确定性完备读入的单流同步图上，$u=1$ 时带权邻接矩阵 $M_K(1)$ 退化为“仅计数边”的邻接矩阵。若每个状态对每个输入符号恰有一条出边，则每行和恒为 $|B_K|$，从而
\[
\Tr(M_K(1)^n)=|B_K|^n,\qquad
\zeta_K(z,1)=\exp\!\left(\sum_{n\ge 1}\frac{|B_K|^n}{n}z^n\right)=\frac{1}{1-|B_K|z}.
\]
因此 primitive 轨道数（按轨道计）为 full shift 的闭式 Möbius 反演：
\[
p_n(1)=\frac{1}{n}\sum_{d\mid n}\mu(d)\,|B_K|^{n/d}.
\]
对本文三核的单流输入字母表大小为
\(
|B_9|=21,\ |B_{13}|=13,\ |B_{21}|=5
\)
（见表 \ref{tab:parallel-addition-kernels-endpoint-primitive}），这解释了为什么核对比必须引入 $u$：否则 $\zeta$ 层面会退化为“只看输入规模”。
\end{corollary}

\begin{corollary}[多流全局端点的闭式提升（避免直积大图）]\label{cor:par-add-global-lift-endpoints}
设单流带权邻接矩阵为 $M_{\mathrm{flow}}(u)$，同余流条数为 $m$（强核 $m=4$，弱核 $m=2$）。全局块级核满足张量积结构
\[
M_{\mathrm{glob}}(u)=M_{\mathrm{flow}}(u)^{\otimes m},
\qquad
\Tr(M_{\mathrm{glob}}(u)^n)=\left(\Tr(M_{\mathrm{flow}}(u)^n)\right)^m.
\]
因此全局 primitive 分层谱满足
\[
B^{\mathrm{glob}}_{n}(u)=\frac{1}{n}\sum_{d\mid n}\mu(d)\left(\Tr\!\left(M_{\mathrm{flow}}(u^d)^{n/d}\right)\right)^m,
\]
从而端点 $u\in\{0,1\}$ 的全局谱可由单流闭式直接提升得到（表 \ref{tab:parallel-addition-kernels-endpoint-primitive}）。
\end{corollary}

做素数影子对比时，计算接口可取：先计算周期迹 $a_n(u)=\Tr(M(u)^n)$，再用 Witt/Möbius 反演提取 primitive 加权谱
\[
p_n(u)=\frac{1}{n}\sum_{d\mid n}\mu(d)\,a_{n/d}(u^d),
\]
与附录 \S\ref{app:witt-weighted} 的接口完全一致；而全局块级直积下有 $a_n^{\mathrm{glob}}(u)=a_n^{\mathrm{flow}}(u)^m$，因此只需单流即可装配全局的 primitive 谱。

\subsubsection{两变量 \texorpdfstring{$\zeta_K(z,u)$}{zeta(z,u)} 与 primitive 分层谱 \texorpdfstring{$B_n(u)$}{B\_n(u)}}
对单流同步图 $\Gamma_K$，以每条边的原子活动度 $\kappa(e)\in\ZZ_{\ge 0}$（本步新决定的非零 $q$ 事件数）作为势，定义带权邻接矩阵族
\[
M_K(u)_{ij}:=\sum_{e:i\to j}u^{\kappa(e)}\in\ZZ[u].
\]
把有限图的 Bowen--Lanford/Brin--Stuck 行列式公式逐点应用到 $M_K(u)$，定义两变量 $\zeta$-指纹
\[
\zeta_K(z,u)
:=\exp\!\left(\sum_{n\ge 1}\frac{\Tr(M_K(u)^n)}{n}z^n\right)
=\frac{1}{\det(I-zM_K(u))}.
\]
这是 Ruelle 加权 $\zeta$ 在有限图情形的具体化（参见 \cite{Ruelle1976ZetaExpanding,ParryPollicott1990Zeta,BrinStuck2002}）。

令 $\mathcal{P}$ 为 $\Gamma_K$ 的 primitive 周期轨道集合，并记 $\kappa(\gamma)=\sum_{e\in\gamma}\kappa(e)$。则 $\zeta_K(z,u)$ 具有 Euler 乘积形式
\[
\zeta_K(z,u)=\prod_{\gamma\in\mathcal{P}}\bigl(1-z^{|\gamma|}u^{\kappa(\gamma)}\bigr)^{-1}.
\]
定义长度恰为 $n$ 的 primitive 轨道“分层素数谱”多项式
\[
B_n(u):=\sum_{\substack{\gamma\in\mathcal{P}\\|\gamma|=n}}u^{\kappa(\gamma)}\in\ZZ[u].
\]
若记 $a_n(u):=\Tr(M_K(u)^n)$，则 Witt/Möbius 反演给出可计算公式
\[
B_n(u)=\frac{1}{n}\sum_{d\mid n}\mu(d)\,a_{n/d}(u^d)
=\frac{1}{n}\sum_{d\mid n}\mu(d)\,\Tr\!\left(M_K(u^d)^{\,n/d}\right),
\]
这正把“周期闭环”分解为“primitive 闭环的幂重复”，从而把“素数影子”严格化。

在全局块级直积装配（$m$ 条同余流）且各流同构时，有
\[
M_K^{\mathrm{glob}}(u)=(M_K^{\mathrm{flow}}(u))^{\otimes m},\qquad
a_n^{\mathrm{glob}}(u)=a_n^{\mathrm{flow}}(u)^m,
\]
故只需单流的 $a_n(u)$ 即可装配全局的 $B_n^{\mathrm{glob}}(u)$（再做一次 Möbius 反演即可）。

\subsubsection{压力曲线 \texorpdfstring{$\lambda_K(u)$}{lambda(u)}、端点与 carry 密度}\label{subsec:kernel-pressure-curve}
对每个核的单流带权邻接矩阵族 $M_K(u)$，定义压力曲线（谱半径曲线）
\[
\lambda_K(u):=\rho(M_K(u)),\qquad P_K(u):=\log\lambda_K(u)\qquad(u\in[0,1]).
\]
它刻画“允许 carry 的程度（$u$）”对指数增长率的响应，可作为第二层谱指纹；其数值曲线与 carry-free 素数谱相图见附录 \ref{app:sync-kernel-A-compare} 的图 \ref{fig:parallel-addition-kernels-lambda-u} 与图 \ref{fig:parallel-addition-kernels-bn0}（脚本可复现）。

\begin{proposition}[端点、单调性与多流提升]\label{prop:kernel-pressure-endpoints}
设 $\kappa(e)\in\ZZ_{\ge 0}$，并令 $A_0:=M_K(0)$ 为 carry-free 骨架邻接矩阵。
则：
\begin{enumerate}
  \item \textbf{端点 $u=0$}：$\lambda_K(0)=\rho(A_0)$；
  \item \textbf{端点 $u=1$}：若在线机对每个输入符号都存在一条出边（确定性完备读入），则 $\lambda_K(1)=|B|$；
  \item \textbf{单调性/连续性}：$u\mapsto \lambda_K(u)$ 在 $[0,1]$ 上非减且连续；
  \item \textbf{多流提升}：若全局块级由 $m$ 条同构同余流张量装配，则
  \(
  \lambda^{\mathrm{glob}}_K(u)=\lambda^{\mathrm{flow}}_K(u)^m
  \)
  且
  \(
  P^{\mathrm{glob}}_K(u)=m\,P^{\mathrm{flow}}_K(u)
  \)。
\end{enumerate}
\end{proposition}

\begin{remark}[carry 密度与压力导数（热力学形式）]
把 $\kappa$ 视为边势并令 $\theta=\log u$，则 $P_K(u)=\log\rho(M_K(e^\theta))$ 是有限图移位的压力函数。
在不可约情形，$P_K$ 在 $(0,1)$ 上可微，并满足
\[
u\frac{\mathrm{d}}{\mathrm{d}u}\log\lambda_K(u)
=\frac{\mathrm{d}}{\mathrm{d}\theta}P_K(e^\theta)
=\mathbb{E}_{\mu_u}[\kappa],
\]
其中 $\mu_u$ 为势 $\theta\kappa$ 的平衡态（finite-type 情形的 Ruelle/Parry--Pollicott 口径；参见 \cite{Ruelle1978Thermodynamic,ParryPollicott1990Zeta}）。
这给出一个与“均匀输入驱动”\textbf{不同}但可比较的 carry 密度读出：前者来自外加输入分布，后者来自压力最优化（指纹层更稳定）。
\end{remark}

\subsubsection{核的热力学形式：零温基态、CLT/LDP 与加权 prime-orbit 定理}\label{subsec:kernel-thermo}
本小节把“carry 强度 $\kappa$”提升为一维势能（可观测量），并把核的单流同步图视为一个一维有限范围相互作用的动力系统。该提升的意义在于：除“能否实现加法/乘法”外，还可严格推出一组新的、可计算的结构结论——零温选择（ground state）、压力曲线响应、carry 统计的中心极限定理与大偏差、以及带权 primitive（“素数类对象”）的计数律与误差控制。

\paragraph{势能与温度参数化}
对每条边 $e$ 取势能 $\kappa(e)\in\ZZ_{\ge 0}$（原子 carry 强度），并令
\[
u=e^{-\beta}\in(0,1],\qquad \beta\ge 0.
\]
则带权矩阵 $M_K(u)$ 等价于局部常势 $\phi_\beta:=-\beta\kappa$ 的有限维转移算子；$P_K(u)=\log\rho(M_K(u))$ 即相应压力（自由能密度）。

\begin{proposition}[平衡态的显式 PF 公式（有限图/局部常势）]\label{prop:kernel-equilibrium-pf}
设 $u\in(0,1]$ 且 $M_K(u)$ 不可约。令 $\lambda=\lambda_K(u)$，取 Perron--Frobenius 的左右特征向量 $r,\ell>0$ 满足
\(
M_K(u)\,r=\lambda r,\ \ell^{\mathsf T}M_K(u)=\lambda \ell^{\mathsf T}
\)
并归一化 $\ell^{\mathsf T}r=1$。则存在一个在边移位上的一阶 Markov 平衡态 $\mu_u$，其转移概率可写成
\[
\mathbb{P}_{\mu_u}(i\to j)=\frac{M_K(u)_{ij}\,r_j}{\lambda r_i}.
\]
并且在该平衡态下，
\(
\partial_\theta \log\lambda_K(e^\theta)=\mathbb{E}_{\mu_{e^\theta}}[\kappa]
\)
成立（与上一个 remark 同口径）。
\end{proposition}

\paragraph{零温极限（ground state）}
当 $\beta\to\infty$（即 $u\to 0$）时，势能 $-\beta\kappa$ 强制系统选择最小平均 carry 的轨道集合。对有限字母表上的局部常势，一维 Gibbs 态在零温下收敛到一个由有限并 SFT 支撑的极限测度，并可由递归算法给出其遍历分解权重（\cite{ChazottesGambaudoUgalde2011ZeroTempLocConst}）。

\begin{theorem}[零温选择落在 carry-free 子移位]\label{thm:kernel-zero-temp}
对单流核的边移位空间，取局部常势 $\phi_\beta:=-\beta\kappa$ 并令 $\mu_\beta$ 为其 Gibbs 平衡态。则当 $\beta\to+\infty$ 时，$\mu_\beta$ 在弱-$\ast$ 拓扑下收敛到一个极限测度 $\mu_0$；并且 $\mu_0$ 支撑在 carry-free 子图（$\kappa=0$ 的边）生成的有限并 SFT 上。若 carry-free 子图只有一个本质强连通分量，则 $\mu_0$ 为该分量的 Parry（最大熵）测度。
\end{theorem}

\begin{corollary}[最小字母表核的零温基态为 Zeckendorf 语法]\label{cor:kernel-21-ground-state}
对 $21$-local 最小字母表核，carry-free 骨架与黄金均值 shift 共轭（定理 \ref{thm:carry-free-zeta-trichotomy}），因此零温极限 $\mu_0$ 等于黄金均值 shift 的 Parry 最大熵测度。特别地，“输出 $1$”的极限密度为
\[
\mu_0(1)=\frac{1}{\varphi+2}\approx 0.276393,
\]
与注 \ref{prop:parry-measure-golden} 的显式公式一致。
\end{corollary}

\begin{remark}[零温谱的“可微指纹”：从 \texorpdfstring{$u=0$}{u=0} 骨架读出一阶响应]\label{rem:kernel-zero-temp-differential-fingerprint}
除“零温极限支撑在哪里”外，更细的机制差异可在 \(u=0\) 骨架上以导数形式直接读出。
具体地，把单流带权矩阵写为
$$
M_K(u)=A_0+uA_1+u^2A_2+\cdots,
$$
并假设 carry-free 骨架 \(A_0\) 的 Perron 根 \(\lambda_0\) 为简单特征值，则谱半径 \(\lambda_K(u)=\rho(M_K(u))\) 在 \(u=0\) 的邻域解析，且一阶系数 \(c_1\) 具有显式 PF 微扰公式（见附录 \ref{app:sync-kernel-A-compare} 的命题 \ref{prop:lambda-linear-response}）：
$$
\lambda_K(u)=\lambda_0+c_1u+O(u^2),
\qquad
c_1=\frac{\ell^{\mathsf T}A_1 r}{\ell^{\mathsf T}r}.
$$
因此平均 carry 密度满足
$$
\mathbb{E}_u[\kappa]
=u\frac{d}{du}\log\lambda_K(u)
=\frac{c_1}{\lambda_0}u+O(u^2),
$$
这使得“无需跑到中间温度，也能在 \(u=0\) 的骨架上区分机制”的审计接口在本文口径下完全闭合。
\end{remark}

\paragraph{carry 统计：CLT 与大偏差（固定温度）}
在 $u\in(0,1)$ 固定时，$\mu_u$ 是有限状态 Markov 平衡态；对边可观测量 $\kappa$ 的部分和
\(
S_n:=\sum_{t=1}^{n}\kappa(e_t)
\)
可直接应用有限状态 Markov 链的标准极限定理。其均值与方差亦可由压力导数给出（参见 \cite{Ruelle1978Thermodynamic} 的转移算子口径）。

\begin{proposition}[carry 的 CLT/LDP 指纹]\label{prop:kernel-clt-ldp}
设 $u\in(0,1)$ 且 $M_K(u)$ 原始。令 $\theta=\log u$，并记
\(
P(\theta):=\log\rho(M_K(e^\theta))
\)。
则：
\begin{enumerate}
  \item \textbf{（LLN/均值）} $\lim_{n\to\infty}\frac{1}{n}S_n=P'(\theta)$，几乎处处且在 $L^1$ 成立；
  \item \textbf{（CLT）} 若 $\kappa$ 非共边界，则
  \(
  \frac{S_n-nP'(\theta)}{\sqrt{n}}
  \)
  在 $\mu_u$ 下收敛到均值 $0$、方差 $P''(\theta)$ 的正态分布；
  \item \textbf{（LDP）} 经验均值 $S_n/n$ 满足大偏差原理，其速率函数为 Legendre 变换
  \[
  I(\alpha)=\sup_{t\in\RR}\bigl(t\alpha-(P(\theta+t)-P(\theta))\bigr).
  \]
\end{enumerate}
\end{proposition}

\begin{corollary}[\texorpdfstring{$K_9$}{K9} 的闭式压力曲线：CLT 方差密度与 LDP 率函数全显式]\label{cor:k9-integrable-pressure-ldp}
对强核 \(K_9\) 的单流编译（附录 \ref{app:sync-kernel-A-compare}），有严格线性
$$
\lambda_9(u)=7+14u,
$$
因此压力（取 \(\theta=\log u\)）为
$$
P_9(\theta)=\log\lambda_9(e^\theta)=\log\!\bigl(7+14e^\theta\bigr).
$$
其导数给出 carry 密度与方差密度（即 CLT 的方差密度）：
$$
P_9'(\theta)=\frac{2e^\theta}{1+2e^\theta},\qquad
P_9''(\theta)=\frac{2e^\theta}{(1+2e^\theta)^2}.
$$
从而
$$
\mathbb{E}_{u}[\kappa]=u\frac{d}{du}\log\lambda_9(u)=\frac{2u}{1+2u}.
$$
并且对任意基准 \(\theta_0=\log u_0\)，记
$$
\alpha_0:=P_9'(\theta_0)=\frac{2e^{\theta_0}}{1+2e^{\theta_0}}\in(0,1).
$$
则由命题 \ref{prop:kernel-clt-ldp} 的 Legendre 变换接口，经验均值 \(\bar\kappa_n=S_n/n\) 在 \(\theta_0\) 平衡态下的率函数可写为闭式
$$
I_{\theta_0}(\alpha)
=\alpha\log\frac{\alpha}{\alpha_0}+(1-\alpha)\log\frac{1-\alpha}{1-\alpha_0},
\qquad \alpha\in[0,1],
$$
其中端点按连续延拓理解（例如 \(\alpha=0\) 时取 \(0\log 0:=0\)）。
该闭式只依赖压力曲线的闭式表达，不要求额外的独立性假设。
\end{corollary}

\paragraph{加权 prime-orbit 定理（“素数影子”的热力学主项）}
对 $B_n(u)=\sum_{|\gamma|=n,\ \gamma\ \mathrm{primitive}}u^{\kappa(\gamma)}$，由迹公式 $a_n(u)=\Tr(M_K(u)^n)$ 与 Möbius 反演可把主项与误差直接追踪到谱隙。

\begin{theorem}[加权 primitive 轨道计数与指数误差]\label{thm:kernel-weighted-prime-orbit}
设 $u\in(0,1]$ 且 $M_K(u)$ 原始，令 $\lambda_1(u)=\lambda_K(u)$，并令 $\lambda_2(u)$ 为其余特征值的最大模。则
\[
B_n(u)=\frac{\lambda_1(u)^n}{n}+O\!\left(\frac{|\lambda_2(u)|^n}{n}\right)\qquad(n\to\infty).
\]
因此 \(\lambda_K(u)\) 与谱隙比 \( |\lambda_2(u)|/\lambda_1(u)\) 构成核的“素数热力学指纹”。
\end{theorem}

更完整的脚本复现、状态机构图与数值输出见附录 \ref{app:sync-kernel-A-compare} 与生成表格。

\paragraph{Chebotarev/Artin-\texorpdfstring{$L$}{L} 类比：把“同余类素数分布”内生到核}
本段把 primitive 轨道的“素数影子”从\emph{总量}升级为\emph{按同余类分层的分布律}。关键点是：对核的同步图在边上附加一个有限阿贝尔群标签（cocycle），即可定义一族扭曲行列式（扭曲 $\zeta/L$），并得到一个完全可审计的“Chebotarev 型”误差公式；其误差指数由\emph{非平凡扭曲的谱半径}显式控制。

\begin{definition}[有限群标签与扭曲矩阵族]\label{def:kernel-group-twist}
取有限群 $G$（实践中常取阿贝尔群 $G=\ZZ/m$ 或其直积，但本定义允许非阿贝尔情形），并给单流同步图 $\Gamma_K$ 的每条边 $e$ 赋一个群标签 $g(e)\in G$。
对任意有限维复表示 $\rho:G\to\mathrm{GL}(d_\rho,\CC)$ 与温度参数 $u\in(0,1]$，定义矩阵值扭曲算子
\[
M_{K,\rho}(u)_{ij}:=\sum_{e:i\to j}u^{\kappa(e)}\,\rho(g(e))\in\mathrm{Mat}_{d_\rho}(\CC),
\]
并把它视为作用在 $\CC^{V}\otimes\CC^{d_\rho}$ 上的块矩阵。定义扭曲 $\zeta/L$-函数为
\[
L_{K,\rho}(z,u):=\det(I-zM_{K,\rho}(u))^{-1}.
\]
当 $\rho=\mathbf{1}$ 为平凡表示时，$M_{K,\mathbf{1}}(u)=M_K(u)$，从而 $L_{K,\mathbf{1}}(z,u)=\zeta_K(z,u)$。
\end{definition}

\begin{definition}[同余类分层的 primitive 谱]\label{def:kernel-chebotarev-spectrum}
对 $c\in G$，定义按“Frobenius 标签”分层的 primitive 轨道谱
\[
B_{n,c}(u):=\sum_{\substack{\gamma\in\mathcal{P}\\|\gamma|=n\\g(\gamma)=c}}u^{\kappa(\gamma)}.
\]
当 $G$ 为阿贝尔群时，其不可约表示为角色 $\chi\in\widehat G$；此时定义扭曲 primitive 谱
\[
B_n(\chi;u):=\sum_{\substack{\gamma\in\mathcal{P}\\|\gamma|=n}}\chi(g(\gamma))\,u^{\kappa(\gamma)}.
\]
\end{definition}

\begin{proposition}[角色傅里叶反演（Chebotarev 分层的精确恒等式）]\label{prop:kernel-chebotarev-fourier}
在定义 \ref{def:kernel-chebotarev-spectrum} 的记号下，对任意 $c\in G$ 有精确恒等式
\[
B_{n,c}(u)=\frac{1}{|G|}\sum_{\chi\in\widehat G}\overline{\chi(c)}\,B_n(\chi;u).
\]
\end{proposition}

\begin{proof}
对每条 primitive 轨道 $\gamma$，用有限阿贝尔群的角色正交关系
\(
\frac{1}{|G|}\sum_{\chi\in\widehat G}\overline{\chi(c)}\,\chi(g(\gamma))=\mathbf{1}_{\{g(\gamma)=c\}}
\)
逐项展开即可。
\end{proof}

\begin{proposition}[扭曲 primitive 谱的 Möbius--Adams 抽取：\texorpdfstring{$(\chi,u)\mapsto (\chi^d,u^d)$}{(chi,u)->(chi^d,u^d)}]\label{prop:kernel-chebotarev-mobius-adams}
沿用定义 \ref{def:kernel-group-twist} 的记号，并额外假设 \(G\) 为有限阿贝尔群。
对任意角色 \(\chi\in\widehat G\) 与 \(u\in(0,1]\)，定义扭曲迹序列
\[
a_n(\chi;u):=\Tr\!\left(M_{K,\chi}(u)^n\right)\qquad(n\ge 1).
\]
则定义 \ref{def:kernel-chebotarev-spectrum} 的扭曲 primitive 谱满足严格恒等式
\[
\boxed{\ 
B_n(\chi;u)=\frac{1}{n}\sum_{d\mid n}\mu(d)\,a_{n/d}\!\bigl(\chi^{d};u^{d}\bigr)
=\frac{1}{n}\sum_{d\mid n}\mu(d)\,\Tr\!\left(M_{K,\chi^{d}}(u^{d})^{\,n/d}\right)\ }.
\]
\end{proposition}
\begin{proof}
对固定 \((\chi,u)\)，设
\(
\zeta_{K,\chi}(z,u):=\det(I-zM_{K,\chi}(u))^{-1}
\)。
由行列式对数展开有
\[
\log\zeta_{K,\chi}(z,u)
=\sum_{n\ge 1}\frac{a_n(\chi;u)}{n}z^n.
\]
另一方面，按 primitive 轨道分解（幂重复）有严格恒等式
\[
\log\zeta_{K,\chi}(z,u)
=\sum_{\gamma\in\mathcal{P}}\sum_{r\ge 1}\frac{1}{r}\Bigl(z^{|\gamma|}\chi(g(\gamma))u^{\kappa(\gamma)}\Bigr)^r
=\sum_{r\ge 1}\frac{1}{r}\sum_{n\ge 1}B_n(\chi^r;u^r)\,z^{nr}.
\]
比较 \(z^n\) 系数并对除子作变元替换得
\[
a_n(\chi;u)=\sum_{d\mid n} d\,B_d(\chi^{n/d};u^{n/d}).
\]
对该关系作 Möbius 反演即得所示公式：
\(
B_n(\chi;u)=\frac{1}{n}\sum_{d\mid n}\mu(d)\,a_{n/d}(\chi^d;u^d)
\)。
证毕。
\end{proof}

\begin{remark}[结构含义：Möbius/Witt 抽取是带 Adams 伸缩的端操作]\label{rem:kernel-chebotarev-mobius-adams-meaning}
命题 \ref{prop:kernel-chebotarev-mobius-adams} 表明：在带角色轴的扭曲语境中，primitive 抽取并非对每个 \((\chi,u)\) 逐点地对 \(n\) 作 Möbius 反演；
相反，它必然同步引入 \(d\)-次 Adams 伸缩
\[
(\chi,u)\longmapsto(\chi^d,u^d),
\]
其中 \(\chi\mapsto\chi^d\) 来自幂重复下的标签幂律 \(\chi(g(\gamma^d))=\chi(g(\gamma))^d\)，而 \(u\mapsto u^d\) 来自能量的可加性 \(\kappa(\gamma^d)=d\,\kappa(\gamma)\)。
该“时间除子 \(d\)”与“角色/能量伸缩”的同步性，是后续把 Chebotarev（空间侧）与 Witt（时间侧）拼成严格交换图的核心机制。
\end{remark}

\begin{corollary}[Chebotarev--Plancherel 能量恒等式：空间侧均方偏差 \texorpdfstring{$=$}{=} 角色侧能量和]\label{cor:kernel-chebotarev-plancherel-energy}
在命题 \ref{prop:kernel-chebotarev-fourier} 的设定下，仍假设 \(G\) 为有限阿贝尔群。
令
\[
\overline{B}_n(u):=\frac{1}{|G|}\sum_{c\in G}B_{n,c}(u)=\frac{1}{|G|}B_n(\mathbf{1};u),
\qquad
\varepsilon_n(c;u):=B_{n,c}(u)-\overline{B}_n(u).
\]
则对任意 \(n\ge 1\) 有精确恒等式
\[
\boxed{\;
\sum_{c\in G}\bigl|\varepsilon_n(c;u)\bigr|^2
=\frac{1}{|G|}\sum_{\chi\in\widehat G\setminus\{\mathbf{1}\}}\bigl|B_n(\chi;u)\bigr|^2\; }.
\]
\end{corollary}
\begin{proof}
由命题 \ref{prop:kernel-chebotarev-fourier} 得
\[
\varepsilon_n(c;u)
=\frac{1}{|G|}\sum_{\chi\ne \mathbf{1}}\overline{\chi(c)}\,B_n(\chi;u).
\]
对有限阿贝尔群 \(G\) 上的角色基应用 Parseval/Plancherel 恒等式即得
\(
\sum_{c\in G}|\varepsilon_n(c;u)|^2=\frac{1}{|G|}\sum_{\chi\ne\mathbf{1}}|B_n(\chi;u)|^2
\)。
证毕。
\end{proof}

\begin{corollary}[商映射下的 \texorpdfstring{$L^2$}{L2} 偏差单调：模数细化只会暴露更多非平凡角色能量]\label{cor:kernel-chebotarev-L2-monotone}
设 \(\pi:G_2\twoheadrightarrow G_1\) 为有限阿贝尔群的满同态，并在两层上分别构造同余分层 primitive 谱 \(B^{(i)}_{n,c}(u)\)（\(i\in\{1,2\}\)）。
令粗层分层谱由细层推前给出
\[
B^{(1)}_{n,c_1}(u):=\sum_{c_2\in \pi^{-1}(c_1)}B^{(2)}_{n,c_2}(u),
\qquad c_1\in G_1,
\]
并定义相对 Haar 密度（均值为 \(1\)）
\[
r^{(i)}_{n}(c;u):=\frac{|G_i|\,B^{(i)}_{n,c}(u)}{B^{(i)}_{n}(\mathbf{1};u)},
\qquad
\frac{1}{|G_i|}\sum_{c\in G_i}r^{(i)}_{n}(c;u)=1.
\]
则在 \(L^2(G_i)\)（按 Haar/均匀测度归一化）意义下有单调性：
\[
\boxed{\;
\bigl\|r^{(1)}_{n}(\cdot;u)-1\bigr\|_{L^2(G_1)}
\le
\bigl\|r^{(2)}_{n}(\cdot;u)-1\bigr\|_{L^2(G_2)}\; }.
\]
等价地：细化群标签（例如细化模数）只会增加（或保持）可见的非平凡角色能量总和。
\end{corollary}
\begin{proof}
由推前定义与 \(|G_2|=|G_1|\cdot|\ker\pi|\) 可知，对任意 \(c_1\in G_1\) 有
\[
r^{(1)}_{n}(c_1;u)
=\frac{1}{|\ker\pi|}\sum_{c_2\in\pi^{-1}(c_1)}r^{(2)}_{n}(c_2;u),
\]
即 \(r^{(1)}_{n}\) 正是 \(r^{(2)}_{n}\) 沿 \(\pi\) 的纤维条件期望（纤维平均）。
而条件期望作为 \(L^2\) 正交投影是收缩算子，故
\(
\|r^{(1)}_{n}-1\|_{2}\le \|r^{(2)}_{n}-1\|_{2}
\)。
证毕。
\end{proof}

\begin{theorem}[核的 Chebotarev 型主项与指数误差（完全可计算）]\label{thm:kernel-chebotarev-exp}
设 $u\in(0,1]$ 且 $M_K(u)$ 原始，记 $\lambda_1(u)=\rho(M_K(u))$，并令
\[
\eta(u):=\max_{\chi\in\widehat G,\ \chi\ne\mathbf{1}}\rho\bigl(M_{K,\chi}(u)\bigr),
\qquad
\Lambda(u):=\max\{|\lambda_2(u)|,\eta(u)\}.
\]
则对每个 $c\in G$，有
\[
B_{n,c}(u)=\frac{1}{|G|}\cdot\frac{\lambda_1(u)^n}{n}+O\!\left(\frac{\Lambda(u)^n}{n}\right)\qquad(n\to\infty).
\]
其中误差指数 $\Lambda(u)/\lambda_1(u)$ 只依赖于核在该群标签下的最坏非平凡扭曲谱半径。
\end{theorem}

\begin{proof}
由命题 \ref{prop:kernel-chebotarev-fourier}，
\(
B_{n,c}(u)=|G|^{-1}\sum_{\chi}\overline{\chi(c)}B_n(\chi;u).
\)
当 $\chi=\mathbf{1}$ 时，$B_n(\mathbf{1};u)=B_n(u)$，由定理 \ref{thm:kernel-weighted-prime-orbit} 得到主项与误差 $O(|\lambda_2(u)|^n/n)$。
当 $\chi\ne\mathbf{1}$ 时，由命题 \ref{prop:kernel-chebotarev-mobius-adams}，
\(
B_n(\chi;u)=\frac{1}{n}\sum_{d\mid n}\mu(d)\,\Tr(M_{K,\chi^d}(u^d)^{\,n/d})
\)
给出，且对任意有限矩阵 $A$ 有粗界 $|\Tr(A^m)|\le (\dim A)\,\rho(A)^m$；因此
\(
B_n(\chi;u)=O(\eta(u)^n/n)
\)
一致成立。合并两类误差即得结论。
\end{proof}

\begin{definition}[核版 RH/GRH（谱隙的平方根界）]\label{def:kernel-rh}
在固定温度 $u$ 下，定义核的“RH 型谱隙条件”
\[
\textsf{RH}_K(u):\quad \max_{i\ge 2}|\lambda_i(u)|\le \lambda_1(u)^{1/2},
\]
以及在给定有限群标签 $G$ 下的“GRH 型扭曲谱隙条件”
\[
\textsf{GRH}_{K,G}(u):\quad \eta(u)\le \lambda_1(u)^{1/2}.
\]
\end{definition}

\begin{corollary}[平方根级误差：核版 RH/GRH 的“素数定理形状”]\label{cor:kernel-rh-sqrt-error}
若 $\textsf{RH}_K(u)$ 成立，则
\[
B_n(u)=\frac{\lambda_1(u)^n}{n}+O\!\left(\frac{\lambda_1(u)^{n/2}}{n}\right).
\]
若进一步 $\textsf{GRH}_{K,G}(u)$ 成立，则对每个 $c\in G$，
\[
B_{n,c}(u)=\frac{1}{|G|}\cdot\frac{\lambda_1(u)^n}{n}+O\!\left(\frac{\lambda_1(u)^{n/2}}{n}\right).
\]
\end{corollary}

\begin{proof}
把定义 \ref{def:kernel-rh} 代入定理 \ref{thm:kernel-weighted-prime-orbit} 与定理 \ref{thm:kernel-chebotarev-exp} 即得。
\end{proof}

\begin{proposition}[Artin 分解的有限维版本（正规覆盖的行列式因子化）]\label{prop:kernel-artin-factorization}
在定义 \ref{def:kernel-group-twist} 的框架下，令 $M_{K,G}(u)$ 为作用在 $\CC^{V}\otimes\CC[G]$ 上的“群扩张邻接算子”：
对基向量 $e_{i}\otimes\delta_g$，令
\[
M_{K,G}(u):(e_i\otimes\delta_g)\mapsto \sum_{e:i\to j}u^{\kappa(e)}\,(e_j\otimes \delta_{g\cdot g(e)}).
\]
则对任意 $z$ 有行列式恒等式
\[
\det(I-zM_{K,G}(u))=\prod_{\rho\in\widehat G}\det(I-zM_{K,\rho}(u))^{\dim\rho},
\]
其中 $\widehat G$ 为 $G$ 的不可约复表示类集合。等价地，覆盖 $\zeta$ 满足
\[
\zeta_{K,G}(z,u)=\prod_{\rho\in\widehat G}L_{K,\rho}(z,u)^{\dim\rho},
\qquad
\zeta_{K,G}:=\det(I-zM_{K,G}(u))^{-1}.
\]
\end{proposition}

\begin{proof}
正则表示分解给出同构
\(
\CC[G]\cong\bigoplus_{\rho\in\widehat G}\rho^{\oplus \dim\rho}.
\)
该同构把 $M_{K,G}(u)$ 约化为在各个不可约分量上的直和；而在 $\rho$-分量上的约化恰为定义 \ref{def:kernel-group-twist} 的 $M_{K,\rho}(u)$。取行列式得到因子化并出现幂次 $\dim\rho$。
\end{proof}

\begin{remark}[对照：Ihara-\texorpdfstring{$\zeta$}{zeta} 的“图版 RH”与 Ramanujan]\label{rem:kernel-graph-rh}
在无回溯边移位/非回溯行走的设置中，Ihara-\(\zeta\) 及其 Bass 行列式公式把“素回路谱”与谱半径/特征值界绑定；对 $q$-正则图，Sunada 的观察可表述为：Ramanujan 条件等价于 Ihara-\(\zeta\) 满足一个 Riemann 假设型的零点/极点约束（见 \cite{TerrasZetaGraphs,StarkTerras1996GraphCoverings}）。
本文的核版 RH/GRH（定义 \ref{def:kernel-rh}）与该图论对照的共同点在于：都把 prime-orbit 误差的“平方根界”转译为一个可检验的谱隙条件。
\end{remark}

\begin{remark}[零温“第一激发态”的可计算读出：最小平均回路]\label{rem:kernel-min-cycle-mean}
给定边权 $w(e):=\kappa(e)$，定义周期平均 carry 密度
\(
\bar\kappa(\gamma)=\kappa(\gamma)/|\gamma|.
\)
则 $\min_{\gamma}\bar\kappa(\gamma)$ 等于有向图的最小平均回路值，可用 Karp 算法在多项式时间内求出（\cite{Karp1978MinCycleMean}）。
对本文 $9/13/21$-local 三核，该最小值为 $0$（存在 carry-free 周期）；而把 carry-free 本质分量收缩后，仍可用同一算法给出“必须产生 carry 的周期族”中最小的正平均密度，作为零温下的第一激发态指纹。
\end{remark}

\begin{corollary}[折叠公式：加法即“叠加后归一化”]\label{cor:add-as-fold}
将 $c,d\in X_\infty$ 视为 $\mathcal{D}$ 中的 $0/1$ 配置，令其逐点和为
\[
(c+d)_k:=c_k+d_k\in\{0,1,2\},
\]
则
\[
c\oplus d=\Fold_\infty(c+d).
\]
\end{corollary}

\textbf{证明：}
由定义 \ref{def:val-on-D}，$\mathrm{Val}(c+d)=\mathrm{Val}(c)+\mathrm{Val}(d)$，再由定理 \ref{thm:zeckendorf-transversal} 的定义 $\Fold_\infty(a)=Z(\mathrm{Val}(a))=\mathrm{Val}^{-1}(\mathrm{Val}(a))$ 即得。

\begin{theorem}[加法的定义性同构（记号固定）]\label{thm:add-definitional}
$(X_\infty,\oplus)$ 为交换幺半群，且 $\mathrm{Val}:(X_\infty,\oplus)\to(\NN,+)$ 为同构。
\end{theorem}

\textbf{证明：}
由定义 \ref{def:stable-add} 直接有 $\mathrm{Val}(c\oplus d)=\mathrm{Val}(c)+\mathrm{Val}(d)$。又 $\mathrm{Val}$ 为双射（Zeckendorf 唯一性），故 $(X_\infty,\oplus)$ 的结合律、交换律与单位元均由 $(\NN,+)$ 传递得到，且 $\mathrm{Val}$ 给出同构。

该定理将加法解释为“从非规范叠加到稳定地址的投影读出”：原始叠加（逐位相加）一般不保持稳定语法，需要通过规范化投影到稳定地址空间。
