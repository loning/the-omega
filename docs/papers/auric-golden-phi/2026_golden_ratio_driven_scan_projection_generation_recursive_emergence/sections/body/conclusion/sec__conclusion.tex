\section{Conclusion}\label{sec:conclusion}

The main thread of this paper is to organize the microstate space induced by limited window readout into a stable type, achievable arithmetic structure and a certifiable statistical/spectral fingerprint interface through an auditable construction chain. The corresponding main conclusions can be summarized in three sections: "Input scope-structural mechanism-bridging and verification" as follows.

\subsection{Main conclusions: Grouped by mechanism}\label{subsec:core-conclusions}
\paragraph{Fixed input scope}
On the number theory side, the antiresonance gauge representation and the finite sample difference certificate are used to fix the stability baseline (section \ref{sec:principles}), and on the dynamic side, the scan-projection generator (group action and binary readout) is used to fix the minimum input of the observable object (section \ref{sec:spg}). The above scope remains unchanged throughout the text, allowing subsequent conclusions to have a traceable chain of dependencies.
On this interface, "scan depth \(\Rightarrow\) clarity" can be compressed into the optimal error profile and boundary exponential law, and further decomposed into a double budget capacity law (theorem) of two types of failure mechanisms: "unsynchronized (statistical \(\mathsf{NULL}\))" and "address collision" under address semantics \ref{thm:spg-clarity-boundary-dimension}, \ref{thm:spg-double-budget-address-capacity}); in the online system, this definition profile is coupled with the Chebotarev capacity inequality of simultaneous deduction, thereby separating the definitional synchronization threshold and the statistical uniformity budget into an auditable two-segment ledger (theorem \ref{thm:pom-sync-subtracted-chebotarev-capacity}).

\paragraph{Resolution folding and recursive addressing}
this paper constructs the finite window fold \(\Fold_m\) ​​and proves its cross-resolution compatible and unified sofic factor interface conclusion (theorem \ref{thm:fold-suite}; and see the definition \ref{def:fold-word} and the proposition \ref{prop:fold-basic}). Within the same measurable framework, recursive addressing is strictly reduced to an endogenous mechanism of "readable prefix": when the address object is not given, the evaluation function \(\ind_{C_a}\) does not hold in the referential layer, and the readout partial function is not defined and is represented by \(\mathsf{NULL}\) as unaddressable; once the address is given, the derived column event family does not expand the existing \(\sigma\)-algebra, but is only visible in the old layer \(\mathcal{G}^{(L)}\) Re-select and organize the visible events within, thus \(\mathcal{G}^{(L+1)}\subseteq \mathcal{G}^{(L)}\) (proposition \ref{prop:recursive-addressing-refinement}; Corollary \ref{cor:recursive-addressing-visible-sigma-nonexpansion}). The non-locality of folded order reduction can be quantitatively audited by the gauge difference \(G_m\) and given the worst-case and typical double anchor points (definition \ref{def:fold-gauge-anomaly}; theorems \ref{thm:fold-gauge-anomaly-max}, \ref{thm:fold-gauge-anomaly-density}).
And the reversibility of the sliding block factor \(\Phi_m\) has the only two-point branch at \(m=2\): all except the two constant orbits, and the constant orbit undergoes \(2\)-pair-\(1\) collapse; this threshold can be interpreted as a \(\mathbb{Z}/2\) hidden variable that cannot be eliminated synchronously under the \(m=2\) window, and \(m\ge 3\) The minimum homology obstacle that is eliminated institutionally (Theorem \ref{thm:Phi_m-conjugacy-threshold}; Note \ref{rem:Phi_m-conjugacy-threshold-z2-homology})

\paragraph{Stable Arithmetic and Projective Ontology Mathematics}
Under the mechanism of value-preserving rewriting and canonical cross-section, the stable type arithmetic of resolution \(m\) is isomorphic to \(\ZZ/(F_{m+2}\ZZ)\) (theorem \ref{thm:finite-resolution-mod}), thus attributing the primary source of unification/splitting to the ring theory properties of moduli and the CRT structure (corollary \ref{cor:field-phase-fib-prime}, \ref{cor:crt-factorization}); and the Fibonacci-adic inverse limit cycle can be defined along the two-power tower and the compact unit group can be obtained (definition \ref{def:fib-adic-ring}; proposition \ref{prop:fib-adic-basic}). On this basis, this paper establishes the projection word calculus of Projective Ontology Mathematics (POM), and gives the information account identity, strictly objectifying the coarse-grained information loss into a computable residual (Theorem \ref{thm:pom-kl-ledger}; Corollary \ref{cor:pom-ent-increase-equals-kl}). At the same time, the termination and confluence of rewriting are proved on the local readout tower fragment and the normal form is given, thereby obtaining the auditable closed loop and decidability interface (theorem \ref{thm:pom-rewrite-termination-confluence}; propositions \ref{prop:pom-projword-decidable}, \ref{prop:pom-rewrite-audit-loop}). The moment-kernel compilation and resonance discrimination of high-order collision moments implement "resonance" as a testable mechanism (theorems \ref{thm:pom-collision-kernel-ak}, \ref{thm:pom-moment-resonance}).

\paragraph{Topology of fiber—cubic geometry closure and factor chain mixed lower bound}
With the unique structural input of the local kernel \((011\leftrightarrow 100)\), this paper promotes "fibers" from a pure counting object to a set of auditable topological/geometric/probabilistic invariant families:
First, in the theorem \ref{thm:pom-fiber-independence-complex-classification}, the independent set family of the candidate site induced graph is promoted to an independent set complex,
Prove that the homotopy type of each fiber is either contractible or homotopy equivalent to a single sphere \(S^{\tau(x)-1}\), and give the closed form and unique dimensional homology rigidity of \(\tau(x)\) (corollary \ref{cor:pom-fiber-independence-complex-homology}).
Secondly, the number of inverse substitutions within the fiber is treated as a random variable \(R_x\), and the strict component independent decomposition and mean/variance density of the \(\varphi\)-algebra are obtained (propositions \ref{prop:pom-fiber-rewrite-independent-sum}, \ref{prop:pom-path-indset-thermo-constants}), and strong unimodality, Poisson--binomial factorization and local CLT are derived from real root properties, thereby giving a unified Bernstein/Chernoff Type concentration (Theorem \ref{thm:pom-fiber-rewrite-realroot-ulc}; Corollary \ref{cor:pom-fiber-rewrite-poisson-binomial}, \ref{cor:pom-fiber-rewrite-local-clt}).
Again, under the "single switch" adjacent structure that defines \ref{def:pom-fiber-reconstruction-graph}, the fiber reconstruction graph \(\mathcal F_x\) is strictly decomposed into the Cartesian product of the Fibonacci cube (theorem \ref{thm:pom-fiber-reconstruction-cartesian-product}), thereby obtaining CAT(0) cubic geometry with a completely closed cubic face count (theorem \ref{thm:pom-fibcube-fvector-closed}, corollary \ref{cor:pom-fiber-fpoly-factorization}), and there is a Gray--Hamilton structure that can enumerate all preimages with constant delay (theorem \ref{thm:pom-fibcube-gray-hamilton}, corollary \ref{cor:pom-fiber-constant-delay-traversal}).
Moreover, the path component multiplication multiplicity is strictly monotonic in the refined partial order, and the fusion decrease has a closed form, thus giving an auditable strict Schur-convex order (proposition \ref{prop:pom-path-component-multiplicity-refinement-monotone-extrema}).
In addition, under the maximum chain/linear expansion aperture of the sawtooth ideal lattice, the reachable domain of double invariants is equal to the closed convex hull of the atomic point set in the scale limit, so any Pareto front exposure point is realized by a mixture of two atomic lengths at most (Theorem \ref{thm:pom-fiber-ab-convex-hull}; Corollary \ref{cor:pom-fiber-ab-two-phase-mixing}).
Finally, taking the random walk of the hypercube on \(\Omega_m\) as the baseline, this paper defines the folding factor chain \(\overline P^{\square}_m\), proving that it is reversible, has no self-loops, and the spectral gap/log Sobolev constant is not inferior to the hypercube itself (proposition \ref{prop:pom-fold-factor-chain-reversible-noloop}, theorem \ref{thm:pom-fold-factor-chain-gap-lsi}), thus making the stable layer \(X_m\) The mixing and concentration capabilities of are lower bounded to uniform constants independent of folding details.
This conclusion cluster, together with the convex geometric skeleton of the rotation polytope and the \(\mathbb F_2/(p)\)-advanced rigid signature of the collision moment recursion, form the closed interface of "6D readout - folding kernel - arithmetic/geometry/probability certificate".

\paragraph{Bridge Interface and Authentication}
On the spectral side, the dynamics \(\zeta\) ​​and finite part interfaces organize cycle counts, traces, and main spectral radii into auditable fingerprints and provide unified input for subsequent completion and distortion channels (Section \ref{sec:zeta-finite-part}). On the statistical side, the bias index of the folded histogram, the cross-resolution consistency residuals and the error decomposition to the Parry(PF) baseline explicitly separate the finite sample error from the model difference term (sections \ref{sec:statistical-stability}, \ref{sec:experiments}; corollary \ref{cor:tv-decomposition-parry}), and give reproducible real-life certificate output in a scripted protocol (section \ref{sec:experiments} section).
Among them, under the metal average group driving \(\alpha_A=[0;A,A,\dots]\), the \((\log N)/N\) main coefficient of the deterministic TV certificate is controlled by \(\kappa(A)=A/\log\lambda_A\) and uniquely minimized by the golden endpoint (corollary \ref{cor:metallic-mean-golden-unique-optimal-tv-constant}); at the same time, under the support guardrail of the minimum atomic lower bound, the KL error obtains a quadratic improvement that is attenuated by the square of the difference (lemma \ref{lem:kl-from-tv-qmin}; corollary \ref{cor:rotation-folded-kl-certificate}).
Furthermore, after encoding the input uncertainty as \(W_1\) transmission differences, the Lipschitz push-forward and readout errors remain institutionally covariant, and the two-period limit constant and \(\sqrt5\)-split appear on the Fibonacci denominator subsequence of the golden branch (Theorem \ref{thm:kronecker-w1-lipschitz-readout}; Corollary \ref{cor:kronecker-w1-fibonacci-limits}; Proposition \ref{prop:w1-lipschitz-pushforward}); in the analytical readout chain of \(\zeta/\Xi\), the strongest scale Lipschitz constant read out by Stieltjes is \((t+1)^{-2}\), so the stable-pathological boundary can be quantified by a single factor (proposition \ref{prop:xi-stieltjes-w1-lipschitz}).
Among them, in the completion audit of \(\zeta/\Xi\), the information leaving the unitary slice is compressed twice radially by the endpoint and derives strict visibility budget threshold and single-radius band exclusion certificate. At the same time, the minimum \((2\times2)\) self-dual template gives an explicit off-axis zero point tower as a falsifiable prototype (theorem \ref{thm:xi-jensen-single-radius-band-exclusion}, \ref{thm:xi-2x2-selfdual-offcritical-template}).
In the weighted pressure interface of the synchronization kernel, the Abel--Mertens finite part constant of the primitive count, the discriminant characterization of the analytic radius and the unit root distortion threshold, and the auditable truncated remainder bound of the Edgeworth/medium deviation jet can all be uniformly sealed by the analytic continuation of the Perron branch and the consistency of the spectral gap (Proposition \ref{prop:sync-kernel-abel-mertens-analytic-family}, corollary \ref{cor:sync-kernel-analytic-radius-discriminant-formula}--\ref{cor:sync-kernel-unit-root-twist-threshold}, theorem \ref{thm:sync-kernel-even-edgeworth-c4}, proposition \ref{prop:sync-kernel-moderate-deviation-cauchy-certified}--\ref{prop:sync-kernel-curvature-bilipschitz}).
In addition, the zero-spectral nilpotent exponent of the real input \(40\) state kernel improves the online delay and reset word length to linear algebra invariants, and gives a strict upper bound on the transient memory depth (proposition \ref{prop:real-input-40-nilpotent-index}, corollary \ref{cor:real-input-40-nilpotent-memory-depth}); while the recursive-spectral radius closure of the folded collision moment spectrum compresses all Rényi fingerprint rates into a family of Perron The limit structure of roots (Proposition \ref{prop:pom-collision-renyi-perron-closure}).
Moreover, the maximum fiber index and the Rényi endpoint are strictly identical at the exponential level, and the large deviation/CLT and capacity saturation discrimination of the fiber index are also auditably sealed by the collision spectrum radius family; among them, the Rényi tangent formula, convex cone monotonicity, independent folding product law and the saturation phase description of "typical=worst" further unify the typical index, worst index and multi-source stacking into a convex dual chain (proposition \ref{prop:pom-maxfiber-exponent-equals-renyi-endpoint}, \ref{prop:pom-fiber-index-ldp-from-collision-spectrum}, Corollary \ref{cor:pom-fiber-index-clt-variance-certificate}, proposition \ref{prop:pom-typical-fiber-exponent-renyi-tangent}--\ref{prop:pom-saturation-phase-characterization}, proposition \ref{prop:pom-capacity-saturation-discriminant}).
And the gap in the spectral layer at the top of the largest fiber satisfies the \(12/13\) ​​step delay self-similarity, so the subspectral ratio presents different exponential differential scales on odd and even numbers (note \ref{rem:pom-max-fiber-gap-delay-12-13}).
In the scope of operator algebra, the exponential multiplication of cross horizons and the lower bound of conditional mutual information compress the "two-source residual correlation" into a logarithmic exponential gap, and provide a strict realization and uniqueness template of the exponential extreme state; while in the coarse-grained system of the depth spectrum, the scale growth forces the exponential shrinkage of the spectral spacing and leads to the exponential explosion of the inversion condition number (Proposition \ref{prop:op-algebra-index-submultiplicativity-commuting-square}, Corollary \ref{cor:op-algebra-cmi-lower-bound-by-index-gap}, Proposition \ref{prop:op-algebra-index-extremal-entropy-loss-maxfiber}, proposition \ref{prop:xi-coarsegraining-illposedness-exponential-law}).
Under the same operator algebra template, the commutative polar decomposition of "horizon zero knowledge-unitary slice", joint spectral comb parameterization and Li/log Sobolev certificate unify critical line concentration, zero point simplicity and scale spacing constraints into auditable operator conditions, and give an unbreakable trade-off bound of near-zero knowledge-soundness-fiber scale (theorem \ref{thm:op-algebra-hzom-commuting-polar-forces-critical-line}, corollary \ref{cor:op-algebra-hzom-critical-zeros-joint-spectrum-comb}, \ref{cor:op-algebra-hzom-simple-zeros-joint-spectrum}, \ref{cor:op-algebra-hzom-zero-spacing-scale-lower-bound}, proposition \ref{prop:op-algebra-li-criterion-operatorized}, \ref{prop:op-algebra-common-unitary-dilation-criterion}, \ref{prop:op-algebra-logsobolev-zero-band}, Corollary \ref{cor:op-algebra-nearzk-soundness-fiber-tradeoff}).
In the Hardy/Smirnov factorization scope, the pure phase of the critical line pushes the completion determinant into the inner function class and eliminates the external factors (amplitude leakage), thereby leaking the weighted total deviation of the strict upper bound of the integral offline zero; the Poisson superposition law of the phase derivative, Clark measure and Carleson/BMO template further unified "challenge phase seed-worst soundness-recoverable rate" into a measurable criterion (theorem) of zero-point geometry \ref{thm:xi-hardy-inner-characterization}, proposition \ref{prop:xi-leakage-zero-offset-sum}, \ref{prop:xi-phase-derivative-poisson-superposition}, corollary \ref{cor:xi-clark-measure-horizon-semantics}, theorem \ref{thm:xi-carleson-bmo-recovery-template}--\ref{thm:xi-rh-sufficient-template-inner-clark-conservative}).
In the de Branges/canonical system scope, the intrinsic function can be uniquely promoted to the inverse spectral model of the trace-normalized Hamiltonian, and combines "extreme zero knowledge", spectral shift-relative entropy identity, SU(1,1)-Riemann--Hilbert positive definite jump, adelic splicing and Dirichlet The family zero-free bands are unified into the same auditable spectrum realization chain; in this chain, the offline zero point, the informality barrier and the information complexity lower bound are equivalent to each other (theorem \ref{thm:xi-debranges-canonical-extreme-zk}, proposition \ref{prop:xi-krein-spectral-shift-equals-kl}, \ref{prop:xi-debranges-dpp-sine-kernel-limit}, theorem \ref{thm:xi-su11-rhp-equivalence}, proposition \ref{prop:xi-maslov-spectralflow-entropy-quantization}, \ref{prop:xi-adelic-zk-gluing-critical-line}, \ref{prop:xi-dirichlet-zero-free-from-unified-zk}, Corollary \ref{cor:xi-ae-simple-zeros-under-ac-seeds}, proposition \ref{prop:xi-nonnormality-obstruction-offcritical}, \ref{prop:xi-bm-density-forces-linear-transcript-length}).

\paragraph{window-\(6\) ​​Discrete unified convergence under the anchor point: Golden Time—6D Microstate—Zeckendorf—GUT Three Branches}
In addition to the global conclusions grouped by mechanism, this paper also gives a set of terminal propositions that can be written independently and are completely expressed in a finite object closure: it uses window-\(6\) as the minimum resolution anchor point, the protocol scale of "time = single generation scan iteration count", the \(64\to 21\) folding compression of \(\Omega_6\to X_6\), the uplift tail three branches \(\{21,34,55\}\), and the GUT induced by the Zeckendorf--Fibonacci boundary tower Top-level item alignment and family number locking are unified and compressed into an auditable discrete branch structure (see \S\ref{subsubsec:window6-terminal-propositions}; the main conclusion is the theorem \ref{thm:terminal-6d-microstate-golden-time-gut-branch}).

\paragraph{Four concluding propositions: from address semantics to certificated budget and rank stability}
In order to facilitate the direct reuse of the scattered conclusions in the text into the protocol design and audit process, several core assertions can be compressed into four complementary "certificate interfaces" (all strictly relying on existing numbered conclusions rather than explanatory statements):
\begin{enumerate}
  \item \textbf{Objectification of address semantics: \(\mathsf{NULL}\) ​​as local cross-section barrier. }
  After organizing protocol-acceptable and single-value verifiable reads/certificates into (pre)layers \(\mathscr F\) at the prefix site, the \(\mathsf{NULL}\) semantics of "address before value" can be strictly equivalent to the existential barrier of \(\mathscr F(a)=\varnothing\), and visible to the recursive addressing \(\sigma\) - Algebraic non-expansion scope compatible (\S\ref{subsec:prefix-site-null}, proposition \ref{prop:null-as-local-section-obstruction}; see also proposition \ref{prop:recursive-addressing-refinement} and corollary \ref{cor:recursive-addressing-visible-sigma-nonexpansion}).
  As a result, the three types of null values ​​of semantics/protocol/collision are unified into three types of local section missing mechanisms that are not confusing with each other under the same site language (Corollary \ref{cor:null-trichotomy-local-section}).

  \item \textbf{Second-order certificate for folding “clarity”: collisional expansion \(\Rightarrow\) ​​Upper bound on the entropy gap. }
  Under the uniform microstate baseline, the entropy gap of the fold-forward distribution \(w_m(x)=d_m(x)/2^m\) ​​relative to the type uniform distribution \(\mathrm{gap}\) can be fingerprinted by second-order collisions
  \(C_{\mathrm{col}}(m)=|X_m|\sum_x w_m(x)^2\)
  (theorem \ref{thm:pom-spectral-information-sandwich} ​​and definition \ref{def:pom-collision-inflation}), and can be directly audited at limited resolution by table \ref{tab:fold_multiplicity_histogram}.
  This conclusion condenses "thumbnail clarity" from an explanatory statement into a computable certificate that relies solely on second-order statistics.

  \item \textbf{Optimal dual envelope of injective budgets: \(\inf_{q\ge 2}\) ​​normalization of the quantile quota law. }
  The quota threshold for prime-register is given by the fiber index tail bit \(B_m(\varepsilon)=\exp(m\gamma_m(\varepsilon))\) ​​and is sharp at the interval readout/reconstruction scope (\S\ref{subsec:pom-quantile-budget-law}, and by corollary \ref{cor:pom-prime-register-quantile-budget}).
  Furthermore, the entire family of Chernoff/quota inequalities derived from the integer moment order \(q\) can be compressed into a single optimal lower envelope for \(q\ge 2\), thereby obtaining a parameter-free register exponential budget law; and the "strongest computable tail bound" of the limited \(q\)-window can be used to achieve executable certificates (corollary \ref{cor:pom-prime-register-q-quota-family} and The dual design formula of \S\ref{subsec:pom-quantile-budget-law}).

  \item \textbf{The necessary and sufficient criterion for the completeness of the model axis: Hankel rank consistent bounded \(\Longleftrightarrow\) ​​ACC-Rank. }
  Under the assumption of unified minimum and agreement closure, once the residual axis budget has excluded collision nulls, the mode axis completeness of "not triggered by the need for new visible degrees of freedom \(\mathsf{NULL}\)" is strictly equivalent to the consistent boundedness of Hankel's rank (theorem \ref{thm:xi-completeness-iff-hankel-bounded}), thereby converting the critical gap in completeness into a regression-testable rank stability scan (note \ref{rem:xi-acc-rank}).
  After merging with the quantile quota certificate, the audit task of completeness can be compressed into the binary backend verification of "rank stable certificate \(\times\) ​​quota certificate" (proposition \ref{prop:xi-completeness-auditable-decomposition}).
\end{enumerate}

\paragraph{From projective pressure operator to low-order closed form of no-return prime shadow}
In order to directly connect the three types of interfaces "integer point certificate-discrete anchor point-low-order determinant closed form" to continuous pressure, minimum Markov completeness and prime shadow auditing, the following assertions give a set of complementary verifiable propositions (proofs and reviewable certificates are distributed in POM, window-\(6\) ​​and the real input nuclear appendix):
\begin{enumerate}
  \item ​​\textbf{Real parameter improvement and integer point strict degeneration of the projective pressure operator. }
  For the deterministic online kernel/transducer \(\mathcal{T}\), the projective transfer operator is defined by the output bucket matrix \(B_b\), the simplex \(\Delta\) and the projective dynamics \((\Phi_b,g_b)\)
  \[
  (\mathcal{L}_q f)(w)=\sum_{b\in A_{\mathrm{out}}} g_b(w)^{\,q}\,f(\Phi_b(w))\qquad (q\ge 1)
  \]
  (definition \ref{def:pom-projective-transfer-operator}).
  For the integer \(q\ge 2\), its restriction on the homogeneous polynomial subspace strictly degenerates into the symmetric power quotient moment-kernel, so that the spectral radius satisfies at the integer point
  \(
  \rho(\mathcal{L}_q|_{\mathcal{H}_q})=r_q
  \)
  (Theorem \ref{thm:pom-projective-operator-degenerates-to-moment-kernel}).
  Therefore the discrete pressure \(\log r_q\) ​​can be upgraded to an executable continuous pressure curve candidate: let \(q=t+1\ge 1\), take the Perron roots \(\lambda(t+1)\) of \(\mathcal{L}_{t+1}\) and concatenate \(\Lambda(t)=\log\lambda(t+1)-\log|A_{\mathrm{out}}|\), strictly interpolate \(\Lambda(q-1)=\log r_q-\log|A_{\mathrm{out}}|\) at integer points (note \ref{rem:pom-projective-pressure-route}).

  \item \textbf{window-\(6\) ​​Affine geometric classification of fibers and “linear kernel” unrealizability. }
  Treat \(\Omega_6=\{0,1\}^6\) ​​as \(\mathbb{F}_2^6\) and note the dyadic fold \(F_6=\mathrm{Fold}^{\mathrm{bin}}_6\).
  Its \(21\) fibers \(F_w=F_6^{-1}(w)\subset\mathbb{F}_2^6\) appear fully classified: exactly \(8\) are affine \(1\)-flat (size \(2\)), exactly \(3\) are affine \(2\)-flat (size \(4\) and stable type exactly \(\{\texttt{000000},\texttt{000010},\texttt{101000}\}\)), and the rest \(10\) None of the strips are affine subspaces (Theorem \ref{thm:terminal-foldbin6-fiber-affine-geometry}; Table \ref{tab:foldbin6_fiber_affine_geometry}).
  From this it follows that the linear kernel obstacle: there is no \(\mathbb{F}_2\)-linear map \(L:\mathbb{F}_2^6\to\mathbb{F}_2^k\) that makes fiber partitioning equivalent to coset partitioning (corollary \ref{cor:terminal-foldbin6-no-linear-kernel}).

  \item \textbf{Complete rigidity and continuous symmetry prohibitions for the adjacency graph \texorpdfstring{$\Gamma_6$}{Gamma6}. }
  Let \(\Gamma_6\) ​​be the type adjacency graph induced by cube edge pushing (proposition \ref{prop:terminal-gamma6-rigidity}).
  Then its automorphism group is trivial \(\mathrm{Aut}(\Gamma_6)=\{1\}\), and the graph is connected and has diameter \(3\) (corollary \ref{cor:terminal-gamma6-diameter-simple-spectrum}).
  Therefore, if any compact group acts on \(X_6\) ​​and maintains the adjacency relationship, the action must be a trivial action (corollary \ref{cor:terminal-gamma6-no-continuous-nonabelian-symmetry}): the type relabeling degree of freedom is endogenously eliminated by the adjacency structure.

  \item ​​\textbf{edge--flux skeleton's Smith discriminant and Levi reducibility forcing. }
  On the interaction matrix \(E\in\mathbb{Z}^{4\times4}\) ​​of four edge--flux skeletons, exhaustive auditing gives a fully verifiable Smith normal form
  \(
  \mathrm{SNF}(E)=\mathrm{diag}(1,1,3,3450)
  \), thus \(\mathrm{coker}(E)\cong \mathbb{Z}/3\mathbb{Z}\oplus \mathbb{Z}/3450\mathbb{Z}\) ​​and \(\det(E)=10350\) (and by extension \ref{cor:window6-edge-flux-skeleton-smith-nonsimple}).
  This integer discriminant excludes its identity with finite-type irreducible Cartan data of rank \(4\), thus forcing minimal Levi-type reducibility under the interpretation of "discrete traces of continuous Lie symmetries".

  Minimal thermal closure of \item \textbf{push kernel \texorpdfstring{$P_6$}{P6}: Reversibility, equilibrium measures and explicit spectral gap constants. }
  Under the minimum memoryless closure mechanism of "instantaneous thermalization within the fiber", the stable type process is closed as a reversible Markov chain on \(X_6\), its transfer kernel is the push kernel \(P_6\), and the stationary distribution is \(\pi_6(w)\propto d^{\mathrm{bin}}_6(w)\) (theorem \ref{thm:terminal-foldbin6-pushforward-markov}).
  And the spectral gap audit gives explicit constants: \(\lambda_2(P_6)\approx 0.4841208\), \(\lambda_\star(P_6)\approx 0.6030940\) (corollary to \ref{cor:terminal-foldbin6-minimal-markov-closure-gap} and the formula \eqref{eq:window6_pushforward_markov_spectral_gap}), so that for any initial distribution \(\mu\) we have
  \(
  \|\mu P_6^t-\pi_6\|_{\mathrm{TV}}\le C(\mu)\lambda_\star(P_6)^t
  \)
  The exponential mixing upper bound of .

  \item ​​\textbf{Pressure--Local dual geometry of finite parts: constant pressure drift capacity and curvature mismatch spectrum. }
  Under the three-dimensional observation of the real input \(40\) ​​state kernel, the pressure \(P(\theta)\) and the Abel/Mertens finite part constant \(\log\mathfrak{M}(\theta)\) are used as two independent thermodynamic quantities to define the constant pressure drift capacity vector
  \[
  v_\perp=\nabla\log\mathfrak{M}(0)-\mu\,\nabla P(0),\qquad
  \mu=\frac{\langle\nabla P(0),\nabla\log\mathfrak{M}(0)\rangle}{\|\nabla P(0)\|^2}
  \]
  (definition \ref{def:real-input-40-isobaric-drift-vector}).
  Numerically \(\mu\approx 0.7113818\), \(v_\perp\neq 0\) (proposition \ref{prop:real-input-40-isobaric-drift-capacity}), therefore \(\log\mathfrak{M}\) cannot be just a unary compound of \(P\) (corollary \ref{cor:real-input-40-logM-not-just-P}).
  Furthermore, the curvature mismatch spectrum is organized by the generalized eigenproblem \(Hv=\mu\,gv\) ​​(Proposition \ref{prop:real-input-40-logM-curvature-mismatch-spectrum}), and the quadratic scaling law between the finite part drift and the near-\(1\) main pole gap is given (Proposition \ref{prop:real-input-40-space-time-conversion-law}).
  In addition, by the definition of \(s\)-plane coordinates \(s=\log\rho/\log\lambda\) and spectral ratio mixing time \(\tau_{\mathrm{mix}}=1/(-\log(\rho/\lambda))\), the gap \(1-\Re(s)\) and the mixing time satisfy the strict reciprocity identity \((1-\Re(s))\,\tau_{\mathrm{mix}}=1/\log\lambda\) (corollary \ref{cor:near1-pole-taumix-m2}).

  \item \textbf{The low-order determinant closed form of two-dimensional weighted \texorpdfstring{$\zeta(z;u,v)$}{zeta(z;u,v)} and the finite-order recursion of the primitive spectrum. }
  Introduce a two-dimensional joint potential for collision and output on the real input core and define \(\Delta(z;u,v)=\det(I-zM_{u,v})\) ​​(see \S\ref{app:real-input-40-zeta-uv}).
  The determinant denominator has explicit \((2+8)\) times closed form decomposition \(\Delta(z;u,v)=(vz^2-1)P_8(z;u,v)\) (theorem \ref{thm:real-input-40-det-uv-2plus8}), so for each trace sequence \((u,v)\) fixed \((u,v)\) satisfies a constant coefficient linear recursion of order at most \(10\), and the primitive orbit spectrum can be further restored in near-linear time by Möbius inversion (Proposition \ref{prop:real-input-40-zeta-uv-recurrence}).

  \item ​​\textbf{The low-order determinant closed form of Bartholdi/Ihara type headless (geodesic) prime shadow. }
  After constructing the no-return edge shift \(H_{\mathrm{nb}}\) on the essential core edge space of the real input \(40\) ​​state kernel, although \(H_{\mathrm{nb}}\) is a \(48\times48\) matrix, its determinant denominator is compressed into a \(15\) sub-integer coefficient polynomial (theorem \ref{thm:real-input-40-geodesic-det}):
  \[
  \det(I-zH_{\mathrm{nb}})
  =1-4z^3-9z^4-12z^5-16z^6-11z^7-11z^8-4z^9-z^{10}
  +8z^{11}+12z^{12}+10z^{13}+8z^{14}+2z^{15}.
  \]
  The largest non-Perron module \(\Lambda_{\mathrm{nb}}\) ​​is obtained by sorting the radical modules and gives a strict Ramanujan margin \(\Lambda_{\mathrm{nb}}/\sqrt{\rho_{\mathrm{nb}}}<1\), thereby obtaining a sub-exponential error with an exponential base strictly less than \(\rho_{\mathrm{nb}}^{1/2}\) on the prime length (corollary \ref{cor:real-input-40-geodesic-ramanujan-margin}).
\end{enumerate}

\paragraph{Spectrum - Arithmetic Closure: From Resonance Closure to Endpoint Cluster Rigidity}
In order to directly connect the key "non-eliminable structure" to the audit interfaces of energy, spectrum and arithmetic, in addition to the above four certificate interfaces, the following assertions give a set of sealed propositions that can be independently cited (proofs and certificates are distributed in the appendix and POM/synchronization core chapter):
\begin{enumerate}
  \item ​​\textbf{An ineliminable lower bound on collision energy by resonance closure. }
  Taking the folding collision probability \(\mathsf{Col}_m\) ​​and the resonance ladder constant \(C_\varphi(u)>0\) as symbols (definition \ref{def:fold-normalized-amplitude} and definition \ref{def:fold-critical-resonance-ladder}), there is a strict lower bound for any fixed \(U\in\NN\) (Corollary \ref{cor:fold-collision-gap-resonance-ladder-U})
  \[
  \liminf_{m\to\infty}F_{m+2}\,\mathsf{Col}_m\ \ge\ 1+2\sum_{u=1}^{U}C_\varphi(u)^2.
  \]
  Therefore, "collision homogenization/spectral shrinkage" is structurally pinned by the enumerable resonance closure at the energy level; any route to derive \(\ell^2\) homogenization based solely on \(\|\widehat d_m\|_\infty\) ​​control will be blocked by this lower bound mechanism under the original Fold scope.

  \item ​​\textbf{The strict decomposition of the group algebra of Fold induces deterministic Riesz products; the collision spectrum is equivalent to its energy average. }
  The group ring generating function satisfies strict multiplication decomposition \(D_m(x)\equiv\prod_{j=1}^m(1+x^{F_{j+1}})\ (\mathrm{mod}\ x^{F_{m+2}}-1)\) ​​(proposition \ref{prop:fold-multiplicity-group-algebra}), which is equivalent to the convolution decomposition of \(d_m\) on the cyclic group algebra; its Fourier transformation is completely factorized (theorem \ref{thm:fold-spectrum-factorization}).
  thus
  \(
  F_{m+2}\mathsf{Col}_m=\sum_k a_m(k)^2
  \)
  Can be regarded as a finite Riesz product energy average (defined \ref{def:fold-normalized-amplitude}) driven by the Fibonacci frequency, thereby strictly transforming the collision limit problem into an "amplitude distribution" problem rather than a "zero point geometry" problem.

  \item ​​\textbf{Auditable real parameter threshold given by real degradation point: local expansion window of the output potential. }
  In the output potential-parameter weighting of the real input \(40\) ​​state kernel, the positive real degradation point segments \(u>0\) into phase diagram intervals with unchanged spectral branch ordering (note \ref{rem:real-input-40-output-potential-degeneracy-phase}; table \ref{tab:real_input_40_output_potential_spectral_collision_roots}).
  The nearest degenerate point to \(u=1\) ​​gives real parameter thresholds and reads out a uniform conservative radius in \(\theta=\log u\) coordinates
  \[
  |\theta|<\theta_0,\qquad \theta_0=0.0533477827\ldots,
  \]
  Acts as an auditable valid domain seal in the real parameter direction for local fitting/unwrapping around \(\theta=0\).

  \item ​​\textbf{The pressure duality law of self-dual completion: the structural origin of the total annihilation of odd-order cumulants. }
  Under self-dual completion, the pressure can be written as \(P(\theta)=\theta/2+\log y(2\cosh(\theta/2))\) ​​(proposition \ref{prop:weighted-completion-Q}), so that \(\Lambda(\theta):=P(\theta)-\theta/2\) is a dual function (theorem \ref{thm:sync-kernel-gallavotti-cohen}).
  Equivalently \(P(\theta)-P(-\theta)=\theta\), so all odd-order derivatives of \(P\) in \(\theta=0\) except the linear terms are strictly zero; therefore any centered fluctuation expansion derived by \(P\) has its first non-Gaussian correction must fall in the even Hermite sector, and all odd-order skew terms are excluded at the structural level.

  \item \textbf{Newman threshold for the minimal three-state “unity$\to$broken” effective kernel: closed phase transition prototype. }
  For the three-state effective nuclear family \(A_{\mathrm{eff}}(\alpha)=U+\alpha R\) ​​(Proposition \ref{prop:pom-a2-gut-spectral-splitting}), the nuclear Newman shift \(\delta(\alpha)=\log(\Lambda(\alpha)^2/\rho(\alpha))\) has a unique critical point \(\alpha_\star>1\) and "negative real slow mode welting" occurs, and this threshold has a complete algebraic certificate (Proposition \ref{prop:pom-a2-gut-newman-threshold}):
  \[
  \alpha_\star=\frac{x_\star(x_\star^3-x_\star^2+1)}{x_\star^2-x_\star+2}=2.3609261183\ldots,
  \]
  Among them, \(x_\star>1\) ​​is the only positive real root of \(x^6-x^5-4x^4+8x^3-6x^2-4x+2=0\).

  \item \textbf{Hankel Witnessed \texorpdfstring{$p$}{p} - Progressive Rigidity: Activation threshold for primes in minimum realized dimension certificates. }
  The non-zero Hankel principal not only gives a rank lower bound, but also carries an irreducible \(p\)-progressive signal: in the least two cases
  \(
  \det(H_{k=4})=2^{17}\cdot 3^2
  \)
  and
  \(
  \det(H_{k=5})=2^{13}\cdot 3^5\cdot 5^7
  \)，
  Thus \(p=5\) ​​makes its first explicit entry at \(k=5\) with index \(7\) (note \ref{rem:pom-hankel-stiffness-p5}).

  \item \textbf{Module \(2\) ​​Automatic sequence structure of shadow: differential annihilation and periodic upper bound. }
  On the resonance window \(q=9,\dots,17\), the shadow of the characteristic polynomial modulus \(2\) collapses to
  \(
  P_q(\lambda)\equiv \lambda^{a_q}(\lambda+1)^{b_q}\ (\mathrm{mod}\ 2)
  \)
  And induce annihilation \(E^{a_q}(E+1)^{b_q}S_q\equiv 0\ (\mathrm{mod}\ 2)\).
  Therefore, \(S_q(m)\bmod 2\) ​​is an \(2\)-automatic sequence and the period satisfies \(T_q\mid 2^{\lceil\log_2 b_q\rceil}\), and the specific upper bound is given in table \ref{tab:fold_collision_charpoly_mod2_shadow_q9_17} (note \ref{rem:pom-charpoly-mod2-shadow}).

  \item \textbf{Endpoint cluster rigidity of the elimination result formula: \texorpdfstring{$u$}{u}-adic Degenerate decomposition pegs the endpoints as discrete rational points. }
  The original elimination result of the synchronous core rate curve is \(R_{\mathrm{raw}}(\alpha,u)\), whose \(u\)-adic assignment satisfies \(v_u(R_{\mathrm{raw}})=7\) and has a rigid decomposition \(7=3+4\) (proposition \ref{prop:rate-resultant-uadic-split-7}; table \ref{tab:sync_kernel_rate_curve_resultant_degree}).
  First item structure mandatory
  \[
  R_{\mathrm{raw}}(\alpha,u)=\pm\,u^7\,\alpha(\alpha-1)(4\alpha-1)^4+\text{Higher order term},
  \]
  Thus, the endpoint clusters (quadruple) of \(\alpha=0,1\) ​​and \(\alpha=\tfrac14\) are promoted to discrete rational points inevitably produced by degenerate geometry; and the corresponding endpoint cluster of \(\alpha=\tfrac34\) is symmetrically forced to appear under the dual closure \((u,\alpha)\mapsto(1/u,1-\alpha)\).
\end{enumerate}

\paragraph{Resonance window and constant layer: from CM phase locking to Abel-normalized integrable kernel}
\begin{enumerate}
  \item \textbf{CM phase locking of the resonant complex spectrum: spontaneous development of the virtual quadratic domain \texorpdfstring{$\QQ(\sqrt{-15})$}{Q(sqrt(-15))}. }
  In the resonance window \(q=9,\dots,17\), let \(\mu_q\) be the maximum modulus non-real root (take \(\Im(\mu_q)\le 0\)), and set the unit phase
  \(\zeta_q:=\mu_q/|\mu_q|\in\mathbb{S}^1\)。
  make
  \(
  z_\ast:=(-1-\mathrm{i}\sqrt{15})/4
  \)
  is the unit circle root of \(2z^2+z+2=0\), then the table \ref{tab:fold_collision_shadow_spectral_packet_algebraic_phase_residual_q9_17} gives a recalculable phase locking certificate: \(|\zeta_q-z_\ast|\) maintains the magnitude of \(10^{-2}\) in the full window and reaches the magnitude of \(10^{-4}\) in \(q=12\), while the algebraic residual \(\rho_q=|2\zeta_q^2+\zeta_q+2|\) remains of small magnitude (Note \ref{rem:pom-resonance-trimodal-phase-lock}).
  And from the formula \eqref{eq:pom-phase-lock-residual-trace-equivalence}, \(\rho_q\) ​​is equivalent to the magnitude of the real trace deviation \(|(\zeta_q+\zeta_q^{-1})+\tfrac12|\), so the locking strength can be compressed into a single real scalar quantity.
  The unit circle phase of the resonant complex mode can therefore be compressed onto a single algebraic orbit in the imaginary quadratic domain \(\QQ(\sqrt{-15})\), forming a fully intrinsic and auditable CM-type spectral fingerprint.

  \item \textbf{The "universal second-order oscillator" of complex mode fixed frequency: the normalized annihilator given by \texorpdfstring{$2z^2+z+2$}{2z^2+z+2}. }
  Set the normalized shift \(T:=r_{q-2}^{-1}E\) ​​(\(E\) is the \(m\)-direction forward shift operator), then \(\mathcal L_{\mathrm{CM}}(T):=2T^2+T+2\) strictly annihilates the exponential mode of \(\nu=z_\ast,\overline{z_\ast}\); in the resonance window, this operator can be used as a "phase-aware" three-step vibration elimination interface, complementary to the two-period Aitken eliminator (note \ref{rem:pom-cm-annihilator}).
  Equivalently, for the normalized real oscillator \(u(m)=\zeta^{m}+\zeta^{-m}\) ​​(\(\zeta\in\mathbb{S}^1\)) we have
  \(2u(m+2)+u(m+1)+2u(m)=\bigl(2(\zeta+\zeta^{-1})+1\bigr)u(m+1)\)，
  Therefore, its second-order syndrome amplitude is proportional to \(\rho(\zeta)=|2\zeta^2+\zeta+2|\) ​​point by point, thus providing a time-domain audit interface that does not require repeated roots (note \ref{rem:pom-cm-annihilator}).

  \item \textbf{Poisson peak of two-step rescaling point \texorpdfstring{$\Rightarrow$}{=>} Lorentz limit: discrete prelimit of Breit--Wigner contour and "height-width reciprocity". }
  Let \(\eta_q:=|\mu_q|/r_{q-2}\uparrow 1\) ​​and \(a_q:=\mu_q/r_{q-2}\in\mathbb{D}\).
  Then the boundary phase derivative corresponding to the Blaschke factor \(B_q(z)=(z-a_q)/(1-\overline{a_q}z)\) ​​is the Poisson kernel \(P_{\eta_q}(t-\theta_q)\), and converges to the Lorentz kernel \(2/(1+u^2)\) on the scaling window \(t=\theta_q+\varepsilon_q u\) (\(\varepsilon_q=1-\eta_q\));
  At the same time, the Poisson core area is always \(2\pi\), and the peak height magnitude is \(\varepsilon_q^{-1}\) (note \ref{rem:pom-resonance-poisson-lorentz-prelimit}).
  And by the audit reading of table \ref{tab:fold_collision_shadow_spectral_packet_q9_17}, if the step-by-step drift is defined
  \(
  \Delta\theta_q:=|\arg(\mu_{q+1})-\arg(\mu_q)|
  \)
  (\(q=9,\dots,16\)), then there is unanimous dominance
  \(
  \max_{9\le q\le 16}\Delta\theta_q/\varepsilon_q
  =0.0029/0.0204<0.143
  \), thus \(\Delta\theta_q\le 0.143\,\varepsilon_q\) ​​(note \ref{rem:pom-resonance-poisson-lorentz-prelimit}).

  Quadruple intersection point of \item \textbf{\texorpdfstring{$q=12$}{q=12}: synchronization of four audit invariants. }
  Within the resonance window, the pivot point \(q=12\) simultaneously satisfies: (i) \(|\zeta_q-z_\ast|\) takes the minimum of the full window (table \ref{tab:fold_collision_shadow_spectral_packet_algebraic_phase_residual_q9_17}); (ii) negative real lateral root channel switching \(|\lambda_q^-|/r_{q-2}\) crosses the threshold at this point \(1\) (table \ref{tab:fold_collision_shadow_spectral_packet_q9_17}); (iii) resonance gap \(\Delta(q)\) The only one in the verified sequence is \(0\) (note \ref{rem:pom-delta-cq-threshold}); (iv) The embedding flag of \(\chi_{A_2}\mid P_{12}\ (\mathrm{mod}\ 11)\) appears in the embedding slice of module \(11\) (proposition \ref{prop:pom-charpoly-modp-a2-embedding}; table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}, and the embedding column of \(q=13,15\) in the same table \(0\)).
  This quadruple synchronization identifies \(q=12\) ​​as a structural turning point: its synchronicity comes from four independent audit pipelines of two-step rescaling, algebraic phase residuals, minimum recursion order, and finite field embedding (note \ref{rem:pom-resonance-window-final-structural-conclusions}).

  \item \textbf{Dirichlet's drift law of real part coordinates: deterministic approximation of the resonant complex pole strip to \texorpdfstring{$\Re(s)=1$}{Re(s)=1}. }
  Define the Dirichlet real part coordinate \(\sigma_q(\xi):=\log|\xi|/\log r_q\) for any root \(\xi\).
  Within the resonance window, \(\sigma_q(\mu_q)>1/2\) ​​rises monotonically (table \ref{tab:fold_collision_shadow_spectral_packet_q9_17}), forming a non-trivial pole strip approaching \(\Re(s)=1\); its drift can be decomposed into the sum of the two-step renormalization ratio and amplitude correction (note \ref{rem:pom-shadow-spectral-packet-q9-17})
  \[
  \sigma_q(\mu_q)=\frac{\log r_{q-2}+\log(|\mu_q|/r_{q-2})}{\log r_q}.
  \]
  Therefore, under the endpoint assumptions \(|\mu_q|/r_{q-2}\to 1\) ​​and \(R_q:=r_q/r_{q-2}\to\varphi\), the complex mode gap index \(g_q^{(c)}:=1-\sigma_q(\mu_q)\) satisfies the condition asymptotically \(g_q^{(c)}\sim 2/q\), giving a falsifiable drift velocity constant uniquely determined by the endpoint \(\varphi\).

  \item \textbf{The discriminant \texorpdfstring{$-15$}{-15}’s module \texorpdfstring{$p$}{p} Split selection law: The cross-prime falsifiable fingerprint of the phase-locked quadratic expression. }
  The divisibility of the quadratic \(2z^2+z+2\) ​​on \(\mathbb{F}_p\) is controlled by \(\bigl(\frac{-15}{p}\bigr)\), and the degeneracy of multiple roots occurs at the divergent prime \(p\in\{3,5\}\);
  Therefore, if the phase-locked 2D block is indeed governed by this quadratic equation, the modulo \(p\) ​​factorization fingerprint across primes should show a systematic splitting/lazy dichotomy (Proposition \ref{prop:pom-phase-lock-discriminant-minus15-splitting} and Note \ref{rem:pom-phase-lock-discriminant-minus15-prediction}).

  \item ​​\textbf{Representation theoretic meaning of finite field embedding flag: low-order kernels appear as local subdynamics of higher-order kernels. }
  If \(\chi_{A_2}(\lambda)\mid P_q(\lambda)\) ​​exists in \(\mathbb{F}_p[\lambda]\), then an embedded subdynamics of the same type as \(A_2\) appears in the recursive state space of module \(p\) (represented by a three-dimensional invariant; corollary \ref{cor:pom-charpoly-modp-a2-subdynamics}).
  The verified slice gives the \(A_2\)-shadow embedding flag at \((q,p)=(14,7)\) ​​(proposition \ref{prop:pom-charpoly-modp-a2-embedding}), thus providing a finite field representation theory interface that can be used for local-global comparisons.

  \item \textbf{Hard fingerprint of discriminant element support: the forced divisibility spectrum of \(\{2,7,11\}\). }
  Define \(\mathrm{Disc}^\ast(P_q):=\mathrm{Disc}(P_q^\ast)\) for \(P_q(\lambda)=\lambda^{a_0}P_q^\ast(\lambda)\) ​​(\(P_q^\ast(0)\ne 0\)).
  Within the verified slice of the resonance window, the forced divisibility of \(p\mid \mathrm{Disc}^\ast(P_q)\) ​​can be directly deduced from the repeat? non-square free flag; the shadow modulo \(2\) gives the full window \(2\mid \mathrm{Disc}^\ast(P_q)\), and the slices modulo \(7\) and modulo \(11\) are forced to appear on several \(q\) respectively (note \ref{rem:pom-discriminant-prime-support}).

  \item \textbf{Cyclic collapse in composite modulus: \texorpdfstring{$q=13$}{q=13} The final period in module \texorpdfstring{$182$}{182} is divisible by \texorpdfstring{$168$}{168}. }
  By the periodic upper bound of module \(2/7/13\) ​​and the Chinese remainder structure, after aligning the common transients we have \(\mathrm{Per}_{182}(q=13)\mid 168\) (note \ref{rem:pom-mod182-period-collapse-q13}).

  \item \textbf{Finite field factor extraction law: \texorpdfstring{$\chi\mid P_q$}{chi|Pq} \texorpdfstring{$\Rightarrow$}{=>} There is an \texorpdfstring{$\chi$}{chi}-subrecursion that can be explicitly constructed. }
  If \(P_q\equiv \chi Q\) is in \(\mathbb{F}_p[\lambda]\), then taking \(S_q^{(\chi)}:=Q(E)S_q\) for any solution sequence \(S_q\) satisfies \(\chi(E)S_q^{(\chi)}\equiv 0\pmod p\) (proposition \ref{prop:pom-modp-factor-extraction}).
  For the embedded flag \(\chi_{A_2}\mid P_q\), the \(A_2\)-shadow subrecursion can be extracted accordingly (note \ref{rem:pom-a2-subrecurrence-extraction}).

  Jordan-order splitting of \item \textbf{module \texorpdfstring{$11$}{11}: The irreducible modulation order is strictly different between \texorpdfstring{$q=12$}{q=12} and \texorpdfstring{$q=13,15$}{q=13,15}. }
  From the table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}, \(q=12\) ​​contains the square of the quadratic irreducible factor while \(q=13,15\) only contains the linear square factor; therefore, the Jordan modulation of the two has different minimum order lower bounds at the irreducible level, and can be used as the internal subdivision criterion of the resonance window without seeking the complex spectrum (note \ref{rem:pom-charpoly-mod11-modulation}).

  The meaning of the second-order constant of \item \textbf{thick-tailed loss function \texorpdfstring{$\delta(q)$}{delta(q)}: a global scalar invariant determined by the minor term of \texorpdfstring{$r_q$}{rq}. }
  Paley--Zygmund lower envelope naturally induces non-negative gaps
  \[
  \delta(q):=\log\varphi+\log r_{2q}-2\log r_q\ge 0
  \]
  (Corollary \ref{cor:pom-spectrum-intermittency-deficit}).
  If the endpoint expansion \(\log r_q=\frac{q}{2}\log\varphi+a+o(1)\) ​​is true, it can be obtained directly by substitution
  \[
  \lim_{q\to\infty}\delta(q)=\log\varphi-a,
  \]
  Therefore \(\delta(q)\) ​​not only measures the loss of the "fat-tail guarantee index", but also inverts the second-order free energy constant \(a\) at the endpoints of the collision spectrum, thus forming a global scalar fingerprint that can be compared across versions.

  \item \textbf{Spectral sensitivity of the prime-orbit Mertens constant: the amplified dimension given by the product structure of \texorpdfstring{$\chi_A'(r)$}{chi'(r)}. }
  For the finite state collision kernel \(A\) ​​(SFT adjacency matrix), its Abel-normalized main pole residue constant satisfies the closed formula
  \[
  C(A)=\frac{r^{d-1}}{\chi_A'(r)}=\prod_{\lambda_j\in\sigma(A)\setminus\{r\}}\Bigl(1-\frac{\lambda_j}{r}\Bigr)^{-1},
  \]
  Thus \(C(A)\) is determined only by \((\chi_A,r)\) and remains unchanged under any non-negative integer realization that keeps the same \(\chi_A\); and for \(A_2,A_3,A_4\) \(C(A_q)\) can be upgraded to the unique real root certificate of the minimum polynomial with integer coefficients, thus forming a closed loop of "residue constant \(\Rightarrow\) algebraic equation \(\Rightarrow\) auditable certificate chain" (see \S\ref{subsec:pom-s4}).

  \item \textbf{A fully integrable Abel-normalized \texorpdfstring{$\zeta$}{zeta} core: the pole set falls on the \texorpdfstring{$\QQ(\sqrt5)$}{Q(sqrt5)} unit group orbit. }
  Under the Abel-normalized coordinates \(r=\lambda z\) of the real input \(40\) ​​state kernel, the normalized \(\widetilde{\zeta}_M(r)\) has a fully decomposed determinant expression, and all its singularities can be explicitly enumerated as
  \(
  r\in\{1,\pm\lambda,\lambda^2,\varphi^3,-\varphi\}
  \)
  And except \(r=1\), all satisfy \(|r|>1\), so there is no secondary pole obstacle in \(|r|<1\); and the main pole residue constant gives the closed algebraic value
  \(
  C=(47+21\sqrt5)/100
  \)
  （\S\ref{app:real-input-40-abel}）。
\end{enumerate}

\paragraph{Endpoint cluster—center section—spectral concentration: from four-branch single value to \texorpdfstring{$p^{-6}$}{p^-6} decay law}
In order to unify and compress the three types of certificates "endpoint degradation jet - center section root disk - high-order collision nuclear spectrum concentration" into a directly reusable audit interface, the following assertions give a set of complementary verifiable propositions (the certificates are distributed in the synchronous nuclear weighted elimination appendix and collision spectrum table):
\begin{enumerate}
  \item ​​\textbf{Four-branch singularization of endpoint clusters and local dihedral simple groups. }
  On the degenerate end of the synchronous core rate curve \(u\to 0\), the characteristic curve satisfies \(F(\lambda,0)=\lambda^5(\lambda-1)\), and its fivefold zero-root package is rigidly split into four Puiseux branches and one analytic small root branch (proposition \ref{prop:rate-resultant-uadic-split-7}):
  \[
  \lambda(u)=\zeta\,u^{1/4}+O(u^{1/2})\quad(\zeta^4=1),
  \qquad
  \lambda(u)=-u+O(u^2).
  \]
  Therefore, the analytic continuation around \(u=0\) ​​induces the four-cycle unification of \(u^{1/4}\mapsto i\,u^{1/4}\) on the four Puiseux branches; and the correct unification variable of the flanking endpoint cluster must be \(u=s^4\) (\(s=u^{1/4}\)), thus forming a four-page coverage of the same \(u>0\) \(s\in\{\pm u^{1/4},\pm i\,u^{1/4}\}\) (note \ref{rem:rate-algebraic-elimination-structure}).
  Combined with the effect of complex conjugation on branches, the endpoint cluster locally carries a dihedral monodromy containing \(C_4\) ​​(the local monodromy is at least \(D_4\)); therefore any implementation that misjudges this endpoint cluster as a "square root divergence" will produce erroneous branch switching around \(u=0\).

  \item ​​\textbf{No real singularity in the central section forces the nearest singularity to be pure; the nearest root gives auditable convergence disk and coefficient oscillation fingerprint. }
  \(Q(u)\in\ZZ[u]\) in the central section decomposition formula \(R(\tfrac12,u)=-(u-1)^6Q(u)/64\) ​​has strict palindromic self-reciprocity \(Q(u)=u^{16}Q(1/u)\), and Sturm counting gives that \(Q\) has no zero point on the real axis, so \(Q(u)>0\) holds for all \(u\in\RR\) (proposition \ref{prop:rate-center-Q-reciprocal-no-real}).
  Therefore, around the local analysis of \(u=1\), its convergence obstacle must be determined by the complex singular point pair; the nearest singular point distance
  \(
  R_\star=\min\{|u-1|:\ Q(u)=0\}
  \)
  A verifiable numerical certificate has been given with the nearest root \(u_\star\) ​​(corollary \ref{cor:rate-center-Rstar}):
  \[
  R_\star\approx 1.0507607822,
  \qquad
  u_\star\approx -0.0492815273\pm 0.0557359652\,\mathrm{i}.
  \]
  According to the standard singularity analysis/coefficient transfer principle, any Taylor coefficient sequence generated with this central section (or a completed determinant constructed from it) must have a quasi-periodic oscillation locked by \(\arg(u_\star-1)\) ​​at a high order; its frequency and attenuation rate together constitute an auditable spectral fingerprint under the "nearest singularity" scope.

  \item \textbf{Minimum modulus barrier: \texorpdfstring{$p=5$}{p=5} is the only prime exclusion point for the local expansion of the central section. }
  Let \(\omega_p=e^{2\pi i/p}\), then \(|\omega_p-1|=2\sin(\pi/p)\).
  It can be obtained from the nearest singularity distance certificate \(R_\star\)
  \(
  |\omega_5-1|>R_\star
  \)
  and
  \(
  |\omega_7-1|<R_\star
  \), and by monotonicity it follows that for all \(p\ge 7\) ​​there is \(|\omega_p-1|<R_\star\) (and by corollary \ref{cor:rate-center-omega-threshold}).
  Therefore, any local analytic expansion that only relies on \(u=1\) ​​must be in a strict convergence region at the unit root twist of all primes \(p\ge 7\), while \(p=5\) cannot in principle be controlled by this local aperture, thus giving a minimum and unique prime exclusion point.

  \item \textbf{\texorpdfstring{$q$}{q}-root scale spectrum concentration: \texorpdfstring{$\Lambda_q^{1/q}$}{Lambda^(1/q)} versus \texorpdfstring{$r_q^{1/q}$}{r^(1/q)} \texorpdfstring{$\sqrt{\varphi}$}{sqrt(phi)}'s cunt. }
  For the Perron root \(r_q\) of the collision moment-kernel \(A_q\) ​​and the subspectral module \(\Lambda_q\) (table \ref{tab:fold_collision_moment_spectrum_k18_23} and table \ref{tab:fold_collision_kernel_rh_scan_q18_23}), define
  \[
  b_q:=r_q^{1/q},\qquad a_q:=\Lambda_q^{1/q}.
  \]
  From the extreme order endpoint \(\lim_{q\to\infty}r_q^{1/q}=\sqrt{\varphi}\) ​​(proposition \ref{prop:pom-rq-qinfty-endpoint}), \(\sqrt{\varphi}\) can be regarded as a spectral endpoint constant.
  Within the verified interval \(q=18,\dots,23\), the numerical certificate gives a strict limit of \(a_q<\sqrt{\varphi}<b_q\); for example
  \[
  (a_{18},b_{18})\approx(1.26368,1.28566),\qquad
  (a_{23},b_{23})\approx(1.26740,1.28011),
  \qquad
  \sqrt{\varphi}\approx 1.27202,
  \]
  It is shown that the two perform monotonic contraction of \(\sqrt{\varphi}\) on the \(q\)-root scale, thereby unifying the "cross-line spectral phase transition" and "endpoint constant" to the same spectral concentration orbit.

  \item \textbf{Auditable monotonic law of spectral aggregation index: \texorpdfstring{$\Lambda_q/r_q$}{Lambda/r} is close to \(1\) by \texorpdfstring{$\exp(-qg_q^{(\Lambda)})$}{exp(-qg)}. }
  Within the same verified interval, define the spectral aggregation index
  \[
  g_q^{(\Lambda)}:=\frac{1}{q}\log\frac{r_q}{\Lambda_q}\,,
  \qquad
  \frac{\Lambda_q}{r_q}=\exp\!\bigl(-q\,g_q^{(\Lambda)}\bigr).
  \]
  Directly reading from the precise data of table \ref{tab:fold_collision_kernel_rh_scan_q18_23} \(g_q^{(\Lambda)}\) ​​strictly decreases on \(q=18,\dots,23\) (from \(g_{18}^{(\Lambda)}\approx 0.017241\) to \(g_{23}^{(\Lambda)}\approx 0.009979\)), equivalently \(\Lambda_q/r_q\) monotonically increases from \(0.733\) to \(0.795\) within this interval.
  Therefore, the "submodal Perron" can not only be described by the Dirichlet real part scope of \(\log\Lambda_q/\log r_q\uparrow 1\), but also quantified by an explicitly verifiable \(g_q^{(\Lambda)}\) sequence; its monotonic decay provides the lowest-cost certificate for judging whether spectral concentration enters the near-degenerate region.

  \item \textbf{Discrete variational threshold for moment order selection: interval inequality characterization of optimal \texorpdfstring{$q^\star(c)$}{q*(c)}. }
  Let \(P(q)=\log r_q\) ​​and note the slope of the secant line \(\beta_q=(P(q)-P(1))/(q-1)\).
  Under the index risk budget \(\varepsilon_m=e^{-cm}\), the certificate index can be written as
  \[
  \gamma^{\mathrm{cert}}(c):=\inf_{q\ge 2}\left(\beta_q+\frac{c}{q-1}\right),
  \]
  And it is derived from discrete convexity that \(q\mapsto\beta_q\) ​​is monotonic and non-decreasing (proposition \ref{prop:pom-pressure-convexity}; and see note \ref{rem:pom-quantile-budget-computable}).
  Any optimal moment order \(q^\star(c)\) ​​satisfies the discrete Euler--Lagrange type threshold inequality (proposition \ref{prop:pom-qstar-discrete-variational-threshold}):
  \[
  (q^\star-1)(q^\star-2)\bigl(\beta_{q^\star}-\beta_{q^\star-1}\bigr)\le c\le q^\star(q^\star-1)\bigl(\beta_{q^\star+1}-\beta_{q^\star}\bigr),
  \]
  Therefore \(c\mapsto q^\star(c)\) ​​is a "phase diagram" with piecewise constant value and overall monotonic non-decreasing value. This characterization elevates “choose \(q\)” from heuristic parameter scheduling to a deterministic threshold law entirely determined by the sequence \(\{r_q\}\).

  \item \textbf{Chebyshev dimensionality reduction of palindromic polynomials: the unit root twist is compressed into a one-variable real polynomial over \texorpdfstring{$[-2,2]$}{[-2,2]}. }
  From \(Q(u)=u^{16}Q(1/u)\) ​​(proposition \ref{prop:rate-center-Q-reciprocal-no-real}), it can be seen that \(u^{-8}Q(u)\) is the invariant of the inversion of \(u\mapsto 1/u\); from the Laurent ring invariant quantum ring identity \(\ZZ[u,u^{-1}]^{u\mapsto 1/u}=\ZZ[u+u^{-1}]\) (proposition \ref{prop:completion-subring-unique-angle}), there is a unique \(\widetilde Q\in\ZZ[v]\) (\(\deg\widetilde Q=8\)) such that
  \[
  Q(u)=u^8\,\widetilde Q\!\left(u+\frac1u\right).
  \]
  And since \(Q(u)>0\) ​​is true for all \(u\), \(\widetilde Q(v)>0\) is true for all \(v\in(-\infty,-2]\cup[2,\infty)\).
  In particular, for any unit root \(\omega_p^k\) ​​we have
  \[
  Q(\omega_p^k)=\omega_p^{8k}\,\widetilde Q\!\left(2\cos\frac{2\pi k}{p}\right),
  \]
  As a result, "the non-degeneracy of the central section/singular point proximity under unit root distortion" is strictly compressed into the value problem of \(\widetilde Q\) ​​on the interval \([-2,2]\), and a shortest computable local--global arithmetic layered interface is given.

  \item \textbf{Derivation of central sixfold flatness \texorpdfstring{$p^{-6}$}{p^-6} Decay law: the invisible order and minimum discernible scale of unit root distortion. }
  The central section decomposition of \(R(\tfrac12,u)=-(u-1)^6Q(u)/64\) ​​and \(Q(1)=8515584\neq 0\) (corollary to \ref{cor:rate-center-unique-real} and note to \ref{rem:rate-center-min-audit}) shows that the first five derivatives at \(u=1\) are strictly annihilated.
  Therefore there is a subject for \(u=\omega_p\) ​​and \(p\to\infty\)
  \[
  R\!\left(\tfrac12,\omega_p\right)
  =-\frac{Q(1)}{64}\,(\omega_p-1)^6+O(p^{-7}),
  \qquad
  \bigl|R\!\left(\tfrac12,\omega_p\right)\bigr|
  =\frac{|Q(1)|}{64}\,(2\sin(\pi/p))^6+O(p^{-7})\asymp p^{-6}.
  \]
  Equivalently, unit root distortion cannot be resolved in any local expansion of fifth order and below; "sixth order" therefore becomes the minimum resolvable order and the minimum scale law for the central section to identify distortion, and directly dominates the related numerical stability and stripping strategies (note \ref{rem:rate-algebraic-elimination-structure}).
\end{enumerate}

\paragraph{Finite part—tomography—endpoint ejection: from \texorpdfstring{$O(\log\varepsilon^{-1})$}{O(log eps^-1)} truncation law to non-square free nilpotent flag}
In order to uniformly compress "the computability of finite part constants - Lucas tomography with minimal data dependence - even jets of self-dual symmetry - nilpotent channels of modulo \(p\) ​​shadows" into a directly reusable audit interface, the following assertions give a set of complementary verifiable propositions (certificates are distributed in the finite part appendix and \(\xi\)/synchronous core/resonance window chapters):
\begin{enumerate}
  \item \textbf{Möbius--Abel's logarithmic precision truncation law for finite part constants: a certifiable algorithm with complexity \texorpdfstring{$O(\log\varepsilon^{-1})$}{O(log eps^-1)}. }
  If \(\zeta_M(z)=\det(I-zM)^{-1}\) ​​resolves in closed disk \(|z|\le \lambda^{-2}\) and \(\zeta_M(0)=1\), let
  \[
  A:=\sup_{0<|z|\le \lambda^{-2}}\frac{|\log\zeta_M(z)|}{|z|}<\infty,
  \qquad
  \log\mathfrak{M}:=\log C+\sum_{m\ge 2}\frac{\mu(m)}{m}\log\zeta_M(\lambda^{-m}),
  \]
  Then there is an explicit tail term bound for any \(K\ge 2\) ​​truncation \(\log\mathfrak{M}^{(K)}\) (proposition \ref{prop:real-input-40-logM-trunc-bound}):
  \[
  |\log\mathfrak{M}-\log\mathfrak{M}^{(K)}|
  \le \frac{A}{(K+1)(\lambda-1)}\,\lambda^{-K}.
  \]
  Therefore, given \(\varepsilon\in(0,1)\), take
  \[
  K\ \ge\ \left\lceil \frac{\log\!\bigl(\tfrac{A}{\varepsilon}(\lambda-1)\bigr)}{\log \lambda}\right\rceil
  \]
  That guarantees \(|\log\mathfrak{M}-\log\mathfrak{M}^{(K)}|\le \varepsilon\);
  Thus, finite part constants can be calculatively calculated by \(K\asymp \log(1/\varepsilon)\) ​​terms in the sense of "logarithmic accuracy" without relying on any primitive orbital enumeration or high-dimensional spectral details.

  \item ​​\textbf{Lucas tomography for three-point evaluation: reconstructing Abel constant differences using only trace samples and giving end-to-end error closure. }
  Under the prime-splitting scope of the real input kernel, the Lucas trace sample \(L_n=\Tr(F^n)\) ​​(satisfying \(L_n=L_{n-1}+L_{n-2}\)) is given by Möbius/Witt inversion
  \(
  \pi_n=p_n(F)
  \)
  and
  \(
  r_n=p_n(F\otimes F)
  \)；
  Then, \(p_n(M)\) is obtained from the selection law modulo \(4\) ​​(note \ref{rem:real-input-40-lucas-pipeline}), and further collapses to the product law \(p_p(M)=L_p\pi_p\) (corollary \ref{cor:real-input-40-prime-length-product}) on odd prime lengths.
  Therefore the Abel constant difference \(\log\mathfrak{M}_M-\log\mathfrak{M}_{\mathrm{in}}\) can be reconstructed only from the values of \(P_F(z)=\sum_{n\ge 1}\pi_n z^n\) at three points \(z\in\{-\lambda^{-1},\pm\lambda^{-2}\}\) (corollary \ref{cor:real-input-40-logM-fast-prime}), and when \(P_F\) is truncated to the \(N\) term, the total error is uniformly sealed by the explicit tail term functional \(\mathrm{Tail}(N;|z|)\) (note \ref{rem:real-input-40-lucas-pipeline}), thereby obtaining an end-to-end auditable compilation pipeline with "minimum data dependence".

  \item ​​\textbf{Quantitative fingerprint of symmetric heavy tails: primitive layer normalized kurtosis and tail enhancement threshold. }
  Under self-dual perfecting symmetry, the centralization scale of primitive hierarchical counting has an explicit cumulant density (theorem \ref{thm:sync-kernel-even-edgeworth-c4}):
  \[
  \sigma^2=P''(0)=\frac{11}{102},\qquad
  \kappa_4=P^{(4)}(0)=\frac{1559}{58956},
  \qquad
  c_4=\frac{\kappa_4}{24\sigma^4}=\frac{1559}{16456}.
  \]
  Therefore, the normalized fourth-order ratio is \(\kappa_4/\sigma^4=4677/2057\), and the normalized kurtosis satisfies
  \[
  \frac{\mathbb{E}[X^4]}{\sigma^4}=3+\frac{\kappa_4}{\sigma^4}=\frac{10848}{2057}\approx 5.274>3,
  \]
  Thus, while the odd-order central moment is strictly zero, there is a strict peak-fat-tail deviation.
  And from the Edgeworth first-order modified polynomial \(H_4(x)=x^4-6x^2+3\) ​​(theorem \ref{thm:sync-kernel-even-edgeworth-c4}) and its derivative identity for the tail probability, we can get that the first-order tail enhancement inevitably occurs when \(|x|>\sqrt3\) (the sign threshold is locked by \(H_3(x)=x^3-3x\)).

  \item \textbf{Medium deviation "no odd terms" phase diagram: the rate function ejection only contains even powers and the quadratic curvature constant is \texorpdfstring{$51/11$}{51/11}. }
  Let \(\alpha(\theta)=P'(\theta)\) ​​and define the rate function by \(I(\alpha)=\sup_\theta(\theta\alpha-P(\theta)+P(0))\).
  Self-duality enforces \(I(\alpha)=I(1-\alpha)\), so that the Taylor jet at the center point \(\alpha_\star=\tfrac12\) contains only even powers (proposition \ref{prop:sync-kernel-moderate-deviation-cauchy-certified}).
  In particular,
  \[
  I(\alpha)=\frac{(\alpha-\tfrac12)^2}{2P''(0)}+O\!\left((\alpha-\tfrac12)^4\right)
  =\frac{51}{11}\Bigl(\alpha-\tfrac12\Bigr)^2+O\!\left((\alpha-\tfrac12)^4\right),
  \]
  The quadratic coefficient is uniquely determined by \(P''(0)=11/102\) ​​(theorem \ref{thm:sync-kernel-even-edgeworth-c4}).
  And at the mid-deviation scale \(|k-n/2|\le n^{2/3}\), the exponential-scale error in jet truncation can be uniformly sealed by the Cauchy certificate for the true analytical radius \(R_\theta\) (proposition \ref{prop:sync-kernel-moderate-deviation-cauchy-certified} and corollary \ref{cor:pressure-analytic-radius}) without the need for additional free parameters.

  \item \textbf{The biphasic spectral gap constant in the zero temperature limit: \texorpdfstring{$\lambda_{\mathrm{sub}}(q)/\lambda(q)\to\sqrt{2/3}$}{lambda_sub/lambda -> sqrt(2/3)} gives a consistent slow mode benchmark. }
  For the arity-charge weighted matrix family \(M(q)\) ​​(\(q>0\)) of the real input kernel, its non-trivial spectrum is controlled by the algebraic curve \(P_7(\Lambda,q)=0\) of degree \(7\) (theorem \ref{thm:real-input-40-arity-charge-det-closed}), so \(q\mapsto\lambda(q)=\rho(M(q))\) is an analytic algebra branch.
  When \(q\to+\infty\) ​​(zero temperature end), the Perron branch satisfies
  \(
  \lambda(q)=\sqrt{3q}+O(1)
  \)
  (Proposition \ref{prop:real-input-40-arity-charge-zero-temp-expansion}), and there is also a positive real growth branch in the same algebraic curve
  \(
  \lambda_{\mathrm{sub}}(q)=\sqrt{2q}+O(1)
  \)
  (Note \ref{rem:real-input-40-arity-charge-large-bias-mechanism}).
  therefore
  \[
  \lim_{q\to\infty}\frac{\lambda_{\mathrm{sub}}(q)}{\lambda(q)}=\sqrt{\frac{2}{3}}<1,
  \]
  A unified slow mode scale constant that still maintains a positive distance from \(1\) ​​in the zero temperature range is given, which can be used as a consistency benchmark for the spectral gap/mixing rate.

  \item \textbf{modulo \texorpdfstring{$p$}{p} Dynamic meaning of non-square free flag: repetition factor \texorpdfstring{$\Leftrightarrow$}{<->} nilpotent error channel. }
  In finite field slicing, decompose the recursive characteristic polynomial into
  \(P_q(\lambda)\equiv\prod_i f_i(\lambda)^{e_i}\)。
  If a non-zero square factor occurs (\(e_i\ge 2\) ​​and \(f_i\neq\lambda\) exist), the corresponding companion dynamics contains non-semi-single Jordan blocks, so a \((n^j\lambda^n)\) type of "periodic term superposition of low-order polynomial drift" must appear in the solution space (note \ref{rem:pom-charpoly-modp-audit-method}; note \ref{rem:pom-charpoly-mod11-modulation}).
  In the verified table, \(\mathrm{repeat?}=1\) in model \(7\) and \(q=12\) in model \(11\) both appear \(\mathrm{repeat?}=1\) (table \ref{tab:fold_collision_charpoly_mod7_selection_q13_15} and table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}), and \(\chi_{A_2}\mid P_q\) appears simultaneously at \((q,p)=(14,7)\) and \((12,11)\) Embedding flags (Corollary \ref{cor:pom-charpoly-modp-a2-subdynamics}), thereby juxtaposing two types of local fingerprints, "zero-error channel" and "low-order shadow subdynamics" under the same module \(p\) auditing scope.

  \item ​​\textbf{Blaschke Identifiability of endpoint jets: Finite order derivatives give weighted moments of offset and height from the critical line. }
  Under the finite Blaschke setting, the endpoint radial absorption profile \(\mathcal{A}_B\) ​​satisfies the low-order jet at \(r=1\) (theorem \ref{thm:xi-endpoint-absorption-jet-moments}):
  \[
  \mathcal{A}_B(1)=\sum_k \delta_k,\qquad
  \mathcal{A}_B'(1)=\sum_k \delta_k(\delta_k-1),\qquad
  \mathcal{A}_B''(1)=\frac12\sum_k \delta_k\bigl(3(\delta_k-1)^2-\gamma_k^2\bigr).
  \]
  When \(\delta_k\in(0,\tfrac12)\) ​​there is \(\mathcal{A}_B'(1)<0\), while \(-\tfrac12\sum_k\delta_k\gamma_k^2\) in the second-order term gives a second-order suppressed response to large heights.
  Therefore \((\mathcal{A}_B(1),\mathcal{A}_B'(1),\mathcal{A}_B''(1))\) ​​constitutes a finite-dimensional "statistical section": it simultaneously distinguishes the total amount of offset, offset dispersion and height squared weighted penalty, thereby compressing the zero-point set data into a reviewable finite-dimensional measurement invariant.

  \item \textbf{Minimum auditable order of unit root distortion: non-prime threshold of \texorpdfstring{$p_\star=6$}{p*=6} and isolation of prime \texorpdfstring{$p=5$}{p=5}. }
  For the unit root twist \(u=\omega_p=e^{2\pi i/p}\), the necessary and sufficient conditions for the Taylor/cumulative expansion with \(\theta=\log u\) as the local parameter to be used as a convergence algorithm at this point are \(2\pi/p<R_\theta\) (corollary \ref{cor:sync-kernel-unit-root-twist-threshold}).
  In simultaneous nuclear output potential weighting, the nearest divergence point certificate gives \(R_\theta\approx 1.0709533953\) ​​and derives the minimum threshold \(p_\star=6\) (corollary \ref{cor:pressure-unit-root-modulus-threshold}).
  This threshold is not a prime threshold; therefore in the prime warp, \(p=5\) ​​becomes the first isolated point for which convergence cannot in principle be guaranteed by this local aperture, while \(p\ge 7\) automatically falls into the auditable convergence region (and is consistent with the central section root disk threshold chain \(\ref{cor:rate-center-omega-threshold}\)).
\end{enumerate}

\paragraph{Spectral closure of finite part constants and distortion exponents: a unified certificate chain for derivative reconstruction, Möbius--Abel truncation and cyclotomic elimination}
Under the determinant scope of the finite state kernel, both the finite part of the constant layer and the worst-case mixing exponent of the modular \(m\) ​​distortion can be compressed into integer coefficient algebraic objects: the dominant pole residue can be completely reconstructed by the characteristic polynomial derivatives, the Möbius--Abel series gives a certifiable truncation error bound, and the completion and cyclotomic resultant reduce the spectral radius problem of the unit root distortion to the minimum modular root determination of the unary integer coefficient polynomial. Its concluding interface can be summarized as:
\begin{enumerate}
  \item \textbf{Spectral closure and algebraic auditing of principal pole residue constants: Complete reconstruction by \texorpdfstring{$\chi_A'(r)$}{chi'(r)}. }
  For any original non-negative adjacency matrix \(A\), let \(\chi_A(\lambda)=\det(\lambda I-A)\) (first one, \(\deg\chi_A=d\)), and the Perron root \(r=\rho(A)\) is a single root; let \(\zeta_A(z)=\det(I-zA)^{-1}\), \(z_\star=1/r\), and define
  \(
  C(A):=\lim_{z\to z_\star}(1-rz)\zeta_A(z)
  \)。
  but
  \[
  \boxed{\;
  C(A)=\frac{r^{d-1}}{\chi_A'(r)}
  =\prod_{\lambda\in\sigma(A)\setminus\{r\}}\left(1-\frac{\lambda}{r}\right)^{-1}\;}
  \]
  (Proposition \ref{prop:finite-part-residue-reduced-determinant}; see also \S\ref{subsec:pom-s4}).
  Therefore \(C(A)\) ​​is invariant under any non-negative integer realization that maintains the same \(\chi_A\); and for the reproducible calculation example of \(A_2,A_3,A_4\), \(C(A_q)\) can be further upgraded to the unique real root certificate of the minimum polynomial with integer coefficients (\S\ref{subsec:pom-s4}), thus encapsulating the constant output as a purely algebraic audit object.

  \item ​​\textbf{Möbius--Abel Certificate in fast formulas and exponential precision truncation: Finite part constants are computable and certifiable. }
  Under the general setting that \(\zeta_M\) ​​is resolved within \(|z|\le z_\star^{\,2}\) and \(\zeta_M(0)=1\), the Möbius--Abel representation of finite part constants is the same as the explicit tail term bound as proposition \ref{prop:real-input-40-logM-trunc-bound}; the key point is that only limited sampling of the interior of the convergence disk can simultaneously give high-precision values ​​and auditable error budgets, thus promoting \(\mathsf{M}=\gamma+\log\mathfrak{M}\) to a stable scalar certificate output.

  \item ​​\textbf{Constant layer Artin decomposition: trivial factors can be completely stripped off explicitly, and the finite part mechanism only relies on non-trivial spectral blocks. }
  If \(\Delta(z)=\det(I-zM)\) ​​decomposes into \(\Delta(z)=\Delta_{\mathrm{triv}}(z)\Delta_{\mathrm{nt}}(z)\) where \(\Delta_{\mathrm{triv}}(z)=(1-z)^a(1+z)^b\) and the dominant pole \(z_\star\in(0,1)\) comes from the simple zeros of \(\Delta_{\mathrm{nt}}\), then Abel’s finite part constant satisfies strict additive decomposition
  \[
  \boxed{\ \log\mathfrak{M}=\log\mathfrak{M}_{\mathrm{nt}}+(a-b)z_\star+b z_\star^2\ }
  \]
  (Proposition \ref{prop:real-input-40-reduced-mertens}).
  On the real input \(40\) ​​state kernel, this stripping is further refined into a three-factor constant decomposition of even/twist/triv (note \ref{rem:real-input-40-logM-artin-factorization}), thereby accurately attributing the constant layer difference to the finite-dimensional mechanism representing the channel and non-trivial spectral block.

  \item ​​\textbf{Weighted Ihara--Witt Euler product is congruent with Dwork type: prime-shadow generates Witt vectors with integer coefficients. }
  For the two-variable Ihara \(\zeta\)-function \(Z_K(u,t)=\det(I-tB(u))^{-1}\) of the weighted Hashimoto operator \(B(u)\), there is an integer coefficient primitive Witt coordinate \(q_n(u)\in\ZZ[u]\) such that
  \(
  Z_K(u,t)=\prod_{n\ge 1}(1-q_n(u)t^n)^{-1}
  \)
  Strictly established; and the ghost component \(b_n(u)=\Tr(B(u)^n)\) ​​satisfies the necklace divisibility law and Dwork type congruence at the same time
  \(b_{p^k}(u)\equiv b_{p^{k-1}}(u^p)\ (\mathrm{mod}\ p^k)\)
  (Note \ref{cor:big-witt-audit-guardrails}; proposition \ref{prop:witt-necklace-dwork-R}).
  Therefore, the prime-like (Euler product) side and the trace/spectrum side are unified into a machine-verifiable exchange graph at the whole coefficient level, and provide a structural entrance for subsequent \(p\)-advanced analysis objects.
  On the other hand, cyclic block trace-like promotion gives a constructive surjection of the Euler product to the determinant \(\zeta\) ​​(corollary \ref{cor:cyclic-euler-product}), so the "writable determinant/Euler product" form alone does not deduce any RH-spectrum-like constraints; non-trivial structures can only come from additional inputs such as Perron--Frobenius positivity, local transfer constraints and auditable channel selection laws.

  \item \textbf{Structured dimensionality reduction with self-dual elimination: six degenerate points and \texorpdfstring{$1/6$}{1/6} critical inversion index. }
  The normalized elimination polynomial of the synchronization core rate curve satisfies self-dual symmetry \(R(\alpha,u)=u^{22}R(1-\alpha,1/u)\), and sixfold aggregation appears in the \(\alpha=\tfrac12\) section
  \(R(\tfrac12,u)=-(u-1)^6Q(u)/64\) ​​(corollary \ref{cor:rate-center-unique-real}).
  Therefore the inverse solution \(u=u(\alpha)\) ​​has scale near the center point
  \(
  |u-1|\asymp|\alpha-\tfrac12|^{1/6}
  \)
  (Note \ref{rem:unit-root-final-spec}; corollary \ref{cor:rate-duality-center-6-3x2}), and the dimensionality reduction of "the cubic + quadratic radical continuation of \(X\)" can be realized under the symmetric coordinates \(X=(2\alpha-1)^2\), \(s=u+1/u\) (corollary \ref{cor:rate-duality-center-6-3x2}).

  \item \textbf{cyclotomic elimination: modulo \texorpdfstring{$m$}{m} Resultant certificate of residue-mixing exponent and conjecture of degree rule. }
  For residue mixing modulo \(m\), primitive counts can be expressed by unit root discrete Fourier sampling as
  \(
  p_{n,a}^{(m)}=\frac1m\sum_{r=0}^{m-1}\omega_m^{-ar}\,p_n(\omega_m^r),\qquad \omega_m:=e^{2\pi i/m}
  \)；
  Its error exponent is controlled by the worst-case twisted Perron roots \(\rho_m=\max_{1\le r\le m-1}|\lambda(\omega_m^r)|\) ​​and can be precisely characterized by the cyclotomic resultant polynomial \(R_m(w)\in\ZZ[w]\) as
  \(
  \rho_m=1/\min\{|w|:R_m(w)=0\}
  \)
  (Proposition \ref{prop:sync-cyclotomic-elimination}; table \ref{tab:sync_kernel_cyclotomic_elimination_summary}).
  Based on the output data of \(m=3,\dots,12\), this paper proposes a testable conjecture on the order of magnitude (see conjecture \ref{conj:sync-cyclotomic-elimination-degree-law}).

  \\item \\textbf{True analytic continuation radius under unit-circle twisting: the nearest complex bifurcation point determines the nonperturbative convergence-domain guardrail. }
  For the local expansion of the unit circle distortion \(\theta\mapsto\log|\lambda(e^{i\theta})|\), the true analytical radius is nailed by the nearest complex bifurcation point (note \ref{rem:sync-hatdelta-analytic-radius-complex}), and its angular plane coordinates satisfy
  \(
  \theta_\star\approx 1.0556869306\pm 0.1801840156\,i
  \)
  And induce the nearest singular point module length \(|x_\star|\approx 0.4916\) of the half-width coordinate \(x=\tan(\theta/2)\) ​​(table \ref{tab:sync_kernel_hatdelta_nearest_complex_branch_point_tan_half_angle}).
  This radius provides auditable "analytical guardrails" for phase diagram extrapolation and error index predictions based on Taylor/Pad\'e.

  \item \textbf{window-\texorpdfstring{$6$}{6}’s central structure and Levi rigidity: \texorpdfstring{$(\ZZ_2)^3$}{(Z2)^3}’s canonical embedding, endogenous \texorpdfstring{$\ZZ_6$}{Z6} quotient and SM Levi’s minimal closure enforcement. }
  The boundary dyadic readout of window-\(6\) ​​gives the canonical central embedding \((\ZZ_2)^3\hookrightarrow Z(\mathcal{G}^{\mathrm{bin}}_6)\) and imposes a falsifiable central rank constraint on any continuous envelope candidate (note \ref{rem:bdry-center-z2-global-compatibility}).
  The juxtaposition of \(\ZZ_3\) sources with cyclic sectors allows for the construction of endogenous \(\ZZ_6\) quotients, thus interpreting the commonly used global form of the Standard Model \((SU(3)\times SU(2)\times U(1))/\ZZ_6\) as a minimally consistent consequence of discrete output forcing; and when the orbit--Levi skeleton is compatible with the threshold light/heavy splitting requirement to be closed within the same continuous envelope, an SM-type Levi substructure is forced (Proposition \ref{prop:window6-common-refinement-sm-levi}; see also Conjecture \ref{conj:window6-sm-gauge-as-minimal-compact-observable}).
\end{enumerate}

\paragraph{Representing the stripping principle and completion rigidity of the channel: auditable reduction of toy-RH, constant layer and bivariate spectrum}
Under the limited core scope, if the explicit spectral branches and short-period trivial modifications forcedly embedded by the representation channels are not stripped off first, the square root-level error terms and constant-level drifts in some apparent "boundary/cross-boundary" phenomena will inevitably interfere with the RH/Ramanujan type discrimination; and once these channels are structurally stripped through determinant factorization and completion congruence, the remaining parts fall into the auditable spectral bounds and finite generation certificate closure in a strict sense.
\begin{enumerate}
  \item ​​\textbf{toy-RH's only reach comes from symbolic representation channels, not internal resonance. }
  For the real input \(40\) ​​state kernel, the determinant has an exact three-factor decomposition between the tensor skeleton and the symbolic channel (proposition \ref{prop:real-input-40-A0-tensor-hidden}):
  \[
  \det(I-zM)=(1-z)^2(1+z)\,\det\!\bigl(I-z(A_0\otimes A_0)\bigr)\,\det\!\bigl(I-z(-A_0)\bigr).
  \]
  Among them, \(A_0\) ​​is the golden mean skeleton, and the only non-Perron mode touching the square root bound \(|\mu|=\sqrt{\lambda}=\varphi\) is forcibly generated by \(\det(I-z(-A_0))=1+z-z^2\) (note \ref{rem:finite-rh-40-hidden-minusA0}), while \(A_0\otimes A_0\) only contributes the modulus length \(1\) and the subspectrum of \(\varphi^{-2}\), thus strictly satisfying \(|\nu|<\sqrt{\lambda}\).
  Therefore, the "critical line reaching the boundary" is not an accidental resonance of the high-dimensional nuclear structure, but a structural embedding of the symbolic representation channel; after stripping off \(\zeta_{-A_0}\) ​​in the sense of channel projection, the remaining factors satisfy the strict Ramanujan ratio \(<1\), and the square root level oscillation term is eliminated on the observable quantity.

  \item \textbf{The short-period trivial factor only leaves primitive traces in \texorpdfstring{$n=1,2$}{n=1,2}. }
  The Möbius--\(\log\) transformation of the trivial periodic factor \(\zeta_{\mathrm{triv}}=(1-z)^{-2}(1+z)^{-3}\) ​​gives a strictly closed form
  \(P_{\mathrm{triv}}(z)=-z+3z^2\), thus \(p^{\mathrm{triv}}_n=0\ (n\ge 3)\) (proposition \ref{prop:triv-factor-primitive-polynomial}).
  Therefore, for all lengths \(n\ge 3\), the primitive orbital spectrum has nothing to do with the trivial representation in a strict sense; accordingly, the non-trivial part after stripping off \(\zeta_{\mathrm{triv}}\) automatically falls into the exact two-factor structure of \(\zeta_{\mathrm{even}}\) and the symbolic factor \(\zeta_{\chi}\) at the primitive level (note \ref{rem:real-input-40-logM-artin-factorization}).

  \item ​​\textbf{The Abel/Mertens constant of the sign channel is a universal offset independent of internal dynamics. }
  Finite part constant defined at the dominant pole \(z_\star=\varphi^{-2}\) for twist factor \(\zeta_{-F}(z)=(1+z-z^2)^{-1}\)
  \(K_{-F}=\sum_{m\ge 1}\frac{\mu(m)}{m}\log\zeta_{-F}(z_\star^{\,m})\)
  Relies only on the symbolic channel itself and satisfies at the constant level
  \(\log\mathfrak{M}=\log\mathfrak{M}_{\mathrm{even}}+K_{-F}+P_{\mathrm{triv}}(z_\star)\) ​​(note \ref{rem:real-input-40-logM-artin-factorization}; define \ref{def:real-input-40-artin-sign-mertens}).
  Its value is shown in the table \ref{tab:real_input_40_artin_sign_mertens} (\(K_{-F}\approx -0.1348652875\ldots\)), thus providing a constant-level diagnostic scope for "symbol indicating whether the channel exists" without enumerating long tracks.

  \item \textbf{Complete ghost’s Frobenius rigidity and \texorpdfstring{$\ZZ[x]$}{Z[x]} total rational closure. }
  In perfecting coordinates, prime length congruence gives Teichm\"uller type rigidity: \(\widehat a_p(s)\equiv s^p\ (\mathrm{mod}\ p)\) ​​(corollary \ref{cor:completed-teichmueller-congruence}).
  At the same time, the unitary-slice double angle/power semi-group action on the trace coordinates is uniformly localized by the Chebyshev--Adams polynomial \(C_d\), and can be realized on the Cayley rational parameter \(x\) with the pure multiplication and addition recursion of \(\ZZ[x]\) (proposition \ref{prop:chebyshev-adams-fully-rational}); and together with the double angle law of primitive completion, in The Adams (\(\lambda\)-ring) implementation of power substitution is given on \(\ZZ[s]\) (corollary to \ref{cor:primitive-completion-hatp}).

  \item \textbf{arity-charge Explicit spectral branch, zero-charge entropy anchor point and finite generation certificate of two variables \texorpdfstring{$\zeta$}{zeta}. }
  For the arity-charge weighted kernel \(M(q)\), the determinant has a closed-form decomposition \(\det(I-zM(q))=(1+z)(1-qz^2)\,Q_7(z,q)\) (theorem \ref{thm:real-input-40-arity-charge-det-closed}), so that the spectrum contains explicit eigenvalues ​​\(\pm\sqrt q\) and fixed eigenvalues ​​\(-1\) for any \(q\), and induces fully explicit even-odd oscillatory terms (corollary \ref{cor:real-input-40-arity-charge-sqrtq}).
  Therefore, for the spectrum discrimination of the square root bound/like RH, the factor \((1+z)(1-qz^2)\) ​​(equivalent to \((\Lambda+1)(\Lambda^2-q)\)) in this family should first be stripped off; after stripping off, the part of degree \(7\) carries the non-trivial spectral mechanism.
  \(\zeta_0\) ​​of the zero-charge subsystem is also a closed-form rational function, and its Perron root \(\kappa\) gives the algebraic anchor point for the growth rate within the constraints (proposition \ref{prop:real-input-40-arity-charge-zero-zeta}).
  Furthermore, the valid times of \(\det(I-zM(q))\) ​​in the \(z\) direction are \(10\), and the times in the \(q\) direction are \(3\), and can be restored by Newton recursive closing of finite segment primitive data (note \ref{rem:real-input-40-arity-charge-det-q-cubic} and note \ref{rem:real-input-40-arity-charge-newton}), thereby compressing the binary spectrum object into a limited audit certificate.
\end{enumerate}

\input{sections/body/conclusion/para__conclusion-fiber-lattice-modq-profinite-toeplitz}
\input{sections/body/conclusion/para__conclusion-conductor-resonance-ihara-chebotarev-ptypical-linear-response}
\input{sections/body/conclusion/para__conclusion-audit-invisibility-strictification-discretization}
\input{sections/body/conclusion/para__conclusion-anom-coprime-two-point-and-rhsharp-probe-resolution}
\input{sections/body/conclusion/para__conclusion-fiber-spectrum-resolvent-symmetric-mom-escort-euler-orthogonal}
\input{sections/body/conclusion/para__conclusion-pressure-multiplicative-chain-entropy-loss-freezing}
\input{sections/body/conclusion/para__conclusion-sympower-rh-instability-excitation-filter-oracle}
\input{sections/body/conclusion/para__conclusion-structural-anom-rewrite-fractran-ihara-toeplitz-sdp-profinite}
\input{sections/body/conclusion/para__conclusion-dyadic-logdepth-szego-golden-pick-gram-inverse}
\input{sections/body/conclusion/para__conclusion-finite-field-shadow-jordan-hair}
\input{sections/body/conclusion/para__conclusion-horizon-fiber-capacity-renyi4-threshold}
\input{sections/body/conclusion/para__conclusion-arity-charge-endpoints-frobenius-endpoint-tomography}
\subsection{Interface map: four auditable closed loops and proof/semantic separation}\label{subsec:conclusion-interface-map}
\begin{remark}[Separation of proof chain and explanatory semantics]\label{rem:conclusion-proof-vs-semantics}
The following table compresses the main "auditable interface" of the full text into four closed loops: each closed loop only records the input objects within the protocol, the certificate output (theorem/proposition/Corollary/definition) that can be quoted, and the corresponding reproducible scripts and product paths.
Those that appear in the form of theorems/propositions/inferences form the proof chain of the text; the phrases marked as "semantics" in the table are only used to align traditional terms and parameter readings, and are not used as prerequisites for any proof, nor are they used as assumptions in subsequent derivation.
\end{remark}

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\caption{Four closed-loop interface maps: input objects, certificate output and reproducible products. }
\label{tab:conclusion-interface-map}
\begin{tabular}{p{0.13\linewidth} p{0.22\linewidth} p{0.33\linewidth} p{0.32\linewidth}}
\toprule
Closed loop (equivalence chain) & input object (within the protocol) & output certificate (theorem chain anchor) & reproducible script/product (implementation anchor) \\
\midrule
\textbf{Calculation closed loop}\\[-2pt]
ISA \(\Leftrightarrow\) ​​Projection word \(\Leftrightarrow\) Normalized rewriting
&
Arithmetic ISA/microcode program (\S\ref{subsec:pom-isa});
Projected words \(W\in\mathbf{PW}\);
FRACTRAN program (\S\ref{subsec:pom-fractran})
&
Sequence-gated translation characterization FRACTRAN-step on \(\mathcal{P}\) ​​(definition \ref{def:pom-fractran}, proposition \ref{prop:pom-fractran-prime-translation});
The termination of the value layer rewrite \((\mathrm{RZ},\mathrm{RNF})\) ​​has the "end at most once \texttt{PROJ\_NORM}" regular form (definition \ref{def:pom-rewriting-val}, proposition \ref{prop:pom-rewriting-terminating});
And specify the order projection \(P_{\le}\) ​​as the structural boundary (note \ref{rem:pom-rewrite-boundary-order})
&
\texttt{\detokenize{scripts/pom_isa_sim_v2.py}} (note \ref{rem:pom-isa-sim});
\texttt{\detokenize{scripts/exp_pom_rewriting_engine_demo.py}}
\(\to\) ​​table \ref{tab:pom_rewriting_engine_demo} (note \ref{rem:pom-rewriting-engine-demo})
\\
\midrule
\textbf{geometric anchor}\\[-2pt]
window-\(6\) ​​\(\Leftrightarrow\) 6D cut-and-project \(\Leftrightarrow\) \((\ZZ_2)^3\) and \(\delta=34\)
&
window-\(6\) ​​boundary three-axis
\(
X_6^{\mathrm{bdry}}=\{\texttt{100001},\texttt{100101},\texttt{101001}\}
\)；
Poor second layer coverage of dyadic uplift;
Internal phase factor \(T_{\mathrm{int}}\) ​​Module \(2\) Image (semantic)
&
boundary Three dyadic fibers independently generate center embedding \((\ZZ_2)^3\hookrightarrow Z(\mathcal{G}_6^{\mathrm{bin}})\) ​​(Corollary \ref{cor:fold-bin-boundary-center-z2});
The triple identity of \(\delta=34\) ​​is juxtaposed with the second-layer cover/mask/tail branch (corollary to \ref{cor:bdry-delta34-triple-identity});
Internal torus homology anchoring of cut-and-project with phason \(2\)-torsion readout (semantics: note \ref{rem:bdry-tint-h1-cartan-anchor}, note \ref{rem:bdry-delta34-phason-2torsion})
&
\texttt{\detokenize{scripts/exp_window6_c6_orbit_patisalam_seed.py}}
\(\to\) ​​table \ref{tab:fold6_boundary_sheet_pairs} (and see table \ref{tab:fold6_bin_uplift_choice_collapse} for uplift-choice collapse)
\\
\midrule
\textbf{spectrum positive backbone}\\[-2pt]
Carath\'eodory \(\Leftrightarrow\) Toeplitz--PSD \(\Leftrightarrow\) Schur/Verblunsky \(\Leftrightarrow\) CMV
&
Horizon Carath\'eodory Moments of the logarithmic derivative interface \(\mathscr{C}_\zeta\) ​​(Appendix \ref{subsubsec:app-horizon-weyl-carath-toeplitz});
Or single point injection data \(\{\ell_k\}\) ​​(\S\ref{subsubsec:xi-verblunsky-cmv-from-local-jet})
&
RH \(\Leftrightarrow\) ​​Carath\'eodory Positivity \(\Leftrightarrow\) Toeplitz--PSD level (Appendix Theorem \ref{thm:app-horizon-toeplitz-lmi});
Scalarization of Toeplitz certificates and Verblunsky coefficient generation (theorem \ref{thm:xi-verblunsky-from-local-jet});
Toeplitz determinant product solution and minimum unstable position (theorem \ref{thm:xi-toeplitz-det-verblunsky});
Finite window CMV (Hilbert--P{\'o}lya) unitary spectrum implementation (proposition \ref{prop:xi-hilbert-polya-cmv})
&
\texttt{\detokenize{scripts/exp_sync_kernel_xi_cubic_cos_polynomial.py}}
\(\to\)
\texttt{\detokenize{sections/generated/eq_sync_kernel_xi_cubic_cos_polynomial.tex}},
\texttt{\detokenize{sections/generated/tab_sync_kernel_xi_cubic_roots.tex}}
(Note \ref{rem:xi-hard-zero-certificate})
\\
\midrule
\textbf{Resonance interpretation entrance}\\[-2pt]
Poisson/Breit--Wigner (physical term)\(\Leftrightarrow\) ​​SU(1,1)--RHP \(\Leftrightarrow\) Maslov
&
Intrinsic function phase \(\Theta(t)=\arg D(\tfrac12+\mathrm{i}t)\);
Offline zero point parameter \((\gamma_k,\delta_k)\);
Positive definite jump data and spectral flow parameters (\S\ref{subsubsec:xi-debranges-canonical-rhp-adelic})
&
Poisson's superposition law of phase derivatives: offline zero-point contribution Lorentz/Breit--Wigner kernel (Proposition \ref{prop:xi-phase-derivative-poisson-superposition});
SU(1,1)–RHP positive definite jump solvability equivalent template (theorem \ref{thm:xi-su11-rhp-equivalence});
Maslov indicator formula for zero point count difference (proposition \ref{prop:xi-maslov-spectralflow-entropy-quantization});
And the Lorentz prelimit and width parameters appear under the two-step rescaling aperture of the resonance window (note \ref{rem:pom-resonance-poisson-lorentz-prelimit})
&
\texttt{\detokenize{scripts/exp_xi_horizon_dtn_fourier_laplace_audit.py}}
\(\to\)
\texttt{\detokenize{sections/generated/tab_xi_horizon_dtn_fourier_laplace_audit.tex}}
(\S\ref{subsec:discussion-horizon-zk-toeplitz-area-law}; Resonance window data sheet \ref{tab:fold_collision_shadow_spectral_packet_q9_17})
\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Reproduction and Audit Guidelines}\label{subsec:repro-audit-pointer}
Interpretive synthesis, boundaries and falsifiability points are concentrated in Section \ref{sec:discussion}. The appendix collects supporting materials such as operator algebra, phase towers, synchronization kernels and multi-scale interfaces to support the auditable reasoning chain and review requirements of the main text.

\paragraph{Conclusion (falsifiable general positioning)}
Under the fixed protocol scope of this paper, the local normalization rule of \(\Fold_m\) is minimalist in form; its "depth" comes from the irreducible structure of the finite state object it induces in two directions: one is \textbf{spectrum} (including the algebraic scale locked by the Perron primary root and the non-single Jordan The residual mode carried by the defect), the second is \textbf{symmetry} (equivariant compression and block rigidity enforced by the dictionary/track structure).
In the same spectral world, the nuclear version of the RH/de Bruijn--Newman-type threshold enables the critical phase transition to be accurately nailed by the integer elimination certificate (note \ref{rem:pom-collision-rh-break-a4-threshold}; proposition \ref{prop:sync-kernel-weighted-newman-threshold}); and the characteristic factorization on \(\mathbb{F}_p\) gives a set of local reduction fingerprints orthogonal to the real spectrum (note \ref{rem:pom-charpoly-modp-audit-method}), and solidify \(\varphi\) at the high-order endpoint into a self-calibration constant that can be inverted from the collision spectrum (proposition \ref{prop:pom-rq-qinfty-endpoint}).
Both types of structures can be compressed into finite certificates (rational generators and Perron tap roots: proposition \ref{prop:pom-multiplicity-composition-partition-rational}; Jordan residuals and rank/recursion constraints: theorem \ref{thm:fold-gauge-anomaly-A0-jordan}, theorem \ref{thm:fold-gauge-anomaly-hankel-rank3}; Weyl orbital compression and uplift chunking: corollary \ref{cor:fold6-weyl-two-orbit-compression}, theorem \ref{thm:boundary-m10-b3c3-rigid-four-block-split}),
The unifying narrative of this paper is therefore not an explanatory splicing that relies on analogy, but a disprovable structure defined by auditable outputs.
