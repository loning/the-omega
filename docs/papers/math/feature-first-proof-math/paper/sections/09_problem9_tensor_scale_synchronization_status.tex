\section{Problem 9: Tensor Block Scaling Synchronization}

\textbf{Latest Reviewed Version:} Q9-R2 (FAIL).

\textbf{Current Status.}
Major gaps from Q9-R1 are largely repaired (diagonal-zero handling, block-consistent rigidity, mode-wise rank strategy), but one proof-critical generic-rank lemma is still incomplete as written.

\textbf{Submission Record.}

\textbf{Q9-V2 (user submission).}
Main upgrades relative to Q9-R1:
\begin{itemize}
\item Adds explicit proof that diagonal blocks $Q^{(\alpha\alpha\alpha\alpha)}\equiv 0$.
\item Replaces prior rigidity with a block-consistent lemma using
$D(d)=\mathrm{diag}(d_1I_3,\dots,d_nI_3)$ and 3D camera row-space eigenvalue forcing.
\item Constructs $F$ via degree-5 minors of concatenated block-column matrices in modes 1--3.
\item Gives a full-mode elimination pipeline intended to prove
$F(\lambda*Q)=0 \Leftrightarrow \lambda=u\otimes v\otimes w\otimes x$
on non-identical index quadruples.
\end{itemize}

\textbf{Review Outcome (Q9-R2).}
Still \textbf{FAIL}: Lemma 3.1 is not fully proved for all required nonconstant triples.

\textbf{Tracking Rule.}
Each newly submitted user version is first appended in this section, then reviewed in `reviews/problem9_review.md`.
