\section{折叠多重度的渐近与"分辨率不足"}\label{app:fold-multiplicity}

平均折叠多重度
\[
d_m=\frac{|\Omega_m|}{|X_m|}=\frac{2^m}{F_{m+2}}
\]
满足
\[
d_m\sim C\left(\frac{2}{\varphi}\right)^m,\quad C=\frac{\sqrt5}{\varphi^2}.
\]
因此对固定窗口长度 $m$，观测将大量微态压缩为同一稳定类型；当 $m$ 增大时，稳定类型虽增多，但微态增长更快，从而"可区分细节"在有限分辨率下持续被折叠。这给出"规律不可读出可归因于分辨率不足"的数学版本：不可读出对应不同微态在折叠后不可区分，而稳定规律对应 $X_m$ 上的递归计数与不变分布仍可复核。

\subsection{折叠同余与有限分辨率半环}
为把折叠多重度与第 7 节的有限分辨率算术对接，这里给出折叠同余的代数化表述。

\begin{definition}[折叠同余与值模映射]
记 $\Omega_m:=\{0,1\}^m$，并定义折叠同余
\[
\omega\equiv_m\omega'\quad\Longleftrightarrow\quad\Fold_m(\omega)=\Fold_m(\omega').
\]
令
\[
N(\omega):=\sum_{k=1}^m \omega_k F_{k+1},
\qquad
\pi_m(\omega):=N(\omega)\bmod F_{m+2}.
\]
并记 $w(r):=\pi_m(Z(r))\in X_m\subset\Omega_m$（$0\le r\le F_{m+2}-1$）。
\end{definition}

\begin{theorem}[折叠同余的模半环表示]
\label{thm:fold-congruence-mod-semirings}
映射 $\pi_m$ 在 $\equiv_m$ 上良定义并诱导双射
\[
\overline\pi_m:\Omega_m/\!\equiv_m\;\longrightarrow\;\ZZ/(F_{m+2}\ZZ).
\]
进一步，若在 $\Omega_m/\!\equiv_m$ 上定义
\[
[\omega]\,\boxplus_m\,[\eta]:=[w(\overline\pi_m([\omega])\!+\!\overline\pi_m([\eta])\bmod F_{m+2})],
\]
\[
[\omega]\,\boxtimes_m\,[\eta]:=[w(\overline\pi_m([\omega])\,\overline\pi_m([\eta])\bmod F_{m+2})],
\]
则 $\overline\pi_m$ 给出半环同构
\[
(\Omega_m/\!\equiv_m,\boxplus_m,\boxtimes_m)\cong\bigl(\ZZ/(F_{m+2}\ZZ),+,\times\bigr).
\]
\end{theorem}

\textbf{证明：}
若 $\omega\equiv_m\omega'$，则 $\Fold_m(\omega)=\Fold_m(\omega')=:x\in X_m$。
由引理 \ref{lem:zeckendorf-range-bijection}，$V_m(x)$ 唯一确定模 $F_{m+2}$ 的剩余类，且
\(V_m(x)\equiv N(\omega)\equiv N(\omega')\pmod{F_{m+2}}\)。
故 $\pi_m$ 在 $\equiv_m$ 上良定义并诱导 $\overline\pi_m$。又对任意 $r$，取 $w(r)\in X_m\subset\Omega_m$，有
\(\overline\pi_m([w(r)])=r\)，故 $\overline\pi_m$ 为双射。由定义可见 $\overline\pi_m$ 保持加法与乘法，故为半环同构。证毕。

\subsection{多重度谱与方差能量恒等式}
将折叠多重度视为模环上的类函数，并给出其 $\ell^2$ 能量与频谱的精确对应。

\begin{definition}[多重度谱]
对 $r\in\ZZ/(F_{m+2}\ZZ)$ 定义多重度剖面
\[
d_m(r):=\bigl|\{\omega\in\Omega_m:\pi_m(\omega)=r\}\bigr|.
\]
记其离散 Fourier 变换为
\[
\widehat d_m(k):=\sum_{r\in\ZZ/(F_{m+2}\ZZ)} d_m(r)\,e^{-2\pi i kr/F_{m+2}},
\qquad k\in\ZZ/(F_{m+2}\ZZ).
\]
并记均值
\(
\overline d_m:=\frac{1}{F_{m+2}}\sum_r d_m(r)=\frac{2^m}{F_{m+2}}.
\)
\end{definition}

\begin{theorem}[多重度能量与方差的谱表示]
\label{thm:fold-multiplicity-energy}
设 $d_m(r)$ 如上，则有精确恒等式
\[
\sum_{r} d_m(r)^2=\frac{1}{F_{m+2}}\sum_{k}|\widehat d_m(k)|^2,
\]
并且方差满足
\[
\frac{1}{F_{m+2}}\sum_r\bigl(d_m(r)-\overline d_m\bigr)^2
=\frac{1}{F_{m+2}^2}\sum_{k\ne 0}|\widehat d_m(k)|^2.
\]
其中 $k=0$ 项给出 $\widehat d_m(0)=2^m$。
\end{theorem}

\textbf{证明：}
在有限交换群 $\ZZ/(F_{m+2}\ZZ)$ 上应用离散 Parseval 恒等式即得
\(
\sum_r d_m(r)^2=\frac{1}{F_{m+2}}\sum_k|\widehat d_m(k)|^2
\)。
又 $\widehat d_m(0)=\sum_r d_m(r)=|\Omega_m|=2^m$，代入
\(
\sum_r(d_m(r)-\overline d_m)^2=\sum_r d_m(r)^2-F_{m+2}\,\overline d_m^2
\)
并整理即可。证毕。

\begin{definition}[折叠碰撞概率]
对独立均匀的 $\omega,\eta\in\Omega_m$，定义
\[
\mathsf{Col}_m:=\mathbb{P}\bigl(\pi_m(\omega)=\pi_m(\eta)\bigr)
=\frac{1}{2^{2m}}\sum_r d_m(r)^2.
\]
称 $\mathsf{Col}_m-1/F_{m+2}$ 为折叠的均匀性缺口。
\end{definition}

\begin{theorem}[碰撞概率的谱恒等式]
\label{thm:fold-collision-spectrum}
折叠碰撞概率满足精确恒等式
\[
\mathsf{Col}_m
=\frac{1}{2^{2m}F_{m+2}}\sum_{k}|\widehat d_m(k)|^2
=\frac{1}{F_{m+2}}+\frac{1}{2^{2m}F_{m+2}}\sum_{k\ne 0}|\widehat d_m(k)|^2.
\]
因此 $\mathsf{Col}_m\ge 1/F_{m+2}$，且等号当且仅当 $d_m(r)\equiv 2^m/F_{m+2}$ 为常数。
\end{theorem}

\textbf{证明：}
由定理 \ref{thm:fold-multiplicity-energy} 的方差恒等式，
\(
\sum_r d_m(r)^2=F_{m+2}\,\overline d_m^2+\frac{1}{F_{m+2}}\sum_{k\ne 0}|\widehat d_m(k)|^2
\)，
再代入 $\overline d_m=2^m/F_{m+2}$ 即得结论。

\begin{theorem}[多重度谱的乘法分解]
\label{thm:fold-spectrum-factorization}
对任意 $k\in\ZZ/(F_{m+2}\ZZ)$，有
\[
\widehat d_m(k)=\prod_{j=1}^m\Bigl(1+e^{-2\pi i k F_{j+1}/F_{m+2}}\Bigr).
\]
从而
\[
|\widehat d_m(k)|=2^m\prod_{j=1}^m\Bigl|\cos\Bigl(\pi k\,F_{j+1}/F_{m+2}\Bigr)\Bigr|.
\]
\end{theorem}

\textbf{证明：}
按定义
\(
\widehat d_m(k)=\sum_{\omega\in\Omega_m}e^{-2\pi i k\pi_m(\omega)/F_{m+2}}
\)。
又 $\pi_m(\omega)\equiv \sum_{j=1}^m \omega_j F_{j+1}\pmod{F_{m+2}}$，故
\[
\widehat d_m(k)=\sum_{\omega\in\Omega_m}\prod_{j=1}^m e^{-2\pi i k\omega_j F_{j+1}/F_{m+2}}
=\prod_{j=1}^m\bigl(1+e^{-2\pi i k F_{j+1}/F_{m+2}}\bigr).
\]
利用 $1+e^{-i\theta}=e^{-i\theta/2}\,2\cos(\theta/2)$ 得到模长公式。证毕。

\begin{corollary}[非平凡频谱的乘性上界]
\label{cor:fold-spectrum-nontrivial-bound}
对任意 $k\ne 0$，有
\[
|\widehat d_m(k)|\le 2^m\prod_{j=1}^m\Bigl|\cos\Bigl(\pi k\,F_{j+1}/F_{m+2}\Bigr)\Bigr|<2^m.
\]
因此碰撞缺口满足
\[
\mathsf{Col}_m-\frac{1}{F_{m+2}}=\frac{1}{2^{2m}F_{m+2}}\sum_{k\ne 0}|\widehat d_m(k)|^2
\le\frac{1}{F_{m+2}}\sum_{k\ne 0}\prod_{j=1}^m\cos^2\Bigl(\pi k\,F_{j+1}/F_{m+2}\Bigr).
\]
\end{corollary}

\subsubsection{临界共振角色与黄金比常数}
\label{subsubsec:fold-critical-resonance}

定理 \ref{thm:fold-spectrum-factorization} 将非平凡频谱写成余弦乘积。本小节给出一族位于
\(
k/F_{m+2}\to \varphi^{-2}
\)
的“bulk 共振角色”，其归一化幅度不随 $m$ 指数衰减，而是收敛到一个严格正的极限常数 \(C_\varphi\)。

\begin{definition}[归一化频谱幅度]\label{def:fold-normalized-amplitude}
定义
\[
a_m(k):=\frac{|\widehat d_m(k)|}{2^m}
=\prod_{j=1}^m\Bigl|\cos\Bigl(\pi k\,F_{j+1}/F_{m+2}\Bigr)\Bigr|.
\]
则由定理 \ref{thm:fold-collision-spectrum}，
\[
\mathsf{Col}_m
=\frac{1}{2^{2m}}\sum_r d_m(r)^2
=\frac{1}{F_{m+2}}\sum_{k} a_m(k)^2.
\]
\end{definition}

\begin{lemma}[d'Ocagne 型同余压缩恒等式]\label{lem:fold-resonance-docagne}
对任意整数 \(m\ge 1\) 与 \(1\le j\le m\)，有整式恒等式
\[
F_mF_{j+1}-F_{j-1}F_{m+2}=(-1)^{j+1}F_{m+1-j}.
\]
从而
\[
F_mF_{j+1}\equiv (-1)^{j+1}F_{m+1-j}\pmod{F_{m+2}}.
\]
\end{lemma}

\textbf{证明：}
利用 \(F_{m+2}=F_{m+1}+F_m\) 与 \(F_{j+1}=F_j+F_{j-1}\)，有
\[
F_mF_{j+1}-F_{j-1}F_{m+2}
=F_m(F_j+F_{j-1})-F_{j-1}(F_{m+1}+F_m)
=F_mF_j-F_{m+1}F_{j-1}.
\]
由 d'Ocagne 恒等式
\(
F_aF_{b+1}-F_{a+1}F_b=(-1)^bF_{a-b}
\)
取 \(a=m\)、\(b=j-1\)，得到
\(
F_mF_j-F_{m+1}F_{j-1}=(-1)^{j-1}F_{m-(j-1)}=(-1)^{j+1}F_{m+1-j}
\)，从而结论成立。证毕。

\begin{theorem}[临界共振常数 \(C_\varphi\)]\label{thm:fold-critical-resonance-constant}
令 \(\varphi=(1+\sqrt5)/2\)。定义无穷乘积
\[
C_\varphi:=\prod_{n=2}^{\infty}\bigl|\cos(\pi \varphi^{-n})\bigr|.
\]
则该乘积收敛且 \(C_\varphi>0\)，并且
\[
\lim_{m\to\infty}a_m(F_m)=\lim_{m\to\infty}a_m(F_{m+1})=C_\varphi.
\]
数值上 \(C_\varphi\approx 0.22441782740472943\)。
\end{theorem}

\textbf{证明：}
由定义 \ref{def:fold-normalized-amplitude}，
\(
a_m(F_m)=\prod_{j=1}^m\bigl|\cos(\pi F_mF_{j+1}/F_{m+2})\bigr|
\)。
用引理 \ref{lem:fold-resonance-docagne} 将分子在模 \(F_{m+2}\) 下压缩为 \(F_{m+1-j}\)，并利用余弦的偶性得到精确表示
\[
a_m(F_m)=\prod_{j=1}^m\Bigl|\cos\Bigl(\pi \frac{F_{m+1-j}}{F_{m+2}}\Bigr)\Bigr|.
\]
固定 \(j\) 令 \(m\to\infty\)，由 Binet 渐近 \(F_n=\varphi^n/\sqrt5+O(\varphi^{-n})\) 得
\[
\frac{F_{m+1-j}}{F_{m+2}}\to \varphi^{-(j+1)}.
\]
另一方面，当 \(j\) 足够大时，上式右端足够小，且存在常数 \(C>0\) 使得对所有充分小的 \(|x|\) 都有
\(
|\log|\cos(\pi x)||\le Cx^2
\)。
由于 \(\sum_{j\ge 1}\varphi^{-2(j+1)}<\infty\)，可对对数级数使用支配收敛，得到乘积极限存在且等于
\[
\prod_{j=1}^{\infty}\bigl|\cos(\pi \varphi^{-(j+1)})\bigr|
=\prod_{n=2}^{\infty}\bigl|\cos(\pi \varphi^{-n})\bigr|
=C_\varphi.
\]
同理 \(a_m(F_{m+1})=a_m(-F_m)=a_m(F_m)\)（因 \(F_{m+1}\equiv -F_m\pmod{F_{m+2}}\) 且 \(\cos\) 偶），从而两者同收敛到 \(C_\varphi\)。证毕。

\begin{corollary}[不可消除的常数级均匀性缺口]\label{cor:fold-critical-resonance-gap}
折叠碰撞概率满足下界
\[
\mathsf{Col}_m
\ge
\frac{1}{F_{m+2}}\Bigl(1+a_m(F_m)^2+a_m(F_{m+1})^2\Bigr).
\]
从而
\[
\mathsf{Col}_m
\ge \frac{1+2C_\varphi^2+o(1)}{F_{m+2}},
\qquad
\mathsf{Col}_m-\frac{1}{F_{m+2}}
\ge \frac{2C_\varphi^2+o(1)}{F_{m+2}}.
\]
特别地，基线 \(1/F_{m+2}\) 的相对因子至少为
\(
1+2C_\varphi^2\approx 1.100728\ldots
\)。
\end{corollary}

\textbf{证明：}
由定义 \ref{def:fold-normalized-amplitude}，
\[
\mathsf{Col}_m=\frac{1}{F_{m+2}}\sum_{k}a_m(k)^2
\ge \frac{1}{F_{m+2}}\Bigl(a_m(0)^2+a_m(F_m)^2+a_m(F_{m+1})^2\Bigr).
\]
其中 \(a_m(0)=|\widehat d_m(0)|/2^m=1\)。再用定理 \ref{thm:fold-critical-resonance-constant} 即得结论。证毕。

\begin{remark}[Edge 频率的非衰减极限]\label{rem:fold-edge-nondecay}
上述共振 \(k=F_m\) 满足 \(k/F_{m+2}\to \varphi^{-2}\)，属于频率空间的 bulk 区域。
另一方面，对任意固定整数 \(k\ge 1\)（独立于 \(m\)），由
\(
a_m(k)=\prod_{j=1}^m|\cos(\pi kF_{j+1}/F_{m+2})|
\)
对尾部指标重排可得 \(a_m(k)\) 亦存在极限，并可写为
\[
\lim_{m\to\infty}a_m(k)=\prod_{n=1}^{\infty}\bigl|\cos(\pi k\,\varphi^{-n})\bigr|=:C^{\mathrm{edge}}_\varphi(k).
\]
因此若要讨论“统一指数谱隙/指数衰减”，必须把频率空间分解为 edge（如 \(k=o(F_{m+2})\)）与 bulk（如 \(k\asymp F_{m+2}\)）两类；全频率意义下的“对所有 \(k\ne 0\) 指数衰减”并不成立。
\end{remark}

\begin{theorem}[奇偶性诱导的谱零点]
\label{thm:fold-spectrum-parity-zero}
若 $F_{m+2}$ 为偶数（等价于 $m+2\equiv 0\pmod 3$），则
\[
\widehat d_m\Bigl(\frac{F_{m+2}}{2}\Bigr)=0.
\]
换言之，频率 $k=F_{m+2}/2$ 为折叠多重度谱的一个严格零点。
\end{theorem}

\textbf{证明：}
由定理 \ref{thm:fold-spectrum-factorization}，
\[
\widehat d_m\Bigl(\frac{F_{m+2}}{2}\Bigr)=\prod_{j=1}^m\Bigl(1+e^{-\pi i F_{j+1}}\Bigr)
=\prod_{j=1}^m\bigl(1+(-1)^{F_{j+1}}\bigr).
\]
当 $m+2\equiv 0\pmod 3$ 时，$F_{m+2}$ 为偶数，$k=F_{m+2}/2$ 为整数。又 Fibonacci 数奇偶性以 3 为周期，存在 $j$ 使 $F_{j+1}$ 为奇数，故因子 $1+(-1)^{F_{j+1}}=0$，从而乘积为零。证毕。

\begin{theorem}[谱零点的同余判别]
\label{thm:fold-spectrum-zero-criterion}
对任意 $k\in\ZZ/(F_{m+2}\ZZ)$，有
\[
\widehat d_m(k)=0\quad\Longleftrightarrow\quad\exists\,j\in\{1,\dots,m\}\ \text{s.t.}\ 2kF_{j+1}\equiv F_{m+2}\pmod{2F_{m+2}}.
\]
等价地，存在 $j$ 使得
\(
e^{-2\pi i k F_{j+1}/F_{m+2}}=-1
\)。
\end{theorem}

\textbf{证明：}
由定理 \ref{thm:fold-spectrum-factorization}，
\(
\widehat d_m(k)=\prod_{j=1}^m(1+e^{-2\pi i k F_{j+1}/F_{m+2}})
\)。
乘积为零当且仅当存在某个 $j$ 使得因子为零，即
\(
e^{-2\pi i k F_{j+1}/F_{m+2}}=-1
\)。
将其改写为同余条件得到结论。证毕。

\begin{definition}[谱零点集合]\label{def:fold-spectrum-zero-set}
记 \(M:=F_{m+2}\)，并定义谱零点集合
\[
\mathcal{Z}_m:=\{k\in\ZZ/(M\ZZ):\widehat d_m(k)=0\}.
\]
\end{definition}

\begin{theorem}[零点余类刚性：解集只依赖于 \texorpdfstring{$\gcd$}{gcd}]\label{thm:fold-zero-coset-rigidity}
设 \(M\) 为偶数，\(w\in\ZZ\)，令 \(g=\gcd(w,M)\)。
则
\[
e^{-2\pi i k w/M}=-1
\quad\Longleftrightarrow\quad
wk\equiv \frac{M}{2}\pmod M.
\]
上述同余在 \(\ZZ/(M\ZZ)\) 中可解当且仅当 \(g\mid M/2\)（等价于 \(M/g\) 为偶数）。
在可解时，其全部解构成一个刚性的等差余类集合
\[
S_g(M):=\left\{\frac{M}{2g}+t\frac{M}{g}\ \ (\bmod M):\ t=0,1,\dots,g-1\right\}.
\]
特别地，解集只依赖于 \(g=\gcd(w,M)\)，与 \(w\) 在该最大公因子之外的细节无关。
\end{theorem}

\begin{proof}
把 \(w=gw_1\)、\(M=gM_1\) 写成既约形式，使得 \(\gcd(w_1,M_1)=1\)。
同余 \(wk\equiv M/2\pmod M\) 可解当且仅当 \(g\mid M/2\)，即 \(M_1\) 为偶数。
在此条件下约去 \(g\) 得
\[
w_1k\equiv \frac{M_1}{2}\pmod{M_1}.
\]
由于 \(\gcd(w_1,M_1)=1\)，存在逆元 \(w_1^{-1}\pmod{M_1}\)。又 \(M_1\) 为偶数蕴含 \(w_1\) 必为奇数，从而 \(w_1^{-1}\) 亦为奇数。
因此
\[
k\equiv \frac{M_1}{2}\,w_1^{-1}\equiv \frac{M_1}{2}\pmod{M_1},
\]
其中最后一步使用了“偶模下半模乘奇数仍同余半模”：\(\frac{M_1}{2}(2r+1)\equiv \frac{M_1}{2}\pmod{M_1}\)。
把模 \(M_1\) 的解提升回模 \(M=gM_1\)，得到
\[
k=\frac{M_1}{2}+tM_1=\frac{M}{2g}+t\frac{M}{g},\qquad t=0,1,\dots,g-1,
\]
这恰给出集合 \(S_g(M)\)。证毕。
\end{proof}

\begin{corollary}[Fibonacci 权重下零点集合的余类并结构]\label{cor:fold-zero-coset-union}
令 \(M:=F_{m+2}\)。若 \(M\) 为偶数，则
\[
\mathcal{Z}_m=\bigcup_{j=1}^{m}\Bigl\{k\in\ZZ/(M\ZZ):e^{-2\pi i kF_{j+1}/M}=-1\Bigr\}
=\bigcup_{\substack{1\le j\le m\\ g_j\mid M/2}} S_{g_j}(M),
\qquad g_j:=\gcd(F_{j+1},M),
\]
并且由 Fibonacci 的最大公因子恒等式 \(\gcd(F_a,F_b)=F_{\gcd(a,b)}\)，有
\[
g_j=\gcd(F_{j+1},F_{m+2})=F_{\gcd(j+1,m+2)}.
\]
因此 \(\mathcal{Z}_m\) 的可能余类只由 \(m+2\) 的约数格控制：\(g_j\) 只能取形如 \(F_d\) 的值，其中 \(d\mid(m+2)\)、且 \(d<m+2\)。
\end{corollary}

\begin{proof}
第一条等号由定理 \ref{thm:fold-spectrum-factorization} 与定理 \ref{thm:fold-spectrum-zero-criterion} 给出：乘积为零当且仅当存在某个因子 \(1+e^{-2\pi i kF_{j+1}/M}\) 为零。
对每个固定的 \(j\)，令 \(w:=F_{j+1}\)。应用定理 \ref{thm:fold-zero-coset-rigidity}，该因子为零的解集正是 \(S_{\gcd(w,M)}(M)\)（不可解时为空集），从而得到余类并表示。
最后一式由 \(\gcd(F_a,F_b)=F_{\gcd(a,b)}\) 取 \(a=j+1\)、\(b=m+2\) 得到。证毕。
\end{proof}

\begin{lemma}[余类交集判据与交集大小]\label{lem:fold-zero-coset-intersection}
设 \(M\) 为偶数且 \(g,h\mid M/2\)。
则
\[
S_g(M)\cap S_h(M)\neq\varnothing
\quad\Longleftrightarrow\quad
2\gcd(g,h)\mid (h-g).
\]
并且一旦交集非空，就有
\[
\card{S_g(M)\cap S_h(M)}=\gcd(g,h).
\]
\end{lemma}

\begin{proof}
由定理 \ref{thm:fold-zero-coset-rigidity} 的构造，集合 \(S_g(M)\) 可等价写为单一同余类
\[
S_g(M)=\left\{k\in\ZZ/(M\ZZ): k\equiv \frac{M}{2g}\pmod{M/g}\right\},
\]
同理
\(
S_h(M)=\{k: k\equiv M/(2h)\pmod{M/h}\}
\)。
两条线性同余的兼容性条件为
\[
\frac{M}{2g}-\frac{M}{2h}\equiv 0\pmod{\gcd(M/g,M/h)}.
\]
注意 \(\gcd(M/g,M/h)=M/\mathrm{lcm}(g,h)=M\gcd(g,h)/(gh)\)，把上式两边同除以 \(M/(gh)\) 得到
\[
\frac{h-g}{2}\equiv 0\pmod{\gcd(g,h)},
\]
即 \(2\gcd(g,h)\mid(h-g)\)，从而得到非空判据。
若兼容，则解集模数为 \(\mathrm{lcm}(M/g,M/h)=M/\gcd(g,h)\)，因此在模 \(M\) 下共有 \(\gcd(g,h)\) 个解，即交集大小为 \(\gcd(g,h)\)。证毕。
\end{proof}

\begin{theorem}[谱零点密度的指数稀疏上界]\label{thm:fold-zero-density-sparse}
令 \(M:=F_{m+2}\)。若 \(M\) 为偶数，则谱零点集合满足
\[
\frac{\card{\mathcal{Z}_m}}{M}
\le
\tau(m+2)\,\frac{F_{\lfloor (m+2)/2\rfloor}}{F_{m+2}}
=\tau(m+2)\,O(\varphi^{-m/2}),
\]
其中 \(\tau(\cdot)\) 为约数个数函数。
\end{theorem}

\begin{proof}
由推论 \ref{cor:fold-zero-coset-union}，\(\mathcal{Z}_m\) 是若干集合 \(S_{g}(M)\) 的并，其中每个 \(g\) 都形如 \(F_d\)，且 \(d\mid(m+2)\)、\(d<m+2\)。
因此这些 \(g\) 的个数不超过 \(\tau(m+2)\)。又对任意真约数 \(d<m+2\) 都有 \(d\le \lfloor (m+2)/2\rfloor\)，并且 Fibonacci 数列单调递增，故
\(
g=F_d\le F_{\lfloor (m+2)/2\rfloor}
\)。
由于 \(\card{S_g(M)}=g\)，用并集的平凡上界得到
\[
\card{\mathcal{Z}_m}
\le \sum_{S_g(M)\subset \mathcal{Z}_m}\card{S_g(M)}
\le \tau(m+2)\,F_{\lfloor (m+2)/2\rfloor}.
\]
两边同除以 \(M=F_{m+2}\) 得到第一条不等式。
最后的 \(O(\varphi^{-m/2})\) 由 Binet 渐近 \(F_n\asymp \varphi^n\) 推出。证毕。
\end{proof}

\begin{remark}[从“零点几何”转向“振幅分布”的必要性]\label{rem:fold-zero-sparse-necessity}
定理 \ref{thm:fold-zero-density-sparse} 表明当 \(m\to\infty\) 时，零点频率在全频率空间 \(\ZZ/(F_{m+2}\ZZ)\) 中的相对密度指数衰减。
因此任何以频率平均为核心的极限量（例如定理 \ref{thm:fold-collision-spectrum} 中的非平凡频谱能量平均）都不可能由“零点数量级”主导；
要获得真正的渐近信息，必须理解非零频率处
\(
|\widehat d_m(k)|=2^m\prod_{j=1}^m\bigl|\cos(\pi kF_{j+1}/F_{m+2})\bigr|
\)
的乘积型振幅分布。等价地，
\[
\frac{|\widehat d_m(k)|^2}{2^{2m}}
=\prod_{j=1}^m\cos^2\Bigl(\pi k\,F_{j+1}/F_{m+2}\Bigr)
=2^{-m}\prod_{j=1}^m\Bigl(1+\cos\Bigl(2\pi k\,F_{j+1}/F_{m+2}\Bigr)\Bigr),
\]
从而该归一化能量剖面可以视为一类由 Fibonacci 频率驱动的有限 Riesz 乘积；其极值与大偏差控制正是进一步把“零点几何”推进到“振荡统计”的入口。
\end{remark}

\begin{theorem}[零点削减的碰撞上界]
\label{thm:fold-collision-zero-reduction}
记 $\mathcal{Z}_m$ 如定义 \ref{def:fold-spectrum-zero-set}，则
\[
\mathsf{Col}_m\le 1-\frac{|\mathcal{Z}_m|}{F_{m+2}}.
\]
特别地，当 $|\mathcal{Z}_m|\ge 1$ 时，
\[
\mathsf{Col}_m\le 1-\frac{1}{F_{m+2}}.
\]
\end{theorem}

\textbf{证明：}
由定理 \ref{thm:fold-collision-spectrum}，
\(
\mathsf{Col}_m-\frac{1}{F_{m+2}}=\frac{1}{2^{2m}F_{m+2}}\sum_{k\ne 0}|\widehat d_m(k)|^2
\)。
而 $|\widehat d_m(k)|\le\sum_r d_m(r)=2^m$，且对 $k\in\mathcal{Z}_m$ 取值为 0，故
\(
\sum_{k\ne 0}|\widehat d_m(k)|^2\le (F_{m+2}-1-|\mathcal{Z}_m|)\,2^{2m}
\)。
代入得到
\[
\mathsf{Col}_m
\le
\frac{1}{F_{m+2}}+\frac{F_{m+2}-1-|\mathcal{Z}_m|}{F_{m+2}}
=1-\frac{|\mathcal{Z}_m|}{F_{m+2}}.
\]
证毕。

\begin{remark}[用约数结构精确计算 \texorpdfstring{$\card{\mathcal{Z}_m}$}{|Z_m|} 的可实现方案]\label{rem:fold-zero-coset-algorithm}
当 \(F_{m+2}\) 为偶数时，推论 \ref{cor:fold-zero-coset-union} 将 \(\mathcal{Z}_m\) 表示为若干等差余类 \(S_g(F_{m+2})\) 的并。
因此可以不扫描全部频率 \(k\)，而仅在“约数尺度”上完成精确计数：
\begin{enumerate}
  \item 枚举 \(d\mid(m+2)\) 且 \(d<m+2\)，令 \(g=F_d\)，并筛去不满足 \(g\mid F_{m+2}/2\) 的项；
  \item 对保留下来的 \(g\) 构造同余 \(k\equiv F_{m+2}/(2g)\pmod{F_{m+2}/g}\)，并用引理 \ref{lem:fold-zero-coset-intersection}（更一般地，可用多同余兼容性判据）计算任意有限交集的大小；
  \item 用 inclusion--exclusion 或其等价的并集计数实现得到 \(\card{\mathcal{Z}_m}\) 的精确值。
\end{enumerate}
该流程把“扫 \(k\)”降为“扫 \(m+2\) 的少量约数 + 有限次 \(\gcd\)/整除判定”，可直接接入数值审计管线。
\end{remark}

\begin{theorem}[最大纤维与谱能量]
\label{thm:fold-max-fiber-spectrum}
记
\[
M_m:=\max_{r\in\ZZ/(F_{m+2}\ZZ)} d_m(r).
\]
则有下界
\[
M_m\ge \overline d_m+\frac{1}{F_{m+2}}\Biggl(\sum_{k\ne 0}|\widehat d_m(k)|^2\Biggr)^{1/2}.
\]
亦即最大折叠纤维由非平凡频谱能量定量驱动。
\end{theorem}

\textbf{证明：}
由方差恒等式
\(
\frac{1}{F_{m+2}}\sum_r(d_m(r)-\overline d_m)^2=\frac{1}{F_{m+2}^2}\sum_{k\ne 0}|\widehat d_m(k)|^2
\)。
取达到最大值的 $r_*$，有
\(
\sum_r(d_m(r)-\overline d_m)^2\ge (M_m-\overline d_m)^2
\)。
合并即得结论。证毕。

\begin{theorem}[最大纤维给出的碰撞下界]
\label{thm:fold-collision-max-fiber}
记 $M_m:=\max_r d_m(r)$，则折叠碰撞概率满足
\[
\mathsf{Col}_m\ge \frac{M_m^2}{2^{2m}}.
\]
因此任何非均匀纤维集中必然导致碰撞缺口的显式下界。
\end{theorem}

\textbf{证明：}
由定义
\(
\mathsf{Col}_m=2^{-2m}\sum_r d_m(r)^2
\)。
由于 $\sum_r d_m(r)^2\ge M_m^2$，代入即得结论。证毕。

\begin{theorem}[最大纤维的 Fourier 下界]
\label{thm:fold-max-fiber-fourier}
设
\(
S_m:=\max_{k\ne 0}|\widehat d_m(k)|
\)。
则
\[
M_m\ge \overline d_m+\frac{S_m}{F_{m+2}}.
\]
特别地，任何非平凡频谱幅度都会以线性比例推高最大纤维。
\end{theorem}

\textbf{证明：}
由 Fourier 反演公式
\(
d_m(r)=\frac{1}{F_{m+2}}\sum_k \widehat d_m(k)\,e^{2\pi i kr/F_{m+2}}
\)。
取 $k_*$ 使 $|\widehat d_m(k_*)|=S_m$，并选取 $r$ 使相位对齐，则
\(
d_m(r)\ge \overline d_m+\frac{S_m}{F_{m+2}}
\)。
由最大值定义得到结论。证毕。

\begin{theorem}[碰撞缺口的双向夹逼]
\label{thm:fold-collision-sandwich}
设 $M_m:=\max_r d_m(r)$ 与零点集合 $\mathcal{Z}_m$ 如前，则
\[
\frac{M_m^2}{2^{2m}}-\frac{1}{F_{m+2}}
\le
\mathsf{Col}_m-\frac{1}{F_{m+2}}
\le
\frac{F_{m+2}-1-|\mathcal{Z}_m|}{F_{m+2}}.
\]
从而折叠均匀性缺口同时受最大纤维与谱零点数量的双向控制。
\end{theorem}

\textbf{证明：}
左侧不等式由定理 \ref{thm:fold-collision-max-fiber} 直接给出。
右侧不等式由定理 \ref{thm:fold-collision-zero-reduction} 得到。证毕。

\begin{corollary}[零点削减项的相对指数小量]
\label{cor:fold-collision-zero-sparsity-impact}
令 \(M:=F_{m+2}\)。若 \(M\) 为偶数，则定理 \ref{thm:fold-collision-zero-reduction} 的“零点削减”改进量满足
\[
0\le \frac{\card{\mathcal{Z}_m}}{M}
\le
\tau(m+2)\,\frac{F_{\lfloor (m+2)/2\rfloor}}{F_{m+2}}
=\tau(m+2)\,O(\varphi^{-m/2}).
\]
因此在 \(m\to\infty\) 的尺度上，零点只能把平凡上界 \(\mathsf{Col}_m\le 1\) 改进一个指数级小量；要获得碰撞缺口的真实 \(O(1/M)\) 尺度与精细渐近，关键在于控制非零频率处的乘积振幅分布（见注 \ref{rem:fold-zero-sparse-necessity} 与本节的共振常数 \(C_\varphi\)）。
\end{corollary}

\textbf{证明：}
由定理 \ref{thm:fold-zero-density-sparse} 有 \(\card{\mathcal{Z}_m}\le \tau(m+2)\,F_{\lfloor (m+2)/2\rfloor}\)，两边同除以 \(M=F_{m+2}\) 并用 \(F_{\lfloor (m+2)/2\rfloor}/F_{m+2}=O(\varphi^{-m/2})\) 即得。证毕。

\input{sections/generated/tab_fold_multiplicity_histogram}

\subsection{二进制区间到 Zeckendorf 前缀的折叠：最大纤维与饱和判别}\label{app:fold-binary-prefix}

前文主要讨论 \(\Fold_m:\Omega_m=\{0,1\}^m\to X_m\)（Fibonacci 权重口径）诱导的折叠多重度。
为刻画“\(\,2^m\) 个二进制地址在 Zeckendorf（\(\varphi\)-基）分层中的投影压缩”，这里引入一个与 \(|\Omega_m|=2^m\) 同规模、但以\emph{整数区间}为输入的折叠投影。

\begin{definition}[二进制区间折叠 \(\mathrm{Fold}^{\mathrm{bin}}_m\)]\label{def:fold-bin}
设 \(Z:\ZZ_{\ge 0}\to\{0,1\}^{\NN}\) 为 Zeckendorf 编码：对每个 \(n\ge 0\)，存在唯一的合法位串
\[
Z(n)=(z_1,z_2,\dots),\qquad z_k\in\{0,1\},\quad z_k z_{k+1}=0,
\]
使得
\[
n=\sum_{k\ge 1} z_k F_{k+1}.
\]
记截断算子 \(\pi_m(z_1,z_2,\dots):=(z_1,\dots,z_m)\in X_m\)。定义
\[
\mathrm{Fold}^{\mathrm{bin}}_m:\{0,1,\dots,2^m-1\}\to X_m,\qquad
\mathrm{Fold}^{\mathrm{bin}}_m(n):=\pi_m(Z(n)).
\]
\end{definition}

\begin{definition}[\(K(m)\) 与 Zeckendorf 值]\label{def:K-of-m}
令 \(K(m)\) 为满足
\begin{equation}\label{eq:K-of-m}
F_{K(m)+1}\le 2^m-1 < F_{K(m)+2}
\end{equation}
的唯一整数。对 \(w=(w_1,\dots,w_m)\in X_m\) 定义其 Zeckendorf 值
\[
V_m(w):=\sum_{k=1}^m w_k F_{k+1}.
\]
\end{definition}

\begin{proposition}[纤维的尾部表示]\label{prop:fold-bin-fiber-tail}
设 \(m\ge 1\)，\(w\in X_m\)，并令 \(K=K(m)\)。记尾部长度 \(L:=K-m\ge 0\)。
则 \(\mathrm{Fold}^{\mathrm{bin}}_m{}^{-1}(w)\) 与满足下列约束的尾部位串 \((c_{m+1},\dots,c_K)\in\{0,1\}^{L}\) 一一对应：
\[
n
=V_m(w)+\sum_{k=m+1}^{K} c_k F_{k+1},
\]
其中
\begin{enumerate}
  \item \textbf{相邻约束：}\quad \(c_k c_{k+1}=0\)（对 \(m+1\le k\le K-1\)）；
  \item \textbf{边界约束：}\quad \(w_m\,c_{m+1}=0\)；
  \item \textbf{阈值约束：}\quad \(V_m(w)+\sum_{k=m+1}^{K} c_k F_{k+1}\le 2^m-1\)。
\end{enumerate}
从而纤维多重度
\(
d^{\mathrm{bin}}_m(w):=\bigl|\mathrm{Fold}^{\mathrm{bin}}_m{}^{-1}(w)\bigr|
\)
只依赖于 \((V_m(w),w_m)\)。
\end{proposition}

\begin{corollary}[最大纤维、组合上界与饱和判别]\label{cor:fold-bin-saturation}
对任意 \(m\ge 1\)，最大纤维由 \(w=0^m\) 取得，并满足组合上界
\begin{equation}\label{eq:fold-bin-upper}
d^{\mathrm{bin}}_{\max}(m):=d^{\mathrm{bin}}_m(0^m)\le |X_{K(m)-m}|=F_{K(m)-m+2}.
\end{equation}
记 \(K=K(m)\)、\(L=K-m\)，并定义在相邻约束下的\emph{最大尾和}
\[
S_{\max}(m):=\max\Bigl\{\sum_{k=m+1}^{K} c_k F_{k+1}:\ (c_{m+1},\dots,c_K)\in\{0,1\}^{L},\ c_k c_{k+1}=0\Bigr\}.
\]
则对 \(w=0^m\)，上界 \eqref{eq:fold-bin-upper} 饱和当且仅当阈值约束对所有合法尾部都是冗余的，即
\begin{equation}\label{eq:fold-bin-sat-iff}
d^{\mathrm{bin}}_{\max}(m)=F_{K(m)-m+2}\quad\Longleftrightarrow\quad 2^m-1\ge S_{\max}(m).
\end{equation}
并且 \(S_{\max}(m)\) 具有完全显式的闭式（由“1010\(\cdots\)”极大尾部取得）：
\begin{equation}\label{eq:fold-bin-Smax-closed}
S_{\max}(m)=
\begin{cases}
F_{K(m)+2}-F_{m+1}, & L=K(m)-m\ \text{为奇数},\\
F_{K(m)+2}-F_{m+2}, & L=K(m)-m\ \text{为偶数}.
\end{cases}
\end{equation}
等价地，饱和判别可改写为
\[
F_{K(m)+2}-2^m\le
\begin{cases}
F_{m+1}-1,& L\ \text{奇},\\
F_{m+2}-1,& L\ \text{偶}.
\end{cases}
\]
\end{corollary}

\begin{remark}[指数级 Diophantine 近似与“仅有限次饱和”]\label{rem:fold-bin-finite}
由 \eqref{eq:fold-bin-Smax-closed} 可见，饱和条件强迫 \(2^m\) 与某个 Fibonacci 数 \(F_{K(m)+2}\) \emph{异常接近}。
用 Binet 公式 \(F_n=\varphi^n/\sqrt5+O(\varphi^{-n})\) 将其改写，可导出一个指数级小的线性对数形式约束：
\[
\Bigl|(K(m)+2)\log\varphi - m\log 2 - \log\sqrt5\Bigr|
\ \lesssim\ \Bigl(\frac{\varphi}{2}\Bigr)^m.
\]
由于 \(\varphi\) 为代数数而 \(2\) 为有理数，上式左侧是非零的对数线性形式；而 Baker 型下界（例如 Matveev 的显式形式）给出其\emph{至多多项式级}的小下界（见 \cite{Matveev2000LinearFormsLogs,BakerWustholz2007LogarithmicForms}），从而与右侧的指数级小量矛盾，推出：
\[
\text{上界饱和只能发生有限次。}
\]
本结论在本文的可复现管线中可直接数值审计（见表 \ref{tab:fold_binary_saturation_points}）。
\end{remark}

\begin{proposition}[计算 \(\boldsymbol{d^{\mathrm{bin}}_m(w)}\) 的 \(O(m)\) Fibonacci-digit DP]\label{prop:fold-bin-digit-dp}
给定 \(m\ge 1\) 与 \(w\in X_m\)。令 \(K=K(m)\) 并记 \(V=V_m(w)\)，再令 \(\mathrm{slack}:=2^m-1-V\)。
将 \(\mathrm{slack}\) 写为 Zeckendorf 位串 \(Z(\mathrm{slack})=(b_1,\dots,b_K)\)（合法，无相邻 \(11\)）。
则 \(d^{\mathrm{bin}}_m(w)\) 可由如下 digit DP 在 \(O(K-m)=O(m)\) 时间内精确计算：
\[
\mathrm{DP}(k,\mathrm{prev},\mathrm{tight})
\quad(k=K,K-1,\dots,m+1),
\]
其中 \(\mathrm{prev}\in\{0,1\}\) 记录上一个（更高位）是否取 \(1\)，\(\mathrm{tight}\in\{0,1\}\) 表示当前前缀是否仍与 \((b_k,\dots,b_K)\) 相同；
转移时允许选择 \(c_k\in\{0,1\}\)，满足
\[
c_k c_{k+1}=0,\qquad
\mathrm{tight}=1\Rightarrow c_k\le b_k,\qquad
w_m=1\Rightarrow c_{m+1}=0,
\]
并按是否保持等号更新 \(\mathrm{tight}\)。该 DP 的输出即为满足 \(\sum_{k=m+1}^K c_k F_{k+1}\le \mathrm{slack}\) 的尾部数目，从而等于 \(d^{\mathrm{bin}}_m(w)\)。
\end{proposition}

\input{sections/generated/tab_fold_binary_saturation_points}
