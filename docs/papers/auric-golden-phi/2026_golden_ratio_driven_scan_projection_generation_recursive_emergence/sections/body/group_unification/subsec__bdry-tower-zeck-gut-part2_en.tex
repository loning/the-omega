\label{subsec:bdry-tower-zeck-gut-part2}
\paragraph{Exceptional kernel sector of the \(m=10\) degeneracy spectrum: \texorpdfstring{$52=\dim\mathfrak{f}_4$}{52=dim f4}}\label{par:gut-exceptional-kernel-f4}
In the \(m=10\) row of Table~\ref{tab:fold_bin_degeneracy_spectrum_m6_m12},
the bin-fold degeneracy spectrum decomposes the stable type set \(X_{10}\) by degeneracy \(d^{\mathrm{bin}}_{10}(w)\) into three sectors:
\[
X_{10}=\mathcal{S}_{10,5}\ \sqcup\ \mathcal{S}_{10,8}\ \sqcup\ \mathcal{S}_{10,9},\qquad
\bigl(|\mathcal{S}_{10,5}|,|\mathcal{S}_{10,8}|,|\mathcal{S}_{10,9}|\bigr)=(55,52,37),
\]
where \(\mathcal{S}_{10,d}:=\{w\in X_{10}: d^{\mathrm{bin}}_{10}(w)=d\}\).
Note that \(|\mathcal{S}_{10,5}|=55\) is the Fibonacci number \(F_{10}\), consistent with the two-route locking of \(\dim=55\) above;
while \(|\mathcal{S}_{10,8}|=52\) satisfies the exact identity with the adjoint dimension of the simple exceptional Lie algebra \(\mathfrak{f}_4\):
\[
\boxed{\ |\mathcal{S}_{10,8}|=52=\dim\mathfrak{f}_4\ }.
\]
Within the same trisector, \(|\mathcal{S}_{10,5}|=55\) and \(|\mathcal{S}_{10,9}|=37\) do not belong to
\(\{\dim\mathfrak{g}:\mathfrak{g}\in\{G_2,F_4,E_6,E_7,E_8\}\}\),
and in the remaining rows of Table~\ref{tab:fold_bin_degeneracy_spectrum_m6_m12} (\(m\in\{6,8,12\}\)) no sector of size \(52\) appears;
therefore the degeneracy spectrum at \(m=10\) endogenously produces, at the dimension level, a separately identifiable ``exceptional kernel sector'' \(\mathcal{S}_{10,8}\);
this identification depends only on the enumerable degeneracy spectrum decomposition, not on the candidate group table preset by the Zeckendorf--GUT dimension signature dictionary.

\paragraph{Completion of the exceptional kernel \texorpdfstring{$52$}{52} with the maximal fiber extremal mode \texorpdfstring{$26$}{26} to \texorpdfstring{$E_6$}{E6}: \texorpdfstring{$52+26=78$}{52+26=78}}\label{par:gut-e6-completion-52-26}
Let \(D_m:=\max_{x\in X_m} d_m(x)\) be the maximal fiber of the window fold \(\Fold_m\) (Theorem~\ref{thm:pom-max-fiber}).
From Table~\ref{tab:fold_max_fiber_achievers_hiddenbit_metrics} one can reproducibly read off
\(
D_{13}=26
\).
Pairing the exceptional kernel sector dimension \(52\) from Conclusion~\ref{par:gut-exceptional-kernel-f4} with this extremal mode scale yields the rigid dimension closure:
\[
\boxed{\ 52+D_{13}=52+26=78=\dim\mathfrak{e}_6\ }.
\]
Therefore, one can propose a discrete completion template that depends only on two existing spectral data: the bin-fold degeneracy spectrum at \(m=10\) selects the exceptional kernel (\(\dim\mathfrak{f}_4\)), and the maximal fiber extremal scale at \(m=13\) provides the completion mode, thereby achieving dimension-level closure to the adjoint dimension of \(\mathfrak{e}_6\).

\paragraph{Extremal mode--driven exceptional tower increments: pointwise alignment with the maximal fiber spectrum}\label{par:gut-maxfiber-exceptional-tower}
From Table~\ref{tab:fold_max_fiber_achievers_hiddenbit_metrics} one further reads off
\[
D_{16}=55,\qquad D_{18}=89,
\]
and combining with the public adjoint dimensions of exceptional Lie algebras
\[
\dim\mathfrak{e}_6=78,\qquad \dim\mathfrak{e}_7=133,\qquad \dim\mathfrak{e}_8=248,
\]
one obtains two dimension increment identities (whose increment sources can be interpreted via the maximal fiber extremal mode):
\[
\boxed{\ \dim\mathfrak{e}_7-\dim\mathfrak{e}_6=55=D_{16}\ },
\qquad
\boxed{\ \dim\mathfrak{e}_8-\dim\mathfrak{e}_7=115=89+26=D_{18}+D_{13}\ }.
\]
Consequently, the ascent of the exceptional tower at the dimension level can be rewritten as discrete increment completion supplied by the maximal fiber spectrum; this formulation does not depend on a specific group embedding choice, and its inputs are solely the extremal spectrum of the fold projection and the standard dimension table.

\paragraph{Unification chain skeleton of the \(m=10\) trisector: \texorpdfstring{$(55,52,37)$}{(55,52,37)}}\label{par:gut-trisector-55-52-37}
By Table~\ref{tab:fold_bin_degeneracy_spectrum_m6_m12}, the bin-fold degeneracy spectrum at \(m=10\) forces the three-sector skeleton \((55,52,37)\) at the stable type level.
Combining Conclusions~\ref{par:gut-exceptional-kernel-f4}--\ref{par:gut-maxfiber-exceptional-tower}, two of the three blocks can be given structural positioning:
\begin{itemize}
  \item \(\mathcal{S}_{10,8}\) (sector size \(52\)): exceptional kernel sector (\(\dim\mathfrak{f}_4\) anchor);
  \item \(\mathcal{S}_{10,5}\) (sector size \(55\)): completion scale sector (aligned with \(D_{16}=55\) and the \(E_6\to E_7\) dimension increment);
  \item \(\mathcal{S}_{10,9}\) (sector size \(37\)): residual interface sector; its dimension-level positioning as a complementary interface can be forced by the same-level decomposition of \(D_{18}\) (Interface Conclusion~\ref{par:gut-d18-endogenous-split}), and under the minimal consistency principle it admits a representation-theoretic semantic closure candidate \(\mathfrak{so}(9)\oplus\mathfrak{u}(1)\) (Interface Conclusion~\ref{par:gut-sector37-so9-u1}; audit criterion see~\ref{par:gut-foldbin-fold-audit-criteria}).
\end{itemize}
This ternary skeleton takes the degeneracy spectrum as its primary input and the dimension closure of the exceptional kernel/extremal mode as its secondary input, thereby promoting the ``interface sector'' (\(37\)) to an object that must be explained.

\paragraph{Interface conclusion: cross-scale complement identity between \texorpdfstring{$\mathrm{Fold}^{\mathrm{bin}}$}{Foldbin} and \texorpdfstring{$\mathrm{Fold}$}{Fold}}\label{par:gut-foldbin-fold-complement-identity}
The following cluster of interface conclusions takes as its sole input the reproducible enumeration spectral data already established in this paper (in particular the degeneracy spectrum output of Table~\ref{tab:fold_bin_degeneracy_spectrum_m6_m12} and the maximal fiber closed-form interface),
without introducing additional extrapolation or interpretive-layer candidates; the exposition does not restate existing paragraphs but records only interface assertions and their direct corollaries/verification protocols.
For the binary interval fold \(\mathrm{Fold}^{\mathrm{bin}}_m:\{0,\dots,2^m-1\}\to X_m\), write
\(
d^{\mathrm{bin}}_m(w):=\bigl|\mathrm{Fold}^{\mathrm{bin}}_m{}^{-1}(w)\bigr|
\)
and let \(d_{\min}(m):=\min_{u\in X_m}d^{\mathrm{bin}}_m(u)\).
For each \(d\), define the degeneracy spectrum sector
\(
\mathcal{S}_{m,d}:=\{w\in X_m:\ d^{\mathrm{bin}}_m(w)=d\}
\).
Then at the already published even window lengths \(m\in\{6,8,10,12\}\), the ``most rigid sector'' complement capacity of the bin-fold is strictly locked to the same quantity as the maximal fiber at a higher resolution of the window fold \(\Fold\):
\[
\boxed{\ \bigl|X_m\setminus \mathcal{S}_{m,d_{\min}(m)}\bigr|\;=\;D_{2m-2}\ }.
\]
This identity constitutes a cross-audit closure that requires no extrapolation: the left-hand side is directly given by the degeneracy spectrum output of Table~\ref{tab:fold_bin_degeneracy_spectrum_m6_m12}, while the right-hand side is given by the maximal fiber closed-form Theorem~\ref{thm:pom-max-fiber} (equivalently its tabular readout); the corresponding verification formulas have been scripted and output as follows:
\input{sections/generated/eq_foldbin_fold_complement_identity_m6_m12}

\paragraph{Interface conclusion: endogenous decomposition of \texorpdfstring{$D_{18}$}{D18} at \texorpdfstring{$m=10$}{m=10} and the forced positioning of the \texorpdfstring{$37$}{37} interface sector}\label{par:gut-d18-endogenous-split}
From the \(m=10\) special case of Interface Conclusion~\ref{par:gut-foldbin-fold-complement-identity},
\(
D_{18}=|X_{10}\setminus\mathcal{S}_{10,5}|
\).
On the other hand, Conclusion~\ref{par:gut-exceptional-kernel-f4} has already provided the same-level trisector decomposition
\(
X_{10}=\mathcal{S}_{10,5}\sqcup\mathcal{S}_{10,8}\sqcup\mathcal{S}_{10,9}
\)
with exact counts \((55,52,37)\), and structurally identified \(|\mathcal{S}_{10,8}|\) as \(\dim\mathfrak{f}_4\).
Therefore one obtains the same-level decomposition identity
\[
D_{18}=|\mathcal{S}_{10,8}|+|\mathcal{S}_{10,9}|=52+37,
\qquad
\boxed{\ |\mathcal{S}_{10,9}|\;=\;D_{18}-\dim\mathfrak{f}_4\ }.
\]
In other words, \(D_{18}\), previously read in as an ``external extremal mode scale,'' can equivalently be rewritten as: the unique interface dimension (\(37\)) obtained by subtracting the exceptional kernel (\(52\)) from the degeneracy spectrum complement at \(m=10\).

\paragraph{Interface conclusion: minimal consistent semantic closure of the \texorpdfstring{$37$}{37} interface sector: \texorpdfstring{$\mathfrak{so}(9)\oplus\mathfrak{u}(1)$}{so(9)+u(1)}}\label{par:gut-sector37-so9-u1}
Without introducing additional candidate group tables and relying solely on the two anchor points already fixed in this paper---the exceptional kernel \(|\mathcal{S}_{10,8}|=52=\dim\mathfrak{f}_4\) (Conclusion~\ref{par:gut-exceptional-kernel-f4}) and the spin-paired readout template of the boundary layer \(33=1\oplus 16\times 2\) (see also the reproducible interface entry for the ``NAP selector and the \(\ZZ_{34}\) Fourier decomposition certificate at \(m=11\)'' in \S\ref{subsec:group-unification-audit-pointers})---one can provide a minimal consistent semantic closure scheme for the \(37\) sector.
\par
Standard Lie algebra facts show: \(\mathfrak{f}_4\) contains the maximal subalgebra \(\mathfrak{so}(9)\), and under \(\mathfrak{so}(9)\) the adjoint representation satisfies
\(
52=36\oplus 16
\)
(where \(16\) is the spinor-type complement).
If the \(52\) sector bears the ``exceptional kernel,'' then the minimally compatible interface should provide a \(36\)-dimensional \(\mathfrak{so}(9)\) skeleton and additionally carry a scale generator to realize the gluing from the kernel to the increment block;
this strictly matches at the dimension level the
\(
37=36+1
\)
forced by Interface Conclusion~\ref{par:gut-d18-endogenous-split}.
Therefore we propose the following closure candidate:
\[
\boxed{\ \mathcal{S}_{10,9}\text{'s \(37\)-dimensional interface can be interpreted as }\mathfrak{so}(9)\oplus\mathfrak{u}(1)\ }.
\]
Here \(\mathfrak{so}(9)\) provides the ``interface skeleton,'' while \(\mathfrak{u}(1)\) carries the single-generator direction compatible with the scale/endpoint precision potential; the spinor-type \(16\)-dimensional directions then appear at adjacent resolution layers via the paired readout template, thereby closing ``exceptional kernel---interface---spinor matter'' into a consistent chain across adjacent layers.

\paragraph{Interface conclusion: directly falsifiable unification audit criteria---cross-scale complement, same-level decomposition, and irreducibility}\label{par:gut-foldbin-fold-audit-criteria}
Beyond the ``kernel---mode---tower'' fingerprint of Conclusion~\ref{par:gut-exceptional-fingerprint}, the above cross-scale complement identity and interface closure provide a set of more direct failure criteria; any single failure negates the corresponding closure chain:
\begin{enumerate}
  \item \textbf{Cross-scale complement audit (even window lengths):} For even \(m\ge 6\), enumerate the degeneracy spectrum of \(\mathrm{Fold}^{\mathrm{bin}}_m\) and take \(d_{\min}(m)\). The identity
  \[
  |X_m|-|\mathcal{S}_{m,d_{\min}(m)}|=D_{2m-2}
  \]
  should hold. This paper verifies it for \(m\in\{6,8,10,12\}\) (Interface Conclusion~\ref{par:gut-foldbin-fold-complement-identity}); its stability at larger \(m\) constitutes a new negation criterion.
  \item \textbf{Same-level decomposition audit of \texorpdfstring{$D_{18}$}{D18} (\texorpdfstring{$m=10$}{m=10}):} Using only the degeneracy spectrum output at the \(m=10\) row, the identity
  \[
  |X_{10}\setminus\mathcal{S}_{10,5}|=|\mathcal{S}_{10,8}|+|\mathcal{S}_{10,9}|=D_{18},
  \]
  namely \(52+37=89\), should hold (Interface Conclusion~\ref{par:gut-d18-endogenous-split}).
  \item \textbf{Representation-theoretic audit of the \texorpdfstring{$37$}{37} interface (\texorpdfstring{$\mathfrak{so}(9)\oplus\mathfrak{u}(1)$}{so(9)+u(1)} scheme):}
  If the semantic closure of \(|\mathcal{S}_{10,9}|=37\) is taken to be \(\mathfrak{so}(9)\oplus\mathfrak{u}(1)\), then for the natural linearized action on \(\mathcal{S}_{10,9}\) induced by the framework's ``scan/translation'' or ``stable type semiring multiplication/scan operator,'' its invariant subspace structure should exhibit:
  \begin{itemize}
    \item a \(1\)-dimensional invariant direction (corresponding to \(\mathfrak{u}(1)\));
    \item the remaining \(36\)-dimensional part behaving as a single irreducible \(\mathfrak{so}(9)\) adjoint-type skeleton.
  \end{itemize}
  Any irreducible decomposition pattern inconsistent with this negates the semantic closure scheme.
\end{enumerate}

\paragraph{Summary (from cross-window assembly to same-level complement)}
The above four items rewrite the unification assemblies that previously spanned different window lengths (in particular the extremal mode input of \(D_{18}\)) as the complement structure of the degeneracy spectrum within the single level \(m=10\),
and on this basis promote the interface sector \(37\) from a ``residual count'' to a positionable, closable, and falsifiable object.
If one continues to extend to higher-order unification, the next hard task is made explicit:
test the stability of Criterion~1 at larger even \(m\), and whether the irreducibility audit of Criterion~3 passes.

\paragraph{Falsifiable audit fingerprints (exceptional kernel---extremal mode---exceptional tower)}\label{par:gut-exceptional-fingerprint}
If the ``exceptional kernel + extremal mode completion'' interpretation is taken as a structural constraint of the unification chain, then at least the following three auditable fingerprints should be satisfied:
\begin{enumerate}
  \item \textbf{Exceptional kernel sector fingerprint:} In the reproducible low-resolution sweep (Table~\ref{tab:fold_bin_degeneracy_spectrum_m6_m12}), there exists a degeneracy spectrum block of sector size exactly \(52\); for example at \(m=10\), \(|\mathcal{S}_{10,8}|=52\), aligned with \(\dim\mathfrak{f}_4\).
  \item \textbf{\texorpdfstring{$E_6$}{E6} completion fingerprint:} The maximal fiber extremal mode provides \(D_{13}=26\) (Table~\ref{tab:fold_max_fiber_achievers_hiddenbit_metrics}), so that \(52+26\) closes to \(\dim\mathfrak{e}_6=78\); if under an alternative fold caliber/kernel family this scale no longer appears, then the ``kernel+mode'' completion mechanism fails in that version.
  \item \textbf{Exceptional tower increment fingerprint:} The dimension increments must satisfy
  \(\dim\mathfrak{e}_7-\dim\mathfrak{e}_6=D_{16}\) and \(\dim\mathfrak{e}_8-\dim\mathfrak{e}_7=D_{18}+D_{13}\) in numerical alignment (Conclusion~\ref{par:gut-maxfiber-exceptional-tower}), thereby collapsing the completion from continuous fine-tuning to a block rule discretized by the extremal mode.
\end{enumerate}

\paragraph{Extended symplectic phase space forcing \texorpdfstring{$\mathfrak{sp}(10)$}{sp(10)}: \texorpdfstring{$55=12\oplus 9\oplus 24\oplus 10$}{55=12+9+24+10} four-block unification skeleton}\label{par:gut-sp10-four-blocks}
Combining the three-channel potential--response symplectic phase space (Remark~\ref{rem:sp6-minimal-continuous-envelope}) with the Legendre--Fenchel dual front of the two-dimensional mixing pressure (Remark~\ref{rem:pom-psi-2d-legendre-front}), one obtains an extended phase space organized by five pairs of conjugate coordinates: take
\[
x:=(\theta_{\varphi},\theta_{\pi},\theta_{e},\ q-1,\ \log u),\qquad
y:=(J_{\varphi},J_{\pi},J_{e},\ \gamma,\ \mathcal{E}),
\]
where the conjugate pairings \((q-1,\gamma)\) and \((\log u,\mathcal{E})\) are given by the two-dimensional convex duality structure
\[
I(\gamma,\mathcal{E})
=\sup_{q\ge 2,\ u\in(0,1)}
\Bigl((q-1)\gamma+(\log u)\mathcal{E}-\Psi(q,u)\Bigr)
\]
(Remark~\ref{rem:pom-psi-2d-legendre-front}).
Its standard symplectic form is
\[
\omega_{\mathrm{tot}}
:=\sum_{c\in\{\varphi,\pi,e\}}\dd\theta_{c}\wedge \dd J_{c}
+\dd(q-1)\wedge \dd\gamma
+\dd(\log u)\wedge \dd\mathcal{E}.
\]
Under this caliber, the minimal continuous linear envelope preserving \(\omega_{\mathrm{tot}}\) is \(\mathrm{Sp}(10,\RR)\);
therefore the binary unification dimension anchor \(D^{\mathrm{bin}}_{\mathrm{GUT}}=55\) is promoted to the \(C_5\) branch forced by the symplectic structure:
\[
\boxed{\ \dim\mathfrak{sp}(10,\RR)=55\ }\qquad(\text{to be distinguished from the dual branch }\dim\mathfrak{so}(11)=55).
\]
\par
Under the standard direct-sum splitting of ``three channels \(\oplus\) two scale axes,'' \(\mathfrak{sp}(10,\RR)\) admits the canonical three-block decomposition
\[
\mathfrak{sp}(10,\RR)\ \cong\ \mathfrak{sp}(6,\RR)\ \oplus\ \mathfrak{sp}(4,\RR)\ \oplus\ \mathcal{X},
\qquad
55=21+10+24,
\]
where the cross block \(\mathcal{X}\) has dimension strictly equal to \(24\).
Therefore \(\dim\mathfrak{su}(5)=24\) plays a more natural role in this framework as the dimension slot of the ``channel (\(\mathfrak{sp}(6)\))---scale (\(\mathfrak{sp}(4)\)) interface layer,''
while \(\mathrm{Sp}(10)\) provides the symplectic closure semantics at the envelope level.
\par
Combining the rigid splitting \(\mathfrak{sp}(6,\RR)=\mathfrak{u}(3)\oplus\mathfrak{p}\) discussed above with the \(21+10+24\) three-block decomposition, the binary dark capacity \(H^{\mathrm{bin}}_6=43\) receives an exact attribution consistent with the block structure:
\[
\boxed{\ 43\ =\ 9\ (\mathfrak{u}(3))\ +\ 24\ (\mathcal{X})\ +\ 10\ (\mathfrak{sp}(4,\RR))\ }.
\]
Consequently the \(55\)-dimensional unification slot is locked at the structural level to
\[
\boxed{\ 55\ =\ 12\ \oplus\ 9\ \oplus\ 24\ \oplus\ 10\ },
\]
where \(12\) corresponds to the visible sector scale read out via low degeneracy rigidity, and the remaining \(43\) constitutes the three-block attributable hidden capacity.

\begin{proposition}[Four-block spectral degeneracy signature and \texorpdfstring{$U(3)$}{U(3)}-isotropy recovery at the self-dual unification point]\label{prop:gut-u1-four-block-spectrum-signature}
Under the four-block decomposition
\(
55=12\oplus 9\oplus 24\oplus 10
\)
determining the dimension skeleton, at the self-dual unification point \(u=1\) (Remark~\ref{rem:selfdual-u1-six-branch-lock}), any realization reading out the unification slot via a linearized transfer operator or response operator should exhibit an invariant block structure compatible with this four-block decomposition; the block multiplicities should stabilize at the observable level to \(\{12,9,24,10\}\) and remain robust under auditable perturbations of the kernel/gate.\par
Furthermore, at this point the channel response tensor should recover \(U(3)\)-isotropy, i.e., be proportional to the identity matrix; the auditable entry point is provided by the isotropy point criterion and the corresponding scan table above.
\end{proposition}

\paragraph{NAP selector: \texorpdfstring{$SO(10)$}{SO(10)} is discretely and uniquely selected under the ``non-abelian layer preservation'' constraint}
Taking \(\{6,8\}\) as the ``non-abelian resonance layers'' (corresponding to the visible counting interfaces \(3\) and \(8\)), and imposing the \textbf{non-abelian layer preservation} constraint:
when unifying upward, replacement/absorption of the abelian layer (\(m=4\)) is allowed, but \(\{6,8\}\) must be preserved as \emph{independent layers} in \(\Sigma(\mathfrak g)\).
Under this constraint, scanning \(\Sigma(\mathfrak g)\) over all simple Lie algebras and minimizing the dimension \(D=\dim\mathfrak g\),
the minimal solution is \(D_5=\mathfrak{so}(10)\), and its signature is precisely \(\Sigma=\{6,8,11\}\).
The scan table of this selector and the counting identity certificate of the ``single discrete replacement \(4\to 11\)'' are generated by scripts as follows:
\input{sections/generated/tab_nap_selector_so10}
\input{sections/generated/eq_nap_selector_so10}
\par
More strongly, if \(\{4,6,8\}\) are all required to remain independent (no merging/absorption allowed), then the minimal simple solution jumps to \(D_{10}=\mathfrak{so}(20)\) (\(\dim=190\), \(\Sigma=\{4,6,8,11,14\}\); see the strict part of Table~\ref{tab:nap_selector_so10}),
thereby promoting ``the abelian layer must be rewritten by unification'' to an auditable, falsifiable discrete forcing conclusion.

\paragraph{Information cost selector \texorpdfstring{$\Omega$}{Omega}: the dual minimum of minimal dimension and minimal information cost for \texorpdfstring{$SO(10)$}{SO(10)}}
Beyond minimizing the dimension \(D=\dim\mathfrak g\), one can use the average multiplicity of the resolution fold
\[
d_m:=\frac{2^m}{F_{m+2}}
\]
to define an independent ``information cost'' functor
\[
\Omega(\mathfrak g):=\sum_{m\in\Sigma(\mathfrak g)}\log d_m.
\]
\begin{proposition}[Linearization of the information cost: \texorpdfstring{$\Omega$}{Omega} is asymptotically equivalent to a ``total resolution action'']\label{prop:gut-omega-linear-action}
There exist constants \(c_0,c_1\) (where \(c_1=\log(2/\varphi)\)) such that for all \(m\ge 1\),
\[
\log d_m
=
c_1\,m+c_0+O(\varphi^{-2m}).
\]
Consequently, for any candidate \(\mathfrak g\),
\[
\Omega(\mathfrak g)
=
c_1\sum_{m\in\Sigma(\mathfrak g)} m
\;+\;
c_0\,|\Sigma(\mathfrak g)|
\;+\;
O\!\left(\sum_{m\in\Sigma(\mathfrak g)}\varphi^{-2m}\right).
\]
Therefore, under a fixed ``mandatory layer'' constraint (such as NAP or strict), the minimization of \(\Omega(\mathfrak g)\) is asymptotically equivalent to the minimization of
\(\sum_{m\in\Sigma(\mathfrak g)} m\); \(\Omega\) can be understood as a linear cost endogenously induced by the golden growth rate of Fibonacci, rather than an empirical penalty term.
\end{proposition}
\begin{proof}
From the Binet asymptotic \(F_{m+2}=\varphi^{m+2}/\sqrt5\cdot(1+O(\varphi^{-2m}))\), one obtains
\(
\log F_{m+2}=(m+2)\log\varphi-\tfrac12\log 5+O(\varphi^{-2m})
\).
Substituting into \(d_m=2^m/F_{m+2}\) yields
\[
\log d_m
=m\log 2-\log F_{m+2}
=m\log(2/\varphi)+\Bigl(\tfrac12\log 5-2\log\varphi\Bigr)+O(\varphi^{-2m}),
\]
and setting \(c_1=\log(2/\varphi)\), \(c_0=\tfrac12\log 5-2\log\varphi\) gives the first formula; summing over \(m\in\Sigma(\mathfrak g)\) gives the second. \qed
\end{proof}
Under the NAP constraint \(\{6,8\}\subseteq\Sigma(\mathfrak g)\), scanning over simple Lie algebras and minimizing \(\Omega\), the minimal solution is again given by \(SO(10)\) (\(\Omega(SO(10))\approx 4.826\)), and the next candidate (e.g., \(SO(12)\)) is already significantly higher (Table~\ref{tab:gut_information_cost_selector}).
Likewise, under the strict constraint \(\{4,6,8\}\subseteq\Sigma(\mathfrak g)\), the minimal solution jumps to \(SO(20)\), accompanied by an order-of-magnitude jump in \(\Omega\) (\(\Omega(SO(20))\approx 8.328\)).
\input{sections/generated/tab_gut_information_cost_selector}
Therefore, the \(SO(10)\) branch is simultaneously the dual minimum of ``minimal dimension'' and ``minimal information cost'' within this framework, providing a second independent and auditable reason for its discrete selection, in parallel with dimension minimization.

\paragraph{Canonical splitting of the \texorpdfstring{$m=11$}{m=11} charge layer: \texorpdfstring{$34=1\oplus 33$}{34=1+33} and \texorpdfstring{$33=(16\ \text{pairs})+1$}{33=(16 pairs)+1}}
From \(|X_{11}^{\mathrm{bdry}}|=F_9=34\) and the explicit bijection \(X_{11}^{\mathrm{bdry}}\cong X_7\) (\(u=10\,v\,01\)),
one can use the Zeckendorf value domain canonical numbering \(j\in\ZZ_{34}\) to provide unambiguous labels for the \(34\) boundary modes at \(m=11\).
Under this numbering, the translation action \(T:e_j\mapsto e_{j+1}\) in its real regular representation forces a canonical \(1\oplus 33\) template: \(Y_0\) is the unique \(T\)-invariant direction, and its orthogonal complement automatically has dimension \(33\).
Within this \(33\)-dimensional orthogonal complement, the coarse spectral grouping further forces \(33=1\oplus 16\times 2\): \(Y_{17}\) yields the unique self-conjugate real direction, and the remainder consists of \(16\) two-dimensional rotation planes:
\input{sections/generated/eq_m11_boundary_z34_decomposition}
This decomposition depends only on the representation theory of \(\ZZ_{34}\) and the canonical numbering, not on any extraneous ``manual pairing'';
therefore it provides an auditable target for the spectral grouping when the projection gate/kernel spectral operators are operatorized (in particular, the heavy direction count \(34-1=33\) precisely interlocks at the counting level with the \(m=11\) charge layer positioning determined by the NAP selector).
