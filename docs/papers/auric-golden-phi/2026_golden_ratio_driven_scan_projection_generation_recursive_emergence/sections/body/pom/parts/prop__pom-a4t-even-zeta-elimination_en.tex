% (User input window)
% 2026-02-14 13:41:27 (Asia/Singapore)

\begin{proposition}[Even Spectrum Quintic Closed Form and Even Spectrum Simplicity for Fourth-Order Kernel Family]\label{prop:pom-a4t-even-zeta-quintic}
Following Remark \ref{rem:pom-collision-rh-break-a4-threshold}, let \(A_4(t)\) be the fourth-order collision kernel family, and define
\[
D_t(z):=\det(I-zA_4(t)).
\]
Then the strict identity holds
\[
D_t(z)\,D_t(-z)=\det(I-z^2A_4(t)^2)=:E_t(u)\qquad(u=z^2),
\]
where \(E_t(u)\in\ZZ[t,u]\) is a quintic integer-coefficient polynomial in \(u\), with completely explicit form and discriminant factorization (equation \eqref{eq:collision_kernel_A4_even_zeta_quintic}).
In particular, for all \(t\ge 0\) and \(t\neq 1\), we have \(\mathrm{Disc}_u(E_t)\neq 0\), thus the five roots of \(E_t(u)\) are all simple at the even spectrum level; therefore on the parameter interval containing \(t_\star\) and integer point \(t=7\), even spectrum degeneracy cannot serve as a trigger mechanism for kernel-version Newman adherence breaking.
\end{proposition}
\begin{proof}
The identity comes from determinant multiplicativity:
\(
\det(I-zA)\det(I+zA)=\det\bigl((I-zA)(I+zA)\bigr)=\det(I-z^2A^2)
\).
Equation \eqref{eq:collision_kernel_A4_even_zeta_quintic} gives \(\mathrm{Disc}_u(E_t)=-256(t-1)^2(t+1)^2Q(t)\), where \(Q(t)\) has all positive coefficients, so when \(t\ge 0\) we have \(Q(t)>0\); thus \(\mathrm{Disc}_u(E_t)=0\) on this half-axis can only occur at \(t=1\).
Therefore for \(t\ge 0\) and \(t\neq 1\), \(E_t\) has no repeated roots, and even spectrum points are always simple. \qed
\end{proof}

\input{sections/generated/eq_collision_kernel_A4_even_zeta_quintic}

\begin{proposition}[Completion Polynomial Twisted Reciprocity and \texorpdfstring{$\Gamma$}{Gamma}-Factor Splitting]\label{prop:pom-a4t-even-spectrum-completion-gamma-factorization}
Following Proposition \ref{prop:pom-a4t-even-zeta-quintic}, let
\[
E_t(u):=\det(I-uA_4(t)^2)\in\ZZ[t,u],
\qquad
\Xi_t(u):=u^5E_t(1/u)+4E_t(u).
\]
Then the strict identity holds
\[
\boxed{\;
\Xi_t(u)
=
u\,(tu+3u^2-2)\,(tu+4t-5u^2+4u+6)\;}
\]
thus \(\Xi_t(u)\) completely splits in \(\ZZ[t,u]\) into one linear factor and two quadratic \(\Gamma\)-factors.
\end{proposition}
\begin{proof}
By the closed form in equation \eqref{eq:collision_kernel_A4_even_zeta_quintic},
\(E_t(u)=1-(2t+4)u+(t^2-4)u^2+(4t+8)u^3+4u^4-4u^5\).
Substituting this into \(\Xi_t(u)=u^5E_t(1/u)+4E_t(u)\) and expanding gives the shown factorization identity. \qed
\end{proof}

\begin{proposition}[\texorpdfstring{$\Gamma$}{Gamma}-Factor Inversion: Even Spectrum Quintic Polynomial Uniquely Reconstructed from Two Quadratic Factors]\label{prop:pom-a4t-even-spectrum-gamma-inversion}
Under the notation of Proposition \ref{prop:pom-a4t-even-spectrum-completion-gamma-factorization}, let
\[
Q_t(u):=(tu+3u^2-2)(tu+4t-5u^2+4u+6).
\]
Then the even spectrum polynomial satisfies the exact reconstruction formula
\[
\boxed{\;
E_t(u)=\frac{4u\,Q_t(u)-u^4\,Q_t(1/u)}{15}\ \in\ \ZZ[t,u]\; } .
\]
Thus the numerator being divisible by \(15\) in \(\ZZ[t,u]\) is a structural fact.
\end{proposition}
\begin{proof}
Proposition \ref{prop:pom-a4t-even-spectrum-completion-gamma-factorization} gives
\[
u^5E_t(1/u)+4E_t(u)=uQ_t(u).
\]
Rewriting this identity under \(u\mapsto 1/u\) and multiplying by \(u^5\) yields
\(
E_t(u)+4u^5E_t(1/u)=u^4Q_t(1/u)
\).
Treating the two equations as a linear system in the unknowns \((E_t(u),u^5E_t(1/u))\) and eliminating gives
\(
15E_t(u)=4uQ_t(u)-u^4Q_t(1/u)
\).
Since the left side is an integer-coefficient polynomial, the right side must be divisible by \(15\) in \(\ZZ[t,u]\), and the quotient is precisely \(E_t(u)\). \qed
\end{proof}

\begin{proposition}[Newman Octic Certificate Curve Intersection Characterization: Resultant Elimination of Perron Curve and Even Spectrum Embedding Curve]\label{prop:pom-a4t-newman-resultant-curve-intersection}
Following Remark \ref{rem:pom-collision-rh-break-a4-threshold}, let the fourth-order kernel family characteristic polynomial be
\(
p_t(x)=\det(xI-A_4(t))=x^5-2x^4-tx^3-2x+2
\), and write its Perron branch as the curve
\[
P_t(r):=r^5-2r^4-tr^3-2r+2=0
\qquad(r>0).
\]
Let \(E_t(u)=\det(I-uA_4(t)^2)\) as in Proposition \ref{prop:pom-a4t-even-zeta-quintic}, and define the even spectrum embedding curve
\(
r^5E_t(1/r)=0
\).
Then the resultant elimination of the two with respect to \(r\) gives a strict identity (equation \eqref{eq:collision_kernel_A4_newman_resultant_curve_intersection}):
its non-trivial factor is precisely the octic integer-coefficient certificate \(P_4(t)\) for the edge-weight threshold (equation \eqref{eq:collision_kernel_A4_edge_weight_threshold}).
Therefore the fourth-order Newman critical \(t_\star\) can be equivalently characterized as: after excluding the degenerate point \(t=-1\), the unique positive parameter point at which the Perron curve and even spectrum embedding curve intersect in the \((t,r)\)-plane.
\end{proposition}
\begin{proof}
By the resultant identity in equation \eqref{eq:collision_kernel_A4_newman_resultant_curve_intersection},
the zero set of \(\mathrm{Res}_r(P_t(r),r^5E_t(1/r))\) (excluding the degenerate factor \(t=-1\)) coincides with the zero set of \(P_4(t)\).
On the other hand, equation \eqref{eq:collision_kernel_A4_edge_weight_threshold} gives the unique positive real root \(t_\star\) of \(P_4\), thus the intersection parameter is unique on the positive half-axis. \qed
\end{proof}

\input{sections/generated/eq_collision_kernel_A4_newman_resultant_curve_intersection}

\begin{proposition}[Newman Octic Threshold Shorter Elimination Characterization: Intersection Certificate of Two Quintic Curves]\label{prop:pom-a4t-newman-short-elimination}
Following Proposition \ref{prop:pom-a4t-even-spectrum-gamma-inversion}, define
\[
G_t(r):=4r^4Q_t(1/r)-rQ_t(r).
\]
Then the strict identity always holds
\[
\boxed{\;G_t(r)=15\,r^5E_t(1/r)\;}
\]
thus the resultant with respect to \(r\) satisfies
\[
\boxed{\;
\mathrm{Res}_r\!\bigl(P_t(r),\,G_t(r)\bigr)
=-2^3\cdot 3^5\cdot 5^5\cdot (t+1)^2\,P_4(t)\;}
\]
where \(P_4(t)\) is the octic integer-coefficient threshold certificate in equation \eqref{eq:collision_kernel_A4_edge_weight_threshold}.
\end{proposition}
\begin{proof}
By Proposition \ref{prop:pom-a4t-even-spectrum-gamma-inversion} taking \(u=1/r\) we get
\[
E_t(1/r)=\frac{4r^3Q_t(1/r)-Q_t(r)}{15r^4},
\]
multiplying both sides by \(15r^5\) yields \(G_t(r)=15r^5E_t(1/r)\).
\par
The proportionality factor in the resultant follows from resultant homogeneity: if \(f\) is a polynomial of degree \(m\), then \(\mathrm{Res}(f,cg)=c^{m}\mathrm{Res}(f,g)\).
Here \(P_t(r)\) is a quintic polynomial in \(r\), so
\[
\mathrm{Res}_r(P_t,\,G_t)=15^5\,\mathrm{Res}_r(P_t,\,r^5E_t(1/r)).
\]
Then substituting the identity in equation \eqref{eq:collision_kernel_A4_newman_resultant_curve_intersection} and simplifying using \(15^5=3^5\cdot 5^5\) yields the conclusion. \qed
\end{proof}

\begin{proposition}[Threshold Reciprocal Locking Law: Rigid Proportionality of \texorpdfstring{$Q_t$}{Qt} at Critical Point]\label{prop:pom-a4t-newman-reciprocal-locking}
Let \(t_\star>0\) be the unique positive real root of \(P_4(t)\) (equation \eqref{eq:collision_kernel_A4_edge_weight_threshold}).
Then there exists \(r_\star>1\) such that simultaneously \(P_{t_\star}(r_\star)=0\) and \(G_{t_\star}(r_\star)=0\);
the latter condition is equivalent to the reciprocal locking relation
\[
\boxed{\;
Q_{t_\star}(r_\star)=4r_\star^{\,3}Q_{t_\star}(1/r_\star)\;}
\]
thus the Newman critical point is characterized by a strict cubic power proportionality balance between "forward evaluation/backward evaluation."
\end{proposition}
\begin{proof}
Let \(r_\star=\rho(A_4(t_\star))>1\) be the Perron branch, so \(P_{t_\star}(r_\star)=0\).
By Proposition \ref{prop:pom-a4t-newman-resultant-curve-intersection}, at this critical parameter the Perron curve and even spectrum embedding curve intersect,
thus \(E_{t_\star}(1/r_\star)=0\).
Then by the identity \(G_t(r)=15r^5E_t(1/r)\) in Proposition \ref{prop:pom-a4t-newman-short-elimination}, we obtain \(G_{t_\star}(r_\star)=0\).
Finally expanding \(G_{t_\star}(r_\star)=0\) gives the shown reciprocal locking relation. \qed
\end{proof}

% (User input window)
% 2026-02-14 20:22:37 (Asia/Singapore)
%
\begin{proposition}[Newman Critical Pole Quadratic Resonance Characterization: \texorpdfstring{$D_t(v^2)=D_t(-v)=0$}{Dt(v^2)=Dt(-v)=0}]\label{prop:pom-a4t-newman-pole-quadratic-resonance}
Following Remark \ref{rem:pom-collision-rh-break-a4-threshold}, let
\(
D_t(z):=\det(I-zA_4(t))
\)
be the spectral determinant of the fourth-order kernel family (its explicit closed form is in equation \eqref{eq:collision_kernel_A4_newman_pole_quadratic_resonance}).
Then the fourth-order Newman critical parameter \(t_\star\) is equivalent to the existence of a unique \(v_\star\in(0,1)\) such that
\[
D_{t_\star}(v_\star^2)=0,\qquad D_{t_\star}(-v_\star)=0.
\]
Equivalently, denoting \(z_+(t)\in(0,1)\) as the smallest positive real root of \(D_t\) and \(z_-(t)\in(-1,0)\) as its negative real root branch, we have
\[
z_+(t_\star)=z_-(t_\star)^2.
\]
After eliminating \(t\), \(v_\star\) satisfies an irreducible octic constraint, and \(t_\star\) can be explicitly given from \(v_\star\) (equation \eqref{eq:collision_kernel_A4_newman_pole_quadratic_resonance}).
\end{proposition}
\begin{proof}
By definition,
\(
D_t(z)=\det(I-zA_4(t))=z^5\det(z^{-1}I-A_4(t))=z^5p_t(1/z)
\);
hence \(D_t(z)=0\) if and only if \(1/z\in\mathrm{spec}(A_4(t))\).
On the other hand, Remark \ref{rem:pom-collision-rh-break-a4-threshold} gives the negative real root channel characterization of Newman threshold attainment: \(t=t_\star\) if and only if there exists \(y<0\) such that \(p_{t_\star}(y)=p_{t_\star}(y^2)=0\).
Let \(v:=-1/y\in(0,1)\), then \(p_{t_\star}(y)=0\iff D_{t_\star}(-v)=0\), and \(p_{t_\star}(y^2)=0\iff D_{t_\star}(v^2)=0\), thereby obtaining the shown equivalent characterization.
\par
The octic constraint and two explicit closed forms for \(t_\star\) are given by direct elimination and substitution expansion; their reproducible output is in equation \eqref{eq:collision_kernel_A4_newman_pole_quadratic_resonance} (script \texttt{scripts/exp\_collision\_kernel\_A4\_pole\_quadratic\_resonance\_and\_cyclotomic\_adjacency\_injection.py}). \qed
\end{proof}

\input{sections/generated/eq_collision_kernel_A4_newman_pole_quadratic_resonance}

\begin{proposition}[Kernel-Version Newman Offset Pole Geometry Criterion: \texorpdfstring{$\delta(t)=\log(z_+(t)/z_-(t)^2)$}{delta(t)=log(z+/z-^2)}]\label{prop:pom-a4t-newman-offset-pole-geometry}
Following Remark \ref{rem:pom-collision-rh-break-a4-threshold}, let \(t\ge 0\) and denote
\(
D_t(z):=\det(I-zA_4(t))
\).
Let \(z_+(t)\in(0,1)\) be the smallest positive real root of \(D_t\), and let \(z_-(t)\in(-1,0)\) be the unique negative real root of \(D_t\).
Define
\[
r(t):=\rho(A_4(t))=\frac{1}{z_+(t)},\qquad
\mu_-(t):=\frac{1}{z_-(t)}<0,\qquad
\Lambda(t):=|\mu_-(t)|=-\frac{1}{z_-(t)}.
\]
Then the Newman-type offset \(\delta(t):=\log(\Lambda(t)^2/r(t))\) satisfies the identity
\[
\delta(t)=\log\!\Bigl(\frac{z_+(t)}{z_-(t)^2}\Bigr),
\]
whence
\[
\delta(t)\begin{cases}
<0 &\Longleftrightarrow z_+(t)<z_-(t)^2,\\
=0 &\Longleftrightarrow z_+(t)=z_-(t)^2,\\
>0 &\Longleftrightarrow z_+(t)>z_-(t)^2.
\end{cases}
\]
Moreover \(\delta(t)=0\) if and only if \(t=t_\star\).
\end{proposition}
\begin{proof}
By \(D_t(z)=0\iff 1/z\in\mathrm{spec}(A_4(t))\) we know \(z_+(t)=1/r(t)\).
On the other hand, Remark \ref{rem:pom-a4t-discriminant-root-type} gives: for each \(t\ge 0\), \(p_t\) has exactly three real roots and one pair of conjugate non-real roots; and \(A_4(t)\) is a non-negative matrix, so its spectral radius \(r(t)\) is a real positive eigenvalue.
Therefore the remaining two real roots are one positive and one negative, where the pole corresponding to the negative real root is exactly \(z_-(t)\in(-1,0)\), and uniqueness holds.
By definition
\(
\delta(t)=\log(\Lambda(t)^2/r(t))
\)
and
\(
\Lambda(t)=-1/z_-(t),\ r(t)=1/z_+(t)
\)
immediately yields \(\delta(t)=\log(z_+(t)/z_-(t)^2)\) and its sign equivalence.
\par
Finally, \(\delta(t)=0\iff z_+(t)=z_-(t)^2\), and the equivalence of this quadratic resonance condition with \(t=t_\star\) is given by Proposition \ref{prop:pom-a4t-newman-pole-quadratic-resonance}. \qed
\end{proof}

\begin{remark}[Pole Ratio Single-Threshold Geometry: Audit Monotone Window of \texorpdfstring{$z_+(t)/z_-(t)^2$}{z+(t)/z-(t)^2}]\label{rem:pom-a4t-pole-ratio-monotone-window}
Under the notation of Proposition \ref{prop:pom-a4t-newman-offset-pole-geometry}, let
\(
g(t):=z_+(t)/z_-(t)^2
\).
The script \texttt{scripts/exp\_collision\_kernel\_A4\_pole\_quadratic\_resonance\_and\_cyclotomic\_adjacency\_injection.py}
performs grid audit of \(g(t)\) on the interval \([t_{E_6},t_{E_8}]\) containing the Newman critical and \((E_6,E_7,E_8)\) exponent intersection points,
yielding that \(g\) is strictly increasing in this window; combined with the uniqueness of \(g(t_\star)=1\), this makes the fourth-order phase transition equivalent to the geometric criterion "whether the positive pole lies to the left/right of the square of the negative pole."
\end{remark}

% (User input window)
% 2026-02-14 18:04:07 (Asia/Singapore)
%
\paragraph{Even Spectrum Curve Quadratic Solvability, Lucas Elimination, and Golden Ratio Resonance Tower}
Proposition \ref{prop:pom-a4t-even-zeta-quintic} and equation \eqref{eq:collision_kernel_A4_even_zeta_quintic} give the integer-coefficient closed form of \(E_t(u)=\det(I-uA_4(t)^2)\).
Viewing this quintic even spectrum equation as an algebraic curve in the \((t,u)\)-plane, we can further derive a conclusion chain "quadratic solvability in \(t\) \(\Rightarrow\) Lucas coordinate quartic elimination \(\Rightarrow\) integer parameter quadratic splitting \(\Rightarrow\) \(\varphi\)-power spectrum discrete resonance."

\begin{conclusion}[Fourth-Order Even Spectrum Curve Quadratic Solvability in \(t\) and Square Discriminant]\label{con:pom-a4-even-spectrum-t-quadratic}
Following Remark \ref{rem:pom-collision-rh-break-a4-threshold}, let \(A_4(t)\) be the fourth-order five-state kernel family, and let
\[
E_t(u):=\det(I-uA_4(t)^2)\in\ZZ[t,u]
\]
be the even spectrum polynomial (equation \eqref{eq:collision_kernel_A4_even_zeta_quintic}).
Then for any fixed \(u\neq 0\), the equation \(E_t(u)=0\) is equivalent to a quadratic equation in \(t\)
\[
\boxed{\;
u^2 t^2+(4u^3-2u)t+\bigl(1-4u-4u^2+8u^3+4u^4-4u^5\bigr)=0\; } .
\]
Its discriminant completely squares in \(\ZZ[u]\):
\[
\boxed{\;
\Delta(u):=\mathrm{Disc}_t\!\bigl(E_t(u)\bigr)
=16u^3(u-1)^2(u+1)^2\; } .
\]
Therefore except at degenerate points \(u\in\{0,\pm1\}\), the even spectrum curve \(E_t(u)=0\) gives two distinct algebraic branches in the \(t\)-coordinate; the branches are controlled only by \(\sqrt u\) and \(u^2-1\).
\end{conclusion}

\begin{conclusion}[Global Rational Parameterization: \texorpdfstring{$u=v^2$}{u=v^2} Normalization and Branch Exchange Involution]\label{con:pom-a4-even-spectrum-rational-param}
Under the notation of Conclusion \ref{con:pom-a4-even-spectrum-t-quadratic}, set \(u=v^2\) (\(v\neq 0\)).
Then the normalization of the even spectrum curve \(E_t(u)=0\) can be given by the parameter \(v\), with two branch solutions as explicit rational functions
\[
\boxed{\;
t_{\pm}(v)= -2v^2+\frac{1}{v^2}\ \pm\ 2\Bigl(v^3-\frac{1}{v}\Bigr)\; } .
\]
And satisfying the strict symmetry \(t_{-}(v)=t_{+}(-v)\), so branch exchange is given by the central involution \(v\mapsto -v\); branch degeneracy only appears at the limit \(v\in\{0,\pm1,\infty\}\) (equivalent to \(u\in\{0,\pm1\}\)).
Therefore the normalization of the even spectrum curve is a genus \(0\) rational curve.
\end{conclusion}

\begin{conclusion}[Lucas Coordinate Elimination Quartic Constraint and Discriminant Always Square]\label{con:pom-a4-even-spectrum-lucas-elimination}
Following Conclusion \ref{con:pom-a4-even-spectrum-t-quadratic}, let \(u\neq 0\) and denote
\[
L:=u+u^{-1}.
\]
Then any point \((t,u)\) satisfying \(E_t(u)=0\) has its projection \((t,L)\) necessarily satisfying the quartic integer-coefficient equation
\[
\boxed{\;
R(t,L)=t^4+2Lt^3+(18+16L-3L^2-4L^3)t^2+(192+18L-80L^2-4L^3+8L^4)t
+(337-48L-164L^2+28L^3+20L^4-4L^5)=0\; } .
\]
And the discriminant of \(R(\cdot,L)\) with respect to variable \(t\) is a perfect square in \(\ZZ[L]\):
\[
\boxed{\;
\mathrm{Disc}_t(R)=256\,(L-2)^6(L+2)^6(4L-17)^2(4L-1)^2\; } .
\]
Therefore the quartic cover \((t,L)\mapsto L\) is constrained by alternating type in the discriminant sense; its degeneracy can only occur at
\(
L\in\{2,-2,17/4,1/4\}
\).
\end{conclusion}

\begin{conclusion}[Integer Parameter Subfamily \texorpdfstring{$L=a^2-2$}{L=a^2-2} Complete Quadratic Splitting]\label{con:pom-a4-even-spectrum-lucas-quadratic-splitting}
Under the polynomial \(R(t,L)\) of Conclusion \ref{con:pom-a4-even-spectrum-lucas-elimination}, take integer parameter \(a\in\ZZ\) and set \(L=a^2-2\).
Then in \(\ZZ[a,t]\) we have the strict factorization
\[
\boxed{\;
R(t,a^2-2)=P_a^{(+)}(t)\,P_a^{(-)}(t)\; } ,
\]
where the two quadratic factors are
\[
P_a^{(+)}(t)=t^2+\bigl(2a^3+a^2-8a-2\bigr)t+\bigl(-2a^5-6a^4+6a^3+24a^2+8a+1\bigr),
\]
\[
P_a^{(-)}(t)=t^2+\bigl(-2a^3+a^2+8a-2\bigr)t+\bigl(2a^5-6a^4-6a^3+24a^2-8a+1\bigr).
\]
Therefore there exists a "quadratically computable" resonance tower indexed by layer \(a\in\ZZ\): each level gives at most four real parameters \(t\), such that the even spectrum constraint \(E_t(u)=0\) achieves exact hit at the Lucas position \(L=u+u^{-1}=a^2-2\).
\end{conclusion}

\begin{conclusion}[Lucas Layer Golden Ratio Resonance Ladder: Real Even Spectrum Hits at \texorpdfstring{$u=\varphi^{\pm 4n}$}{u=phi^{pm4n}}]\label{con:pom-a4-even-spectrum-golden-resonance-ladder}
Let \(\varphi=(1+\sqrt5)/2\), and let the Lucas sequence \((L_n)_{n\ge 0}\) be defined by \(L_0=2,L_1=1\) and \(L_{n+1}=L_n+L_{n-1}\).
Take \(a=L_{2n}\in\ZZ\) (\(n\ge 1\)), then we have the identity
\[
L:=a^2-2=L_{2n}^2-2=L_{4n}.
\]
For any real parameter \(t\) satisfying \(P_{L_{2n}}^{(\pm)}(t)=0\), Conclusion \ref{con:pom-a4-even-spectrum-lucas-quadratic-splitting} gives \(R(t,L_{4n})=0\), thus there exists \(u\in\RR_{>0}\) such that \(u+u^{-1}=L_{4n}\) and \(E_t(u)=0\).
Since \(L_{4n}=\varphi^{4n}+\varphi^{-4n}\), the above positive real root must be
\[
u=\varphi^{4n}\qquad\text{or}\qquad u=\varphi^{-4n}.
\]
Equivalently, the matrix \(A_4(t)\) is forced to have a real even spectrum hit as an eigenvalue
\[
\lambda=\pm u^{-1/2}
\in\{\pm\varphi^{2n},\ \pm\varphi^{-2n}\}.
\]
Therefore the fourth-order kernel family, on a completely discrete and infinitely extendable quadratic field parameter sequence, precisely adheres to the \(\varphi\)-power spectrum channel.
\end{conclusion}

\begin{conclusion}[Resonance Tower Scale Law: \texorpdfstring{$t_n\sim 2\varphi^{6n}$}{t_n~2phi^{6n}} and \texorpdfstring{$\varphi^6$}{phi^6} Magnification Ratio]\label{con:pom-a4-even-spectrum-resonance-scale-law}
Under the setting of Conclusion \ref{con:pom-a4-even-spectrum-golden-resonance-ladder}, let \(t_n\) be the largest positive real root of the quadratic equation \(P_{L_{2n}}^{(-)}(t)=0\) (equivalently, the largest positive real root of \(R(t,L_{4n})=0\)).
Then as \(n\to\infty\) the asymptotic expansion holds
\[
\boxed{\;
t_n=2\varphi^{6n}-2\varphi^{4n}+O(\varphi^{2n})\; } ,
\]
and the inter-layer magnification ratio limit exists and satisfies
\[
\boxed{\;
\lim_{n\to\infty}\frac{t_{n+1}}{t_n}=\varphi^6\; } .
\]
Therefore the above Lucas/golden ratio resonance is not a scattered point set, but exhibits strict exponential hierarchy; its scale constant is locked by \(\varphi^6\).
\end{conclusion}
\begin{conclusion}[Resonance Tower and Newman Critical Arithmetic Separation: Quadratically Computable Family Does Not Contain Octic Critical Point]\label{con:pom-a4-even-spectrum-resonance-vs-newman-separation}
By Conclusion \ref{con:pom-a4-even-spectrum-lucas-quadratic-splitting}, all resonance parameters \(t\) generated by the Lucas resonance tower satisfy some quadratic equation \(P_a^{(\pm)}(t)=0\), with algebraic degree at most \(2\), and
\[
t\in\QQ(\sqrt{a^2-4})
\qquad(a\in\ZZ).
\]
In contrast, the fourth-order Newman critical parameter \(t_\star\) is determined by the octic integer-coefficient certificate \(P_4(t_\star)=0\) in equation \eqref{eq:collision_kernel_A4_edge_weight_threshold}, and \(P_4\) is irreducible over \(\QQ[t]\), thus \(t_\star\) has minimal polynomial degree \(8\).
Therefore
\[
\boxed{\;
t_\star\notin \bigcup_{a\in\ZZ}\ \QQ(\sqrt{a^2-4})\;}
\]
i.e., the Newman critical mechanism is strictly disjoint in algebraic degree from all the above Lucas/golden ratio resonance towers; the two types of phenomena belong to different arithmetic resolution levels.
\end{conclusion}

% (User input window)
% 2026-02-14 20:46:17 (Asia/Singapore)
%
\paragraph{Newman Octic Critical Field Sparse Generator and Local Arithmetic Fingerprint}
At the same octic resolution level, the Newman critical field can not only be characterized by the \(t\)-coordinate octic certificate \(P_4(t)=0\) (equation \eqref{eq:collision_kernel_A4_edge_weight_threshold});
following the negative real root channel in Remark \ref{rem:pom-collision-rh-break-a4-threshold}, one can also select a sparse integer-coefficient generator with significantly smaller discriminant, thereby compressing the local modulo \(p\) fingerprint and ramification portrait into a shorter auditable certificate.

\begin{conclusion}[Newman Octic Field Sparse Generator and Linear-Quintic Elimination Constraint]\label{con:pom-a4-newman-sparse-generator-elimination}
Let the two polynomials \(\mathcal Z(z)\in\ZZ[z]\) and \(\mathcal C(t,z)\in\ZZ[t,z]\) be as in equation \eqref{eq:collision_kernel_A4_newman_sparse_generator}.
Then there exists the strict elimination identity
\[
\boxed{\;
\mathrm{Res}_z\!\bigl(\mathcal Z(z),\mathcal C(t,z)\bigr)=-8\,P_4(t)\;}
\]
where \(P_4(t)\) is the fourth-order Newman threshold octic integer-coefficient certificate (equation \eqref{eq:collision_kernel_A4_edge_weight_threshold}).
Therefore for any root \(z\) satisfying \(\mathcal Z(z)=0\), since \(\mathcal Z\) has no zero root, the equation \(\mathcal C(t,z)=0\) gives a unique solution in \(t\)
\[
t=t(z)=\frac{z^5+2z^4-2z-2}{z^3}\in\QQ(z),
\]
and this \(t(z)\) must satisfy \(P_4(t(z))=0\).
Since \(P_4\) is irreducible over \(\QQ[t]\) (Conclusion \ref{con:pom-a4-even-spectrum-resonance-vs-newman-separation}), we have \([\QQ(t(z)):\QQ]=8\);
on the other hand \(\QQ(t(z))\subseteq \QQ(z)\) and \(\deg\mathcal Z=8\) force \([\QQ(z):\QQ]\le 8\), whence \([\QQ(z):\QQ]=8\) and
\[
\boxed{\;\QQ(t(z))=\QQ(z)\;}
\]
i.e., the Newman critical octic field can be equivalently understood as: the same number field generated by one linear-quintic constraint \(\mathcal C(t,z)=0\) and one six-term sparse octic constraint \(\mathcal Z(z)=0\).
Reproducible script: \texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator.py}.
\end{conclusion}

\input{sections/generated/eq_collision_kernel_A4_newman_sparse_generator}

\begin{conclusion}[Discriminant Square Difference and Power-Integral Index: Extra Bad Primes of \(t\)-Generator Come from Index Contribution]\label{con:pom-a4-newman-discriminant-index-artifact}
Denote \(\mathrm{Disc}(\cdot)\) for polynomial discriminant.
Then equation \eqref{eq:collision_kernel_A4_newman_sparse_generator} gives \(\mathrm{Disc}_z(\mathcal Z)\) and \(\mathrm{Disc}_t(P_4)\) complete factor decompositions, satisfying the strict square ratio relation
\[
\boxed{\;
\frac{\mathrm{Disc}_t(P_4)}{\mathrm{Disc}_z(\mathcal Z)}
=\Bigl(2^{5}\cdot 5\cdot 109^{2}\cdot 4139873\Bigr)^2\;}
\]
thus the primes \(5,109,4139873\) do not appear in the discriminant support of the sparse generator \(\mathcal Z\), but only appear with perfect square exponent in the discriminant of the \(t\)-generator.
In the arithmetic sense, this is equivalent to: they do not carry the quadratic resolution information of the octic field, but only record the power-integral index contribution of \(\ZZ[t]\) relative to the same number field ring of integers (cf. the discriminant-index identity calibration in Proposition \ref{prop:pom-e8-intersection-field-discriminant-index}).
\end{conclusion}

\begin{conclusion}[Common Quadratic Spine: \texorpdfstring{$\QQ(\sqrt{-1151})$}{Q(sqrt(-1151))} Controls Frobenius Parity of Both Generators]\label{con:pom-a4-newman-common-quadratic-spine}
By the discriminant decomposition in equation \eqref{eq:collision_kernel_A4_newman_sparse_generator}, the square-free parts of \(\mathrm{Disc}_z(\mathcal Z)\) and \(\mathrm{Disc}_t(P_4)\) are consistent, both equal to \(-1151\);
\par
Let \(L_\star\) be the splitting field of \(P_4(t)\) (equivalently, also the splitting field of \(\mathcal Z(z)\)).
Then \(L_\star\) contains the unique discriminant quadratic subfield
\[
K_2=\QQ(\sqrt{-1151})
\]
(consistent with the \(t_\star\)-calibration of Proposition \ref{prop:pom-a4t-newman-k2-class-number}).
For any prime \(p\nmid 2\cdot 7\cdot 23\cdot 1151\), under the natural permutation representation of the group \(\mathrm{Gal}(L_\star/\QQ)\subseteq S_8\), the parity of the Frobenius permutation is strictly given by the same Kronecker symbol:
\[
\mathrm{sgn}(\mathrm{Frob}_p)=\left(\frac{-1151}{p}\right).
\]
Therefore all local parity diagnostics of the fourth-order Newman critical can be losslessly transferred under any generator representation, and \(\QQ(\sqrt{-1151})\) constitutes the non-eliminable quadratic spine of this critical arithmetic.
\end{conclusion}

\begin{conclusion}[Sparse Generator Galois Group is \texorpdfstring{$S_8$}{S8}: Three Splitting-Type Certificates Exclude Primitive Small Groups]\label{con:pom-a4-newman-sparse-galois-s8}
Let \(G=\mathrm{Gal}(\mathcal Z/\QQ)\).
By the modulo \(p\) splitting certificates from script \texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator.py}:
\begin{itemize}
  \item Modulo \(13\): \(\mathcal Z\bmod 13\) is irreducible (Frobenius contains an \(8\)-cycle);
  \item Modulo \(3\): \(\mathcal Z\bmod 3\) has factor degree pattern \((7,1)\) (Frobenius contains a \(7\)-cycle, hence primitive);
  \item Modulo \(5\): \(\mathcal Z\bmod 5\) has factor degree pattern \((6,2)\) (Frobenius contains \((6)(2)\) cycle type).
\end{itemize}
Moreover, since \(\mathrm{Disc}_z(\mathcal Z)\) is non-square, \(G\nsubseteq A_8\).
Combined with the classification of degree \(8\) primitive subgroups: primitive candidates containing a \(7\)-cycle are only \(S_8,A_8,\mathrm{AGL}(1,8),\mathrm{PGL}_2(7)\);
the latter two do not contain \((6,2)\) cycle type, so they are excluded.
Therefore
\[
\boxed{\;G=S_8\;}
\]
and by the field isomorphism in Conclusion \ref{con:pom-a4-newman-sparse-generator-elimination}, the splitting field of \(P_4(t)\) is the same as \(\mathcal Z\), so \(P_4\) also has the same full symmetric Galois group \(S_8\).
\end{conclusion}

\begin{conclusion}[Local Ramification Portrait of Discriminant Support Primes: Two Triple Roots and One Double Root]\label{con:pom-a4-newman-ramification-portrait}
At discriminant support primes, the reduction decomposition of the sparse generator \(\mathcal Z\) exhibits stable multiple root structures:
\[
\mathcal Z(z)\equiv (z-2)^3\cdot Q_7(z)\pmod 7\quad(\deg Q_7=5),
\]
\[
\mathcal Z(z)\equiv (z-6)^3\cdot(z-7)\cdot Q_{23}^{(1)}(z)\cdot Q_{23}^{(2)}(z)\pmod{23}\quad(\deg Q_{23}^{(i)}=2),
\]
\[
\mathcal Z(z)\equiv (z+499)^2\cdot\prod_{i=1}^{3}Q_{1151}^{(i)}(z)\pmod{1151}\quad(\deg Q_{1151}^{(i)}=2).
\]
And modulo \(2\) there is complete collapse \(\mathcal Z(z)\equiv z^8\pmod 2\).
Therefore this octic field exhibits triple root fingerprints at \(7\) and \(23\), double root fingerprint at \(1151\), and full wild ramification root fingerprint at \(2\);
this gives a directly verifiable local ramification portrait: two triple cores, one double core, and one full wild core.
\end{conclusion}

\begin{conclusion}[\texorpdfstring{$E_8$}{E8} Index Shadow: After Removing Quadratic Spine, the Unique Odd Prime Support is \(\{7,23\}\)]\label{con:pom-a4-newman-e8-exponent-shadow}
By equation \eqref{eq:collision_kernel_A4_newman_sparse_generator}, the odd prime support of \(\mathrm{Disc}_z(\mathcal Z)\) after removing the quadratic spine \(1151\) leaves only
\[
\{7,23\}.
\]
The \(E_8\) Coxeter exponent set is \(\{1,7,11,13,17,19,23,29\}\), which contains exactly the prime exponents \(7\) and \(23\).
Therefore in the sparse generator coordinates of the fourth-order Newman critical, there appears a strictly statable index projection: after removing the quadratic spine, all odd prime ramification of this octic field is compressed into the two prime exponents of \(E_8\).
\end{conclusion}

\begin{conclusion}[Lossless Transfer of Local Fingerprints: Using Sparse Generator to Replace Threshold Polynomial for Modulo \texorpdfstring{$p$}{p} Decoding]\label{con:pom-a4-newman-local-fingerprint-transfer}
By the field isomorphism in Conclusion \ref{con:pom-a4-newman-sparse-generator-elimination} and the index square ratio in Conclusion \ref{con:pom-a4-newman-discriminant-index-artifact},
for any prime
\[
p\nmid 2\cdot 5\cdot 7\cdot 23\cdot 109\cdot 1151\cdot 4139873,
\]
the two generators give the same finite \(\FF_p\)-algebra (isomorphic to the direct product of decomposition homomorphisms), thus
\[
\boxed{\;
\mathcal Z(z)\bmod p\ \text{and}\ P_4(t)\bmod p\ \text{have the same factor degree multiset}\;}
\]
i.e., they induce the same Frobenius cycle type.
Therefore the modulo \(p\) local fingerprint (splitting type and parity layer determined by the quadratic spine) of the fourth-order Newman critical field can be computationally transferred to the six-term sparse polynomial \(\mathcal Z\), thereby compressing the high-coefficient octic threshold polynomial modular decomposition problem into a fast decomposition problem of sparse octic polynomials.
\end{conclusion}

% (User input window)
% 2026-02-15 00:29:25 (Asia/Singapore)
%
\paragraph{Sparse Generator Field Integral Ring and Localizations: Power-Integral Basis, Ramification Index, and Residue Algebra Thickness}
In the sparse generator coordinates, the Newman octic critical field not only exhibits compressible structure at the discriminant and modular splitting type level,
its power-integral basis, localizations at four ramification primes, and residue algebras can also be written as completely explicit and verifiable arithmetic interfaces.

\begin{conclusion}[Newman Sparse Generator Field Power-Integral Basis and 2-Eisenstein Structure]\label{con:pom-a4-newman-integral-basis-2eisenstein}
Let \(K=\QQ(\theta)\), where \(\theta\) is any root of the sparse octic polynomial \(\mathcal Z(z)\in\ZZ[z]\) (equation \eqref{eq:collision_kernel_A4_newman_sparse_generator}).
Then \(\mathcal Z\) is Eisenstein at prime \(2\), whence it is irreducible over \(\QQ\) and \(2\) is totally ramified in \(K/\QQ\).
Moreover \(K\) is a monogenic octic field:
\[
\mathcal O_K=\ZZ[\theta],
\qquad
\{1,\theta,\theta^2,\dots,\theta^7\}\ \text{is an integral basis of}\ \mathcal O_K.
\]
Therefore its field discriminant satisfies
\[
\mathrm{Disc}(K)=\mathrm{Disc}_z(\mathcal Z)=-2^{10}\cdot 7^{2}\cdot 23^{2}\cdot 1151,
\]
and the discriminant of \(\mathcal Z\) equals the discriminant of this power-integral basis (no extra index artifact).
Moreover, \(\mathcal Z\) has exactly two real roots, so \(K\) has signature \((r_1,r_2)=(2,3)\), whence unit rank is \(r_1+r_2-1=4\).
Reproducible script: \texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator\_arithmetic.py}.
\end{conclusion}

\input{sections/generated/eq_collision_kernel_A4_newman_sparse_generator_arithmetic}

\begin{conclusion}[Complete Prime Ideal Decomposition of Four Ramification Primes: \(2\) Totally Wild Ramified, \(\{7,23,1151\}\) Purely Tame Ramified]\label{con:pom-a4-newman-prime-ideal-decomposition-ramified}
In the number field \(K\) of Conclusion \ref{con:pom-a4-newman-integral-basis-2eisenstein},
\(2,7,23,1151\) are all ramification primes; their prime ideal decompositions are as listed in equation \eqref{eq:collision_kernel_A4_newman_sparse_generator_arithmetic}.
In particular:
\begin{itemize}
  \item At \(2\): there is only one prime ideal \(\mathfrak p_2\) with \(e(\mathfrak p_2\mid 2)=8,f(\mathfrak p_2\mid 2)=1\), so \((2)=\mathfrak p_2^{8}\);
  \item At \(7\): decomposes into two branches: \(\mathfrak p_7\) satisfies \(e=3,f=1\), while \(\mathfrak q_7\) satisfies \(e=1,f=5\), so \((7)=\mathfrak p_7^{3}\mathfrak q_7\);
  \item At \(23\): decomposes into four branches: \(\mathfrak p_{23}\) satisfies \(e=3,f=1\), \(\mathfrak a_{23}\) satisfies \(e=1,f=1\), \(\mathfrak b_{23},\mathfrak c_{23}\) satisfy \(e=1,f=2\), so \((23)=\mathfrak p_{23}^{3}\mathfrak a_{23}\mathfrak b_{23}\mathfrak c_{23}\);
  \item At \(1151\): decomposes into four branches: \(\mathfrak p_{1151}\) satisfies \(e=2,f=1\), the other three satisfy \(e=1,f=2\), so \((1151)=\mathfrak p_{1151}^{2}\mathfrak b_{1151}\mathfrak c_{1151}\mathfrak d_{1151}\).
\end{itemize}
\end{conclusion}

\begin{conclusion}[Tame Ramification Minimal Formula for Discriminant Index and Local Inertia Group Order]\label{con:pom-a4-newman-tame-discriminant-inertia}
For each odd ramification prime \(p\in\{7,23,1151\}\), by the decomposition in Conclusion \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified} we know \(p\nmid e(\mathfrak p\mid p)\), so \(K/\QQ\) is purely tamely ramified at all odd ramification places.
Therefore the \(p\)-adic index of the field discriminant satisfies the strict identity
\[
\boxed{\;
v_p(\mathrm{Disc}(K))=\sum_{\mathfrak p\mid p} f(\mathfrak p\mid p)\,\bigl(e(\mathfrak p\mid p)-1\bigr)\;}
\]
whence
\[
v_7(\mathrm{Disc}(K))=2,\qquad v_{23}(\mathrm{Disc}(K))=2,\qquad v_{1151}(\mathrm{Disc}(K))=1.
\]
Moreover in the tame ramification case the inertia subgroup must be cyclic: at \(p=7,23\) the inertia group order is \(3\), at \(p=1151\) the inertia group order is \(2\).
Furthermore, by \(\mathcal O_K=\ZZ[\theta]\) in Conclusion \ref{con:pom-a4-newman-integral-basis-2eisenstein}, there is no extra square term from generator index in the discriminant index.
\end{conclusion}

\begin{conclusion}["Two-Prime Certificate" Forces \texorpdfstring{$S_8$}{S8}: Generated by \((3)(5)\) at \(p=7\) and \(8\)-Cycle at \(p=13\)]\label{con:pom-a4-newman-two-prime-s8}
Let \(L\) be the splitting field of \(\mathcal Z\), and denote \(G=\mathrm{Gal}(L/\QQ)\le S_8\) acting on the eight roots of \(\mathcal Z\).
Since \(13\nmid \mathrm{Disc}(K)\), prime \(13\) is unramified in \(L/\QQ\); and \(\mathcal Z\bmod 13\) is irreducible (script \texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator.py}), so there exists a Frobenius element whose cycle type is an \(8\)-cycle.
\par
On the other hand, Conclusion \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified} shows that \(7\) exhibits one branch with \(e=3\) tame ramification in \(K/\QQ\) and simultaneously one decomposition branch with residue degree \(5\);
therefore in some decomposition group of the splitting field \(L\), the inertia group contains an element of order \(3\), while the residue field Frobenius (in the quotient group \(D/I\)) contains an element of order divisible by \(5\).
Since \(7\equiv 1\pmod 3\), in the tame ramification case the Frobenius conjugation action on the inertia group is \(x\mapsto x^{7}\), so this action is trivial on inertia of order \(3\);
thus the decomposition group contains an element of order \(15\).
Under the natural permutation representation in \(S_8\), an element of order \(15\) can only have cycle type \((3)(5)\), whence \(G\) contains a \((3)(5)\)-type element.
\par
Therefore \(G\) contains both an \(8\)-cycle and a \((3)(5)\).
Group-theoretically, a transitive subgroup containing a \(5\)-cycle must be primitive; by Jordan's theorem, a primitive subgroup once containing a prime-length cycle (here \(5\)) must contain \(A_8\);
but an \(8\)-cycle is an odd permutation, so \(G\) cannot be contained in \(A_8\).
Thus \(G=S_8\).
\end{conclusion}

\begin{conclusion}[Residue Algebra Decomposition at Four Ramification Points: Nilpotency Thickness of \(\mathcal O_K/p\mathcal O_K\)]\label{con:pom-a4-newman-residue-algebra-decomposition}
By \(\mathcal O_K=\ZZ[\theta]\) in Conclusion \ref{con:pom-a4-newman-integral-basis-2eisenstein} and the local decomposition in Conclusion \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified},
the residue algebras at the four ramification primes can be written as explicit direct product isomorphisms:
\[
\mathcal O_K/2\mathcal O_K \cong \FF_2[\varepsilon]/(\varepsilon^8),
\]
\[
\mathcal O_K/7\mathcal O_K \cong \FF_7[\varepsilon]/(\varepsilon^3)\times \FF_{7^5},
\]
\[
\mathcal O_K/23\mathcal O_K \cong \FF_{23}\times \FF_{23}[\varepsilon]/(\varepsilon^3)\times \FF_{23^2}\times \FF_{23^2},
\]
\[
\mathcal O_K/1151\mathcal O_K \cong \FF_{1151}[\varepsilon]/(\varepsilon^2)\times (\FF_{1151^2})^{3}.
\]
Therefore this octic field has nilpotency thickness parameters \((8;3;3;2)\) at the four ramification points, corresponding to residue field degree spectrum
\[
(1;\,5;\,1,2,2;\,2,2,2).
\]
\end{conclusion}

\begin{conclusion}[Four Normalized Local Coordinates: \(\theta\) and \(\theta-a\) Give Unified Valuation Scale]\label{con:pom-a4-newman-normalized-local-parameters}
At the ramification prime ideals in Conclusion \ref{con:pom-a4-newman-prime-ideal-decomposition-ramified}, there exists natural "coordinate-valuation" locking.
Denote \(v_{\mathfrak p}\) for the discrete valuation associated with \(\mathfrak p\mid p\), normalized by \(v_{\mathfrak p}(p)=1\).
Then \(\theta\) gives local parameters at the four ramification points respectively:
\[
v_{\mathfrak p_2}(\theta)=\frac{1}{8},\qquad
v_{\mathfrak p_7}(\theta-2)=\frac{1}{3},\qquad
v_{\mathfrak p_{23}}(\theta-6)=\frac{1}{3},\qquad
v_{\mathfrak p_{1151}}(\theta+499)=\frac{1}{2}.
\]
This normalization allows any algebraic quantity related to criticality to have its \(p\)-adic sensitivity directly projected onto the valuation constants locked by the ramification indices \((8,3,3,2)\).
Reproducible script: \texttt{scripts/exp\_collision\_kernel\_A4\_newman\_sparse\_generator\_arithmetic.py}.
\end{conclusion}


