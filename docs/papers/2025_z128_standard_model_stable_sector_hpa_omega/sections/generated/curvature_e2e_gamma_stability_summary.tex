\noindent\AuditTag $\widehat\gamma$ stability sweep (bounded counterfactual family): we vary the threshold rule for the protocolized scalar statistic (zero/mean/median), the baseline convention (mean vs median recentering), and a small set of odd smoothing windows for $\widehat\chi$. For each configuration we refit $\widehat\gamma$ by WLS and summarize the resulting family by min/max/mean/std and a simple spread score $(\max-\min)/\mathrm{std}$.