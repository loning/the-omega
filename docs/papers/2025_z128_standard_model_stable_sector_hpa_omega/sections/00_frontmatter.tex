
\begin{abstract}
Within the Holographic Phase Arithmetic (HPA) scan--projection readout paradigm, we reformulate the Standard Model (SM) as a problem of \emph{stable readout sectors} under finite resolution.
At the minimal balanced coupling scale $(m,n)=(6,3)$, the local readout space is the $6$-bit microstate set $\Omega_6:=\{0,1\}^6$ with $|\Omega_6|=64$.
Introducing three commuting stability channels, denoted $\varphi$--$\pi$--$\e$, and defining stability by their joint readout constraints, we obtain a rigid \emph{resolution folding} $64\to 21$: the $\varphi$-stable type set $X_6$ has cardinality $|X_6|=21$ and admits a canonical $18\oplus 3$ split into cyclic and boundary types induced by the $\pi$-channel.

We then propose a \emph{scan--chirality locking} principle: the orientation class of Hilbert addressing (a choice among the dihedral layout families $D_4$) fixed at $t=0$ turns parity into a protocol change at the readout level.
A discrete Hilbert-path chirality index flips sign under both reflection (parity) and traversal reversal (time reversal), providing an auditable finite-resolution sense in which the arrow of time and spatial handedness couple to the same sign datum.
In the closed constant-geometry sector of the HPA--$\Omega$ program, the same protocol language yields quantitative rigidity targets: the theorem-level value $\alpha_{\mathrm{geo}}^{-1}=4\pi^3+\pi^2+\pi\approx 137.0363037759$ matches CODATA 2022 at relative deviation $\sim 2.22\times 10^{-6}$; electroweak volume quantization yields $\alpha^{-1}(\mu_Z)=13\pi^2\approx 128.3048572142$ and $\sin^2\theta_W(\mu_Z)=3/13\approx 0.2307692308$ with explicit PDG deviations at the per-mille level; and CP violation admits the rigid reference $J_{\mathrm{geo}}=1/(11\pi^7)\approx 3.00994\times 10^{-5}$ against the PDG CKM Jarlskog value.
Within this quantitative backbone, scan--chirality locking provides a protocol-geometric account of maximal parity violation in the weak sector, antimatter as the conjugate readout induced by scan reversal, and small CP violation as a residual holonomy/phase-space volume effect.
Gauge fields are interpreted as compensating connections enforcing cross-site phase consistency under the three defect channels.

Finally, we provide minimal standard-library Python scripts that reproduce the Fold$_6$ surjectivity, the rigid $(2,3,4)$ degeneracy histogram, and the Hilbert chirality-index transformation law, and we record falsifiable interface predictions.
\end{abstract}

\begin{sloppypar}
\textbf{Keywords}: holographic phase arithmetic; resolution folding; Zeckendorf representation; golden mean shift; Hilbert addressing; dihedral group; chirality; antimatter duality; gauge connections; Weinberg angle; Jarlskog invariant; CP violation.
\end{sloppypar}

\paragraph{Conventions.}
Unless otherwise stated, $\log$ denotes the natural logarithm.
We use Fibonacci numbers $F_1=F_2=1$, $F_{k+1}=F_k+F_{k-1}$.
For $N\in\NN$, the Zeckendorf digits $c_k\in\{0,1\}$ satisfy $c_k c_{k+1}=0$ and
\[
N=\sum_{k\ge 1} c_k F_{k+1}.
\]
We write $w=w_1\cdots w_m$ for a finite binary word with letters $w_i\in\{0,1\}$.
We reserve $m$ for Zeckendorf window length and reserve $n$ for Hilbert order.
The standard Abel path refers to the limit process $r\uparrow 1$ with $r\in(0,1)$.

\paragraph{Layered audit rule.}
We separate a \emph{mathematical layer} (definitions and provable finite-resolution statements) from a \emph{physical identification layer} (interfaces and falsifiable mapping hypotheses).

\tableofcontents
\newpage


