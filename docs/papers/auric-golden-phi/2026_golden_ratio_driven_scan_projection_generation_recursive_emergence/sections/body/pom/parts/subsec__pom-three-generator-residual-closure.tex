\subsection{Residual closure of three generators: from exchange certificates to computable exception signatures}\label{subsec:pom-three-generator-residual-closure}
In this section, the three generators$$
\mathrm{PROJ}_u,\qquad E_m,\qquad \mathrm{LIFT}_G
$$
Tighten it into a set of closure caliber of "projection word grammar with parameters + checkable residual fingerprint".Section \ref{subsec:pom-derivable-calculus-closure} has given the interface definition of the projection word $2$-category $\mathbf{PW}$ (definition \ref{def:pom-projword-2cat}) and shown an executable normalization example containing \(E_m,\mathrm{LIFT}_G,\mathrm{PROJ}_u\) (note \ref{rem:pom-projword-lift-proj-normalizer-demo}).The key point here is: instead of treating “\(E_m\) and
\subsubsection{Exception signature: carrying residuals with finite part constants of non-trivial twisted channels}\label{subsec:pom-anom-signature}
\paragraph{motivation}On the core interface, the semantics of \(\mathrm{LIFT}_G\) are given by group expansion/role decomposition: for the finite Abelian label group \(G\), its \(\zeta\) (or determinant) can be decomposed by role family into a family of warped channels (see Section \ref{sec:zeta-finite-part}, especially the DFT/inversion caliber of note \ref{prop:arity-chebotarev-mertens-fourier}).\(E_m\), on the other hand, is a fold-induced conditional expectation (definitionThe two can be strictly exchanged in the case of "pure product fiber" (Beck--Chevalley type square), but in the general case of "label and folded fiber coupling", the commutative violation will push the information into a non-trivial distortion channel; and this article happens to have an auditable constant-level interface to carry this violation: Abel/Hadamard finite part constant \(\log\mathfrak{M}(\theta)\) (definition \ref{def:abel-finite-part}).
\begin{definition}[Distorted Finite Part Anomaly Signature]\label{def:pom-anom-signature}Take a potential kernel \(K\) (or equivalently a family of weighted matrices \(M_\theta\)), and the lift semantics of a finite abelian label group \(G\).For each character \(\chi\in\widehat G\), denote \(\zeta_{\theta,\chi}\) as the \(\zeta\) interface of the \(\chi\)-distorted channel (the determinant factor in the sense of character decomposition), and define its Abel/Hadamard finite part constant \(\log\mathfrak{M}_\chi(\theta)\) according to the definition \ref{def:abel-finite-part}.Define \textbf{Exception Signature} as a finite family of constants for non-trivial characters$$
\mathsf{Anom}_{G}(K;\theta)
:=\Bigl(\log\mathfrak{M}_\chi(\theta)\Bigr)_{\chi\in\widehat G\setminus\{\chi_0\}},
$$
Among them, \(\chi_0\) is an ordinary character.\end{definition}

\begin{remark}[Why choose the finite part instead of just the spectral radius ratio]\label{rem:pom-anom-why-finite-part}The spectral radius ratio \(\rho_\chi/\lambda\) quantifies the exponential gap of the "slow mode" and is the main scale certificate of distortion breaking; but it is not sensitive to the differences between different distortion channels at the same exponential level.The finite part constant \(\log\mathfrak{M}_\chi(\theta)\) captures the constant-level drift in the prime-orbit Mertens principal term (Proposition \ref{prop:abel-finite-part-mobius-zeta}), and can stably carry the residual output of "exchange failure" at the constant level; this is the minimum complement needed to turn "non-commutatability" from metaphysics into a verifiable signature.\end{remark}

\subsubsection{Commutative Law with Residuals: Upgrading Rewritten Certificates to Verifiable Exceptions}\label{subsec:pom-residual-swap}
\begin{proposition}[\texorpdfstring{XPH0X}{Em}--\texorpdfstring{XPH1X}{LIFT} exchange certificate with exceptions (interface version)]\label{prop:pom-em-lift-residual-swap}In the projective word $2$-category \(\mathbf{PW}\) (definition\(
E_m\circ \mathrm{LIFT}_G
\)
rewritten as\(
\mathrm{LIFT}_G\circ E_m
\)
And comes with a verifiable residual signature \(\mathsf{Anom}_G(K;\theta)\):For arbitrary object\end{proposition}

\begin{proof}
This proposition is an interface-level encapsulation: the role decomposition of \(\mathrm{LIFT}_G\) splits the \(\zeta\)/finite output after lift into a family of twist factors on \(\widehat G\) (note \ref{prop:arity-chebotarev-mertens-fourier}), while the semantics of \(E_m\) is the conditional expectation along the folded fiber (proposition \ref{prop:fold-conditional-expectation}).When both satisfy the Beck--Chevalley-type pullback condition, the conditional expectation and label are promoted to a direct product structure, thus strictly commutating on each warp channel, and the anomaly signature is zero.Under normal circumstances, exchange is no longer a strict equation; but since all auditable outputs in this article are ultimately extracted through the same \(\zeta_\theta=1/\det(I-zM_\theta)\) and finite part interface (defined as \ref{def:abel-finite-part}), the \emph {observable part} of the exchange failure must fall on the finite part difference of "non-trivial twisted channels relative to trivial channels", that is, \(\mathsf{Anom}_G(K;\theta)\).\end{proof}

\begin{corollary}[Zero exception \texorpdfstring{XPH0X}{<->} Beck--Chevalley can be pulled back (closed-loop interface version)]\label{cor:pom-zero-anom-pullback-exactness}Under the setting of proposition \ref{prop:pom-em-lift-residual-swap}, "strict exchange" is understood as: for each twisted character channel \(\chi\in\widehat G\), the closed-loop interface used in this article\[
\text{Finite core/matrix family}\Rightarrow \zeta \Rightarrow \logXPH0X\]
above, the composite \(E_m\circ \mathrm{LIFT}_G\) gives the same channel output as \(\mathrm{LIFT}_G\circ E_m\) (equivalently: the block is a pullback block of the discrete Beck--Chevalley type).Then there is an equivalent description:\[
\boxed{\;
\mathsf{Anom}_G(K;\theta)=0
\quad\Longleftrightarrow\quad
E_m\\]
\end{corollary}

\begin{proof}
Proposition \ref{prop:pom-em-lift-residual-swap} has compressed all observable residuals of exchange failure into \(\mathsf{Anom}_G(K;\theta)\);Therefore, "zero residual" and "strict exchange on the closed-loop interface" are necessary and sufficient conditions for each other.\end{proof}

\subsubsection{Universal exchangeable group quotient: Promote the residual from "numeric signature" to "group direction erased by folding"}\label{subsec:pom-bc-quotient}Proposition \ref{prop:pom-em-lift-residual-swap} and corollary \ref{cor:pom-zero-anom-pullback-exactness} compress the exchange failure of \(E_m\) and \(\mathrm{LIFT}_G\) into a constant-level exception signature \(\mathsf{Anom}_G(K;\theta)\).At the level of "deducible calculus", numerical residuals alone are still not enough to constitute a computable structural object: it is also necessary to ontology "which part of the group information the exchange failure comes from" into a constructible object.\par
This paragraph gives a pure group theory strictification: for a fixed resolution \(m\), construct an \textbf {universal group quotient}
\begin{definition}[fiber visible equivalent and Beck--Chevalley interchangeable supplier]\label{def:pom-bc-quotient}Fixed object \(\mathcal{I}(m;K;\kappa)\) and resolution \(m\).Let the fold be \(\Fold_m:\Omega_m\to X_m\), and let \(E_m\) denote its induced conditional expectation/fiber average interface (proposition \ref{prop:fold-conditional-expectation}).Let the lift semantics consist of a finite abelian group \(G\) and a group label function (of step or length \(m\))\[
\ell:\Omega_m\to G
\]
is given (its origin can be the accumulation of cocycles on the edge of the core along a path of length \(m\); also written\par
Define the "fiber indistinguishable" relationship as\[
a\sim_m b\quad\Longleftrightarrow\quad \Fold_m(a)=\Fold_m(b)\qquad(a,b\in\Omega_m),
\]
And define \textbf{fiber invariant subgroup} (the group direction erased by folding) as\[
N^{\mathrm{BC}}_m
:=\left\langle\ \ell(a)\ell(b)^{-1}\ :\ a,b\in\Omega_m,\ a\sim_m b\ \right\rangle\ \le\ G.
\]
Let \(\pi_m:G\twoheadrightarrow G^{\mathrm{BC}}_m:=G/N^{\mathrm{BC}}_m\) be the quotient mapping, and note \(\bar\ell_m:=\pi_m\circ \ell:\Omega_m\to G^{\mathrm{BC}}_m\).Then \(\bar\ell_m\) is constant on each folded fiber, thus inducing a well-defined \textbf{visible label}\[
\ell^{\mathrm{vis}}_m:X_m\to G^{\mathrm{BC}}_m,
\qquad
\ell^{\mathrm{vis}}_m\bigl(\Fold_m(a)\bigr)=\bar\ell_m(a).
\]
\end{definition}

\begin{proposition} [strict commutatability: zero exceptions on \(G^{\mathrm{BC}}_m\)] \label{prop:pom-bc-quotient-strictify}The notation used to define \ref{def:pom-bc-quotient} is used.Replace the group axis of \(\mathrm{LIFT}_G\) with \(G^{\mathrm{BC}}_m\) (that is, use \(\bar\ell_m\) as the lift label), then \(E_m\) and \(\mathrm{LIFT}_{G^{\mathrm{BC}}_m}\) are strictly exchanged in the sense of a closed-loop interface; equivalently,\[
\mathsf{Anom}_{G^{\mathrm{BC}}_m}(K;\theta)=0
\]
(holds for all considered potential parameters \(\theta\) and all non-trivial twist channels).\end{proposition}
\begin{proof}
By construction, \(\bar\ell_m\) depends only on \(\Fold_m(a)\), i.e. the existence of \(\ell^{\mathrm{vis}}_m\) makes \(\bar\ell_m=\ell^{\mathrm{vis}}_m\circ \Fold_m\).Therefore for any character \(\overline\chi\in\widehat{G^{\mathrm{BC}}_m}\), the function\(
f_{\overline\chi}:=\overline\chi\circ \bar\ell_m
=
\overline\chi\circ \ell^{\mathrm{vis}}_m\circ \Fold_m
\)
It is \(\sigma(\Fold_m)\)-measurable, so it satisfies \(E_m f_{\overline\chi}=f_{\overline\chi}\).This is exactly the strict version of the Beck--Chevalley-type "label and folded fiber decoupling": on each visible twist channel, the conditional expectation no longer introduces inter-channel mixing, so the exchange block of \(E_m\) with \(\mathrm{LIFT}_{G^{\mathrm{BC}}_m}\) is strictly closed on the closed-loop interface.From \ref{cor:pom-zero-anom-pullback-exactness} we get \(\mathsf{Anom}_{G^{\mathrm{BC}}_m}(K;\theta)=0\). Certification completed.\end{proof}

\begin{proposition}[Universality (maximum): Any strictly commutative group quotient must be factorized]\label{prop:pom-bc-quotient-universal}Inherit the settings defining \ref{def:pom-bc-quotient}.Let \(\phi:G\twoheadrightarrow H\) be any full homomorphism.If \(\phi\circ \ell\) is invariant on folded fibers (equivalently: there is \(\ell_H^{\mathrm{vis}}:X_m\to H\) such that \(\phi\circ\ell=\ell_H^{\mathrm{vis}}\circ\Fold_m\) is also equivalent to \(E_m\) and \(\mathrm{LIFT}_H\) being strictly exchanged in the sense of a closed-loop interface), then there must be\[
N^{\mathrm{BC}}_m\ \le\ \ker(\phi),
\]
There is thus a unique full homomorphism \(\bar\phi:G^{\mathrm{BC}}_m\twoheadrightarrow H\) such that \(\phi=\bar\phi\circ \pi_m\).\end{proposition}
\begin{proof}
For any \(a\sim_m b\), it is assumed that \(\phi\circ\ell\) does not become \(\phi(\ell(a))=\phi(\ell(b))\) on the fiber, that is, \(\phi(\ell(a)\ell(b)^{-1})=1\).Hence \(\ell(a)\ell(b)^{-1}\in\ker(\phi)\).From the generated definition of \(N^{\mathrm{BC}}_m\), we get \(N^{\mathrm{BC}}_m\le \ker(\phi)\).So \(\phi\) induces the only \(\bar\phi\) on the business group \(G/N^{\mathrm{BC}}_m\), and satisfies \(\phi=\bar\phi\circ\pi_m\). Certification completed.\end{proof}

\begin{remark}[Computability: from fiber enumeration to the standard form of quotient groups]\label{rem:pom-bc-quotient-computable}Define the construction of \ref{def:pom-bc-quotient} to be directly computable at finite resolution: given a computable fold \(\Fold_m:\Omega_m\to X_m\) and a computable label \(\ell:\Omega_m\to G\), enumerate each fiber\(
\ell(a)\ell(b)^{-1}
\)
A limited generated set of \(N^{\mathrm{BC}}_m\) can be obtained.When \(G\) is a finite Abelian group, \(G\) can be expressed as the direct product of a finite number of cyclic groups, and the generator matrix can be made into Smith canonical form, thereby obtaining the invariant decomposition of the quotient group \(G^{\mathrm{BC}}_m=G/N^{\mathrm{BC}}_m\) and the role domain \(\widehat{G^{\mathrm{BC}}_m}\) in polynomial time.So the "strict commutability quotient" not only exists, but can be directly checked as a discrete output of the audit pipeline.\end{remark}

\begin{corollary}[Exceptional support: only from \texorpdfstring{XPH0X}{N^BC_m}-direction]\label{cor:pom-anom-supported-on-bc-kernel}Inherit the settings defining \ref{def:pom-bc-quotient}.Think of \(\widehat{G^{\mathrm{BC}}_m}\) as a subgroup of \(\widehat G\) (ie: \(\widehat{G^{\mathrm{BC}}_m}\cong\{\chi\in\widehat G:\chi|_{N^{\mathrm{BC}}_m}=1\}\)).Then for any \(\chi\in\widehat G\), if \(\chi|_{N^{\mathrm{BC}}_m}=1\), the exchange residual of the corresponding twisted channel is zero:\[
\chi|_{N^{\mathrm{BC}}_m}=1
\quad\Longrightarrow\quad
\bigl(\mathsf{Anom}_G(K;\theta)\bigr)_{\chi}=0.
\]
Therefore any non-zero anomaly coordinates can only occur in the direction of \(\widehat G\setminus \widehat{G^{\mathrm{BC}}_m}\) (i.e. characters that are non-trivial to \(N^{\mathrm{BC}}_m\)).\end{corollary}
\begin{proof}
If \(\chi|_{N^{\mathrm{BC}}_m}=1\), then \(\chi\) factors into a composite of some \(\overline\chi\in\widehat{G^{\mathrm{BC}}_m}\) and the quotient map \(\pi_m\).Therefore the \(\chi\)-distortion channel is equivalent to the visible distortion channel on \(G^{\mathrm{BC}}_m\).By the proposition \ref{prop:pom-bc-quotient-strictify}, the anomaly of all visible distortion channels is zero, thus corresponding to the coordinate \(\bigl(\mathsf{Anom}_G(K;\theta)\bigr)_{\chi}=0\). Certification completed.\end{proof}

\input{sections/body/pom/parts/cor__pom-bc-pontryagin-dual-classification}

\begin{corollary}[Visible register inverse system on resolution tower]\label{cor:pom-bc-inverse-system}For any \(m_2\ge m_1\), since the folding condition is expected to satisfy the tower property \(E_{m_1}\circ E_{m_2}=E_{m_1}\) (see \SXPH4X), the fiber indistinguishable relationship of the \(m_2\) layer refines the \(m_1\) layer, so\[
N^{\mathrm{BC}}_{m_2}\ \le\ N^{\mathrm{BC}}_{m_1},
\qquad\text{thus}\qquadG^{\mathrm{BC}}_{m_2}\twoheadrightarrow G^{\mathrm{BC}}_{m_1}.
\]
Therefore \(\{G^{\mathrm{BC}}_m\}_{m\ge 1}\) constitutes an inverse system and its inverse limit can be defined\[
G^{\mathrm{BC}}_\infty:=\varprojlim_m G^{\mathrm{BC}}_m.
\]
This limiting object can be interpreted as: a group register visible skeleton that is still strictly compatible with folding under "infinite resolution readout".\end{corollary}

\subsubsection{Combinable laws of anomaly signatures: \texorpdfstring{XPH1X}{2}-cohomology class}\label{subsec:pom-anom-cohomology} as \texorpdfstring{XPH0X}{PW}Thinking of the "labeled exchange certificate" of the proposition \ref{prop:pom-em-lift-residual-swap} as an \(2\)-morphism generator of \(\mathbf{PW}\), the key benefit is that the exception is no longer a post hoc patch, but a residual class that can be composed at the rewrite level and only undergoes boundary transformations when changing the specification path.
\begin{definition}[Exception accumulation and normalized exception]\label{def:pom-anom-accumulation}Fix a parametric object \(\mathcal{I}(m;K;\kappa)\), a finite Abelian lift group \(G\) and the potential parameter \(\theta\).In the subfragment generated only by \(\mathrm{PROJ}_u,E_m,\mathrm{LIFT}_G\), let\[
\mathcal{A}_G:=\RR^{\widehat G\setminus\{\chi_0\}}
\]
And use component addition as a group operation.For any rewritten certificate (\(2\)-morphism) \(\alpha:w\Rightarrow w'\), define its \textbf{exception accumulation}\(\mathrm{anom}(\alpha)\in\mathcal{A}_G\)
is: after decomposing \(\alpha\) into a series of local rewriting steps, take the label of each step and sum it up, where(i) The labels of idempotent/pylon union/strictly commutative steps are \(0\);(ii) The label of each \(E_m\)--\(\mathrm{LIFT}_G\) with residual exchange step is \(\mathsf{Anom}_G(K;\theta)\) (define \ref{def:pom-anom-signature}).Given a normal form selection operator \(N\) and its family of normalized certificates \(\eta_w:w\Rightarrow N(w)\), the \textbf {normalized exception} of the definition word is\[
\mathrm{Anom}(w):=\mathrm{anom}(\eta_w).
\]
\end{definition}

\begin{proposition}[Additive compound laws and boundary terms]\label{prop:pom-anom-additive-up-to-boundary}Under the setting of defining \ref{def:pom-anom-accumulation}, the longitudinal composition of \(2\)-morphisms by \(\mathrm{anom}\) is strictly additive: if \(\alpha:w\Rightarrow w'\) and\[
\mathrm{anom}(\beta\circ\alpha)=\mathrm{anom}(\beta)+\mathrm{anom}(\alpha).
\]
Furthermore, for any composable \(1\)-morphism \(w_1,w_2\), there exists an \(2\)-cochain \(B(w_2,w_1)\in\mathcal{A}_G\) that only depends on the selection of the normal form, such that\[
\mathrm{Anom}(w_2\circ w_1)
=
\mathrm{Anom}(w_2)+\mathrm{Anom}(w_1)+\partial B(w_2,w_1),
\]
Among them, \(\partial B\) represents the boundary term resulting from the comparison of the two normalization paths of "first specification and then merging" and "direct specification" (equivalently: when changing the normalized certificate family \(\{\eta_w\}\), \(\mathrm{Anom}\) only changes one coboundary).\end{proposition}

\begin{corollary}[Zero exceptions are path invariants]\label{cor:pom-anom-cohomology-class}"Whether the anomaly is zero" has nothing to do with the reduction path/reduction benchmark: if \(\mathrm{Anom}(w)=0\) under a certain normalization choice, the resulting \(\mathrm{Anom}'(w)\) for any other normalization choice is also zero.Equivalently, \(\mathrm{Anom}\) defines a computable (\(2\)-) cohomology class whose disappearance characterizes whether the exchange failure can be completely eliminated by rewriting.\end{corollary}

\begin{corollary}[Strict additivity of exception classes (horizontal composition)]\label{cor:pom-anom-class-additive}Under the notation of the proposition \ref{prop:pom-anom-additive-up-to-boundary}, for any composable \(1\)-morphism \(w_1,w_2\), its anomalous cohomology class satisfies the strict additive law\[
\boxed{\;
[\mathrm{Anom}(w_2\circ w_1)]
=
[\mathrm{Anom}(w_2)]
+
[\mathrm{Anom}(w_1)]\;}
\]
(Because the boundary term \(\partial B(w_2,w_1)\) disappears after taking the cohomology class).\end{corollary}

\begin{remark} [\(2\) with residuals-confluence: internalizing "non-commutative" as center label]\label{rem:pom-residual-2confluence}On three-generator fragments, we do not require \(E_m\) to be {strictly} exchanged with \(\mathrm{LIFT}_G\)Therefore, "confluence" should be understood as an \(2\)-confluence caliber with residuals: within a fixed complexity upper bound, any two rewritten paths can be merged into the same interface normal form (proposition \ref{prop:pom-three-gen-interface-normal-form}), and their residual differences (if they occur) must fall on the boundary terms of proposition \ref{prop:pom-anom-additive-up-to-boundary}; therefore \(\bigl(\mathrm{NF}(w),[\mathrm{Anom}(w)]\bigr)\) (normal form + anomalous cohomology class) becomes a path invariant.This is exactly the encapsulation of the "regular morphism + limited signature verification" of the inference \ref{cor:pom-three-gen-normal-form-decidable} at the calculus level: non-commutability no longer destroys deducibility, but is promoted to composable and auditable \(2\)-morphism data.\end{remark}

\begin{remark}[Discrete contact point of view: anomaly is curvature (closed layer standard field strength)]\label{rem:pom-anom-as-curvature}Consider the parameter pair \((m,G)\) of object \(\mathcal{I}(m;K;\kappa)\) as the two axes of a discrete "base plane":resolution/modulus axisThen the proposition \ref{prop:pom-em-lift-residual-swap} gives\[
E_m\circ \mathrm{LIFT}_G\ \Rightarrow\ \mathrm{LIFT}_G\circ E_m
\]
It can be understood as the \emph {curvature \(2\)-cell} of the discrete connection on a basic block:This curvature degenerates into an identity \(2\)-morphism when lift is decoupled from the folded fibers (Beck--Chevalley-type blocks can be pulled back); the general case of "block unclosure" pushes the observable residuals into non-trivial twist channels and is minimally compressed into a finite family of constants \(\mathsf{Anom}_G(K;\theta)\) (defining \ref{def:pom-anom-signature}).Thus in the closed-layer primitives caliber of this paper, the gauge field can be defined without a priori a continuous fiber bundle/Lie group: it is minimally given by a discrete parallel shift \((E_m,\mathrm{LIFT}_G)\) and its curvature certificate \(\mathsf{Anom}_G\).Inference The cohomology invariance of \ref{cor:pom-anom-cohomology-class} corresponds to the fact that "changing the gauge datum (discrete gauge selection) only changes a coboundary, but does not change whether the curvature is zero."\end{remark}

\begin{remark}[For completion: critical pairs determine additional exception generators]\label{rem:pom-anom-completion}The engineering implication of \ref{cor:pom-anom-cohomology-class} is that when extending the projection word fragment (for example, adding \(\mathrm{MOM}_q\) or the more general lift/cocycle), you only need to enumerate the critical pairs between the old and new generators and complete the rewriting system. The new "non-strict commutability" will automatically appear as an \(\mathcal{A}\)-value label; the exception signature therefore becomes a composable residual cluster, rather than a rule-by-rule temporary patch.For example, in the four-generator extension, the new coupling of \(\mathrm{MOM}_q\) toBounded critical pair enumeration and completion auditing on three generator fragments see note \ref{rem:pom-threegen-kb-completion-audit}.\end{remark}

\begin{proposition}[Moment’s abnormal amplification law (\texorpdfstring{XPH0X}{q} times gap amplification)]\label{prop:pom-anom-moment-amplification}In the four-generator extended fragment (definition \ref{def:pom-four-gen-rewrite-rules}), take any three-generator projection word \(w\).Extend the "center label accumulation" caliber that defines \ref{def:pom-anom-accumulation} to the protocol track containing \(\mathrm{MOM}_q\) according to the same protocol certificate specification (that is, only accumulate \(\mathsf{Anom}_G\) labels for each residual exchange step of \(E_m\)--\(\mathrm{LIFT}_G\), and take the quotient according to the coboundary).Then for any integer \(q\ge 2\), there is a linear amplification law of the anomalous cohomology class:\[
\boxed{\;
[\mathrm{Anom}(\mathrm{MOM}_q\circ w)]
=
q\cdot [\mathrm{Anom}(w)].\;}
\]
Therefore, once the anomaly type difference between two words is too small at the constant level to be easily verified numerically, the difference can be linearly amplified by \(q\) by selecting a higher-order moment gate, and still stay within the compilable/auditable semantic domain of the limited kernel.\end{proposition}

\begin{proof}[Proof Idea]According to the fiber-product semantics of \(\mathrm{MOM}_q\), it promotes the underlying track constraints to a synchronized world of \(q\) copies; therefore, any protocol certificate track for \(w\) corresponds to a "share-by-share promotion" \(q\) parallel certificate track on \(\mathrm{MOM}_q\circ w\).Since the anomaly label group \(\mathcal{A}_G\) takes component addition as the central operation in the definition of \ref{def:pom-anom-accumulation}, and \(\mathrm{anom}\) is strictly additive for longitudinal composition (Proposition Certification completed.\end{proof}

\begin{remark}[Reproducible: automatic verification of holonomy/path differences]\label{rem:pom-holonomy-cocycle-audit}In order to reduce the scope of "Exception as \(2\)-cohomology class" to the executable certificate, we provide a minimal holonomy verification script:\texttt{scripts/exp\_pom\_holonomy\_cocycle\_audit.py}。
This script randomly generates several projection words containing \(\mathrm{PROJ}_u,E_m,\mathrm{LIFT}_G\), and uses two different rewriting priorities to reduce them to the same regular form candidate;Then compare(i) Whether the lexical regular form is consistent,as well as(ii) Under the selected certificate specification (gauge), whether the holonomy count accumulated by exchanging certificates (each \(E_m\)--\(\mathrm{LIFT}_G\) swap) is consistent.When a discrepancy is detected, the script writes the counterexample, the two reduction trajectories, and their holonomy statistics\texttt{artifacts/export/pom\_holonomy\_cocycle\_audit.json}，
Key critical pair/boundary correction required for locating completion (Proposition \ref{prop:pom-anom-additive-up-to-boundary}).\end{remark}

\begin{remark}[Reproducible: critical pair enumeration and completion auditing of three-generation fragments]\label{rem:pom-threegen-kb-completion-audit}Note: \ref{rem:pom-holonomy-cocycle-audit} gives the path difference verification of "random words + two protocol priorities".To get an \emph{exhaustive} audit closer to Knuth--Bendix completion, we provide a script\texttt{scripts/exp\_pom\_projword\_three\_gen\_kb\_completion\_audit.py}：
Enumerate all overlap contexts of length \(3\) under a fixed complexity upper bound (limit \(m\), limit lift's group list, limit temperature parameter sampling set),And compare the interface normal form and residual count of the two local reduction branches for each critical pair.The script outputs a JSON summary and several counterexample traces to\texttt{artifacts/export/pom\_projword\_three\_gen\_kb\_completion\_audit.json}，
And generate table \ref{tab:pom_projword_three_gen_kb_completion_audit} as a reproducible witness:\input{sections/generated/tab_pom_projword_three_gen_kb_completion_audit}
A zero \(\#\)NF mismatch in the \noindent table means that the bounded fragment passed the local merge audit at the lexical level;The occurrence of residual mismatch corresponds to the boundary term in the proposition \ref{prop:pom-anom-additive-up-to-boundary},The script gives completion candidate fixes in JSON clustered by critical pair type.\end{remark}

\begin{proposition}[The convergence is not an absolute convergence, but an abnormal convergence model (bounded audit version)]\label{prop:pom-confluence-mod-anom-audited}Within the same complexity upper bound as note \ref{rem:pom-threegen-kb-completion-audit}, the three-generator fragment \((\mathrm{PROJ}_u,E_m,\mathrm{LIFT}_G)\) satisfies:\begin{enumerate}
\item\item\end{enumerate}
Therefore, it is natural to take the equivalence relationship of projection words as "regular form consistency + exception class consistency": changing the priority of the specification only changes one coboundary, but does not change whether the exception class is zero.\end{proposition}

\begin{definition}[Algorithmic Equivalence (Normal Form + Exception Class)]\label{def:pom-algorithmic-equivalence-nf-anom}Within the above complexity upper bound, for any two projection words \(w_1,w_2\), define\[
w_1\sim w_2
\iff
\mathrm{NF}(w_1)=\mathrm{NF}(w_2)
\\text{and}\[\mathrm{Anom}(w_1)]=[\mathrm{Anom}(w_2)].
\]
Among them, \(\mathrm{NF}\) can be the canonical representation of the interface of proposition \ref{prop:pom-three-gen-interface-normal-form}, and \([\mathrm{Anom}(w)]\) is the abnormal cohomology class determined by proposition \ref{prop:pom-anom-additive-up-to-boundary} (the representative element calculated under the specification of any fixed protocol certificate, quotient is taken according to the coboundary).In the implementation of this article, we fix a normalizer and certificate specification, so the equivalence relationship can be directly determined by the "normal formal parameters + limited exception signature verification" of the inference \ref{cor:pom-three-gen-normal-form-decidable}.\end{definition}

\begin{corollary}[Contains the normal form and finite signature evaluation of \texorpdfstring{XPH0X}{(PROJ,Em,LIFT)}]\label{cor:pom-three-gen-normal-form-decidable}Fixed word complexity upper bounds (e.g. limit \(m\), limit lift's group size, limit temperature parameter sampling set).Then any projection word generated only by\(
\mathsf{Anom}_G(K;\theta)
\)
a limited sampling set.So on this complexity class, "algorithmic equivalence" (commutative/isomorphic in the natural sense) boils down to:Compare the normal shape parameter \((u,m,G)\) and compare the anomaly signatures on a limited number of sampling points to see if they are consistent.\end{corollary}

\begin{definition}[Conservative semantic functor \texorpdfstring{XPH0X}{Sem} (bounded complexity slice)]\label{def:pom-conservative-sem-functor}LetAfter considering each generator as an \(\Theta\)-family, the parameterized projection word \(2\)-category is obtained, denoted as \(\mathbf{PW}*\Theta\).\par
On this slice, define two computable semantic output domains:\begin{enumerate}
\item \textbf{Signature field \(\mathbf{Sig}*\Theta\):} records the verifiable fingerprints of the constant layer and the regular shape layer. Get the word \(w\)  \[
  \mathsf{Sig}(w):=\Bigl(\mathrm{NF}(w),\ G^{\mathrm{BC}}_m(w),\ [\mathrm{Anom}(w)],\ \{\mathsf{Anom}_{G}(K;\theta)\}_{\theta\in\Theta}\Bigr),
  \]
Among them, \(\mathrm{NF}(w)\) is the formal representation of the interface (proposition \ref{prop:pom-three-gen-interface-normal-form}),\(G^{\mathrm{BC}}_m(w)\) is the universal exchangeable quotient that defines \ref{def:pom-bc-quotient} (recording "the largest group of registers still visible from the perspective of \(E_m\)"),\([\mathrm{Anom}(w)]\) is an abnormal \(2\)-cohomology class (proposition \ref{prop:pom-anom-additive-up-to-boundary}),And \(\{\mathsf{Anom}_{G}(K;\theta)\}_{\theta\in\Theta}\) is a family of limited sampling signatures (defining \ref{def:pom-anom-signature}).\item \textbf{spectral domain A typical choice is: per-channel parameter \((\chi,u)\) with integer \(q\) output weighted collision moment-kernel's Perron root\(r^{(w)}_{q,\chi}(u)\), or equivalently the pressure/budget curve \(\Sigma_{\mathrm{space}}(w)(q;\chi,u)\) and its discrete convex closure type normal form \(\mathrm{Ty}_{\mathrm{space}}(w;\chi,u)\) (\SXPH3X).\end{enumerate}
So we get compositional semantic assignment (computable functor on the slice)\[
\boxed{\;
\mathsf{Sem}:\mathbf{PW}*\Theta\longrightarrow \mathbf{Sig}*\Theta\times \mathbf{Spec}*\Theta,\qquad
\mathsf{Sem}(w):=(\mathsf{Sig}(w),\mathsf{Spec}(w)).\;}
\]
\end{definition}

\begin{theorem}[Semantic conservatism on bounded slices (decidable core of natural isomorphism)]\label{thm:pom-sem-conservative-on-slice}Within the same complexity upper bound as inference \ref{cor:pom-three-gen-normal-form-decidable}, \(\mathsf{Sem}\) is conservative: if the two words \(w_1,w_2\) satisfy\(
\mathsf{Sem}(w_1)=\mathsf{Sem}(w_2)
\)，
Then there is a reversible \(2\)-morphism (natural isomorphism) such that \(w_1\simeq w_2\); equivalently \(w_1\sim w_2\) (defining \ref{def:pom-algorithmic-equivalence-nf-anom}).\end{theorem}

\begin{proof}
\(\mathsf{Sem}(w_1)=\mathsf{Sem}(w_2)\) implies that the signature components are equal, specifically \(\mathrm{NF}(w_1)=\mathrm{NF}(w_2)\) and \([\mathrm{Anom}(w_1)]=[\mathrm{Anom}(w_2)]\).By defining \ref{def:pom-algorithmic-equivalence-nf-anom}, we get \(w_1\sim w_2\), and by inferring the decidability caliber of \ref{cor:pom-three-gen-normal-form-decidable}, we get the corresponding reversible certificate.The spectral component can be considered a redundant check in this conclusion (but provides additional discriminative power in subsequent type-directed rewriting and multi-scale budget comparisons). Certification completed.\end{proof}

\begin{remark}[Symbol certificate upgrade: replacing finite sampling with twisted characteristic polynomials]\label{rem:pom-anom-symbolic-certificate}It is inferred that \ref{cor:pom-three-gen-normal-form-decidable}'s "limited sampling signature" is an engineering-friendly auditable caliber;If you wish to further upgrade the equivalence to a certificate of algebraic equality for \emph {completely formal}, you can symbolize the twist channel under a fixed complexity upper bound:For each non-trivial character \(\chi\in\widehat G\setminus\{\chi_0\}\), compile the projection word \(w\) (including the temperature parameter of \(\mathrm{PROJ}_u\)) into a finite-dimensional distortion matrix \(M_{\chi}^{(w)}(u)\), and define\[
\Delta_\chi(w;z,u):=\det\bigl(I-z\,M_{\chi}^{(w)}(u)\bigr).
\]
In the interface setting of this article, the matrix entries are generated by edge weights of finite graph kernels (typically powers of \(u\) or finitely generated analytic functions), so \(\Delta_\chi\) is a computable symbolic object within a given complexity class (polynomials with respect to \(z\), with coefficients in the computable algebraic domain of \(u\)).Once the two implementations are completely consistent for \(\Delta_\chi\) for all non-trivial \(\chi\), then the corresponding twisted \(\zeta\) is consistent with the finite part constant, so the exception signature is consistent; at this time, "auditable" equivalence is promoted to "provable" equivalence.\end{remark}

\subsubsection{Computable rewriting system and interface canonical form of three generators}\label{subsec:pom-three-gen-rewrite-normal}
This paragraph returns the "semantic interface" to the lexical level: a set of rewriting rules that only rely on parameters is given, and any\(\mathrm{PROJ}_u,E_m,\mathrm{LIFT}_G\)
The generated projection words reduce to an executable interface canonical form and explicitly wrap the non-commutative residuals into anomaly signatures (definition \ref{def:pom-anom-signature}).
\begin{definition}[Rewrite rule family on three generator fragments]\label{def:pom-three-gen-rewrite-rules}Take in $\mathbf{PW}$ only the subfragments generated by \(\mathrm{PROJ}_u\), \(\mathrm{LIFT}_G\), \(E_m\).The rewriting rule family defined above is as follows (all are $2$-morphism certificates, isomorphisms of units and associators are omitted):\begin{enumerate}
\item \textbf{Temperature gate merge (idempotent group):}  $$
  (\mathrm{RPROJ})\quad
  \mathrm{PROJ}_u\circ \mathrm{PROJ}_v\ \Rightarrow\ \mathrm{PROJ}_{uv}.
  $$
\item \textbf{lift merge (direct product promotion):}  $$
  (\mathrm{RLIFT})\quad
  \mathrm{LIFT}_G\circ \mathrm{LIFT}_H\ \Rightarrow\ \mathrm{LIFT}_{G\times H}.
  $$
\item \textbf{Conditional expectation tower shrinkage:}  $$
  (\mathrm{RE})\quad
  E_{m_1}\circ E_{m_2}\ \Rightarrow\ E_{\min(m_1,m_2)}.
  $$
\item \textbf{Temperature gate and lift are interchangeable (weight pull back):}  $$
  (\mathrm{RPL})\quad
  \mathrm{PROJ}_u\circ \mathrm{LIFT}_G\ \Rightarrow\ \mathrm{LIFT}_G\circ \mathrm{PROJ}_u.
  $$
\item \textbf{\(E_m\)--lift exchange with residual:}Taking proposition \ref{prop:pom-em-lift-residual-swap} as the certificate caliber, reduce \(E_m\circ\mathrm{LIFT}_G\) to \(\mathrm{LIFT}_G\circ E_m\) and output the abnormal signature \(\mathsf{Anom}_G(K;\theta)\) as the verifiable residual.\end{enumerate}
\end{definition}

\begin{proposition}[Terminability of three generator rewrites (reverse order metric)]\label{prop:pom-three-gen-termination}Take normal order\(
\mathrm{LIFT}\prec \mathrm{PROJ}\prec E
\)。
For any three-generator projection word \(W\), let \(\ell(W)\) be the word length (number of generators), and define \textbf {reverse order metric}\[
\mu(W):=\#\{(i,j):i<j,\\]
Then under the rule family \ref{def:pom-three-gen-rewrite-rules}, each rewrite will strictly reduce the lexicographic order\(
(\ell(W),\mu(W))
\)。
Therefore the rewriting of this fragment must be terminated; the residual anomaly is recorded as an \(2\)-morphism label and does not affect the drop metric of the \(1\)-morphism.\end{proposition}
\begin{proof}
Rule \((\mathrm{RPROJ}),(\mathrm{RLIFT}),(\mathrm{RE})\) condenses similar doors into one door, so \(\ell(W)\) is strictly reduced.Swapping rules \((\mathrm{RPL})\) with \(E_m\)--\(\mathrm{LIFT}_G\) with residuals only changes the relative order of the gates and does not increase the word length; since they swap "reverse-order pairs" for forward-order pairs, \(\mu(W)\) strictly decreases.Therefore, each step strictly reduces the lexicographic order \((\ell,\mu)\), and there is no infinite rewrite chain. Certification completed.\end{proof}

\begin{remark}[Reproducible: Three-Generation Normalizer and Abnormal Signing Certificate]\label{rem:pom-projword-threegen-anom-normalizer-demo}The script \texttt{scripts/exp\_pom\_projword\_three\_gen\_anom\_normalizer.py} implementation defines a minimal three-generator rewrite fragment of \ref{def:pom-three-gen-rewrite-rules}:It reduces any projection words generated by \texttt{E[m]}, \texttt{PROJ[u]}, \texttt{LIFT[G]} to the interface canonical form.\(\mathrm{LIFT}\circ\mathrm{PROJ}\circ E\) (proposition \ref{prop:pom-three-gen-interface-normal-form}),And output an exchange certificate with residual items at each \(E_m\)--\(\mathrm{LIFT}_G\) exchange(Interface is \(\mathsf{Anom}_G(K;\theta)\), definition is \ref{def:pom-anom-signature}).This script writes the regular form, rewrite trajectory and certificate list to\texttt{artifacts/export/pom\_projword\_three\_gen\_anom\_normalizer\_demo.json}，
As an executable witness of "exceptional signatures entering the protocol trace".\end{remark}

\begin{remark}[About the scope of application of \texorpdfstring{XPH0X}{RLIFT}]\label{rem:pom-rlift-scope}Rule \((\mathrm{RLIFT})\) reflects "collocated promotion of independent axes": combining two mutually independent finite label promotions into a single promotion to the direct product group.In more general group expansions (such as non-trivial semidirect products or cocycle twists), the corresponding coalescence law should replace the direct product with the fiber product/composition of the expanded data; this article mainly uses finite Abelian labels and its role decomposition in the auditable implementation, so the above writing method is enough to cover the numerical interface and rewriting examples of this article.\end{remark}

\begin{remark}[Crossover anomaly: A computable audit condition for \texorpdfstring{XPH0X}{RLIFT} independence]\label{rem:pom-rlift-cross-anom-test}Suppose \(G,H\) is a finite Abelian group, then \(\widehat{G\times H}\cong\widehat G\times\widehat H\).For any \((\chi,\psi)\in\widehat G\times\widehat H\), denote \(\chi\otimes\psi\) as the product role on \(G\times H\)\((g,h)\mapsto \chi(g)\psi(h)\), and note \(\chi_0,\psi_0\) as an ordinary character.Under the exception signature caliber (define \ref{def:pom-anom-signature}), define \textbf{cross exception} as\[
\Delta^{\mathrm{sep}}_{\chi,\psi}(\theta)
:=
\log\mathfrak{M}_{\chi\otimes\psi}(\theta)
-\log\mathfrak{M}_{\chi\otimes\psi_0}(\theta)
-\log\mathfrak{M}_{\chi_0\otimes\psi}(\theta),
\qquad
\chi\neq\chi_0,\ \psi\neq\psi_0.
\]
If the semantics of the two lifts are indeed independent axis juxtaposition (corresponding to the direct product lift of \((\mathrm{RLIFT})\)), then each sampling parameter \(\theta\) should have\(\Delta^{\mathrm{sep}}_{\chi,\psi}(\theta)=0\): The equivalent statement is that there is a verifiable decomposition on the twisted channel\(\zeta_{\theta,\chi\otimes\psi}=\zeta_{\theta,\chi\otimes\psi_0}\cdot \zeta_{\theta,\chi_0\otimes\psi}\)，
Therefore, the level of finite part constants is strictly additive.Therefore, the non-zero value of\end{remark}

\begin{proposition}[interface regular form: reducible order of \texorpdfstring{XPH0X}{(PROJ,Em,LIFT)}]\label{prop:pom-three-gen-interface-normal-form}Under the rule family \ref{def:pom-three-gen-rewrite-rules}, any three-generator projection word can be rewritten as the following "interface normal form":$$
\boxed{\;
\mathrm{LIFT}_G\ \circ\ \mathrm{PROJ}_u\ \circ\ E_m
\;}
$$
and is accompanied by an exception signature output of \(\mathsf{Anom}_G(K;\theta)\) (non-zero in the presence of non-commutative residuals).Furthermore, by integrating this regular form into the \(\zeta\)/primitive/limited compilation convention of \ref{rem:pom-zeta-normal-form}, an executable judgment process from lexical normalization to spectral fingerprint verification is obtained.\end{proposition}

\begin{proof}
RulesTherefore, any words can be sorted into the form of Certification completed.\end{proof}

\subsubsection{Higher-order crossing anomalies and axis-decomposable rigor: Möbius complete decomposition}\label{subsec:pom-rlift-higher-cross-anom}
Note \ref{rem:pom-rlift-cross-anom-test} gives the two-body crossover anomaly \(\Delta^{\mathrm{sep}}\) as a necessary audit condition for the independence of \((\mathrm{RLIFT})\).When the number of collocated axes of lift is \(k\ge 3\), in order to obtain the {sufficient and necessary} computable discrimination of
\begin{definition}[High-order crossover anomaly family \texorpdfstring{XPH0X}{DeltaS}]\label{def:pom-higher-cross-anomaly}Take \(k\ge 2\) and let \(G_1,\dots,G_k\) be the finite Abelian group, denoted\[
G:=\prod_{i=1}^k G_i,
\qquad
\widehat G\cong \prod_{i=1}^k \widehat{G_i}.
\]
For each sampling parameter \(\theta\) and each \(\chi\in\widehat G\), it is assumed that the Abel/Hadamard finite part constant \(\log\mathfrak{M}_{\chi}(\theta)\in\RR\) is given, and the normalization is agreed\[
\log\mathfrak{M}_{\chi^0}(\theta)=0,
\qquad
\chi^0:=(\chi_{1,0},\dots,\chi_{k,0})
\]
(Ordinary role of each coordinate).Order \([k]:=\{1,\dots,k\}\).For any non-empty subset \(S\subseteq[k]\) and any sub-tuple \(\chi_S=(\chi_i)_{i\in S}\in\prod_{i\in S}\widehat{G_i}\), define the \(S\)-order crossover anomaly as\[
\Delta_S(\chi_S;\theta)
:=\sum_{T\subseteq S}(-1)^{|S|-|T|}\,\log\mathfrak{M}_{\chi^{(T)}}(\theta),
\]
Among them, \(\chi^{(T)}\in\widehat G\) means: when \(i\in T\), the \(i\) coordinate takes \(\chi_i\), and when \(i\notin T\), the \(i\) coordinate takes the ordinary character \(\chi_{i,0}\).When \(|S|=2\), this definition is consistent with \(\Delta^{\mathrm{sep}}\) annotating \ref{rem:pom-rlift-cross-anom-test} (under normalization by \(\log\mathfrak{M}_{\chi^0}=0\)).\end{definition}

\begin{theorem}[Möbius complete decomposition]\label{thm:pom-mobius-complete-decomposition}Under the setting of defining \ref{def:pom-higher-cross-anomaly}, for any \(\chi=(\chi_1,\dots,\chi_k)\in\widehat G\) and any \(\theta\), the identity is established\[
\boxed{\;
\log\mathfrak{M}_\chi(\theta)
=\sum_{\varnothing\neq S\subseteq[k]}\Delta_S(\chi_S;\theta)\; }.
\]
\end{theorem}
\begin{proof}
This identity is equivalent to the Möbius inversion at Boolean points.Let \(f(\chi):=\log\mathfrak{M}_\chi(\theta)\) (fix \(\theta\)).Define the difference operator for each coordinate \(i\in[k]\)\[
(\delta_i f)(\chi):=f(\chi)-f(\chi^{(i\mapsto 0)}),
\]
And let \(\delta_S:=\prod_{i\in S}\delta_i\) for any non-empty \(S\subseteq[k]\) (the operator is commutative).Expand available\(
(\delta_S f)(\chi^{(S)})=\sum_{T\subseteq S}(-1)^{|S|-|T|}f(\chi^{(T)})
\)，
Therefore \(\Delta_S(\chi_S;\theta)=(\delta_S f)(\chi^{(S)})\).on the other hand,\[
\mathrm{Id}=\prod_{i=1}^k\bigl((\mathrm{Id}-\delta_i)+\delta_i\bigr)
=\sum_{S\subseteq[k]}\delta_S\prod_{i\notin S}(\mathrm{Id}-\delta_i).
\]
Apply it to \(f\) and take the value at \(\chi\); using the coordinate-by-coordinate zeroing effect of \(f(\chi^0)=0\) and \(\prod_{i\notin S}(\mathrm{Id}-\delta_i)\), we get\(
f(\chi)=\sum_{\varnothing\neq S\subseteq[k]}(\delta_S f)(\chi^{(S)})
\)，
That is the decomposition shown. Certification completed.\end{proof}

\begin{theorem}[Necessary and sufficient conditions for axis decomposability and strictness]\label{thm:pom-axis-decomposability-iff}Under the above settings, the following two conditions are equivalent:\begin{enumerate}
\item  \[
  \log\mathfrak{M}_{\chi}(\theta)=\sum_{i=1}^k L_i(\chi_i).
  \]
\item  \[
  \Delta_S(\chi_S;\theta)=0.
  \]
\end{enumerate}
\end{theorem}
\begin{proof}
\((1)\Rightarrow(2)\): If \(\log\mathfrak{M}_\chi(\theta)=\sum_i L_i(\chi_i)\), then when \(|S|\ge 2\),\(\Delta_S\) is the difference of at least two coordinates and must be zero.\par
\((2)\Rightarrow(1)\): From theorem \ref{thm:pom-mobius-complete-decomposition},\[
\log\mathfrak{M}_\chi(\theta)=\sum_{i=1}^k\Delta_{\{i\}}(\chi_i;\theta)+\sum_{|S|\ge 2}\Delta_S(\chi_S;\theta).
\]
Under (2) the second term disappears, let \(L_i(\chi_i):=\Delta_{\{i\}}(\chi_i;\theta)\) we get (1). Certification completed.\end{proof}

\begin{proposition}[Insufficient two-body verification: pure three-body coupling exists]\label{prop:pom-pairwise-cross-anom-insufficient}When \(k\ge 3\), only testing all two-body crossover anomalies \(\Delta_{\{i,j\}}\equiv 0\) cannot deduce axis decomposability.\end{proposition}
\begin{proof}
Take each \(\widehat{G_i}\) containing a non-trivial element \(a_i\) and define a function under fixed \(\theta\)\[
\log\mathfrak{M}_{\chi}(\theta):=
\begin{cases}
1, & \chi=(a_1,a_2,a_3,\chi_{4,0},\dots,\chi_{k,0}),\\
0, & \text{other situations}.\end{cases}
\]
Then the function on any two-body cross-section (the rest of the coordinates are trivial) is always \(0\), so \(\Delta_{\{i,j\}}\equiv 0\);But \(\Delta_{\{1,2,3\}}(a_1,a_2,a_3;\theta)=1\neq 0\), therefore \ref{thm:pom-axis-decomposability-iff} cannot be decomposed by the theorem. Certification completed.\end{proof}

\begin{remark} [Alignment with the Prime Register Limit: Auditable Judgments of Euler Decomposability] \label{rem:pom-higher-cross-anom-euler}At the prime-register's limit caliber \(\widehat{\ZZ}\cong\prod_p\ZZ_p\) (definition \ref{def:pom-profinite}),If \(\log\mathfrak{M}\) is regarded as a constant layer load with multiple axes (multiple primes or multiple direct integrals) and lift,Then the "all-order crossover anomalies disappear" given by theorem \ref{thm:pom-axis-decomposability-iff}It is the necessary and sufficient condition to strictly transform the coalescence law into a direct product (Euler type restricted product factorization) without \emph{not introducing} cocycle/semi-direct product and other expanded data;The proposition \ref{prop:pom-pairwise-cross-anom-insufficient} shows that the two-body audit \(\Delta^{\mathrm{sep}}\) is only a necessary condition in the multi-axis case.\end{remark}
\input{parts/subsec__pom-mom-rewrite-normal}
