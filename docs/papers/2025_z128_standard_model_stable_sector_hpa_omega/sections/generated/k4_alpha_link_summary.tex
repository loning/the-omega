\paragraph{Audit summary (K4 $\to$ $\alpha$ link).} \AuditTag We test a finite family of low-complexity aggregations of the $m=6$ $U(1)$ exit weights (from the trap/exit table in Appendix~\ref{app:leakage_kernel}) and allow only a coarse bounded scale family. Each candidate reports log mismatches to $\alpha_{\mathrm{em}}^{-1}$ (CODATA) and $\alpha^{-1}(m_Z)$ (PDG).
\noindent\AuditTag Selected candidate: \texttt{sum\_neg\_log\_p(k=1)} with predicted value 170.059388 and mismatches $(e_{\mathrm{low}},e_Z)=(0.215904,0.284469)$.