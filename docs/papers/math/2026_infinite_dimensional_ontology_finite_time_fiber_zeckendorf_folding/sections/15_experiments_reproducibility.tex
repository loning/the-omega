\section{实验与复现协议：不变量计算、局部化检验与双边界评测}

\subsection{通用复现约定与误差预算（读者合同）}

\AuditTag 本节把“可复现”提升为审计对象：每个写入正文的数值/表格片段必须由脚本生成并可一键再现；每个含随机性的实验必须给出随机性来源、重复次数与误差预算口径。

\paragraph{环境与入口。}
一键复现入口为 \texttt{python3 scripts/run\_all.py}（固定默认参数；见脚本内的配置块）。所有供正文引用的片段写入 \texttt{sections/generated/}，并以 \texttt{\textbackslash input} 的方式纳入本文。

\paragraph{确定性实验 vs.\ 随机性实验。}
\begin{itemize}
  \item \textbf{确定性实验（枚举/精确计算）}：例如对 $X_m$ 的枚举计数与纤维谱统计；其“统计误差”为 $0$，误差来源主要是实现错误。对此类实验，我们通过恒等式交叉核对与脚本内断言来做失败点检查。
  \item \textbf{随机性实验（仿真/抽样统计）}：例如局域动力学与涌现实验；其误差来自有限步长与随机种子。默认展示 \texttt{seed=0} 的基准输出；若需要误差条，则以多种随机种子重复运行得到均值与置信区间（推荐 $R\ge 8$ 个种子），并报告标准误 $\mathrm{SE}=\hat\sigma/\sqrt{R}$ 或 $95\%$ 置信区间。
\end{itemize}

\paragraph{主张—脚本—生成物索引（摘要）。}
\AuditTag 为审稿与复核方便，表 \ref{tab:repro_index} 给出本节核心条目的脚本入口与正文引入片段的对应关系；更细粒度的编号与依赖见推理台账（附录 \ref{app:inference_ledger}）。

\begin{table}[H]
\centering
\begin{tabular}{>{\raggedright\arraybackslash}p{0.30\linewidth} >{\raggedright\arraybackslash}p{0.34\linewidth} >{\raggedright\arraybackslash}p{0.28\linewidth}}
\toprule
\textbf{条目} & \textbf{脚本命令（入口）} & \textbf{生成片段（正文纳入）}\\
\midrule
组合计数核对 & \path{python3 scripts/exp_counts_check.py --m-max 24} & \path{sections/generated/counts_check.tex}\\
第一层纤维谱/熵 & \path{python3 scripts/exp_fiber_spectrum.py --m 16} & \path{sections/generated/fiber_entropy_summary.tex}\\
holonomy 光谱对照 & \path{python3 scripts/exp_holonomy_spectrum.py --m 12 --ell 6 --q 7 --seed 0} & \path{sections/generated/holonomy_interface_vs_bulk.tex}\\
涌现指标摘要 & \path{python3 scripts/exp_emergence_local_dynamics.py ... --seed 0} & \path{sections/generated/emergence_metrics.tex}\\
\bottomrule
\end{tabular}
\caption{复现索引：条目—脚本—生成片段对应关系（摘要）。}
\label{tab:repro_index}
\end{table}

\subsection{组合定理的枚举核对}

对 $m\le m_{\max}$ 枚举 $X_m$ 与 $C_m$，核对
$$
\abs{X_m}=F_{m+2},\qquad \abs{C_m}=L_m,\qquad \abs{B_m}=F_{m-2},
$$
并对 $m\le m_{\max}/2$ 核对接口亏损定理
$$
\abs{X_{2m}}=\abs{X_m}^2-F_m^2.
$$
其中 $m_{\max}$ 可选为 $20$--$24$ 以保持枚举可行。

为保证可复现性，本文所有写入正文的枚举结果均由脚本生成并以 \texttt{\\input} 方式引入：
\begin{center}
\path{python3 scripts/exp_counts_check.py --m-max 24}
\end{center}
对应生成片段如下：
\begin{center}
\input{sections/generated/counts_check}
\end{center}
\paragraph{误差预算与失败点检查。}
\AuditTag 本实验为确定性枚举，统计误差为 $0$。失败点来自实现错误或参数越界；脚本通过多条恒等式（包括接口亏损恒等式）交叉核对并在不一致时直接报错退出。

\subsection{第一层时间纤维谱的计算}

固定 $\Fold_m$（第 5 节数值--Zeckendorf 定义），遍历 $\Omega_m$ 中全部 $2^m$ 个窗口微态，统计
$$
\abs{\mathcal F_m(x)}=\abs{\{b\in\Omega_m:\Fold_m(b)=x\}},
$$
并计算
$$
p_m(x)=\frac{\abs{\mathcal F_m(x)}}{2^m},\qquad H(X_m),\qquad H(\Omega_m\mid X_m).
$$
评测指标包括纤维大小分布的极值、方差与集中性，以及随 $m$ 的标度。

本文给出一组基准参数下的可复现输出：
\begin{center}
\path{python3 scripts/exp_fiber_spectrum.py --m 16}
\end{center}
其熵与极值摘要由脚本写入：
\begin{center}
\input{sections/generated/fiber_entropy_summary}
\end{center}
\paragraph{误差预算与失败点检查。}
\AuditTag 本实验为确定性遍历（枚举 $\Omega_m$ 的全部 $2^m$ 个微态），统计误差为 $0$。失败点来自折叠实现不一致或计数错误；脚本生成的熵与分布摘要可与组合计数核对（例如总质量归一性）进行一致性检查。

\subsection{第二层残差纤维的候选实现与最小化实验}

在未给出一般性最小充分统计量证明前，提出可复现的候选残差实现族：
\begin{enumerate}
  \item 有限进位/溢出状态（记录有限阶溢出与边界携带）；
  \item 有限状态转导器内部状态（将规范化过程编译为最小 DFA/Mealy 机并取其状态集合）。
\end{enumerate}
对每一实现族，比较其在固定 $\mathcal O_m,\mathcal D$ 下的预测误差与状态规模，以经验方式逼近最小 $R_m(x)$。

\subsection{holonomy 谱的计算与接口局部化检验}

为计算 holonomy，需显式给定：
\begin{itemize}
  \item 允许边集 $\mathcal E_m$（由选定局部规则/重写系统导出）；
  \item 平行移动 $\mathcal P_{x\to x'}$（由残差更新规则诱导）。
\end{itemize}
在此基础上：
\begin{enumerate}
  \item 枚举短回路族 $\Gamma_\ell$（$\ell$ 为回路长度上界）；
  \item 计算每个回路的 $\mathrm{Hol}(\gamma)$ 并提取统计量（不动点比例、循环分解长度、谱半径等）；
  \item 构造含接口回路族 $\Gamma_\ell^{\mathrm{int}}$ 与不含接口回路族 $\Gamma_\ell^{\mathrm{bulk}}$，比较两者的 holonomy 复杂度分布，以检验假设 \ref{ass:curvature_concentration}（接口曲率集中）。
\end{enumerate}
\paragraph{误差预算与显著性口径。}
\AuditTag 若对 $\Gamma_\ell$ 做\emph{完全枚举}，则接口/体区两类回路族的统计量差异为确定值，采样误差为 $0$；此时误差预算主要来自实现错误与数值舍入（可通过重复计算与一致性断言控制）。若仅对回路族做抽样，则以 bootstrap 或置换检验给出差异的 $95\%$ 置信区间，并在预先声明的阈值（例如 $p<0.01$）下报告显著性。

\paragraph{群论对照：从单个回路到 holonomy 群}
当 $R_m(x_0)$ 为有限集合且沿边平行移动为双射时，$\mathrm{Hol}(\gamma)$ 是 $R_m(x_0)$ 上的置换，从而自然属于 $\mathrm{Sym}(R_m(x_0))$；由不同回路生成的全部 holonomy 变换生成一个子群 $\mathrm{Hol}_m(x_0)$（见注 \ref{rem:holonomy-group}）。因此，本节所提取的统计量可理解为对该群元素复杂度的采样测量：不动点比例与循环分解长度对应置换的轨道结构，谱半径可由将置换嵌入到某个线性表示后定义。接口局部化检验在群论上对应比较两类回路族生成的子群（或其典型元素）的复杂度分布差异。

为给出最小可运行实例，本文实现了一个玩具离散连接：以 $X_m$ 上的合法单比特翻转为允许边集 $\mathcal E_m$，以有限残差载体 $R\\cong\\mathbb Z_q$ 为纤维，并将平行移动实现为 $\mathbb Z_q$ 上的仿射映射。该构造刻意使 holonomy 的非平凡性集中在访问边界态集合 $B_m$ 的回路族上，从而对照检验假设 \ref{ass:structural_lower_bound}（结构性下界）与假设 \ref{ass:curvature_concentration}（接口曲率集中）的统计趋势。运行命令：
\begin{center}
\path{python3 scripts/exp_holonomy_spectrum.py --m 12 --ell 6 --q 7}
\end{center}
对照表格片段如下：
\begin{center}
\input{sections/generated/holonomy_interface_vs_bulk}
\end{center}

\subsection{动态分辨率实验：AMR/octree 与时间成本标度}

实现 octree/AMR 调度（第 11 节），并在固定目标 $\mathcal G$ 下测量
$$
\mathbb E[T(\mathcal G)\mid m],\qquad \mathrm{Var}(T(\mathcal G)\mid m),
$$
以及 $T(\mathcal G)$ 与局部指标 $E(v)$、边界态密度、holonomy 复杂度代理量之间的相关性。

\paragraph{随机性与误差预算。}
\AuditTag 本小节涉及随机仿真（随机种子驱动的局部更新、噪声与缺陷注入）。默认给出 \texttt{seed=0} 的基准复现输出；如需误差条，推荐在相同参数下用一组种子 $\{s_1,\dots,s_R\}$ 重复运行并报告均值与 $95\%$ 置信区间（或标准误）。对“相关性/回归”的显著性报告应预先声明阈值，并与样本量（步数、格点数、重复次数）一起给出。

作为“局部依赖图 + 有限残差 + 可观测宏态”的最小动力学演示，本文在三维格点依赖图上实现了一个局域更新族，并比较基线轨道与局部扰动轨道的传播前沿、相关函数与残差活动强度。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_local_dynamics.py --m 10 --steps 60 --threshold 0.50 --defect-rate 0.006}
\end{center}
对应摘要片段如下：
\begin{center}
\input{sections/generated/emergence_metrics}
\end{center}

此外，为识别“传播/缺陷密度/团簇规模”的涌现区域，可对关键参数 $(\texttt{threshold},\texttt{defect\_rate})$ 做网格扫描并生成相图。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_scan.py --m 10}
\end{center}
最佳区域摘要（按半径/缺陷密度/团簇规模的加权评分）如下：
\begin{center}
\input{sections/generated/emergence_phase_scan}
\end{center}
并给出两张相图示例（稳定文件名导出，便于论文引用）：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_phase_radius.png}
  \caption{参数扫描下的传播前沿强度代理量（平均 $\mathrm{radius_{max}}$）。}
\end{figure}
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_phase_interface.png}
  \caption{参数扫描下的缺陷密度代理量（平均 $\mathrm{interface\_frac_{max}}$）。}
\end{figure}

\paragraph{对象/运动统计（缺陷团簇作为对象候选）}
在固定参数点上，可将边界态集合 $B_m$ 的 6-邻域连通分量视为“激活支撑”的对象候选，并以团簇质心的时间序列定义其运动。本文给出一个可复现的追踪协议与统计量（对象数、寿命、速度分布）：
\begin{center}
\path{python3 scripts/exp_emergence_objects.py --m 10 --steps 100}
\end{center}
其摘要片段如下：
\begin{center}
\input{sections/generated/emergence_objects_summary}
\end{center}
并给出对象数随时间变化示例图：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_object_count.png}
  \caption{对象候选（缺陷团簇）数量随时间变化。}
\end{figure}

\paragraph{对象周期性/准周期性：固定点比例与周期检出}
在相同追踪协议下，可对对象质心轨迹进行周期检测：寻找最小周期 $p$ 使得轨迹在容差内重复至少若干轮，从而把“固定点/周期轨”从解释层接口推进到可计算统计量。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_objects_periodicity.py --m 10 --steps 180}
\end{center}
对应摘要片段如下：
\begin{center}
\input{sections/generated/emergence_objects_periodicity_summary}
\end{center}
并给出周期对象比例随时间的示例曲线：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_object_periodic_fraction.png}
  \caption{可检出周期对象比例随时间的演化（当前参数下以 $p=1$ 的近似固定点为主）。}
\end{figure}

\paragraph{空间 plaquette 的 holonomy 统计（曲率在缺陷附近的时序证据）}
为把“holonomy/曲率集中”从抽象宏态转移图推进到空间演化过程，本文在三维格点上为每条空间边赋予 $\mathbb Z_q$ 值连接 $A_e(t)$，并对每个基本小方格（plaquette）回路计算离散 holonomy。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_space_holonomy.py --m 10 --steps 80}
\end{center}
对应摘要片段如下：
\begin{center}
\input{sections/generated/emergence_space_holonomy_summary}
\end{center}
并给出时序示例图：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_space_holonomy_rate.png}
  \caption{空间 plaquette holonomy 非平凡率与缺陷触达率的时序对照。}
\end{figure}

\paragraph{诱导连接对照：精确 1-形式与方向依赖连接}
为区分“由标量势诱导的无旋连接”（精确 1-形式）与“方向依赖的局域连接”，本文提供了一个对照实验：在同一动力学轨道上，分别以 (i) 接口门控连接、(ii) 由单一比特场差分诱导的精确 1-形式、(iii) 按边方向选择不同比特的方向依赖连接，计算 plaquette holonomy 的非平凡率及其与缺陷触达率的相关性。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_space_holonomy_induced.py --m 10 --steps 80}
\end{center}
对应摘要片段如下：
\begin{center}
\input{sections/generated/emergence_space_holonomy_induced_summary}
\end{center}
并给出时序对照图：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_space_holonomy_compare.png}
  \caption{三种连接的 holonomy 非平凡率与缺陷触达率的对照。精确 1-形式在 plaquette 上给出近零 holonomy，而方向依赖连接给出显著非平凡率，并与缺陷触达呈正相关。}
\end{figure}

\paragraph{方向驱动与运动相：缺陷团簇的净位移统计}
为推动系统从“驻留型对象（$p=1$）”向“可迁移对象”过渡，本文引入一个最小的非平衡驱动族：其一是对残差场 $r$ 在 $x$ 方向做有偏输运（advection），其二是在宏态更新偏置中加入残差 $x$-梯度驱动项（\texttt{drive\_gamma}），从而在缺陷附近形成可持续的定向载流倾向。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_transport.py --m 10 --steps 240 --advect-p 0.0 --coupling 2.0 --drive-gamma 6.0}
\end{center}
对应摘要片段如下：
\begin{center}
\input{sections/generated/emergence_transport_summary}
\end{center}
并给出漂移速度分布示例图：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_transport_drift_hist.png}
  \caption{方向驱动（advection + 残差梯度驱动）下对象漂移速度分布（按寿命归一化）。}
\end{figure}

\paragraph{运动相参数扫描}
对驱动强度 \texttt{advect\_p}、耦合强度 \texttt{coupling} 以及梯度驱动强度 \texttt{drive\_gamma}（固定）进行网格扫描，可得到“移动对象比例”与“移动对象平均漂移幅度”的相图。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_transport_scan.py --m 10 --drive-gamma 6.0}
\end{center}
最佳区域摘要如下：
\begin{center}
\input{sections/generated/emergence_transport_scan}
\end{center}
并给出两张相图示例：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_transport_phase_moving.png}
  \caption{运动相相图：移动对象比例（moving\_frac）。}
\end{figure}
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_transport_phase_drift.png}
  \caption{运动相相图：移动对象平均漂移幅度（mean $|$drift$|$）。}
\end{figure}

\paragraph{行走/准行走对象：形状稳定性--漂移联合判据}
为进一步逼近“粒子运动”的可检验结构，本文在同一输运动力学上引入一个更严格的对象运动判据：对每条对象轨迹同时考察 (i) 每步平均漂移幅度 $|v|$，(ii) 连续两帧缺陷团簇形状的 Jaccard 相似度（在质心坐标系下的相对体素集合）。在此基础上，
\begin{itemize}
  \item \emph{行走周期轨}：存在最小周期 $p\ge 2$，使得形状在周期内重复且每周期净位移非零；
  \item \emph{准行走对象}：形状稳定性（Jaccard 中位数）超过阈值且 $|v|$ 超过阈值。
\end{itemize}
运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_transport_walkers.py --m 10 --drive-gamma 6.0}
\end{center}
对应摘要片段如下：
\begin{center}
\input{sections/generated/emergence_transport_walkers_summary}
\end{center}
并给出稳定性--漂移散点图（阈值虚线）：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_transport_walkers_stability_scatter.png}
  \caption{对象轨迹的形状稳定性（Jaccard）与漂移幅度的联合分布；右上象限为“准行走对象”。}
\end{figure}

\paragraph{二缺陷相互作用（散射型）统计}
在相同动力学参数下，将两个局域缺陷团簇沿 $x$ 方向分离放置并演化，记录“两个最大缺陷团簇”之间的距离时序，并统计最近距离分布与“近接触”事件率。运行命令：
\begin{center}
\path{python3 scripts/exp_emergence_transport_scattering.py --m 10 --drive-gamma 6.0}
\end{center}
对应摘要片段如下：
\begin{center}
\input{sections/generated/emergence_transport_scattering_summary}
\end{center}
并给出一个距离时序示例：
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{artifacts/export/emergence_transport_scattering_distance.png}
  \caption{二缺陷相互作用：两个最大缺陷团簇的距离随时间变化示例；虚线为近接触阈值。}
\end{figure}

\subsection{双边界合成的剪枝加速评测}

给定终端约束类 $\mathcal F_T$（或终端权重 $\Phi$），比较两种求解方式：
\begin{enumerate}
  \item 仅前向搜索（或仅前向传播）；
  \item 前向--后向合成（第 13 节 smoothing）。
\end{enumerate}
评测指标包括：收敛步数、有效候选状态数、剪枝率、以及在相同计算预算下的终端满足率。前向--后向算法的数值稳定化可参照 Rabiner 综述的缩放/对数域实现。\cite{rabiner_hmm_tutorial}
