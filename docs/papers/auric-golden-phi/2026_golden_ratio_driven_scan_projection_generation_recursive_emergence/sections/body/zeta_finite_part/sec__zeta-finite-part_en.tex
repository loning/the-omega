\section{Dynamical \texorpdfstring{$\zeta$}{zeta} functions and the finite part: from syntax to spectral fingerprints}\label{sec:zeta-finite-part}

This section translates the preceding ``finite-state syntax/kernel'' interface into spectral-side objects: using transfer operators/adjacency matrices on finite graphs (SFT/sofic) as the foundation, it employs determinants and trace generating functions to characterize periodic orbits and the dominant spectral radius, thereby obtaining auditable \(\zeta\) fingerprints and finite-part constants. The purpose of this interface is twofold: on the one hand, it strictly aligns the counting on the primitive (prime-like) side with the linear-algebraic objects on the spectral/trace side; on the other hand, it provides a unified input for the subsequent completion (self-dual compression to a one-variable \(\Xi\)), unit-root twisting, and congruence equidistribution audit (Chebotarev caliber).\par
The subsections below present, in order: the dynamical \(\zeta\) and discrepancy objects at the stable syntax level, the \(\zeta_B\) and primitive orbit spectrum for the online synchronization kernel, the \(\zeta\) in the operator caliber (Fredholm/transfer operators and the strictly commutative bridge package), the finite-part constants and twisted equidistribution (Dirichlet--Mertens tensor and slow-mode fingerprint), as well as the completion/\(\Xi\) zero audit, the finite-data certificates for the profinite Chebotarev framework, and the \(\mathbf{PW}\) type-safety provisions.

\subfile{syntax/main}

\subfile{online_kernel/main}

\subfile{operator/main}

\subfile{finite_part/main}

\subfile{xi/main}
