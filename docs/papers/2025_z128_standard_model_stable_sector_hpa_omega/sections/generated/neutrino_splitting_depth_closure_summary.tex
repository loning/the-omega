\textbf{Splitting closure attempt (audit):} bounded rational r-offsets with $q\le 12$ are evaluated in three modes: (\texttt{cap}) protocol-only CAP-minimizer by denominator/numerator complexity, (\texttt{proto}) an invariant-derived candidate at the $m=6$ anchor (18$\oplus$3 split), and (\texttt{match}) mismatch-minimizer to the recorded oscillation splittings.