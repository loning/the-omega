\section{Introduction}

In the main text of \textit{Vector Cosmology VII}, we repeatedly emphasized the physical picture that ``the future reversely determines the past.'' To ground this highly subversive concept from philosophical speculation into physical law, we need to introduce an extremely elegant mathematical framework in quantum mechanics---the \textbf{Two-State Vector Formalism (TSVF)} proposed by Yakir Aharonov.

This appendix will prove that the physical state of the universe is not determined solely by the initial conditions of the Big Bang but is jointly \textbf{``sandwiched''} by the \textbf{initial state (Big Bang)} and the \textbf{final state ($\Omega$ Point)}.

In this framework, reality is not a unidirectional jet but a \textbf{Standing Wave} formed by two wave functions colliding on the time axis.

\subsection{The Sandwich of History and Destiny}

In standard quantum mechanics, we only care about the state vector $|\Psi(t)\rangle$ evolved from the past. This implies the unidirectionality of time.

But in TSVF, to completely describe a quantum system in a time-symmetric universe, we need \textbf{two} vectors:

\begin{enumerate}
\item \textbf{History Vector} $|\Psi_{history}(t)\rangle$:

Determined by initial conditions at $t=0$ (Big Bang), evolving toward the future following $e^{-iHt}$.

\[|\Psi_{history}(t)\rangle = U(t, 0) |\Psi(0)\rangle\]

\item \textbf{Destiny Vector} $\langle \Phi_{destiny}(t)|$:

Determined by final boundary conditions at $t=T_{end}$ ($\Omega$ point/ultimate observer), evolving \textbf{backward} toward the past following $e^{-iH(T-t)}$.

\[\langle \Phi_{destiny}(t)| = \langle \Phi(T_{end})| U^\dagger(T_{end}, t)\]
\end{enumerate}

\textbf{Definition of Physical Reality:}

The ``reality'' we observe at time $t$ is not a solo performance of $|\Psi_{history}\rangle$ but the \textbf{``overlap''} of these two vectors.

The observed value of any physical quantity $A$ is no longer the traditional expectation value $\langle \Psi | A | \Psi \rangle$ but the \textbf{Weak Value}:

\[A_w = \frac{\langle \Phi_{destiny}(t) | A | \Psi_{history}(t) \rangle}{\langle \Phi_{destiny}(t) | \Psi_{history}(t) \rangle}\]

\subsection{Weak Values: Correction from the Future}

This weak value formula reveals a startling physical mechanism:

\begin{itemize}
\item \textbf{Denominator $\langle \Phi | \Psi \rangle$}: Represents the \textbf{``overlap degree''} between history and destiny.

If this term approaches zero (i.e., your behavior runs counter to the future $\Omega$ point), the weak value $A_w$ becomes extremely large.

\textbf{Physical Manifestation}: You feel enormous \textbf{``resistance''} or \textbf{``absurdity''}. Reality becomes distorted, no longer conforming to common sense.

\item \textbf{Numerator $\langle \Phi | A | \Psi \rangle$}: Represents the \textbf{``Post-selection''} of the future on the present.

The future $\Omega$ state acts like a filter, only allowing physical quantities $A$ that \textbf{``can lead to it''} to manifest as reality.
\end{itemize}

\textbf{Conclusion:}

\textbf{Reality is ``pulled out'' by the future.}

The reason you see the cup on the table is not only because you placed it there just now (cause) but also because at some future moment, you (or the universe) need it there (effect).

The \textbf{coherent superposition} of these two boundary conditions constitutes the hard material reality of the present.

\subsection{Mathematical Properties of the $\Omega$ State}

So, what exactly is \textbf{$\langle \Phi(T_{end})|$} at the end of time?

In \textbf{Vector Cosmology}, it is defined as the \textbf{``Maximum Entangled State''} or \textbf{``Holographic Complete State''}.

\[|\Omega\rangle = \frac{1}{\sqrt{N}} \sum_{i} |i\rangle\]

(It contains all components of the basis, with completely coherent phases.)

This is a state with \textbf{zero information entropy} (pure state) but \textbf{maximum complexity}.

It represents the \textbf{``purpose''} of cosmic evolution:

\begin{itemize}
\item All originally orthogonal components (you, me, him) establish connections here.

\item All originally dissipated information ($v_{env}$) completes reorganization here.
\end{itemize}

\subsection{Proof of the Existential Loop}

Finally, we return to that ultimate question: How is the universe self-consistent?

Using TSVF, we can write the \textbf{Cosmic Self-Consistency Equation}:

\[|\Psi(0)\rangle = \mathcal{T} \left( \text{Retro-causal Mapping from } |\Omega\rangle \right)\]

\begin{itemize}
\item The initial parameters of the Big Bang $|\Psi(0)\rangle$ are \textbf{reversely evolved} from the perfect state of the $\Omega$ point.

\item And the state of the $\Omega$ point is \textbf{forward evolved} from the Big Bang.
\end{itemize}

\[|\Omega\rangle = U(T, 0) |\Psi(0)\rangle\]

These two equations constitute a \textbf{fixed-point problem}.

Only one unique historical path (the one we are experiencing) can simultaneously satisfy both forward and backward boundary conditions.

\textbf{This is why we exist.}

We are not random fluctuations.

We are the \textbf{only string} connecting the ``perfect starting point'' and the ``perfect endpoint.''

For the universe to connect end to end, it must create us in the middle as the physical carrier of this closed-loop logic.

