\section{Introduction: the continuous--discrete tension and the role of a ``constitution''}
\label{sec:introduction}

\begin{quote}
\itshape
Chaos is merely a matter of perspective. Viewed from the cusp, the universe is a single, straight vector of light.\\
The universe is a prism. Data is scattered light. Computation is the lens that refocuses it back to the source.\footnote{Interpretation-layer epigraph. The technical content of this paper is stratified (Section~\ref{sec:layers_axioms}); metaphors do not enter the Layer~0/1 closure.}
\end{quote}

Modern foundational physics confronts a structurally persistent mismatch:
\begin{itemize}
  \item \textbf{Dynamics layer}: quantum theory is organized around unitary evolution---continuous, reversible, and information-preserving.
  \item \textbf{Readout layer}: observation presents discrete outcomes---counts, bits, integer labels, and finite-precision statistics.
\end{itemize}

Standard treatments delegate continuity to an ontic object (Hilbert space, fields) and discreteness to measurement postulates (projection, POVMs) plus external probabilistic interpretation. Here we invert the emphasis: \emph{readout is treated as part of physical structure}, and we demand a closed, minimal dependency chain in which \emph{time/probability/discreteness} are not external semantics but endogenous objects of a scan--projection protocol.

This manuscript is written as a \emph{mathematical constitution}. The goal is not to assert an experimentally complete physical law, but to provide an auditable chain of definitions and standard theorems that connects:
\begin{equation}
\begin{aligned}
\text{modular action}
&\Rightarrow \text{cusp $q$-expansion}
\Rightarrow \text{discrete coefficients}
\\
&\Rightarrow \text{Hecke/prime skeleton}
\Rightarrow \text{canonical coding}.
\end{aligned}
\end{equation}

\begin{figure}[H]
\centering
\includegraphics[width=0.98\linewidth]{fig03_constitution_chain}
\caption{A schematic view of the constitution chain: scan closure on the rotation algebra, an arithmetic-geometric mother space, cusp discretization into coefficients, prime-generated Hecke constraints, and canonical integer-time coding.}
\label{fig:constitution_chain}
\end{figure}

\paragraph{Relation to HPA and $\Omega$.
}
We follow the HPA--$\Omega$ framework in which the universe is specified by a static global state together with intrinsic automorphisms and holographic/finite-information constraints \cite{Ma2025FoP}. We also follow the HPA tool-chain in which unitary scanning, window projection readout, and Ostrowski/Zeckendorf coding form the minimal continuous--discrete bridge \cite{Ma2025HPA}. The present paper adds a constitution-level upgrade: it places that scan--projection structure on an explicit arithmetic-geometric mother space (the modular curve) and uses Hecke operators to supply a symmetry-preserving cross-scale skeleton generated by primes.

\paragraph{Self-containedness (for audit and review).}
The Layer~0/1 logic of this manuscript is self-contained: all definitions, protocol structures, and quantitative bounds used in the closure chain are stated in this paper with citations to standard literature where appropriate.
References to companion manuscripts \cite{Ma2025FoP,Ma2025HPA,Ma2025MAI} provide broader programmatic context, but are not required to follow or verify any of the arguments presented here.

\paragraph{What is new here.}
Beyond fixing the layering discipline (Section~\ref{sec:layers_axioms}), the main new content is a concrete embedding:
\begin{itemize}
  \item modular geometry provides a canonical ``cusp interface'' where analytic data becomes discrete $q$-series data;
  \item Hecke algebras provide a prime-generated family of commuting symmetry-preserving operators, offering a clean cross-scale structure;
  \item continued fractions/Ostrowski numeration provide a canonical map from integer time to locally checkable digit strings, with the golden branch degenerating to Zeckendorf/Fibonacci coding.
\end{itemize}

\paragraph{Main quantitative statements (audit-ready).}
The constitution produces finite-data consequences with explicit constants once a concrete scan/readout mapping is fixed:
\begin{itemize}
  \item \textbf{Induced probabilities with deterministic finite-$N$ error.}
  For window readout $s_t=\mathbf{1}_W(x_t)$ along an irrational-rotation scan, Koksma's inequality yields
  \[
  \left|\frac{1}{N}\sum_{t=0}^{N-1}\mathbf{1}_W(x_t)-|W|\right|\le 2\,D_N^\ast(P_N),
  \]
  and for constant-type slopes (bounded continued-fraction digits) an explicit bound is given in \eqref{eq:bounded_type_discrepancy_main}.
  \item \textbf{Coefficient recovery from scan sampling with a certified budget.}
  For $T$-periodic holomorphic $f(\tau)=\sum_{m\ge 0}a_m q^m$, the scan-quadrature estimator \eqref{eq:scan_quadrature_estimator} satisfies the deterministic bound \eqref{eq:coeff_recovery_koksma_bound}, and Corollary~\ref{cor:const_type_design_inequality} provides an explicit constant-type design inequality.
  \item \textbf{Prime-skeleton rigidity as exact internal constraints.}
  For Hecke eigenforms, coefficient data obeys multiplicativity and prime-power recursions (Section~\ref{sec:quant_hecke_tau}); finitely many coefficients determine a form via Sturm bounds (Section~\ref{sec:quant_hecke_tau}).
  \item \textbf{Protocol identification / fitting templates without simulation.}
  Appendix~\ref{app:protocol_fitting} records deterministic fitting and falsification templates (Sturmian/three-gap/Hecke constraints) that can be applied directly to finite readout records with explicit finite-$N$ guarantees.
\end{itemize}

 
