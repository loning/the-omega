Under the uniform codon prior, $I(\mathsf{Gen}(C);w_{\mu^\ast}(C))=2.577209$ bits, while the maximum over all $24$ encodings is $2.639709$ bits (achieved by $\mu_\mathrm{MI}$ with $A\mapsto \texttt{01},\ C\mapsto \texttt{10},\ G\mapsto \texttt{11},\ U\mapsto \texttt{00}$).
