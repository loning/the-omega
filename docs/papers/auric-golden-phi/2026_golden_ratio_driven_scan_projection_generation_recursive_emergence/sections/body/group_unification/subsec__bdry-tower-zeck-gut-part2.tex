\label{subsec:bdry-tower-zeck-gut-part2}
\paragraph{\(m=10\) 退化谱的例外核扇区：\texorpdfstring{$52=\dim\mathfrak{f}_4$}{52=dim f4}}\label{par:gut-exceptional-kernel-f4}
在表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的 \(m=10\) 行，
bin-fold 退化谱把稳定类型集合 \(X_{10}\) 按退化度 \(d^{\mathrm{bin}}_{10}(w)\) 分解为三扇区：
\[
X_{10}=\mathcal{S}_{10,5}\ \sqcup\ \mathcal{S}_{10,8}\ \sqcup\ \mathcal{S}_{10,9},\qquad
\bigl(|\mathcal{S}_{10,5}|,|\mathcal{S}_{10,8}|,|\mathcal{S}_{10,9}|\bigr)=(55,52,37),
\]
其中 \(\mathcal{S}_{10,d}:=\{w\in X_{10}: d^{\mathrm{bin}}_{10}(w)=d\}\)。
注意 \(|\mathcal{S}_{10,5}|=55\) 为 Fibonacci 数 \(F_{10}\)，与上文 \(\dim=55\) 的两路锁定一致；
而 \(|\mathcal{S}_{10,8}|=52\) 则与简单例外李代数 \(\mathfrak{f}_4\) 的伴随维数满足精确恒等式
\[
\boxed{\ |\mathcal{S}_{10,8}|=52=\dim\mathfrak{f}_4\ }.
\]
在同一三扇区内，\(|\mathcal{S}_{10,5}|=55\)、\(|\mathcal{S}_{10,9}|=37\) 不落在
\(\{\dim\mathfrak{g}:\mathfrak{g}\in\{G_2,F_4,E_6,E_7,E_8\}\}\) 之中，
并且在表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的其余行（\(m\in\{6,8,12\}\)）中并不出现扇区大小为 \(52\) 的分块，
因此 \(m=10\) 的退化谱在维数层内禀地产生一个可被单独指认的“例外核扇区”\(\mathcal{S}_{10,8}\)；
该指认仅依赖可枚举退化谱分解，而不依赖 Zeckendorf--GUT 维数签名字典所预置的候选群表。

\paragraph{例外核 \texorpdfstring{$52$}{52} 与最大纤维极值模 \texorpdfstring{$26$}{26} 的 \texorpdfstring{$E_6$}{E6} 完备化：\texorpdfstring{$52+26=78$}{52+26=78}}\label{par:gut-e6-completion-52-26}
令 \(D_m:=\max_{x\in X_m} d_m(x)\) 为窗口折叠 \(\Fold_m\) 的最大纤维（定理 \ref{thm:pom-max-fiber}）。
由表 \ref{tab:fold_max_fiber_achievers_hiddenbit_metrics} 可复核地读出
\(
D_{13}=26
\)。
将结论 \ref{par:gut-exceptional-kernel-f4} 的例外核扇区维数 \(52\) 与该极值模尺度配对，则得到刚性维数闭合：
\[
\boxed{\ 52+D_{13}=52+26=78=\dim\mathfrak{e}_6\ }.
\]
因此，可提出一个仅依赖既有两条谱数据的离散完备化模板：以 \(m=10\) 的 bin-fold 退化谱选出例外核（\(\dim\mathfrak{f}_4\)），再以 \(m=13\) 的最大纤维极值尺度给出完备模，从而在维数层闭合到 \(\mathfrak{e}_6\) 的伴随维数。

\paragraph{极值模驱动的例外塔增量：与最大纤维谱逐点对齐}\label{par:gut-maxfiber-exceptional-tower}
由表 \ref{tab:fold_max_fiber_achievers_hiddenbit_metrics} 进一步读出
\[
D_{16}=55,\qquad D_{18}=89,
\]
并结合公开的例外李代数伴随维数
\[
\dim\mathfrak{e}_6=78,\qquad \dim\mathfrak{e}_7=133,\qquad \dim\mathfrak{e}_8=248,
\]
得到两条维数增量恒等式（其增量来源可由最大纤维极值模解释）：
\[
\boxed{\ \dim\mathfrak{e}_7-\dim\mathfrak{e}_6=55=D_{16}\ },
\qquad
\boxed{\ \dim\mathfrak{e}_8-\dim\mathfrak{e}_7=115=89+26=D_{18}+D_{13}\ }.
\]
从而例外塔的上升在维数层可被重写为由最大纤维谱所供给的离散增量补全；该表述不依赖具体的群嵌入选择，其输入仅为折叠投影的极值谱与标准维数表。

\paragraph{\(m=10\) 三扇区的统一链骨架：\texorpdfstring{$(55,52,37)$}{(55,52,37)}}\label{par:gut-trisector-55-52-37}
由表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12}，\(m=10\) 的 bin-fold 退化谱在稳定类型层强制出三扇区骨架 \((55,52,37)\)。
结合结论 \ref{par:gut-exceptional-kernel-f4}--\ref{par:gut-maxfiber-exceptional-tower}，可将其中两块作结构性定位：
\begin{itemize}
  \item \(\mathcal{S}_{10,8}\)（扇区大小 \(52\)）：例外核扇区（\(\dim\mathfrak{f}_4\) 锚点）；
  \item \(\mathcal{S}_{10,5}\)（扇区大小 \(55\)）：完备化尺度扇区（与 \(D_{16}=55\) 以及 \(E_6\to E_7\) 的维数增量对齐）；
  \item \(\mathcal{S}_{10,9}\)（扇区大小 \(37\)）：剩余接口扇区；其作为补余接口的维数地位可由 \(D_{18}\) 的同层分解强制定位（接口化结论 \ref{par:gut-d18-endogenous-split}），并可在最小一致性原则下给出一个表示论语义闭合候选 \(\mathfrak{so}(9)\oplus\mathfrak{u}(1)\)（接口化结论 \ref{par:gut-sector37-so9-u1}；审计判据见 \ref{par:gut-foldbin-fold-audit-criteria}）。
\end{itemize}
该三元骨架以退化谱为第一输入，以例外核/极值模的维数闭合为第二输入，从而把“接口扇区”（\(37\)）提升为必须被解释的剩余对象。

\paragraph{接口化结论：\texorpdfstring{$\mathrm{Fold}^{\mathrm{bin}}$}{Foldbin}–\texorpdfstring{$\mathrm{Fold}$}{Fold} 的跨尺度补余恒等式}\label{par:gut-foldbin-fold-complement-identity}
以下接口化结论簇仅以本文既有的可复现枚举谱数据为输入（尤其是表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的退化谱输出与最大纤维闭式接口），
不引入额外外推或解释层候选；行文亦不复述既有段落，仅记录接口断言与其直接推论/核对协议。
对二进制区间折叠 \(\mathrm{Fold}^{\mathrm{bin}}_m:\{0,\dots,2^m-1\}\to X_m\)，记
\(
d^{\mathrm{bin}}_m(w):=\bigl|\mathrm{Fold}^{\mathrm{bin}}_m{}^{-1}(w)\bigr|
\)
并令 \(d_{\min}(m):=\min_{u\in X_m}d^{\mathrm{bin}}_m(u)\)。
对每个 \(d\) 定义退化谱扇区
\(
\mathcal{S}_{m,d}:=\{w\in X_m:\ d^{\mathrm{bin}}_m(w)=d\}
\)。
则在已公开的偶数窗长 \(m\in\{6,8,10,12\}\) 上，bin-fold 的“最刚性扇区”补余容量与窗口折叠 \(\Fold\) 的更高分辨率最大纤维严格锁定为同一量：
\[
\boxed{\ \bigl|X_m\setminus \mathcal{S}_{m,d_{\min}(m)}\bigr|\;=\;D_{2m-2}\ }.
\]
该恒等式构成一条无需外推的交叉审计闭合：左端由表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12} 的退化谱输出直接给出，而右端由最大纤维闭式定理 \ref{thm:pom-max-fiber}（等价地其表格读出）给出；相应核对式已脚本化输出如下：
\input{sections/generated/eq_foldbin_fold_complement_identity_m6_m12}

\paragraph{接口化结论：\texorpdfstring{$D_{18}$}{D18} 的 \texorpdfstring{$m=10$}{m=10} 内生分解与 \texorpdfstring{$37$}{37} 接口扇区的强制定位}\label{par:gut-d18-endogenous-split}
由接口化结论 \ref{par:gut-foldbin-fold-complement-identity} 的 \(m=10\) 特例可得
\(
D_{18}=|X_{10}\setminus\mathcal{S}_{10,5}|
\)。
另一方面，结论 \ref{par:gut-exceptional-kernel-f4} 已给出同层三扇区分解
\(
X_{10}=\mathcal{S}_{10,5}\sqcup\mathcal{S}_{10,8}\sqcup\mathcal{S}_{10,9}
\)
及其精确计数 \((55,52,37)\)，并将 \(|\mathcal{S}_{10,8}|\) 结构性指认为 \(\dim\mathfrak{f}_4\)。
因此得到同层分解恒等式
\[
D_{18}=|\mathcal{S}_{10,8}|+|\mathcal{S}_{10,9}|=52+37,
\qquad
\boxed{\ |\mathcal{S}_{10,9}|\;=\;D_{18}-\dim\mathfrak{f}_4\ }.
\]
换言之，先前作为“外部极值模尺度”读入的 \(D_{18}\) 可等价改写为：\(m=10\) 的退化谱补余在扣除例外核（\(52\)）后的唯一接口维数（\(37\)）。

\paragraph{接口化结论：\texorpdfstring{$37$}{37} 接口扇区的最小一致语义闭合：\texorpdfstring{$\mathfrak{so}(9)\oplus\mathfrak{u}(1)$}{so(9)+u(1)}}\label{par:gut-sector37-so9-u1}
在不引入额外候选群表、仅依赖本文已固化的两条锚点——例外核 \(|\mathcal{S}_{10,8}|=52=\dim\mathfrak{f}_4\)（结论 \ref{par:gut-exceptional-kernel-f4}）与边界层的自旋成对读出模板 \(33=1\oplus 16\times 2\)（并见 \S\ref{subsec:group-unification-audit-pointers} 中“NAP 选择器与 \(m=11\) 的 \(\ZZ_{34}\) Fourier 分解证书”的可复核接口条目）——可给出 \(37\) 扇区的一个最小一致语义闭合方案。
\par
标准李代数事实表明：\(\mathfrak{f}_4\) 含最大子代数 \(\mathfrak{so}(9)\)，并且在 \(\mathfrak{so}(9)\) 下伴随表示满足
\(
52=36\oplus 16
\)
（其中 \(16\) 为自旋型补余）。
若 \(52\) 扇区承担“例外核”，则与其最小相容的接口应当提供一个 \(\mathfrak{so}(9)\) 的 \(36\) 维骨架并额外携带一个尺度生成元，以实现从核到增量块的黏合；
这与接口化结论 \ref{par:gut-d18-endogenous-split} 所强制的
\(
37=36+1
\)
在维数层严格匹配。
因此提出如下闭合候选：
\[
\boxed{\ \mathcal{S}_{10,9}\text{ 的 \(37\) 维接口可解释为 }\mathfrak{so}(9)\oplus\mathfrak{u}(1)\ }.
\]
其中 \(\mathfrak{so}(9)\) 给出“接口骨架”，而 \(\mathfrak{u}(1)\) 承载与标度/端点精度势相容的单生成元方向；自旋型 \(16\) 维方向则在相邻分辨率层上以成对读出模板出现，从而把“例外核—接口—自旋物质”在相邻层上闭合为一致链条。

\paragraph{接口化结论：直接可证伪的统一审计判据——跨尺度补余、同层分解与不可约性}\label{par:gut-foldbin-fold-audit-criteria}
在结论 \ref{par:gut-exceptional-fingerprint} 的“核—模—塔”指纹之外，上述跨尺度补余恒等式与接口闭合提供一组更直接的失败判据；任一失败即否定对应闭合链条：
\begin{enumerate}
  \item \textbf{跨尺度补余审计（偶数窗长）} 对偶数 \(m\ge 6\)，枚举 \(\mathrm{Fold}^{\mathrm{bin}}_m\) 的退化谱并取 \(d_{\min}(m)\)。应满足
  \[
  |X_m|-|\mathcal{S}_{m,d_{\min}(m)}|=D_{2m-2}.
  \]
  本稿在 \(m\in\{6,8,10,12\}\) 上成立（接口化结论 \ref{par:gut-foldbin-fold-complement-identity}）；其在更大 \(m\) 上的稳定性构成新的否定判据。
  \item \textbf{\texorpdfstring{$D_{18}$}{D18} 的同层分解审计（\texorpdfstring{$m=10$}{m=10}）} 仅用 \(m=10\) 行的退化谱输出，应满足
  \[
  |X_{10}\setminus\mathcal{S}_{10,5}|=|\mathcal{S}_{10,8}|+|\mathcal{S}_{10,9}|=D_{18},
  \]
  即 \(52+37=89\)（接口化结论 \ref{par:gut-d18-endogenous-split}）。
  \item \textbf{\texorpdfstring{$37$}{37} 接口的表示论审计（\texorpdfstring{$\mathfrak{so}(9)\oplus\mathfrak{u}(1)$}{so(9)+u(1)} 方案）}
  若将 \(|\mathcal{S}_{10,9}|=37\) 的语义闭合为 \(\mathfrak{so}(9)\oplus\mathfrak{u}(1)\)，则对 \(\mathcal{S}_{10,9}\) 上由体系内“扫描/平移”或“稳定类型半环乘法/扫描算子”诱导的自然线性化作用，其不变子空间结构应出现：
  \begin{itemize}
    \item 一条 \(1\) 维不变方向（对应 \(\mathfrak{u}(1)\)）；
    \item 其余 \(36\) 维部分表现为单一不可约的 \(\mathfrak{so}(9)\) 伴随型骨架。
  \end{itemize}
  任意不可约分解型式与此不符，即否定该语义闭合方案。
\end{enumerate}

\paragraph{小结（从跨窗拼接到同层补余）}
以上四条把原先分属不同窗长的统一拼接（尤其是 \(D_{18}\) 的极值模输入）改写为 \(m=10\) 同层退化谱的补余结构，
并据此将接口扇区 \(37\) 从“残差计数”提升为可定位、可闭合且可证伪的对象。
若继续向更高阶统一延拓，则下一步的硬任务被明确化为：
检验判据 1 在更大偶数 \(m\) 上的稳定性，以及判据 3 的不可约性审计是否通过。

\paragraph{可证伪审计指纹（例外核—极值模—例外塔）}\label{par:gut-exceptional-fingerprint}
若将上述“例外核 + 极值模补全”的解释视为统一链的结构约束，则至少应满足下列三条可审计指纹：
\begin{enumerate}
  \item \textbf{例外核扇区指纹：}在可复核的低分辨率 sweep 中（表 \ref{tab:fold_bin_degeneracy_spectrum_m6_m12}），存在扇区大小恰为 \(52\) 的退化谱分块；例如在 \(m=10\) 时 \(|\mathcal{S}_{10,8}|=52\)，并与 \(\dim\mathfrak{f}_4\) 对齐。
  \item \textbf{\texorpdfstring{$E_6$}{E6} 完备化指纹：}最大纤维极值模提供 \(D_{13}=26\)（表 \ref{tab:fold_max_fiber_achievers_hiddenbit_metrics}），使得 \(52+26\) 闭合到 \(\dim\mathfrak{e}_6=78\)；若在更改折叠口径/核族后该尺度不再出现，则“核+模”完备化机制在该版本下失效。
  \item \textbf{例外塔增量指纹：}维数增量需满足
  \(\dim\mathfrak{e}_7-\dim\mathfrak{e}_6=D_{16}\) 与 \(\dim\mathfrak{e}_8-\dim\mathfrak{e}_7=D_{18}+D_{13}\) 的数值对齐（结论 \ref{par:gut-maxfiber-exceptional-tower}），从而将补全从连续微调收束为由极值模离散化的补块规则。
\end{enumerate}

\paragraph{扩展辛相空间强制 \texorpdfstring{$\mathfrak{sp}(10)$}{sp(10)}：\texorpdfstring{$55=12\oplus 9\oplus 24\oplus 10$}{55=12+9+24+10} 的四块统一骨架}\label{par:gut-sp10-four-blocks}
把三通道势—响应辛相空间（注 \ref{rem:sp6-minimal-continuous-envelope}）与二维混合压力的 Legendre--Fenchel 对偶前沿（注 \ref{rem:pom-psi-2d-legendre-front}）并置，可得到一个由五对共轭坐标组织的扩展相空间：取
\[
x:=(\theta_{\varphi},\theta_{\pi},\theta_{e},\ q-1,\ \log u),\qquad
y:=(J_{\varphi},J_{\pi},J_{e},\ \gamma,\ \mathcal{E}),
\]
其中 \((q-1,\gamma)\) 与 \((\log u,\mathcal{E})\) 的共轭配对由二维凸对偶结构
\[
I(\gamma,\mathcal{E})
=\sup_{q\ge 2,\ u\in(0,1)}
\Bigl((q-1)\gamma+(\log u)\mathcal{E}-\Psi(q,u)\Bigr)
\]
给出（注 \ref{rem:pom-psi-2d-legendre-front}）。
其标准辛形式为
\[
\omega_{\mathrm{tot}}
:=\sum_{c\in\{\varphi,\pi,e\}}\dd\theta_{c}\wedge \dd J_{c}
+\dd(q-1)\wedge \dd\gamma
+\dd(\log u)\wedge \dd\mathcal{E}.
\]
在该口径下，保持 \(\omega_{\mathrm{tot}}\) 的最小连续线性包络为 \(\mathrm{Sp}(10,\RR)\)；
因此二进制统一维数锚点 \(D^{\mathrm{bin}}_{\mathrm{GUT}}=55\) 被提升为辛结构所强制的 \(C_5\) 分支：
\[
\boxed{\ \dim\mathfrak{sp}(10,\RR)=55\ }\qquad(\text{与 }\dim\mathfrak{so}(11)=55\text{ 的对照分支相区别}).
\]
\par
按“三通道 \(\oplus\) 两条标度轴”的标准直和分裂，\(\mathfrak{sp}(10,\RR)\) 出现规范的三块分解
\[
\mathfrak{sp}(10,\RR)\ \cong\ \mathfrak{sp}(6,\RR)\ \oplus\ \mathfrak{sp}(4,\RR)\ \oplus\ \mathcal{X},
\qquad
55=21+10+24,
\]
其中交叉块 \(\mathcal{X}\) 的维数严格为 \(24\)。
因此 \(\dim\mathfrak{su}(5)=24\) 在本框架内更自然的角色是“通道（\(\mathfrak{sp}(6)\)）—标度（\(\mathfrak{sp}(4)\)）接口层”的维数槽位，
而 \(\mathrm{Sp}(10)\) 则提供包络层的辛闭包语义。
\par
结合上文关于 \(\mathfrak{sp}(6,\RR)=\mathfrak{u}(3)\oplus\mathfrak{p}\) 的刚性分裂，以及上述 \(21+10+24\) 三块分解，二进制暗容量 \(H^{\mathrm{bin}}_6=43\) 获得一个与分块结构一致的精确归因：
\[
\boxed{\ 43\ =\ 9\ (\mathfrak{u}(3))\ +\ 24\ (\mathcal{X})\ +\ 10\ (\mathfrak{sp}(4,\RR))\ }.
\]
从而维数 \(55\) 的统一槽位在结构层面被锁定为
\[
\boxed{\ 55\ =\ 12\ \oplus\ 9\ \oplus\ 24\ \oplus\ 10\ },
\]
其中 \(12\) 对应于由低退化刚性读出的可见扇区规模，其余 \(43\) 为三块可归因的隐藏容量。

\begin{proposition}[自对偶统一点的四块谱简并签名与 \texorpdfstring{$U(3)$}{U(3)} 各向同性恢复]\label{prop:gut-u1-four-block-spectrum-signature}
在上述四块分解
\(
55=12\oplus 9\oplus 24\oplus 10
\)
所确定的维数骨架下，于自对偶统一点 \(u=1\)（注 \ref{rem:selfdual-u1-six-branch-lock}），任何以线性化转移算子或响应算子读出统一槽位的实现，其谱应呈现与该四块分解相容的不变块结构；其块多重度在可观测层面稳定为 \(\{12,9,24,10\}\)，并对核/门的可审计微扰保持鲁棒。\par
此外，在该点通道响应张量应恢复 \(U(3)\)-各向同性，即与单位阵成比例；其可审计入口由上文的各向同性点判据与相应扫描表提供。
\end{proposition}

\paragraph{NAP 选择器：在“非阿贝尔层保留”约束下 \texorpdfstring{$SO(10)$}{SO(10)} 被离散地唯一挑出}
把 \(\{6,8\}\) 视为“非阿贝尔共振层”（对应 \(3\) 与 \(8\) 的可见计数接口），并施加\textbf{非阿贝尔层保留}约束：
向上统一时允许替换/吸收阿贝尔层（\(m=4\)），但要求 \(\{6,8\}\) 作为\emph{独立层}保留在 \(\Sigma(\mathfrak g)\) 中。
在该约束下，对所有简单李代数扫描 \(\Sigma(\mathfrak g)\) 并最小化维数 \(D=\dim\mathfrak g\)，
得到最小解为 \(D_5=\mathfrak{so}(10)\)，且其签名恰为 \(\Sigma=\{6,8,11\}\)。
该选择器的扫描表与“单次离散替换 \(4\to 11\)”的计数等式证书由脚本生成如下：
\input{sections/generated/tab_nap_selector_so10}
\input{sections/generated/eq_nap_selector_so10}
\par
更强地，若要求 \(\{4,6,8\}\) 三层均保持独立（不允许被合并/吸收），则最小简单解跃迁到 \(D_{10}=\mathfrak{so}(20)\)（\(\dim=190\)，\(\Sigma=\{4,6,8,11,14\}\)；见表 \ref{tab:nap_selector_so10} 的 strict 部分），
从而把“阿贝尔层必须被统一重写”提升为一个可审计、可证伪的离散强迫结论。

\paragraph{信息代价选择器 \texorpdfstring{$\Omega$}{Omega}：\texorpdfstring{$SO(10)$}{SO(10)} 的最小维数/最小信息代价双极小}
除最小化维数 \(D=\dim\mathfrak g\) 外，可用分辨率折叠的平均多重度
\[
d_m:=\frac{2^m}{F_{m+2}}
\]
定义一个独立的“信息代价”函子
\[
\Omega(\mathfrak g):=\sum_{m\in\Sigma(\mathfrak g)}\log d_m.
\]
在 NAP 约束 \(\{6,8\}\subseteq\Sigma(\mathfrak g)\) 下，对简单李代数扫描并最小化 \(\Omega\)，最小解仍由 \(SO(10)\) 给出（\(\Omega(SO(10))\approx 4.826\)），且下一候选（例如 \(SO(12)\)）已显著更高（表 \ref{tab:gut_information_cost_selector}）。
同理，在 strict 约束 \(\{4,6,8\}\subseteq\Sigma(\mathfrak g)\) 下最小解跃迁到 \(SO(20)\)，并伴随 \(\Omega\) 的数量级上跳（\(\Omega(SO(20))\approx 8.328\)）。
\input{sections/generated/tab_gut_information_cost_selector}
因此，\(SO(10)\) 分支在本框架内同时是“最小维数”与“最小信息代价”的双重极小解，为其离散选择提供了与维数最小化并行、可审计的第二条独立理由。

\paragraph{\texorpdfstring{$m=11$}{m=11} 电荷层的规范分裂：\texorpdfstring{$34=1\oplus 33$}{34=1+33} 且 \texorpdfstring{$33=(16\ \text{对})+1$}{33=(16 pairs)+1}}
由 \(|X_{11}^{\mathrm{bdry}}|=F_9=34\) 与显式双射 \(X_{11}^{\mathrm{bdry}}\cong X_7\)（\(u=10\,v\,01\)），
可用 Zeckendorf 值域的规范编号 \(j\in\ZZ_{34}\) 为 \(m=11\) 的 \(34\) 个边界模态给出无歧义标签。
在该编号下，平移作用 \(T:e_j\mapsto e_{j+1}\) 的实正则表示强制给出一个规范的 \(1\oplus 33\) 模板：其中 \(Y_0\) 是唯一的 \(T\)-不变方向，其正交补空间维数自动为 \(33\)。
并在该 \(33\) 维正交补内进一步强制 \(33=1\oplus 16\times 2\) 的粗谱并群：\(Y_{17}\) 给出唯一自共轭实方向，其余为 \(16\) 个二维旋转平面：
\input{sections/generated/eq_m11_boundary_z34_decomposition}
该分解只依赖 \(\ZZ_{34}\) 的表示论与规范编号，不依赖任何外加“手工配对”；
因此它为后续把投影门/核谱算子化时的谱裂并群提供一个可审计靶标（特别地，\(34-1=33\) 的重方向计数与 NAP 选择器所确定的 \(m=11\) 电荷层定位在计数层面精确咬合）。
