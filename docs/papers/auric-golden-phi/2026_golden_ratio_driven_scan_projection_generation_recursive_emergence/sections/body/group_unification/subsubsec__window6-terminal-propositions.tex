\subsubsection{Terminal proposition: golden branch time, 6D microstate and Zeckendorf--GUT's discrete three branches}\label{subsubsec:window6-terminal-propositions}

Under the fixed single-generation scan-binary window readout protocol in this article, "time" is defined as the iteration count of \emph {single-generation scan operator} (\SXPH1X), and the anti-resonance baseline is fixed at the Hurwitz/Markov worst approximation scale, making the golden branch \(\alpha=\varphi^{-1}\) a canonical representative (\SXPH2X).Within this framework, window-\(6\) serves as a minimal anchor point that simultaneously controls: discrete time scales for binary window readout, compression of 6D hypercube microstates into stable grammars (with Zeckendorf truncated window surjections \(\mathrm{Fold}^{\mathrm{bin}}_6:\{0,\dots,63\}\twoheadrightarrow X_6\) and \(|X_6|=21\) as minimally computable interfaces; see It presents auditable edge separation, stabilizer rigidity, and four edge--flux skeletons (interaction matrix and spectral fingerprint) induced by local perturbation push under the action of geometric automorphism.This section only describes terminal assertions and auditable interface pointers, and does not describe intermediate derivation.
\begin{enumerate}
\item }
\begin{theorem} [Scan iteration time and standardization of golden branches]  \label{thm:terminal-time-protocol-golden-branch}
In the scan-projection readout framework, given a single generation action \(T^t\) and a binary window readout\(s_t(x)=\ind_W(T^t x)\in\{0,1\}\), the tick variable \(t\) is defined as the iteration count of \(T\), rather than plus the continuous coordinates (\SXPH3X).Independently, the "time measure" of the protocol layer can be taken as the logarithmic contraction of the posterior cylindrical set mass (entropy rate time \(\tau_{\mathrm{info}}(t)\), defined as \ref{def:entropy-rate-time}), thus strictly distinguishing the "input length \(t\)" from the "time measure" (\SXPH3X).At the object level, if there is a maximum isometry factor, the scanning phase can be expressed as a Kronecker translation on the compact commutative group; in the case of a one-dimensional circle, it is a symbolic interface of irrational rotation and interval window (Appendix \ref{app:cut_project_background}'s Sturmian prototype and phason parameterization note \ref{rem:app-cutproject-phason-time}).At the protocol layer, this article fixes the minimum interface of \emph {single generation, binary window readout}, and requires the specification normalization layer to be compatible with the Fibonacci/Ostrowski level; under this caliber, the anti-resonance baseline is fixed with the Hurwitz/Markov worst approximation scale, making the golden branch \(\alpha=\varphi^{-1}\) (and its \(\mathrm{SL}_2(\ZZ)\) equivalent class) the canonical representative of \emph {anti-resonance extreme} (defining \ref{def:markov-constant} and \SXPH3X).At the same time, when the Ostrowski hierarchy degenerates into the Fibonacci hierarchy and is identical to the Zeckendorf canonical representation (no adjacent \(11\)) (see definition \ref{def:fold-bin} for Zeckendorf Coded auditable input caliber), the golden branch is also the only rigid implementation of this normalization layer; therefore, under the discipline of "single generation + binary window + normative normalization", the golden branch is the smallest and standardized geometric representation that can be read out in time, and is not confused with the ontology description of "time = iteration count".  \end{theorem}

\begin{corollary}[\texorpdfstring{XPH0X}{log phi} scale lock and endogenous correlation time closed form]  \label{cor:terminal-tau-corr-logphi}
When the stable grammar takes the golden mean constraint and uses Parry's maximum entropy equilibrium state as the protocol benchmark, the time unit is locked by the entropy rate \(\log\varphi\) (Proposition \ref{prop:time-unit-logphi-protocol}).At the same time, in the Parry equilibrium state of the \(\chi\) slice \(t=0\) of the real input \(40\) state kernel, the spectral gap ratio satisfies  \(
  \rho_{\mathrm{corr}}(0)=\varphi^{-1}
  \)
And the endogenous correlation time has a closed form  \[
  \tau_{\mathrm{corr}}(0)=\frac{1}{-\log\rho_{\mathrm{corr}}(0)}=\frac{1}{\log\varphi}
  \]
(Note \ref{cor:chi-slice-t0-golden-tau}).The framework also provides auditable permutations for rescaling step times to event times (tabular certificates for event clocks in \SXPH0X).  \end{corollary}

\begin{corollary}[Ostrowski--Zeckendorf consistency and truncation folding interface of the golden branch]\label{cor:terminal-ostrowski-zeckendorf-binfold}Under the golden branch \(\alpha=\varphi^{-1}=[0;1,1,1,\dots]\), the Ostrowski legal numeric constraints degenerate into the binary constraints \(d_n\in\{0,1\}\) andTherefore, for any time step \(N\in\NN\), taking the first \(m\) bits of the Zeckendorf number gives us the binary interval folded readout  \[
  \mathrm{Fold}^{\mathrm{bin}}_m(N)=\pi_m\bigl(Z(N)\bigr)
  \]
(definition \ref{def:fold-bin}).This gives an auditable implementation layer of the "Iteration Counting  \end{corollary}

\begin{definition}[Natural continuation of Zeckendorf truncation fold and tail fiber parameters]\label{def:terminal-fold-zeck-extension}For each \(m\ge 1\), take the Zeckendorf digits \(Z(N)=(c_k)_{k\ge 1}\) (\(N=\sum_{k\ge 1}c_kF_{k+1}\), \(c_k\in\{0,1\}\), and \(c_kc_{k+1}=0\)) and the first  \[
  \mathrm{Fold}^{\mathrm{zeck}}_m:\NN\to X_m,\qquad
  \mathrm{Fold}^{\mathrm{zeck}}_m(N):=\pi_m\bigl(Z(N)\bigr).
  \]
Its restriction on the interval \(\{0,\dots,2^m-1\}\) is the binary interval fold \(\mathrm{Fold}^{\mathrm{bin}}_m\) (defining \ref{def:fold-bin}).Furthermore, if \(\mathrm{Fold}^{\mathrm{zeck}}_m(N)=w\in X_m\), then \(N\) can be written as  \[
  N=V_m(w)\ +\ \sum_{k\ge m+1} c_kF_{k+1},
  \]
The tail digitsThe truncated fold aperture with tail fiber parameterization is fixed by definition \ref{def:fold-bin} with proposition \ref{prop:fold-bin-fiber-tail} as an auditable interface.  \end{definition}

\begin{definition}[Next type multi-valued operator and time factor graph]\label{def:terminal-succ-m}For continuation folding \(\mathrm{Fold}^{\mathrm{zeck}}_m\), define the "next type" multivalued operator induced by time advance \(N\mapsto N+1\) on \(X_m\)  \[
  \mathrm{Succ}_m(w):=\Bigl\{\mathrm{Fold}^{\mathrm{zeck}}_m(N+1):\ N\in\NN,\ \mathrm{Fold}^{\mathrm{zeck}}_m(N)=w\Bigr\}\subseteq X_m,
  \]
And define the directed graph \(G^{\mathrm{Succ}}_m\) accordingly: the vertex set is \(X_m\), and if \(w'\in\mathrm{Succ}_m(w)\), set the directed edge \(w\to w'\).  \end{definition}

\begin{theorem}[Zeckendorf truncated folding time factor: unique branch point and unique convergence point]\label{thm:terminal-succ-unique-branch-merge}For each \(m\ge 2\), define two interleaved extremum words  \[
  u_m:=(u_{m,1},\dots,u_{m,m}),\qquad
  u_{m,k}:=
  \begin{cases}
1,& k+m\ \text{even},\\0,& \text{otherwise},  \end{cases}
  \]
  \[
  b_m:=(b_{m,1},\dots,b_{m,m}),\qquad
  b_{m,k}:=
  \begin{cases}
1,& k+m\ \text{odd},\\0,& \text{otherwise}.  \end{cases}
  \]
Then \(u_m,b_m\in X_m\), and the last digit of \(u_m\) is \(1\), and the last digit of \(b_m\) is \(0\), and satisfies  \(V_m(u_m)=F_{m+2}-1\)、\(V_m(b_m)=F_{m+1}-1\)。
There is the following uniqueness on the directed graph \(G^{\mathrm{Succ}}_m\) that defines \ref{def:terminal-succ-m}:  \begin{enumerate}
\item If \(w\neq b_m\), then \(|\mathrm{Succ}_m(w)|=1\).\item \(\mathrm{Succ}_m(b_m)=\{0^m,0^{m-1}1\}\). Therefore, \(G^{\mathrm{Succ}}_m\) has exactly one branch vertex with out-degree \(2\), namely \(b_m\).The only convergence vertex of \item with in-degree \(2\) is \(0^m\), and its predecessors are exactly \(u_m\) and \(b_m\).  \end{enumerate}
\emph{Minimum proof structure:} is parameterized by tail defining \ref{def:terminal-fold-zeck-extension}, whose preimage is given by \(N=V_m(w)+\) (legal tail combination) when fixingIfIf \(w_m=0\) andWhen \(w=b_m\), \(V_m(b_m)=F_{m+1}-1\).If tail takes the branch of \(c_{m+1}=0\), then the low \(m\) bit is normalized by adding one to obtain \(0^{m-1}1\);If tail allows \(c_{m+1}=1\) (the boundary constraint does not prevent it at this time, because the last bit of \(b_m\) is \(0\)), then\(N=(F_{m+1}-1)+F_{m+2}+\cdots\Rightarrow N+1=F_{m+3}+\cdots\), so the first \(m\) bits are all zeros, which is \(\mathrm{Fold}^{\mathrm{zeck}}_m(N+1)=0^m\).Finally, adding one to the canonical representation of \(u_m\) obtained from The out-degree of the remaining vertices is \(1\) excluding the second incoming edge, so the convergence uniqueness is established.  \end{theorem}
  \input{sections/generated/tab_fold_zeck_succ_unique_branch_audit_m2_m10}

\begin{remark}[window-\(6\) specialization: unique branch point \texttt{101010}]\label{rem:terminal-succ-m6-specialization}Taking \(m=6\), the only branch point is \(b_6=\texttt{101010}\), and\(\mathrm{Succ}_6(b_6)=\{\texttt{000000},\texttt{000001}\}\) (table \ref{tab:fold_zeck_succ_unique_branch_audit_m2_m10}, row \(m=6\)).The two branches respectively correspond to the two legal continuations of tail boundary bit \(c_7\) (weight \(F_8=21\)) taking \(0/1\): \(c_7=0\) gives \(0^{5}1\), while \(c_7=1\) triggers the reset branch and gives \(0^6\).  \end{remark}

\begin{definition}[reset event collection]\label{def:terminal-reset-events}For each \(m\ge 2\), define a set of reset events of depth  \[
  \mathcal R_m
  :=\Bigl\{
  N\in\NN:\ \mathrm{Fold}^{\mathrm{zeck}}_m(N)=b_m,\ \mathrm{Fold}^{\mathrm{zeck}}_m(N+1)=0^m
  \Bigr\}.
  \]
Remember its incrementing enumeration as \(\mathcal R_m=\{r_1<r_2<\cdots\}\).  \end{definition}

\begin{theorem}[Sturmian of reset event--golden rotation structure: return time binary spectrum and strong consistent error bound]\label{thm:terminal-reset-events-sturmian}For any \(m\ge 2\), let \(\mathcal R_m=\{r_n\}_{n\ge 1}\) be defined as \ref{def:terminal-reset-events}.Then the following properties hold simultaneously:  \begin{enumerate}
\item \textbf{Return time binary spectrum:} For all \(n\ge 1\), the difference only takes two values    \[
    r_{n+1}-r_n\in\{F_{m+3},F_{m+4}\}.
    \]
\item \textbf{Fibonacci word order:} Let the letters \(A:=F_{m+4}\),\item \textbf{density and strong consistent error bound:}The natural density of \(\mathcal R_m\) exists and is    \[
    \lim_{N\to\infty}\frac{\#(\mathcal R_m\cap[0,N))}{N}=\varphi^{-(m+2)},
    \]
And the counting function satisfies uniformly bounded deviation    \[
    \Bigl|\#(\mathcal R_m\cap[0,N))-\varphi^{-(m+2)}N\Bigr|\le 1\qquad(\forall N\in\NN).
    \]
  \end{enumerate}
The proof is based on properties such as Ostrowski/Zeckendorf representation and Sturmian return word structure (two return lengths and balance), see \cite{MorseHedlund1940,LindMarcus1995,Slater1950DistributionIntegers,Slater1967GapsSteps} for details.  \end{theorem}
  \input{sections/generated/tab_fold_zeck_reset_event_audit_m2_m10}
  \input{sections/generated/tab_fold_zeck_reset_event_gaps_m6}

\begin{remark}[A testable answer to "Is time essentially a golden plane rotation, "]\label{rem:terminal-time-golden-rotation-operational}In addition to the ontology description (theorem \ref{thm:terminal-time-protocol-golden-branch}) of "time = iteration count" at the protocol layer, if the reset event defining \ref{def:terminal-reset-events} is used as the event clock scale, then its indication sequence is given by theorem \ref{thm:terminal-reset-events-sturmian} as a Sturmian process of slope \(\varphi^{-1}\), which is equivalent to the symbolic encoding of golden angle rotation.Therefore, the strict meaning of "time = golden slice rotation" in the framework of this article is: time advancement under limited window observation has an equivalent interface implemented with a golden rotation return time structure, rather than identifying the tick itself as a rotation geometry.  \end{remark}

\item \textbf{6D hypercube microstates with window-\(6\) folding: irreducible compression from \(\Omega_6=\{0,1\}^6\) to stable type \(X_6\). }
\begin{theorem} [dyadic folding of window-\(6\): \texorpdfstring{XPH0X}{64->21} with rigidity degradation histogram]  \label{thm:terminal-foldbin6-64-to-21-hist}
At the minimum resolution \(m=6\), the window microstate space is \(\Omega_6=\{0,1\}^6\) (\(|\Omega_6|=64\)), and the stable syntax space is \(X_6\subseteq\Omega_6\) (adjacent \(11\) is prohibited, so \(|X_6|=F_8=21\), see \SXPH6X).The fiber size for binary interval folding \(\mathrm{Fold}^{\mathrm{bin}}_6:\{0,\dots,63\}\twoheadrightarrow X_6\) (defining \ref{def:fold-bin}) takes only\(\{2,3,4\}\), and the degradation histogram is  \[
  \#\{d^{\mathrm{bin}}_6=2\}=8,\qquad
  \#\{d^{\mathrm{bin}}_6=3\}=4,\qquad
  \#\{d^{\mathrm{bin}}_6=4\}=9
  \]
(Corollary \ref{cor:fold-bin-m6-fiber-hist}; and see Table \ref{tab:fold_bin_gauge_m6_invariants}).  \end{theorem}

  \input{subsubsec__window6-boson12-double-lock}

\begin{theorem}[window-\(6\)'s information theory constants: \texorpdfstring{XPH0X}{H2(W)} and single-step "lost information" closed form]\label{thm:terminal-foldbin6-information-loss-constants}Let \(U\sim\mathrm{Unif}(\{0,\dots,63\})\), and let \(W:=\mathrm{Fold}^{\mathrm{bin}}_6(U)\in X_6\).Then the distribution of \(W\) is given by fiber multiplicity:  \[
  \mathbb{P}(W=w)=\frac{d^{\mathrm{bin}}_6(w)}{64},\qquad d^{\mathrm{bin}}_6(w):=\bigl|(\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w)\bigr|.
  \]
Note that \(H_2(W):=-\sum_{w\in X_6}\mathbb{P}(W=w)\log_2\mathbb{P}(W=w)\) is the Shannon entropy in terms of \(\log_2\), then there is a strict identity under the uniform microstate baseline  \[
  H_2(W)=6-\mathbb{E}\bigl[\log_2 d^{\mathrm{bin}}_6(W)\bigr],
  \qquad
  I_{\mathrm{lost}}:=H_2(U)-H_2(W)=\mathbb{E}\bigl[\log_2 d^{\mathrm{bin}}_6(W)\bigr].
  \]
At \(m=6\), the above formula can be converted into a closed formula from the degradation degree histogram \((2{:}8,3{:}4,4{:}9)\) (table  \[
  \mathbb{E}\bigl[\log_2 d^{\mathrm{bin}}_6(W)\bigr]
  =\frac{11}{8}+\frac{3}{16}\log_2 3
  \approx 1.67218\ \text{bits},
  \qquad
  H_2(W)=\frac{37}{8}-\frac{3}{16}\log_2 3\approx 4.32782\ \text{bits}.
  \]
Furthermore, the deviation of the output distribution from the uniform distribution on \(X_6\) can be given by  \(
  D^{(2)}_{\mathrm{KL}}(P_W\Vert \mathrm{Unif}(X_6))=\log_2|X_6|-H_2(W)
  \)
is given, which on window-\(6\) is an audit constant determined entirely by the fiber table.\emph {Verification Points:} The script \texttt{scripts/exp\_foldbin6\_information\_loss\_constants.py} directly enumerates \(N\in\{0,\dots,63\}\) and outputs the certificate fragment \eqref{eq:foldbin6_information_loss_constants} and JSON.  \end{theorem}
  \input{sections/generated/eq_foldbin6_information_loss_constants}

\begin{proposition}[6D reachable mean convex body and differentiable fingerprint (pressure gradient/Hessian)]  \label{prop:terminal-6d-reachable-mean-convex-body}
Under the potential vector caliber of \SXPH2X, when \(6\) binary channels are selected as potential components, an orbit with a length of \(n\) generates a counting vector\(N_n\in\{0,\dots,n\}^6\), which falls to \([0,1]^6\) after normalization.The corresponding set of reachable means \(\mathcal{R}\subset[0,1]^6\) is a compact convex body; its \(3\)-dimensional slices (fixed three coordinates or three linear combinations) constitute a "3D slice of a 6D cube".More importantly, the gradient of the pressure function \(P(\theta)=\log\rho(M_\theta)\) and the readout of the Hessian on the slice give a local first/second order fingerprint of the convex body and endogenize the geometry into an auditable object (\SXPH1X) in the form of a convex duality.  \end{proposition}

\begin{theorem}[cubic edge separation of bin-fold: one-bit detectability of Hamming radius \(1\)]\label{thm:terminal-foldbin6-cube-edge-separation}Let \(\Omega_6=\{0,1\}^6\) be an \(6\)-dimensional hypercube vertex set, and let \(Q_6\) be its graph structure with Hamming distance \(1\).Define binary number  \[
  \mathrm{int}_6(\omega_1\cdots\omega_6):=\sum_{k=1}^6 \omega_k\,2^{6-k}\in\{0,\dots,63\},
  \]
And limit the binary interval folding to the vertex set to get the label function  \[
  F_6:\Omega_6\to X_6,\qquad F_6(\omega):=\mathrm{Fold}^{\mathrm{bin}}_6\bigl(\mathrm{int}_6(\omega)\bigr).
  \]
Then \(F_6\) is strictly separated on each cube edge: for any \(u,v\in\Omega_6\), if \(\mathrm{Ham}(u,v)=1\), then  \[
  F_6(u)\neq F_6(v).
  \]
Equivalently, for any \(x\in X_6\), fibers \(F_6^{-1}(x)\subset\Omega_6\) are independent sets in \(Q_6\).  \end{theorem}
\emph{Verification points:}If \(\mathrm{Ham}(u,v)=1\), then \(|\mathrm{int}_6(u)-\mathrm{int}_6(v)|\in\{1,2,4,8,16,32\}\);And if \(F_6(u)=F_6(v)\), then \(\mathrm{int}_6(u),\mathrm{int}_6(v)\) belongs to a certain \((\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(x)\), the difference must fall within\(\{F_8,F_9,F_{10},F_9-F_8\}=\{21,34,55,13\}\) (corollary \ref{cor:fold-bin-m6-fiber-hist}). The conclusion is reached if the two sets do not intersect.
\begin{corollary}[window-\(6\) coarse-grained minimum error detectability]\label{cor:terminal-foldbin6-onebit-error-detecting}Under the above notation, any single bit flip changes the stable type output:For any \(\omega\in\Omega_6\) and any \(\omega'\in\Omega_6\) satisfying \(\mathrm{Ham}(\omega,\omega')=1\), there must be  \(F_6(\omega)\neq F_6(\omega')\)。
  \end{corollary}

\begin{corollary}["non-resident" stable type sequence under local one-hop scan]\label{cor:terminal-foldbin6-no-dwell}For any path \(\omega_0\sim \omega_1\sim\omega_2\sim\cdots\) on \(Q_6\) (the Hamming distance of each step is \(1\)), its stable type readout \(w_t:=F_6(\omega_t)\) satisfies  \[
  w_{t+1}\neq w_t\qquad (t\ge 0).
  \]
  \end{corollary}

The cubic geometry stabilizer of \begin{corollary}[window-\(6\) is only \texorpdfstring{XPH0X}{Z2}]\label{cor:terminal-foldbin6-geo-stabilizer-z2}Within \(\mathrm{Aut}(Q_6)\cong (\ZZ_2)^6\rtimes S_6\), the geometric stabilizer that holds \(F_6\circ\sigma=F_6\) is the group of order \(2\):  \[
  \bigl\{\sigma\in\mathrm{Aut}(Q_6):\ F_6\circ\sigma=F_6\bigr\}=\{\mathrm{id},\sigma_{\mathrm{geo}}\}\cong \ZZ_2,
  \]
The optional generator is the involution  \[
  \sigma_{\mathrm{geo}}(\omega_1,\omega_2,\omega_3,\omega_4,\omega_5,\omega_6)=\bigl(1-\omega_5,\ \omega_2,\ \omega_3,\ \omega_4,\ 1-\omega_1,\ \omega_6\bigr),
  \]
And the \(\pm 34\) mask fingerprint carried on the binary number is shown in the corollary \ref{cor:foldbin6-geo-mask-34}; for detailed exhaustive auditing and certificate output, see the theorem \ref{thm:foldbin6-geo-stabilizer-z2}.  \end{corollary}

\begin{corollary}[Geometric realizability judgment: window-\(6\)’s uplift \texorpdfstring{XPH0X}{delta}-branch only allows \texorpdfstring{XPH1X}{δ=34}]\label{cor:terminal-window6-local-uplift-admissibility}If the "uplift branch" of window-\(6\) is understood as: the integer displacement achieved by a certain \(\sigma\in\mathrm{Aut}(Q_6)\) on the microscopic number \(\mathrm{int}_6\) while keeping the stable type label \(F_6\) unchanged,Then the only possible displacement besides the trivial displacement \(0\) is \(\pm 34\).Therefore, among the three branches \(\{F_8,F_9,F_{10}\}=\{21,34,55\}\) of the dyadic uplift tail offset, only \(F_9=34\) is compatible with the requirement that the local geometry is achievable and the label is invariant.  \end{corollary}
  \begin{proof}
From the inference of \ref{cor:terminal-foldbin6-geo-stabilizer-z2}, the only \(\sigma\) that satisfy \(F_6\circ\sigma=F_6\) are \(\mathrm{id}\) and \(\sigma_{\mathrm{geo}}\).The latter only changes the two weights to \(2^5\) and \(2^1\), so  \(
  \mathrm{int}_6(\sigma_{\mathrm{geo}}(\omega))-\mathrm{int}_6(\omega)\in\{0,\pm(2^5+2^1)\}=\{0,\pm 34\}
  \)。
On the other hand, the set of dyadic uplift tail offsets of window-\(6\) is included in \(\{F_8,F_9,F_{10}\}=\{21,34,55\}\) (Theorem \ref{thm:terminal-window6-tail-three-branch}), so that non-zero local displacements can only be \(34\). Certification completed.  \end{proof}

\begin{remark}[Separation of geometric symmetry and fold-gauge (window-\(6\) minimal model)]\label{rem:terminal-foldbin6-geo-vs-gauge}Theorem \ref{thm:terminal-foldbin6-cube-edge-separation} and corollary \ref{cor:terminal-foldbin6-geo-stabilizer-z2} show that: under the geometric automorphism that maintains the adjacency ofAs a direct consequence, \(X_6\)'s discrete chunking dictionaries on window-\(6\) (e.g. the rigid four-chunk decomposition of table \ref{tab:window6_patisalam_9633_partition}) cannot be interpreted as label contingencies of \(\mathrm{Aut}(Q_6)\) but must fall back on the arithmetic constraints of the folding/normalization discipline itself.  \end{remark}

\begin{theorem}[Ternary law of the minimum Hamming distance inside the fiber: \texorpdfstring{XPH0X}{\{2,3,5\}}]\label{thm:terminal-foldbin6-fiber-hamming-three-valued}For any \(w\in X_6\), define the minimum Hamming distance within its fiber  \[
  \delta_{\min}(w):=\min\bigl\{\mathrm{Ham}(\omega,\omega'):\ \omega\neq \omega',\ \omega,\omega'\in F_6^{-1}(w)\bigr\}.
  \]
Then for all \(w\in X_6\) we have  \[
  \delta_{\min}(w)\in\{2,3,5\},
  \]
And \(\delta_{\min}(w)=5\) is if and only ifThe three-valued law and word-by-word enumeration classification are shown in Table \ref{tab:foldbin6_fiber_hamming_min_distance} (the JSON audit certificate is generated and output by the script \texttt{scripts/exp\_foldbin6\_fiber\_hamming\_min\_distance.py}).  \end{theorem}
  \input{sections/generated/tab_foldbin6_fiber_hamming_min_distance}

\begin{corollary}[lower bound of the shortest recurrence delay of stable type]\label{cor:terminal-foldbin6-min-return-delay}Let \(\omega_0\sim\omega_1\sim\cdots\) be any path on \(Q_6\), and let \(w_t:=F_6(\omega_t)\).If there are \(t_1<t_2\), \(w_{t_1}=w_{t_2}=w\) and \(\omega_{t_1}\neq \omega_{t_2}\) for a certain \(w\in X_6\), then there must be  \[
  t_2-t_1\ \ge\ \mathrm{Ham}(\omega_{t_1},\omega_{t_2})\ \ge\ \delta_{\min}(w).
  \]
Therefore, for the two types of \(\delta_{\min}(w)=5\) and \(w\), there are strict minimum recurrence delay constraints in any local one-hop scan.  \end{corollary}

\begin{proposition}[Type adjacency graph rigid elimination "relabeled degrees of freedom": \texorpdfstring{XPH0X}{Aut(Gamma6)=1}]\label{prop:terminal-gamma6-rigidity}Define a simple graph \(\Gamma_6\) on \(X_6\): for \(u\neq v\in X_6\), let \(u\sim_{\Gamma_6} v\) if and only if there is an edge \(\omega\sim\omega'\) of \(Q_6\) such that\(F_6(\omega)=u\) and \(F_6(\omega')=v\).Then \(\Gamma_6\) is connected, and its automorphism group is trivial:  \[
  \mathrm{Aut}(\Gamma_6)=\{1\}.
  \]
This assertion is a strictly auditable judgment on a limited object. The audit output is shown in the generated fragment \texttt{eq\_window6\_type\_adjacency\_graph\_rigidity} and script \texttt{scripts/exp\_window6\_type\_adjacency\_graph\_rigidity.py} (and outputs a JSON certificate).  \end{proposition}
  \input{sections/generated/eq_window6_type_adjacency_graph_rigidity}

\begin{corollary}[Rigidity of type adjacency multiple structures (weighted version)]\label{cor:terminal-gamma6-multiplicity-rigidity}Defines for \(u\neq v\in X_6\) the adjacency multiplicity induced by \(Q_6\) edges  \[
  A_{uv}
  :=\#\bigl\{\{\omega,\omega'\}\in E(Q_6):\ F_6(\omega)=u,\ F_6(\omega')=v\bigr\},
  \qquad A_{uu}:=0,
  \]
This results in a symmetric integer matrix \(A=(A_{uv})\) (equivalent to a weighted undirected graph on \(X_6\)).If \(\pi\in\mathrm{Sym}(X_6)\) satisfies \(A_{\pi(u)\pi(v)}=A_{uv}\) and holds for all \(u,v\in X_6\), then \(\pi=\mathrm{id}\).  \begin{proof}
If \(A_{\pi(u)\pi(v)}=A_{uv}\), then \(A_{uv}>0\iff A_{\pi(u)\pi(v)}>0\), thus \(\pi\) maintains the adjacency relationship of the simple graph \(\Gamma_6\); from the proposition  \end{proof}
  \end{corollary}

\begin{corollary}[Geometric automorphism does not induce type renumbering]\label{cor:terminal-foldbin6-no-type-relabeling-by-geo}Let \(\sigma\in \mathrm{Aut}(Q_6)\) exist and \(\pi\in\mathrm{Sym}(X_6)\) exist so  \(
  F_6\circ\sigma=\pi\circ F_6
  \)。
then there must be \(\pi=\mathrm{id}\), therefore \(\sigma\in \mathrm{Stab}_{\mathrm{geo}}(F_6)\).  \begin{proof}
From the edge preservation of \(\sigma\) and \(F_6\circ\sigma=\pi\circ F_6\), we can know that \(\pi\) maintains the adjacency relationship of \(\Gamma_6\); from the proposition \ref{prop:terminal-gamma6-rigidity}, we can get \(\pi=\mathrm{id}\).  \end{proof}
  \end{corollary}

  \input{sections/body/group_unification/cor__window6-type-superselection-autgamma6}

\begin{theorem}[\(\mathrm{Fold}^{\mathrm{bin}}_6\) pushed stable type Markov kernel: reversibility and stationary measure]\label{thm:terminal-foldbin6-pushforward-markov}Let \(Q_6\) be an \(6\) dimensional hypercube graph, the vertex set be \(\Omega_6=\{0,1\}^6\), and let the label function  \(
  F_6(\omega):=\mathrm{Fold}^{\mathrm{bin}}_6(\mathrm{int}_6(\omega))\in X_6
  \)
Such as theorem \ref{thm:terminal-foldbin6-cube-edge-separation}.Define push adjacency count (directed edge count) for \(w,w'\in X_6\)  \[
  A_6(w,w')
  :=\#\bigl\{(\omega,\omega'):\ \omega\sim\omega'\text{ in }Q_6,\ F_6(\omega)=w,\ F_6(\omega')=w'\bigr\},
  \]
And define the Markov kernel on \(X_6\) accordingly  \[
  P_6(w,w'):=\frac{A_6(w,w')}{6\,d^{\mathrm{bin}}_6(w)},
  \qquad d^{\mathrm{bin}}_6(w):=\bigl|(\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w)\bigr|.
  \]
Then \(P_6\) is a random kernel on \(X_6\); and \((X_6,P_6)\) is invertible, and its stationary distribution is  \[
  \pi_6(w)=\frac{d^{\mathrm{bin}}_6(w)}{64}.
  \]
More strongly, there are detailed equilibrium identities for any \(w,w'\in X_6\)  \(
  \pi_6(w)P_6(w,w')=\pi_6(w')P_6(w',w)
  \)。
  \end{theorem}
  \begin{proof}
By definition \(\sum_{w'}A_6(w,w')=6\,d^{\mathrm{bin}}_6(w)\), the row sum of \(P_6\) is \(1\).On the other hand, the undirected edges of \(Q_6\) appear the same number of times in \(A_6(w,w')\) and \(A_6(w',w)\), so \(A_6(w,w')=A_6(w',w)\).then  \[
  \pi_6(w)P_6(w,w')=\frac{d^{\mathrm{bin}}_6(w)}{64}\cdot\frac{A_6(w,w')}{6\,d^{\mathrm{bin}}_6(w)}
  =\frac{A_6(w,w')}{64\cdot 6}
  =\pi_6(w')P_6(w',w),
  \]
Detailed balance and reversibility are obtained; stability is achieved by \(\sum_w \pi_6(w)P_6(w,w')=\pi_6(w')\). Certification completed.  \end{proof}
  \input{sections/generated/eq_window6_pushforward_markov_kernel}

  \input{subsubsec__window6-cube-fold6-additional-results}

\begin{theorem}[Affine geometric classification of fibers in \texorpdfstring{XPH0X}{F2^6}: only \texorpdfstring{XPH1X}{11/21} is affine flat]\label{thm:terminal-foldbin6-fiber-affine-geometry}Treat \(\Omega_6=\{0,1\}^6\) as the affine space \(\mathbb{F}_2^6\) (addition is bitwise XOR), and write the fibers as  \(F_w:=F_6^{-1}(w)\subset \mathbb{F}_2^6\)（\(w\in X_6\)）。
Then the \(21\) fibers of window-\(6\) satisfy the following precise classification:  \begin{enumerate}
\item Exactly \(8\) fibers are affine \(1\)-flat (size \(2\)).\item has exactly \(3\) fibers that are affine \(2\)-flat (size \(4\)), and the corresponding stable type is    \[
    \{\texttt{000000},\ \texttt{000010},\ \texttt{101000}\}.
    \]
\item The remaining \(10\) fibers are not affine subspaces: the \(4\) fibers are a set of "affine plane missing points" of size \(3\) (affine envelope dimension \(2\)), and the other \(6\) fibers are nonlinear subsets of size \(4\) but affine envelope dimension \(3\).  \end{enumerate}
\emph{Verification points:} The script \texttt{scripts/exp\_foldbin6\_fiber\_affine\_geometry.py} enumerates all \(w\in X_6\), calculates \(|F_w|\), \(\dim_{\mathrm{aff}}(F_w)\) and whether it is an affine subspace for each fiber, and generates the table \ref{tab:foldbin6_fiber_affine_geometry} and JSON certificate.  \end{theorem}
  \input{sections/generated/tab_foldbin6_fiber_affine_geometry}

\begin{corollary} [Linear kernel barrier: window-\(6\)'s "canonical redundancy" cannot be explained by the Abelian linear quotient] \label{cor:terminal-foldbin6-no-linear-kernel}There is no linear mapping \(L:\mathbb{F}_2^6\to \mathbb{F}_2^k\) such that the fiber partitioning of \(\mathrm{Fold}^{\mathrm{bin}}_6\) is exactly equal to the coset partitioning of \(L\).  \begin{proof}
If the fibers are cosets of  \end{proof}
  \end{corollary}

\begin{theorem}[edge--flux skeleton of window-\(6\): four interaction matrices and spectral fingerprints]\label{thm:terminal-window6-edge-flux-skeleton}Let \(Q_6\) be the \(6\) dimensional hypercube map and \(F_6:=\mathrm{Fold}^{\mathrm{bin}}_6:\Omega_6\to X_6\) be the dyadic fold label function of window-\(6\).Rigid four-piece decomposition using table \ref{tab:window6_patisalam_9633_partition}  \(
  X_6=X^{(2)}_6\sqcup X^{(1)}_6\sqcup X^{(L)}_6\sqcup X^{(R)}_6
\), and let the corresponding micro-vertex union  \[
  U_\alpha:=\bigcup_{w\in X^{(\alpha)}_6}F_6^{-1}(w)\subset \Omega_6
  \qquad (\alpha\in\{2,1,L,R\}).
  \]
Define the undirected edge count between four blocks (symmetric matrix)  \[
  E_{\alpha\beta}
  :=\#\bigl\{\{\omega,\omega'\}\in E(Q_6):\ \omega\in U_\alpha,\ \omega'\in U_\beta\bigr\},
  \qquad (\alpha,\beta\in\{2,1,L,R\}),
  \]
And define the coarse-grained Markov kernel with a uniform prior within each block (uniformly distributed on \(U_\alpha\))  \[
  T_{\alpha\beta}=
  \begin{cases}
    \dfrac{2E_{\alpha\alpha}}{6|U_\alpha|}, & \alpha=\beta,\\[6pt]
    \dfrac{E_{\alpha\beta}}{6|U_\alpha|}, & \alpha\neq\beta.
  \end{cases}
  \]
Then the four microscopic volumes \(|U_\alpha|\), edge counting matrix \(E\), kernel \(T\) and their characteristic polynomial decomposition and boundary sector cut/flux constant  \(\Phi_R=\frac{|\partial U_R|}{64\cdot 6}=\frac{1}{12}\)
Uniquely determined by a reviewable finite exhaustion and given by the generated fragment \eqref{eq:window6_edge_flux_skeleton} (including \(\mathbb{P}(\text{leave }U_R\mid U_R)=\frac{8}{9}\)).In particular, \(T\) is invertible and stationary with \(\pi_\alpha=|U_\alpha|/64\) (derived from the uniform equilibrium and finite-count detailed equilibrium identities of \(Q_6\) simple random walks).  \end{theorem}
  \input{sections/generated/eq_window6_edge_flux_skeleton}
  \input{subsubsec__window6-edge-flux-arithmetic}

\item \textbf{window-\(6\) uplift's three-item tail offset }
\begin{theorem}[discrete three-branch window-\(6\) uplift aligned with Zeckendorf--GUT top]  \label{thm:terminal-window6-tail-three-branch}
For any \(w\in X_6\), the non-zero offset of its dyadic uplift preimage relative to the Zeckendorf value \(V_6(w)\) can only be taken from  \[
  \Delta_{\mathrm{tail}}(6)\subseteq\{F_8,F_9,F_{10}\}=\{21,34,55\}
  \]
(Corollary \ref{cor:fold-bin-m6-fiber-hist}; Corollary \ref{cor:fold6-tail-offsets-gut-top-terms}).On the other hand, the boundary lexicon satisfies \(|X_m^{\mathrm{bdry}}|=F_{m-2}\), thus inducing a unique resolution signature \(\Sigma(\mathfrak g)\) for the Zeckendorf decomposition of any candidate Lie algebra \(\mathfrak g\) (\(D=\sum_{m\in\Sigma(\mathfrak g)}|X_m^{\mathrm{bdry}}|\) (\SXPH1X; table \ref{tab:zeckendorf_gut_signatures}).Under this dictionary, the highest Fibonacci terms of the Zeckendorf--GUT decomposition of \((SU(5),SO(10),E_6)\) are respectively \((F_8,F_9,F_{10})\), thus strictly aligning with the three tail offsets of the window-\(6\) uplift, and deriving a discrete three-branch selection channel at the protocol layer.  \end{theorem}

\begin{theorem}[window-\(6\) Dynamic implementation of unified triplet \texorpdfstring{XPH0X}{\{21,34,55\}}]\label{thm:terminal-window6-unification-triple-dynamic}At the minimal non-trivial anchor point of \(m=6\), the triple  \[
  (F_8,F_9,F_{10})=(21,34,55)
  \]
At the same time, three independent interfaces appear:  \begin{enumerate}
\item\item\item  \end{enumerate}
Therefore, \(\{21,34,55\}\) of window-\(6\) is not a number table matched after the fact, but a unified triple intersection forced endogenously by the Zeckendorf truncation folding and time advancement mechanism.  \end{theorem}

\item \textbf{Branch lock for family number: }
\begin{theorem}[Family number-locked three-branch selection law]  \label{thm:terminal-family-uplift-lock}
Taking \(SU(5)\) type chiral substance standard count \(15\) of each generation as external input, let the total count \(M(N_f)=15N_f\).Then for \(N_f\in\{2,3,4\}\) and \(M(N_f)\), the highest terms of the Zeckendorf decomposition are \(F_8,F_9,F_{10}\);The family number is thus promoted to a discrete branch selection quantity at the window-\(6\) level, and \(N_f=3\) selects exactly the \(F_9=34\) branch and aligns with the \(SO(10)\) top-level entry (Table \ref{tab:window6_family_uplift_lock}).  \end{theorem}

\item \textbf{window-\(6\) internal \texorpdfstring{XPH0X}{1+8+12} discrete splitting and \texorpdfstring{XPH1X}{δ=34} sheet parity selection law. }
\begin{proposition}[\texorpdfstring{XPH0X}{1+8+12} splitting and enumerability of light/heavy dictionaries]  \label{prop:terminal-window6-1-8-12-split}
Under the dyadic folding caliber, the uplift-choice threshold leads to a three-valued collapse on \(\Xi(w):=|(\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w)|\in\{2,3,4\}\) and thereby induces a fully enumerable three-sector decomposition on \(X_6\)  \[
  X_6=X_6^{\mathrm{zero}}\ \oplus\ X_6^{\mathrm{light}}\ \oplus\ X_6^{\mathrm{heavy}},
  \qquad
  |X_6^{\mathrm{zero}}|=1,\ |X_6^{\mathrm{light}}|=8,\ |X_6^{\mathrm{heavy}}|=12,
  \]
Its complete vocabulary and threshold conditions are given in table \ref{tab:fold6_bin_uplift_choice_collapse}.This discrete dictionary can be canonically lifted back to the boundary layer of \(m=10\) via a four-step translational uplift of the isomorphism  \end{proposition}

\begin{corollary}[\texorpdfstring{XPH0X}{δ=34}'s two-layer coverage and discrete parity selection rules]  \label{cor:terminal-delta-parity-rule}
The boundary sector \(X_6^{\mathrm{bdry}}\) of window-\(6\) presents a standard two-layer coverage under dyadic uplift: each boundary word has exactly two pre-images, and the difference between the two is strictly \(34=F_9\), thus inducing an endogenous \(\ZZ_2\) sheet parity classification (Table \ref{tab:fold6_boundary_sheet_pairs}).Furthermore, boundary sheet-flip induces a gauge-centered embedding under the direct integral decomposition of the fold-gauge group \(\mathcal{G}^{\mathrm{bin}}_6=\mathrm{Aut}(\mathrm{Fold}^{\mathrm{bin}}_6)\)\((\ZZ_2)^3\hookrightarrow Z(\mathcal{G}^{\mathrm{bin}}_6)\), independently contributed by the \(S_2\) factors of the three boundary fibers (\(\texttt{100001},\texttt{100101},\texttt{101001}\)) (corollary to \ref{cor:fold-bin-boundary-center-z2}; see also the corresponding row of table \ref{tab:fold_bin_gauge_m6_fibers}).Under a protocol gate approach that requires observable coupling to be compatible with this hierarchy, the mixture of bulk and boundary terms must be implemented via sheet flipping of \(\delta=34\), giving a discrete selection law that does not require continuous fine-tuning (see the "\(\delta\)-parity" discussion paragraph in \SXPH2X).  \end{corollary}

\item }
\begin{theorem}[Maximum fiber, combination upper bound and saturation discrimination (bin-fold)]  \label{thm:terminal-foldbin-saturation-diophantine}
For any \(m\ge 1\), the maximum fiber of the binary interval fold \(\mathrm{Fold}^{\mathrm{bin}}_m\) is obtained by the zero word \(0^m\):  \[
  d^{\mathrm{bin}}_{\max}(m):=\max_{w\in X_m}d^{\mathrm{bin}}_m(w)=d^{\mathrm{bin}}_m(0^m).
  \]
Let \(K(m)\) satisfy  \[
  F_{K(m)+1}\le 2^m-1 < F_{K(m)+2}
  \]
Unique integer (defining \ref{def:K-of-m}). then there is a combinatorial upper bound  \[
  d^{\mathrm{bin}}_{\max}(m)\le F_{K(m)-m+2},
  \]
And the upper bound is saturated if and only if the threshold constraints are redundant for all legal tails, i.e.  \[
  d^{\mathrm{bin}}_{\max}(m)=F_{K(m)-m+2}
  \quad\Longleftrightarrow\quad
  2^m-1\ge S_{\max}(m),
  \]
in  \(
  S_{\max}(m)
  :=\max\{\sum_{k=m+1}^{K(m)}c_kF_{k+1}:\ c_k\in\{0,1\},\ c_kc_{k+1}=0\}
  \)
Has a fully explicit closed form (obtained from the staggered tail \(1010\cdots\)):  \[
  S_{\max}(m)=
  \begin{cases}
F_{K(m)+2}-F_{m+1}, & K(m)-m\ \text{is an odd number},\\F_{K(m)+2}-F_{m+2}, & K(m)-m\ \text{is an even number},  \end{cases}
  \]
See corollary \ref{cor:fold-bin-saturation}.  \end{theorem}

\begin{corollary}[Exponential logarithmic linear form constraints and "only finite-order saturation" mechanism]\label{cor:terminal-foldbin-finite-saturation}If saturation occurs at a certain \(m\), then \(2^m\) and \(F_{K(m)+2}\) are extremely close and satisfy exponentially small logarithmic linear form constraints  \[
  \Bigl|(K(m)+2)\log\varphi-m\log 2-\log\sqrt5\Bigr|
  \ \lesssim\ \Bigl(\frac{\varphi}{2}\Bigr)^m.
  \]
Therefore, combined with the Baker--W\"ustholz/Matveev type lower bound, it can be deduced that saturation can only occur a limited number of times (note \ref{rem:fold-bin-finite}; and see the reproducible audit output of table \ref{tab:fold_binary_saturation_points}).  \end{corollary}

\item }
\begin{proposition}[cut-and-project \texorpdfstring{XPH0X}{=>} interface for folded histogram invariants]  \label{prop:terminal-cut-project-to-fold-hist}
In the 6D icosahedral cut-and-project prototype (\SXPH9X), given a window set \(W\subset E_\perp\) (boundary measure is zero) and an arbitrary phase \(\theta\), its local patch can be given by the sliding window encoding of Count/measure objects on \(X_m\). In particular, at \(m=6\), \(X_6\) is determined only by the golden mean syntax, independent of geometric implementation and phase selection; therefore this compression at \(m=6\) can be understood as an implementation-independent finite-type factor interface that can be used as an auditable isomorphic invariant for cross-implementation comparisons (\SXPH10X vs. Appendix \ref{app:cut_project_background}).  \end{proposition}

\begin{corollary} [Auditable prototype of torus parameterization and rotational scanning]  \label{cor:terminal-rotation-window-prototype}
In the one-dimensional prototype, the readout of torus rotation  \end{corollary}

\item }  \end{enumerate}

\begin{theorem}[6D Microstate—Golden Time—Discrete Unification of GUT Branches]\label{thm:terminal-6d-microstate-golden-time-gut-branch}
At the minimum local resolutionAt the same time, the Zeckendorf--Fibonacci boundary tower signature uniquely decomposes the Lie algebra dimension \(D=\dim\mathfrak g\) into the sum of boundary layer counts and aligns the top term of \((SU(5),SO(10),E_6)\) exactly with \((F_8,F_9,F_{10})\) (\SXPH3X; table \ref{tab:zeckendorf_gut_signatures}).Furthermore, the family number \(N_f\) achieves branch locking on \(N_f\in\{2,3,4\}\) through the top-level Zeckendorf term of \(15N_f\), especially \(N_f=3\) discretely selects the \(F_9=34\) branch and aligns it with the top level of \(SO(10)\) (theorem \ref{thm:terminal-family-uplift-lock}).Therefore, at the minimum resolution anchor point of the protocol, "top selection of unified candidates" appears as a discrete selection problem jointly determined by \emph {endogenous time scale (\(\log\varphi\))} and \emph {dyadic uplift tail offset}, rather than a continuously adjustable family of parameters.\end{theorem}

\paragraph{Material pointer (auditable entry)}\begin{itemize}
\item \textbf{time = iteration count (object layer/protocol layer split) and\SXPH0X; \SXPH1X; Proposition \ref{prop:time-unit-logphi-protocol}.\item \textbf{Ostrowski--Zeckendorf consistency and bin-fold truncation interface of the golden branch:}\SXPH0X; definition \ref{def:fold-bin}; inference \ref{cor:terminal-ostrowski-zeckendorf-binfold}.\itemDefinition \ref{def:terminal-fold-zeck-extension}--\ref{def:terminal-reset-events}; Theorem \ref{thm:terminal-succ-unique-branch-merge}--\ref{thm:terminal-reset-events-sturmian};Table \ref{tab:fold_zeck_succ_unique_branch_audit_m2_m10}, Table \ref{tab:fold_zeck_reset_event_audit_m2_m10}, Table \ref{tab:fold_zeck_reset_event_gaps_m6};Script \texttt{scripts/exp\_fold\_zeck\_time\_factor\_succ\_reset\_audit.py} and certificate \texttt{artifacts/export/fold\_zeck\_time\_factor\_succ\_reset\_audit.json}.\itemNote \ref{cor:chi-slice-t0-golden-tau}; and event clock tables in \SXPH1X (e.g. generated fragments such as \texttt{tab\_real\_input\_40\_event\_time\_dilation}).\item \textbf{window-\(6\) dyadic folded \((2,3,4)\) degradation spectrum histogram with gauge invariant:}Corollary \ref{cor:fold-bin-m6-fiber-hist}; Table \ref{tab:fold_bin_gauge_m6_invariants}; Script \texttt{scripts/exp\_fold\_bin\_gauge\_m6\_invariants.py}.\item \textbf{Information theoretic constants of \(\mathrm{Fold}^{\mathrm{bin}}_6\) (\(H_2(W)\),Theorem \ref{thm:terminal-foldbin6-information-loss-constants};Certificate fragment \eqref{eq:foldbin6_information_loss_constants};Script \texttt{scripts/exp\_foldbin6\_information\_loss\_constants.py} and certificate \texttt{artifacts/export/foldbin6\_information\_loss\_constants.json}.\item \textbf{\(\mathbb{F}_2^6\) affine geometric classification of fibers (only \(11/21\) is affine flat):}Theorem \ref{thm:terminal-foldbin6-fiber-affine-geometry}, corollary \ref{cor:terminal-foldbin6-no-linear-kernel};Table \ref{tab:foldbin6_fiber_affine_geometry};Script \texttt{scripts/exp\_foldbin6\_fiber\_affine\_geometry.py} and certificate \texttt{artifacts/export/foldbin6\_fiber\_affine\_geometry.json}.\item \textbf{Edge separation (one bit detectable) and geometric stabilizer on 6D cube \(Q_6\):}Theorem \ref{thm:terminal-foldbin6-cube-edge-separation}; Theorem \ref{thm:foldbin6-geo-stabilizer-z2}, Corollary \ref{cor:foldbin6-geo-mask-34};and the exhaustive audit script \texttt{scripts/exp\_foldbin6\_geo\_stabilizer.py} and certificate \texttt{artifacts/export/foldbin6\_geo\_stabilizer.json}.\item \textbf{Ternary law of fiber internal geometric separation \(\delta_{\min}(w)\) \(\{2,3,5\}\):}Theorem \ref{thm:terminal-foldbin6-fiber-hamming-three-valued}, corollary \ref{cor:terminal-foldbin6-min-return-delay};Table \ref{tab:foldbin6_fiber_hamming_min_distance};Script \texttt{scripts/exp\_foldbin6\_fiber\_hamming\_min\_distance.py} and certificate \texttt{artifacts/export/foldbin6\_fiber\_hamming\_min\_distance.json}.\item \textbf{rigidity of type adjacency graph \(\Gamma_6\) (\(\mathrm{Aut}(\Gamma_6)=\{1\}\)):}Proposition \ref{prop:terminal-gamma6-rigidity};Generate fragment \texttt{eq\_window6\_type\_adjacency\_graph\_rigidity};Script \texttt{scripts/exp\_window6\_type\_adjacency\_graph\_rigidity.py} and certificate \texttt{artifacts/export/window6\_type\_adjacency\_graph\_rigidity.json}.\itemTheorem \ref{thm:terminal-foldbin6-pushforward-markov};Certificate fragment \eqref{eq:window6_pushforward_markov_kernel};Script \texttt{scripts/exp\_window6\_edge\_flux\_skeleton.py} and certificate \texttt{artifacts/export/window6\_pushforward\_markov\_kernel.json}.\item \textbf{edge--flux skeleton of window-\(6\) (four interaction matrices \(E\), coarse-grained kernelTheorem \ref{thm:terminal-window6-edge-flux-skeleton};Certificate fragment \eqref{eq:window6_edge_flux_skeleton};Script \texttt{scripts/exp\_window6\_edge\_flux\_skeleton.py} and certificate \texttt{artifacts/export/window6\_edge\_flux\_skeleton.json}.\item \textbf{Zeckendorf--GUT signature aligned with window-\(6\) three branches:}\SXPH0X; table \ref{tab:zeckendorf_gut_signatures}; script \texttt{scripts/exp\_boundary\_tower\_gut\_signatures.py}.\item \textbf{family number lock (\(15N_f\) top-level item) aligned with window-\(6\) uplift:}Table \ref{tab:window6_family_uplift_lock}; script \texttt{scripts/exp\_window6\_family\_uplift\_lock.py}.\item \textbf{\(\delta=34\) second layer coverage and sheet parity:}Table \ref{tab:fold6_boundary_sheet_pairs}; Table \ref{tab:fold6_bin_uplift_choice_collapse}.\item \textbf{Maximum fiber saturation discrimination and Diophantine rigidity:}Corollary \ref{cor:fold-bin-saturation} and note \ref{rem:fold-bin-finite} (Appendix \ref{app:fold-multiplicity}).\item\SXPH0X; appendix \ref{app:cut_project_background} (note \ref{rem:app-cutproject-phason-time}).\end{itemize}

