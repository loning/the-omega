t_{p,m}:=\frac{1}{2p}+\frac{(2m+1)\pi i}{p\log\lambda}
\]
是 \(\mathcal{P}(s)\) 在 \(\Re(s)>0\) 的解析延拓的一个对数型分支奇点。
\end{corollary}
\begin{proof}
由命题 \ref{prop:finite-rh-40}，\(\widehat{\zeta}(s)\) 在
\(
s_0=\frac12+\frac{(2m+1)\pi i}{\log\lambda}
\)
处有极点，且 \(\Re(s_0)=1/2>0\)。取 \(t_{p,m}=s_0/p\)，由命题 \ref{prop:primedirichlet-branch-inherit} 立得结论。证毕。
\end{proof}

\begin{remark}[自然边界机制的普适化]\label{rem:primedirichlet-natural-boundary-universal}
定理 \ref{thm:real-input-40-primedirichlet-dense-branch} 的证明只用到了两点结构事实：
\begin{enumerate}
  \item $\widehat{\zeta}(s)=\zeta_M(\lambda^{-s})$ 在 $s=1$ 的极点来自 Perron 根 $\lambda$，从而 $\log\widehat{\zeta}(s)$ 在该点具有对数型分支奇性；
  \item prime-zeta 形式的恒等式
  \[
  \mathcal{P}(s)=\sum_{k\ge 1}\frac{\mu(k)}{k}\log\widehat{\zeta}(ks)
  \]
  把该分支奇性按比例 $s\mapsto s/k$ 复制到 $s_{p,m}=1/p+2\pi i m/(p\log\lambda)$（奇素数 $p$）处，并由 $k/p$ 的实部比较排除不同 $k$ 的抵消。
\end{enumerate}
因此该“稠密分支奇点 \(\Rightarrow\) \(\Re(s)=0\) 自然边界”的现象并不依赖本核的具体因式分解；
只要 $M$ 为 primitive 非负整数矩阵（保证 Perron 根简单）并以同样方式定义 primitive--Dirichlet 级数，完全同型的稠密奇点机制都会出现。
\end{remark}

\paragraph{把 toy-RH 推进到“真实 RH 研究形态”的五件套（本稿已闭合的接口清单）}\label{par:finite-rh-rhstyle-toolkit}
为了避免把同一内容以多种叙事重复陈述，这里把本文已经得到的“RH 形状”接口按研究路线所需的层级重新打包为五个可审计部件（均为本稿中已给出的可引用命题/定理）：
\begin{enumerate}
  \item \textbf{谱 \(\leftrightarrow\) 临界线字典（几何对齐）}：
  由 Dirichlet 变量 \(z=\lambda^{-s}\) 得极点竖线实部恒等式 \(\Re(s)=\log|\mu|/\log\lambda\)，从而 \(|\mu|=\sqrt{\lambda}\iff \Re(s)=1/2\)（见第 \ref{sec:zeta-finite-part} 节的命题 \ref{prop:s-plane-spectrum-poles} 及其推论 \ref{cor:rh-stratification-splane}，并与本附录命题 \ref{prop:finite-rh-40} 的特例一致）。
  \item \textbf{平方根误差的刚性二分（达界 \(\Rightarrow\) \(\Omega\)-振荡）}：
  有限维显式公式模板（命题 \ref{prop:finite-dimensional-explicit-formula}）表明：一旦存在达界谱点 \(|\mu|=\sqrt{\lambda}\)，则一般出现不可改进的平方根级振荡；在真实输入核上该振荡由唯一达界特征值 \(-\sqrt{\lambda}\) 产生并被完全显式化（命题 \ref{prop:finite-rh-40} 与推论 \ref{cor:real-input-40-chebyshev-explicit}）。
  \item \textbf{从长度到“尺度” \(x\) 的素数定理形状（\(x/\log x\) 主项）}：
primitive 主项 \(p_n\sim \lambda^n/n\) 在末项主导下等价转写为 \(\pi(x)\sim \frac{\lambda\log\lambda}{\lambda-1}\frac{x}{\log x}\)（见注 \ref{rem:finite-rh-angle-entropy} 后的“primitive 轨道计数曲线与 \(x/\log x\)”段落）。
  \item \textbf{toy-GRH 的极限化（profinite/Haar 等分）}：
  对有限模轴束的 Chebotarev 估计在核版 GRH（平方根界）下可提升到 profinite 逆极限轴：对任意 cylinder 集合给出 Haar 权重的主项与平方根误差（定理 \ref{thm:pom-profinite-axis}）。
  \item \textbf{prime Dirichlet 级数的自然边界（“素数层直接级数”的不可延拓约束）}：
  primitive--Dirichlet 级数 \(\mathcal{P}(s)\) 在 \(\Re(s)=0\) 上出现稠密分支奇点，故虚轴为自然边界（定理 \ref{thm:real-input-40-primedirichlet-dense-branch}），并且该机制在 primitive 非负整数核的一般情形下普适（注 \ref{rem:primedirichlet-natural-boundary-universal}）。
\end{enumerate}
上述五件套给出一个更接近 RH 研究的“可累积路线图”：首先在谱侧通过 lift/扭曲增加临界角度多样性（注 \ref{rem:finite-rh-angle-entropy}），同时用 Chebotarev/profinite 轴把同余等分布提升到极限口径；并在 prime-zeta 的自然边界约束下，优先通过 \(\zeta\)（或其对数导数/显式公式）而非直接 primitive 级数来组织误差项与振荡项的可控叠加。

\paragraph{从有限核模板到 Ihara/Selberg/Hilbert--P{\'o}lya 路线（升级方案：谱 \(\Rightarrow\) 函数方程 + 临界线结构 + Ramanujan 界）}
本稿的“有限核 \(\zeta=\det(I-zT)^{-1}\)”原型与三条成熟谱路线在结构上可直接对齐；其差异仅在于“算子所处的范畴（有限矩阵 vs.\ 无限维核算子）”与“扭曲索引的算术来源（角色/覆盖 vs.\ Hecke/素数）”：
\begin{itemize}
  \item \textbf{Ihara/图 \(\zeta\)（Ramanujan \(\Leftrightarrow\) 图上 RH）}：
  对正则图，Ihara \(\zeta\) 的“临界圆/临界线”条件与邻接谱满足 Ramanujan 界是等价范式（见 \cite{TerrasZetaGraphs,StarkTerras1996GraphCoverings}）。这与本文的 kernel RH/GRH（“非平凡谱半径 \(\le \lambda^{1/2}\)”）属于同一母式：\(\zeta\) 的临界几何由谱半径界控制，而覆盖/角色扭曲把同余分解组织成 Artin 型因子（参见本稿对 Dirichlet 分层与扭曲谱的统一口径）。
  \item \textbf{Selberg/Ruelle（\(\zeta\) 的 Fredholm 行列式与谱解释）}：
  在双曲测地流与一般 Axiom A 系统中，Ruelle/Selberg 型 \(\zeta\) 可表述为某类转移算子族的（广义）Fredholm 行列式，其零点/极点与算子的谱数据（共振/本征值）对应（见 \cite{Ruelle1976ZetaExpanding,ParryPollicott1990Zeta,Fried1986RuelleSelberg}）。因此，若要让“临界线上的虚部结构”不再退化为有限晶格，最自然的升级是把本文有限核的 \(T\) 替换为一族可控的核算子 \(\mathcal{L}_u\)，并用 Fredholm 行列式 \(\det(I-z\mathcal{L}_u)\) 继承本文的 \(\zeta\)-接口。
  \item \textbf{Hilbert--P{\'o}lya/Connes（显式公式 \(\leftrightarrow\) trace formula 的谱化）}：
  RH 的一条谱解释路线是把显式公式写成 trace formula，并把临界零点视作某种“谱/共振”（见 \cite{Connes1999TraceFormulaNCG}）。从本文角度，这对应于把“trace/primitive 的交换图”（本稿已闭合）提升为一个带算术对称的算子范畴：扭曲通道的组织不再仅来自有限群角色，而由 Hecke/素数索引的算子族提供。
\end{itemize}
在这三条对齐之上，本文给出的“二变量自对偶—完成化”结构（附录 \ref{app:kernel-functional-equation} 的 \(s\mapsto 1-s\) 函数方程与 Hardy 实值化）可作为升级路线的硬约束：先保持自对偶/完成化以锁定函数方程与临界线几何，再把有限维谱半径界推广为无限维的 Ramanujan 型谱界（核版 GRH），并用 Artin/角色分解与 profinite 轴把同余等分布提升到极限口径。

\paragraph{与解析数论零密度技术的对齐（定位性备注）}
在 \(\zeta\) 理论中，“向 RH 推进”的一条可累积路线是改进 Dirichlet 多项式的大值控制，从而改进零密度界 \(N(\sigma,T)\) 并反馈到素数误差项的阈值。
例如 Guth--Maynard 给出新的大值估计并推出改进的零密度上界与短区间素数计数阈值（\(N(\sigma,T)\le T^{30(1-\sigma)/13+o(1)}\) 及相关推论，见 \cite{GuthMaynard2024LargeValues}）。

从“对齐口径”的角度，零密度技术的作用并不在于直接推出“全体零点都在临界线”，而在于把偏离临界线的零点集合压稀，从而把显式公式中的振荡误差项控制到更强的可积/可平均形式。
这对应于本文有限维原型中“最坏扭曲谱半径比 $\Lambda/\lambda_1$ 决定误差指数”的结构：当无法一步获得平方根界（核版 GRH），仍可通过控制“坏扭曲”的出现频率/规模来改进误差可控范围。

从本文的“有限维 \(\zeta\)”视角看，上述链条可被理解为一种连续谱版的“谱—极点—误差三角形”：零点（或极点）分布的稀疏性对应于显式公式中振荡项的可控叠加，而误差项的可达指数则由可用的大值界/矩母函数控制。有限维模型提供的优势在于：该三角形可被完全审计地压缩到有限谱数据（命题 \ref{prop:finite-rh-triangle-dict} 及推论 \ref{cor:finite-rh-explicit-formula}），从而为“把误差指数写成可检验谱不变量”提供了一个可计算原型。

\paragraph{真实 \texorpdfstring{$\zeta$}{zeta} 的可量化边界（用于对齐与路线设计）}
为了把“向 RH 推进”的外部进展接入本文的谱语言，这里列出若干在 2024--2025 仍可直接调用的硬输入（均为可核验的显式常数/阈值）：
\begin{itemize}
  \item \textbf{验证高度（partial RH）：}已严格验证到高度 \(3\cdot 10^{12}\)，即所有满足 \(0<\Im(\rho)\le 3\cdot 10^{12}\) 的非平凡零点都在临界线 \(\Re(\rho)=\tfrac12\) 上（\cite{PlattTrudgian2020RH3e12}）。
  \item \textbf{de Bruijn--Newman 常数：}Rodgers--Tao 证明 \(\Lambda\ge 0\)（\cite{RodgersTao2018NewmanNonnegative}），而 RH 等价于 \(\Lambda\le 0\)，因此 RH 等价于 \(\Lambda=0\)。同时 Polymath15 给出无条件上界 \(\Lambda\le 0.22\)，并给出依赖进一步数值验证的条件改进（\cite{Polymath2019NewmanUpper}）。
  \item \textbf{临界线上零点比例：}已有结果表明超过 \(5/12\) 的零点位于临界线（\cite{PrattRoblesZaharescuZeindler2018FiveTwelfths}）。该类“比例推进”可被视为把偏离临界线的零点集合压稀的一条可累积路线。
  \item \textbf{显式零点计数误差：}Bellotti--Wong 改进了 \(N(T)\) 的显式误差界，并给出具体常数（\cite{BellottiWong2024ImprovedNT}），可直接用于 Turing 方法/数值核验与显式应用。
  \item \textbf{Turing 方法关键量的改进：}Amberger 给出 \(|S_1(t_2)-S_1(t_1)|\) 的改进界，从而改进 Turing 方法所需的块长度阈值（\cite{Amberger2025S1}）。
  \item \textbf{\(\Re(s)\approx 1\) 的显式控制：}Cully-Hugill--Leong 与 Leong 分别给出 \(\zeta'(s)/\zeta(s)\) 与 \(1/\zeta(s)\) 在 \(\sigma\) 接近 \(1\) 时的显式上界（\cite{CullyHugillLeong2023Zeta1Line,Leong2024LogDerivRecip}），可作为零自由区与误差项常数的工程输入。
\end{itemize}

\begin{definition}[核版 Newman 阈值（可检验参数）]\label{def:kernel-newman-threshold}
给定一个带参数的核族 \(M(u)\)（例如由势函数/温度化门 \(\mathrm{PROJ}_u\) 或角色扭曲生成；见定义 \ref{def:pom-proj-temp} 与 \ref{def:pom-proj-chi}），记主谱半径为 \(\lambda_1(u)\)，最坏非平凡扭曲谱半径为 \(\Lambda(u)\)。
定义核版 Newman 阈值为
\[
\beta_\ast:=\inf\{\beta\in\RR_{\ge 0}:\ \Lambda(e^{-\beta})\le \lambda_1(e^{-\beta})^{1/2}\}.
\]
当 \(\beta_\ast=0\) 时，原点切片（\(u=1\)）已满足核版 GRH 的平方根界；当 \(\beta_\ast>0\) 时，需向零温方向变形（\(\beta\) 增大、\(u=e^{-\beta}\) 变小）后才进入平方根区间。
\end{definition}

\begin{remark}[可复现输出：纯碰撞轴的核版 Newman 阈值（有限模数代理）]\label{rem:kernel-newman-threshold-repro}
以真实输入 40 状态核的三轴扭曲族为例，固定 \((q,r)=(1,1)\)，并在第三轴取纯碰撞可观测量 \(N_2\)（即 \(\xi=\mathbf{1}_{d=2}\)），对每个奇素数 \(p\) 考察复合扭曲
\(
u=\exp(-\beta)\,\omega_p^k
\)
（\(k\not\equiv 0\!\!\pmod p\)）。定义
\[
\lambda_1(\beta):=\rho(M(\exp(-\beta))),\qquad
\Lambda_p(\beta):=\max_{k\neq 0}\rho\bigl(M(\exp(-\beta)\omega_p^k)\bigr),
\]
并令 \(\beta_\ast(p)\) 为满足 \(\Lambda_p(\beta)\le \lambda_1(\beta)^{1/2}\) 的最小 \(\beta\)（若存在）。
表 \ref{tab:real_input_40_kernel_newman_threshold} 给出若干 \(p\) 的数值扫描结果：若在扫描区间内未观测到满足平方根界的 \(\beta\)，则给出下界 \(\beta_\ast(p)>\beta_{\max}\)，并同时列出扫描网格上 \(\Lambda_p/\sqrt{\lambda_1}\) 的最小值及其发生位置（脚本 \texttt{scripts/exp\_real\_input\_40\_kernel\_newman\_threshold.py} 一键生成）：
\input{sections/generated/tab_real_input_40_kernel_newman_threshold}
\end{remark}

\begin{remark}[从 toy-RH 到“角度熵”：一个可操作的升级方向]\label{rem:finite-rh-angle-entropy}
命题 \ref{prop:finite-rh-triangle-dict} 表明：有限维 toy-RH 的“临界线位置”只取决于归一化谱的模长约束（\( |\mu|\le \sqrt\lambda \)）。
若要在有限模型中模拟更接近 \(\zeta\) 的“零点密度/统计形态”，则必须让临界模长（\(|\mu|=\sqrt\lambda\)）上的特征值携带大量不同的角度，从而把单一晶格振荡推广为多频振荡叠加（推论 \ref{cor:finite-rh-explicit-formula} 的结构推广）。
在本文的可编译口径下，这一目标可通过增加 lift/角色轴束（例如 profinite 同余轴或更高维 abelian 扭曲族）来实现，并可用“单位圆谱角分布的熵/多样性”作为可复现的实验指纹。
一个更直接的“多频涨落”读出是去 prime-powers 的 \(\mathsf{E}_n^\sharp\)（定义 \ref{def:primitive-sharp}）：它把 Perron 的 prime-power 干涉项剥离后，把平方根级误差压缩为临界模相位和；当相位有理无关时还给出 Haar 推前的极限分布（注 \ref{rem:primitive-sharp-limit-law}）。
\end{remark}

\begin{remark}[平方根界的可审计证书：Lyapunov/内积构造]\label{rem:finite-rh-lyapunov}
核版 (G)RH 的平方根界是一个谱半径不等式。除直接对角化/求特征值外，还可用“构造内积”的方式给出可审计证书：
对任意矩阵 $A$ 与阈值 $r$，$\rho(A)<r$ 等价于存在正定矩阵 $H\succ 0$ 使 $A^\ast H A\prec r^2 H$（命题 \ref{prop:lyapunov-spectral-radius-certificate}）。
因此在扭曲分量 $A=M_{K,\rho}(u)$ 上，若能显式给出证书矩阵 $H_{\rho,u}$，就可把平方根界写成一条可复核的不等式，而不依赖数值特征值排序的稳健性。
\par
更重要的是：不等式
\(
A^\ast H A\prec r^2 H
\)
是关于未知量 \(H\) 的线性矩阵不等式（LMI），因此可以被直接转写为半正定可行性问题（SDP）。
这给出一条“可审计 + 可自动搜索”的研究回路：
\begin{enumerate}
  \item 固定一个 lift/标记骨架（例如置换 lift；参见命题 \ref{prop:kernel-artin-factorization} 的 Artin 因子化框架）并把其自由参数（生成元、置换、cocycle）视为待优化变量；
  \item 对每个不可约通道 \(\rho\neq\mathbf{1}\)，构造相应的扭曲块矩阵 \(M_{K,\rho}(u)\)；
  \item 对每个通道解 SDP 寻找 \(H_{\rho,u}\succ 0\) 使
  \[
  M_{K,\rho}(u)^\ast H_{\rho,u} M_{K,\rho}(u)\prec \lambda_1(u)\,H_{\rho,u}
  \qquad(\text{即 }r=\sqrt{\lambda_1(u)}),
  \]
  若全部非平凡通道均可行，则得到核版 GRH 的一个\textbf{可审计证书对象}（\(H\) 矩阵本身即可独立复核）。
\end{enumerate}
该“证书-可行性”视角把“零点/极点在临界线”的断言转写为有限个 SDP 的可行性检验，从而特别适合做系统性搜索与结构归纳：先用计算找到满足（或逼近）平方根界的 lift，再从证书形状中抽象出一般定理。
\end{remark}

\paragraph{primitive 轨道计数曲线与 \texorpdfstring{$x/\log x$}{x/log x} 的常数因子}
令
\[
\Pi(N):=\sum_{n\le N}p_n
\]
为按长度累计的 primitive 轨道数（按轨道计）。由 $p_n\sim \lambda^n/n$ 的“末项主导 + 几何尾”可得
\[
\Pi(N)\sim \frac{\lambda}{\lambda-1}\cdot \frac{\lambda^N}{N}.
\]
在本核上 $\lambda=\varphi^2$ 且 $\lambda-1=\varphi$，故
\[
\Pi(N)\sim \varphi\cdot\frac{\varphi^{2N}}{N}=\frac{\varphi^{2N+1}}{N}.
\]
若定义“轨道范数” $N(\tau):=\lambda^{|\tau|}$ 并令
\[
\pi(x):=\#\{\tau\ \mathrm{primitive}:\ N(\tau)\le x\},
\]
则 $\pi(\lambda^N)=\Pi(N)$，从而
\[
\pi(x)\sim \frac{\lambda\log\lambda}{\lambda-1}\cdot \frac{x}{\log x}.
\]
对 $\lambda=\varphi^2$，该常数因子化简为
\[
\frac{\lambda\log\lambda}{\lambda-1}
=\frac{\varphi^2\log(\varphi^2)}{\varphi}
=2\varphi\log\varphi.
\]

定义轨道 Mertens 和
\[
\mathcal{M}(N):=\sum_{n\le N}\frac{p_n}{\lambda^n}
=\log N+\mathsf{M}_{\varphi^2}+o(1),
\]
其中常数项可写为
\[
\mathsf{M}_{\varphi^2}
=\gamma+\sum_{n\ge 1}\left(\frac{p_n}{\lambda^n}-\frac{1}{n}\right),
\]
$\gamma$ 为 Euler--Mascheroni 常数。数值上
\[
\sum_{n\ge 1}\left(\frac{p_n}{\lambda^n}-\frac{1}{n}\right)\approx -0.254312661901716,
\qquad
\mathsf{M}_{\varphi^2}\approx 0.322903002999817.
\]
另外记 $P(z)=\sum_{n\ge 1}p_n z^n$，则在 $z\to z_\star=\lambda^{-1}$ 时
\[
P(z)=-\log(1-\lambda z)+\log\mathfrak{M}+o(1),
\]
其中
\[
\log\mathfrak{M}\approx -0.254312661901716,\qquad
\mathfrak{M}\approx 0.7754493106833835.
\]
由因式分解可得主极点 $z_\star=\lambda^{-1}=\varphi^{-2}$，并且
\[
\zeta_M(z)=\frac{C}{1-\lambda z}+O(1),\qquad
C=\lim_{z\to z_\star}(1-\lambda z)\,\zeta_M(z)=\frac{47+21\sqrt5}{100}\ (\approx 0.9395742753).
\]

\begin{proposition}[主极点留数常数的闭式（可审计）]\label{prop:real-input-40-residue-constant}
沿用上文 \(\lambda=\rho(M)=\varphi^2\) 与 \(z_\star=\lambda^{-1}\) 的记号，并定义主极点留数常数
\[
C_{\mathrm{real}}:=\lim_{z\to z_\star}(1-\lambda z)\,\zeta_M(z).
\]
则 \(C_{\mathrm{real}}\) 具有闭式
\[
\boxed{\;
C_{\mathrm{real}}=\frac{47+21\sqrt5}{100}=\frac{13+21\varphi}{50}\; }.
\]
\end{proposition}

\begin{corollary}[动力系统版 Chebyshev 显式公式：主项 \(\boldsymbol{+}\) 平方根振荡项]\label{cor:real-input-40-chebyshev-explicit}
记
\[
\Lambda(n):=a_n=\Tr(M^n),
\qquad
\Psi(N):=\sum_{n\le N}\Lambda(n).
\]
则由谱闭式
\(
a_n=\varphi^{2n}+(-\varphi)^n+2+3(-1)^n+\varphi^{-2n}+\varphi^{-n}
\)
对几何级数逐项求和，得到完全精确恒等式（无任何 \(O(\cdot)\) 项）：
\[
\boxed{\;
\begin{aligned}
\Psi(N)
={}&\frac{\lambda^{N+1}-\lambda}{\lambda-1}
+\frac{1-\lambda^{-N}}{\lambda-1}
+\frac{(-\varphi)^N-1}{\varphi}
+2N+\frac{3}{2}\bigl((-1)^N-1\bigr)
+\bigl(\varphi-\varphi^{1-N}\bigr),
\end{aligned}}
\]
其中 \(\lambda=\varphi^2\)。
因此“RH-sharp”的平方根尺度振荡项具有显式主振幅：
\[
\frac{(-\varphi)^N}{\varphi}=(-1)^N\frac{1}{\varphi}\,\lambda^{N/2},
\]
其大小恰为主项尺度的平方根，且符号严格交替。
\end{corollary}
\begin{proof}
由 \(\zeta_M(z)=1/\Delta(z)\) 且 \(z_\star\) 为 \(\Delta\) 的简单零点，得
\(
\zeta_M(z)=\frac{1}{-\Delta'(z_\star)}\cdot\frac{1}{z-z_\star}+O(1)
\)。
又 \(1-\lambda z=-\lambda(z-z_\star)+O((z-z_\star)^2)\)，故
\(
C_{\mathrm{real}}=\frac{\lambda}{\Delta'(z_\star)}=\frac{1}{-z_\star\Delta'(z_\star)}
\)。
将 \(\Delta(z)=\det(I-zM)\) 的因子分解代入并化简即可得到所示代数闭式。证毕。
\end{proof}
进一步由 Witt 分解/ Möbius--log 反演（与 \S\ref{subsec:zeta-online-kernel} 相同口径）可得一个直接由 $\zeta_M$ 主极点读出的 Abel/有限部闭式：
\[
\log\mathfrak{M}
=\log C+\sum_{m\ge 2}\frac{\mu(m)}{m}\log\zeta_M(z_\star^{\,m}),
\qquad z_\star=\lambda^{-1}.
\]
右端把有理函数 $\zeta_M$ 代入代数数 $z_\star^{\,m}$，由于 $z_\star<1$，该级数以指数速度收敛。

\paragraph{显式 Möbius--$\zeta$ 级数与对数乘积（仅用 \texorpdfstring{$\det(I-zM)$}{det} 的因式分解）}
由
\[
\zeta_M(z)=\frac{1}{\Delta(z)},\qquad
\Delta(z)=\det(I-zM)=(1-z)^2(1+z)^3(1-3z+z^2)(1+z-z^2),
\]
以及 $z_\star=\lambda^{-1}\in(0,1)$，可将上式等价写成只含 $\Delta$ 的形式
\[
\log\mathfrak{M}
\;=\;
\log C-\sum_{m\ge 2}\frac{\mu(m)}{m}\log \Delta(z_\star^{\,m}).
\]
进一步把 $\Delta$ 的因子逐项展开，可得一个完全“正实数对数”的闭式：
\[
\log\mathfrak{M}
=\log C
-\sum_{m\ge 2}\frac{\mu(m)}{m}\Bigl[
2\log(1-z_\star^{\,m})
3\log(1+z_\star^{\,m})
\log(1-3z_\star^{\,m}+z_\star^{\,2m})
\log(1+z_\star^{\,m}-z_\star^{\,2m})
\Bigr].
\]
该式在数值上极稳定：因为 $z_\star^{\,m}$ 指数趋零，且每项只需对正数取对数。
等价地，可写成 Euler--Möbius 乘积（将“有限部常数”完全压缩为一个可比标量）：
\[
\mathfrak{M}
=C\prod_{m\ge 2}\zeta_M(z_\star^{\,m})^{\mu(m)/m}
=C\prod_{m\ge 2}\Delta(z_\star^{\,m})^{-\mu(m)/m}.
\]
\begin{lemma}[莫比乌斯坍缩：平凡因子的显式求和]\label{lem:mobius-collapse}
对 $|z|<1$ 有恒等式
\[
\sum_{m\ge 1}\frac{\mu(m)}{m}\log(1-z^m)=-z,\qquad
\sum_{m\ge 1}\frac{\mu(m)}{m}\log(1+z^m)=z-z^2.
\]
因此
\[
\sum_{m\ge 1}\frac{\mu(m)}{m}\log\Bigl((1-z^m)^{-a}(1+z^m)^{-b}\Bigr)=(a-b)z+bz^2.
\]
\end{lemma}

\begin{proof}
第一条恒等式可由幂级数展开直接核对：当 $|z|<1$ 时
\[
\sum_{m\ge 1}\frac{\mu(m)}{m}\log(1-z^m)
=-\sum_{m\ge 1}\frac{\mu(m)}{m}\sum_{k\ge 1}\frac{z^{mk}}{k}
=-\sum_{n\ge 1}\frac{z^n}{n}\sum_{m\mid n}\mu(m)
=-z.
\]
其中用到恒等式 $\sum_{m\mid n}\mu(m)=\mathbf{1}_{\{n=1\}}$。

第二条恒等式由
\(
\log(1+z^m)=\log(1-z^{2m})-\log(1-z^m)
\)
化为第一条：令
\(
f(z):=\sum_{m\ge 1}\frac{\mu(m)}{m}\log(1-z^m)
\)，
则
\[
\sum_{m\ge 1}\frac{\mu(m)}{m}\log(1+z^m)=f(z^2)-f(z)=(-z^2)-(-z)=z-z^2.
\]
最后一式由线性组合
\(
-a\log(1-z^m)-b\log(1+z^m)
\)
与前两条直接推出。证毕。
\end{proof}

\begin{lemma}[从 \texorpdfstring{$m\ge 2$}{m>=2} 开始的显式求和]\label{lem:mobius-collapse-mge2}
对 $|z|<1$ 有恒等式
\[
\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1-z^m)=-z-\log(1-z),
\]
\[
\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1+z^m)=z-z^2-\log(1+z).
\]
\end{lemma}
\begin{proof}
由引理 \ref{lem:mobius-collapse} 的两条恒等式，直接写
\(
\sum_{m\ge 2}=\sum_{m\ge 1}-\text{(}m=1\text{项)}
\)
即得。证毕。
\end{proof}

\begin{corollary}[一般形态：\texorpdfstring{$(1-z)^a(1+z)^b$}{(1-z)^a(1+z)^b} 的 \texorpdfstring{$m\ge 2$}{m>=2} Möbius--$\log$ 闭式]\label{cor:mobius-collapse-mge2-ab}
对 $|z|<1$，令
\[
S_{a,b}(z):=\sum_{m\ge 2}\frac{\mu(m)}{m}\log\Bigl((1-z^m)^{-a}(1+z^m)^{-b}\Bigr).
\]
则有严格恒等式
\[
\boxed{\;
S_{a,b}(z)=(a-b)z+bz^2+a\log(1-z)+b\log(1+z)\; }.
\]
\end{corollary}

\begin{proof}
由定义
\(
S_{a,b}(z)=-a\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1-z^m)-b\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1+z^m)
\)。
代入引理 \ref{lem:mobius-collapse-mge2} 的两条闭式并整理即得。证毕。
\end{proof}

\begin{remark}[剥离平凡因子后的非平凡部分]
将引理 \ref{lem:mobius-collapse} 作用于 $(1-z)^2(1+z)^3$，其 Möbius--$\log$ 求和贡献可精确压缩为
\[
(-z_\star+3z_\star^2),
\]
其中 $-z_\star$ 来自 $(1-z)^2$ 与 $(1+z)^3$ 的线性合并项，而 $+3z_\star^2$ 是 $(1+z)^3$ 在修正恒等式中的二次修正项。
在本核的主极点 $z_\star=\varphi^{-2}$ 处可进一步化为完全代数值
\[
-z_\star+3z_\star^2
=-\varphi^{-2}+3\varphi^{-4}
=9-4\sqrt5
\approx 0.055728090000840.
\]
因此“剥离平凡因子”不仅产生线性项，还会带来一个可显式计算的二次项；这不会改变主极点位置，但会在 Abel 有限部常数的压缩表达中产生可观测的常数级修正。
\end{remark}

\begin{proposition}[平凡因子的 primitive 贡献是有限多项式]\label{prop:triv-factor-primitive-polynomial}
定义
\[
\zeta_{\mathrm{triv}}(z):=\frac{1}{(1-z)^2(1+z)^3},
\qquad
P_{\mathrm{triv}}(z):=\sum_{m\ge 1}\frac{\mu(m)}{m}\log\zeta_{\mathrm{triv}}(z^m).
\]
则对 \(|z|<1\) 有精确恒等式
\[
\boxed{\ P_{\mathrm{triv}}(z)=-z+3z^2\ }.
\]
因此平凡因子对 primitive 轨道生成函数 \(P(z)=\sum_{n\ge 1}p_n z^n\) 的影响仅发生在 \(n=1,2\)：
\[
p^{\mathrm{triv}}_1=-1,\qquad p^{\mathrm{triv}}_2=3,\qquad p^{\mathrm{triv}}_n=0\ (n\ge 3),
\]
其中“负号”仅表示 Witt/Möbius--$\log$ 变换下的代数拆分（不对应真实轨道的负计数）。
\end{proposition}
\begin{proof}
由引理 \ref{lem:mobius-collapse}，
\[
P_{\mathrm{triv}}(z)
=-2\sum_{m\ge 1}\frac{\mu(m)}{m}\log(1-z^m)-3\sum_{m\ge 1}\frac{\mu(m)}{m}\log(1+z^m)
=-2(-z)-3(z-z^2)=-z+3z^2.
\]
比较系数即得 \(p^{\mathrm{triv}}_n\) 的结论。证毕。
\end{proof}

\begin{proposition}[两条 Möbius--$\log$ 级数的闭式：\texorpdfstring{$\log\mathfrak{M}$}{logM} 的二核压缩]\label{prop:real-input-40-mertens-two-series}
在真实输入 $40$ 状态核上，
\(
\Delta(z)=(1-z)^2(1+z)^3(1-3z+z^2)(1+z-z^2)
\)
且 \(z_\star=\lambda^{-1}=\varphi^{-2}\in(0,1)\)、\(C=\frac{47+21\sqrt5}{100}\)。
定义
\[
\log K_0
:=\log C-z_\star+3z_\star^2+2\log(1-z_\star)+3\log(1+z_\star)
=\log\!\left(\frac{5+2\sqrt5}{10}\right)+(9-4\sqrt5).
\]
则 Abel 有限部常数满足严格闭式
\[
\boxed{\;
\log\mathfrak{M}
=
\log K_0
-\sum_{m\ge 2}\frac{\mu(m)}{m}
\log\!\Bigl(
(1-3z_\star^{\,m}+z_\star^{\,2m})(1+z_\star^{\,m}-z_\star^{\,2m})
\Bigr)\; }.
\]
\end{proposition}
\begin{proof}
由上文公式
\(
\log\mathfrak{M}
=\log C-\sum_{m\ge 2}\frac{\mu(m)}{m}\log \Delta(z_\star^{\,m})
\)
并逐项展开 \(\Delta\) 的四个因子，得到
\[
\log\mathfrak{M}
=\log C
-2\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1-z_\star^{\,m})
-3\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1+z_\star^{\,m})
(\cdots),
\]
其中 \((\cdots)\) 为两条二次因子 \(1-3z+z^2\) 与 \(1+z-z^2\) 的 Möbius--$\log$ 求和项。
对前两条求和，应用引理 \ref{lem:mobius-collapse-mge2} 即得
\[
-2\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1-z_\star^{\,m})
-3\sum_{m\ge 2}\frac{\mu(m)}{m}\log(1+z_\star^{\,m})
=-z_\star+3z_\star^2+2\log(1-z_\star)+3\log(1+z_\star),
\]
从而得到所示二核压缩公式。证毕。
\end{proof}

\begin{remark}[可复现数值：\texorpdfstring{$\log K_0,S_3,S_{\pm},\log\mathfrak{M}$}{logK0,S3,Spm,logM}]\label{rem:real-input-40-mertens-two-series-numeric}
表 \ref{tab:real_input_40_abel_mertens_two_series_split} 给出二核压缩分解的高精度数值。可见
\(\log K_0\) 的量级约为 \(10^{-3}\)，而 \(\log\mathfrak{M}\) 的主贡献来自两条二次因子
\((1-3z+z^2)\) 与 \((1+z-z^2)\) 对应的两条 Möbius--$\log$ 级数之和。
\input{sections/generated/tab_real_input_40_abel_mertens_two_series_split}
\end{remark}

\begin{theorem}[平凡因子完全闭式剥离与 \texorpdfstring{$\mathfrak{M}_{\mathrm{carry}}$}{M_carry}（二次 carry-常数）]\label{thm:real-input-40-carry-constant}
令 \(z_\star=\varphi^{-2}\)、\(C=\frac{47+21\sqrt5}{100}\)。
定义
\[
\mathfrak{M}_{\mathrm{carry}}
:=\exp\!\left(
-\sum_{m\ge 2}\frac{\mu(m)}{m}
\Bigl[
\log(1-3z_\star^{\,m}+z_\star^{\,2m})
\;+\;
\log(1+z_\star^{\,m}-z_\star^{\,2m})
\Bigr]\right).
\]
则 Abel 有限部常数满足乘法分解
\[
\boxed{\;
\mathfrak{M}
\;=\;
\underbrace{C\cdot z_\star(1+z_\star)^3\cdot e^{z_\star^3}}_{\text{完全闭式的平凡因子部分}}
\cdot
\underbrace{\mathfrak{M}_{\mathrm{carry}}}_{\text{仅由两条二次因子贡献的 carry-常数}}\; }.
\]
\end{theorem}

\begin{proof}
由命题 \ref{prop:real-input-40-mertens-two-series}，
\(
\log\mathfrak{M}
=\log K_0-(S_3+S_\pm)
\)，
其中
\(
\log K_0=\log C-z_\star+3z_\star^2+2\log(1-z_\star)+3\log(1+z_\star)
\)。
注意到 \(z_\star=\varphi^{-2}\) 满足 \(1-z_\star=\varphi^{-1}\)，从而 \((1-z_\star)^2=z_\star\)；并且由 \(z_\star^2-3z_\star+1=0\) 得
\(
-z_\star+3z_\star^2=z_\star^3
\)。
于是
\[
K_0=C\cdot (1-z_\star)^2(1+z_\star)^3\cdot e^{-z_\star+3z_\star^2}
=C\cdot z_\star(1+z_\star)^3\cdot e^{z_\star^3}.
\]
把 \(\mathfrak{M}_{\mathrm{carry}}:=\exp(-(S_3+S_\pm))\) 代回即得乘法分解。证毕。
\end{proof}

\begin{remark}[可复现数值与可认证误差界（carry-常数拆分）]\label{rem:real-input-40-carry-constant-certified}
脚本 \texttt{scripts/exp\_real\_input\_40\_carry\_constant\_certified.py} 直接按定理 \ref{thm:real-input-40-carry-constant} 计算
\(\log\mathfrak{M}\)、\(\log\mathfrak{M}_{\mathrm{carry}}\)
以及显式尾界
\(
|\mathrm{tail}(N)|\le \frac{5 z_\star^{N+1}}{(N+1)(1-z_\star)}
\)，并输出一段可直接嵌入的公式块：
\input{sections/generated/eq_real_input_40_carry_constant_certified}
\end{remark}

\begin{remark}[常数层的 Artin 分解：\texorpdfstring{$\log\mathfrak{M}$}{logM} 的三因子乘法因子化]\label{rem:real-input-40-logM-artin-factorization}
由
\(
\Delta(z)=(1-z)^2(1+z)^3(1-3z+z^2)(1+z-z^2)
\)
可把 \(\zeta_M(z)=1/\Delta(z)\) 分解为三因子
\[
\zeta_M(z)=\zeta_{\mathrm{triv}}(z)\cdot \zeta_{\mathrm{even}}(z)\cdot \zeta_{\chi}(z),
\]
其中
\[
\zeta_{\mathrm{triv}}(z)=\frac{1}{(1-z)^2(1+z)^3},\qquad
\zeta_{\mathrm{even}}(z)=\frac{1}{1-3z+z^2},\qquad
\zeta_{\chi}(z)=\frac{1}{1+z-z^2}.
\]
在主极点 \(z_\star=\lambda^{-1}=\varphi^{-2}\) 处，仅 \(\zeta_{\mathrm{even}}\) 具有极点，另外两因子解析且不为零；因此留数与 Abel 有限部常数均可作乘法分解。具体地，若记
\[
P_{\mathrm{triv}}(z):=\sum_{m\ge 1}\frac{\mu(m)}{m}\log\zeta_{\mathrm{triv}}(z^m),\qquad
K_{-F}:=\sum_{m\ge 1}\frac{\mu(m)}{m}\log\zeta_{\chi}(z_\star^{\,m}),
\]
则存在 \(\log\mathfrak{M}_{\mathrm{even}}\) 使得
\[
\boxed{\ \log\mathfrak{M}=\log\mathfrak{M}_{\mathrm{even}}+K_{-F}+P_{\mathrm{triv}}(z_\star)\ }.
\]
其中 \(P_{\mathrm{triv}}(z)\) 由命题 \ref{prop:triv-factor-primitive-polynomial} 给出为有限多项式（因此完全闭式），而 \(K_{-F}\) 为表 \ref{tab:real_input_40_artin_sign_mertens} 中的 Artin sign Mertens 常数。三因子在常数层的闭式/数值核验由脚本 \texttt{scripts/exp\_real\_input\_40\_logM\_artin\_factorization.py} 一键生成：

\input{sections/generated/eq_real_input_40_logM_artin_factorization}
\end{remark}

\begin{corollary}[二核级数的可认证尾界]\label{cor:real-input-40-mertens-two-series-tail}
令 \(q:=z_\star=\varphi^{-2}\)。
对任意 \(N\ge 2\)，令
\[
S_3^{(>N)}:=\sum_{m>N}\frac{\mu(m)}{m}\log(1-3q^m+q^{2m}),
\qquad
S_\pm^{(>N)}:=\sum_{m>N}\frac{\mu(m)}{m}\log(1+q^m-q^{2m}).
\]
则有可认证上界
\[
\boxed{\;
\bigl|S_3^{(>N)}\bigr|+\bigl|S_\pm^{(>N)}\bigr|
\le
5\sum_{m>N}\frac{q^m}{m}
\le
\frac{5}{1-q}\cdot\frac{q^{N+1}}{N+1}\; }.
\]
\end{corollary}
\begin{proof}
对 \(t\in[0,q^2]\) 直接估计可得
\(
0\le -\log(1-3t+t^2)\le 4t
\)
与
\(
0\le \log(1+t-t^2)\le t
\)。
令 \(t=q^m\)（当 \(m\ge 2\) 时 \(t\le q^2\)），并用 \(|\mu(m)|\le 1\) 得
\[
\left|\frac{\mu(m)}{m}\log(1-3q^m+q^{2m})\right|\le \frac{4q^m}{m},\qquad
\left|\frac{\mu(m)}{m}\log(1+q^m-q^{2m})\right|\le \frac{q^m}{m}.
\]
求和得到第一条不等式。第二条由
\(
\sum_{m>N}q^m/m\le \frac{q^{N+1}}{(N+1)(1-q)}
\)
给出。证毕。
\end{proof}

\begin{proposition}[约化 Mertens 常数：去平凡因子后的唯一贡献]\label{prop:real-input-40-reduced-mertens}
设
\[
\Delta(z)=\det(I-zM)=\Delta_{\mathrm{triv}}(z)\,\Delta_{\mathrm{nt}}(z),
\qquad
\Delta_{\mathrm{triv}}(z)=(1-z)^a(1+z)^b,
\]
其中 $a,b\in\ZZ_{\ge 0}$。假设主极点 $z_\star\in(0,1)$ 来自非平凡块，即 $\Delta_{\mathrm{nt}}(z_\star)=0$ 且为简单零点，而 $\Delta_{\mathrm{triv}}(z_\star)\neq 0$。
令
\[
\log\mathfrak{M}_{\mathrm{nt}}
:=\log C_{\mathrm{nt}}-\sum_{m\ge 2}\frac{\mu(m)}{m}\log \Delta_{\mathrm{nt}}(z_\star^{\,m}),
\qquad
C_{\mathrm{nt}}:=\frac{1}{-z_\star\,\Delta_{\mathrm{nt}}'(z_\star)}.
\]
则 Abel 有限部常数满足严格分解
\[
\boxed{\;
\log\mathfrak{M}
\;=\;
\log\mathfrak{M}_{\mathrm{nt}}
\;+\;
(a-b)\,z_\star
\;+\;
b\,z_\star^2.
\;}
\]
特别地，除去一个完全显式的“平凡修正项”\((a-b)z_\star+bz_\star^2\) 后，常数项的真正机制信息只依赖于非平凡谱块 $\Delta_{\mathrm{nt}}$。
\end{proposition}
\begin{proof}
由 $\Delta=\Delta_{\mathrm{triv}}\Delta_{\mathrm{nt}}$ 且 $\Delta_{\mathrm{nt}}(z_\star)=0$ 为简单零点，可得
\(
\Delta'(z_\star)=\Delta_{\mathrm{triv}}(z_\star)\Delta_{\mathrm{nt}}'(z_\star)
\)，
从而主留数常数满足
\(
C=\frac{1}{-z_\star\Delta'(z_\star)}=C_{\mathrm{nt}}/\Delta_{\mathrm{triv}}(z_\star)
\)。
将
\[
\log\mathfrak{M}=\log C-\sum_{m\ge 2}\frac{\mu(m)}{m}\log\Delta(z_\star^{\,m})
\]
代入并分解出 $\Delta_{\mathrm{triv}}$ 与 $\Delta_{\mathrm{nt}}$，得到
\[
\log\mathfrak{M}
=\log\mathfrak{M}_{\mathrm{nt}}
-\log\Delta_{\mathrm{triv}}(z_\star)
-\sum_{m\ge 2}\frac{\mu(m)}{m}\log\Delta_{\mathrm{triv}}(z_\star^{\,m}).
\]
对 $\Delta_{\mathrm{triv}}(z)=(1-z)^a(1+z)^b$，用引理 \ref{lem:mobius-collapse} 的两条恒等式并注意到
\(
\sum_{m\ge 2}=\sum_{m\ge 1}-\text{(}m=1\text{项)}
\)，
可直接化简得到
\[
-\log\Delta_{\mathrm{triv}}(z)-\sum_{m\ge 2}\frac{\mu(m)}{m}\log\Delta_{\mathrm{triv}}(z^{m})
=(a-b)z+bz^2.
\]
取 $z=z_\star$ 即得结论。证毕。
\end{proof}

\begin{definition}[Artin 符号通道的 Mertens 常数]\label{def:real-input-40-artin-sign-mertens}
在张量拆分中，因子
\(
\Delta_{\mathrm{twist}}(z)=1+z-z^2
\)
等价于
\(
\det(I-z(-F))=\det(I+zF)
\)
（其中 \(F=\begin{pmatrix}1&1\\1&0\end{pmatrix}\)）。
记
\[
\zeta_{-F}(z):=\frac{1}{1+z-z^2},
\qquad
\lambda=\varphi^2,\quad z_\star=\lambda^{-1}.
\]
定义其 Abel/Mertens 有限部常数（纯 twist 因子）为
\[
\boxed{\;
K_{-F}
:=\sum_{m\ge 1}\frac{\mu(m)}{m}\log\zeta_{-F}(z_\star^{\,m})
=-\sum_{m\ge 1}\frac{\mu(m)}{m}\log\!\bigl(1+z_\star^{\,m}-z_\star^{\,2m}\bigr)\; }.
\]
\end{definition}

\begin{remark}[可复现数值：\texorpdfstring{$K_{-F}$}{K_-F}]\label{rem:real-input-40-artin-sign-mertens-value}
由表 \ref{tab:real_input_40_finite_part_split} 的拆分，\(\log\mathfrak{M}_{\mathrm{twist}}\) 正是上述 \(K_{-F}\)（它完全由 \(1+z-z^2\) 这一非主极点因子贡献）。
为便于复核，我们同时给出一个高精度常数表：
\input{sections/generated/tab_real_input_40_artin_sign_mertens}
\end{remark}

此外，由 $\sqrt5=2\varphi-1$，留数常数亦可写成
\[
C=\frac{47+21\sqrt5}{100}=\frac{13+21\varphi}{50}.
\]
