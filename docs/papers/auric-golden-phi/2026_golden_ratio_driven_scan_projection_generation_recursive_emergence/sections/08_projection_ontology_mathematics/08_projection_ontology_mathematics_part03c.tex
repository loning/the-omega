$$
S_3(m)=\Theta(r_3^m),
\qquad
\mathbb{P}\bigl[\Fold_m(\omega_1)=\Fold_m(\omega_2)=\Fold_m(\omega_3)\bigr]
=\Theta\!\left(\left(\frac{r_3}{8}\right)^m\right).
$$
进一步，令 \(\pi_m(x):=d_m(x)/2^m\) 为均匀微态经折叠后的输出分布，则其 \(\mathrm{R\acute{e}nyi}\)--3 投影熵率存在且为
$$
h_3:=\lim_{m\to\infty}\frac{1}{m}\Bigl(-\log\sum_{x\in X_m}\pi_m(x)^3\Bigr)
=\log\!\left(\frac{8}{r_3}\right).
$$
\end{corollary}
\begin{proof}
由命题 \ref{prop:pom-s3-recurrence} 得 \(S_3(m)\) 的生成函数分母为 \(1-2z-4z^2+2z^3=\det(I-zA_3)\)，其收敛半径由该分母的最小模零点决定，等价于特征多项式的最大实根 \(r_3\)，故 \(S_3(m)=\Theta(r_3^m)\)。又
$$
\sum_{x\in X_m}\pi_m(x)^3=\sum_x \frac{d_m(x)^3}{8^m}=\frac{S_3(m)}{8^m},
$$
代入渐近即得 \(h_3=\log(8/r_3)\)。证毕。
\end{proof}

\begin{remark}[三阶碰撞核的严格 RH 裕量（\texorpdfstring{$A_3$}{A3}）]\label{rem:pom-collision-rh-margin-a3}
记 \(\lambda:=\rho(A_3)=r_3\approx 3.086130197651\)。由
\[
\det(\lambda I-A_3)=\lambda^3-2\lambda^2-4\lambda+2
\]
可得三条特征值均为实数，除 Perron 根外另外两根数值约为
\[
\mu_2\approx -1.514136929335,\qquad
\mu_3\approx 0.428006731684.
\]
因此
\[
\max\{|\mu_2|,|\mu_3|\}\approx 1.514137\ <\ \sqrt{r_3}\approx 1.756739,
\]
即三阶碰撞核同样严格满足核版 RH 不等式并留有裕量。
\end{remark}

\begin{remark}[等价的三态 \texorpdfstring{$A_3$}{A3} realization：同一递推的不同非负整数实现]\label{rem:pom-a3-alternative-realization}
三阶碰撞矩序列 \(\{S_3(m)\}_{m\ge 1}\) 的有限维 realization 并不唯一：只要保持同一特征多项式
\(
\lambda^3-2\lambda^2-4\lambda+2
\)
并匹配读出向量，即可得到同一 \(S_3\)。
例如取
$$
A_3':=
\begin{pmatrix}
0&1&0\\
1&0&1\\
0&3&2
\end{pmatrix},
\qquad
u_3':=(0,6,2),\qquad
v_3':=(0,0,1)^{\mathsf T},
$$
则对一切 \(m\ge 1\) 有
$$
S_3(m)=u_3' \, (A_3')^{\,m-1}\, v_3',
$$
并且
$$
\det(I-zA_3')=1-2z-4z^2+2z^3.
$$
因此 \(A_3'\) 与正文采用的 \(A_3\) 共享同一递推分母与 Perron 根 \(r_3\)，只是给出了一个不同的三态非负整数表示。
\end{remark}

\begin{definition}[三阶碰撞核的 \texorpdfstring{$\zeta$}{zeta} 与有限部常数]\label{def:pom-collision-zeta-a3}
对三态核 \(A_3\) 定义
$$
\zeta_{A_3}(z):=\exp\!\left(\sum_{n\ge 1}\frac{\mathrm{Tr}(A_3^n)}{n}z^n\right)
:=\frac{1}{\det(I-zA_3)}.
$$
相应的 primitive 轨道数 \(p_n(A_3)\)、以及 Abel/Mertens 有限部常数 \(\log\mathfrak{M}(A_3)\) 与 \(\mathsf{M}(A_3)=\gamma+\log\mathfrak{M}(A_3)\) 可按第 \ref{sec:zeta-finite-part} 节的通用接口抽取；其可复现输出见表 \ref{tab:collision_kernel_A3_primitive} 与 \ref{tab:collision_kernel_A3_finite_part}（脚本 \texttt{scripts/exp\_collision\_kernel\_A3\_primitive.py} 与 \texttt{scripts/exp\_collision\_kernel\_A3\_finite\_part.py}）。
\end{definition}

\input{sections/generated/tab_collision_kernel_A3_primitive}
\input{sections/generated/tab_collision_kernel_A3_finite_part}

\subsubsection{四阶碰撞矩与 \texorpdfstring{$\mathrm{R\acute{e}nyi}$}{Renyi}--4 投影熵率}\label{subsec:pom-s4}
\begin{definition}[四阶折叠碰撞矩]\label{def:pom-s4}
定义四阶碰撞和
$$
S_4(m):=\sum_{x\in X_m} d_m(x)^4.
$$
等价地，从 \(\Omega_m\) 上均匀独立抽取四条窗口 \(\omega_1,\omega_2,\omega_3,\omega_4\)，则
$$
\mathbb{P}\bigl[\Fold_m(\omega_1)=\Fold_m(\omega_2)=\Fold_m(\omega_3)=\Fold_m(\omega_4)\bigr]=\frac{S_4(m)}{16^m}.
$$
\end{definition}

\begin{proposition}[四阶碰撞核的五态非负整数 realization]\label{prop:pom-s4-recurrence}
存在一个五态的非负整数矩阵
$$
A_4:=
\begin{pmatrix}
0&1&0&0&0\\
0&0&7&0&1\\
0&1&2&0&0\\
1&0&1&0&0\\
0&0&0&1&0
\end{pmatrix},
$$
以及读出向量
$$
u_4:=(9,404,1613,25,114)^{\mathsf T},\qquad
v_4:=(1,0,0,0,0)^{\mathsf T},
$$
使得对一切 \(m\ge 2\),
$$
S_4(m)=2\,u_4^{\mathsf T}A_4^{\,m-2}v_4.
$$
从而 \(S_4(m)\) 满足精确五阶递推：对一切 \(m\ge 13\),
$$
S_4(m)=2S_4(m-1)+7S_4(m-2)+2S_4(m-4)-2S_4(m-5).
$$
\end{proposition}
\begin{proof}
由表 \ref{tab:fold_collision_moment_recursions} 可审计地核验 \(S_4(m)\) 满足阶数为 \(5\) 的整数递推（脚本 \texttt{scripts/exp\_fold\_collision\_moment\_recursions.py}）。另一方面，上述 \(A_4\) 与 \((u_4,v_4)\) 给出一个完全显式的五态非负整数 realization；其与递推以及小 \(m\) 的枚举值一致性可由脚本 \texttt{scripts/exp\_fold\_collision\_kernel\_A4\_verify.py} 逐项核验。证毕。
\end{proof}

\begin{corollary}[四阶碰撞指数与 \texorpdfstring{$h_4$}{h4}]\label{cor:pom-s4-asymptotic}
设 \(r_4:=\rho(A_4)\) 为多项式
$$
x^5-2x^4-7x^3-2x+2=0
$$
的最大实根（\(r_4\approx 3.846059041178\)）。则
$$
S_4(m)=\Theta(r_4^m),
\qquad
h_4:=\lim_{m\to\infty}\frac{1}{m}\Bigl(-\log\sum_{x\in X_m}\pi_m(x)^4\Bigr)
=\log\!\left(\frac{16}{r_4}\right).
$$
\end{corollary}
\begin{proof}
由命题 \ref{prop:pom-s4-recurrence}，生成函数分母为 \(\det(I-zA_4)\)，其最小模零点为 \(z_\star=1/\rho(A_4)\)，从而 \(S_4(m)=\Theta(r_4^m)\)。并且
$$
\sum_{x\in X_m}\pi_m(x)^4=\sum_x\frac{d_m(x)^4}{16^m}=\frac{S_4(m)}{16^m},
$$
代入渐近即得 \(h_4=\log(16/r_4)\)。证毕。
\end{proof}


\begin{remark}[四阶碰撞核的贴线 RH 破缺：谱分解数值证书（\texorpdfstring{$A_4$}{A4}）]\label{rem:pom-collision-rh-break-a4-spectrum}
记 \(r_4=\rho(A_4)\approx 3.846059041178\)。
直接求谱（等价地，对 \(\det(\lambda I-A_4)=\lambda^5-2\lambda^4-7\lambda^3-2\lambda+2\) 求根）得到非 Perron 特征值近似为
\[
\mu_2\approx -1.963592213449,\qquad
\mu_3\approx 0.503913309522,\qquad
\mu_{4,5}\approx -0.193190068626\pm 0.698726683936\,\mathrm{i}.
\]
因此次谱半径
\[
\Lambda_4:=\max_{\mu\in\sigma(A_4)\setminus\{r_4\}}|\mu|
=|\mu_2|
\approx 1.963592213449,
\]
而
\(
\sqrt{r_4}\approx 1.961137180612
\)，从而核版 RH 的平方根界（定义 \ref{def:kernel-rh} 的同型条件）在四阶碰撞核上出现严格越线：
\[
\Lambda_4>\sqrt{r_4},\qquad
R_4:=\frac{\Lambda_4}{\sqrt{r_4}}\approx 1.001251841463.
\]
用“极点实部指数”口径可写为
\[
\beta_4:=\frac{\log \Lambda_4}{\log r_4}\approx 0.500928740205=\frac12+9.2874\times 10^{-4},
\]
并可引入贴线偏移量
\[
\delta_4:=\log\!\left(\frac{\Lambda_4^2}{r_4}\right)=2\log R_4\approx 0.002502117125.
\]
 该超出由一个稳定的负实模态主导（\(\mu_2<0\)），从而对应一条不可消去的反相振荡慢模。
\end{remark}

\begin{remark}[四阶碰撞核的单参数 RH 临界点：边权阈值 \texorpdfstring{$t_\star$}{t*}（resultant 消元，可审计）]\label{rem:pom-collision-rh-break-a4-threshold}
把 \(A_4\) 中唯一的多重边权 \(7\) 视为连续参数 \(t>0\)，定义一参数族
\[
A_4(t):=
\begin{pmatrix}
0&1&0&0&0\\
0&0&t&0&1\\
0&1&2&0&0\\
1&0&1&0&0\\
0&0&0&1&0
\end{pmatrix},
\qquad
p_t(x):=\det(xI-A_4(t))=x^5-2x^4-tx^3-2x+2.
\]
记 \(r(t)=\rho(A_4(t))\)、\(\Lambda(t)=\max_{\mu\in\mathrm{spec}(A_4(t))\setminus\{r(t)\}}|\mu|\)，并令 Newman 型偏移
\(
\delta(t):=\log(\Lambda(t)^2/r(t))
\)
（与注 \ref{rem:pom-collision-rh-break-a4} 的口径一致）。
在该族上，\(\delta(t)\) 的达界点可由\textbf{负实根通道}刻画：临界点等价于存在 \(y<0\) 使得 \(p_t(y)=p_t(y^2)=0\)，从而（排除退化根 \(y=\pm 1\)）满足
\[
y^8-2y^6+2y^5-2y^4+2y^3-2=0.
\]
令 \(z:=-y>0\)，则等价地
\(
z^8-2z^6-2z^5-2z^4-2z^3-2=0
\)，并且由 \(p_t(y)=0\) 可显式解出
\[
t=\frac{y^5-2y^4-2y+2}{y^3}
=z^2+2z-\frac{2}{z^2}-\frac{2}{z^3}.
\]
进一步对 \(y\) 作结式消元可得 \(t\) 的 8 次整系数证书 \(P_4(t)=0\)；其唯一正实根记为 \(t_\star\)。
数值核验表明：当 \(t<t_\star\) 时 \(\Lambda(t)<\sqrt{r(t)}\)（严格 kernel RH），当 \(t=t_\star\) 时恰达界（RH-sharp，且 \(\mu_\star=-\sqrt{r_\star}\)），而当 \(t>t_\star\) 时出现 \(\Lambda(t)>\sqrt{r(t)}\) 的越线。
 因此原整数核 \(A_4=A_4(7)\) 位于临界右侧且仅有临界附近的超出 \(7-t_\star\ll 1\)，从而解释了 \(R_4-1\) 的 \(10^{-3}\) 量级。
\end{remark}
\input{sections/generated/eq_collision_kernel_A4_edge_weight_threshold}


\begin{remark}[四阶碰撞核的 near-RH 破缺（\texorpdfstring{$A_4$}{A4}）]\label{rem:pom-collision-rh-break-a4}
记 \(r_4=\rho(A_4)\)，并令次谱半径
\[
\Lambda_4:=\max_{\mu\in\mathrm{spec}(A_4)\setminus\{r_4\}}|\mu|.
\]
 则四阶碰撞核已出现\textbf{仅有轻微}的核版 RH 超出：\(\Lambda_4>\sqrt{r_4}\)（见式 \ref{eq:collision_kernel_A4_rh_breaking}），其对应的 \(s\)-平面非平凡极点列实部指数
\(
\beta_4=\log\Lambda_4/\log r_4
\)
 严格大于 \(1/2\)，但仅为临界附近的 \(10^{-3}\) 量级超出。
\par
为统一刻画这一“Newman 型偏移”，可对任意有限核 \(A\) 定义
\[
\delta(A):=\log\!\left(\frac{\Lambda(A)^2}{\rho(A)}\right),
\qquad
\Lambda(A):=\max_{\mu\in\mathrm{spec}(A)\setminus\{\rho(A)\}}|\mu|.
\]
则核版 RH 等价于 \(\delta(A)\le 0\)。在本例中 \(\delta(A_4)>0\) 但数值极小（式 \ref{eq:collision_kernel_A4_rh_breaking}）。
\par
结合注 \ref{rem:pom-collision-rh-margin} 与注 \ref{rem:pom-collision-rh-margin-a3}（\(q=2,3\) 的严格裕量），可见 \(q=4\) 是已构造范围内的最小破缺阶。
更一般地，对已核验的高阶递推（表 \ref{tab:fold_collision_moment_recursions}），直接对特征多项式做根扫描即可得到
\(\Lambda_q/\sqrt{r_q}\) 与 \(\beta_q\) 随 \(q\) 的快速劣化：从 \(q=4\) 起跨过临界线，且 \(q\ge 5\) 迅速增强（表 \ref{tab:fold_collision_kernel_rh_scan_q2_8}）。
此外，在 \(q\le 8\) 的扫描中，达成 \(\Lambda_q\) 的最大非 Perron 根在数值上始终为负实根（反相模态），因此越线首先由实轴模态触发。
\end{remark}

\input{sections/generated/eq_collision_kernel_A4_rh_breaking}
\input{sections/generated/tab_fold_collision_kernel_rh_scan_q2_8}


\begin{definition}[四阶碰撞核的 \texorpdfstring{$\zeta$}{zeta} 与有限部常数]\label{def:pom-collision-zeta-a4}
对五态核 \(A_4\) 定义
$$
\zeta_{A_4}(z):=\exp\!\left(\sum_{n\ge 1}\frac{\mathrm{Tr}(A_4^n)}{n}z^n\right)
:=\frac{1}{\det(I-zA_4)}.
$$
相应的 primitive 轨道数 \(p_n(A_4)\)、以及 Abel/Mertens 有限部常数 \(\log\mathfrak{M}(A_4)\) 与 \(\mathsf{M}(A_4)=\gamma+\log\mathfrak{M}(A_4)\) 可按第 \ref{sec:zeta-finite-part} 节的通用接口抽取；其可复现输出见表 \ref{tab:collision_kernel_A4_primitive} 与 \ref{tab:collision_kernel_A4_finite_part}（脚本 \texttt{scripts/exp\_collision\_kernel\_A4\_primitive.py} 与 \texttt{scripts/exp\_collision\_kernel\_A4\_finite\_part.py}）。
\end{definition}

\input{sections/generated/tab_collision_kernel_A4_primitive}
\input{sections/generated/tab_collision_kernel_A4_finite_part}

\begin{remark}[常数层稀有性尺度：\texorpdfstring{$\mathsf{M}(A_4)/\mathsf{M}(A_3)$}{M(A4)/M(A3)} 的近减半]\label{rem:pom-collision-mertens-halving}
表 \ref{tab:collision_kernel_A3_finite_part} 与表 \ref{tab:collision_kernel_A4_finite_part} 给出
\(
\mathsf{M}(A_3)\approx 0.1560083
\)
与
\(
\mathsf{M}(A_4)\approx 0.0730980
\)。
因此常数层收缩比
\[
\boxed{\;
\frac{\mathsf{M}(A_4)}{\mathsf{M}(A_3)}\approx 0.4686\;}
\]
 在数值上接近一半。
该比值提供一个\textbf{独立于指数主尺度 \(r_q\)} 的细结构指纹：当把碰撞从三重提升到四重时，
除了 Perron 根 \(r_q\) 的指数尺度改变之外，primitive 主项的常数级漂移（Mertens 常数）也显著收缩。
并且表中 \texttt{tail proxy} 极小，说明该 finite-part 常数的抽取在数值上高度稳定、适合作为跨阶稀有性刻度。
\end{remark}

\subsubsection{五阶碰撞矩与 \texorpdfstring{$\mathrm{R\acute{e}nyi}$}{Renyi}--5 投影熵率}\label{subsec:pom-s5}
\begin{definition}[五阶折叠碰撞矩]\label{def:pom-s5}
定义五阶碰撞和
$$
S_5(m):=\sum_{x\in X_m} d_m(x)^5.
$$
等价地，从 \(\Omega_m\) 上均匀独立抽取五条窗口 \(\omega_1,\dots,\omega_5\)，则
$$
\mathbb{P}\bigl[\Fold_m(\omega_1)=\cdots=\Fold_m(\omega_5)\bigr]=\frac{S_5(m)}{32^m}.
$$
\end{definition}

\begin{proposition}[五阶递推、主根与 \texorpdfstring{$h_5$}{h5}]\label{prop:pom-s5-recurrence}
对一切 \(m\ge 13\) 有精确递推
$$
S_5(m)=2S_5(m-1)+11S_5(m-2)+8S_5(m-3)+20S_5(m-4)-10S_5(m-5).
$$
令 \(r_5\) 为特征多项式
$$
x^5-2x^4-11x^3-8x^2-20x+10=0
$$
的最大实根（\(r_5\approx 4.800521005045\)）。则
$$
S_5(m)=\Theta(r_5^m),
\qquad
h_5:=\lim_{m\to\infty}\frac{1}{m}\Bigl(-\log\sum_{x\in X_m}\pi_m(x)^5\Bigr)
=\log\!\left(\frac{32}{r_5}\right).
$$
\end{proposition}
\begin{proof}
递推由脚本 \texttt{scripts/exp\_fold\_collision\_moment\_recursions.py} 对枚举值逐项核验（并确定最早起始点）。其特征多项式的 Perron 根为 \(r_5\)，从而 \(S_5(m)=\Theta(r_5^m)\)。再用
$$
\sum_{x\in X_m}\pi_m(x)^5=\sum_x\frac{d_m(x)^5}{32^m}=\frac{S_5(m)}{32^m},
$$
即得 \(h_5=\log(32/r_5)\)。证毕。
\end{proof}

\begin{remark}[\texorpdfstring{$\mathrm{R\acute{e}nyi}$}{Renyi} 指纹谱：从 \texorpdfstring{$q=2,3$}{q=2,3} 到更高阶]\label{rem:pom-renyi-spectrum}
对任意整数 \(q\ge 2\) 定义
$$
S_q(m):=\sum_{x\in X_m} d_m(x)^q,\qquad
H_q(m):=-\log\sum_{x\in X_m}\pi_m(x)^q=-\log\!\left(\frac{S_q(m)}{2^{qm}}\right).
$$
对 \(q=2,3,4\)，命题 \ref{prop:pom-s2-recurrence}、\ref{prop:pom-s3-recurrence} 与 \ref{prop:pom-s4-recurrence} 给出严格有限态碰撞核与闭式熵率 \(h_q=\log(2^q/r_q)\)。
对 \(q=5,6,7,8\)，可审计地核验存在小阶整系数递推（表 \ref{tab:fold_collision_moment_recursions}；脚本 \texttt{scripts/exp\_fold\_collision\_moment\_recursions.py}），从而 \(r_q\) 与 \(h_q\) 可由递推特征多项式的 Perron 根精确抽取；其数值列于表 \ref{tab:fold_collision_renyi_spectrum}（脚本 \texttt{scripts/exp\_fold\_collision\_renyi\_spectrum.py}）。
对 \(q\ge 9\)，同样可由经核验的整系数递推直接给出精确 \(r_q\)（从而精确 \(h_q\)），并已列于表 \ref{tab:fold_collision_renyi_spectrum}。
\par
若需标准 Rényi 熵率的归一化口径，可取 \(h_q^{\mathrm{R}}:=h_q/(q-1)\)（与 \S\ref{subsec:pom-higher-collision-spectrum} 中的定义一致）。
\end{remark}

\input{sections/08_projection_ontology_mathematics/08_projection_ontology_mathematics_part03_collision_gibbs_extensions.tex}

\begin{proposition}[\texorpdfstring{$h_q$}{hq} 的几何化：投影输出测度的 Rényi 维数谱]\label{prop:pom-renyi-dimension-spectrum}
沿用上文 \(\pi_m(x)=d_m(x)/2^m\) 的输出分布记号。把分辨率 \(m\) 与几何尺度对应为
\[
\varepsilon_m:=\varphi^{-m}.
\]
定义输出测度在尺度 \(\varepsilon_m\) 下的 \(q\)-阶 Rényi 维数近似为
\[
D_q(m):=\frac{1}{q-1}\cdot \frac{\log\sum_{x\in X_m}\pi_m(x)^q}{\log \varepsilon_m}
\qquad(q\ge 2).
\]
若 \(h_q=\lim_{m\to\infty}\frac{1}{m}H_q(m)\) 存在（例如当 \(S_q(m)=\Theta(r_q^m)\) 时成立），则极限
\[
D_q:=\lim_{m\to\infty}D_q(m)
\]
存在且满足闭式
\[
\boxed{\;
D_q=\frac{h_q}{(q-1)\log\varphi}\; }.
\]
\end{proposition}
\begin{proof}
由定义 \(H_q(m)=-\log\sum_x\pi_m(x)^q\) 与 \(\log\varepsilon_m=-m\log\varphi\) 得
\[
D_q(m)=\frac{1}{q-1}\cdot \frac{-H_q(m)}{-m\log\varphi}
=\frac{1}{(q-1)\log\varphi}\cdot \frac{1}{m}H_q(m).
\]
令 \(m\to\infty\) 即得结论。证毕。
\end{proof}

\begin{remark}[可审计输出：\(D_q\) 由表中 \(h_q\) 直接读出]\label{rem:pom-renyi-dimension-values}
命题 \ref{prop:pom-renyi-dimension-spectrum} 表明：一旦 \(h_q\) 可由碰撞核/递推谱半径抽取，则几何维数谱 \(D_q\) 无需额外假设即可直接由闭式给出。
由表 \ref{tab:fold_collision_renyi_spectrum} 可见 \(D_q\) 在已核验范围内非常接近 \(1\) 且随 \(q\) 缓慢下降；这可理解为：高阶矩更偏向大纤维（极少数 \(x\) 的 \(d_m(x)\) 主导），从而“内空间厚度”在 Rényi 意义下逐步变薄。
例如由表中精确 \(h_q\)（或等价的 \(r_q\)）直接代入
\(
D_q=h_q/((q-1)\log\varphi)
\)
得到
\[
D_3\approx 0.9897,\qquad D_4\approx 0.9875,\qquad D_5\approx 0.9855,
\]
从而“近均匀但随阶数缓慢变薄”的弱多重分形趋势在数值上清晰可见。
\par
更定量地，顶层纤维谱缺口（推论 \ref{cor:pom-second-max-gap-constant}）直接给出“次大层”相对“最大层”的显式衰减率。
令
\(
\varepsilon_m:=1-\frac{D_m^{(2)}}{D_m}
\)。
则在大 \(m\) 的奇偶分支上分别有极限
\[
\varepsilon_{2k}\to \varphi^{-6},
\qquad
\varepsilon_{2k+1}\to \tfrac12\varphi^{-5}.
\]
因此对固定足够大的 \(m\)，随 \(q\) 增大有
\[
\left(\frac{D_m^{(2)}}{D_m}\right)^q
=(1-\varepsilon_m)^q
=\exp\!\bigl(q\log(1-\varepsilon_m)\bigr)
\approx \exp(-q\varepsilon_m),
\]
从而“第二层”在 \(e^{-1}\) 精度下的交叉阶约为
\[
q_{\ast,\mathrm{even}}\approx \varphi^{6}\approx 18,
\qquad
q_{\ast,\mathrm{odd}}\approx 2\varphi^{5}\approx 22.
\]
这把“高阶矩由极少数大纤维主导”的经验指纹转写为一个可检验的量化预测：当继续把可审计递推推进到 \(q>17\) 时，\(q\in[18,22]\) 附近应出现更强的顶端支配（从而更明显的低维有效行为/压缩现象）。
\end{remark}

\input{sections/generated/tab_fold_collision_renyi_spectrum}

\begin{remark}[扩展指纹（可复核）：\texorpdfstring{$q\le 20$}{q<=20} 的 \texorpdfstring{$r_q$}{rq} 与 \texorpdfstring{$h_q$}{hq}]\label{rem:pom-renyi-spectrum-q20}
为把 Rényi 指纹谱推进到更高阶，我们用模 \(F_{m+2}\) 的 residue DP 直接计算
\(
S_q(m)=\sum_{r\in\ZZ/F_{m+2}\ZZ}c_m(r)^q
\)
并由比值收敛（Aitken \(\Delta^2\)）估计 \(r_q\)。
表 \ref{tab:fold_collision_renyi_spectrum_mod_dp_q20} 给出 \(q\le 20\) 的可复核扩展输出；其中 \(q\le 16\) 与由递推/有限核抽取的精确 \(r_q\) 在数值上吻合到表格精度，\(q\ge 17\) 则给出 DP 窗口 \(m\le 30\) 下的稳定估计值（脚本 \texttt{scripts/exp\_fold\_collision\_renyi\_spectrum\_mod\_dp\_q20.py} 一键生成）。
\end{remark}

\begin{remark}[为何 \(m\le 30\) 的 DP 窗口足够：由特征多项式抽取的谱隙给出几何衰减（可复核）]\label{rem:pom-dp-window-spectral-gap}
在已存在最小递推的情形下（例如 \(q\le 23\) 的已核验窗口内，见表 \ref{tab:fold_collision_kernel_rh_scan_q2_17} 与表 \ref{tab:fold_collision_kernel_rh_scan_q18_23}），记 \(r_q\) 为 Perron 根，\(\Lambda_q=\max_{\mu\ne r_q}|\mu|\) 为次谱模。
则在一般位置（主根简单且主模态系数非零）下，比值估计 \(S_q(m+1)/S_q(m)\to r_q\) 的主误差项满足几何衰减率
\[
\left|\frac{S_q(m+1)}{S_q(m)}-r_q\right|=O\!\left(\left(\frac{\Lambda_q}{r_q}\right)^{m}\right),
\]
而 Aitken \(\Delta^2\) 会把主几何项进一步压低到更高阶。
\par
在共振区 \(q=9,\dots,17\) 内，达到 \(\Lambda_q\) 的次主导根在数值上稳定为负实根，从而比值误差具有二周期交替结构；此时 Aitken \(\Delta^2\) 的作用可被严格解释为\textbf{消去该交替主误差项}，并把主控衰减尺度从 \(\Lambda_q/r_q\) 推进到由第三模指纹 \(\delta_{\mathrm{ait}}(q)\) 控制（推论 \ref{cor:pom-aitken-cancels-two-cycle}；表 \ref{tab:fold_collision_resonance_aitken_certificate_q9_17}）。
\par
对本文关心的 DP 扩展区间 \(q\le 20\)，由表中可直接读出 \(\Lambda_q/r_q\) 仍显著小于 \(1\)：例如
\[
\frac{\Lambda_{15}}{r_{15}}\approx 0.6918,\qquad
\frac{\Lambda_{20}}{r_{20}}\approx 0.7590,
\]
从而在窗口端点 \(m\approx 30\) 处几何项分别约为 \((0.69)^{30}\approx 1.5\times 10^{-5}\) 与 \((0.759)^{30}\approx 2.6\times 10^{-4}\)。
这给出一个完全由已审计特征多项式导出的解释：为什么在 \(m\in[24,30]\) 的 residue DP 窗口上，简单的“比值收敛 + Aitken \(\Delta^2\)”就足以产出稳定的 \(r_q\) 指纹估计（并与表 \ref{tab:fold_collision_renyi_spectrum_mod_dp_q20} 的观测精度一致）。
\end{remark}
\input{sections/generated/tab_fold_collision_renyi_spectrum_mod_dp_q20}

\begin{remark}[端点收敛速度（可复核）：\texorpdfstring{$q\le 60$}{q<=60} 时 \texorpdfstring{$h_q/q\to\log(2/\sqrt{\varphi})$}{hq/q->log(2/sqrt(phi))}]\label{rem:pom-renyi-endpoint-q60}
命题 \ref{prop:pom-rq-qinfty-endpoint} 给出端点常数
\(
\lim_{q\to\infty}h_q/q=\log(2/\sqrt{\varphi})
\)
以及 \(O(1/q)\) 级误差界。
表 \ref{tab:fold_collision_renyi_endpoint_convergence_q60} 用模 \(F_{m+2}\) 的 residue DP 在窗口 \(m\in[24,30]\) 上计算 \(S_q(m)=\sum_r c_m(r)^q\)，并对 \(\log S_q(m)\) 作线性回归估计 \(\log r_q\)，从而得到 \(h_q/q\) 的数值证书：
当 \(q=60\) 时，\(\log(2/\sqrt{\varphi})-(h_q/q)\) 已降至 \(10^{-4}\) 量级，与端点误差界的 \(O(1/q)\) 形状一致。
\end{remark}
\input{sections/generated/tab_fold_collision_renyi_endpoint_convergence_q60}

\begin{remark}[自由能超额 \texorpdfstring{$g_q$}{gq} 的快速衰减（可复核数值律）]\label{rem:pom-free-energy-excess-gq}
为定量刻画 \(r_q\) 到端点尺度 \(\varphi^{q/2}\) 的收敛速度，引入“自由能超额”（free-energy excess）
\[
g_q:=\log r_q-\frac{q}{2}\log\varphi
= q\Bigl(\log\!\left(\frac{2}{\sqrt{\varphi}}\right)-\frac{h_q}{q}\Bigr).
\]
注意表 \ref{tab:fold_collision_renyi_endpoint_convergence_q60} 的 \texttt{gap} 列正是 \(g_q/q\)。
因此在已核验窗口 \(m\in[24,30]\) 上可直接得到
\[
g_{20}\approx 0.1724,\quad g_{30}\approx 0.0939,\quad g_{40}\approx 0.0444,\quad g_{60}\approx 0.00834.
\]
特别地，
\[
\frac{r_{60}}{\varphi^{30}}=\exp(g_{60})\approx 1.0084,
\]
即到 \(q=60\) 时相对端点尺度的乘性误差已在 \(1\%\) 内。
这远强于命题 \ref{prop:pom-rq-qinfty-endpoint} 给出的普适包络 \(g_q\le \log\varphi\)（例如只会给出 \(g_{60}\le\log\varphi\approx 0.48\)）。
\par
在目前四个审计点上，\(g_q\) 的衰减呈现出明显快于 \(1/q\) 的趋势。
一个保守的指数包络（仅用这四点即可验证覆盖）为
\[
g_q\ \le\ 0.1724\,e^{-0.06(q-20)}\qquad(q\ge 20).
\]
该现象在本文中不作为定理使用，而应被视为一个带审计 provenance 的可追踪数值律：随着更大 \(q\) 的端点表扩展，可以直接复核/证伪其衰减型态。
\end{remark}

\begin{remark}[端点残差的 \texorpdfstring{$q^{-4}$}{q^-4} 超收敛迹象：以 \texttt{gap} 为幂次指纹（可复核）]\label{rem:pom-endpoint-gap-qminus4}
令端点常数
\[
L_\infty:=\log\!\left(\frac{2}{\sqrt{\varphi}}\right),
\qquad
\mathrm{gap}_q:=L_\infty-\frac{h_q}{q}=\frac{g_q}{q},
\]
其中 \(\mathrm{gap}_q\) 正是表 \ref{tab:fold_collision_renyi_endpoint_convergence_q60} 的 \texttt{gap} 列。
从该表可直接读出
\(
\mathrm{gap}_{20}=8.62\times 10^{-3}
\)
与
\(
\mathrm{gap}_{60}=1.39\times 10^{-4}
\)。
因此尺度诊断量
\(
q^4\mathrm{gap}_q
\)
在两端点上分别为
\[
20^4\mathrm{gap}_{20}\approx 1.38\times 10^3,
\qquad
60^4\mathrm{gap}_{60}\approx 1.80\times 10^3,
\]
落在同一数量级。
这给出一个强烈的数值指示：在当前折叠系统的已审计区间内，端点残差更接近 \(q^{-4}\) 级而非“普通的” \(q^{-1}\) 慢收敛；等价地 \(g_q=q\,\mathrm{gap}_q\) 更接近 \(q^{-3}\) 标度。
该观察不作为定理使用，但它提供了一个可随更高 \(q\) 端点表扩展而直接复核/证伪的衰减幂次指纹。
\end{remark}

\begin{remark}[共振坍缩与端点幂次的同步：低阶修正项的结构性消去（可证伪预测）]\label{rem:pom-endpoint-gap-resonance-link}
在 \(q\ge 9\) 的已核验窗口内，moment-kernel 的最小实现维数出现结构性坍缩/共振（定理 \ref{thm:pom-moment-resonance}；表 \ref{tab:fold_collision_resonance_gap_delta_q_9_17} 与表 \ref{tab:fold_collision_resonance_nullmodes_q9_17}），这意味着可见模态空间中存在额外的线性约束（其显式整数证书由 Hankel-null 输出给出）。
把这一事实与上面的端点幂次指纹放在一起，一个自然的机制解释是：这些额外约束会消去若干原本贡献端点误差的低阶修正通道，从而把 \(\mathrm{gap}_q\) 的首个非零修正阶推高，表现为更快的 \(q^{-4}\) 级衰减。
\par
相应地得到一个新的、可直接检验的预测：
\[
\boxed{\;
\text{若在某个折叠门/参数变体中共振缺口消失（无额外约束），则 }\mathrm{gap}_q\text{ 的衰减幂次应退化回 }q^{-1}\text{ 或 }q^{-2}\text{ 级。}\;}
\]
因此“端点残差的衰减幂次”本身可作为折叠门是否触发该类共振约束的结构指纹之一。
\end{remark}

\begin{remark}[端点平台指纹 \texorpdfstring{$C_q$}{Cq} 与两点外推器：不增大 \texorpdfstring{$m$}{m} 窗口的常数层自校验]\label{rem:pom-endpoint-extrapolator}
定义端点平台指纹
\[
C_q:=q^4\,\mathrm{gap}_q
=q^4\left(L_\infty-\frac{h_q}{q}\right).
\]
若 \(\mathrm{gap}_q\approx C/q^4\) 成立，则 \(C_q\) 在中高阶 \(q\) 上应进入平台（仅剩小幅振荡）。
在该模型下，用两点 \((p,q)\) 可消去未知常数 \(C\)，得到端点外推器
\[
\boxed{\;
\widehat L_\infty(p,q)
:=\frac{q^4\,(h_q/q)-p^4\,(h_p/p)}{q^4-p^4}\; }.
\]
该外推器仅依赖 \(h_q/q\) 的表格读数，并可在同一审计窗口上做端点常数层的交叉一致性检查：
例如由表 \ref{tab:fold_collision_renyi_endpoint_convergence_q60} 的 \((p,q)=(20,60)\) 得
\(
\widehat L_\infty(20,60)\approx 0.4525081
\)，
与同表的 \(L_\infty=0.4525412680\) 已相差 \(3.3\times 10^{-5}\) 量级。
同时也可用表 \ref{tab:fold_collision_renyi_spectrum_mod_dp_q20} 的 \(q\le 20\) 输出构造若干 \(\widehat L_\infty(p,q)\)，作为端点常数层的内部自洽诊断量。
\end{remark}

\begin{proposition}[普适夹逼：高阶碰撞主指数与投影熵率仅由 \texorpdfstring{$|X_m|$}{|Xm|} 与最大纤维锁定]\label{prop:pom-rq-universal-bounds}
对整数 \(q\ge 2\)，令
$$
S_q(m):=\sum_{x\in X_m} d_m(x)^q,\qquad d_m(x):=\bigl|\Fold_m^{-1}(x)\bigr|.
$$
并令 \(r_q\) 表示 \(S_q(m)\) 的指数增长率（例如定理 \ref{thm:pom-collision-kernel-ak} 给出的 Perron 根；等价地 \(r_q=\lim_{m\to\infty}S_q(m)^{1/m}\)），定义未归一化 \(q\)-阶投影熵率
$$
h_q:=\log\!\left(\frac{2^q}{r_q}\right)=q\log 2-\log r_q.
$$
则有两侧界
$$
\frac{2^q}{\varphi^{q-1}}\ \le\ r_q\ \le\ 2\,\varphi^{(q-1)/2},
$$
等价地
$$
(q-1)\log\!\left(\frac{2}{\sqrt{\varphi}}\right)\ \le\ h_q\ \le\ (q-1)\log\varphi.
$$
\end{proposition}
\begin{proof}
对任意固定 \(m\) 与 \(q\ge 2\)，由幂均值/Holder 不等式，
$$
S_q(m)=\sum_{x\in X_m}d_m(x)^q\ \ge\ \frac{\bigl(\sum_x d_m(x)\bigr)^q}{|X_m|^{\,q-1}}
=\frac{2^{qm}}{F_{m+2}^{\,q-1}},
$$
其中 \(\sum_x d_m(x)=|\Omega_m|=2^m\) 且 \(|X_m|=F_{m+2}\)。
另一方面令 \(D_m:=\max_{x\in X_m} d_m(x)\)，则
$$
S_q(m)=\sum_x d_m(x)^q\ \le\ D_m^{\,q-1}\sum_x d_m(x)=2^m D_m^{\,q-1}.
$$
两式取对数并除以 \(m\)，再令 \(m\to\infty\)，结合 Fibonacci 渐近 \(F_{m+2}\sim \varphi^{m}/\sqrt5\) 与最大纤维渐近 \(D_m=\Theta(\varphi^{m/2})\)（定理 \ref{thm:pom-max-fiber}），得到
$$
\log r_q=\lim_{m\to\infty}\frac{1}{m}\log S_q(m)\in\Bigl[q\log 2-(q-1)\log\varphi,\ \log 2+\frac{q-1}{2}\log\varphi\Bigr],
$$
从而 \(r_q\) 的界成立。代入 \(h_q=q\log 2-\log r_q\) 即得 \(h_q\) 的等价界。证毕。
\end{proof}

\begin{proposition}[极端阶端点：\texorpdfstring{$r_q^{1/q}\to\sqrt{\varphi}$}{rq^(1/q)->sqrt(phi)} 与归一化熵端点常数]\label{prop:pom-rq-qinfty-endpoint}
令 \(r_q\) 表示 \(S_q(m)\) 的指数增长率（例如由 moment-kernel 的 Perron 根给出），并记 \(\varphi\) 为黄金比例。
则当 \(q\to\infty\) 时有极限
\[
\boxed{\;
\lim_{q\to\infty} r_q^{1/q}=\sqrt{\varphi}\; }.
\]
等价地，
\[
\lim_{q\to\infty}\frac{1}{q}\,h_q
=\log\!\left(\frac{2}{\sqrt{\varphi}}\right),
\qquad
\lim_{q\to\infty}\frac{1}{q-1}\,h_q
=\log\!\left(\frac{2}{\sqrt{\varphi}}\right),
\]
从而 Rényi 维数谱端点满足
\[
\lim_{q\to\infty}D_q
=\frac{\log\!\left(2/\sqrt{\varphi}\right)}{\log\varphi}
\]
（其中 \(D_q\) 见命题 \ref{prop:pom-renyi-dimension-spectrum}）。
\par
并且对任意整数 \(q\ge 2\) 有显式误差界
\[
\boxed{\;
0\ \le\ \log\!\left(\frac{2}{\sqrt{\varphi}}\right)-\frac{h_q}{q}\ \le\ \frac{\log\varphi}{q}\; }.
\]
\end{proposition}
\begin{proof}
对任意 \(m,q\ge 2\)，由
\(
D_m:=\max_x d_m(x)
\)
得两侧夹逼
\[
D_m^q\ \le\ S_q(m)\ \le\ |X_m|\,D_m^q.
\]
两端取 \(1/(mq)\) 次方，得到
\[
D_m^{1/m}\ \le\ S_q(m)^{1/(mq)}\ \le\ D_m^{1/m}\,|X_m|^{1/(mq)}.
\]
令 \(m\to\infty\)，左端由定理 \ref{thm:pom-max-fiber} 给出
\(
\lim_{m\to\infty}D_m^{1/m}=\varphi^{1/2}
\)；
右端的因子满足
\(
\lim_{m\to\infty}|X_m|^{1/(mq)}=\varphi^{1/q}
\)
（因为 \(|X_m|=F_{m+2}\sim \varphi^m/\sqrt5\)）。
因此对每个固定 \(q\ge 2\) 有
\[
\sqrt{\varphi}\ \le\ r_q^{1/q}\ \le\ \sqrt{\varphi}\,\varphi^{1/q}.
\]
令 \(q\to\infty\) 即得 \(\lim_{q\to\infty}r_q^{1/q}=\sqrt{\varphi}\)。
最后由 \(h_q=q\log 2-\log r_q\) 与命题 \ref{prop:pom-renyi-dimension-spectrum} 的闭式，直接得到各端点极限。
\par
由 \(\sqrt{\varphi}\le r_q^{1/q}\le \sqrt{\varphi}\,\varphi^{1/q}\) 得
\[
\frac{1}{2}\log\varphi\ \le\ \frac{1}{q}\log r_q\ \le\ \frac{1}{2}\log\varphi+\frac{1}{q}\log\varphi.
\]
代入 \(\frac{h_q}{q}=\log 2-\frac{1}{q}\log r_q\)，即得
\[
0\ \le\ \log\!\left(\frac{2}{\sqrt{\varphi}}\right)-\frac{h_q}{q}\ \le\ \frac{\log\varphi}{q}.
\]
证毕。
\end{proof}

\begin{remark}[由碰撞谱自洽重建 \texorpdfstring{$\varphi$}{phi}（无需 Fibonacci 输入）]\label{rem:pom-recover-phi-from-collision-spectrum}
表 \ref{tab:fold_collision_renyi_spectrum}（以及其更高阶扩展表）给出经 DP/递推审计的 \(r_q\)（从而 \(h_q=\log(2^q/r_q)\)）。
令
\[
s_q:=\frac{h_q}{q}=\log 2-\frac{1}{q}\log r_q,
\qquad
g_q:=e^{s_q}=\frac{2}{r_q^{1/q}}.
\]
由端点极限 \(\lim_{q\to\infty}s_q=\log(2/\sqrt{\varphi})\) 可得一个完全内部的“反解常数”恒等式：
\[
\boxed{\;
\varphi
=4e^{-2\lim_{q\to\infty}s_q}
=\left(\lim_{q\to\infty}r_q^{1/q}\right)^2\; }.
\]
工程上，为避免 \(q\)-方向收敛偏慢，可对序列 \(s_q\) 做 Aitken \(\Delta^2\) 加速（与我们在 \(m\)-比值上使用的加速完全平行）：
\[
s_q^{(\infty)}
\approx
s_{q+2}-\frac{(s_{q+2}-s_{q+1})^2}{s_{q+2}-2s_{q+1}+s_q},
\qquad
\widehat\varphi_q:=4e^{-2s_q^{(\infty)}}.
\]
表 \ref{tab:fold_collision_phi_reconstruction_from_renyi} 给出仅用已审计 \(\{r_q\}\) 反演得到的 \(\widehat\varphi_q\) 的收敛示例（脚本 \texttt{scripts/exp\_fold\_collision\_phi\_reconstruction\_from\_renyi.py} 一键生成）。
\end{remark}

\input{sections/generated/tab_fold_collision_phi_reconstruction_from_renyi}

\begin{proposition}[压力的凸性与指纹的凹性]\label{prop:pom-pressure-convexity}
对任意 \(m\ge 2\)，函数 \(q\mapsto \log S_q(m)\) 在 \(q\ge 0\) 上为凸函数。
因此在 \(S_q(m)=\Theta(r_q^m)\)（例如由命题 \ref{prop:pom-moment-kernel-exists} 的 Perron 主尺度保证）时，压力
\[
P(q):=\log r_q
\]
在整数点上满足离散凸性：对任意整数 \(q_0<q_1<q_2\) 有
\[
\boxed{\;
P(q_1)\ \le\ \frac{q_2-q_1}{q_2-q_0}\,P(q_0)\ +\ \frac{q_1-q_0}{q_2-q_0}\,P(q_2)\; }.
\]
等价地，未归一化 Rényi 指纹 \(h_q=q\log 2-P(q)\) 在整数点上为凹的：
\[
\boxed{\;
h_{q_1}\ \ge\ \frac{q_2-q_1}{q_2-q_0}\,h_{q_0}\ +\ \frac{q_1-q_0}{q_2-q_0}\,h_{q_2}\; }.
\]
\end{proposition}
\begin{proof}
固定 \(m\)。取任意 \(q_0,q_2\ge 0\) 与 \(\theta\in(0,1)\)，令 \(q_1=\theta q_0+(1-\theta)q_2\)。
由 Hölder 不等式（或 log-sum-exp 的凸性）得
\[
S_{q_1}(m)=\sum_{x\in X_m}d_m(x)^{q_1}
\le
\left(\sum_{x\in X_m}d_m(x)^{q_0}\right)^{\theta}
\left(\sum_{x\in X_m}d_m(x)^{q_2}\right)^{1-\theta}
=S_{q_0}(m)^{\theta}S_{q_2}(m)^{1-\theta}.
\]
取对数即得 \(q\mapsto \log S_q(m)\) 的凸性。
\par
令 \(q_0<q_1<q_2\) 为整数并取
\(
\theta=\frac{q_2-q_1}{q_2-q_0}
\)
使得 \(q_1=\theta q_0+(1-\theta)q_2\)。
对上式取 \(\frac{1}{m}\log\) 并令 \(m\to\infty\)，得到
\[
\log r_{q_1}\le \theta\log r_{q_0}+(1-\theta)\log r_{q_2},
\]
即 \(P\) 的离散凸性。最后 \(h_q=q\log 2-P(q)\) 为线性函数减去凸函数，从而在整数点上为凹。证毕。
\end{proof}

\begin{proposition}[跨 \texorpdfstring{$q$}{q} 不等式：\texorpdfstring{$g(q)$}{g(q)} 的单调性]\label{prop:pom-crossq-g-monotone}
对任意整数 \(m\ge 2\) 与 \(2\le p<q\)，令 \(d_m=(d_m(x))_{x\in X_m}\) 并记 \(S_k(m)=\sum_x d_m(x)^k=\|d_m\|_k^k\)。
则有限维范数不等式给出
\[
\|d_m\|_p\ \le\ |X_m|^{\frac{1}{p}-\frac{1}{q}}\ \|d_m\|_q,
\]
从而
\[
S_q(m)\ \ge\ S_p(m)^{q/p}\,|X_m|^{1-q/p}.
\]
若存在 \(r_k\) 使 \(S_k(m)=\Theta(r_k^m)\)，并且 \(|X_m|\sim \varphi^m\)，则有跨阶约束
\[
\boxed{\;
\log r_q \ \ge\ \frac{q}{p}\log r_p + \left(1-\frac{q}{p}\right)\log\varphi\; }.
\]
等价地，定义
\[
g(q):=\frac{\log r_q-\log\varphi}{q},
\]
则 \(g(q)\) 在 \(q\ge 2\) 上单调不减。
表 \ref{tab:fold_collision_crossq_g_profile_q2_23} 给出 \(q=2,\dots,23\) 的可复算读数，可作为后续扩展/重实现的回归一致性检查（脚本 \texttt{scripts/exp\_fold\_collision\_crossq\_g\_profile\_q2\_23.py} 一键生成）。
\end{proposition}
\begin{proof}
对长度 \(n=|X_m|\) 的非负向量 \(d_m\)，当 \(2\le p<q\) 时有 \(\|d_m\|_p\le n^{1/p-1/q}\|d_m\|_q\)。
两边取 \(q\) 次幂得到
\(
\|d_m\|_q^q \ge \|d_m\|_p^q\,n^{1-q/p}
\)，
即
\(
S_q(m)\ge S_p(m)^{q/p}|X_m|^{1-q/p}
\)。
对两边取 \(\frac{1}{m}\log\) 并令 \(m\to\infty\)，利用
\(
\lim_{m\to\infty}\frac{1}{m}\log S_k(m)=\log r_k
\)
与
\(
\lim_{m\to\infty}\frac{1}{m}\log|X_m|=\log\varphi
\)
即得第一式；再代回定义得到 \(g(q)\) 的单调性。证毕。
\end{proof}

\input{sections/generated/tab_fold_collision_crossq_g_profile_q2_23}

\begin{remark}[更高阶碰撞矩的精确递推与阶数规律（可审计）]\label{rem:pom-moment-rec}
二阶与三阶碰撞矩分别由三态核 \(A_2,A_3\) 实现（命题 \ref{prop:pom-s2-recurrence} 与 \ref{prop:pom-s3-recurrence}）。更高阶 \(q\) 下，序列 \(\{S_q(m)\}\) 仍呈现出\textbf{小阶整数线性递推}的现象：对 \(q=4,5,6,7,8\) 可验证存在阶数分别为 \(5,5,7,7,9\) 的精确递推（从某个 \(m\) 起对所有 \(m\) 成立），其系数见表 \ref{tab:fold_collision_moment_recursions}。这些递推等价于“存在有限维的 \(q\)-阶碰撞核/矩阵 realization”，从而 \(S_q(m)=\Theta(r_q^m)\) 的指数尺度由对应特征多项式的 Perron 根 \(r_q\) 控制。
对 \(q=2,\dots,8\)，递推阶数与 \(r_q,h_q\) 的汇总见表 \ref{tab:fold_collision_moment_spectrum_k2_8}。
\input{sections/generated/tab_fold_collision_moment_spectrum_k2_8}

在已核验范围 \(q\le 8\) 内，上述最小递推阶数与 Hankel 最小实现维数均呈现出“每两阶增加 \(2\)”的规律（见定理 \ref{thm:pom-moment-hankel} 与推论 \ref{cor:pom-moment-minreal}）。
把它整理为一个可直接核验的\textbf{闭包律}：记 \(d_q\) 为 \(S_q(m)\) 的最小整系数递推阶数（等价地最小线性 realization 维数），则在 \(q=2,\dots,8\) 上有
\[
\boxed{\;
d_q = 2\left\lfloor \frac{q}{2}\right\rfloor+1\;}
\]
（等价地：奇 \(q\) 时 \(d_q=q\)，偶 \(q\) 时 \(d_q=q+1\)）。
工程上这给出一个可审计的复杂度承诺：在非共振典型窗口中，推进到更高阶只需要 \(O(q)\) 级状态增长（每两阶仅增加 2 个自由度），而非指数级爆炸。
然而当 \(q\) 继续增大时，该规律出现\textbf{结构性坍缩/共振}：对 \(q=9,\dots,17\)，在模 \(F_{m+2}\) 的审计窗口 \(m\le 26\)（并在额外窗口 \(m\le 32\) 上作点检）上可验证存在更低阶的整系数递推（表 \ref{tab:fold_collision_moment_recursions_9_17}），其最小阶数
\[
(d_9,d_{10},d_{11},d_{12},d_{13},d_{14},d_{15},d_{16},d_{17})=(7,9,9,13,11,13,11,13,13),
\]
明显低于“每两阶增加 2”的线性外推。该现象表明“闭合维数指纹”出现稳定的秩缺口，其机制与 Fibonacci 同余结构的代数退化相关。共振的机制化判别见定理 \ref{thm:pom-moment-resonance}。
\end{remark}

\begin{corollary}[闭合维数外推在 \texorpdfstring{$q\ge 9$}{q>=9} 处失效（反例族）]\label{cor:pom-moment-dim-collapse}
令 \(d_q\) 表示 \(S_q(m)\) 的最小整系数递推阶数（等价地最小线性 realization 维数；亦即 Hankel 最小实现维数，见定理 \ref{thm:hankel-rank-minimal}）。
则在已核验范围 \(q=9,\dots,23\) 内，有严格不等式
\[
\boxed{\;
d_q\ <\ 2\lfloor q/2\rfloor+1\; }.
\]
更具体地，
\[
(d_9,\dots,d_{23})=(7,9,9,13,11,13,11,13,13,17,15,17,15,17,17),
\]
见表 \ref{tab:fold_collision_moment_recursions_9_17} 与表 \ref{tab:fold_collision_moment_recursions_18_23}。
因此任何将 \(2\lfloor q/2\rfloor+1\) 视作对所有 \(q\) 的“最小闭合维数规律”的强形式陈述均不成立；该维数律仅描述了非共振典型情形下的上界饱和模式（注 \ref{rem:pom-closure-dim-mode-trunc}）。
\end{corollary}
\begin{definition}[闭合缺陷 \texorpdfstring{$\Delta_q$}{Deltaq}：共振/选择定则的不变量化]\label{def:pom-closure-defect-deltaq}
令 \(h=\lfloor q/2\rfloor\)。
定义\textbf{闭合缺陷}（closure defect）
\[
\boxed{\;
\Delta_q\ :=\ (2h+1)-d_q\;}
\]
其中 \(2h+1\) 为“差分模态截断”下的名义模态数（注 \ref{rem:pom-closure-dim-mode-trunc}），而 \(d_q\) 为最小递推阶数/最小 realization 维数（推论 \ref{cor:pom-moment-dim-collapse}）。
\end{definition}
\begin{remark}[解释：\texorpdfstring{$\Delta_q>0$}{Deltaq>0} 即“选择定则”，并给出可量化降维增益]\label{rem:pom-closure-defect-selection-rule}
当 \(\Delta_q>0\) 时，定理 \ref{thm:pom-moment-resonance} 表明存在非平凡的模态约束使得名义 \(2h+1\) 维模态 realization 不是最小实现；因此 \(\Delta_q\) 可被视为“可激发模态空间的余维”，即共振/坍缩触发的\emph{选择定则强度}。
\par
工程上，这直接量化了对 \(q\)-阶谱后端的可压缩增益：若某实现以 \(2h+1\) 个差分模态为状态闭包推进，则在共振相区可进一步压到 \(d_q\) 维最小实现（由递推/伴随矩阵或 Hankel 最小化恢复），其名义压缩比约为
\[
\mathrm{gain}(q)\ \approx\ \frac{2h+1}{d_q}.
\]
例如 \(q=15\) 时，\(2h+1=15\) 而已核验 \(d_{15}=11\)，对应一个稳定的结构性降维缺口。
\end{remark}
\begin{proof}[推论 \ref{cor:pom-moment-dim-collapse} 的证明]
由表 \ref{tab:fold_collision_moment_recursions_9_17} 与表 \ref{tab:fold_collision_moment_recursions_18_23} 的已核验最小递推阶数直接比较即可。证毕。
\end{proof}

\begin{remark}[闭合维数 \texorpdfstring{$2\lfloor q/2\rfloor+1$}{2 floor(q/2)+1} 的结构来源：同余角色模态的有限截断]\label{rem:pom-closure-dim-mode-trunc}
命题 \ref{prop:pom-s2-plancherel} 给出一个把“几何—同余—谱”焊接在一起的可审计模板：二阶碰撞矩
\(
S_2(m)=\sum_r c_m(r)^2
\)
既是同余切片上 residue 分布的 \(\ell^2\) 质量，也是其离散傅里叶系数的 Plancherel 能量平均；并且每个频率 \(j\) 上的傅里叶系数 \(\widehat c_m(j)\) 由局部因子 \(\prod_k(1+\chi_j(F_{k+1}))\) 完全决定。

在更高阶 \(q\) 的 moment-kernel 编译（命题 \ref{prop:pom-moment-kernel-exists}）中，本质上是在同步直积上施加“逐位输出一致”的约束；该约束把 \(q\) 份拷贝的相位信息压缩为一个\textbf{有限取值的差分模态索引}：它记录“相位在共轭配对下的净偏置”（可理解为 \(\widehat c\) 与 \(\overline{\widehat c}\) 的幂次差 \(a-b\) 的有效范围）。由配对/共轭不变性，净偏置的可取范围被强制截断为
\[
-(\lfloor q/2\rfloor)\ \le\ a-b\ \le\ \lfloor q/2\rfloor,
\]
因此在\emph{非共振的典型情形}下，只需跟踪 \(2\lfloor q/2\rfloor+1\) 个模态即可闭合地推进到下一步分辨率；这给出了“每两阶增加 2 个自由度”的结构原因。该模态截断与 Hankel 秩下界（定理 \ref{thm:pom-moment-hankel}）在已核验范围 \(q\le 8\) 内精确吻合，从而把经验指纹升级为可解释的有限闭包机制。
而表 \ref{tab:fold_collision_moment_recursions_9_17} 的 \(q\ge 9\) 现象表明：当 Fibonacci 同余结构诱发代数退化时，可见模态空间会出现额外约束，使得读出所能激发的子空间维数严格小于名义上界（推论 \ref{cor:pom-moment-dim-collapse}）。因此更关键的统一对象不再是“继续提高 \(q\)”，而是刻画何时、以及为何出现不可激发的模态（见猜想 \ref{conj:pom-moment-visible} 与定理 \ref{thm:pom-moment-resonance} 的模态约束口径）。
\end{remark}

\input{sections/generated/tab_fold_collision_moment_recursions}

\input{sections/generated/tab_fold_collision_moment_recursions_9_17}

\input{sections/generated/tab_fold_collision_moment_recursions_18_23}

\paragraph{更高阶矩谱常数（\texorpdfstring{$q=9,\dots,23$}{q=9..23}）：Perron 根与熵率}
表 \ref{tab:fold_collision_moment_recursions_9_17} 与表 \ref{tab:fold_collision_moment_recursions_18_23} 给出 \(q=9,\dots,23\) 的已核验最小递推。
由递推特征多项式的 Perron 根可抽取指数尺度 \(r_q\) 及相应的（非归一化）Rényi 指纹率
\(
h_q=\log(2^q/r_q)
\)
（定义 \ref{def:pom-moment-pressure}）。
下表汇总 \(q=9,\dots,23\) 的 \(r_q\)、\(r_q^{1/q}\) 与 \(h_q\)：
\input{sections/generated/tab_fold_collision_moment_spectrum_k9_17}
\input{sections/generated/tab_fold_collision_moment_spectrum_k18_23}

\begin{remark}[更高阶 DP 端点指纹（\(q=24,25\)）]\label{rem:pom-renyi-endpoint-q24-25}
在更高的 Rényi 阶上，可通过 residue DP 直接计算 \(S_q(m)\)，并用比值收敛与 Aitken \(\Delta^2\) 加速在固定窗口上估计 \(r_q\)。
例如在 \(m\in\{34,35,36,37\}\) 的窗口上得到稳定估计：
\[
\widehat r_{24}\approx 369.478420314,\quad \widehat h_{24}^{\mathrm R}\approx 0.4662365216;\qquad
\widehat r_{25}\approx 466.281715009,\quad \widehat h_{25}^{\mathrm R}\approx 0.4659953968.
\]
这些数值继续支持高阶 \(h_q^{\mathrm R}\) 的窄带收敛现象；相应的 DP 端点估计可由脚本
\texttt{scripts/exp\_fold\_collision\_renyi\_spectrum\_mod\_dp\_q20.py --q-max 25 --m-max 37}
一键生成。
但在当前窗口 \(m\le 37\) 下，尚未拟合到可全窗核验的低阶整系数递推（因此暂未进入全谱根扫描/ \(\beta_q,\delta_q\) 的“精确”口径）。
\end{remark}

进一步，把核版 RH/GRH 的平方根界（定义 \ref{def:kernel-rh}）移植到碰撞核 \(A_q\)：令
\[
\Lambda_q:=\max_{\mu\in\sigma(A_q)\setminus\{r_q\}}|\mu|,
\qquad
R_q:=\frac{\Lambda_q}{\sqrt{r_q}},
\qquad
\beta_q:=\frac{\log \Lambda_q}{\log r_q},
\qquad
\delta_q:=\log\!\left(\frac{\Lambda_q^2}{r_q}\right)=2\log R_q.
\]
表 \ref{tab:fold_collision_kernel_rh_scan_q2_17} 给出 \(q=2,\dots,17\) 的可复算输出（脚本 \texttt{scripts/exp\_fold\_collision\_kernel\_rh\_scan\_q2\_17.py} 一键生成）。
表 \ref{tab:fold_collision_kernel_rh_scan_q18_23} 给出 \(q=18,\dots,23\) 的扩展输出（数据可由脚本 \texttt{scripts/exp\_fold\_collision\_moment\_signature\_q18\_25.py} 导出）。
\input{sections/generated/tab_fold_collision_kernel_rh_scan_q2_17}
\input{sections/generated/tab_fold_collision_kernel_rh_scan_q18_23}

\begin{remark}[从 \texorpdfstring{$q=4$}{q=4} 起的系统性越线与快速劣化]\label{rem:pom-collision-kernel-rh-phase}
由表 \ref{tab:fold_collision_kernel_rh_scan_q2_17} 与表 \ref{tab:fold_collision_kernel_rh_scan_q18_23} 可见：在已核验范围内，\(\delta_q<0\) 仅发生在 \(q=2,3\)，而从 \(q=4\) 起即出现严格越线 \(\delta_q>0\)（其中 \(q=4\) 为贴线量级）。
并且 \(\beta_q\) 与 \(\delta_q\) 在 \(q\le 23\) 的已核验范围内随 \(q\) 单调上升，表现为“误差指数”从平方根相（\(\beta\le\frac12\)）向主项同阶（\(\beta\to 1\)）快速劣化的谱相变。
此外，在 \(q\le 17\) 的范围内达到 \(\Lambda_q\) 的主导非 Perron 根恒为负实根，从而存在稳定的反相慢模态主导越线。
\end{remark}

\input{sections/08_projection_ontology_mathematics/08_projection_ontology_mathematics_part03c_shadow_spectral_packet_q9_17.tex}

\begin{remark}[高阶递推的最短初值（\(q=9,10,11,12\)）]\label{rem:pom-moment-init-9-12}
为便于复核，列出最短初值（\(S_q(2),\dots,S_q(1+d_q)\)）：
$$
\begin{aligned}
&(S_9(2),\dots,S_9(8))=(514,1538,41416,382292,5376484,54810488,707836456),\\
&(S_{10}(2),\dots,S_{10}(10))=(1026,3074,122196,1406968,25250620,311420704,5187436232,72412721272,1102141254640),\\
&(S_{11}(2),\dots,S_{11}(10))=(2050,6146,362488,5265380,120053140,1787525384,38572713688,667461412712,12816890401888),\\
&(S_{12}(2),\dots,S_{12}(14))=(4098,12290,1079268,19982248,576435244,10344013168,290291967800,6227618870536,150911581885024,\\
&\qquad\qquad\qquad\qquad\qquad\quad 3397767385301568,81304379149198128,1859608501854266944,43678793149065441920).
\end{aligned}
$$
其余 \(q=13,14,15,16,17\) 的初值与系数由脚本 \texttt{scripts/exp\_fold\_collision\_moment\_recursions\_mod\_dp.py --precomputed} 的导出记录给出。
\end{remark}

\begin{theorem}[坍缩/共振判别：模态约束导致 Hankel 秩下降]\label{thm:pom-moment-resonance}
设 \(q\ge 2\)，记 \(h=\lfloor q/2\rfloor\)。在差分模态截断下（注 \ref{rem:pom-closure-dim-mode-trunc}），令 \(M_{q,\ell}(m)\) 表示净偏置 \(\ell=a-b\) 的模态贡献，则
$$
S_q(m)=\sum_{\ell=-h}^{h} M_{q,\ell}(m).
$$
并假设这些模态贡献可组织为一个以 \(\{M_{q,\ell}\}\) 为坐标的 \(2h+1\) 维线性 realization（非共振情形下正是“模态闭合推进”的标准实现）。
设存在从某个 \(m_0\) 起成立的整系数线性递推
$$
S_q(m)=\sum_{i=1}^{d} c_i S_q(m-i)\qquad(m\ge m_0),
$$
其特征多项式
$$
P_q(\lambda)=\lambda^d-\sum_{i=1}^d c_i\lambda^{d-i}.
$$
若 \(d<2h+1\)，则：
\begin{enumerate}
  \item \(S_q\) 的 Hankel 秩满足 \(\mathrm{rank}(H)\le d<2h+1\)；
  \item 该 \(2h+1\) 维线性 realization 必非最小，从而存在非零系数 \(\alpha_{-h},\dots,\alpha_h\) 使
  $$
  \sum_{\ell=-h}^{h}\alpha_\ell M_{q,\ell}(m)=0\qquad(m\ge m_0),
  $$
  即出现一个\textbf{模态约束}，导致“坍缩/共振”。
\end{enumerate}
在 \(q=9,\dots,17\) 的已核验递推（表 \ref{tab:fold_collision_moment_recursions_9_17}）下，对 \(q=9,\dots,15\) 的特征多项式为
$$
\begin{aligned}
P_9(\lambda)&=\lambda^7-2\lambda^6-62\lambda^5-386\lambda^4-2819\lambda^3-62\lambda^2-900\lambda+450,\\
P_{10}(\lambda)&=\lambda^9-2\lambda^8-96\lambda^7-830\lambda^6-7945\lambda^5-2\lambda^4-1852\lambda^3+830\lambda^2-4\lambda+4,\\
P_{11}(\lambda)&=\lambda^9-2\lambda^8-153\lambda^7-1740\lambda^6-21249\lambda^5+9432\lambda^4+86213\lambda^3+1484\lambda^2+18348\lambda-9174,
\\
P_{12}(\lambda)&=\lambda^{13}-2\lambda^{12}-243\lambda^{11}-3608\lambda^{10}-56447\lambda^9+61236\lambda^8+667319\lambda^7-3608\lambda^6\\
&\quad+9582\lambda^5-61242\lambda^4-15404\lambda^3+7216\lambda^2-8\lambda+8,\\
P_{13}(\lambda)&=\lambda^{11}-2\lambda^{10}-388\lambda^9-7414\lambda^8-148038\lambda^7+317916\lambda^6+4165856\lambda^5\\
&\quad-136252\lambda^4-1565891\lambda^3-318938\lambda^2-289380\lambda+144690,\\
P_{14}(\lambda)&=\lambda^{13}-2\lambda^{12}-621\lambda^{11}-15140\lambda^{10}-385463\lambda^9+1443744\lambda^8+22761161\lambda^7\\
&\quad-15140\lambda^6+2116566\lambda^5-1443750\lambda^4-63044\lambda^3+30280\lambda^2-8\lambda+8,\\
P_{15}(\lambda)&=\lambda^{11}-2\lambda^{10}-1000\lambda^9-30766\lambda^8-994458\lambda^7+6188172\lambda^6+119408756\lambda^5\\
&\quad-8289820\lambda^4-134208623\lambda^3-6186122\lambda^2-16637076\lambda+8318538,
\end{aligned}
$$
并且 \(q=16,17\) 的递推阶数均为 \(d=13<2\lfloor q/2\rfloor+1\)（系数见表 \ref{tab:fold_collision_moment_recursions_9_17}）。因此 \(q=9,\dots,17\) 均满足 \(d<2h+1\)，Hankel 秩下降并触发模态约束。
\end{theorem}
\begin{proof}
由递推得 \(S_q\) 的 Hankel 秩不超过递推阶数 \(d\)（定理 \ref{thm:hankel-rank-minimal}），若 \(d<2h+1\) 则存在秩下降。
另一方面，差分模态截断提供一个以 \(\{M_{q,\ell}\}\) 为坐标的 \(2h+1\) 维线性表示；既然最小线性表示维数不超过 \(d\)，该表示必非最小，因此存在非零线性关系 \(\sum_{\ell}\alpha_\ell M_{q,\ell}(m)\equiv 0\)。
最后一段直接由表中的递推系数写出特征多项式并比较阶数即可。证毕。
\end{proof}

\input{sections/08_projection_ontology_mathematics/08_projection_ontology_mathematics_part03c_resonance_aitken_certificate.tex}

\begin{remark}[2-进影子律：\texorpdfstring{$P_q(\lambda)$}{Pq} 在模 \texorpdfstring{$2$}{2} 下坍缩为 \texorpdfstring{$\lambda^a(\lambda+1)^b$}{lambda^a(lambda+1)^b}（可审计）]\label{rem:pom-charpoly-mod2-shadow}
对共振窗口 \(q=9,\dots,17\) 的已核验最小递推（表 \ref{tab:fold_collision_moment_recursions_9_17}；定理 \ref{thm:pom-moment-resonance}），把特征多项式 \(P_q(\lambda)\) 的系数按奇偶性约化到 \(\mathbb{F}_2\) 上，会出现一个完全内部的“2-进影子坍缩”：
\[
P_q(\lambda)\equiv \lambda^{a_q}(\lambda+1)^{b_q}\pmod 2,
\qquad a_q+b_q=d_q.
\]
其具体 \((a_q,b_q)\) 与对应的稀疏模 \(2\) 表达式见表 \ref{tab:fold_collision_charpoly_mod2_shadow_q9_17}（脚本 \texttt{scripts/exp\_fold\_collision\_charpoly\_mod2\_shadow\_q9\_17.py} 一键生成）。
特别地，从定理 \ref{thm:pom-moment-resonance} 列出的系数可直接读出
\[
P_{13}(\lambda)\equiv \lambda^3(\lambda+1)^8,\qquad
P_{14}(\lambda)\equiv \lambda^7(\lambda+1)^6,\qquad
P_{15}(\lambda)\equiv \lambda^3(\lambda+1)^8\pmod 2.
\]
\par
机制上，这意味着在特征 \(2\) 的世界里递推算子的谱点只落在 \(0\) 与 \(-1\)（注意 \(-1\equiv 1\in\mathbb{F}_2\)）两处：伴随矩阵在 \(\mathbb{F}_2\) 上呈现“幂零 + 幺正（unipotent）”的极端刚性形态。
等价地，令前移算子 \(E\) 作用于序列 \(S\) 为 \((ES)(m):=S(m+1)\)，则递推在模 \(2\) 下满足完全显式的差分湮灭律：
\[
P_q(E)S_q\equiv 0\pmod 2
\qquad\Longrightarrow\qquad
E^{a_q}(E+1)^{b_q}S_q\equiv 0\pmod 2.
\]
把差分算子记为 \(\mathsf{D}:=E+1\)（特征 \(2\) 下等同于前向差分），即得一个“内生的奇偶选择定则”：对每个 \(q=9,\dots,17\)，都有
\[
\mathsf{D}^{b_q}S_q(m+a_q)\equiv 0\pmod 2.
\]
\end{remark}

\input{sections/generated/tab_fold_collision_charpoly_mod2_shadow_q9_17}

\input{sections/08_projection_ontology_mathematics/08_projection_ontology_mathematics_part03c_charpoly_mod3_mod5_selection_q13_15}

\begin{remark}[最小递推的端点刚性：首系数 \(2\) 与偶阶的 \texorpdfstring{$\pm 8$}{±8} 尾差分块]\label{rem:pom-recurrence-endpoint-rigidity}
在已核验的最小整系数递推中（表 \ref{tab:fold_collision_moment_recursions}、\ref{tab:fold_collision_moment_recursions_9_17}、\ref{tab:fold_collision_moment_recursions_18_23}），始终有
\[
c_1=2,
\qquad\text{即}\qquad
P_q(\lambda)=\lambda^{d_q}-2\lambda^{d_q-1}+\cdots.
\]
这表明“向后一步”的系数在做完 fiber-product 约束与最小化之后仍保持一个刚性的二元步进结构。
\par
更进一步地，在定理 \ref{thm:pom-moment-resonance} 列出的偶阶多项式 \(P_{12},P_{14}\) 中（以及表 \ref{tab:fold_collision_moment_recursions_9_17} 的 \(q=16\) 最小递推系数中），末端两项的系数固定为
\[
(c_{d_q-1},c_{d_q})=(8,-8),
\qquad\text{等价地}\qquad
P_q(\lambda)=\cdots+(\cdots)\lambda^2-8\lambda+8.
\]
因此最深滞后只以一个“尾差分块”的形式出现：
\[
8\bigl(S_q(m-d_q+1)-S_q(m-d_q)\bigr).
\]
若令后向差分 \(T_q(m):=S_q(m)-S_q(m-1)\)，则上述偶阶递推可改写为
\[
S_q(m)=\sum_{i=1}^{d_q-2}c_i\,S_q(m-i)\ +\ 8\,T_q(m-d_q+1),
\]
把最深一层历史的进入方式压缩为差分信道。
该端点块在一般线性递推中并非必然，因而可作为后续重写 realization/提取隐藏模态约束的一个结构把手。
\end{remark}

\begin{proposition}[奇滞后中性（odd-lag neutrality；可审计）]\label{prop:pom-moment-odd-lag-neutrality}
在已核验范围 \(q=2,\dots,23\) 内（表 \ref{tab:fold_collision_moment_recursions}、表 \ref{tab:fold_collision_moment_recursions_9_17} 与表 \ref{tab:fold_collision_moment_recursions_18_23}），\(S_q(m)\) 的最小整系数递推可写为
\[
S_q(m)=\sum_{i=1}^{d_q} c_i\,S_q(m-i)\qquad(m\ge m_0),
\]
其中阶数 \(d_q\) 皆为奇数，并满足\textbf{奇滞后系数零和}：
\[
\sum_{\substack{1\le i\le d_q\\ i\ \mathrm{odd}}} c_i=0.
\]
等价地，若按约定
\[
P_q(\lambda)=\lambda^{d_q}-\sum_{i=1}^{d_q}c_i\,\lambda^{d_q-i},
\]
则有
\[
P_q(1)+P_q(-1)=0.
\]
亦即 \(P_q\) 的\textbf{偶次幂系数和为 \(0\)}（因为 \(d_q\) 为奇数时，偶次幂项恰对应递推中的奇滞后项）。
\end{proposition}

\begin{proof}
由于 \(d_q\) 为奇数，\(1^{d_q}+(-1)^{d_q}=0\)，从而
\[
P_q(1)+P_q(-1)
= -\sum_{i=1}^{d_q} c_i\bigl(1^{d_q-i}+(-1)^{d_q-i}\bigr)
= -2\sum_{\substack{1\le i\le d_q\\ i\ \mathrm{odd}}}c_i,
\]
故 \(\sum_{i\ \mathrm{odd}}c_i=0\iff P_q(1)+P_q(-1)=0\)。
在上述三张表中，\((c_1,\dots,c_{d_q})\) 已逐项显式给出；对每个 \(q\) 直接把奇指标项相加即可得到零和恒等式，从而命题成立。证毕。
\end{proof}

\begin{remark}[机制解释：奇通道的“直流中性”与差分型投影门]\label{rem:pom-moment-odd-lag-neutrality-gate}
把递推按奇偶滞后拆开：
\[
S_q(m)=\sum_{i\ \mathrm{even}}c_i\,S_q(m-i)\ +\ \sum_{i\ \mathrm{odd}}c_i\,S_q(m-i).
\]
由命题 \ref{prop:pom-moment-odd-lag-neutrality} 的零和条件，并注意 \(d_q\) 为奇数（因此 \(d_q\) 本身属于奇滞后集合），可将奇滞后部分改写为“锚定差分”：
\[
\sum_{i\ \mathrm{odd}}c_i\,S_q(m-i)
=\sum_{\substack{i\ \mathrm{odd}\\ i<d_q}}c_i\bigl(S_q(m-i)-S_q(m-d_q)\bigr).
\]
因此\textbf{奇滞后通道对常数模态（直流）完全中性}，只能读取历史窗口中的相对差分。
等价地，把 \(P_q\) 分解为偶/奇部分
\(
E_q(\lambda):=\frac{P_q(\lambda)+P_q(-\lambda)}{2}=Q_q(\lambda^2)
\)
与
\(
O_q(\lambda):=\frac{P_q(\lambda)-P_q(-\lambda)}{2}
\)，
则奇滞后中性 \(P_q(1)+P_q(-1)=0\) 即 \(E_q(1)=0\)，从而 \(Q_q(1)=0\) 并推出 \((\lambda^2-1)\mid E_q(\lambda)\)。
这把“先在两步尺度（\(\lambda^2\)）上升维，再经投影门消元”的直观口径转写为一个纯代数、可审计的因子约束。
在“先升维再投影”的口径下，这等价于：递推算子在奇滞后子通道上内生地携带一个差分型投影门（消去一个不可见方向），从而提供一个零成本、可复算的一致性审计抓手：在已核验范围内，只需检查 \(\sum_{i\ \mathrm{odd}}c_i\) 是否为 \(0\) 即可。
\end{remark}

\begin{conjecture}[Fourier 零点簇驱动的 Hankel 共振（可证伪机制假说）]\label{conj:pom-fourier-zero-driven-resonance}
附录 \ref{app:fold-multiplicity} 将折叠多重度剖面 \(d_m(r)\) 的频谱写成 Riesz 乘积并定义谱零点集合
\(
\mathcal{Z}_m=\{k:\widehat d_m(k)=0\}
\)
（定义 \ref{def:fold-spectrum-zero-set}）。当 \(m+2\equiv 0\pmod 3\) 时，存在强制零点 \(k=F_{m+2}/2\in\mathcal{Z}_m\)（定理 \ref{thm:fold-spectrum-parity-zero}），并且更一般地 \(\mathcal{Z}_m\) 具有由 \(\gcd\)/余类并决定的刚性结构（推论 \ref{cor:fold-zero-coset-union}）。
\par
\textbf{假说：}这些“可定位的谱删去”（尤其是 \(m+2\equiv 0\pmod 3\) 的算术节拍点）会在 moment-kernel 的模态推进中诱发额外的线性约束，使得可见模态空间出现更稳定/更强的秩缺口，从而在 \(q\ge 9\) 的窗口内触发（或放大）定理 \ref{thm:pom-moment-resonance} 中的 Hankel 共振坍缩。
\par
\textbf{可证伪检验：}在用于审计 \(d_q\) 的 \(m\)-窗口内，对每个 \(m\) 计算局部 Hankel 块的数值条件数/奇异值剖面，并把 \(m+2\equiv 0\pmod 3\) 的点标注出来；若这些点并不呈现系统性的“更易坍缩/更稳定缺口”，则该机制解释应被否定。相应的对齐输出可由脚本 \texttt{scripts/exp\_fold\_collision\_col\_hankel\_alignment.py} 生成（见实现说明）。
\end{conjecture}

\input{sections/08_projection_ontology_mathematics/08_projection_ontology_mathematics_part03c_charpoly_q16_q17.tex}

\begin{corollary}[模态约束的维数缺口]\label{cor:pom-resonance-deficit}
在定理 \ref{thm:pom-moment-resonance} 的假设下，存在至少
\(
2h+1-d
\)
条线性无关的系数向量 $\alpha$ 使
\[
\sum_{\ell=-h}^{h}\alpha_\ell M_{q,\ell}(m)=0\qquad(m\ge m_0).
\]
因此共振的“约束维数缺口”为 $2h+1-d$。
\end{corollary}
\begin{proof}
以 $\{M_{q,\ell}\}$ 为坐标的线性表示的像空间维数至多为最小实现维数 $d$，因此在 $2h+1$ 维坐标空间中，产生零序列的线性组合构成的核空间维数至少为 $2h+1-d$。取其一组基即得线性无关的约束族。证毕。
\end{proof}

\begin{definition}[共振缺口函数 \texorpdfstring{$\Delta(q)$}{Delta(q)}]\label{def:pom-resonance-gap-delta}
对整数 \(q\ge 2\)，令 \(h=\lfloor q/2\rfloor\)，并令 \(d_q\) 表示 \(S_q(m)\) 的最小整系数递推阶数（等价地最小线性 realization 维数）。定义共振缺口函数
$$
\Delta(q):=\bigl(2\lfloor q/2\rfloor+1\bigr)-d_q.
$$
\end{definition}

\begin{proposition}[缺口即约束维数：\texorpdfstring{$\Delta(q)$}{Delta(q)} 条线性无关的不可激发约束]\label{prop:pom-delta-counts-constraints}
在定理 \ref{thm:pom-moment-resonance} 的假设下，若递推阶数为 \(d=d_q\)，则至少存在 \(\Delta(q)\) 条线性无关的系数向量 \(\alpha\) 使
\[
\sum_{\ell=-h}^{h}\alpha_\ell M_{q,\ell}(m)=0\qquad(m\ge m_0).
\]
因此 \(\Delta(q)\) 可作为“共振强度”的整数指纹：其数值等于至少可见的不可激发模态约束的维数下界。
\end{proposition}

\begin{proof}
由推论 \ref{cor:pom-resonance-deficit} 立得：缺口为 \(2h+1-d_q=\Delta(q)\)。证毕。
\end{proof}

\begin{corollary}[缺口作为最小新增读出轴/纤维数下界]\label{cor:pom-delta-min-extra-readouts}
沿用命题 \ref{prop:pom-delta-counts-constraints} 的记号。把
\[
M_q(m):=\bigl(M_{q,-h}(m),\ldots,M_{q,h}(m)\bigr)\in\RR^{2h+1}
\]
视为名义“模态坐标向量”。若在当前读出/编译接口下，\(S_q\) 的最小线性 realization 维数为 \(d_q\)，则
存在一个秩至多为 \(d_q\) 的线性像空间，使得所有 \(m\ge m_0\) 的模态向量 \(M_q(m)\) 都落在一个至多 \(d_q\) 维的可见子空间中。
因此不可见核的维数至少为
\(
\Delta(q)=2h+1-d_q
\)，并且要在同一时间侧数据预算下把该核方向区分开来，至少需要引入 \(\Delta(q)\) 个额外线性独立的读出函数
（等价地：\(\Delta(q)\) 条额外独立投影轴/纤维坐标）。
\end{corollary}

\begin{proof}
线性代数事实：若线性映射 \(\Phi:\RR^{2h+1}\to\RR^{d_q}\) 的秩 \(\le d_q\)，则 \(\ker(\Phi)\) 的维数 \(\ge 2h+1-d_q\)。
要使扩展读出 \((\Phi,\psi_1,\ldots,\psi_k)\) 在 \(\RR^{2h+1}\) 上对 \(\ker(\Phi)\) 的方向变为可区分，必要条件是
\(\{\psi_i\}_{i=1}^k\) 在 \(\ker(\Phi)\) 上的限制张成其对偶空间，故 \(k\ge \dim\ker(\Phi)\ge \Delta(q)\)。
将 \(\psi_i\) 解释为新增的独立线性可观测/读出轴（或本体语义下的新增纤维坐标）即得结论。
\end{proof}

\begin{definition}[共振 syndrome（校验子）]\label{def:pom-resonance-syndrome}
沿用推论 \ref{cor:pom-delta-min-extra-readouts} 的模态向量
\(
M_q(m)\in\RR^{2h+1}
\)。
在共振假设下，存在一个维数至多为 \(d_q\) 的可见子空间 \(V_q^{\mathrm{vis}}\subseteq\RR^{2h+1}\)，使得对所有 \(m\ge m_0\) 有 \(M_q(m)\in V_q^{\mathrm{vis}}\)，并且其余维数缺口为 \(\Delta(q)=2h+1-d_q\)（定义 \ref{def:pom-resonance-gap-delta}）。
\par
取一组\textbf{线性无关}的线性泛函 \(L_{q,1},\dots,L_{q,\Delta(q)}\in(\RR^{2h+1})^\ast\)，它们均在 \(V_q^{\mathrm{vis}}\) 上消失（即对任意 \(v\in V_q^{\mathrm{vis}}\) 都有 \(L_{q,j}(v)=0\)），并定义线性映射
\[
L_q:\RR^{2h+1}\to\RR^{\Delta(q)},\qquad
L_q(v):=\bigl(L_{q,1}(v),\dots,L_{q,\Delta(q)}(v)\bigr).
\]
定义\textbf{syndrome} 为向量值序列
\[
R_q(m):=L_q\bigl(M_q(m)\bigr)\in\RR^{\Delta(q)},
\qquad
R_{q,j}(m):=\bigl(R_q(m)\bigr)_j.
\]
\end{definition}

\begin{corollary}[syndrome 的自洽零律与可证伪审计]\label{cor:pom-resonance-syndrome-audit}
在定义 \ref{def:pom-resonance-syndrome} 的设定下，对所有 \(m\ge m_0\) 都有
\[
\boxed{\;R_q(m)\equiv 0\;}
\qquad\text{（即 }R_{q,j}(m)=0,\ j=1,\dots,\Delta(q)\text{）}.
\]
因此：对任意来自模拟或真实输入的模态观测 \(\widehat M_q(m)\in\RR^{2h+1}\)，若存在某个 \(m\) 使 \(L_q(\widehat M_q(m))\neq 0\)，则该观测不满足共振窗口内的模态约束（定理 \ref{thm:pom-moment-resonance} 与命题 \ref{prop:pom-delta-counts-constraints} 的口径），从而在严格意义下不可由同一投影门实现的可见 realization 产生。
\end{corollary}
\begin{proof}
由 \(M_q(m)\in V_q^{\mathrm{vis}}\) 以及 \(L_{q,j}|_{V_q^{\mathrm{vis}}}\equiv 0\)，立得 \(R_q(m)=L_q(M_q(m))=0\)。
反向的“可证伪”陈述只是该零律的逆否命题表述。证毕。
\end{proof}

\begin{corollary}[寄存器意义：共振缺口 \texorpdfstring{$\Delta(q)$}{Delta(q)} 等于可省寄存器数]\label{cor:pom-resonance-register-savings}
在共振假设下，存在一个可逆线性变换 \(T_q\in\mathrm{GL}_{2h+1}(\RR)\)，使得对所有 \(m\ge m_0\) 都有
\[
\boxed{\;
T_q\,M_q(m)=\bigl(\widetilde M_q(m),\,0_{\Delta(q)}\bigr),\qquad
\widetilde M_q(m)\in\RR^{d_q}\; }.
\]
因此：
\begin{itemize}
  \item 若非共振（\(d_q=2h+1\)），推进/存储模态向量一般需要 \(2h+1\) 个自由寄存器；
  \item 若共振且 \(d_q<2h+1\)，则可将模态推进压缩到 \(d_q\) 维，名义坐标中多出的 \(\Delta(q)=2h+1-d_q\) 个寄存器在严格模型下被强制为零（可用作 syndrome 校验子）。
\end{itemize}
\end{corollary}
\begin{proof}
由定义 \ref{def:pom-resonance-syndrome}，存在线性映射 \(L_q:\RR^{2h+1}\to\RR^{\Delta(q)}\) 使得 \(\ker(L_q)\supseteq V_q^{\mathrm{vis}}\) 且 \(\mathrm{rank}(L_q)=\Delta(q)\)。
取 \(\Delta(q)\) 个线性无关的行向量作为 \(L_q\) 的行，并再补上 \(d_q\) 个线性无关的行向量，使得合起来构成 \((\RR^{2h+1})^\ast\) 的一组基；将这 \(2h+1=d_q+\Delta(q)\) 个行向量堆叠成矩阵即得可逆的 \(T_q\in\mathrm{GL}_{2h+1}(\RR)\)。
由于对所有 \(m\ge m_0\) 有 \(M_q(m)\in V_q^{\mathrm{vis}}\subseteq\ker(L_q)\)，故
\(
L_q(M_q(m))=0
\)，等价于 \(T_q M_q(m)\) 的最后 \(\Delta(q)\) 个坐标恒为零；前 \(d_q\) 个坐标记为 \(\widetilde M_q(m)\) 即得所示形式。
寄存器表述只是该坐标压缩的直接翻译。证毕。
\end{proof}

\begin{remark}[共振作为资源：约束缺口 \texorpdfstring{$\Delta(q)$}{Delta(q)} 增强审计性]\label{rem:pom-resonance-as-resource}
当 \(\Delta(q)>0\) 时，共振不仅意味着“名义模态数过多”，更意味着存在 \(\Delta(q)\) 条独立的线性校验律可用于一致性审计：syndrome \(R_q(m)=L_q(M_q(m))\) 在严格模型下恒为零（推论 \ref{cor:pom-resonance-syndrome-audit}）。
因此在已知最小递推/最小 realization 的口径下，高阶矩并不必然更“昂贵”：一方面其推进可由低维递推完成（维数 \(d_q\)），另一方面其额外的缺口 \(\Delta(q)\) 反而提供更多可证伪的自洽约束方向。
这把共振从“观察现象”转写为“可用校验码”：缺口越大，可用的 syndrome 方程越多。
\end{remark}

\begin{remark}[已核验窗口内的 \texorpdfstring{$\Delta(q)$}{Delta(q)} 读数（\(q=9,\dots,17\)）]\label{rem:pom-delta-q9-17}
在已核验的最小递推阶数 \(d_q\)（表 \ref{tab:fold_collision_moment_recursions_9_17}）下，可直接计算 \(\Delta(q)\) 并得到一个可跨版本对比的整数指纹序列。
下表由脚本 \texttt{scripts/exp\_fold\_collision\_resonance\_nullmodes\_hankel.py} 一键生成：
\end{remark}

\input{sections/generated/tab_fold_collision_resonance_gap_delta_q_9_17}

\begin{conjecture}[秩缺口阶梯：\texorpdfstring{$\Delta(q)$}{Delta(q)} 的阶梯像与偶性]\label{conj:pom-delta-staircase}
在定义 \ref{def:pom-resonance-gap-delta} 的记号下，随着 \(q\) 增大，共振缺口 \(\Delta(q)\)（即名义模态上界与最小实现维数之间的秩缺口）应呈现稳定的阶梯结构：
\begin{enumerate}
  \item \textbf{偶性：}\(\Delta(q)\in 2\mathbb{Z}_{\ge 0}\)；
  \item \textbf{阶梯像：}\(\Delta(q)\) 在宽 \(q\)-区间内保持常值，偶尔以步长 \(2\)（或其倍数）跳变。
\end{enumerate}
在已核验范围 \(q\le 23\) 内，\(\Delta(q)\) 的读数确实只出现 \(0,2,4,6\)，并显示出“长平台 + 偶数跃迁”的稳定迹象（见表 \ref{tab:fold_collision_resonance_gap_delta_q_9_17} 与推论 \ref{cor:pom-moment-dim-collapse} 的 \(d_q\) 列表）。
\end{conjecture}

\begin{remark}[双层降维框架：对称性降维与 Fibonacci 共振坍缩的来源可分解]\label{rem:pom-two-layer-dim-reduction}
对固定的 \(q\)-重 moment-kernel，存在两类互不混淆的降维机制：
\begin{itemize}
  \item \textbf{普适层（群对称）：}由 \(S_q\)-置换对称，对称商核 \(\widetilde A_q\) 的维数满足
  \(
  \dim(\widetilde A_q)\le \binom{q+9}{9}=O(q^9)
  \)
  且保持同一 Perron 主尺度 \(r_q\)（命题 \ref{prop:pom-ak-symmetric-compression}）；
  \item \textbf{特异层（算术共振）：}当 \(d_q<2\lfloor q/2\rfloor+1\) 时，由差分模态口径出现额外线性约束，其强度由缺口 \(\Delta(q)\) 量化（定义 \ref{def:pom-resonance-gap-delta} 与命题 \ref{prop:pom-delta-counts-constraints}）。
\end{itemize}
因此“从 \(Q^q\) 到多项式规模”的压缩可以被拆解为：先由群对称完成普适的直方图化降维，再由 Fibonacci 同余结构诱发的代数退化触发额外的算术坍缩（其可审计指纹即 \(\Delta(q)\) 与相应的线性证书）。
\end{remark}

\begin{remark}[可执行共振证书：Hankel-null 的 \texorpdfstring{$\mathrm{NullModes}(q)$}{NullModes(q)} 输出]\label{rem:pom-nullmodes-algorithm}
对固定 \(q\)（例如 \(q=9,\dots,17\)），脚本 \texttt{scripts/exp\_fold\_collision\_resonance\_nullmodes\_hankel.py} 在审计窗口上计算序列 \(a_n:=S_q(n+2)\)，并构造尺寸 \((2h+1)\times L\) 的 Hankel 截断块 \(H=(a_{i+j})\)。
当 \(d_q<2h+1\) 时，向量空间 \(\ker(H^{\mathsf T})\) 的维数至少为 \(\Delta(q)\)，其任意一组基
\[
\mathrm{NullModes}(q)=\{\alpha^{(j)}\}_{j=1}^{\Delta(q)}\subset\ZZ^{2h+1}
\]
给出一族可机读的“共振证书”：对审计窗口内所有可用的平移 \(t\)，都有
\(
\sum_{i=0}^{2h}\alpha^{(j)}_i\,a_{i+t}=0
\)。
该输出把“阶数下降/秩缺口”升级为可复现的显式线性组合证书，并可作为跨实现/跨版本的对比对象。
\end{remark}

\input{sections/generated/tab_fold_collision_resonance_nullmodes_q9_17}

\input{sections/generated/tab_fold_collision_resonance_nullmodes_primitive_quotient_q9_17}

\begin{remark}[Hankel-null 的递推向量与倍乘冗余：\texorpdfstring{$\Delta_{\mathrm{prim}}(q)$}{Delta_prim(q)} 的原始商空间（可复算）]\label{rem:pom-nullmodes-recurrence-plus-signature}
在共振窗口 \(\Delta(q)>0\) 下，表 \ref{tab:fold_collision_resonance_nullmodes_q9_17} 给出的
\(
\mathrm{NullModes}(q)\subset\ZZ^{2\lfloor q/2\rfloor+1}
\)
是一组 Hankel-null 证书的整数基，其维数为 \(\Delta(q)\)。
其中存在一个天然可识别的“递推向量”方向：将最小递推的特征多项式
\[
P_q(\lambda)=p_0+p_1\lambda+\cdots+p_{d_q}\lambda^{d_q}\qquad(p_{d_q}=1)
\]
的系数按低次到高次排列，并补零嵌入到长度 \(2\lfloor q/2\rfloor+1\) 的坐标中，
\[
\alpha^{\mathrm{rec}}_q:=(p_0,p_1,\ldots,p_{d_q},0,\ldots,0),
\]
则该向量必落在 Hankel-null 中（其代数机制与多项式倍乘结构见注 \ref{rem:pom-nullmodes-q14-poly-alignment}）。
在已审计输出里，这一对齐甚至可直接\emph{逐项对照}：例如 \(q=9\) 时，定理 \ref{thm:pom-moment-resonance} 给出
\(
P_9(\lambda)=\lambda^7-2\lambda^6-\cdots-900\lambda+450
\)，而表 \ref{tab:fold_collision_resonance_nullmodes_q9_17} 的首条证书向量正是
\(
\alpha^{(1)}=(450,-900,-62,-2819,-386,-62,-2,1,0)
\)
（即 \(\alpha^{\mathrm{rec}}_9\)）。
\par
然而，除 \(\alpha^{\mathrm{rec}}_q\) 外的其余 Hankel-null 向量往往并不对应新的“不可激发模态”物理约束：如注 \ref{rem:pom-nullmodes-q14-poly-alignment} 所述，低次倍乘 \(Q(\lambda)P_q(\lambda)\) 会自动生成额外的 Hankel-null 证书（只要窗口行数允许）。
因此若要把 Hankel-null 的“额外向量”解释为结构信息，需要先剥离这一倍乘冗余。
令 \(\mathcal{M}(P_q)\subseteq\ker(H^{\mathsf T})\) 表示由所有可容纳于窗口长度（即满足 \(\deg(QP_q)\le 2\lfloor q/2\rfloor\)）的倍乘 \(Q(\lambda)P_q(\lambda)\) 的系数向量生成的子空间，并定义原始缺口（primitive quotient）
\[
\boxed{
\Delta_{\mathrm{prim}}(q):=\dim\Bigl(\ker(H^{\mathsf T})\ \big/\ \mathcal{M}(P_q)\Bigr).
}
\]
在当前已审计的共振窗口 \(q=9,\dots,17\) 内，我们得到 \(\Delta_{\mathrm{prim}}(q)=0\)，并且表 \ref{tab:fold_collision_resonance_nullmodes_q9_17} 的每条 NullMode 都可显式分解为
\(
A^{(j)}(\lambda)=Q^{(j)}(\lambda)P_q(\lambda)
\)；
其乘子 \(Q^{(j)}\) 的系数向量列于表 \ref{tab:fold_collision_resonance_nullmodes_primitive_quotient_q9_17}。
因此在该窗口内，Hankel-null 的多条“额外向量”应理解为同一最小递推在固定长度下的倍乘展开证书，而非额外独立的物理约束。
\end{remark}

\begin{remark}[Hankel-null 与特征多项式的对齐：以 \texorpdfstring{$q=14$}{q=14} 为例]\label{rem:pom-nullmodes-q14-poly-alignment}
对任一满足整系数线性递推的序列 \(a_n\)，若其特征多项式写成
\[
P(\lambda)=p_0+p_1\lambda+\cdots+p_d\lambda^d\qquad(p_d=1),
\]
则对所有可用平移 \(t\) 都有线性关系
\(
\sum_{j=0}^{d} p_j\,a_{t+j}=0
\)。
因此当 Hankel 块的行数 \(\ge d+1\) 时，系数向量 \((p_0,\dots,p_d,0,\dots,0)\) 必属于 \(\ker(H^{\mathsf T})\)；
更一般地，任意低次多项式倍乘 \(Q(\lambda)P(\lambda)\) 也会给出额外的 Hankel-null 向量（只要行数允许）。
\par
在共振例 \(q=14\) 中，名义行数 \(2h+1=15\) 而最小阶 \(d_{14}=13\)，故 \(\Delta(14)=2\)。
写
\(
P_{14}(\lambda)=\sum_{j=0}^{13}p_j\lambda^j
\)
与
\(
(\lambda+2)P_{14}(\lambda)=\sum_{j=0}^{14}\widetilde p_j\lambda^j
\)。
则表 \ref{tab:fold_collision_resonance_nullmodes_q9_17} 给出的两条 \(\mathrm{NullModes}(14)\) 可直接对齐为
\[
\alpha^{(1)}=(p_0,p_1,\ldots,p_{13},0),
\qquad
\alpha^{(2)}=(\widetilde p_0,\widetilde p_1,\ldots,\widetilde p_{14}),
\]
从而提供一个极便宜的审计一致性检查：只要把定理 \ref{thm:pom-moment-resonance} 中的 \(P_{14}\) 系数与表向量逐项对照即可。
同时它也提示：\(\mathrm{NullModes}(q)\) 的某些“形状特征”往往更多反映了最小递推多项式的系数剖面，而不应被过早解释为差分模态索引 \(\ell\) 上的局部化效应；
若要研究 \(\ell\)-方向的支撑/集中性，需要回到具体的模态 realization 上直接求解约束。
\end{remark}

\begin{remark}[算术节拍标注：\texorpdfstring{$m+2\equiv 0\pmod 3$}{m+2=0 mod 3} 点与 Hankel 证书对齐（可证伪）]\label{rem:pom-mod3-beat-alignment}
附录 \ref{app:fold-multiplicity} 中定理 \ref{thm:fold-spectrum-parity-zero} 表明：当 \(m+2\equiv 0\pmod 3\)（等价于 \(F_{m+2}\) 为偶数）时，折叠多重度谱存在严格 Fourier 零点（从而零点集合 \(\mathcal{Z}_m\) 非空）。
这给出一个无需拟合的、纯算术驱动的“节拍点集”。
为检验“Fourier 零点簇驱动共振坍缩/递推阶数下降”的机制假说，我们在同一 \(m\) 轴上对齐
\(\,F_{m+2}\mathrm{Col}_m\)（碰撞常数代理；附录 \ref{app:fold-multiplicity}）与
共振区 \(q=9\) 的 Hankel 证书代理（固定 \(d_q\times d_q\) Hankel 子式的行列式规模），并用红点标注上述节拍点（图 \ref{fig:fold_collision_col_hankel_mod3_alignment_q9}；脚本 \texttt{scripts/exp\_fold\_collision\_col\_hankel\_mod3\_alignment.py} 一键生成）。
若节拍点系统性对应子式规模的下凹/条件数劣化，则支持该机制；反之则给出直接的证伪信号。
\end{remark}

\input{sections/generated/fig_fold_collision_col_hankel_mod3_alignment_q9}

\subsubsection{秩缺口与表示可见性}\label{subsec:pom-moment-visibility}
从同余计数口径 \(c_m(r)\) 出发，\(S_q(m)=\sum_r c_m(r)^q\) 可理解为模 \(F_{m+2}\) 上的 \(L^q\) 量；其傅里叶展开等价于对角色空间做 \(q\)-重张量提升后再施加共轭对称与“输出一致”的投影。差分模态截断给出上界 \(2\lfloor q/2\rfloor+1\)，而表 \ref{tab:fold_collision_moment_recursions_9_17} 的秩缺口说明：在某些 \(q\) 上存在不可见的表示分量，使得可观测子空间维数下降，从而触发共振。

\begin{conjecture}[模态可见性与闭合维数]\label{conj:pom-moment-visible}
设 \(V_q\) 为由同余角色张量提升并施加共轭对称与输出一致约束得到的模态空间，令 \(V_q^{\mathrm{vis}}\) 表示对碰撞读出可见的子空间。则
\[
d_q=\dim V_q^{\mathrm{vis}}\ \le\ 2\lfloor q/2\rfloor+1,
\]
并且在非共振情形下取等号；共振对应于 \(V_q^{\mathrm{vis}}\) 出现额外约束而降维。
\end{conjecture}

\begin{theorem}[Hankel 秩下界：高阶矩的最小线性实现维数]\label{thm:pom-moment-hankel}
记 \(S_q(m)=\sum_{x\in X_m} d_m(x)^q\)，并令 \(a_n:=S_q(n+2)\)（对 \(q=2,3\) 可再作常数缩放 \(a_n:=S_q(n+2)/2\)，不影响以下结论）。则对 \(q=2,3,4,5,6,7,8\)，其 Hankel 矩阵 \(H=(a_{i+j})_{i,j\ge 0}\) 满足
\[
\mathrm{rank}(H)\ \ge\ 2\lfloor q/2\rfloor+1.
\]
更具体地，存在一个 \((2\lfloor q/2\rfloor+1)\times(2\lfloor q/2\rfloor+1)\) 的 Hankel 主子式行列式非零，见表 \ref{tab:fold_collision_moment_hankel_rank}。
\end{theorem}
\begin{proof}
对每个 \(q\) 直接构造 Hankel 块 \(H_0=(a_{i+j})_{0\le i,j<d}\)（其中 \(d=2\lfloor q/2\rfloor+1\)）并计算其行列式；若 \(\det(H_0)\neq 0\) 则 \(\mathrm{rank}(H)\ge d\)。该计算由脚本 \texttt{scripts/exp\_fold\_collision\_moment\_hankel\_rank.py} 逐项给出并输出为可审计表格。{\CJKfamily{song}证毕。}
\end{proof}

\begin{corollary}[高阶矩的最小实现维数等于递推阶数（在已核验范围内）]\label{cor:pom-moment-minreal}
对 \(q=4,5,6,7,8\)，表 \ref{tab:fold_collision_moment_recursions} 给出的递推阶数分别为 \(5,5,7,7,9\)，因此存在维数分别为 \(5,5,7,7,9\) 的线性表示（由递推的伴随/companion 实现给出），从而 Hankel 秩满足 \(\mathrm{rank}(H)\le d\)。结合定理 \ref{thm:pom-moment-hankel} 的下界，可得这些 \(q\) 下 \(\mathrm{rank}(H)=d\)，并且任何线性 realization \(a_n=u^{\mathsf T}A^n v\) 的维数至少为 \(d\)（等价地，任意加权自动机/有理级数实现的最小状态数至少为 \(d\)，见定理 \ref{thm:hankel-rank-minimal}）。
\end{corollary}

\input{sections/generated/tab_fold_collision_moment_hankel_rank}

\begin{remark}[\texorpdfstring{$p$}{p}-进刚性指纹：\texorpdfstring{$p=5$}{p=5} 在 \texorpdfstring{$k=5$}{k=5} 的 Hankel 证书中首次显性出现]\label{rem:pom-hankel-stiffness-p5}
表 \ref{tab:fold_collision_moment_hankel_rank} 的非零 Hankel 主子式行列式不仅给出秩下界，也携带一个完全内部的算术刚性信号。
对素数 \(p\) 定义“刚性指数”
\[
\mathsf{Stiff}_p(k):=v_p(\det(H_k)),
\]
其中 \(v_p\) 为 \(p\)-进赋值，\(H_k\) 为表中用于 \(S_k\) 的 Hankel 见证块。
在最小的两个可手工分解的例子里：
\[
\det(H_{k=4})=1179648=2^{17}\cdot 3^2,
\qquad
\det(H_{k=5})=155520000000=2^{13}\cdot 3^5\cdot 5^7.
\]
因此 \(\mathsf{Stiff}_5(4)=0\) 而 \(\mathsf{Stiff}_5(5)=7\)：第一个携带高 \(5\)-进赋值的 Hankel 刚性证书恰好出现在 \(k=5\)。
这一跳变为后续追踪“模 \(5\)”类算术效应提供了一个可复核的起点指标（例如随 \(k\) 增大继续记录 \(\mathsf{Stiff}_5(k)\) 的增长与共振缺口 \(\Delta(k)\) 的对照）。
\end{remark}

\subsubsection{任意阶碰撞矩的 moment-kernel 构造与多重分形谱}\label{subsec:pom-moment-general}

\paragraph{从 \texorpdfstring{$q$}{q}-重碰撞到有限态 moment-kernel}
二阶与三阶的核心新点是：碰撞矩并非仅是一个“可枚举序列”，而是由一个\textbf{有限状态纤维积核}生成，从而完全落入 Perron--Frobenius/\(\zeta\) 的统一接口。
这一事实对任意 \(q\ge 2\) 都成立：固定 \(q\) 后，“\(q\) 条窗口折叠到同一稳定类型”的约束是一个有限记忆条件，因此可被编译为一个有限图（SFT/sofic）并由整数邻接矩阵计数。

\begin{definition}[\texorpdfstring{$q$}{q}-重碰撞关系与 moment 序列]\label{def:pom-moment-q}
对整数 \(q\ge 2\)，定义 \(q\)-重碰撞关系
$$
R_m^{(q)}:=
\{(\omega^{(1)},\dots,\omega^{(q)})\in\Omega_m^q:\ \Fold_m(\omega^{(1)})=\cdots=\Fold_m(\omega^{(q)})\},
$$
并定义 \(q\)-阶碰撞矩（moment）
$$
S_q(m):=|R_m^{(q)}|=\sum_{x\in X_m} d_m(x)^q.
$$
\end{definition}

\begin{proposition}[任意阶 moment-kernel 的存在与可执行构造]\label{prop:pom-moment-kernel-exists}
对每个固定的 \(q\ge 2\)，存在一个有限状态非负整数核（邻接矩阵）\(A_q\) 与整数读出向量 \(u_q,v_q\) 使得
$$
S_q(m)=u_q^{\mathsf T}A_q^{\,m-m_0}v_q\qquad(m\ge m_0),
$$
从而 \(\{S_q(m)\}_{m\ge m_0}\) 满足某个\textbf{有限阶整系数线性递推}。

并且 \(A_q\) 可按下述“编译路线”显式构造（因此可推演、可重写、可计算）：
\begin{enumerate}
  \item 选取实现 \(\Fold_\infty\) 的确定性在线核（延迟 \(\delta_{\min}=3\) 的同步核，附录给出其显式有限状态表示）；
  \item 取其 \(q\) 份拷贝并做同步直积，把状态扩展为 \(q\)-元组；
  \item 在每一步只保留那些使 \(q\) 份拷贝产生\textbf{相同输出位}的转移，并投影到可达的本质强连通分量；
  \item 得到的有限图即为一个 \(q\)-重碰撞的 fiber-product 核，其邻接矩阵就是所求 \(A_q\)，其路径计数即给出 \(S_q(m)\)。
\end{enumerate}
\end{proposition}

\begin{proof}
把 \(\Fold_\infty\) 视为一个确定性有限转导器：输入为窗口字母（或其由两条合法输入轨道诱导的输入字母），输出为稳定地址比特；其内部状态空间有限且具有常数延迟（命题 \ref{prop:pom-delay-min} 的在线核即是一个具体实现）。
固定 \(q\) 后，约束“\(q\) 份输出逐位相同”是一个局部同步约束：它只需在每一步比较当前输出符号是否一致，因而仍是有限状态可识别的。
因此可对该转导器做同步直积并施加“输出一致”的筛选，得到一个有限有向图 \(G^{(q)}\)。
长度 \(m\) 的合法路径条数等于 \(R_m^{(q)}\) 的计数（即 \(S_q(m)\)），从而可写成某个非负整数邻接矩阵的矩阵幂读出形式 \(u_q^{\mathsf T}A_q^{m-m_0}v_q\)。
线性递推由 Cayley--Hamilton 定理立得，且递推系数为整数（因为 \(A_q\) 为整数矩阵）。证毕。
\end{proof}

\begin{remark}[可计算的对称压缩：星形 fiber-product 的 \texorpdfstring{$O(q^2)$}{O(q^2)} 轨道表示]\label{rem:pom-star-sympress}
命题 \ref{prop:pom-moment-kernel-exists} 的一般编译路线在 \(q\) 增大时会遇到同步直积状态数的指数爆炸。
但在一类“星形耦合”情形下（即碰撞约束可被组织为“中心轨道与每条叶子轨道分别满足同一 pair 约束”，并且该 pair 约束带有\emph{中心输入符号}分解，例如存在 \(3\)-态 pair kernel 的分解 \(A_2=B_0+B_1\)），
则 \(q\)-重碰撞的星形核天然具有叶子置换对称：
未压缩态空间可取为 \(\{1,2,3\}^{q-1}\)，而 Perron 主特征值由对称子空间决定，从而可将其压缩到轨道计数态
\((n_1,n_2,n_3)\)（满足 \(n_1+n_2+n_3=q-1\)）并把维度降至
\(\binom{q+1}{2}\)。
脚本 \texttt{scripts/exp\_fold\_collision\_star\_kernel\_sympress\_demo.py} 给出该“轨道压缩 \(\Rightarrow\) \(\widetilde A_q\) \(\Rightarrow\) Perron 根估计”的可执行转移计数接口（输入为分解矩阵 \(B_0,B_1\)）。
\end{remark}

\paragraph{从 \texorpdfstring{$r_q$}{r\_q} 到压力与“纤维多重度谱”：凸共轭上界（严格）}
已核验的 \(q\)-阶碰撞核满足 \(S_q(m)=\Theta(r_q^m)\)，其中 \(r_q=\rho(A_q)\) 为 Perron 根（推论 \ref{cor:pom-s2-asymptotic}、\ref{cor:pom-s3-asymptotic}、\ref{cor:pom-s4-asymptotic} 及表 \ref{tab:fold_collision_moment_recursions}）。
因此 moment 指纹不仅给出单点常数 \(h_q=\log(2^q/r_q)\)，还诱导出一条可做 Legendre 变换的“压力曲线”，从而把“空间损失”提升为真正的谱结构。

\begin{definition}[moment 压力与归一化对数矩母函数]\label{def:pom-moment-pressure}
对 \(q\ge 2\) 定义
$$
\tau(q):=\limsup_{m\to\infty}\frac{1}{m}\log S_q(m),
$$
并定义归一化压力（即输出分布 \(\pi_m(x)=d_m(x)/2^m\) 的 \(q\)-阶对数矩母函数）
$$
P(q):=\limsup_{m\to\infty}\frac{1}{m}\log\sum_{x\in X_m}\pi_m(x)^q
=\tau(q)-q\log 2.
$$
当存在 Perron 根 \(r_q\) 使 \(S_q(m)=\Theta(r_q^m)\) 时，有 \(\tau(q)=\log r_q\)，并且 \(P(q)=\log(r_q/2^q)=-h_q\)。
\end{definition}

\input{sections/08_projection_ontology_mathematics/08_projection_ontology_mathematics_part03c_pressure_multifractal_q60.tex}

\begin{definition}[纤维多重度的对数谱函数]\label{def:pom-fiber-spectrum}
令
$$
\gamma_m(x):=\frac{1}{m}\log d_m(x)\qquad(x\in X_m).
$$
