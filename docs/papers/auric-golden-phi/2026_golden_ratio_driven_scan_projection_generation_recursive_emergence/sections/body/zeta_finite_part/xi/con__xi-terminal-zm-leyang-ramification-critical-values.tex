\begin{conclusion}[Lee--Yang The geometric origin of cubic factors: weight mapping \texorpdfstring{$\pi_y$}{pi\_y} Critical value elimination and tangent envelope]\label{con:xi-terminal-zm-leyang-ramification-critical-values}
in conclusion \ref{con:xi-terminal-zm-elliptic-normalization} ellipse model
\[
E:\ Y^2=X^3-X+\frac14
\]
above, follow the conclusion \ref{con:xi-terminal-zm-y-quadratic-extension-minpoly} physical branch of
\[
y:=X^2+Y-\frac12\in\QQ(E),
\qquad
\omega:=\frac{\dd X}{2Y}.
\]
but \(y\) Number of inductions \(4\) finite morphism \(\pi_y:E\to\mathbb P^1_y\), and satisfy \((y)_\infty=4O\) (see conclusion \ref{con:xi-terminal-zm-elliptic-y-hurwitz-passport}）。
Then there is a strict differential identity
\[
\boxed{\;
\dd y=\bigl(4XY+3X^2-1\bigr)\,\omega\; }.
\]
And in the affine open set \(Y\neq 0\) is equivalently written as \(\dd y=\frac{4XY+3X^2-1}{2Y}\,\dd X\)。
Therefore the mapping \(\pi_y:E\to\mathbb P^1_y\)、\(P\mapsto y(P)\) is given by an explicit algebraic curve
\[
4XY+3X^2-1=0
\]
exist \(E\) is given by the intersection point on \(Y\) and then get its \(X\)-coordinates satisfy complete decomposition
\[
\boxed{\;
(X-1)(X+1)\bigl(16X^3-9X^2+1\bigr)=0\; }.
\]
Equivalently, its fifth expansion is
\[
16X^{5}-9X^{4}-16X^{3}+10X^{2}-1=0.
\]
More precisely:
\begin{enumerate}
  \item when \(X=1\), the only finite divergence point is \(\bigl(1,-\tfrac12\bigr)\), its divergence value is \(y=0\)；
  \item when \(X=-1\), the only finite divergence point is \(\bigl(-1,\tfrac12\bigr)\), its divergence value is \(y=1\)；
  \item if \(\alpha\) is an irreducible cubic equation \(16X^{3}-9X^{2}+1=0\), take
  \[
  Y=\frac{1-3\alpha^{2}}{4\alpha},
  \qquad
  P_{\alpha}:=(\alpha,Y)\in E(\overline{\QQ}),
  \]
  but \(P_{\alpha}\) for \(\pi_y\), and its divergence value is
  \[
  y_{\alpha}
  =\alpha^{2}+\frac{1-3\alpha^{2}}{4\alpha}-\frac12
  =\frac{(\alpha-1)\bigl(4\alpha^{2}+\alpha-1\bigr)}{4\alpha}.
  \]
\end{enumerate}
thereby \(\pi_y\) The set of finite divergence values ​​is exactly \(y=0\)、\(y=1\) and \(\{y_{\alpha}\}\)。
Among them, Lee--Yang cubic polynomial
\[
P_{\mathrm{LY}}(y):=256y^3+411y^2+165y+32
\]
It can be characterized as a strict consequential identity by a pure elimination certificate
\[
\boxed{\;
\mathrm{Res}_X\!\left(16X^3-9X^2+1,\ 4Xy-\bigl(4X^3-3X^2-2X+1\bigr)\right)
=-64\,P_{\mathrm{LY}}(y)\; }.
\]
Equivalently, exchange the elimination order (here the two equations are about \(X\) the times are all \(3\))get
\[
\boxed{\;
P_{\mathrm{LY}}(y)=\frac1{64}\,
\mathrm{Res}_X\!\left(4Xy-\bigl(4X^3-3X^2-2X+1\bigr),\ 16X^3-9X^2+1\right)\; }.
\]
Therefore the three divergence values ​​given by the above three points \(\{y_{\alpha}\}\) exactly \(P_{\mathrm{LY}}(y)=0\) of three.
Therefore, the Lee--Yang cubic factor is not only the discriminant kernel of the quartic spectrum (Conclusion \ref{con:xi-terminal-zm-discriminant-leyang}), which is also equivalently given \(\pi_y\) have three irrational critical values ​​in \(\QQ[y]\) (Conclusion \ref{con:xi-terminal-zm-leyang-cubic-arithmetic}）；
Geometrically, this is equivalent to a family of parabolas
\[
Y=y-X^2+\frac12
\]
and \(E\) in the elimination sense.
\end{conclusion}
\begin{proof}[Points of proof]
Differential relations from curve equations \(2Y\,\dd Y=(3X^2-1)\dd X\) have to \(\dd Y=(3X^2-1)\omega\),thereby
\[
\dd y=2X\dd X+\dd Y=\bigl(4XY+3X^2-1\bigr)\omega.
\]
Therefore, the finite divergence point satisfies \(4XY+3X^2-1=0\)。
It can be solved from this
\(
Y=(1-3X^2)/(4X)
\)
and substitute \(E\) equation, simplified to get
\(
(X-1)(X+1)(16X^3-9X^2+1)=0
\)。
when \(X=1\) and \(X=-1\) are obtained respectively when \(y=0\) and \(y=1\)。
to satisfy \(16X^3-9X^2+1=0\), the non-trivial divergence point of \(y=X^2+Y-\frac12\) and \(Y=(1-3X^2)/(4X)\) jointly obtain linear constraints
\[
4Xy=4X^3-3X^2-2X+1.
\]
pair variables \(X\) by elimination (resultant expression) we get the strict identity
\(
\mathrm{Res}_X(16X^3-9X^2+1,\ 4Xy-(4X^3-3X^2-2X+1))=-64\,P_{\mathrm{LY}}(y)
\)，
Thus the non-trivial finite divergence value is given by \(P_{\mathrm{LY}}(y)=0\) accurately described and concluded \ref{con:xi-terminal-zm-discriminant-leyang} The discriminant decomposition of is consistent.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Knot splitting of weight plane quartic model: dual elimination of Lee--Yang cubic factors and conjugate fiber cubic factors]\label{con:xi-terminal-zm-leyang-conjugate-fiber-cubic}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-ramification-critical-values} mark, and is represented by
\(
Y=y-X^{2}+\tfrac12
\)
eliminate \(Y\)。
but \(E\) has the first fourteenth representation
\[
\QQ(E)\cong \QQ(X,y)/(F),
\qquad
F(X,y):=X^{4}-X^{3}-(2y+1)X^{2}+X+y(y+1).
\]
Under this planar quartic model,\(\pi_y:E\to\mathbb P^{1}_{y}\) is equivalent to the system of equations
\[
F(X,y)=0,\qquad \partial_{X}F(X,y)=0,
\]
and to \(y\) Obtain the strict resultant identity by elimination
\[
\boxed{\;
\mathrm{Res}_{y}\bigl(F(X,y),\partial_{X}F(X,y)\bigr)=-(X-1)(X+1)\bigl(16X^{3}-9X^{2}+1\bigr)\; }.
\]
\par
make
\(
g(X):=16X^{3}-9X^{2}+1
\), and defined with \(P_{\mathrm{LY}}\) conjugated cubic factors
\[
Q_{\mathrm{LY}}(y):=256y^{3}+195y^{2}+93y-48\in\ZZ[y].
\]
Then for the variable \(X\) elimination gives another strict result split
\[
\boxed{\;
\mathrm{Res}_{X}\bigl(F(X,y),g(X)\bigr)=P_{\mathrm{LY}}(y)\,Q_{\mathrm{LY}}(y)\; }.
\]
Therefore, for any satisfying \(g(X)=0\) root \(\alpha\)：
in disagreement constraints \(\partial_XF=0\), the weight value under
\[
y=\frac{4\alpha^{3}-3\alpha^{2}-2\alpha+1}{4\alpha},
\qquad
P_{\mathrm{LY}}(y)=0,
\]
And its elliptical inversion \(\iota:(X,Y)\mapsto (X,-Y)\) gives the second (non-divergent) fiber,
Its weight value satisfies \(Q_{\mathrm{LY}}(y)=0\)；
Two sets of cubic roots \(\iota\) exchange.
Thus the Lee--Yang factor \(P_{\mathrm{LY}}(y)\) is recognized as \(\pi_y\), and \(Q_{\mathrm{LY}}(y)\) portrays the same \(g(X)=0\) on conjugate non-divergent fibers.
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(Y=y-X^{2}+\tfrac12\) substitution \(E\) and expand it, we get \(F(X,y)=0\) the first four times.
In the plane model, the finite bifurcation point satisfies \(F=\partial_XF=0\)；
right \(y\) do the consequential elimination and in \(\ZZ[X]\), we get \(\mathrm{Res}_{y}(F,\partial_XF)\) split identity.
\par
right \(g(X)=0\), the equation \(F(X,y)=0\) gives the corresponding two weight fibers (given by \(Y\mapsto -Y\) exchange);
right \(X\) can be obtained by elimination of the resultant expression \(y\) is directly decomposed into two cubic factors.
The weight value of the bifurcation point is given by the conclusion \ref{con:xi-terminal-zm-leyang-ramification-critical-values} linear constraints of
\(
4Xy=4X^{3}-3X^{2}-2X+1
\)
determined, so its corresponding factor must be \(P_{\mathrm{LY}}\), the other factor is defined as \(Q_{\mathrm{LY}}\) and correspond to conjugate non-divergent fibers.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Weight four-overlay Hurwitz Passport with Divergence Divider:\texorpdfstring{$(2,2,2,2,2,4)$}{(2,2,2,2,2,4)} full explicitness]\label{con:xi-terminal-zm-elliptic-y-hurwitz-passport}
mapping \(\pi_y:E\to\mathbb P^1_y\) is the number of times \(4\), and satisfies:
\begin{enumerate}
  \item \textbf{pole structure.}
  \(y\) at infinity \(O\) has a unique pole everywhere, and
  \[
  \boxed{\ (y)_\infty=4O\ }.
  \]
  thus \(O\) exist \(\infty\in\mathbb P^1_y\) Full divergence above, divergence index \(e_O=4\)。

  \item \textbf{Differences are eliminated.}
  set up \(\alpha_1,\alpha_2,\alpha_3\) is the cubic equation \(16X^3-9X^2+1=0\) of the three roots, and let
  \[
  Q_i:=\left(\alpha_i,\ \frac{1-3\alpha_i^2}{4\alpha_i}\right)\in E(\overline{\QQ})\qquad(i=1,2,3).
  \]
  but \(\pi_y\)'s divergence divisor is
  \[
  \boxed{\;
  R=3O+\Bigl(1,-\frac12\Bigr)+\Bigl(-1,\frac12\Bigr)+Q_1+Q_2+Q_3\; }.
  \]
  Therefore, except for the point at infinity, all divergence points are single divergences (exponential \(2\)）。

  \item \textbf{Set of divergence values ​​(Hurwitz Passport).}
  The divergence value on the base is
  \[
  \boxed{\ \{\infty,0,1\}\cup\{y_1,y_2,y_3\}\ },
  \]
  in \(y_1,y_2,y_3\) for \(P_{\mathrm{LY}}(y)=0\) of three.
  Its local divergence type is on five finite divergence values. \((2)\) and the point at infinity \((4)\), that is, the Hurwitz passport is \((2,2,2,2,2,4)\)。
  because \(y=\infty\) The cyclic form of the local divergence single value on four sheets of paper is \(4\)-loop, and any finite divergence value is a transposition,
  Therefore, the geometric single value group consists of a \(4\)-loop is generated with a transposition, thus must be \(S_4\)。

  \item \textbf{Principal divisor decomposition of two special fibers.}
  There is a strict principal divisor identity
  \[
  \boxed{\;
  \mathrm{div}(y)=\Bigl(0,\frac12\Bigr)+\Bigl(-1,-\frac12\Bigr)+2\Bigl(1,-\frac12\Bigr)-4O\; },
  \qquad
  \boxed{\;
  \mathrm{div}(y-1)=\Bigl(2,-\frac52\Bigr)+\Bigl(1,\frac12\Bigr)+2\Bigl(-1,\frac12\Bigr)-4O\; }.
  \]
  Especially, in \(E(\QQ)\) gives two strict relations under the group law of
  \[
  \Bigl(0,\frac12\Bigr)+\Bigl(-1,-\frac12\Bigr)+2\Bigl(1,-\frac12\Bigr)=O,
  \qquad
  \Bigl(2,-\frac52\Bigr)+\Bigl(1,\frac12\Bigr)+2\Bigl(-1,\frac12\Bigr)=O.
  \]
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(X\) and \(Y\) exist \(O\) The extreme orders at are respectively \(2\) and \(3\),get \(y=X^2+Y-\tfrac12\) exist \(O\), the extreme order is \(4\),thereby \((y)_\infty=4O\)。
The point of divergence is \(\dd y=0\) is given as the zero point, see conclusion \ref{con:xi-terminal-zm-leyang-ramification-critical-values}；
And by the Riemann--Hurwitz formula \(\pi_y\) for inspection: due to \(g(E)=1\) and \(\deg(\pi_y)=4\), the total divergence must be \(8\),and \(O\) contribute \(e_O-1=3\), the remaining five finite divergence points must therefore be single divergences.
The decomposition of two principal divisors is given by \(y=0\) and \(y=1\) of fiber in \(X\)-The factorization on the coordinates is directly deduced (where the divergence point corresponds to the double intersection point), and is consistent with the conclusion \ref{con:xi-terminal-zm-elliptic-normalization} The result of the divisor is consistent.
The group law relationship consists of the main element except the son in \(\mathrm{Pic}^0(E)\cong E\) in the corresponding unit element is obtained.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Mainization of divergent divisors and group traces: The additive trace of Lee--Yang's three divergence points collapses into an explicit rational point]\label{con:xi-terminal-zm-leyang-ramification-divisor-principalization-trace}
Follow the conclusion \ref{con:xi-terminal-zm-elliptic-y-hurwitz-passport} mark, and order
\[
f:=4XY+3X^2-1\in\QQ(E)^\times.
\]
set up \(O\) is an infinite point, remember two rational divergence points
\[
Q_0:=\Bigl(1,-\frac12\Bigr),\qquad Q_0':=\Bigl(-1,\frac12\Bigr),
\]
And order \(Q_1,Q_2,Q_3\in E(\overline{\QQ})\) to satisfy \(16X^3-9X^2+1=0\) of three irrational points of disagreement (these \(Y\)-coordinates given by \(4XY+3X^2-1=0\) is the only certainty).
Then there is a strict principal divisor identity
\[
\boxed{\;
\mathrm{div}(f)=Q_0+Q_0'+Q_1+Q_2+Q_3-5O\; }.
\]
Therefore, it is established in the sense of the elliptic curve group law
\[
Q_1+Q_2+Q_3=-(Q_0+Q_0')=\Bigl(\frac14,-\frac18\Bigr)\in E(\QQ),
\]
and
\[
Q_0+Q_0'=\Bigl(\frac14,\frac18\Bigr).
\]
Therefore, the three divergence points located above the three Lee--Yang divergence values ​​(their definition domain is \(\QQ(\alpha)\)，\(\alpha\) for \(16X^3-9X^2+1=0\) satisfies a global addition constraint in the sense of group law: its addition trace strictly falls into a fixed rational point \((\tfrac14,-\tfrac18)\)。
Combined with conclusion \ref{con:xi-terminal-zm-elliptic-mw-generator-alignment} free generator of \(R=(0,\tfrac12)\) and \(Q_0=-[2]R\)、\(Q_0'=-[3]R\), the above trace identity can be rewritten as
\[
Q_1+Q_2+Q_3=5R.
\]
This identity can also be regarded as the linear equivalence relationship given by Riemann-Hurwitz in \(\mathrm{Pic}^0(E)\cong E\) under the materialization.
due to conclusion \ref{con:xi-terminal-zm-elliptic-rational-points-denominator-law} given \(E(\QQ)_{\mathrm{tors}}=\{O\}\),and \((\tfrac14,-\tfrac18)\neq O\), so the rational trace point is of infinite order.
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-leyang-ramification-critical-values} The differential identity of \(\dd y=f\,\omega\) it can be known:
\(f\) The finite zero point is exactly \(\pi_y\), and by the conclusion \ref{con:xi-terminal-zm-elliptic-y-hurwitz-passport} The Hurwitz passport can be seen to be first order zero.
at infinity point \(O\) to obtain the homogenization parameters \(t\) make \(X\sim t^{-2}\)、\(Y\sim t^{-3}\),but
\[
f=4XY+3X^2-1\sim 4t^{-5},
\]
Therefore \(f\) exist \(O\) has a fifth-order pole and no other poles, resulting in the principal divisor identity shown.
because \(\mathrm{div}(f)\) exist \(\mathrm{Pic}^{0}(E)\cong E\) is the zero category, and it is derived \(Q_0+Q_0'+Q_1+Q_2+Q_3=O\)。
Finally, a reasonable point \(Q_0,Q_0'\) using the chordal addition formula: slope
\[
m=\frac{\frac12-(-\frac12)}{-1-1}=-\frac12
\]
given
\[
x(Q_0+Q_0')=m^2-1-(-1)=\frac14,\qquad
y(Q_0+Q_0')=m\Bigl(1-\frac14\Bigr)-\Bigl(-\frac12\Bigr)=\frac18,
\]
thereby \(Q_0+Q_0'=(\tfrac14,\tfrac18)\) and \(-(Q_0+Q_0')=(\tfrac14,-\tfrac18)\) and draw conclusions.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Lee--Yang norm identity of discriminant:\texorpdfstring{$-y(y-1)P_{\mathrm{LY}}(y)$}{-y(y-1)P(y)} is the relative norm of different generators]\label{con:xi-terminal-zm-leyang-discriminant-norm-identity}
set up
\[
k:=\QQ(y),\qquad K:=\QQ(E)=\QQ(X,Y),
\]
but \(K/k\) is the number of times \(4\) function domain expansion.
This expansion is divisible and its different ideals are \(K\) is the main ideal.
make
\[
\delta:=4Xy-\bigl(4X^3-3X^2-2X+1\bigr)\in K.
\]
exist \(E\) using \(Y=y-X^2+\tfrac12\) can be transformed into
\[
\delta=4XY+3X^2-1,
\]
and \(\delta\) exist \(\pi_y\) is exactly first-order zero at the finite divergence point.
therefore
\[
\mathfrak D_{K/k}=(\delta)=\bigl(1-3X^2-4XY\bigr).
\]
Then its relative norm satisfies the strict identity
\[
\boxed{\;
\operatorname{N}_{K/k}(\delta)=-y(y-1)\,P_{\mathrm{LY}}(y)\; }.
\]
\end{conclusion}
\begin{proof}[Points of proof]
Depend on \(Y=y-X^2+\tfrac12\) erase \(Y\) have to \(X\) exist \(k\) satisfies the quartic equation on
\[
F(X,y)=X^4-X^3-(2y+1)X^2+X+y(y+1)=0,
\]
and directly calculate it to give \(\delta=-\partial_XF(X,y)\)。
because \(F\) for about \(X\), the standard theory gives different ideals
\(
\mathfrak D_{K/k}=\bigl(\partial_XF(X,y)\bigr)
\)；
thereby \(\mathfrak D_{K/k}=(\delta)\)。
Will again \(y=X^2+Y-\tfrac12\) Substitute \(\partial_XF(X,y)\), that is, get
\(
\partial_XF(X,y)=1-3X^2-4XY
\), so \((\delta)=\bigl(1-3X^2-4XY\bigr)\)。
because \(F\) about \(X\) first, norm \(\operatorname{N}_{K/k}(\delta)\) is equal to the resulting expression \(\mathrm{Res}_X(F,\delta)\) (equivalent to \(\mathrm{Disc}_X(F)\).
And the result formula is calculated as \(-y(y-1)P_{\mathrm{LY}}(y)\), and conclusion \ref{con:xi-terminal-zm-discriminant-leyang} The discriminant decomposition of is consistent.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[\texorpdfstring{$2$}{2}-division polynomial in \texorpdfstring{$y$}{y}-Norm squaring under projection: hiding cubic governing polynomials]\label{con:xi-terminal-zm-2division-norm-square}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-discriminant-norm-identity} The mark still makes \(k=\QQ(y)\)、\(K=\QQ(E)\)。
remember \(2\)-division three times
\[
D(X):=4X^3-4X+1=4Y^2\in K.
\]
and remember its presence \(y\)-image control polynomial in coordinates
\[
S(y):=16y^3-8y^2-4y+1.
\]
then it is expanding \(K/k\) satisfies the strict identity
\[
\boxed{\;
\operatorname{N}_{K/k}\bigl(D(X)\bigr)=S(y)^2\; }.
\]
Equivalently, by \(2Y=2y-2X^2+1\) (see conclusion \ref{con:xi-terminal-zm-y-quadratic-extension-minpoly})get
\[
\boxed{\;
\operatorname{N}_{K/k}(2Y)=-S(y)\; }.
\]
Therefore a cubic polynomial \(S(y)\) accurately portray \(E[2]\setminus\{O\}\) exist \(\pi_y\); and because \(D(X)=(2Y)^2\) exist \(K\) is the square, and its relative norm is \(k\) must be a perfect square, and the above formula gives the minimum integer coefficient certificate of the squared.
\par
Equivalently, by \(E[2]\setminus\{O\}\) the characteristic equation of \(Y=0\) and \(D(X)=0\) have to \(y=X^2-\tfrac12\), so there is a strict elimination certificate
\[
\boxed{\;
\mathrm{Res}_X\!\left(4X^3-4X+1,\ y-\Bigl(X^2-\frac12\Bigr)\right)=S(y)\; }.
\]
\end{conclusion}
\begin{proof}[Points of proof]
Still using the fourth equation \(F(X,y)=0\) (see conclusion \ref{con:xi-terminal-zm-leyang-discriminant-norm-identity}) as \(K/k\), then
\(
\operatorname{N}_{K/k}(D(X))=\mathrm{Res}_X(F,D)
\)
and
\(
\operatorname{N}_{K/k}(2Y)=\mathrm{Res}_X(F,2y-2X^2+1)
\)
Calculated as a standard result formula;
Direct simplification yields the identity shown.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Lee--Yang Isomorphism of cubic domains and critical cubic domains: Explicit generator correspondence and discriminant \texorpdfstring{$37$}{37} indestructibility]\label{con:xi-terminal-zm-leyang-cubic-field-isomorphism}
make \(\alpha\) is the cubic equation
\[
16X^3-9X^2+1=0
\]
any root of , and define
\[
\beta:=\frac{4\alpha^3-3\alpha^2-2\alpha+1}{4\alpha}
=\alpha^{2}-\frac34\alpha-\frac12+\frac{1}{4\alpha}.
\]
but:
\begin{enumerate}
  \item \(\beta\) satisfies the Lee--Yang cubic equation \(P_{\mathrm{LY}}(\beta)=0\)。
  \item thus induces cubic domain identity \(\QQ(\alpha)=\QQ(\beta)\), that is, "non-trivial divergence point" \(X\)-coordinate field" and "Lee--Yang divergence value field" are the same cubic algebraic number field, but the selection of generators is different.
  \The polynomial discriminants of the two representations of item satisfy
  \[
  \boxed{\ \mathrm{Disc}(16X^3-9X^2+1)=-2^2\cdot 3^3\cdot 37\ },
  \qquad
  \boxed{\ \mathrm{Disc}(P_{\mathrm{LY}})=-3^9\cdot 31^2\cdot 37\ }.
  \]
  And the difference between the two is strictly a square factor:
  \[
  \boxed{\ \mathrm{Disc}(P_{\mathrm{LY}})=\mathrm{Disc}(16X^3-9X^2+1)\cdot\Bigl(\frac{837}{2}\Bigr)^{2}\ }.
  \]
  Therefore, the squared free parts of both discriminants are the same \(-111=-3\cdot 37\), the corresponding unique quadratic resolution subdomains are all \(\QQ(\sqrt{-111})\)；
  Therefore two \(S_3\)-expanded splitting domain shares this virtual quadratic subdomain.
  At the same time, in this cubic field, prime numbers \(37\) necessary divergence (which appears in the above discriminant to odd powers and cannot be eliminated by the square of the integer exponent), so that the Lee--Yang cubic field is arithmetically primed by the elliptic discriminant \(37\) firmly anchored.
  \item due to \(16X^3-9X^2+1\) and \(P_{\mathrm{LY}}(y)\) are all in \(\QQ\) is irreducible and the discriminant is non-square, so the Galois groups of its split domain are all isomorphic to \(S_3\), and the times of splitting domains are all \(6\)。
  Depend on \(\QQ(\alpha)=\QQ(\beta)\) It can be seen that the minimum splitting domains of the two are consistent, so the irrational divergence point and the irrational divergence value generate \(S_3\)-closure is completely consistent.
\end{enumerate}
\end{conclusion}
\begin{proof}[Points of proof]
By conclusion \ref{con:xi-terminal-zm-leyang-ramification-critical-values} The critical condition of
\(
4\alpha\beta=4\alpha^3-3\alpha^2-2\alpha+1
\), so \(\beta\in\QQ(\alpha)\)。
Compare this linear relationship with \(\alpha\) can be obtained by elimination of the cubic equation \(P_{\mathrm{LY}}(\beta)=0\),thereby \(\QQ(\beta)\subseteq\QQ(\alpha)\)。
Both are three expansions, so they must be equal.
The discriminant is decomposed into a direct algebraic calculation.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\input{con__xi-terminal-zm-leyang-cubic-field-minimal-discriminant}

\input{con__xi-terminal-zm-leyang-branchpolynomial-discriminant-modp-collisions}

\begin{conclusion}[bad prime number \texorpdfstring{$37$}{37} Collapse of the same root at: discriminant and congruence decomposition of three cubic control polynomials]\label{con:xi-terminal-zm-leyang-badprime-37-common-double-root}
Follow the conclusion \ref{con:xi-terminal-zm-leyang-conjugate-fiber-cubic} The conjugate cubic
\(
Q_{\mathrm{LY}}(y)=256y^{3}+195y^{2}+93y-48
\)
Three times with Lee--Yang
\(
P_{\mathrm{LY}}(y)=256y^{3}+411y^{2}+165y+32
\)，
And note that the bisection image controls three times (Conclusion \ref{con:xi-terminal-zm-2division-norm-square}）
\[
C(y):=16y^{3}-8y^{2}-4y+1
\qquad(\text{i.e. conclusion \ref{con:xi-terminal-zm-2division-norm-square} in }S(y)).
\]
Then the discriminants of the three all carry prime factors \(37\), and satisfies strict decomposition:
\[
\mathrm{Disc}\bigl(C(y)\bigr)=2^{8}\cdot 37,\qquad
\mathrm{Disc}\bigl(P_{\mathrm{LY}}(y)\bigr)=-3^{9}\cdot 31^{2}\cdot 37,\qquad
\mathrm{Disc}\bigl(Q_{\mathrm{LY}}(y)\bigr)=-3^{3}\cdot 37\cdot 2677^{2}.
\]
Furthermore, in \(\FF_{37}[y]\), double-root collapse occurs with the same parameter value among the three:
\[
\begin{aligned}
C(y)&\equiv 16\,(y-7)(y-6)^{2}\ (\mathrm{mod}\ 37),\\
P_{\mathrm{LY}}(y)&\equiv -3\,(y-14)(y-6)^{2}\ (\mathrm{mod}\ 37),\\
Q_{\mathrm{LY}}(y)&\equiv -3\,(y-16)(y-6)^{2}\ (\mathrm{mod}\ 37).
\end{aligned}
\]
therefore \(y\equiv 6\ (\mathrm{mod}\ 37)\) are simultaneously common multiple roots of three cubic control polynomials.
At the same time, for the physical base point \(P=(2,-\tfrac52)\in E(\QQ)\)(in conclusion \ref{con:xi-terminal-zm-elliptic-rational-points-denominator-law}), its inverse element satisfies \(y(-P)=6\)，
The collapse point thus coincides with an explicit rational point marker at the integer level.
\end{conclusion}
\begin{proof}[Points of proof]
discriminant and model \(37\) factored into \(\ZZ[y]\) and \((\ZZ/37\ZZ)[y]\) within finite algebraic calculations.
\(y(-P)=6\) Depend on \(y=X^{2}+Y-\tfrac12\) at point \(-P=(2,\tfrac52)\) can be obtained by directly substituting.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\input{con__xi-terminal-zm-elliptic-lattes-badprime-37-degree-drop}

\input{con__xi-terminal-zm-elliptic-weight-doubling-modular-equation}
\input{con__xi-terminal-zm-4division-weight-image}

\begin{conclusion}[Puiseux unification at all divergence points: explicit formulas for square root critical exponents and leading coefficients]\label{con:xi-terminal-zm-branch-puiseux-uniformization}
Will \(F(X,y)=0\) is regarded as defining an algebraic function \(X=X(y)\) of the quartic equation (see conclusion \ref{con:xi-terminal-zm-leyang-discriminant-norm-identity}）。
Then for each finite difference value
\(
y_0\in\{0,1\}\cup\{y_1,y_2,y_3\}
\)
(in \(P_{\mathrm{LY}}(y_i)=0\)), there are local parameters \(t\) make \(y-y_0=t^2\), and there are exactly two branches that merge twice at this point, and their Puiseux expansion has a universal square root form
\[
X=\alpha_0\pm c_0\,t+O(t^2).
\]
in:
\begin{enumerate}
  \item is in \(y_0=0\)(merging point \(X=1\)), desirable \(y=t^2\), then the two merge branches satisfy
  \[
  X=1\pm \frac{1}{\sqrt2}\,t+\frac58\,t^2\mp \frac{43}{64\sqrt2}\,t^3+O(t^4),
  \]
  Right now
  \[
  X=1\pm \frac{1}{\sqrt2}\,y^{1/2}+\frac58\,y\mp \frac{43}{64\sqrt2}\,y^{3/2}+O(y^2).
  \]
  \item is in \(y_0=1\)(merging point \(X=-1\)), desirable \(y-1=t^2\), then the two merge branches satisfy
  \[
  X=-1\pm \frac{i}{\sqrt6}\,t+O(t^2),
  \]
  Right now
  \[
  X=-1\pm \frac{i}{\sqrt6}\,(y-1)^{1/2}+O(y-1).
  \]
  \item at any Lee--Yang divergence value \(y_0=y_i\)(merging point \(X=\alpha_i\),in \(\alpha_i\) for \(16X^3-9X^2+1=0\), the leading coefficient satisfies
  \[
  \boxed{\;
  c_0^2=\frac{2}{3}\cdot\frac{3\alpha_i^2-1}{\alpha_i^2-1}\ \in\ \QQ(\alpha_i)=\QQ(y_i)\; }.
  \]
\end{enumerate}
\par
Get local parameters \(t=\sqrt{y-y_0}\), then the two combined branches can be written as
\[
\lambda_{\pm}(y)=\alpha_0\pm c_0\sqrt{y-y_0}+O(y-y_0).
\]
Therefore define the boundary free energy for any logarithmic branch \(f_{\mathrm{edge}}(y):=\log\lambda_{\pm}(y)\), then in \(y\to y_0\) has a standard \(1/2\) order divergence
\[
f_{\mathrm{edge}}(y)=\log\alpha_0\pm \frac{c_0}{\alpha_0}\sqrt{y-y_0}+O(y-y_0),
\qquad
\frac{\dd}{\dd y}f_{\mathrm{edge}}(y)=\pm \frac{c_0}{2\alpha_0}(y-y_0)^{-1/2}+O(1).
\]
exist \(y_0=y_{\mathrm{LY}}\),\(\alpha_0=\lambda_{\mathrm{LY}}\) is the conclusion \ref{con:xi-terminal-zm-leyang-lambda-cubic} the only real root of
\[
c_0^2=\frac{2(3\lambda_{\mathrm{LY}}^{2}-1)}{3(\lambda_{\mathrm{LY}}^{2}-1)},
\qquad
c_0\approx 0.7476109845904598\ldots.
\]
\end{conclusion}
\begin{proof}[Points of proof]
At the point of divergence there is \(F(\alpha_0,y_0)=0\)、\(\partial_XF(\alpha_0,y_0)=0\),and \(\partial_{XX}F(\alpha_0,y_0)\neq 0\)。
right \(F\) as a binary Taylor expansion and let \(y-y_0=t^2\) gives us the square root Puiseux.
Generally, the leading coefficient is given by the second-order expansion \(c_0^2=-2(\partial_yF)/(\partial_{XX}F)\) exist \((\alpha_0,y_0)\);
in the original spectral coordinates \((\lambda,y)\) under (where \(F(X,y)=\Pi(X,y)\)), the same formula can also be written as
\(
c_0^2=-2\,\partial_y\Pi(\alpha_0,y_0)/\partial_{\lambda}^{2}\Pi(\alpha_0,y_0)
\)。
right \(y_0=y_i\) situation reuse \(\alpha_i\), we get the closed form shown.
right \(y_0=0,1\) can further recurse the coefficients to obtain the expansions listed.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[From ellipse to height:\texorpdfstring{$S_4$}{S4} The geometric closure curve of the split domain belongs to \(16\), the intermediate formal business belongs to stratification]\label{con:xi-terminal-zm-s4-splitting-curve-genus16}
make \(\widetilde{K}/\QQ(y)\) is the fourth equation \(F(X,y)\), and let \(\widetilde{C}\) is the smooth projective curve corresponding to its function domain.
in conclusion \ref{con:xi-terminal-zm-generic-galois-s4} Generics \(S_4\) under sex,\(\widetilde{C}\to\mathbb P^1_y\) for \(S_4\)-Galois cover, and at six divergence values
\[
\{\infty,0,1,y_1,y_2,y_3\}
\]
The orders of the inert group at are respectively
\[
e_\infty=4,\qquad e_0=e_1=e_{y_1}=e_{y_2}=e_{y_3}=2.
\]
in \(e_\infty=4\) corresponds to \(y\) at infinity \(O\) the total divergence pole at \((y)_\infty=4O\)(in conclusion \ref{con:xi-terminal-zm-elliptic-y-hurwitz-passport}）。
Obtained from Riemann--Hurwitz formula
\[
\boxed{\ g(\widetilde{C})=16\ }.
\]

In addition, for \(S_4\) gives two structural intermediates:
\begin{enumerate}
  \item \textbf{Symbolic quadratic quotient (fixed subgroup \(A_4\)）。}
  The corresponding curve is double overlay
  \[
  C_2:\ w^2=-y(y-1)P_{\mathrm{LY}}(y),
  \]
  The points of divergence are the above six points, so \(g(C_2)=2\)。
  \item \textbf{Klein quaternary subgroup quotient (fixed subgroup \(V_4\)）。}
  Corresponding \(S_3\)-Galois cover \(C_6\to\mathbb P^1_y\) are dichotomous at the above six points, so \(g(C_6)=4\)。
\end{enumerate}
Therefore, although the root domain is normalized to the genus \(1\) of the elliptic curve \(E\), but enter \(S_4\) The geometric closure of the split domain inevitably introduces properties \(16\) of the high attribute curve;
Quadratic AND \(S_3\) regular merchants fall into the \(2\) and genus \(4\), giving a clear hierarchy of geometric complexity.
\end{conclusion}
\begin{proof}[Points of proof]
right \(S_4\)-Galois cover uses the Riemann--Hurwitz formula:
If at the difference value \(b\) where the order of the inert group is \(e_b\), then the total difference contribution is \(|S_4|\,(1-1/e_b)\)。
Substitute \(|S_4|=24\) and \((e_\infty,e_0,e_1,e_{y_1},e_{y_2},e_{y_3})=(4,2,2,2,2,2)\) have to \(g(\widetilde{C})=16\)。
Second quotient and \(S_3\) quotient is determined by the same formula in degree \(2\) and times \(6\) are calculated separately.
Reproducible verification script:\texttt{scripts/exp\_fold\_zm\_elliptic\_leyang\_cover\_geometry\_audit.py}. Certification completed.
\end{proof}

\begin{remark}[Further structured directions:\texorpdfstring{$S_4$}{S4} Representation on Galois closure and periodic data]\label{rem:xi-terminal-zm-s4-closure-representation-periods}
By conclusion \ref{con:xi-terminal-zm-s4-splitting-curve-genus16} of \(S_4\)-Galois cover \(\widetilde{C}\to\mathbb P^1_y\) (genus \(16\)），
naturally get group \(S_4\) exist \(H^{0}(\widetilde{C},\Omega^{1})\) on the representation.
Make an irreducible decomposition of this representation and combine it with the elliptic curve \(E\) modular anchor point (discriminant \(37\), see notes \ref{rem:xi-terminal-elliptic-37a1-modular-anchor}) docking,
Branch unification, spectral branch replacement, and related periodic integral and elliptic logarithmic data can be uniformly characterized under the same framework.
\end{remark}

\input{con__xi-terminal-zm-elliptic-leyang-weight-doubling-audit-chain}

