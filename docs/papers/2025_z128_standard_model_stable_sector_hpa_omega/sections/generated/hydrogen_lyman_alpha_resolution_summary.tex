\paragraph{Audit summary (hydrogen Lyman-$\alpha$ $\mu\to r\to m_{\mathrm{eff}}$).} \AuditTag We map the vendored Lyman-$\alpha$ transition energy (a composite atomic scale) to $\mu$ in GeV and then to $r(\mu)=\log(\mu/m_e)/\log\varphi$ using $m_e=\texttt{M\_E\_GEV}$. We report $m_{\mathrm{eff}}(\mu)=6+\lfloor r(\mu)/r_{\mathrm{step}}\rfloor$ with $r_{\mathrm{step}}=2\pi$ (Section~\ref{sec:falsifiability_predictions}). Interpretation boundary: $m_{\mathrm{eff}}<6$ is a scale label only.