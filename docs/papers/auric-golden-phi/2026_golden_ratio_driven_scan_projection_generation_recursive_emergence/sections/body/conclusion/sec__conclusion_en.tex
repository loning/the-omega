\section{Conclusion}\label{sec:conclusion}

The main thread of this paper organizes the microstate space induced by finite-window readout into stable types, realizable arithmetic structures, and certifiable statistical/spectral fingerprint interfaces through an auditable constructive chain. The corresponding main conclusions can be consolidated along the three-stage structure of ``input caliber---constructive mechanism---bridging and verification'' as follows.

\subsection{Main Conclusions: Grouped by Mechanism}\label{subsec:core-conclusions}
\paragraph{Fixing the input caliber}
On the number-theoretic side, the anti-resonance canonical representative and finite-sample discrepancy certificate fix the stability baseline (Section \ref{sec:principles}); on the dynamical side, the scan--projection generator (group action and binary readout) fixes the minimal input of observable objects (Section \ref{sec:spg}). These calibers remain unchanged throughout the paper, ensuring that subsequent conclusions possess a traceable dependency chain.
On this interface, ``scan depth \(\Rightarrow\) clarity'' can be compressed into the optimal misclassification profile and boundary exponential law, and under address semantics can be further decomposed into the double-budget capacity law for two failure mechanisms---``unsynchronized (statistical-type \(\mathsf{NULL}\))'' and ``address collision'' (Theorems \ref{thm:spg-clarity-boundary-dimension}, \ref{thm:spg-double-budget-address-capacity}); under online regime, this clarity profile is further coupled with the sync-subtracted Chebotarev capacity inequality, thereby separating the definitional synchronization threshold from the statistical equalization budget into two auditable ledger segments (Theorem \ref{thm:pom-sync-subtracted-chebotarev-capacity}).

\paragraph{Resolution folding and recursive addressing}
This paper constructs finite-window folding \(\Fold_m\) and proves its cross-resolution compatibility and unified sofic factor interface conclusions (Theorem \ref{thm:fold-suite}; see also Definition \ref{def:fold-word} and Proposition \ref{prop:fold-basic}). Within the same measurable framework, recursive addressing is rigorously formalized as the endogenous mechanism of ``readable prefixes'': when the address object is not given, the evaluation function \(\ind_{C_a}\) does not hold at the referential level, the readout partial function is undefined and represented by \(\mathsf{NULL}\) to indicate non-addressability; once an address is given, the derived cylinder event family does not expand the existing visible \(\sigma\)-algebra, but merely reselects and reorganizes visible events within the old-layer visible \(\mathcal{G}^{(L)}\), so that \(\mathcal{G}^{(L+1)}\subseteq \mathcal{G}^{(L)}\) (Proposition \ref{prop:recursive-addressing-refinement}; Corollary \ref{cor:recursive-addressing-visible-sigma-nonexpansion}). The non-locality of fold order reduction can be quantitatively audited via the gauge anomaly \(G_m\), providing both worst-case and typical dual anchor points (Definition \ref{def:fold-gauge-anomaly}; Theorems \ref{thm:fold-gauge-anomaly-max}, \ref{thm:fold-gauge-anomaly-density}).
Moreover, the invertibility of the sliding block factor \(\Phi_m\) exhibits a unique two-point bifurcation at \(m=2\): except for two constant orbits, all are one-to-one, while constant orbits undergo a \(2\)-to-\(1\) collapse; this threshold can be interpreted as the minimal homological obstruction that a \(\mathbb{Z}/2\) hidden variable cannot be synchronously eliminated under \(m=2\) window but is regime-wise eliminated for \(m\ge 3\) (Theorem \ref{thm:Phi_m-conjugacy-threshold}; Remark \ref{rem:Phi_m-conjugacy-threshold-z2-homology}).

\paragraph{Stable arithmetic and Projection Ontology Mathematics}
Under value-preserving rewriting and canonical transversal mechanisms, the stable type arithmetic at resolution \(m\) is isomorphic to \(\ZZ/(F_{m+2}\ZZ)\) (Theorem \ref{thm:finite-resolution-mod}), thereby tracing the first-principle source of unification/splitting to the ring-theoretic properties and CRT structure of the modulus (Corollaries \ref{cor:field-phase-fib-prime}, \ref{cor:crt-factorization}); one can further define a Fibonacci-adic inverse limit ring along the binary-power subtower and obtain a compact unit group (Definition \ref{def:fib-adic-ring}; Proposition \ref{prop:fib-adic-basic}). On this basis, the paper establishes the projection word calculus of Projection Ontology Mathematics (POM), and gives the information ledger identity that rigorously objectifies the information loss from coarse-graining as computable residuals (Theorem \ref{thm:pom-kl-ledger}; Corollary \ref{cor:pom-ent-increase-equals-kl}). Simultaneously, on local readout tower slices, we prove termination and confluence of rewriting and give normal forms, yielding auditable closure and decidability interfaces (Theorem \ref{thm:pom-rewrite-termination-confluence}; Propositions \ref{prop:pom-projword-decidable}, \ref{prop:pom-rewrite-audit-loop}). The moment-kernel compilation and resonance discrimination for higher-order collision moments establish ``resonance'' as a verifiable mechanism (Theorems \ref{thm:pom-collision-kernel-ak}, \ref{thm:pom-moment-resonance}).

\paragraph{Topological--cubical geometry closure and factor chain mixing lower bounds for fibers}
Under the sole structural input of the local kernel \((011\leftrightarrow 100)\), this paper promotes ``fibers'' from purely combinatorial objects to a family of auditable topological/geometric/probabilistic invariants:
First, in Theorem \ref{thm:pom-fiber-independence-complex-classification}, the induced graph of candidate sites is promoted from an independence set family to an independence complex,
proving that the homotopy type of each fiber is either contractible or homotopy equivalent to a single sphere \(S^{\tau(x)-1}\), and giving a closed-form expression for \(\tau(x)\) and unique dimensional homological rigidity (Corollary \ref{cor:pom-fiber-independence-complex-homology}).
Second, treating the number of inverse substitutions within a fiber as a random variable \(R_x\), we obtain a strict component-independent decomposition and mean/variance density in \(\varphi\)-algebra (Propositions \ref{prop:pom-fiber-rewrite-independent-sum}, \ref{prop:pom-path-indset-thermo-constants}), and derive strong unimodality, Poisson--binomial factorization and local CLT from real-rootedness, yielding unified Bernstein/Chernoff-type concentration (Theorem \ref{thm:pom-fiber-rewrite-realroot-ulc}; Corollaries \ref{cor:pom-fiber-rewrite-poisson-binomial}, \ref{cor:pom-fiber-rewrite-local-clt}).
Third, under the ``single toggle'' adjacency structure of Definition \ref{def:pom-fiber-reconstruction-graph}, the fiber reconstruction graph \(\mathcal F_x\) strictly decomposes as a Cartesian product of Fibonacci cubes (Theorem \ref{thm:pom-fiber-reconstruction-cartesian-product}), thereby obtaining CAT(0) cubical geometry and completely closed-form cubical face counts (Theorem \ref{thm:pom-fibcube-fvector-closed}, Corollary \ref{cor:pom-fiber-fpoly-factorization}), with a constant-delay enumerable Gray--Hamilton structure for all preimages (Theorem \ref{thm:pom-fibcube-gray-hamilton}, Corollary \ref{cor:pom-fiber-constant-delay-traversal}).
Moreover, path component multiplication multiplicity is strictly monotone on the refinement poset, with closed-form fusion descent quantity, yielding an auditable strict Schur-convex ordering (Proposition \ref{prop:pom-path-component-multiplicity-refinement-monotone-extrema}).
Furthermore, under the zigzag ideal lattice maximal chain/linear extension caliber, the bi-invariant reachable domain equals the closed convex hull of atomic point sets in the scale limit, so any Pareto frontier exposed point is realized by mixing at most two atomic lengths (Theorem \ref{thm:pom-fiber-ab-convex-hull}; Corollary \ref{cor:pom-fiber-ab-two-phase-mixing}).
Finally, using the hypercube random walk on \(\Omega_m\) as baseline, this paper defines the fold factor chain \(\overline P^{\square}_m\), proves it is reversible, loop-free, and has spectral gap/log-Sobolev constant no worse than the hypercube itself (Proposition \ref{prop:pom-fold-factor-chain-reversible-noloop}, Theorem \ref{thm:pom-fold-factor-chain-gap-lsi}), thereby lower-bounding the mixing and concentration capability of the stable layer \(X_m\) by a uniform constant independent of fold details.
This conclusion cluster, together with the convex geometry skeleton of rotation polytopes and the \(\mathbb F_2/(p)\)-adic rigidity signatures of collision moment recursions, constitutes the closed interface of ``6D readout---fold kernel---arithmetic/geometric/probabilistic certificates.''

\paragraph{Bridging interfaces and verification}
On the spectral side, the dynamical \(\zeta\) and finite-part interfaces organize periodic counts, traces, and principal spectral radii into auditable fingerprints, providing unified input for subsequent completion and twisted channels (Section \ref{sec:zeta-finite-part}). On the statistical side, deviation indicators for fold histograms, cross-resolution consistency residuals, and the error decomposition formula for the Parry(PF) baseline explicitly separate finite-sample error from model discrepancy terms (Sections \ref{sec:statistical-stability}, \ref{sec:experiments}; Corollary \ref{cor:tv-decomposition-parry}), with script-based protocols providing reproducible certificate outputs (Section \ref{sec:experiments}).
For the metallic mean family driven by \(\alpha_A=[0;A,A,\dots]\), the \((\log N)/N\) leading coefficient of the deterministic TV certificate is controlled by \(\kappa(A)=A/\log\lambda_A\) and uniquely minimized by the golden endpoint (Corollary \ref{cor:metallic-mean-golden-unique-optimal-tv-constant}); meanwhile, under the support guardrail of minimal atomic lower bound, KL error obtains quadratic improvement with decay by discrepancy squared (Lemma \ref{lem:kl-from-tv-qmin}; Corollary \ref{cor:rotation-folded-kl-certificate}).
Further, encoding input uncertainty as \(W_1\) transport discrepancy, Lipschitz pushforward and readout error remain regime-wise covariant, and exhibit two-periodic limit constant and \(\sqrt5\)-splitting on the Fibonacci denominator subsequence of the golden branch (Theorem \ref{thm:kronecker-w1-lipschitz-readout}; Corollary \ref{cor:kronecker-w1-fibonacci-limits}; Proposition \ref{prop:w1-lipschitz-pushforward}); in the \(\zeta/\Xi\) analytic readout chain, the strongest scale Lipschitz constant for Stieltjes readout is \((t+1)^{-2}\), so the stable--ill-posed boundary can be quantified by a single factor (Proposition \ref{prop:xi-stieltjes-w1-lipschitz}).
Within the \(\zeta/\Xi\) completion audit, information leaving the unitary slice is endpoint-quadratic radially compressed, leading to strict visibility budget thresholds and single-radius band exclusion certificates, while the minimal \((2\times2)\) self-dual template gives an explicit off-axis zero tower as a falsifiable prototype (Theorems \ref{thm:xi-jensen-single-radius-band-exclusion}, \ref{thm:xi-2x2-selfdual-offcritical-template}).
In the weighted pressure interface of the synchronization kernel, the Abel--Mertens finite-part constant for primitive counts, discriminant characterization of the analytic radius and unit-root twist threshold, as well as auditable truncation remainder bounds for Edgeworth/moderate deviation sprays, can all be sealed by the analytic continuation of the Perron branch and spectral gap uniformity (Proposition \ref{prop:sync-kernel-abel-mertens-analytic-family}, Corollaries \ref{cor:sync-kernel-analytic-radius-discriminant-formula}--\ref{cor:sync-kernel-unit-root-twist-threshold}, Theorem \ref{thm:sync-kernel-even-edgeworth-c4}, Propositions \ref{prop:sync-kernel-moderate-deviation-cauchy-certified}--\ref{prop:sync-kernel-curvature-bilipschitz}).
Furthermore, the zero-spectral nilpotent index of the true input 40-state kernel promotes online delay and reset word length to linear-algebraic invariants, giving strict upper bounds on transient memory depth (Proposition \ref{prop:real-input-40-nilpotent-index}, Corollary \ref{cor:real-input-40-nilpotent-memory-depth}); the recursion--spectral radius closure of the fold collision moment spectrum compresses all R\'{e}nyi fingerprint rates into a family of Perron root limit structures (Proposition \ref{prop:pom-collision-renyi-perron-closure}).
Moreover, the maximum fiber exponent and R\'{e}nyi endpoint are strictly identical at the exponential level, and the large deviation/CLT and capacity saturation discrimination for the fiber index are also given auditable closure by the collision spectral radius family; the R\'{e}nyi tangent formula, convex cone monotonicity, independent fold product law, and ``typical = worst-case'' saturation phase characterization further unify the typical exponent, worst-case exponent, and multi-source stacking into a single convex duality chain (Propositions \ref{prop:pom-maxfiber-exponent-equals-renyi-endpoint}, \ref{prop:pom-fiber-index-ldp-from-collision-spectrum}, Corollary \ref{cor:pom-fiber-index-clt-variance-certificate}, Propositions \ref{prop:pom-typical-fiber-exponent-renyi-tangent}--\ref{prop:pom-saturation-phase-characterization}, Proposition \ref{prop:pom-capacity-saturation-discriminant}).
Additionally, the gap at the top spectral layer of maximum fiber satisfies \(12/13\)-step delay self-similarity, so the sub-spectral ratio exhibits different exponential differential scales on odd and even cases (Remark \ref{rem:pom-max-fiber-gap-delay-12-13}).
In the operator algebra caliber, the exponential sub-multiplicativity of crossed horizons and the conditional mutual information lower bound compress ``residual correlation between two sources'' into a logarithmic exponential gap, giving strict realization and uniqueness templates for exponential extremal states; under the coarse-graining regime of the depth spectrum, scale growth forces exponential contraction of spectral spacing and exponential blowup of inversion condition numbers (Proposition \ref{prop:op-algebra-index-submultiplicativity-commuting-square}, Corollary \ref{cor:op-algebra-cmi-lower-bound-by-index-gap}, Propositions \ref{prop:op-algebra-index-extremal-entropy-loss-maxfiber}, \ref{prop:xi-coarsegraining-illposedness-exponential-law}).
Under the same operator algebra template, the commuting polar decomposition for ``horizon zero-knowledge---unitary slice,'' joint spectral comb parameterization, and Li/log-Sobolev certificates unify critical line concentration, zero simplicity, and scale spacing constraints into auditable operator conditions, giving impenetrable tradeoff bounds for near-zero-knowledge---soundness---fiber scale (Theorem \ref{thm:op-algebra-hzom-commuting-polar-forces-critical-line}, Corollaries \ref{cor:op-algebra-hzom-critical-zeros-joint-spectrum-comb}, \ref{cor:op-algebra-hzom-simple-zeros-joint-spectrum}, \ref{cor:op-algebra-hzom-zero-spacing-scale-lower-bound}, Propositions \ref{prop:op-algebra-li-criterion-operatorized}, \ref{prop:op-algebra-common-unitary-dilation-criterion}, \ref{prop:op-algebra-logsobolev-zero-band}, Corollary \ref{cor:op-algebra-nearzk-soundness-fiber-tradeoff}).
In the Hardy/Smirnov factorization caliber, critical line pure-phaseness pushes the completion determinant into the inner function class and eliminates the outer factor (amplitude leakage), so the leakage integral strictly upper-bounds the weighted total offset of off-line zeros; the Poisson superposition law for phase derivatives, Clark measure, and Carleson/BMO templates further promote ``challenge phase seed---worst-case soundness---recoverable rate'' to measurable criteria for zero geometry (Theorem \ref{thm:xi-hardy-inner-characterization}, Propositions \ref{prop:xi-leakage-zero-offset-sum}, \ref{prop:xi-phase-derivative-poisson-superposition}, Corollary \ref{cor:xi-clark-measure-horizon-semantics}, Theorems \ref{thm:xi-carleson-bmo-recovery-template}--\ref{thm:xi-rh-sufficient-template-inner-clark-conservative}).
In the de Branges/canonical system caliber, the inner function can be uniquely lifted to an inverse spectral model of a trace-normalized Hamiltonian, unifying ``extreme zero-knowledge,'' spectral shift--relative entropy identity, SU(1,1)--Riemann--Hilbert positive-definite jump, adelic patching, and Dirichlet family zero-free strip into a single auditable spectral realization chain; within this chain, off-line zeros and non-normality obstructions are mutually equivalently constrained by information complexity lower bounds (Theorem \ref{thm:xi-debranges-canonical-extreme-zk}, Propositions \ref{prop:xi-krein-spectral-shift-equals-kl}, \ref{prop:xi-debranges-dpp-sine-kernel-limit}, Theorem \ref{thm:xi-su11-rhp-equivalence}, Propositions \ref{prop:xi-maslov-spectralflow-entropy-quantization}, \ref{prop:xi-adelic-zk-gluing-critical-line}, \ref{prop:xi-dirichlet-zero-free-from-unified-zk}, Corollary \ref{cor:xi-ae-simple-zeros-under-ac-seeds}, Propositions \ref{prop:xi-nonnormality-obstruction-offcritical}, \ref{prop:xi-bm-density-forces-linear-transcript-length}).

\paragraph{Discrete unification consolidation at the window-\(6\) anchor: golden time---6D microstate---Zeckendorf--GUT three-branch}
In addition to the globally organized conclusions grouped by mechanism, this paper also provides a set of terminal proposition groups that can stand independently and are expressed entirely in terms of finite objects: using window-\(6\) as the minimal resolution anchor, they unify the protocolized timescale of ``time = single-generator scan iteration count,'' the \(\Omega_6\to X_6\) fold compression \(64\to 21\), the uplift tail three-branch \(\{21,34,55\}\), and the GUT top-layer alignment and family number locking induced by the Zeckendorf--Fibonacci boundary tower, into a single auditable discrete branching structure (see \S\ref{subsubsec:window6-terminal-propositions}; the main conclusion is Theorem \ref{thm:terminal-6d-microstate-golden-time-gut-branch}).

\paragraph{Four concluding propositions: from address semantics to certified budgets and rank stability}
To facilitate the direct reuse of conclusions scattered throughout the main text in protocol design and audit workflows, several core assertions can be compressed into four complementary ``certificate interfaces'' (all strictly depending on existing numbered conclusions rather than explanatory statements):
\begin{enumerate}
  \item \textbf{Objectification of address semantics: \(\mathsf{NULL}\) as local section obstruction.}
  After organizing protocol-acceptable and single-valued verifiable readout/certificates as a (pre)sheaf \(\mathscr F\) on the prefix site, the \(\mathsf{NULL}\) semantics of ``address precedes value'' can be strictly equated with the existence obstruction \(\mathscr F(a)=\varnothing\), compatible with the visible \(\sigma\)-algebra non-expansion caliber of recursive addressing (\S\ref{subsec:prefix-site-null}, Proposition \ref{prop:null-as-local-section-obstruction}; see also Proposition \ref{prop:recursive-addressing-refinement} and Corollary \ref{cor:recursive-addressing-visible-sigma-nonexpansion}).
  Consequently, semantic/protocol/collision null values are unified under the same site language as three mutually non-confusable local section absence mechanisms (Corollary \ref{cor:null-trichotomy-local-section}).

  \item \textbf{Second-order certificate for fold ``clarity'': collision inflation \(\Rightarrow\) entropy gap upper bound.}
  Under the uniform microstate baseline, the entropy gap \(\mathrm{gap}\) of the fold pushforward distribution \(w_m(x)=d_m(x)/2^m\) relative to the type-uniform distribution can be certified by a logarithmic upper bound of the second-order collision fingerprint
  \(C_{\mathrm{col}}(m)=|X_m|\sum_x w_m(x)^2\)
  (Theorem \ref{thm:pom-spectral-information-sandwich} and Definition \ref{def:pom-collision-inflation}), and can be directly audited at finite resolution from Table \ref{tab:fold_multiplicity_histogram}.
  This conclusion compresses ``thumbnail clarity'' from an explanatory statement into a computable certificate depending only on second-order statistics.

  \item \textbf{Optimal dual envelope for the injectivization budget: \(\inf_{q\ge 2}\) normalization of the quantile quota law.}
  The quota threshold for the prime register can be given by the fiber index tail quantile \(B_m(\varepsilon)=\exp(m\gamma_m(\varepsilon))\), which is sharp under the interval readout/reconstruction caliber (\S\ref{subsec:pom-quantile-budget-law}, Corollary \ref{cor:pom-prime-register-quantile-budget}).
  Furthermore, the entire family of Chernoff/quota inequalities derived from integer moment order \(q\) can be compressed into a single optimal lower envelope over \(q\ge 2\), yielding a parameter-free register exponent budget law; an executable certificate can be realized using the ``strongest computable tail bound'' from a finite \(q\)-window (Corollary \ref{cor:pom-prime-register-q-quota-family} and the dual design form in \S\ref{subsec:pom-quantile-budget-law}).

  \item \textbf{Necessary and sufficient criterion for pattern axis completeness: Hankel rank uniformly bounded \(\Longleftrightarrow\) ACC-Rank.}
  Under unified minimality and protocol closure assumptions, once the residual axis budget has excluded collision null, the pattern axis completeness condition ``no \(\mathsf{NULL}\) is triggered by the need for additional visible degrees of freedom'' is strictly equivalent to uniform boundedness of the Hankel rank (Theorem \ref{thm:xi-completeness-iff-hankel-bounded}), thereby converting the key gap in completeness to rank stability scans amenable to regression testing (Remark \ref{rem:xi-acc-rank}).
  Combined with the quantile quota certificate, the completeness audit task can be compressed into a binary backend verification of ``rank stability certificate \(\times\) quota certificate'' (Proposition \ref{prop:xi-completeness-auditable-decomposition}).
\end{enumerate}

\paragraph{From projective pressure operators to low-order closed forms of no-backtracking prime shadows}
To directly interface ``integer point certificates---discrete anchors---low-order determinant closed forms'' with continuous pressure, minimal Markov completion, and prime shadow audits, the following assertions provide a set of complementary verifiable propositions (proofs and auditable certificates distributed in POM, window-\(6\), and true input kernel appendices):
\begin{enumerate}
  \item \textbf{Real parameter lift of projective pressure operator and strict degeneration at integer points.}
  For a deterministic online kernel/transducer \(\mathcal{T}\), define the projective transfer operator from output bucketing matrix \(B_b\), simplex \(\Delta\), and projective dynamics \((\Phi_b,g_b)\) by
  \[
  (\mathcal{L}_q f)(w)=\sum_{b\in A_{\mathrm{out}}} g_b(w)^{\,q}\,f(\Phi_b(w))\qquad (q\ge 1)
  \]
  (Definition \ref{def:pom-projective-transfer-operator}).
  For integer \(q\ge 2\), its restriction to the homogeneous polynomial subspace strictly degenerates to the symmetric power quotient moment-kernel, so the spectral radius at integer points satisfies
  \(
  \rho(\mathcal{L}_q|_{\mathcal{H}_q})=r_q
  \)
  (Theorem \ref{thm:pom-projective-operator-degenerates-to-moment-kernel}).
  Thus discrete pressure \(\log r_q\) can be upgraded to an executable continuous pressure curve candidate: let \(q=t+1\ge 1\), take the Perron root \(\lambda(t+1)\) of \(\mathcal{L}_{t+1}\) and set \(\Lambda(t)=\log\lambda(t+1)-\log|A_{\mathrm{out}}|\), strictly interpolating \(\Lambda(q-1)=\log r_q-\log|A_{\mathrm{out}}|\) at integer points (Remark \ref{rem:pom-projective-pressure-route}).

  \item \textbf{Affine geometric classification of window-\(6\) fibers and ``linear kernel'' non-realizability.}
  View \(\Omega_6=\{0,1\}^6\) as \(\mathbb{F}_2^6\) and denote the dyadic fold \(F_6=\mathrm{Fold}^{\mathrm{bin}}_6\).
  Its \(21\) fibers \(F_w=F_6^{-1}(w)\subset\mathbb{F}_2^6\) admit a complete classification: exactly \(8\) are affine \(1\)-flats (size \(2\)), exactly \(3\) are affine \(2\)-flats (size \(4\), with stable types exactly \(\{\texttt{000000},\texttt{000010},\texttt{101000}\}\)), and the remaining \(10\) are all non-affine subspaces (Theorem \ref{thm:terminal-foldbin6-fiber-affine-geometry}; Table \ref{tab:foldbin6_fiber_affine_geometry}).
  This yields the linear kernel obstruction: no \(\mathbb{F}_2\)-linear map \(L:\mathbb{F}_2^6\to\mathbb{F}_2^k\) makes the fiber partition identical to a coset partition (Corollary \ref{cor:terminal-foldbin6-no-linear-kernel}).

  \item \textbf{Complete rigidity of type adjacency graph \texorpdfstring{$\Gamma_6$}{Gamma6} and continuous symmetry prohibition.}
  Let \(\Gamma_6\) be the type adjacency graph induced by cubical edge pushforward (Proposition \ref{prop:terminal-gamma6-rigidity}).
  Then its automorphism group is trivial \(\mathrm{Aut}(\Gamma_6)=\{1\}\), and the graph is connected with diameter \(3\) (Corollary \ref{cor:terminal-gamma6-diameter-simple-spectrum}).
  Therefore, any compact group acting on \(X_6\) while preserving adjacency must act trivially (Corollary \ref{cor:terminal-gamma6-no-continuous-nonabelian-symmetry}): type relabeling freedom is endogenously eliminated by the adjacency structure.

  \item \textbf{Smith discriminant of edge--flux skeleton and Levi reducibility forcing.}
  For the \(4\times4\) interaction matrix \(E\in\mathbb{Z}^{4\times4}\) of the four edge--flux skeleton blocks, exhaustive audit yields the completely verifiable Smith normal form
  \(
  \mathrm{SNF}(E)=\mathrm{diag}(1,1,3,3450)
  \), so \(\mathrm{coker}(E)\cong \mathbb{Z}/3\mathbb{Z}\oplus \mathbb{Z}/3450\mathbb{Z}\) and \(\det(E)=10350\) (Corollary \ref{cor:window6-edge-flux-skeleton-smith-nonsimple}).
  This integer discriminant excludes identification with any finite-type irreducible Cartan data of rank \(4\), forcing minimal Levi-type reducibility under the ``discrete trace of continuous Lie symmetry'' interpretation.

  \item \textbf{Minimal thermalization closure of pushforward kernel \texorpdfstring{$P_6$}{P6}: reversibility, equilibrium measure, and explicit spectral gap constant.}
  Under the ``instant thermalization within fibers'' minimal memoryless closure mechanism, the stable type process closes to a reversible Markov chain on \(X_6\) with transition kernel \(P_6\) and stationary distribution \(\pi_6(w)\propto d^{\mathrm{bin}}_6(w)\) (Theorem \ref{thm:terminal-foldbin6-pushforward-markov}).
  Moreover, spectral gap audit yields explicit constants: \(\lambda_2(P_6)\approx 0.4841208\), \(\lambda_\star(P_6)\approx 0.6030940\) (Corollary \ref{cor:terminal-foldbin6-minimal-markov-closure-gap} and equation \eqref{eq:window6_pushforward_markov_spectral_gap}), so for any initial distribution \(\mu\),
  \(
  \|\mu P_6^t-\pi_6\|_{\mathrm{TV}}\le C(\mu)\lambda_\star(P_6)^t
  \)
  with exponential mixing upper bound.

  \item \textbf{Local bi-geometry of pressure--finite-part: isobaric drift capacity and curvature mismatch spectrum.}
  Under the three-dimensional observation of the true input 40-state kernel, taking pressure \(P(\theta)\) and Abel/Mertens finite-part constant \(\log\mathfrak{M}(\theta)\) as two independent thermodynamic quantities, define the isobaric drift capacity vector
  \[
  v_\perp=\nabla\log\mathfrak{M}(0)-\mu\,\nabla P(0),\qquad
  \mu=\frac{\langle\nabla P(0),\nabla\log\mathfrak{M}(0)\rangle}{\|\nabla P(0)\|^2}
  \]
  (Definition \ref{def:real-input-40-isobaric-drift-vector}).
  Numerically \(\mu\approx 0.7113818\), \(v_\perp\neq 0\) (Proposition \ref{prop:real-input-40-isobaric-drift-capacity}), so \(\log\mathfrak{M}\) cannot be merely a univariate composite of \(P\) (Corollary \ref{cor:real-input-40-logM-not-just-P}).
  Furthermore, the curvature mismatch spectrum is organized by the generalized eigenvalue problem \(Hv=\mu\,gv\) (Proposition \ref{prop:real-input-40-logM-curvature-mismatch-spectrum}), yielding a quadratic conversion law between finite-part drift and the near-\(1\) principal pole gap (Proposition \ref{prop:real-input-40-space-time-conversion-law}).
  Additionally, by defining \(s\)-plane coordinate \(s=\log\rho/\log\lambda\) and spectral ratio mixing time \(\tau_{\mathrm{mix}}=1/(-\log(\rho/\lambda))\), the gap \(1-\Re(s)\) and mixing time satisfy the strict reciprocal identity \((1-\Re(s))\,\tau_{\mathrm{mix}}=1/\log\lambda\) (Corollary \ref{cor:near1-pole-taumix-m2}).

  \item \textbf{Low-order determinant closed form of bivariate weighted \texorpdfstring{$\zeta(z;u,v)$}{zeta(z;u,v)} and finite-order recursion of primitive spectrum.}
  On the true input kernel, introduce two-dimensional joint potential for collision and output, defining \(\Delta(z;u,v)=\det(I-zM_{u,v})\) (see \S\ref{app:real-input-40-zeta-uv}).
  The determinant denominator has explicit \((2+8)\)-degree closed factorization \(\Delta(z;u,v)=(vz^2-1)P_8(z;u,v)\) (Theorem \ref{thm:real-input-40-det-uv-2plus8}), so for each fixed \((u,v)\), the trace sequence \(a_n(u,v)=\Tr(M_{u,v}^n)\) satisfies a constant-coefficient linear recurrence of order at most \(10\), and primitive orbit spectrum can be recovered in near-linear time via M\"obius inversion (Proposition \ref{prop:real-input-40-zeta-uv-recurrence}).

  \item \textbf{Low-order determinant closed form of Bartholdi/Ihara-type no-backtracking (geodesic) prime shadow.}
  On the essential core edge space of the true input 40-state kernel, after constructing the no-backtracking edge shift \(H_{\mathrm{nb}}\), although \(H_{\mathrm{nb}}\) is a \(48\times48\) matrix, its determinant denominator compresses to a degree-\(15\) integer-coefficient polynomial (Theorem \ref{thm:real-input-40-geodesic-det}):
  \[
  \det(I-zH_{\mathrm{nb}})
  =1-4z^3-9z^4-12z^5-16z^6-11z^7-11z^8-4z^9-z^{10}
  +8z^{11}+12z^{12}+10z^{13}+8z^{14}+2z^{15}.
  \]
  Root modulus sorting yields the maximum non-Perron modulus \(\Lambda_{\mathrm{nb}}\) and gives strict Ramanujan margin \(\Lambda_{\mathrm{nb}}/\sqrt{\rho_{\mathrm{nb}}}<1\), obtaining sub-exponential error with exponential base strictly less than \(\rho_{\mathrm{nb}}^{1/2}\) at prime lengths (Corollary \ref{cor:real-input-40-geodesic-ramanujan-margin}).
\end{enumerate}

\paragraph{Spectral--arithmetic sealing: from resonance closure to endpoint cluster rigidity}
To directly interface key ``non-eliminable structures'' with energy, spectral, and arithmetic audit interfaces, beyond the four certificate interfaces above, the following assertions provide a set of independently citable sealing propositions (proofs and certificates distributed in appendices and POM/sync-kernel chapters):
\begin{enumerate}
  \item \textbf{Resonance closure giving non-eliminable lower bound on collision energy.}
  Using notation of fold collision probability \(\mathsf{Col}_m\) and resonance ladder constant \(C_\varphi(u)>0\) (Definitions \ref{def:fold-normalized-amplitude} and \ref{def:fold-critical-resonance-ladder}), for any fixed \(U\in\NN\), there is a strict lower bound (Corollary \ref{cor:fold-collision-gap-resonance-ladder-U})
  \[
  \liminf_{m\to\infty}F_{m+2}\,\mathsf{Col}_m\ \ge\ 1+2\sum_{u=1}^{U}C_\varphi(u)^2.
  \]
  Thus ``collision approaches uniformity/spectral contraction'' is structurally pinned at the energy level by an enumerable resonance closure structure; any route attempting to derive \(\ell^2\) equalization solely from \(\|\widehat d_m\|_\infty\) control, under the original Fold caliber, will encounter blockage by this lower bound mechanism.

  \item \textbf{Fold group algebra strict factorization induces deterministic Riesz product; collision spectrum equals its energy average.}
  The group ring generating function satisfies strict product factorization \(D_m(x)\equiv\prod_{j=1}^m(1+x^{F_{j+1}})\ (\mathrm{mod}\ x^{F_{m+2}}-1)\) (Proposition \ref{prop:fold-multiplicity-group-algebra}), equivalent to convolution factorization of \(d_m\) in the cyclic group algebra; its Fourier transform completely factorizes (Theorem \ref{thm:fold-spectrum-factorization}).
  Hence
  \(
  F_{m+2}\mathsf{Col}_m=\sum_k a_m(k)^2
  \)
  can be viewed as energy average of a finite Riesz product driven by Fibonacci frequencies (Definition \ref{def:fold-normalized-amplitude}), strictly translating the collision limit problem into an ``amplitude distribution'' problem rather than a ``zero geometry'' problem.

  \item \textbf{Auditable real parameter threshold from real degeneracy points: local expansion window of output potential.}
  In the output potential one-parameter weighting of the true input 40-state kernel, positive real degeneracy points partition \(u>0\) into phase diagram intervals with invariant spectral support ordering (Remark \ref{rem:real-input-40-output-potential-degeneracy-phase}; Table \ref{tab:real_input_40_output_potential_spectral_collision_roots}).
  The nearest degeneracy point to \(u=1\) gives a real parameter threshold, reading out a unified conservative radius in the \(\theta=\log u\) coordinate
  \[
  |\theta|<\theta_0,\qquad \theta_0=0.0533477827\ldots,
  \]
  usable as an auditable effective domain seal in the real parameter direction for local fitting/expansion around \(\theta=0\).

  \item \textbf{Pressure parity law under self-dual completion: structural origin of odd-order cumulant annihilation.}
  Under self-dual completion, pressure can be written as \(P(\theta)=\theta/2+\log y(2\cosh(\theta/2))\) (Proposition \ref{prop:weighted-completion-Q}), so \(\Lambda(\theta):=P(\theta)-\theta/2\) is an even function (Theorem \ref{thm:sync-kernel-gallavotti-cohen}).
  Equivalently \(P(\theta)-P(-\theta)=\theta\), so except for the linear term, all odd-order derivatives of \(P\) at \(\theta=0\) are strictly zero; therefore any central fluctuation expansion derived from \(P\) must have its first non-Gaussian correction fall in the even Hermite sector, while all odd-order skewness terms are structurally excluded.

  \item \textbf{Newman threshold for minimal three-state ``unification\texorpdfstring{$\to$}{->}breaking'' effective kernel: closed-form phase transition prototype.}
  For the three-state effective kernel family \(A_{\mathrm{eff}}(\alpha)=U+\alpha R\) (Proposition \ref{prop:pom-a2-gut-spectral-splitting}), the kernel-version Newman offset \(\delta(\alpha)=\log(\Lambda(\alpha)^2/\rho(\alpha))\) has a unique critical point \(\alpha_\star>1\) with ``negative real slow mode edge-touching,'' and this threshold has complete algebraic certificate (Proposition \ref{prop:pom-a2-gut-newman-threshold}):
  \[
  \alpha_\star=\frac{x_\star(x_\star^3-x_\star^2+1)}{x_\star^2-x_\star+2}=2.3609261183\ldots,
  \]
  where \(x_\star>1\) is the unique positive real root of \(x^6-x^5-4x^4+8x^3-6x^2-4x+2=0\).

  \item \textbf{Hankel witness of \texorpdfstring{$p$}{p}-adic rigidity: activation threshold for primes in minimal realization dimension certificates.}
  Nonzero Hankel principal minors not only give rank lower bounds but also carry irreducible \(p\)-adic signals: in the first two minimal cases,
  \(
  \det(H_{k=4})=2^{17}\cdot 3^2
  \)
  while
  \(
  \det(H_{k=5})=2^{13}\cdot 3^5\cdot 5^7
  \), so \(p=5\) first manifestly enters at \(k=5\) with exponent \(7\) (Remark \ref{rem:pom-hankel-stiffness-p5}).

  \item \textbf{Automatic sequence structure of mod \(2\) shadow: difference annihilation and period upper bound.}
  On the resonance window \(q=9,\dots,17\), the characteristic polynomial mod \(2\) shadow collapses to
  \(
  P_q(\lambda)\equiv \lambda^{a_q}(\lambda+1)^{b_q}\ (\mathrm{mod}\ 2)
  \)
  inducing annihilator \(E^{a_q}(E+1)^{b_q}S_q\equiv 0\ (\mathrm{mod}\ 2)\).
  Hence \(S_q(m)\bmod 2\) is a \(2\)-automatic sequence with period \(T_q\mid 2^{\lceil\log_2 b_q\rceil}\), with concrete upper bounds given in Table \ref{tab:fold_collision_charpoly_mod2_shadow_q9_17} (Remark \ref{rem:pom-charpoly-mod2-shadow}).

  \item \textbf{Endpoint cluster rigidity from elimination resultant: \texorpdfstring{$u$}{u}-adic degeneration factorization pins endpoints as discrete rational points.}
  For the raw elimination resultant \(R_{\mathrm{raw}}(\alpha,u)\) of the sync-kernel rate curve, its \(u\)-adic valuation satisfies \(v_u(R_{\mathrm{raw}})=7\) with rigid factorization \(7=3+4\) (Proposition \ref{prop:rate-resultant-uadic-split-7}; Table \ref{tab:sync_kernel_rate_curve_resultant_degree}).
  Leading term structure forces
  \[
  R_{\mathrm{raw}}(\alpha,u)=\pm\,u^7\,\alpha(\alpha-1)(4\alpha-1)^4+\text{higher order terms},
  \]
  promoting endpoint clusters at \(\alpha=0,1\) and \(\alpha=\tfrac14\) (quadruple) to discrete rational points inevitably produced by degeneration geometry; under duality closure \((u,\alpha)\mapsto(1/u,1-\alpha)\), symmetry forces the corresponding endpoint cluster at \(\alpha=\tfrac34\).
\end{enumerate}

\paragraph{Resonance window and constant layer: from CM phase locking to Abel-normalized integrable kernels}
\begin{enumerate}
  \item \textbf{CM phase locking of resonance complex spectrum: spontaneous emergence of imaginary quadratic field \texorpdfstring{$\QQ(\sqrt{-15})$}{Q(sqrt(-15))}.}
  In the resonance window \(q=9,\dots,17\), let \(\mu_q\) be the maximum-modulus non-real root (taking \(\Im(\mu_q)\le 0\)), and set unit phase
  \(\zeta_q:=\mu_q/|\mu_q|\in\mathbb{S}^1\).
  Let
  \(
  z_\ast:=(-1-\mathrm{i}\sqrt{15})/4
  \)
  be the unit circle root of \(2z^2+z+2=0\); then Table \ref{tab:fold_collision_shadow_spectral_packet_algebraic_phase_residual_q9_17} provides auditable phase-locking certificate: \(|\zeta_q-z_\ast|\) remains at \(10^{-2}\) scale across the window and reaches \(10^{-4}\) scale at \(q=12\), while algebraic residual \(\rho_q=|2\zeta_q^2+\zeta_q+2|\) remains small (Remark \ref{rem:pom-resonance-trimodal-phase-lock}).
  Moreover, by equation \eqref{eq:pom-phase-lock-residual-trace-equivalence}, \(\rho_q\) is equivalent to the magnitude of real trace deviation \(|(\zeta_q+\zeta_q^{-1})+\tfrac12|\), so locking strength can be compressed to a single real scalar.
  Thus the unit circle phase of resonance complex modes can be compressed to a single algebraic orbit in imaginary quadratic field \(\QQ(\sqrt{-15})\), forming a completely intrinsic and auditable CM-type spectral fingerprint.

  \item \textbf{``Universal second-order oscillator'' at fixed frequency of complex modes: normalized annihilator from \texorpdfstring{$2z^2+z+2$}{2z^2+z+2}.}
  Set normalized shift \(T:=r_{q-2}^{-1}E\) (\(E\) is the forward shift in \(m\)-direction), then \(\mathcal L_{\mathrm{CM}}(T):=2T^2+T+2\) strictly annihilates exponential modes for \(\nu=z_\ast,\overline{z_\ast}\); within the resonance window this operator serves as a ``phase-aware'' three-step damping interface, complementary to the two-period Aitken eliminator (Remark \ref{rem:pom-cm-annihilator}).
  Equivalently, for normalized real oscillator \(u(m)=\zeta^{m}+\zeta^{-m}\) (\(\zeta\in\mathbb{S}^1\)),
  \(2u(m+2)+u(m+1)+2u(m)=\bigl(2(\zeta+\zeta^{-1})+1\bigr)u(m+1)\),
  so its second-order synthesis magnitude is pointwise proportional to \(\rho(\zeta)=|2\zeta^2+\zeta+2|\), yielding a time-domain audit interface without complex root finding (Remark \ref{rem:pom-cm-annihilator}).

  \item \textbf{Two-step rescaling Poisson peak \texorpdfstring{$\Rightarrow$}{=>} Lorentz limit: Breit--Wigner profile discrete pre-limit and ``height--width reciprocity''.}
  Let \(\eta_q:=|\mu_q|/r_{q-2}\uparrow 1\) and \(a_q:=\mu_q/r_{q-2}\in\mathbb{D}\).
  Then the boundary phase derivative of the corresponding Blaschke factor \(B_q(z)=(z-a_q)/(1-\overline{a_q}z)\) is the Poisson kernel \(P_{\eta_q}(t-\theta_q)\), converging to Lorentz kernel \(2/(1+u^2)\) in the scaling window \(t=\theta_q+\varepsilon_q u\) (\(\varepsilon_q=1-\eta_q\));
  meanwhile Poisson kernel area is identically \(2\pi\), peak height scale is \(\varepsilon_q^{-1}\) (Remark \ref{rem:pom-resonance-poisson-lorentz-prelimit}).
  Moreover, from audit readings in Table \ref{tab:fold_collision_shadow_spectral_packet_q9_17}, defining stepwise drift
  \(
  \Delta\theta_q:=|\arg(\mu_{q+1})-\arg(\mu_q)|
  \)
  (\(q=9,\dots,16\)), there is uniform domination
  \(
  \max_{9\le q\le 16}\Delta\theta_q/\varepsilon_q
  =0.0029/0.0204<0.143
  \), so \(\Delta\theta_q\le 0.143\,\varepsilon_q\) (Remark \ref{rem:pom-resonance-poisson-lorentz-prelimit}).

  \item \textbf{Quadruple confluence point at \texorpdfstring{$q=12$}{q=12}: synchronization of four audit invariants.}
  Within the resonance window, pivot point \(q=12\) simultaneously satisfies: (i) \(|\zeta_q-z_\ast|\) takes window minimum (Table \ref{tab:fold_collision_shadow_spectral_packet_algebraic_phase_residual_q9_17}); (ii) negative real outer root channel switch \(|\lambda_q^-|/r_{q-2}\) crosses threshold \(1\) at this point (Table \ref{tab:fold_collision_shadow_spectral_packet_q9_17}); (iii) resonance gap \(\Delta(q)\) uniquely takes \(0\) in verified sequence (Remark \ref{rem:pom-delta-cq-threshold}); (iv) in mod \(11\) embedding slice, embedding flag \(\chi_{A_2}\mid P_{12}\ (\mathrm{mod}\ 11)\) appears (Proposition \ref{prop:pom-charpoly-modp-a2-embedding}; Table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}, with embedding column \(0\) for same table \(q=13,15\)).
  This quadruple synchronization identifies \(q=12\) as a structural inflection point: its synchrony stems from four mutually independent audit pipelines of two-step rescaling, algebraic phase residual, minimal recurrence order, and finite field embedding (Remark \ref{rem:pom-resonance-window-final-structural-conclusions}).

  \item \textbf{Drift law in Dirichlet real-part coordinate: deterministic approach of resonance complex pole strip toward \texorpdfstring{$\Re(s)=1$}{Re(s)=1}.}
  For any root \(\xi\), define Dirichlet real-part coordinate \(\sigma_q(\xi):=\log|\xi|/\log r_q\).
  Within the resonance window, \(\sigma_q(\mu_q)>1/2\) and monotonically increasing (Table \ref{tab:fold_collision_shadow_spectral_packet_q9_17}), forming a non-trivial pole strip approaching \(\Re(s)=1\); its drift decomposes as sum of two-step renormalization ratio and amplitude correction (Remark \ref{rem:pom-shadow-spectral-packet-q9-17})
  \[
  \sigma_q(\mu_q)=\frac{\log r_{q-2}+\log(|\mu_q|/r_{q-2})}{\log r_q}.
  \]
  Hence under endpoint assumption \(|\mu_q|/r_{q-2}\to 1\) and \(R_q:=r_q/r_{q-2}\to\varphi\), complex mode gap exponent \(g_q^{(c)}:=1-\sigma_q(\mu_q)\) satisfies conditional asymptotic \(g_q^{(c)}\sim 2/q\), yielding a falsifiable drift rate constant uniquely determined by endpoint \(\varphi\).

  \item \textbf{Mod \texorpdfstring{$p$}{p} splitting selection law for discriminant \texorpdfstring{$-15$}{-15}: cross-prime falsifiable fingerprint of phase-locking quadratic.}
  Splitting property of quadratic \(2z^2+z+2\) over \(\mathbb{F}_p\) is controlled by \(\bigl(\frac{-15}{p}\bigr)\), with double root degeneration at ramified primes \(p\in\{3,5\}\);
  thus if the phase-locking two-block is truly governed by this quadratic, then cross-prime mod \(p\) factorization fingerprints should exhibit systematic splitting/inertia dichotomy (Proposition \ref{prop:pom-phase-lock-discriminant-minus15-splitting} and Remark \ref{rem:pom-phase-lock-discriminant-minus15-prediction}).

  \item \textbf{Representation-theoretic meaning of finite field embedding flag: lower-order kernel appearing as local sub-dynamics of higher-order kernel.}
  If \(\chi_{A_2}(\lambda)\mid P_q(\lambda)\) in \(\mathbb{F}_p[\lambda]\), then in the mod \(p\) recurrence state space there appears an embedded sub-dynamics isomorphic to \(A_2\) (a three-dimensional invariant sub-representation; Corollary \ref{cor:pom-charpoly-modp-a2-subdynamics}).
  Verified slice gives \(A_2\)-shadow embedding flag at \((q,p)=(14,7)\) (Proposition \ref{prop:pom-charpoly-modp-a2-embedding}), providing a finite field representation-theoretic interface for local--global comparison.

  \item \textbf{Hard fingerprint of discriminant prime support: \(\{2,7,11\}\) forced divisibility spectrum.}
  For \(P_q(\lambda)=\lambda^{a_0}P_q^\ast(\lambda)\) (\(P_q^\ast(0)\ne 0\)), define \(\mathrm{Disc}^\ast(P_q):=\mathrm{Disc}(P_q^\ast)\).
  Within verified slices of resonance window, from repeat non-square-free flags, forced divisibility \(p\mid \mathrm{Disc}^\ast(P_q)\) can be directly inferred; mod \(2\) shadow gives full-window \(2\mid \mathrm{Disc}^\ast(P_q)\), while mod \(7\), mod \(11\) slices force appearance at various \(q\) (Remark \ref{rem:pom-discriminant-prime-support}).

  \item \textbf{Period collapse under composite modulus: \texorpdfstring{$q=13$}{q=13} eventual period divides \texorpdfstring{$168$}{168} mod \texorpdfstring{$182$}{182}.}
  From period upper bounds mod \(2/7/13\) and Chinese remainder structure, after aligning common transient, \(\mathrm{Per}_{182}(q=13)\mid 168\) (Remark \ref{rem:pom-mod182-period-collapse-q13}).

  \item \textbf{Finite field factor extraction law: \texorpdfstring{$\chi\mid P_q$}{chi|Pq} \texorpdfstring{$\Rightarrow$}{=>} existence of explicitly constructible \texorpdfstring{$\chi$}{chi}-sub-recurrence.}
  If in \(\mathbb{F}_p[\lambda]\), \(P_q\equiv \chi Q\), then for any solution sequence \(S_q\), taking \(S_q^{(\chi)}:=Q(E)S_q\) satisfies \(\chi(E)S_q^{(\chi)}\equiv 0\pmod p\) (Proposition \ref{prop:pom-modp-factor-extraction}).
  For embedding flag \(\chi_{A_2}\mid P_q\), can extract \(A_2\)-shadow sub-recurrence accordingly (Remark \ref{rem:pom-a2-subrecurrence-extraction}).

  \item \textbf{Jordan-order split mod \texorpdfstring{$11$}{11}: irreducible modulation order strictly different between \texorpdfstring{$q=12$}{q=12} and \texorpdfstring{$q=13,15$}{q=13,15}.}
  From Table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}, \(q=12\) contains square of quadratic irreducible factor while \(q=13,15\) contain only linear square factors; thus their Jordan modulation has different minimal degree lower bounds at the irreducible level, usable as internal subdivision criterion within resonance window without complex spectrum computation (Remark \ref{rem:pom-charpoly-mod11-modulation}).

  \item \textbf{Meaning of heavy-tail deficit function \texorpdfstring{$\delta(q)$}{delta(q)} as second-order constant: global scalar invariant determined by sub-leading term of \texorpdfstring{$r_q$}{rq}.}
  Paley--Zygmund lower envelope naturally induces non-negative gap
  \[
  \delta(q):=\log\varphi+\log r_{2q}-2\log r_q\ge 0
  \]
  (Corollary \ref{cor:pom-spectrum-intermittency-deficit}).
  If endpoint expansion \(\log r_q=\frac{q}{2}\log\varphi+a+o(1)\) holds, then direct substitution yields
  \[
  \lim_{q\to\infty}\delta(q)=\log\varphi-a,
  \]
  so \(\delta(q)\) not only measures ``heavy-tail guarantee exponent'' deficit but also inverts the second-order free energy constant \(a\) of collision spectrum endpoint, constituting a global scalar fingerprint for cross-version comparison.

  \item \textbf{Spectral sensitivity of prime-orbit Mertens constant: amplification scale from product structure of \texorpdfstring{$\chi_A'(r)$}{chi'(r)}.}
  For finite-state collision kernel \(A\) (SFT adjacency matrix), its Abel-normalized principal pole residue constant satisfies closed form
  \[
  C(A)=\frac{r^{d-1}}{\chi_A'(r)}=\prod_{\lambda_j\in\sigma(A)\setminus\{r\}}\Bigl(1-\frac{\lambda_j}{r}\Bigr)^{-1},
  \]
  so \(C(A)\) is determined solely by \((\chi_A,r)\) and invariant under any non-negative integer realization preserving the same \(\chi_A\); for \(A_2,A_3,A_4\), \(C(A_q)\) can be promoted to unique real root certificate of integer-coefficient minimal polynomial, forming closed loop ``residue constant \(\Rightarrow\) algebraic equation \(\Rightarrow\) auditable certificate chain'' (see \S\ref{subsec:pom-s4}).

  \item \textbf{A completely integrable Abel-normalized \texorpdfstring{$\zeta$}{zeta} kernel: pole set falls on \texorpdfstring{$\QQ(\sqrt5)$}{Q(sqrt5)} unit group orbit.}
  Under Abel-normalized coordinate \(r=\lambda z\) for true input 40-state kernel, normalized \(\widetilde{\zeta}_M(r)\) has completely factored determinant expression, with all singularities explicitly enumerable as
  \(
  r\in\{1,\pm\lambda,\lambda^2,\varphi^3,-\varphi\}
  \)
  and all except \(r=1\) satisfy \(|r|>1\), so no secondary pole obstruction within \(|r|<1\); principal pole residue constant yields closed algebraic value
  \(
  C=(47+21\sqrt5)/100
  \)
  (\S\ref{app:real-input-40-abel}).
\end{enumerate}

\paragraph{Endpoint clusters---central cross-section---spectral condensation: from four-branch monodromy to \texorpdfstring{$p^{-6}$}{p^-6} decay law}
To unify ``endpoint degeneration jet---central cross-section root disk---higher-order collision kernel spectral condensation'' three certificate types into directly reusable audit interfaces, the following assertions provide a set of complementary verifiable propositions (certificates distributed in sync-kernel weighted elimination appendix and collision spectrum tables):
\begin{enumerate}
  \item \textbf{Four-branch monodromy of endpoint cluster and local dihedral-type monodromy.}
  At the degenerate end \(u\to 0\) of the sync-kernel rate curve, characteristic curve satisfies \(F(\lambda,0)=\lambda^5(\lambda-1)\), whose quintuple zero root bundle rigidly splits into four Puiseux branches and one analytic small root branch (Proposition \ref{prop:rate-resultant-uadic-split-7}):
  \[
  \lambda(u)=\zeta\,u^{1/4}+O(u^{1/2})\quad(\zeta^4=1),
  \qquad
  \lambda(u)=-u+O(u^2).
  \]
  Thus analytic continuation around \(u=0\) induces \(u^{1/4}\mapsto i\,u^{1/4}\) four-cycle monodromy for the four Puiseux branches; correct unification variable for flank endpoint cluster must take \(u=s^4\) (\(s=u^{1/4}\)), forming four-sheet cover \(s\in\{\pm u^{1/4},\pm i\,u^{1/4}\}\) for the same \(u>0\) (Remark \ref{rem:rate-algebraic-elimination-structure}).
  Combined with complex conjugation action on branches, endpoint cluster locally carries dihedral-type monodromy containing \(C_4\) (local monodromy at least \(D_4\)); any realization mistaking this endpoint cluster as ``square root ramification'' must produce incorrect branch switching when encircling \(u=0\).

  \item \textbf{No real singularity in central cross-section forces nearest singularity purely complex; nearest root gives auditable convergence disk and coefficient oscillation fingerprint.}
  In central cross-section factorization \(R(\tfrac12,u)=-(u-1)^6Q(u)/64\), \(Q(u)\in\ZZ[u]\) has strict palindromic self-reciprocity \(Q(u)=u^{16}Q(1/u)\), and Sturm count gives \(Q\) having no zeros on real axis, so \(Q(u)>0\) for all \(u\in\RR\) (Proposition \ref{prop:rate-center-Q-reciprocal-no-real}).
  Therefore for local analytic expansion around \(u=1\), convergence obstruction must be determined by complex singularity pairs; nearest singularity distance
  \(
  R_\star=\min\{|u-1|:\ Q(u)=0\}
  \)
  and nearest root \(u_\star\) are given auditable numerical certificate (Corollary \ref{cor:rate-center-Rstar}):
  \[
  R_\star\approx 1.0507607822,
  \qquad
  u_\star\approx -0.0492815273\pm 0.0557359652\,\mathrm{i}.
  \]
  By standard singularity analysis/coefficient transfer principle, any Taylor coefficient sequence generated by this central cross-section (or completion determinant constructed from it) must exhibit quasi-periodic oscillation locked by \(\arg(u_\star-1)\) at high order; its frequency and decay rate together constitute auditable spectral fingerprint under the ``nearest singularity'' caliber.

  \item \textbf{Minimal modulus obstruction: \texorpdfstring{$p=5$}{p=5} is unique prime exclusion point for central cross-section local expansion.}
  Let \(\omega_p=e^{2\pi i/p}\), then \(|\omega_p-1|=2\sin(\pi/p)\).
  From nearest singularity distance certificate \(R_\star\),
  \(
  |\omega_5-1|>R_\star
  \)
  while
  \(
  |\omega_7-1|<R_\star
  \), and by monotonicity, for all \(p\ge 7\), \(|\omega_p-1|<R_\star\) (Corollary \ref{cor:rate-center-omega-threshold}).
  Therefore any local analytic expansion depending only on \(u=1\) must lie within strict convergence domain at all prime \(p\ge 7\) unit root twists, while at \(p=5\) in principle cannot be controlled by this local caliber, yielding a minimal and unique prime exclusion point.

  \item \textbf{\texorpdfstring{$q$}{q}-root scale spectral condensation of higher-order collision kernels: sandwich of \texorpdfstring{$\Lambda_q^{1/q}$}{Lambda^(1/q)} and \texorpdfstring{$r_q^{1/q}$}{r^(1/q)} toward \texorpdfstring{$\sqrt{\varphi}$}{sqrt(phi)}.}
  For collision moment-kernel \(A_q\) Perron root \(r_q\) and sub-spectral modulus \(\Lambda_q\) (Tables \ref{tab:fold_collision_moment_spectrum_k18_23} and \ref{tab:fold_collision_kernel_rh_scan_q18_23}), define
  \[
  b_q:=r_q^{1/q},\qquad a_q:=\Lambda_q^{1/q}.
  \]
  From extreme order endpoint \(\lim_{q\to\infty}r_q^{1/q}=\sqrt{\varphi}\) (Proposition \ref{prop:pom-rq-qinfty-endpoint}), view \(\sqrt{\varphi}\) as spectral endpoint constant.
  In verified interval \(q=18,\dots,23\), numerical certificate gives strict sandwich \(a_q<\sqrt{\varphi}<b_q\); e.g.
  \[
  (a_{18},b_{18})\approx(1.26368,1.28566),\qquad
  (a_{23},b_{23})\approx(1.26740,1.28011),
  \qquad
  \sqrt{\varphi}\approx 1.27202,
  \]
  showing both make monotone contracting sandwich toward \(\sqrt{\varphi}\) at \(q\)-root scale, unifying ``crossing-line spectral phase transition'' and ``endpoint constant'' into the same spectral condensation trajectory.

  \item \textbf{Auditable monotonicity law for spectral condensation exponent: \texorpdfstring{$\Lambda_q/r_q$}{Lambda/r} approaches \(1\) as \texorpdfstring{$\exp(-qg_q^{(\Lambda)})$}{exp(-qg)}.}
  In the same verified interval, define spectral condensation exponent
  \[
  g_q^{(\Lambda)}:=\frac{1}{q}\log\frac{r_q}{\Lambda_q}\,,
  \qquad
  \frac{\Lambda_q}{r_q}=\exp\!\bigl(-q\,g_q^{(\Lambda)}\bigr).
  \]
  From precise data in Table \ref{tab:fold_collision_kernel_rh_scan_q18_23}, directly read \(g_q^{(\Lambda)}\) strictly decreasing on \(q=18,\dots,23\) (from \(g_{18}^{(\Lambda)}\approx 0.017241\) to \(g_{23}^{(\Lambda)}\approx 0.009979\)), equivalently \(\Lambda_q/r_q\) monotonically increasing from \(0.733\) to \(0.795\) in this interval.
  Thus ``sub-mode sticking to Perron'' can be described not only by Dirichlet real-part caliber \(\log\Lambda_q/\log r_q\uparrow 1\) but also quantified by an explicitly verifiable \(g_q^{(\Lambda)}\) sequence; its monotone decay provides lowest-cost certificate for judging whether spectral condensation enters near-degeneracy region.

  \item \textbf{Discrete variational threshold for moment order selection: interval inequality characterization of optimal \texorpdfstring{$q^\star(c)$}{q*(c)}.}
  Let \(P(q)=\log r_q\) and denote secant slope \(\beta_q=(P(q)-P(1))/(q-1)\).
  Under exponential risk budget \(\varepsilon_m=e^{-cm}\), certificate exponent can be written as
  \[
  \gamma^{\mathrm{cert}}(c):=\inf_{q\ge 2}\left(\beta_q+\frac{c}{q-1}\right),
  \]
  and from discrete convexity, derive \(q\mapsto\beta_q\) monotone non-decreasing (Proposition \ref{prop:pom-pressure-convexity}; see also Remark \ref{rem:pom-quantile-budget-computable}).
  Any optimal moment order \(q^\star(c)\) satisfies discrete Euler--Lagrange-type threshold inequality (Proposition \ref{prop:pom-qstar-discrete-variational-threshold}):
  \[
  (q^\star-1)(q^\star-2)\bigl(\beta_{q^\star}-\beta_{q^\star-1}\bigr)\le c\le q^\star(q^\star-1)\bigl(\beta_{q^\star+1}-\beta_{q^\star}\bigr),
  \]
  so \(c\mapsto q^\star(c)\) is piecewise constant and globally monotone non-decreasing ``phase diagram''. This characterization promotes ``choosing \(q\)'' from heuristic parameter tuning to deterministic threshold law determined entirely by sequence \(\{r_q\}\).

  \item \textbf{Chebyshev dimension reduction of palindromic polynomial: unit root twist compressed to one-variable real polynomial on \texorpdfstring{$[-2,2]$}{[-2,2]}.}
  From \(Q(u)=u^{16}Q(1/u)\) (Proposition \ref{prop:rate-center-Q-reciprocal-no-real}), \(u^{-8}Q(u)\) is invariant under inversion \(u\mapsto 1/u\); by Laurent ring invariant subring identity \(\ZZ[u,u^{-1}]^{u\mapsto 1/u}=\ZZ[u+u^{-1}]\) (Proposition \ref{prop:completion-subring-unique-angle}), there exists unique \(\widetilde Q\in\ZZ[v]\) (\(\deg\widetilde Q=8\)) such that
  \[
  Q(u)=u^8\,\widetilde Q\!\left(u+\frac1u\right).
  \]
  Since \(Q(u)>0\) for all real \(u\), get \(\widetilde Q(v)>0\) for all \(v\in(-\infty,-2]\cup[2,\infty)\).
  Particularly, for any unit root \(\omega_p^k\),
  \[
  Q(\omega_p^k)=\omega_p^{8k}\,\widetilde Q\!\left(2\cos\frac{2\pi k}{p}\right),
  \]
  so ``central cross-section non-degeneracy/singularity proximity under unit root twist'' is strictly compressed to value problem of \(\widetilde Q\) on interval \([-2,2]\), yielding shortest computable local--global arithmetic stratification interface.

  \item \textbf{Central six-fold flatness yields \texorpdfstring{$p^{-6}$}{p^-6} decay law: invisible order of unit root twist and minimal discernible scale.}
  Central cross-section factorization \(R(\tfrac12,u)=-(u-1)^6Q(u)/64\) with \(Q(1)=8515584\neq 0\) (Corollary \ref{cor:rate-center-unique-real} and Remark \ref{rem:rate-center-min-audit}) shows the first five derivatives strictly annihilate at \(u=1\).
  Thus for \(u=\omega_p\) with \(p\to\infty\), leading term
  \[
  R\!\left(\tfrac12,\omega_p\right)
  =-\frac{Q(1)}{64}\,(\omega_p-1)^6+O(p^{-7}),
  \qquad
  \bigl|R\!\left(\tfrac12,\omega_p\right)\bigr|
  =\frac{|Q(1)|}{64}\,(2\sin(\pi/p))^6+O(p^{-7})\asymp p^{-6}.
  \]
  Equivalently, unit root twist is indiscernible in any local expansion of fifth order or below; ``sixth order'' thus becomes the minimal discernible order and minimal scale law for central cross-section identifying twist, directly governing related numerical stability and peeling strategies (Remark \ref{rem:rate-algebraic-elimination-structure}).
\end{enumerate}

\paragraph{Finite-part---tomography---endpoint spray: from \texorpdfstring{$O(\log\varepsilon^{-1})$}{O(log eps^-1)} truncation law to non-square-free nilpotent flags}
To unify ``computability of finite-part constants---Lucas tomography with minimal data dependence---even spray under self-dual symmetry---nilpotent channel of mod \(p\) shadow'' into directly reusable audit interfaces, the following assertions provide a set of complementary verifiable propositions (certificates distributed in finite-part appendix and \(\xi\)/sync-kernel/resonance-window chapters):
\begin{enumerate}
  \item \textbf{Logarithmic precision truncation law for M\"obius--Abel finite-part constant: certifiable algorithm with complexity \texorpdfstring{$O(\log\varepsilon^{-1})$}{O(log eps^-1)}.}
  If \(\zeta_M(z)=\det(I-zM)^{-1}\) is analytic in closed disk \(|z|\le \lambda^{-2}\) with \(\zeta_M(0)=1\), and let
  \[
  A:=\sup_{0<|z|\le \lambda^{-2}}\frac{|\log\zeta_M(z)|}{|z|}<\infty,
  \qquad
  \log\mathfrak{M}:=\log C+\sum_{m\ge 2}\frac{\mu(m)}{m}\log\zeta_M(\lambda^{-m}),
  \]
  then for any \(K\ge 2\) truncation \(\log\mathfrak{M}^{(K)}\), there is explicit tail bound (Proposition \ref{prop:real-input-40-logM-trunc-bound}):
  \[
  |\log\mathfrak{M}-\log\mathfrak{M}^{(K)}|
  \le \frac{A}{(K+1)(\lambda-1)}\,\lambda^{-K}.
  \]
  Thus given \(\varepsilon\in(0,1)\), taking
  \[
  K\ \ge\ \left\lceil \frac{\log\!\bigl(\tfrac{A}{\varepsilon}(\lambda-1)\bigr)}{\log \lambda}\right\rceil
  \]
  guarantees \(|\log\mathfrak{M}-\log\mathfrak{M}^{(K)}|\le \varepsilon\);
  so finite-part constant in ``logarithmic precision'' sense can be certificated computed with \(K\asymp \log(1/\varepsilon)\) terms, independent of any primitive orbit enumeration or high-dimensional spectral detail.

  \item \textbf{Lucas tomography from three-point evaluation: reconstructing Abel constant difference using only trace samples with end-to-end error seal.}
  Under prime-splitting caliber of true input kernel, Lucas trace sample \(L_n=\Tr(F^n)\) (satisfying \(L_n=L_{n-1}+L_{n-2}\)) yields via M\"obius/Witt inversion
  \(
  \pi_n=p_n(F)
  \)
  and
  \(
  r_n=p_n(F\otimes F)
  \);
  further by mod \(4\) selection law get \(p_n(M)\) (Remark \ref{rem:real-input-40-lucas-pipeline}), at odd prime length further collapsing to product law \(p_p(M)=L_p\pi_p\) (Corollary \ref{cor:real-input-40-prime-length-product}).
  Thus Abel constant difference \(\log\mathfrak{M}_M-\log\mathfrak{M}_{\mathrm{in}}\) can be reconstructed using only \(P_F(z)=\sum_{n\ge 1}\pi_n z^n\) evaluated at three points \(z\in\{-\lambda^{-1},\pm\lambda^{-2}\}\) (Corollary \ref{cor:real-input-40-logM-fast-prime}), and when truncating \(P_F\) to \(N\) terms, total error is uniformly sealed by explicit tail functional \(\mathrm{Tail}(N;|z|)\) (Remark \ref{rem:real-input-40-lucas-pipeline}), yielding an end-to-end auditable compilation pipeline with ``minimal data dependence''.

  \item \textbf{Quantitative fingerprint of symmetric heavy tail: primitive layer standardized kurtosis and tail enhancement threshold.}
  Under self-dual completion symmetry, primitive layered count centralized scale has explicit cumulant density (Theorem \ref{thm:sync-kernel-even-edgeworth-c4}):
  \[
  \sigma^2=P''(0)=\frac{11}{102},\qquad
  \kappa_4=P^{(4)}(0)=\frac{1559}{58956},
  \qquad
  c_4=\frac{\kappa_4}{24\sigma^4}=\frac{1559}{16456}.
  \]
  Hence standardized fourth-order ratio is \(\kappa_4/\sigma^4=4677/2057\), standardized kurtosis satisfies
  \[
  \frac{\mathbb{E}[X^4]}{\sigma^4}=3+\frac{\kappa_4}{\sigma^4}=\frac{10848}{2057}\approx 5.274>3,
  \]
  exhibiting strict peak--heavy-tail deviation while all odd central moments are strictly zero.
  Moreover, from Edgeworth first-order correction polynomial \(H_4(x)=x^4-6x^2+3\) (Theorem \ref{thm:sync-kernel-even-edgeworth-c4}) and its derivative identity for tail probability, first-order tail enhancement unavoidably appears for \(|x|>\sqrt3\) (sign threshold locked by \(H_3(x)=x^3-3x\)).

  \item \textbf{``No odd term'' phase diagram for moderate deviation: rate function spray contains only even powers with quadratic curvature constant \texorpdfstring{$51/11$}{51/11}.}
  Let \(\alpha(\theta)=P'(\theta)\) and define rate function by \(I(\alpha)=\sup_\theta(\theta\alpha-P(\theta)+P(0))\).
  Self-dual parity forces \(I(\alpha)=I(1-\alpha)\), so Taylor spray at center point \(\alpha_\star=\tfrac12\) contains only even powers (Proposition \ref{prop:sync-kernel-moderate-deviation-cauchy-certified}).
  Particularly,
  \[
  I(\alpha)=\frac{(\alpha-\tfrac12)^2}{2P''(0)}+O\!\left((\alpha-\tfrac12)^4\right)
  =\frac{51}{11}\Bigl(\alpha-\tfrac12\Bigr)^2+O\!\left((\alpha-\tfrac12)^4\right),
  \]
  where quadratic coefficient is uniquely determined by \(P''(0)=11/102\) (Theorem \ref{thm:sync-kernel-even-edgeworth-c4}).
  Moreover, under moderate deviation scale \(|k-n/2|\le n^{2/3}\), exponential scale error of spray truncation can be uniformly sealed by true analytic radius \(R_\theta\) Cauchy certificate (Proposition \ref{prop:sync-kernel-moderate-deviation-cauchy-certified} and Corollary \ref{cor:pressure-analytic-radius}), without additional free parameters.

  \item \textbf{Bi-phase spectral gap constant at zero temperature limit: \texorpdfstring{$\lambda_{\mathrm{sub}}(q)/\lambda(q)\to\sqrt{2/3}$}{lambda_sub/lambda -> sqrt(2/3)} gives uniform slow mode benchmark.}
  For arity-charge weighted matrix family \(M(q)\) (\(q>0\)) of true input kernel, its non-trivial spectrum is controlled by degree-\(7\) algebraic curve \(P_7(\Lambda,q)=0\) (Theorem \ref{thm:real-input-40-arity-charge-det-closed}), so \(q\mapsto\lambda(q)=\rho(M(q))\) is an analytic algebraic branch.
  At \(q\to+\infty\) (zero temperature end), Perron branch satisfies
  \(
  \lambda(q)=\sqrt{3q}+O(1)
  \)
  (Proposition \ref{prop:real-input-40-arity-charge-zero-temp-expansion}), and the same algebraic curve also has a positive real growth branch
  \(
  \lambda_{\mathrm{sub}}(q)=\sqrt{2q}+O(1)
  \)
  (Remark \ref{rem:real-input-40-arity-charge-large-bias-mechanism}).
  Therefore
  \[
  \lim_{q\to\infty}\frac{\lambda_{\mathrm{sub}}(q)}{\lambda(q)}=\sqrt{\frac{2}{3}}<1,
  \]
  giving uniform slow mode scale constant maintaining positive distance from \(1\) in zero temperature interval, usable as uniform benchmark for spectral gap/mixing rate.

  \item \textbf{Dynamical meaning of mod \texorpdfstring{$p$}{p} non-square-free flag: repeated factor \texorpdfstring{$\Leftrightarrow$}{<->} nilpotent error channel.}
  In finite field slice, decompose recurrence characteristic polynomial as
  \(P_q(\lambda)\equiv\prod_i f_i(\lambda)^{e_i}\).
  If non-zero square factor appears (exists \(e_i\ge 2\) with \(f_i\neq\lambda\)), then corresponding companion dynamics contains non-semisimple Jordan block, so solution space must exhibit \((n^j\lambda^n)\)-type ``periodic term superposed with low-order polynomial drift'' (Remarks \ref{rem:pom-charpoly-modp-audit-method}; \ref{rem:pom-charpoly-mod11-modulation}).
  In verified tables, mod \(7\) for \(q\in\{13,14,15\}\) and mod \(11\) for \(q=12\) all show \(\mathrm{repeat?}=1\) (Tables \ref{tab:fold_collision_charpoly_mod7_selection_q13_15} and \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}), and at \((q,p)=(14,7)\), \((12,11)\) simultaneously show embedding flag \(\chi_{A_2}\mid P_q\) (Corollary \ref{cor:pom-charpoly-modp-a2-subdynamics}), juxtaposing ``nilpotent error channel'' and ``lower-order shadow sub-dynamics'' two local fingerprints under the same mod \(p\) audit caliber.

  \item \textbf{Identifiability of Blaschke endpoint spray: finite-order derivatives give weighted moments of off-critical-line offset and height.}
  Under finite Blaschke setting, endpoint radial absorption profile \(\mathcal{A}_B\) at \(r=1\) has low-order spray satisfying (Theorem \ref{thm:xi-endpoint-absorption-jet-moments}):
  \[
  \mathcal{A}_B(1)=\sum_k \delta_k,\qquad
  \mathcal{A}_B'(1)=\sum_k \delta_k(\delta_k-1),\qquad
  \mathcal{A}_B''(1)=\frac12\sum_k \delta_k\bigl(3(\delta_k-1)^2-\gamma_k^2\bigr).
  \]
  When \(\delta_k\in(0,\tfrac12)\), have \(\mathcal{A}_B'(1)<0\), while second-order term \(-\tfrac12\sum_k\delta_k\gamma_k^2\) gives second-order suppression response to large height.
  Thus \((\mathcal{A}_B(1),\mathcal{A}_B'(1),\mathcal{A}_B''(1))\) constitutes a finite-dimensional ``statistical cross-section'': simultaneously distinguishing offset total, offset dispersion, and weighted penalty of height squared, compressing zero set data into auditable finite-dimensional measure invariants.

  \item \textbf{Minimal auditable order for unit root twist: \texorpdfstring{$p_\star=6$}{p*=6} non-prime threshold and isolation of prime \texorpdfstring{$p=5$}{p=5}.}
  For unit root twist \(u=\omega_p=e^{2\pi i/p}\), with \(\theta=\log u\) as local parameter, Taylor/cumulant expansion at this point can be used as convergent algorithm if and only if \(2\pi/p<R_\theta\) (Corollary \ref{cor:sync-kernel-unit-root-twist-threshold}).
  In sync-kernel output potential weighting, nearest branch point certificate gives \(R_\theta\approx 1.0709533953\) deriving minimal threshold \(p_\star=6\) (Corollary \ref{cor:pressure-unit-root-modulus-threshold}).
  This threshold is not a prime threshold; thus in prime twist, \(p=5\) becomes the first isolated point that in principle cannot be guaranteed convergent by this local caliber, while \(p\ge 7\) automatically fall into auditable convergence domain (consistent with central cross-section root disk threshold chain \ref{cor:rate-center-omega-threshold}).
\end{enumerate}

\paragraph{Spectral closed forms of finite-part constants and twisted exponents: unified certificate chain of derivative reconstruction, M\"obius--Abel truncation, and cyclotomic elimination}
Under determinant caliber of finite-state kernels, both constant-layer finite-part and worst mixing exponent of mod \(m\) twist can be compressed into integer-coefficient algebraic objects: principal pole residue can be completely reconstructed from characteristic polynomial derivative, M\"obius--Abel series gives certifiable truncation error bounds, while completion and cyclotomic resultant reduce unit root twist spectral radius problem to minimal modulus root determination of univariate integer-coefficient polynomial. Conclusive interfaces can be summarized as:
\begin{enumerate}
  \item \textbf{Spectral closed form and algebraic audit of principal pole residue constant: complete reconstruction from \texorpdfstring{$\chi_A'(r)$}{chi'(r)}.}
  For any primitive non-negative adjacency matrix \(A\), let \(\chi_A(\lambda)=\det(\lambda I-A)\) (monic, \(\deg\chi_A=d\)), with Perron root \(r=\rho(A)\) being a simple root; let \(\zeta_A(z)=\det(I-zA)^{-1}\), \(z_\star=1/r\), and define
  \(
  C(A):=\lim_{z\to z_\star}(1-rz)\zeta_A(z)
  \).
  Then
  \[
  \boxed{\;
  C(A)=\frac{r^{d-1}}{\chi_A'(r)}
  =\prod_{\lambda\in\sigma(A)\setminus\{r\}}\left(1-\frac{\lambda}{r}\right)^{-1}\;}
  \]
  (Proposition \ref{prop:finite-part-residue-reduced-determinant}; also see \S\ref{subsec:pom-s4}).
  Hence \(C(A)\) is invariant under any non-negative integer realization preserving the same \(\chi_A\); for reproducible examples of \(A_2,A_3,A_4\), \(C(A_q)\) can be further promoted to unique real root certificate of integer-coefficient minimal polynomial (\S\ref{subsec:pom-s4}), encapsulating constant output as purely algebraic audit object.

  \item \textbf{M\"obius--Abel fast formula and exponential precision truncation certificate: finite-part constant computable and certifiable.}
  Under general setting where \(\zeta_M\) is analytic in \(|z|\le z_\star^{\,2}\) with \(\zeta_M(0)=1\), M\"obius--Abel representation of finite-part constant and explicit tail bound see Proposition \ref{prop:real-input-40-logM-trunc-bound}; key point is: only need finite sampling inside convergence disk to simultaneously give high-precision numerical value and auditable error budget, promoting \(\mathsf{M}=\gamma+\log\mathfrak{M}\) to stable scalar certificate output.

  \item \textbf{Constant layer Artin factorization: trivial factor completely explicitly peelable, finite-part mechanism depends only on non-trivial spectral block.}
  If \(\Delta(z)=\det(I-zM)\) factors as \(\Delta(z)=\Delta_{\mathrm{triv}}(z)\Delta_{\mathrm{nt}}(z)\), where \(\Delta_{\mathrm{triv}}(z)=(1-z)^a(1+z)^b\) and principal pole \(z_\star\in(0,1)\) comes from simple zero of \(\Delta_{\mathrm{nt}}\), then Abel finite-part constant satisfies strict additive factorization
  \[
  \boxed{\ \log\mathfrak{M}=\log\mathfrak{M}_{\mathrm{nt}}+(a-b)z_\star+b z_\star^2\ }
  \]
  (Proposition \ref{prop:real-input-40-reduced-mertens}).
  On true input 40-state kernel, this peeling further refines to even/twist/triv three-factor constant factorization (Remark \ref{rem:real-input-40-logM-artin-factorization}), precisely attributing constant-layer difference to representation channels and non-trivial spectral block finite-dimensional mechanism.

  \item \textbf{Weighted Ihara--Witt Euler product and Dwork-type congruence: prime-shadow generates integer-coefficient Witt vectors.}
  For weighted Hashimoto operator \(B(u)\) bivariate Ihara \(\zeta\)-function \(Z_K(u,t)=\det(I-tB(u))^{-1}\), there exist integer-coefficient primitive Witt coordinates \(q_n(u)\in\ZZ[u]\) such that
  \(
  Z_K(u,t)=\prod_{n\ge 1}(1-q_n(u)t^n)^{-1}
  \)
  strictly holds; ghost components \(b_n(u)=\Tr(B(u)^n)\) simultaneously satisfy necklace divisibility law and Dwork-type congruence
  \(b_{p^k}(u)\equiv b_{p^{k-1}}(u^p)\ (\mathrm{mod}\ p^k)\)
  (Remark \ref{cor:big-witt-audit-guardrails}; Proposition \ref{prop:witt-necklace-dwork-R}).
  Thus prime-like (Euler product) side and trace/spectral side are unified at integer-coefficient level as machine-verifiable commutative diagram, providing structural entry for subsequent \(p\)-adic analysis objects.
  On the other hand, cyclic block trace class lift gives constructive surjection from Euler product to determinant \(\zeta\) (Corollary \ref{cor:cyclic-euler-product}), so mere ``can be written as determinant/Euler product'' formal property does not imply any RH-class spectral constraint; non-trivial structure can only come from additional inputs like Perron--Frobenius positivity, local transition constraints, and auditable channel selection laws.

  \item \textbf{Structured dimension reduction of self-dual elimination: six-fold degeneration point and \texorpdfstring{$1/6$}{1/6} critical inversion exponent.}
  Normalized elimination polynomial of sync-kernel rate curve satisfies self-dual symmetry \(R(\alpha,u)=u^{22}R(1-\alpha,1/u)\), with six-fold aggregation at \(\alpha=\tfrac12\) cross-section
  \(R(\tfrac12,u)=-(u-1)^6Q(u)/64\) (Corollary \ref{cor:rate-center-unique-real}).
  Hence solving \(u=u(\alpha)\) near center point has scaling
  \(
  |u-1|\asymp|\alpha-\tfrac12|^{1/6}
  \)
  (Remark \ref{rem:unit-root-final-spec}; Corollary \ref{cor:rate-duality-center-6-3x2}), achievable as ``cubic in \(X\) + quadratic root extension'' dimension reduction under symmetric coordinates \(X=(2\alpha-1)^2\), \(s=u+1/u\) (Corollary \ref{cor:rate-duality-center-6-3x2}).

  \item \textbf{Cyclotomic elimination: resultant certificate and degree law conjecture for mod \texorpdfstring{$m$}{m} residue-mixing exponent.}
  For mod \(m\) residue mixing, primitive count can be represented by unit root discrete Fourier sampling as
  \(
  p_{n,a}^{(m)}=\frac1m\sum_{r=0}^{m-1}\omega_m^{-ar}\,p_n(\omega_m^r),\qquad \omega_m:=e^{2\pi i/m}
  \);
  its error exponent is controlled by worst twist Perron root \(\rho_m=\max_{1\le r\le m-1}|\lambda(\omega_m^r)|\), precisely characterized by cyclotomic resultant polynomial \(R_m(w)\in\ZZ[w]\) as
  \(
  \rho_m=1/\min\{|w|:R_m(w)=0\}
  \)
  (Proposition \ref{prop:sync-cyclotomic-elimination}; Table \ref{tab:sync_kernel_cyclotomic_elimination_summary}).
  Based on output data for \(m=3,\dots,12\), this paper proposes a testable degree law conjecture (see Conjecture \ref{conj:sync-cyclotomic-elimination-degree-law}).

  \item \textbf{True analytic continuation radius for unit circle twist: nearest complex branch point pins non-perturbative convergence domain guardrail.}
  For local expansion of unit circle twist \(\theta\mapsto\log|\lambda(e^{i\theta})|\), true analytic radius is pinned by nearest complex branch point (Remark \ref{rem:sync-hatdelta-analytic-radius-complex}), whose angular plane coordinate satisfies
  \(
  \theta_\star\approx 1.0556869306\pm 0.1801840156\,i
  \)
  inducing nearest singularity modulus \(|x_\star|\approx 0.4916\) in half-angle coordinate \(x=\tan(\theta/2)\) (Table \ref{tab:sync_kernel_hatdelta_nearest_complex_branch_point_tan_half_angle}).
  This radius provides auditable ``analytic guardrail'' for phase diagram extrapolation and error exponent prediction based on Taylor/Pad\'e.

  \item \textbf{Central structure of window-\texorpdfstring{$6$}{6} and Levi rigidity: canonical embedding of \texorpdfstring{$(\ZZ_2)^3$}{(Z2)^3}, intrinsic \texorpdfstring{$\ZZ_6$}{Z6} quotient, and minimal closure forcing of SM Levi.}
  Boundary dyadic readout of window-\(6\) gives canonical central embedding \((\ZZ_2)^3\hookrightarrow Z(\mathcal{G}^{\mathrm{bin}}_6)\), imposing falsifiable central rank constraint on any continuous envelope candidate (Remark \ref{rem:bdry-center-z2-global-compatibility}).
  Juxtaposing with \(\ZZ_3\) source from cyclic sector can construct intrinsic \(\ZZ_6\) quotient, interpreting global form \((SU(3)\times SU(2)\times U(1))/\ZZ_6\) commonly used in Standard Model as minimal consistency consequence forced by discrete output; when orbit--Levi skeleton and threshold light/heavy splitting requirements are compatibly closed within the same continuous envelope, will force SM-type Levi substructure (Proposition \ref{prop:window6-common-refinement-sm-levi}; also see Conjecture \ref{conj:window6-sm-gauge-as-minimal-compact-observable}).
\end{enumerate}

\paragraph{Peeling principles for representation channels and completion rigidity: toy-RH, constant layer, and auditable reduction of bivariate spectrum}
Under finite kernel caliber, if explicit spectral branches and short-period trivial decorations forcibly embedded by representation channels are not first peeled, square-root-level error terms and constant-level drift in several apparent ``reaching/crossing boundary'' phenomena will inevitably interfere with RH/Ramanujan-type discrimination; once these channels are structurally peeled by determinant factorization and completion congruence, remaining part falls into auditable spectral bounds and finitely generated certificate closure in strict sense.
\begin{enumerate}
  \item \textbf{Unique boundary achievement in toy-RH comes from sign representation channel, not internal resonance.}
  For true input 40-state kernel, determinant has precise three-factor factorization between tensor skeleton and sign channel (Proposition \ref{prop:real-input-40-A0-tensor-hidden}):
  \[
  \det(I-zM)=(1-z)^2(1+z)\,\det\!\bigl(I-z(A_0\otimes A_0)\bigr)\,\det\!\bigl(I-z(-A_0)\bigr).
  \]
  Here \(A_0\) is golden mean skeleton, and unique mode touching square root bound \(|\mu|=\sqrt{\lambda}=\varphi\) is forcibly produced by \(\det(I-z(-A_0))=1+z-z^2\) (Remark \ref{rem:finite-rh-40-hidden-minusA0}), while \(A_0\otimes A_0\) only contributes sub-spectrum of modulus \(1\) and \(\varphi^{-2}\), strictly satisfying \(|\nu|<\sqrt{\lambda}\).
  Thus ``critical line achievement'' is not accidental resonance of high-dimensional kernel structure, but structural embedding of sign representation channel; after peeling \(\zeta_{-A_0}\) in channel projection sense, remaining factor satisfies strict Ramanujan ratio \(<1\), eliminating square-root-level oscillation term in observables.

  \item \textbf{Short-period trivial factor leaves primitive trace only at \texorpdfstring{$n=1,2$}{n=1,2}.}
  M\"obius--\(\log\) transform of trivial period factor \(\zeta_{\mathrm{triv}}=(1-z)^{-2}(1+z)^{-3}\) gives strict closed form
  \(P_{\mathrm{triv}}(z)=-z+3z^2\), so \(p^{\mathrm{triv}}_n=0\ (n\ge 3)\) (Proposition \ref{prop:triv-factor-primitive-polynomial}).
  Thus for all length \(n\ge 3\), primitive orbit spectrum in strict sense is independent of trivial presentation; correspondingly, non-trivial part after peeling \(\zeta_{\mathrm{triv}}\) automatically falls into precise two-factor structure of \(\zeta_{\mathrm{even}}\) and sign factor \(\zeta_{\chi}\) at primitive layer (Remark \ref{rem:real-input-40-logM-artin-factorization}).

  \item \textbf{Abel/Mertens constant of sign channel is universal offset independent of internal dynamics.}
  For twist factor \(\zeta_{-F}(z)=(1+z-z^2)^{-1}\) finite-part constant defined at principal pole \(z_\star=\varphi^{-2}\),
  \(K_{-F}=\sum_{m\ge 1}\frac{\mu(m)}{m}\log\zeta_{-F}(z_\star^{\,m})\)
  depends only on this sign channel itself, satisfying at constant layer
  \(\log\mathfrak{M}=\log\mathfrak{M}_{\mathrm{even}}+K_{-F}+P_{\mathrm{triv}}(z_\star)\) (Remark \ref{rem:real-input-40-logM-artin-factorization}; Definition \ref{def:real-input-40-artin-sign-mertens}).
  Numerical value see Table \ref{tab:real_input_40_artin_sign_mertens} (\(K_{-F}\approx -0.1348652875\ldots\)), providing constant-level diagnostic caliber for ``whether sign representation channel exists'' without enumerating long orbits.

  \item \textbf{Frobenius rigidity of completion ghost and \texorpdfstring{$\ZZ[x]$}{Z[x]} all-rational closure.}
  Under completion coordinates, prime length congruence gives Teichm\"uller-type rigidity: \(\widehat a_p(s)\equiv s^p\ (\mathrm{mod}\ p)\) (Corollary \ref{cor:completed-teichmueller-congruence}).
  Meanwhile, double-angle/power semigroup action on unitary-slice is locally unified in trace coordinates by Chebyshev--Adams polynomial \(C_d\), realizable as pure multiply-add recurrence on \(\ZZ[x]\) in Cayley rational parameter \(x\) (Proposition \ref{prop:chebyshev-adams-fully-rational}); together with double-angle law of primitive completion, gives Adams (\(\lambda\)-ring) realization of power substitution on \(\ZZ[s]\) (Corollary \ref{cor:primitive-completion-hatp}).

  \item \textbf{Explicit spectral branches, zero-charge entropy anchor, and finitely generated certificate of arity-charge bivariate \texorpdfstring{$\zeta$}{zeta}.}
  For arity-charge weighted kernel \(M(q)\), determinant has closed factorization \(\det(I-zM(q))=(1+z)(1-qz^2)\,Q_7(z,q)\) (Theorem \ref{thm:real-input-40-arity-charge-det-closed}), so spectrum for any \(q\) contains explicit eigenvalues \(\pm\sqrt q\) and fixed eigenvalue \(-1\), inducing completely explicit even-odd oscillation terms (Corollary \ref{cor:real-input-40-arity-charge-sqrtq}).
  Thus spectral discrimination regarding square root bound/RH-class in this family should first peel factor \((1+z)(1-qz^2)\) (equivalent \((\Lambda+1)(\Lambda^2-q)\)); after peeling, degree-\(7\) part carries non-trivial spectral mechanism.
  Zero-charge subsystem \(\zeta_0\) is also closed rational function, whose Perron root \(\kappa\) gives algebraic anchor for constrained growth rate (Proposition \ref{prop:real-input-40-arity-charge-zero-zeta}).
  Further, \(\det(I-zM(q))\) has effective degree \(10\) in \(z\) direction, degree \(3\) in \(q\) direction, recoverable by finite-segment primitive data via Newton recurrence closure (Remarks \ref{rem:real-input-40-arity-charge-det-q-cubic} and \ref{rem:real-input-40-arity-charge-newton}), compressing bivariate spectral object into finite audit certificate.
\end{enumerate}

\input{sections/body/conclusion/para__conclusion-fiber-lattice-modq-profinite-toeplitz_en}
\input{sections/body/conclusion/para__conclusion-conductor-resonance-ihara-chebotarev-ptypical-linear-response_en}
\input{sections/body/conclusion/para__conclusion-audit-invisibility-strictification-discretization_en}
\input{sections/body/conclusion/para__conclusion-anom-coprime-two-point-and-rhsharp-probe-resolution_en}
\input{sections/body/conclusion/para__conclusion-fiber-spectrum-resolvent-symmetric-mom-escort-euler-orthogonal_en}
\input{sections/body/conclusion/para__conclusion-pressure-multiplicative-chain-entropy-loss-freezing_en}
\input{sections/body/conclusion/para__conclusion-sympower-rh-instability-excitation-filter-oracle_en}
\input{sections/body/conclusion/para__conclusion-structural-anom-rewrite-fractran-ihara-toeplitz-sdp-profinite_en}
\input{sections/body/conclusion/para__conclusion-dyadic-logdepth-szego-golden-pick-gram-inverse_en}
\input{sections/body/conclusion/para__conclusion-finite-field-shadow-jordan-hair_en}
\input{sections/body/conclusion/para__conclusion-horizon-fiber-capacity-renyi4-threshold_en}
\input{sections/body/conclusion/para__conclusion-arity-charge-endpoints-frobenius-endpoint-tomography_en}
\subsection{Interface Map: Four Auditable Closed Loops and Proof/Semantics Separation}\label{subsec:conclusion-interface-map}
\begin{remark}[Separation of proof chain and explanatory semantics]\label{rem:conclusion-proof-vs-semantics}
The following table compresses the main ``auditable interfaces'' of the full text into four closed loops: each loop records only protocol-internal input objects, citable certificate outputs (theorems/propositions/corollaries/definitions), and corresponding reproducible scripts and artifact paths.
All items appearing as theorems/propositions/corollaries constitute the proof chain of the main text; phrases marked as ``semantics'' in the table are used only to align traditional terminology and parameter readouts, not as premises of any proof, nor as assumptions in subsequent derivations.
\end{remark}

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\caption{Interface map of four closed loops: input objects, certificate outputs, and reproducible artifacts.}
\label{tab:conclusion-interface-map}
\begin{tabular}{p{0.13\linewidth} p{0.22\linewidth} p{0.33\linewidth} p{0.32\linewidth}}
\toprule
Closed loop (equivalence chain) & Input objects (protocol-internal) & Output certificates (theorem chain anchors) & Reproducible scripts/artifacts (implementation anchors)\\
\midrule
\textbf{Computational closure}\\[-2pt]
ISA \(\Leftrightarrow\) projection word \(\Leftrightarrow\) normalized rewriting
&
Arithmetic ISA/microcode programs (\S\ref{subsec:pom-isa});
Projection words \(W\in\mathbf{PW}\);
FRACTRAN programs (\S\ref{subsec:pom-fractran})
&
Order-gated translation on \(\mathcal{P}\) characterizes FRACTRAN-step (Definition \ref{def:pom-fractran}, Proposition \ref{prop:pom-fractran-prime-translation});
Value-layer rewriting \((\mathrm{RZ},\mathrm{RNF})\) termination and ``at most one \texttt{PROJ\_NORM} at end'' normal form (Definition \ref{def:pom-rewriting-val}, Proposition \ref{prop:pom-rewriting-terminating});
Explicitly identifying order projection \(P_{\le}\) as structural boundary (Remark \ref{rem:pom-rewrite-boundary-order})
&
\texttt{\detokenize{scripts/pom_isa_sim_v2.py}} (Remark \ref{rem:pom-isa-sim});
\texttt{\detokenize{scripts/exp_pom_rewriting_engine_demo.py}}
\(\to\) Table \ref{tab:pom_rewriting_engine_demo} (Remark \ref{rem:pom-rewriting-engine-demo})
\\
\midrule
\textbf{Geometric anchor}\\[-2pt]
window-\(6\) \(\Leftrightarrow\) 6D cut-and-project \(\Leftrightarrow\) \((\ZZ_2)^3\) and \(\delta=34\)
&
window-\(6\) boundary three axes
\(
X_6^{\mathrm{bdry}}=\{\texttt{100001},\texttt{100101},\texttt{101001}\}
\);
Dyadic uplift two-sheet cover difference;
Mod \(2\) image of internal phase factor \(T_{\mathrm{int}}\) (semantics)
&
Boundary three dyadic fibers independently generate central embedding \((\ZZ_2)^3\hookrightarrow Z(\mathcal{G}_6^{\mathrm{bin}})\) (Corollary \ref{cor:fold-bin-boundary-center-z2});
Triple identity of \(\delta=34\) and juxtaposition of two-sheet cover/mask/tail branch (Corollary \ref{cor:bdry-delta34-triple-identity});
Internal torus homology anchoring and phason \(2\)-torsion readout of cut-and-project (semantics: Remarks \ref{rem:bdry-tint-h1-cartan-anchor}, \ref{rem:bdry-delta34-phason-2torsion})
&
\texttt{\detokenize{scripts/exp_window6_c6_orbit_patisalam_seed.py}}
\(\to\) Table \ref{tab:fold6_boundary_sheet_pairs} (also see uplift-choice collapse in Table \ref{tab:fold6_bin_uplift_choice_collapse})
\\
\midrule
\textbf{Spectral positivity backbone}\\[-2pt]
Carath\'eodory \(\Leftrightarrow\) Toeplitz--PSD \(\Leftrightarrow\) Schur/Verblunsky \(\Leftrightarrow\) CMV
&
Moments of horizon Carath\'eodory logarithmic derivative interface \(\mathscr{C}_\zeta\) (Appendix \ref{subsubsec:app-horizon-weyl-carath-toeplitz});
Or single-point spray data \(\{\ell_k\}\) (\S\ref{subsubsec:xi-verblunsky-cmv-from-local-jet})
&
RH \(\Leftrightarrow\) Carath\'eodory positivity \(\Leftrightarrow\) Toeplitz--PSD hierarchy (Appendix Theorem \ref{thm:app-horizon-toeplitz-lmi});
Scalarization of Toeplitz certificate and Verblunsky coefficient generation (Theorem \ref{thm:xi-verblunsky-from-local-jet});
Toeplitz determinant product factorization and minimal destabilization localization (Theorem \ref{thm:xi-toeplitz-det-verblunsky});
Finite-window CMV (Hilbert--P{\'o}lya) unitary spectral realization (Proposition \ref{prop:xi-hilbert-polya-cmv})
&
\texttt{\detokenize{scripts/exp_sync_kernel_xi_cubic_cos_polynomial.py}}
\(\to\)
\texttt{\detokenize{sections/generated/eq_sync_kernel_xi_cubic_cos_polynomial.tex}},
\texttt{\detokenize{sections/generated/tab_sync_kernel_xi_cubic_roots.tex}}
(Remark \ref{rem:xi-hard-zero-certificate})
\\
\midrule
\textbf{Resonance explanation entry}\\[-2pt]
Poisson/Breit--Wigner (physics terminology) \(\Leftrightarrow\) SU(1,1)--RHP \(\Leftrightarrow\) Maslov
&
Inner function phase \(\Theta(t)=\arg D(\tfrac12+\mathrm{i}t)\);
Off-line zero parameters \((\gamma_k,\delta_k)\);
Positive-definite jump data and spectral flow parameters (\S\ref{subsubsec:xi-debranges-canonical-rhp-adelic})
&
Poisson superposition law for phase derivative: off-line zero contribution Lorentz/Breit--Wigner kernel (Proposition \ref{prop:xi-phase-derivative-poisson-superposition});
SU(1,1)--RHP positive-definite jump solvability equivalence template (Theorem \ref{thm:xi-su11-rhp-equivalence});
Maslov index formula for zero count difference (Proposition \ref{prop:xi-maslov-spectralflow-entropy-quantization});
Lorentz pre-limit and width parameter under resonance window two-step rescaling caliber (Remark \ref{rem:pom-resonance-poisson-lorentz-prelimit})
&
\texttt{\detokenize{scripts/exp_xi_horizon_dtn_fourier_laplace_audit.py}}
\(\to\)
\texttt{\detokenize{sections/generated/tab_xi_horizon_dtn_fourier_laplace_audit.tex}}
(\S\ref{subsec:discussion-horizon-zk-toeplitz-area-law}; resonance window data Table \ref{tab:fold_collision_shadow_spectral_packet_q9_17})
\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Reproduction and Audit Guide}\label{subsec:repro-audit-pointer}
Explanatory synthesis, boundaries, and falsifiable points are collected in Section \ref{sec:discussion}. The appendices compile supporting materials on operator algebras, phase towers, synchronization kernels, and multi-scale interfaces, supporting the auditable reasoning chain and verification needs of the main text.

\paragraph{Concluding remark (falsifiable programmatic positioning)}
Under the protocol caliber fixed in this paper, the local normalization rules of \(\Fold_m\) are formally minimal; their ``depth'' comes from the irreducible structure of the finite-state objects they induce in two directions: one is the \textbf{spectrum} (including the algebraic scale locked by the Perron principal root and the residual modes carried by non-semisimple Jordan defects), and the other is \textbf{symmetry} (the equivariant compression and block rigidity enforced by lexicographic/orbit structure).
In the same spectral world, kernel-version RH/de Bruijn--Newman-type thresholds allow critical phase transitions to be precisely pinned down by integer-coefficient elimination certificates (Remark \ref{rem:pom-collision-rh-break-a4-threshold}; Proposition \ref{prop:sync-kernel-weighted-newman-threshold}); the characteristic polynomial factorization over \(\mathbb{F}_p\) gives a set of local reduction fingerprints orthogonal to the real spectrum (Remark \ref{rem:pom-charpoly-modp-audit-method}), and at higher-order endpoints, \(\varphi\) is crystallized as a self-calibrating constant invertible from the collision spectrum (Proposition \ref{prop:pom-rq-qinfty-endpoint}).
Both structural types can be compressed into finite certificates (rational generating functions and Perron principal roots: Proposition \ref{prop:pom-multiplicity-composition-partition-rational}; Jordan residuals and rank/recursion constraints: Theorems \ref{thm:fold-gauge-anomaly-A0-jordan}, \ref{thm:fold-gauge-anomaly-hankel-rank3}; Weyl orbit compression and uplift blocking: Corollary \ref{cor:fold6-weyl-two-orbit-compression}, Theorem \ref{thm:boundary-m10-b3c3-rigid-four-block-split}),
and therefore the unified narrative of this paper is not an explanatory patchwork relying on analogy, but a falsifiable structure defined by auditable outputs.
