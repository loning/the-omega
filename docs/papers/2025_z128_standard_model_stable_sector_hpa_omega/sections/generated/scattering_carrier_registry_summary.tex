\paragraph{Scattering carrier registry (audit).} \AuditTag This fragment records a fixed CAP-selected toy scattering carrier as a reproducible registry artifact and is intended to be used by downstream toy simulations (M2-first; M1 fallback if missing). M2 enabled: eligible\_targets=3, objective(mean abs-log)=0.024693, gap=0.000000.