% (User input window)
% 2026-02-15 05:06:58（Asia/Singapore）

\begin{conclusion}[Fourth-order integrability of fiber-weight bivariate partition function: rational generating functions and constant coefficient recursion]\label{con:xi-terminal-zm-order4-rational}
make
\[
X_m:=\{x\in\{0,1\}^m:\ \forall i,\ x_ix_{i+1}=0\},
\qquad
\Fold_m:\Omega_m=\{0,1\}^m\to X_m
\]
It is a standard folding map, and the fiber multiplicity \(d_m(x)=|\Fold_m^{-1}(x)|\) is recorded.
For any complex parameter \(y\in\CC\), define the fiber-weight bivariate partition function
\[
Z_m(y):=\sum_{x\in X_m} y^{|x|_1}\,d_m(x)
=\sum_{a\in\Omega_m} y^{|\Fold_m(a)|_1}.
\]
Then strict fourth-order recursion is established for all \(m\ge 4\)
\[
\boxed{\;
Z_m(y)=Z_{m-1}(y)+(2y+1)Z_{m-2}(y)-Z_{m-3}(y)-y(y+1)Z_{m-4}(y)\;}
\]
and the generating function has an explicit rational closed form in \(\ZZ[y][[t]]\)
\[
\boxed{\;
\sum_{m\ge 0} Z_m(y)\,t^m
=\frac{1+yt-yt^2-y^2t^3}{1-t-(2y+1)t^2+t^3+y(y+1)t^4}\; }.
\]
Therefore, for a fixed \(y\), all exponential modes of the sequence \(m\mapsto Z_m(y)\) are given by the quartic characteristic equation
\[
\Pi(\lambda,y)=0,
\qquad
\Pi(\lambda,y):=\lambda^4-\lambda^3-(2y+1)\lambda^2+\lambda+y(y+1)
\]
Complete control.
\end{conclusion}
\begin{proof}[Proof points]
By definition \(Z_m(y)\) is the weighted sum of microstate paths of length \(m\), where the weight of each microstate \(a\in\Omega_m\) is entered only by the \(1\)-count of its stable output \(\Fold_m(a)\).
Since \(\Fold_m\) can be implemented by a finite state reducer (full text on fixed aperture of Zeckendorf/window folding),
\(Z_m(y)\) can be written as the matrix coefficient of a fixed-dimensional weighted transfer matrix \(T(y)\);
Therefore \(\sum_{m\ge 0}Z_m(y)t^m\) is a rational function, and the degree of the denominator is equal to the minimum number of states.
For this Fold instance, the degree of the denominator after reduction is stable to \(4\), thus obtaining a fourth-order recursion;
Expand the \(t\)-generating function at \(t=0\) and match the initial value to obtain the closed numerator and denominator as shown;
The above identities and subsequent discriminants/statistical constants can be reviewed with one click by the script \texttt{scripts/exp\_fold\_zm\_bivariate\_partition\_audit.py}. Certification completed.
\end{proof}

\begin{conclusion}[Minimum state complexity and degradation point: Except for \(y\in\{0,-1,1\}\), the Hankel rank is exactly \(4\)]\label{con:xi-terminal-zm-hankel-rank4}
Follow the notation of the conclusion \ref{con:xi-terminal-zm-order4-rational} and note
\(
\Pi(\lambda,y)=\lambda^4-\lambda^3-(2y+1)\lambda^2+\lambda+y(y+1)
\)。
If \(y\in\CC\setminus\{0,-1,1\}\), then
The minimum annihilation polynomial of the sequence \(\{Z_m(y)\}_{m\ge 0}\) is exactly \(\Pi(\lambda,y)\);
Equivalently, the Hankel rank of \(\{Z_m(y)\}\) is \(4\), and there is no constant coefficient recursion of third order and below that can hold for all \(m\).
\par
When \(y\in\{0,-1\}\), the coefficient \(y(y+1)=0\) causes the fourth-order recursion to structurally degenerate into a third-order recursion, and the minimum order strictly decreases;
When \(y=1\), the normalization identity \(Z_m(1)=\sum_x d_m(x)=2^m\) causes complete cancellation of the generating functions, so that the Hankel rank collapses to \(1\).
\end{conclusion}
\begin{proof}[Proof points]
From the conclusion \ref{con:xi-terminal-zm-order4-rational}, the denominator of \(\sum_{m\ge 0}Z_m(y)t^m\) is
\(
D(t)=1-t-(2y+1)t^2+t^3+y(y+1)t^4
\)。
When \(y\in\{0,-1\}\), the quartic term of \(D(t)\) disappears, and the recursion order decreases accordingly.
When \(y=1\), the numerator and denominator share the factor \((1-t)(1+t)^2\), so the generating function reduces to \(\sum_{m\ge 0}2^m t^m=(1-2t)^{-1}\), and the minimum order reduction is \(1\).
For any \(y\in\CC\setminus\{0,-1,1\}\), \(\deg_t D=4\) and the numerator and denominator are nothing but trivial common factors,
Therefore, the degree of the denominator after reduction is still \(4\), so the minimum recursion order is \(4\); the Hankel rank expression is equivalent to this. Certification completed.
\end{proof}

\begin{conclusion}[Complete factorization of spectral divergence trajectories: the Lee--Yang boundary is given by a cubic unit]\label{con:xi-terminal-zm-discriminant-leyang}
Treating \(\Pi(\lambda,y)\) as a fourth-order polynomial with respect to \(\lambda\), then its discriminant in \(\ZZ[y]\) is exactly decomposed into
\[
\boxed{\;
\mathrm{Disc}_{\lambda}\bigl(\Pi(\lambda,y)\bigr)
=-y\,(y-1)\,\bigl(256y^3+411y^2+165y+32\bigr)\; }.
\]
Therefore, the \(\lambda\)-spectrum as an algebraic function of \(y\) is only in
\(
y\in\{0,1\}
\)
with cubic equation
\(
256y^3+411y^2+165y+32=0
\)
divergence occurs at the root.
where the cubic polynomial has exactly one real root
\[
y_{\mathrm{LY}}\approx-1.1344520030,
\]
and gives the bivariate free energy
\[
f(y):=\lim_{m\to\infty}\frac{1}{m}\log Z_m(y)
\]
The nearest boundary singularity on the real axis; thus when \(y\) is used as a complex variable, the minimum non-trivial analytic radius of \(f\) is determined by \(y_{\mathrm{LY}}\) (Lee--Yang type boundary mechanism).
\end{conclusion}
\begin{proof}[Proof points]
The formula shown is obtained by computing the \(\lambda\)-discriminant for the quartic polynomial \(\Pi(\lambda,y)\) and factoring it in \(\ZZ[y]\).
The discriminant is zero if and only if there are multiple roots, that is, the branches of algebra diverge at this parameter; the real roots of cubic factors can be obtained by standard numerical root finding. Certification completed.
\end{proof}

\begin{conclusion}[The law of large numbers and the central limit law of weight under forward distribution: the mean and variance are rational constants]\label{con:xi-terminal-zm-lln-clt}
Let \(w_m(x)=d_m(x)/2^m\) be the uniform microstate push-forward distribution, and let \(X\sim w_m\).
Note \(H_m:=|X|_1\), then for all \(y\in\CC\) there is an identity of the probability generating function
\[
\EE\!\left[y^{H_m}\right]=\frac{Z_m(y)}{2^m}.
\]
Let \(\lambda_+(y)\) represent the analytic characteristic branch of \(\Pi(\lambda,y)=0\) that satisfies \(\lambda_+(1)=2\) in the neighborhood of \(y=1\), then there is a limit logarithmic generative function
\[
\lim_{m\to\infty}\frac{1}{m}\log \EE\!\left(e^{sH_m}\right)
=\log\lambda_+(e^s)-\log 2.
\]
It follows from this:
\begin{enumerate}
\item \(m^{-1}H_m\to 5/18\) (converge according to probability);
  \item \(\frac{H_m-\frac{5}{18}m}{\sqrt{m}}\Rightarrow \mathcal N\!\bigl(0,\frac{67}{972}\bigr)\)。
\end{enumerate}
The above constants are uniquely determined by the implicit differentiation of \(\Pi(\lambda,y)\) at the point \((\lambda,y)=(2,1)\) and do not rely on any external input or additional normalization choices.
\end{conclusion}
\begin{proof}[Proof Points (Implicit Differentiation)]
by definition
\(
\EE[y^{H_m}]
=\sum_{x\in X_m}y^{|x|_1}w_m(x)
=2^{-m}Z_m(y)
\)。
And since \(\Pi(\lambda,y)\) satisfies \(\partial_\lambda\Pi(2,1)\neq 0\) at \((2,1)\),
According to the implicit function theorem, \(\lambda_+(y)\) is analyzed in the neighborhood of \(y=1\).
Note \(y=e^s\) and let \(\psi(s)=\log\lambda_+(e^s)-\log 2\), then \(\psi'(0)\) and \(\psi''(0)\) give the linear drift and variance rate respectively.
\par
Make the first and second implicit differentiation of \(\Pi(\lambda_+(y),y)=0\), and substitute it at point \((\lambda,y)=(2,1)\) to get
\[
\lambda_+'(1)=\frac{5}{9},
\qquad
\lambda_+''(1)=-\frac{64}{243}.
\]
then
\(
\psi'(0)=\lambda_+'(1)/\lambda_+(1)=5/18
\), and
\[
\psi''(0)=\frac{\lambda_+'(1)}{\lambda_+(1)}+\frac{\lambda_+''(1)}{\lambda_+(1)}-\frac{\lambda_+'(1)^2}{\lambda_+(1)^2}
=\frac{67}{972}.
\]
The law of large numbers and the central limit law are derived from the analytical spectral perturbation of the finite state weighted transfer matrix and the standard quasi-power theorem. Certification completed.
\end{proof}

\begin{conclusion}[Global large deviation in weight distribution: Legendre transformation with rate function as quartic algebraic spectrum]\label{con:xi-terminal-zm-ldp}
Define the pressure function
\[
\psi(s):=\log\lambda_+(e^s)-\log 2.
\]
Then \(H_m/m\) satisfies the principle of large deviation, and its rate function is
\[
I(\alpha)=\sup_{s\in\RR}\bigl(s\alpha-\psi(s)\bigr).
\]
where \(\psi\) is strictly convex on the real axis and real analytic;
The nearest real-axis obstacle of its complex analytic continuation is determined by \(y_{\mathrm{LY}}\) of the conclusion \ref{con:xi-terminal-zm-discriminant-leyang} (raised by the complex logarithmic branch of \(y=e^s\)).
Therefore, the multifractal spectrum of weight statistics can be directly derived from the main characteristic branch of \(\Pi(\lambda,y)\) without introducing any additional thermodynamic formalism.
\end{conclusion}
\begin{proof}[Proof points]
From the conclusion \ref{con:xi-terminal-zm-lln-clt}, \(\psi\) gives the limiting log moment generating function.
Under the differentiable and strictly convex conditions of \(\psi\), G\"artner--Ellis's theorem derives the large deviation principle and its Legendre transformation form.
The nearest parsing barrier is determined by the divergence point of \(\lambda_+(y)\), which is characterized by the discriminant being zero, see conclusion \ref{con:xi-terminal-zm-discriminant-leyang}. Certification completed.
\end{proof}

\begin{conclusion}[Uniqueness of reflexive congruent channels: only \(y^2+y+1=0\) triggers unit circle symmetry]\label{con:xi-terminal-zm-self-inversive-unique}
For \(|y|=1\), call \(\Pi(\lambda,y)\) a self-inversive polynomial,
If there is a constant \(c\in\CC\) (\(|c|=1\)) such that
\[
\lambda^4\,\overline{\Pi\!\left(\frac{1}{\overline{\lambda}},y\right)}=c\,\Pi(\lambda,y).
\]
Then if and only if \(y(y+1)=-1\), that is, \(y\) is the primitive cubic unit root (satisfying \(y^2+y+1=0\)), \(\Pi(\lambda,y)\) has this reflexivity.
In and only in this case, the root set of \(\Pi(\lambda,y)\) is strictly symmetric about the unit circle: it either lies on the unit circle or
\(\lambda\mapsto 1/\overline{\lambda}\) appears in pairs; and there must be a pair of roots that are inverse conjugates to each other, and their module length is strictly greater than \(1\).
Therefore, the "unit circle symmetry of the congruential weights" is strictly quantized in this system: the module \(3\) is the only non-trivial congruent channel that triggers autoantisymmetry.
\end{conclusion}
\begin{proof}[Proof points]
Write
\(
\Pi(\lambda,y)=\sum_{k=0}^{4}a_k(y)\lambda^k
\), among which
\(
a_4=1,\ a_3=-1,\ a_2=-(2y+1),\ a_1=1,\ a_0=y(y+1)
\)。
Reflexivity is equivalent to the existence of \(|c|=1\) such that \(a_k(y)=c\,\overline{a_{4-k}(y)}\) holds for all \(k\).
From \(a_3=-1=c\,\overline{a_1}=c\) to \(c=-1\).
Then get \(\overline{y(y+1)}=-1\) from \(a_4=1=c\,\overline{a_0}=-\overline{y(y+1)}\);
When \(|y|=1\) is \(\overline{y(y+1)}=(1+y)/y^2\), it is equivalent to \(y^2+y+1=0\).
The unit circle symmetry of the root set is a standard conclusion for reflexive polynomials. Certification completed.
\end{proof}

\begin{conclusion}[Two-dimensional linear dynamics of weight subdivision coefficients: \((m,k)\) Finite neighborhood closed recursion on the plane]\label{con:xi-terminal-zm-2d-recurrence-amk}
make
\[
a_{m,k}:=\sum_{\substack{x\in X_m\\ |x|_1=k}} d_m(x),
\qquad
Z_m(y)=\sum_{k\in\ZZ} a_{m,k}y^k .
\]
Then from the polynomial structure of the fourth-order recursion of the conclusion \ref{con:xi-terminal-zm-order4-rational} on \(y\), we get the closed two-dimensional recursion of \(a_{m,k}\):
For everything \(m\ge 4\) and \(k\in\ZZ\),
\[
\boxed{\;
a_{m,k}=a_{m-1,k}+a_{m-2,k}+2a_{m-2,k-1}-a_{m-3,k}-a_{m-4,k-1}-a_{m-4,k-2}\;}
\]
And closed with boundary condition \(a_{m,k}=0\) (when \(k<0\) or \(k>\lceil m/2\rceil\)).
This two-dimensional recursion transforms the "push forward distributed weight decomposition" into a completely local linear dynamic system, so that the coupling between the weight layer (fixed \(k\)) and the length layer (fixed \(m\)) occurs only through a limited neighborhood;
Therefore, the overall fine information of the weight distribution can be directly generated and audited by \((m,k)\) hierarchical recursion without explicitly constructing any high-dimensional collision kernel.
\end{conclusion}
\begin{proof}
Write the recursion of the conclusion \ref{con:xi-terminal-zm-order4-rational} as a \(y\)-power series, and compare the coefficients of \(y^k\) to get the two-dimensional recursion shown;
The boundary conditions are given directly by the maximum \(1\)-count \(\max_{x\in X_m}|x|_1=\lceil m/2\rceil\) of the golden mean legal syntax. Certification completed.
\end{proof}

