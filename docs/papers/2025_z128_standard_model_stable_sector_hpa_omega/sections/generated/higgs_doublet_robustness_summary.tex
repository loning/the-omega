\noindent Robustness sweep for the uplift-derived Higgs-like doublet (interface): we recompute the deterministic pairing selection over a bounded family of addressing and coarse-graining variants around the audited $m=10$ instance. The canonical pairing is $\{\{0,3\},\{1,2\}\}$, and the sweep match rate is 0.545. A mismatch under a counterfactual addressing basis is treated as an explicit sensitivity finding and is recorded in the table.