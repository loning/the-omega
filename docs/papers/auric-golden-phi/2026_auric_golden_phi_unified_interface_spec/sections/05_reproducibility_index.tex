\section{可复现性索引（最小入口）}

\subsection{核心可复现断言（跨论文通用）}

\begin{itemize}
  \item \textbf{黄金均值计数：}
  对 $X_m=\{w\in\{0,1\}^m:\text{无相邻 }11\}$，
  \[
  \card{X_m}=F_{m+2}.
  \]
  \item \textbf{折叠压缩：}
  在窗口词视角，$\Fold_m:\{0,1\}^m\twoheadrightarrow X_m$；
  在整数标签视角，$\Fold_m:Y_m\to X_m$（$Y_m=\{0,\dots,2^m-1\}$）。
  \item \textbf{纤维对象化：}
  \[
  \mathcal{F}_m(x)=\Fold_m^{-1}(x).
  \]
  \item \textbf{信息论时间：}
  在生成分割/遍历条件下，柱集后验测度的指数收缩率等于 KS 熵率（SMB/KS 模板）。
\end{itemize}

\subsection{建议的最短验证路线}

\begin{itemize}
  \item 先在 $m=6$ 的离散核上做全枚举：验证 $2^6=64$ 被折叠到 $F_8=21$，并复核纤维退化谱。
  \item 再在无理旋转（或环面平移）上做符号化：验证复杂度 $p(n)=n+1$ 的 Sturmian 指纹。
  \item 最后在生成分割条件下估计柱集收缩率：把其斜率与熵率估计对齐。
\end{itemize}

