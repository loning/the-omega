\section{A Page-curve surrogate in the protocol language (BH5)}
\label{app:bh_page_surrogate}

\paragraph{Scope and status.}
\AuditTag This appendix records a protocol-computable surrogate for Page-curve behavior.
It is an interface/audit module: it does not claim a quantum-gravity path-integral derivation.
Its purpose is to (i) state the unitarity/recovery target in terms of an external record and
(ii) provide deterministic, reproducible diagnostics that distinguish
``blackbody-like low-order statistics'' from ``information recovery via higher-order correlations''.
The scope contract and failure-point rules are stated in Appendix~\ref{app:bh_scope_contract}.

\subsection{Surrogate observable: remaining microstate ambiguity given a record prefix}
\label{subsec:bh5_surrogate_definition}

\paragraph{Setup.}
Fix a window length $m$ and treat the microstate index stream as $N_1,\dots,N_L$ with
$N_i\in\{0,\dots,2^m-1\}$.
Let $W_i=\mathrm{Fold}_m(N_i)\in X_m$ be the coarse stable-label record.
Let $g_m(w)=|\mathrm{Fold}_m^{-1}(w)|$ denote folding degeneracy.

\paragraph{Coarse-record ambiguity.}
If the external record includes only $W_1,\dots,W_L$, then each $N_i$ is ambiguous within
its folding fiber of size $g_m(W_i)$.
The corresponding unresolved micro-ambiguity (bits) is the additive surrogate
\[
U_{\mathrm{coarse}}:=\sum_{i=1}^{L}\log_2 g_m(W_i).
\]
This quantity is protocol-computable at fixed $m$ once the record $W_i$ is given and the fold map is fixed.

\paragraph{Recovery goal.}
\InterfaceTag A recovery mechanism is any extension of the external record that drives the
remaining ambiguity $U(t)$ to (approximately) zero as the record length increases, within stated
tolerance and under stated budget constraints.

\paragraph{A Page-like criterion (audit).}
\AuditTag We say a record exhibits \emph{Page-like} behavior (in the surrogate sense) if there exists a time $t_\ast$
such that $U(t)$ increases for $t<t_\ast$ (coarse/thermal-like epoch), then decreases for $t>t_\ast$ and approaches $0$
by the end of the non-vacuum record (recovery epoch), within the declared hypothesis class and tolerance.

\subsection{A deterministic two-phase toy that exhibits Page-like recovery (audit)}
\label{subsec:bh5_two_phase_toy}

\paragraph{Toy schedule (audit-only).}
\AuditTag To isolate the logic of ``coarse blackbody-like epoch'' versus ``late recovery'',
we use a two-phase record:
\begin{itemize}
\item \textbf{Phase I (coarse).} Emit only stable labels $W_i$.
\item \textbf{Phase II (recovery).} Emit an auxiliary bitstream that encodes fiber coordinates for the same
microstates, making the record information-complete at fixed $m$.
\end{itemize}
This is a protocol-level surrogate and is not claimed as a literal semiclassical Hawking model.

\begin{figure}[t]
\centering
\includegraphics[width=0.82\textwidth]{figures/bh_page_surrogate.png}
\caption{A Page-curve surrogate in the protocol language: the remaining micro-ambiguity $U(t)$ (bits)
as a function of record length under the deterministic two-phase toy (Phase I coarse labels; Phase II
auxiliary recovery bits). The figure is generated by \texttt{scripts/exp\_bh\_page\_surrogate.py}.}
\label{fig:bh_page_surrogate}
\end{figure}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{r l r r}
\toprule
tick $t$ & phase & $U(t)$ [bits] & recovered fraction \\
\midrule
\input{sections/generated/bh_page_surrogate_curve_rows.tex}%
\end{tabular}
\caption{Sampled rows for the Page-surrogate curve. Rows are generated by
\texttt{scripts/exp\_bh\_page\_surrogate.py}.}
\label{tab:bh_page_surrogate_rows}
\end{table}

\paragraph{A mixed single-stream refinement (audit).}
\AuditTag The two-phase toy above separates the coarse and recovery epochs explicitly.
For a more black-hole-like proxy in which both epochs are carried by a \emph{single} stable-label stream,
we also record a deterministic mixed single-stream model:
payload coarse labels and recovery digits (fiber information) are interleaved by a CAP-selected finite family,
and the record is self-describing (the decoding parameters are embedded as a short stable-label header).
This refinement is generated by \texttt{scripts/exp\_bh\_page\_surrogate\_mixed\_stream.py}.

\paragraph{RB-A/RB-D certificate hooks (audit).}
\AuditTag In the mixed single-stream refinement, the interleaving schedule parameters (e.g.\ \texttt{scramble\_delay} and
\texttt{exponent}) are selected from an explicit finite candidate family by a deterministic CAP lexicographic key.
This is an RB-A style certificate (unique minimizer in a finite family).
Robustness of the observed Page-like surrogate shape under bounded variations (message seeds, legal absorption mode, and $m$)
is an RB-D style target; it is audited by generated diagnostics.

\IfFileExists{figures/bh_page_surrogate_u_curve.png}{
\begin{figure}[t]
\centering
\includegraphics[width=0.82\textwidth]{figures/bh_page_surrogate_u_curve.png}
\caption{Queue-equivalent single-stream Page surrogate: $U(t)$ computed directly from folding fibers under the internal CAP-selected schedule.}
\label{fig:bh_page_surrogate_u_curve}
\end{figure}
}{}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{r r r r r}
\toprule
tick $t$ & payload emitted & recovery emitted & $U(t)$ [bits] & recovered fraction \\
\midrule
\input{sections/generated/bh_page_surrogate_u_curve_rows.tex}%
\end{tabular}
\caption{Sampled rows for the queue-equivalent single-stream $U(t)$ curve. Rows are generated by \texttt{scripts/exp\_bh\_page\_surrogate\_u\_curve.py}.}
\label{tab:bh_page_surrogate_u_curve_rows}
\end{table}

\input{sections/generated/bh_page_surrogate_u_curve_summary.tex}%

\paragraph{RB-D robustness audit (generated).}
\AuditTag The following table reports a bounded robustness sweep for the Page surrogate across $(m,\texttt{mode},\texttt{seed})$,
recording exact-recovery success and a Page-like shape flag for $U(t)$.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{r l r r r r r r r}
\toprule
$m$ & mode & seed & ok & page-ok & peak frac & $t$ & esc extra & ticks \\
\midrule
\input{sections/generated/bh_page_surrogate_rb_d_rows.tex}%
\end{tabular}
\caption{RB-D robustness audit for the record-level Page surrogate. Rows are generated by \texttt{scripts/exp\_bh\_page\_surrogate\_robustness\_audit.py}.}
\label{tab:bh_page_surrogate_rb_d}
\end{table}

\input{sections/generated/bh_page_surrogate_rb_d_summary.tex}%

\paragraph{Record-noise audit (interface robustness).}
\AuditTag In addition to RB-D robustness under bounded protocol and message variations, one can test operational
recoverability under external record noise (symbol corruption). The following audit table perturbs the non-vacuum
prefix of the observed record by bounded substitutions and reports exact recovery rates.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{r l l r r r r r}
\toprule
$m$ & mode & corr & $p$ & trials & ok rate & wrong rate & exc rate \\
\midrule
\input{sections/generated/bh_page_record_noise_audit_rows.tex}%
\end{tabular}
\caption{Record-noise audit for exact recovery from the external record. Rows are generated by \texttt{scripts/exp\_bh\_page\_surrogate\_record\_noise\_audit.py}.}
\label{tab:bh_page_record_noise_audit}
\end{table}

\input{sections/generated/bh_page_record_noise_audit_summary.tex}%

\paragraph{Finite-family error correction wrapper (optional; audit).}
\AuditTag The exact recovery map can be wrapped in a finite-family error-correcting layer at the record level.
As a minimal instance, we repeat each stable-label symbol $r$ times and decode by majority vote (repetition code),
with $r$ restricted to a small finite family. The following table reports how this improves operational recovery
rates under the same bounded record-noise model.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{r l l r r r r r r}
\toprule
$m$ & mode & corr & $p$ & $r$ & trials & ok rate & wrong rate & exc rate \\
\midrule
\input{sections/generated/bh_page_record_noise_ecc_audit_rows.tex}%
\end{tabular}
\caption{Record-noise audit with a repetition-code wrapper. Rows are generated by \texttt{scripts/exp\_bh\_page\_surrogate\_record\_noise\_ecc\_audit.py}.}
\label{tab:bh_page_record_noise_ecc_audit}
\end{table}

\input{sections/generated/bh_page_record_noise_ecc_audit_summary.tex}%

\IfFileExists{figures/bh_page_surrogate_mixed.png}{
\begin{figure}[t]
\centering
\includegraphics[width=0.82\textwidth]{figures/bh_page_surrogate_mixed.png}
\caption{Mixed single-stream Page surrogate: $U(t)$ over the non-vacuum portion of the record.}
\label{fig:bh_page_surrogate_mixed}
\end{figure}
}{}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{r r r r r}
\toprule
tick $t$ & payload emitted & recovery emitted & $U(t)$ [bits] & recovered fraction \\
\midrule
\input{sections/generated/bh_page_surrogate_mixed_curve_rows.tex}%
\end{tabular}
\caption{Sampled rows for the mixed single-stream Page surrogate. Rows are generated by
\texttt{scripts/exp\_bh\_page\_surrogate\_mixed\_stream.py}.}
\label{tab:bh_page_surrogate_mixed_rows}
\end{table}

\input{sections/generated/bh_page_surrogate_mixed_summary.tex}%

\paragraph{Legal absorption as a protocol subalphabet (BH-like specialization).}
\AuditTag To reduce external assumptions while keeping the record self-delimiting with negligible framing overhead,
one may restrict the infalling/absorbed microstates to a protocol-level \emph{legal absorption} subset.
The natural BH-like specialization in this program is:
\emph{cyclic/trap types are absorbable while boundary types are treated as distinguished exits},
consistent with the $m=6$ trap/exit channelization of Appendix~\ref{app:leakage_kernel}.
The corresponding audit sweep is recorded in BH4 (Appendix~\ref{app:bh_evaporation_leak_channel},
Table~\ref{tab:bh_absorption_mode_sweep_bh4}).

\subsection{PT toy certificate: coarse loss vs.\ full-record reversibility at \texorpdfstring{$m=6$}{m=6}}
\label{subsec:bh5_pt_toy_certificate}

\AuditTag The script \texttt{scripts/exp\_black\_hole\_queue\_equivalence.py} provides a minimal PT toy:
at fixed $m$, recording only $w\in X_m$ coarse-grains away fiber information, while recording a stable label
together with a fiber coordinate $(w,j)$ makes the emission record invertible (information is conserved in the
record). This is a discrete, protocol-native model of ``information is hidden in the record correlations/side
information''.

