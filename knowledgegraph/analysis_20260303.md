# KnowledgeGraph 实质分析（2026-03-03）

## 1. 构建结果（本轮）

- 源目录扫描：`1779` 个源文件变更（`sections` 1293，`scripts` 486）。
- 任务生成：`17401` 个入图任务。
  - `tex_knowledge_unit`：`16821`
  - 非 tex 任务：`580`
- 实际入图（append-only）：
  - payload atom：`17107`
  - sidecar 关系文件（`*.meta.json`）：`17107`

## 2. 图谱规模与结构

- 节点总数：`17107`
- 边总数（parents）：`9211`
- 平均入边：`0.5384`
- 根节点（无父依赖）：`8982`
- 扩展名分布：
  - `tex`: `16621`
  - `py`: `486`

类型分布（Top）：

- `tp-proof`: `6774`
- `tp-thm`: `2554`
- `tp-prop`: `1991`
- `tp-cor`: `1819`
- `tp-claim`: `1553`
- `tp-note`: `1121`
- `tp-method`: `495`
- `tp-def`: `445`
- `tp-conj`: `290`

## 3. 关系存储迁移状态

- 已切换为：**关系写入 sidecar JSON**（`<payload>.meta.json`）。
- payload 文件名不再承载 `from-*`，避免超长路径问题。
- 扫描器兼容旧格式（文件名带 `__from-*`）与新格式（sidecar）。

## 4. DAG 修复状态

已完成机制级修复并全量重建，当前结果：

- `kg_scan_atoms.py`：`error_count=0`，`warning_count=0`
- `kg_check_dag.py`：通过（无环）
- `kg_build_index.py --spec book_grg`：通过（可拓扑排序）

采用的修复策略（非手工逐点改边）：

1. 关系迁移到 sidecar JSON（`*.meta.json`），文件名不再承载 `from`，避免超长与解析脆弱性。
2. 入图时做父边类型约束（如 `def` 不依赖 `thm`，`note` 不依赖 `note`，`cor` 不依赖 `cor`）。
3. 入图时做增量反环检查：若新增 `child -> parent` 会成环，则自动丢弃该候选父边。

## 5. 结论

- 当前图谱已满足“知识 atom + append-only + DAG”三项核心约束。
- 已可以稳定进行结构分析、索引构建与后续 PDF 编译。
