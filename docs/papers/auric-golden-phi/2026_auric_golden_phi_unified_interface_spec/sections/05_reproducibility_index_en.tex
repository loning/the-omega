\section{Reproducibility Index (Minimal Entry Points)}

\subsection{Core Reproducible Assertions (Cross-Paper Universal)}

\begin{itemize}
  \item \textbf{Golden-Mean Cardinality:}
  For $X_m=\{w\in\{0,1\}^m:\text{no adjacent }11\}$,
  \[
  \card{X_m}=F_{m+2}.
  \]
  \item \textbf{Folding Compression:}
  From the sliding-window word perspective, $\Fold_m:\{0,1\}^m\twoheadrightarrow X_m$;
  from the integer-label perspective, $\Fold_m:Y_m\to X_m$ ($Y_m=\{0,\dots,2^m-1\}$).
  \item \textbf{Fiber Objectification:}
  \[
  \mathcal{F}_m(x)=\Fold_m^{-1}(x).
  \]
  \item \textbf{Information-Theoretic Time:}
  Under generating partition / ergodicity conditions, the exponential contraction rate of cylindric set posterior measure equals the KS entropy rate (SMB/KS template).
\end{itemize}

\subsection{Recommended Shortest Verification Path}

\begin{itemize}
  \item First perform full enumeration on the discrete kernel at $m=6$: verify that $2^6=64$ is folded to $F_8=21$, and double-check the fiber degeneracy spectrum.
  \item Then perform symbolization on irrational rotation (or torus translation): verify the Sturmian fingerprint with complexity $p(n)=n+1$.
  \item Finally estimate the cylindric set contraction rate under generating partition conditions: align its slope with the entropy rate estimate.
\end{itemize}

