\paragraph{Audit summary (trapping transition scan).} \AuditTag This scan runs the full-fusion interface in \texttt{FULL\_FUSION\_MODE=metrics} over a small grid of $(n,m,I_{\mathrm{obs}},c)$ settings and records the horizon occupancy $f_{\mathrm{hor}}$ together with delay proxies and the transition proxy $\Delta\tau=\tau_{\mathrm{trap}}-\tau_{\mathrm{free}}$. Rows are generated deterministically by \texttt{scripts/exp\_full\_fusion\_trapping\_transition\_scan.py}.