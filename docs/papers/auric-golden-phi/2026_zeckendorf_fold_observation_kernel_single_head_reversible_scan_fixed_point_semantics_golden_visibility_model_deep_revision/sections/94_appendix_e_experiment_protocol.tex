\section{附录 E：实验协议与指标（完整定义）}

\begin{itemize}
  \item \textbf{参数：}$L=1024$、$\delta=1$、步数 $N=2\times 10^5$。
  \item \textbf{初始化：}$i_0$ 均匀取自 $\ZZ/L\ZZ$，$h_0$ 均匀取自 $Y_6$，环场 $C_0$ 各格点独立均匀取自 $Y_6$。
  \item \textbf{轮函数：}$f_0,f_{\mathrm{fold}},f_{\mathrm{mix}}$ 如正文定义。
  \item \textbf{误差范数：}$\norm{e}_\infty,\norm{e}_1$ 如正文第 9 节。
  \item \textbf{总变差：}$\norm{\pi-u}_{\TV}=\frac12\sum_{h\in Y_6}\abs{\pi(h)-1/64}$。
  \item \textbf{图谱：}由 $(x_t,x_{t+1})$ 构造边支持集 $E$，计算 $\card{E}$ 与强连通分量分解。
\end{itemize}

