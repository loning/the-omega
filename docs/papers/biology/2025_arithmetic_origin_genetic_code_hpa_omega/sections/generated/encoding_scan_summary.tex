\paragraph{Encoding scan.}
There are $24$ bijective two-bit encodings. Under the standard stop set $\{\mathrm{UAA,UAG,UGA}\}$, the number of stop codons landing in the boundary sector has histogram: $\{0:16,\ 1:8,\ 2:0,\ 3:0\}$.
In particular, no encoding places all three stop codons inside the boundary sector, and the stronger RF boundary-index inclusion test (``all stop $N\in\{14,17,19,48,51,53\}$'') succeeds for 0 encodings.

\paragraph{Start--stop boundary homology.}
Exactly 1 encoding satisfies start--stop boundary homology, namely $A\mapsto 00,\ C\mapsto 01,\ G\mapsto 10,\ U\mapsto 11$.

\paragraph{Mutual information diagnostic.}
Under the uniform codon prior, the mutual information $I(\mathsf{Gen}(C);w_\mu(C))$ ranges from $2.545959$ to $2.639709$ bits across the $24$ encodings.
