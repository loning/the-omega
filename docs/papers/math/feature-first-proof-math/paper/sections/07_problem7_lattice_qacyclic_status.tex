\section{Problem 7: Uniform Lattices and $\mathbb Q$-Acyclic Universal Covers}

\textbf{Latest Reviewed Version:} Q7-R4 (under Opus review).

\textbf{Current Status.}
Full proof provided in \texttt{07\_problem7\_lattice\_qacyclic.tex}. The answer is \textbf{YES} (for $d=\dim(G/K)\ge 5$) via:
\begin{enumerate}
\item $\Gamma$ is a $d$-dimensional $\mathbb Q$-Poincar\'e duality group (LHS spectral sequence).
\item The orbifold $X/\Gamma$ is a compact $\mathbb Q$-homology manifold (transfer argument, $|\Gamma_x|=2^j$ invertible in $\mathbb Q$).
\item $\pi_1^{\mathrm{top}}(X/\Gamma)=\Gamma$ (codimension $\ge 2$ of singular set, since $K$ connected $\Rightarrow$ orientation-preserving $\Rightarrow$ even-multiplicity $-1$ eigenvalue).
\item Rational surgery + Farrell--Jones $\Rightarrow$ closed manifold $M$ with $\mathbb Q$-equivalence $M\to X/\Gamma$.
\item Spectral sequence comparison $\Rightarrow$ $\widetilde M$ is $\mathbb Q$-acyclic.
\end{enumerate}

\textbf{Tracking Rule.}
Each newly submitted user version is first appended in this section, then reviewed in \texttt{reviews/problem7\_review.md}.
