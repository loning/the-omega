\section{Problem 6: Existence of Large $\epsilon$-Light Sets}

\textbf{Latest Reviewed Version:} Q6-R2 (FAIL).

\textbf{Current Status.}
Still not closed globally. This version contains correct obstruction and correct structural lemmas, plus useful negative insight that the linearized condition is too strong to prove the target statement in general.

\textbf{Submission Record.}

\textbf{Q6-V2 (user submission).}
Main delivered claims:
\begin{itemize}
\item Sharp universal obstruction: any universal constant must satisfy $c\le 1/2$ (matching example).
\item Correct linearization lemma
\[
L_S\preceq \frac12\sum_{u\in S}L_u,
\]
and corollary
\[
\sum_{u\in S}L_u\preceq 2\varepsilon L \Rightarrow L_S\preceq \varepsilon L.
\]
\item Correct observation that this linearized sufficient condition is strictly stronger than the original target (already fails as a route on matchings for $\varepsilon<1/2$).
\item Explicitly states the remaining gap: no full universal existence proof (or counterexample family) yet.
\end{itemize}

\textbf{Review Outcome (Q6-R2).}
FAIL for original Q6 (main existence theorem unresolved), with verified correctness of all proved partial components.

\textbf{Tracking Rule.}
Each newly submitted user version is first appended in this section, then reviewed in `reviews/problem6_review.md`.
