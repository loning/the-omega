\paragraph{Audit summary (wormhole parameter sweep on full fusion).} \AuditTag We run the full-fusion experiment in a metrics-only mode over a bounded grid of wormhole parameters $(\varepsilon_{\mathrm{ptr}}, r_{\mathrm{ptr}}, \lambda_{\mathrm{jump}})$ and report deltas relative to the wormhole-off baseline. Columns include $E_{\mathrm{wh}}$ (explicit shortcut cost ledger), delay gains (including the separate wormhole-induced component $\tau_{\mathrm{wh}}$), a pointer-jump exit rate (wormhole-assisted leakage), and measurement proxies $(V,D)$ with $V^2+D^2\le 1$. Grid: eps=4, radius=3, jump=3; best=(eps=0.200, radius=2.50, jump_rate0=0.100), E_wh=1.856326.