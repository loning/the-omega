\begin{proposition}[Finite-field embedding fingerprint: cubic characteristic of second-order collision kernel manifests in higher-order moment recursions (auditable)]\label{prop:pom-charpoly-modp-a2-embedding}
Denote by \(\chi_{A_2}(\lambda)\) the characteristic cubic of the second-order collision kernel \(A_2\) (Proposition \ref{prop:pom-collision-det})
\[
\chi_{A_2}(\lambda):=\lambda^3-2\lambda^2-2\lambda+2.
\]
On the minimal integer-coefficient recursion characteristic polynomial \(P_q(\lambda)\) within the resonance window (Theorem \ref{thm:pom-moment-resonance}), there exists an auditable finite-field embedding phenomenon:
\[
\boxed{\;
\chi_{A_2}(\lambda)\mid P_{14}(\lambda)\ \ (\mathrm{mod}\ 7),
\qquad
\chi_{A_2}(\lambda)\mid P_{12}(\lambda)\ \ (\mathrm{mod}\ 11).\;}
\]
The corresponding explicit factorizations are found in Table \ref{tab:fold_collision_charpoly_mod7_selection_q13_15} and Table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}.
The above finite-field factorizations are generated directly from the \(P_q\) coefficients listed in Theorem \ref{thm:pom-moment-resonance} by script \texttt{scripts/exp\_fold\_collision\_charpoly\_modp\_factorization.py} through reduction and factorization, requiring no new DP data.
\end{proposition}

\begin{remark}[\texorpdfstring{$(A_2)$}{(A2)} cubic skeleton's cross-sectional identity: resonance window finite-field embedding and near-\texorpdfstring{$1$}{1} pole slow mode share unified coefficient template source]\label{rem:pom-a2-cubic-skeleton-crosschapter}
The conclusion of Proposition \ref{prop:pom-charpoly-modp-a2-embedding} is not merely "some cubic factor appears": it \emph{enforces} the appearance of the specific cubic skeleton \(\chi_{A_2}(\lambda)\) on mod \(7\) and mod \(11\) reduction slices.
On the other hand, in the near-\(1\) pole analysis of the real-input kernel's pure collision, the four-state pure-collision twist matrix \(W(u)=D(u)(F\otimes F)\) (also denoted \(A_\xi(u)\)) satisfies
\[
\det(\lambda I-W(u))=(\lambda+1)\bigl(\lambda^3-2\lambda^2-(u+1)\lambda+u\bigr)
\]
(Proposition \ref{prop:real-input-40-collision-pressure}; see also proof of Proposition \ref{prop:arity-collision-quadratic-closed}), yielding a cubic slow-mode kernel at \(u=1\)
\[
\chi_{\xi,1}(\lambda):=\lambda^3-2\lambda^2-2\lambda+1.
\]
Note that \(\chi_{\xi,1}\) and \(\chi_{A_2}\) share the same quadratic/linear coefficient skeleton \(-2\lambda^2-2\lambda\), differing only in the constant term; thus both can be viewed as two realizations of the same "\(A_2\)-type cubic template" under different interfaces/normalizations.
In this sense, the finite-field spectral fingerprint of the resonance window (\(\chi_{A_2}\mid P_q\)) and the slow-mode cubic kernel of the near-\(1\) pole (\(\chi_{\xi,1}\)) share algebraically the same minimal cubic structural skeleton, thereby providing a cross-sectional auditable bridge interface: the arithmetic shadow of resonance recursions and the slow mode of the near-\(1\) pole are not unrelated "coincidental cubics," but rather two manifestations of the same cubic skeleton template.
\end{remark}

\begin{corollary}[Representation-theoretic meaning of embedding flag: \texorpdfstring{$A_2$}{A2} three-dimensional invariant subdynamics in mod \texorpdfstring{$p$}{p} recursion state space]\label{cor:pom-charpoly-modp-a2-subdynamics}
Let \(p\) be prime, and let \(d=d_q\) be the minimal integer-coefficient recursion order of \(S_q\) (Theorem \ref{thm:pom-moment-resonance}).
Reducing the recursion over \(\mathbb{F}_p\) yields a \(d\)-dimensional state space \(V_q(p)\cong\mathbb{F}_p^{d}\) with forward-shift operator \(E\) (companion action), whose characteristic polynomial is \(P_q(\lambda)\ (\mathrm{mod}\ p)\).
If
\[
\chi_{A_2}(\lambda)\mid P_q(\lambda)\qquad(\mathrm{in}\ \mathbb{F}_p[\lambda]),
\]
and \(\chi_{A_2}\) is irreducible over \(\mathbb{F}_p[\lambda]\) (e.g., \(p=7,11\)),
then \(V_q(p)\) admits a three-dimensional \(E\)-invariant subspace \(W\subset V_q(p)\) such that \(E|_{W}\) has characteristic polynomial exactly \(\chi_{A_2}\).
Equivalently, in the mod \(p\) sense, high-order recursion dynamics contains an embedded subdynamics isomorphic to the second-order collision kernel \(A_2\) (an "\(A_2\)-shadow" subrepresentation).
\par
In particular, by Proposition \ref{prop:pom-charpoly-modp-a2-embedding}, at slice \((q,p)=(14,7)\) this embedding flag exists; hence the mod \(7\) recursion for \(q=14\) contains a strict three-dimensional \(A_2\)-shadow channel.
\end{corollary}
\begin{proof}
Over \(\mathbb{F}_p\), \((V_q(p),E)\) is a finite-dimensional \(\mathbb{F}_p[\lambda]\)-module (letting \(\lambda\) act as \(E\)).
By the structure theorem, \(V_q(p)\) decomposes as a direct sum of primary components; if the irreducible polynomial \(\chi_{A_2}\) divides the characteristic polynomial, then the \(\chi_{A_2}\)-primary component is nonzero.
When \(\chi_{A_2}\) is irreducible, \(\mathbb{F}_p[\lambda]/(\chi_{A_2})\) is a three-dimensional vector space with \(\lambda\) action given by the companion block of \(\chi_{A_2}\); thus there exists a three-dimensional cyclic submodule \(W\subseteq V_q(p)\) isomorphic to it, satisfying that \(E|_W\) has characteristic polynomial \(\chi_{A_2}\).
\end{proof}

\begin{remark}[\texorpdfstring{$A_2$}{A2} embedding points and discriminant vanishing: local spectral ramification fingerprint over finite fields]\label{rem:pom-a2-embedding-local-ramification}
In the two embedding slices \((q,p)=(14,7)\), \((12,11)\) of Proposition \ref{prop:pom-charpoly-modp-a2-embedding}, Tables \ref{tab:fold_collision_charpoly_mod7_selection_q13_15} and \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15} both show \(\mathrm{repeat?}=1\), meaning \(P_q(\lambda)\) is not square-free over \(\mathbb{F}_p[\lambda]\) after removing finite transient factor \(\lambda^{a_0}\).
Equivalently, if we set \(\widetilde P_q(\lambda):=P_q(\lambda)/\lambda^{a_0}\), then
\[
\gcd\!\bigl(\widetilde P_q(\lambda),\partial_\lambda \widetilde P_q(\lambda)\bigr)\neq 1\quad(\mathrm{in}\ \mathbb{F}_p[\lambda]),
\]
so the discriminant of \(\widetilde P_q\) vanishes mod \(p\), and this slice can be identified as a ramification point of the spectral covering over the local field.
It should be emphasized that \(\mathrm{repeat?}=1\) does not require \(\chi_{A_2}\) itself to appear as a repeated factor; in verified cases, the mod \(7\) repeated factor at \(q=14\) is \((\lambda^2+1)^2\), while the mod \(11\) repeated factor at \(q=12\) is \((\lambda^2\pm 4\lambda-3)^2\) (see factorizations in respective tables).
\par
Therefore, these two embedding events present as a juxtaposed fingerprint in the finite-field dynamical sense: on one hand, the state space contains a strict three-dimensional \(A_2\)-shadow subrepresentation (Corollary \ref{cor:pom-charpoly-modp-a2-subdynamics}); on the other hand, in some nonzero phase sector, a Jordan modulation channel appears, forced by ramification/repeated-root nature.
This juxtaposition elevates the embedding flag from "same factor appears" to a local spectral ramification point directly identifiable by discriminant vanishing and non-square-freeness, thus forming a purely finite-field, auditable local diagnostic chain.
\end{remark}

\begin{proposition}[Finite-field factor extraction law: \texorpdfstring{$\chi\mid P_q$}{chi|Pq} forces existence of constructible \texorpdfstring{$\chi$}{chi}-subrecursion]\label{prop:pom-modp-factor-extraction}
Let \(p\) be prime, and suppose over \(\mathbb{F}_p[\lambda]\) we have factorization
\[
P_q(\lambda)\ \equiv\ \chi(\lambda)\,Q(\lambda)\qquad(\chi\ \text{monic}).
\]
For any solution sequence satisfying \(P_q(E)S_q\equiv 0\pmod p\), define the derived sequence
\[
S_q^{(\chi)}:=Q(E)\,S_q\qquad(\mathrm{in}\ \mathbb{F}_p^{\NN}).
\]
Then within the effective recursion interval we must have
\[
\boxed{\ \chi(E)\,S_q^{(\chi)}\equiv 0\pmod p\ }.
\]
\end{proposition}
\begin{proof}
By definition
\(
\chi(E)\,S_q^{(\chi)}=\chi(E)Q(E)S_q=P_q(E)S_q\equiv 0
\).
\end{proof}

\begin{remark}[Constructive elevation of embedding flag: explicit extraction of \texorpdfstring{$A_2$}{A2}-subrecursion]\label{rem:pom-a2-subrecurrence-extraction}
Take \(\chi=\chi_{A_2}\). By Proposition \ref{prop:pom-charpoly-modp-a2-embedding},
at slices \((q,p)=(12,11)\) and \((14,7)\) we have \(\chi_{A_2}\mid P_{12}\ (\mathrm{mod}\ 11)\), \(\chi_{A_2}\mid P_{14}\ (\mathrm{mod}\ 7)\) respectively (embedding columns in Tables \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15} and \ref{tab:fold_collision_charpoly_mod7_selection_q13_15}).
Therefore on the respective finite fields we can take
\[
S_{12}^{(A_2)}:=\Bigl(\frac{P_{12}}{\chi_{A_2}}\Bigr)(E)\,S_{12}\quad(\mathrm{mod}\ 11),
\qquad
S_{14}^{(A_2)}:=\Bigl(\frac{P_{14}}{\chi_{A_2}}\Bigr)(E)\,S_{14}\quad(\mathrm{mod}\ 7),
\]
and by Proposition \ref{prop:pom-modp-factor-extraction} obtain explicit annihilation laws
\[
\chi_{A_2}(E)\,S_{12}^{(A_2)}\equiv 0\pmod{11},
\qquad
\chi_{A_2}(E)\,S_{14}^{(A_2)}\equiv 0\pmod{7}.
\]
Thus the embedding column provides not only a passive fingerprint, but can be elevated to an active construction interface: it forces existence of an \(A_2\)-shadow subdynamics extractable from \(S_q\) by finite shift-linear operators.
\end{remark}

\begin{remark}[Mod \texorpdfstring{$7$}{7} selection law: at-a-glance boundary between in-field short period and extension long period (auditable)]\label{rem:pom-charpoly-mod7-selection}
From Table \ref{tab:fold_collision_charpoly_mod7_selection_q13_15}'s \(\mathbb{F}_7[\lambda]\) factorization:
\begin{itemize}
  \item For \(q=13\), all nonzero eigenphases fall within \(\mathbb{F}_7^\ast\) (max irreducible degree \(1\)), with repeated roots (e.g., \((\lambda-2)^3\)).
  Hence after aligning finite transients (from \(\lambda^2\)), the mod \(7\) recursion dynamics is forced into an auditable class of "short period + polynomial modulation"; the table gives rough upper bound
  \[
  \mathrm{Per}_7(q=13)\ \mid\ 42.
  \]
  \item For \(q=15\), irreducible quadratic and cubic factors appear (max irreducible degree \(3\)), so phases must enter extensions \(\mathbb{F}_{7^2}\) and \(\mathbb{F}_{7^3}\);
  simultaneously, square factor \((\lambda\pm 1)^2\) forces generalized eigenvalue modulation.
  Hence under the same rough bound caliber, the table reads unified upper bound
  \[
  \mathrm{Per}_7(q=15)\ \mid\ 19152.
  \]
\end{itemize}
The above conclusions are read entirely from \(P_q(\lambda)\)'s finite-field factorization, independent of new DP computation; they split odd-order \(q\) within the same resonance window into two directly auditable dynamical complexity classes mod \(7\).
\end{remark}

\begin{remark}[Square factor \texorpdfstring{$\Rightarrow$}{=>} polynomial modulation: mod \texorpdfstring{$11$}{11} recursion audit handle]\label{rem:pom-charpoly-mod11-modulation}
Table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15} shows: in mod \(11\) slices, \(q=12\) exhibits square of quadratic factor \((\lambda^2\pm 4\lambda-3)^2\), while \(q=13,15\) show square of linear factors \((\lambda\pm 1)^2\) (and all three have \(\mathrm{repeat?}=1\)).
This means the corresponding finite-field recursion operator has nontrivial Jordan blocks under that prime shadow, so its solution space contains "polynomial-modulated exponential phase" (generalized eigenmode) components.
\par
Furthermore, the \emph{irreducible degree} of these square factors strictly splits in this slice: for \(q=12\), the square factor passes through an irreducible quadratic (\(\deg f=2\)), so any minimal annihilator that kills that Jordan modulation component contains at least \(f(E)^2\) with \(\deg f=2\) in \(\mathbb{F}_{11}[E]\), giving minimal polynomial degree lower bound \(4\);
while for \(q=13,15\), square factors fall on linear phases (\(\deg f=1\)), corresponding lower bound only \(2\).
Hence although the rough period upper bound in this slice is uniformly the table value, the irreducible order of Jordan modulation can still be strictly distinguished structurally by finite-field factorization; this distinction provides a phase-diagram refinement handle without complex spectral solving.
\par
Thus a mod \(11\) auditable consistency check is: after reducing recursion to \(\mathbb{F}_{11}\), if observed sequence behavior is hypothesized as "pure period given only by semisimple phases" (i.e., not requiring \(\times 11\) modulation factor), one must further verify whether that specific sequence's minimal annihilating polynomial over \(\mathbb{F}_{11}\) has already removed square factors (e.g., audit by refitting minimal recursion/Hankel rank over finite field once).
\end{remark}

\begin{remark}[Mod \texorpdfstring{$13$}{13} low-complexity fingerprint: \texorpdfstring{$q=13$}{q=13} pure period upper bound]\label{rem:pom-charpoly-mod13-small-period}
Table \ref{tab:fold_collision_charpoly_mod13_fingerprint_q13} shows: mod \(13\), \(P_{13}(\lambda)\)'s nonzero irreducible factor degrees are at most \(2\) (max irreducible degree \(2\)), and besides transient factor \(\lambda^2\), no square factors (\(\mathrm{repeat?}=0\)).
Hence after aligning finite transients, mod \(13\) recursion dynamics is of pure periodic class, whose rough period upper bound can be taken
\[
\mathrm{Per}_{13}(q=13)\ \mid\ 13^2-1=168,
\]
consistent with table value.
\end{remark}

\begin{remark}[Cross-prime audit method: automatic generation of mod \texorpdfstring{$p$}{p} recursion fingerprint from \texorpdfstring{$P_q(\lambda)\bmod p$}{Pq mod p}]\label{rem:pom-charpoly-modp-audit-method}
For any given \((q,p)\), factor \(P_q(\lambda)\) over \(\mathbb{F}_p[\lambda]\) as
\(
P_q(\lambda)\equiv \prod_i f_i(\lambda)^{e_i}
\).
This factorization directly yields a set of auditable fingerprints:
\begin{itemize}
  \item Whether square factors appear (some \(e_i\ge 2\) with \(f_i\neq \lambda\)): corresponding to existence of "generalized phase/polynomial modulation" channels;
  \item Maximum irreducible degree \(\max_i \deg f_i\): gives minimal extension field order that must be entered, thus phase order upper bound factor \((p^{\deg f_i}-1)\);
  \item If all nonzero factors are linear: phase set falls entirely within \(\mathbb{F}_p^\ast\), can directly give minimal period upper bound (times possible modulation factor \(p\)).
\end{itemize}
Compared to audits using only recursion order \(d_q\), this "finite-field factorization fingerprint" simultaneously provides interpretation (where extension modes appear, where Jordan modulation occurs) and computable upper bounds (e.g., period bound columns in Tables \ref{tab:fold_collision_charpoly_mod7_selection_q13_15}--\ref{tab:fold_collision_charpoly_mod13_fingerprint_q13}).
\par
\textbf{Unified structural law for period upper bounds (computable criterion for Jordan hair).}
Write factorization as
\[
P_q(\lambda)\equiv \lambda^{a_0}\prod_{j} f_j(\lambda)^{e_j}\qquad(\mathrm{in}\ \mathbb{F}_p[\lambda]),
\]
where each \(f_j\) is irreducible and \(f_j\neq \lambda\).
Denote \(d_j:=\deg f_j\), and set
\[
\varepsilon_{q,p}:=\ind\{\exists j:\ e_j\ge 2\}\in\{0,1\}.
\]
Then in the tables' \emph{period bound} column, we adopt the unified rough upper bound
\[
\mathrm{PerBound}_p(q):=
p^{\varepsilon_{q,p}}\cdot \mathrm{lcm}_{j}\bigl(p^{d_j}-1\bigr),
\]
where \(\lambda^{a_0}\) affects only finite transients and does not enter the final period upper bound.
This structural law reflects two mutually independent finite-field mechanisms: irreducible degrees \(d_j\) determine minimal extension field order and viable period factor \(p^{d_j}-1\) that phases must enter; while \(\varepsilon_{q,p}=1\) corresponds to non-square-free forced Jordan/generalized eigenvector contribution, thus extra factor \(p\) appears in final period upper bound.
\par
Therefore, "whether period upper bound contains extra factor \(p\)" is equivalent to table's \(\mathrm{repeat?}\) flag, and this non-semisimplicity criterion requires no full factorization: checking
\(\gcd(\widetilde P_q,\partial_\lambda\widetilde P_q)\neq 1\) for \(\widetilde P_q(\lambda):=P_q(\lambda)/\lambda^{a_0}\) directly reads out \(\varepsilon_{q,p}\).
\end{remark}

\begin{remark}[Irreducible degree spectrum inversion from period upper bound: bypassing polynomial factorization]\label{rem:pom-charpoly-modp-degree-inversion}
Using notation from Remark \ref{rem:pom-charpoly-modp-audit-method}, set
\(
T_{q,p}:=\mathrm{PerBound}_p(q)=p^{\varepsilon_{q,p}}\cdot \mathrm{lcm}_j(p^{d_j}-1)
\).
Then as long as integer factorization of \(T_{q,p}\) (and \(\varepsilon_{q,p}\)) is obtained, constraints on degree spectrum \(\{d_j\}\) can be extracted without factoring \(P_q(\lambda)\ (\mathrm{mod}\ p)\):
if \(\ell\) is any prime and
\(
\ell\mid (T_{q,p}/p^{\varepsilon_{q,p}})
\),
then there exists some \(j\) such that
\(
\ell\mid (p^{d_j}-1)
\).
Hence \(d_j\) must be multiple of \(\mathrm{ord}_\ell(p)\), where
\[
\mathrm{ord}_\ell(p):=\min\{d\ge 1:\ p^d\equiv 1\ (\mathrm{mod}\ \ell)\}.
\]
Thus each \(\ell\) generates a "must-cover" degree constraint; uniform coverage over all \(\ell\) can be expressed as a finite minimal covering problem: from all candidate degree sets (generated by \(\mathrm{ord}_\ell(p)\) and multiples), select a degree multiset that simultaneously explains appearance of all \(\ell\).
\par
When the upper bound law of Remark \ref{rem:pom-charpoly-modp-audit-method} is further lifted to tight equality at given slice, the above covering problem degenerates to an executable "period bound \(\Rightarrow\) degree spectrum" inversion protocol, thereby providing an audit channel for local factorization structure under mod-prime prism without polynomial factorization.
\end{remark}

\begin{remark}[Discriminant prime support fingerprint: repeat? \texorpdfstring{$\Rightarrow$}{=>} \texorpdfstring{$p$}{p}-divisibility (auditable)]\label{rem:pom-discriminant-prime-support}
For \(P_q(\lambda)\in\ZZ[\lambda]\), let \(a_0\) be exponent of zero-eigenvalue transient factor \(\lambda^{a_0}\), and write
\(
P_q(\lambda)=\lambda^{a_0}P_q^\ast(\lambda)
\)
with \(P_q^\ast(0)\ne 0\).
Define discriminant of nonzero spectral part
\[
\mathrm{Disc}^\ast(P_q):=\mathrm{Disc}\!\bigl(P_q^\ast\bigr)\in\ZZ.
\]
If some prime \(p\) makes \(P_q^\ast(\lambda)\ (\mathrm{mod}\ p)\) non-square-free over \(\overline{\mathbb{F}}_p\) (equivalent to \(\gcd(P_q^\ast,(P_q^\ast)')\not\equiv 1\ (\mathrm{mod}\ p)\)), then \(p\mid \mathrm{Disc}^\ast(P_q)\).
Hence from finite-field factorization fingerprint's non-squarefree flag (repeat?) we can directly read forced divisibility conclusions:
\begin{itemize}
  \item By Table \ref{tab:fold_collision_charpoly_mod2_shadow_q9_17}, in resonance window \(q=9,\dots,17\) have
  \(P_q(\lambda)\equiv \lambda^{a_q}(\lambda+1)^{b_q}\ (\mathrm{mod}\ 2)\) with \(b_q\ge 4\), so \(2\mid \mathrm{Disc}^\ast(P_q)\) holds for full window;
  \item By Table \ref{tab:fold_collision_charpoly_mod11_fingerprint_q12_15}, in mod \(11\) verified slices \(q\in\{12,13,15\}\) all satisfy repeat?\(=1\), so \(11\mid \mathrm{Disc}^\ast(P_q)\);
  \item By Table \ref{tab:fold_collision_charpoly_mod7_selection_q13_15}, in mod \(7\) verified slices \(q\in\{13,14,15\}\) all satisfy repeat?\(=1\), so \(7\mid \mathrm{Disc}^\ast(P_q)\).
\end{itemize}
Hence \(\{2,7,11\}\) form a set of "discriminant prime support" hard fingerprints forced by repeated-root nature within this resonance window, serving as invariant reference for cross-version regression audit.
\end{remark}
\begin{remark}[Period collapse under composite modulus: final period upper bound for \texorpdfstring{$q=13$}{q=13} mod \texorpdfstring{$182$}{182}]\label{rem:pom-mod182-period-collapse-q13}
Take \(q=13\) and \(M:=2\cdot 7\cdot 13=182\).
By Table \ref{tab:fold_collision_charpoly_mod2_shadow_q9_17}'s mod \(2\) shadow and Remark \ref{rem:pom-charpoly-mod2-shadow}'s difference order budget, after aligning finite transients have final period upper bound \(T_{13}\mid 8\);
by Tables \ref{tab:fold_collision_charpoly_mod7_selection_q13_15} and \ref{tab:fold_collision_charpoly_mod13_fingerprint_q13} obtain
\(
\mathrm{Per}_7(q=13)\mid 42
\)
and
\(
\mathrm{Per}_{13}(q=13)\mid 168
\).
After aligning a common transient (e.g., apply \(E^{3}\)), these three period upper bounds can be transferred to mod \(M\) via Chinese remainder structure:
\[
\boxed{\ \mathrm{Per}_{182}(q=13)\ \mid\ \mathrm{lcm}(8,42,168)=168\ }.
\]
This example shows: although original minimal recursion order is \(d_{13}=11\), its dynamics mod composite modulus \(182\) can close within short period \(168\), thus providing a directly citable "cross-prime audit sieve" compression instance.
\end{remark}
\input{sections/generated/tab_fold_collision_charpoly_mod7_selection_q13_15}
\input{sections/generated/tab_fold_collision_charpoly_mod11_fingerprint_q12_15}
\input{sections/generated/tab_fold_collision_charpoly_mod13_fingerprint_q13}




