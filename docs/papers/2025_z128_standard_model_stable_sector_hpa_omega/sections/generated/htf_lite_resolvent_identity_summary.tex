\paragraph{HTF-lite resolvent identity audit summary.} \AuditTag For the irrational rotation scan $x_t=x_0+t\alpha$ and the Fourier kernel $f_m(x)=\exp(2\pi i m x)$, the Abel-regularized trace admits an explicit resolvent-mode form $S_m(r)=\sum_{t\ge 0} r^t f_m(x_t)=\exp(2\pi i m x_0)/(1-r\exp(2\pi i m\alpha))$ on $|r|<1$. Table~\ref{tab:htf_lite_resolvent_identity_audit} verifies this identity numerically against a long finite sum with an explicit truncation tail bound. The associated mode eigenvalue has modulus $|\lambda|=1$, so the pole location lies on $|r|=1$ (boundary). Table~\ref{tab:htf_lite_resolvent_pole_audit} shows the pole radius under a hypothetical amplitude shift $\lambda\mapsto \exp(\delta)\lambda$; for $\delta>0$ the pole radius becomes $\exp(-\delta)<1$, illustrating the same interior-pole obstruction motif used in the pole-barrier template (Appendix~\ref{app:trace_pole_barrier_template}).