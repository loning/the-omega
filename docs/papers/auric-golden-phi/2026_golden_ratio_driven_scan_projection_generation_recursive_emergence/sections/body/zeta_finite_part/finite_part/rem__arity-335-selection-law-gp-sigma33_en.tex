\begin{remark}[Selection-Law Discriminant \texorpdfstring{$g(p)$}{g(p)}: Worst-Direction Quadratic-Law Constant Given Solely by \texorpdfstring{$\Sigma_{33}$}{Sigma33}]\label{rem:arity-335-selection-law-gp}
Define the dimensionless discriminant
\[
g(p):=\left(1-\frac{\rho_{3,3,p}}{\lambda}\right)p^2.
\]
In the quadratic approximation regime of Remark \ref{prop:arity-335-worst-direction-cov}, take the minimal nontrivial angle in the third axis
\(
t=(0,0,2\pi/p)
\), and let \(\Sigma=\nabla^2P(0)\).
By the small-angle expansion
\(
\log\bigl(\rho(M(e^{\mathrm{i}t}))/\lambda\bigr)=-\tfrac12\,t^\top\Sigma t+O(|t|^3)
\)
we obtain
\[
1-\frac{\rho_{3,3,p}}{\lambda}
=\frac12\,\Sigma_{33}\left(\frac{2\pi}{p}\right)^2+o(p^{-2}),
\qquad\Longrightarrow\qquad
\lim_{p\to\infty}g(p)=2\pi^2\,\Sigma_{33}.
\]
Thus the "near-constancy" of \(g(p)\) is no longer just an empirical picture, but a falsifiable limiting law determined by the single matrix element \(\Sigma_{33}\) of the Fisher quadratic form; any significant deviation at a single prime (e.g., \(p=5\)) can be rigorously interpreted as: the system has left the quadratic-approximation-dominated regime and entered a higher-order resonance- or cohomological-obstruction-dominated regime.
\par
On the other hand, if the "near-coboundary generic small-angle perturbation" mechanism holds, then \(g(p)\) should remain approximately constant (with only slow drift) as \(p\) varies; if the "\(p=5\) special resonance" mechanism holds, then \(g(5)\) will significantly deviate from \(g(p)\) at other primes.
\par
The spectral scan for \(p\in\{5,7,11,13,17,19,23,29,31\}\) (computing only the spectral radius, without relying on the constant tensor) is generated by the script
\texttt{scripts/exp\_arity\_335\_n2\_selection\_law\_primes.py}
and output as \verb|\input|-able table fragments:
\input{sections/generated/tab_real_input_40_arity_335_n2_selection_law_primes}
where the character index \(j\) achieving the worst spectral radius is also provided (to check "whether the worst twist is still a pure collision twist \((0,0,\pm1)\)").
From the table one can see: within this scan range \(g(p)\) exhibits slow drift and converges toward a constant level; equivalently, the quadratic-law constant
\[
\kappa_p:=\frac{1-\rho_{3,3,p}/\lambda}{(2\pi/p)^2}
\]
monotonically approaches its limit \(\kappa_\infty=\sigma_\xi^2/2\) as \(p\) increases (Remark \ref{prop:arity-335-kappa-variance}).
Meanwhile, the worst character is always \((0,0,1)\) and \((0,0,-1)\) (in the sense of modulo \(p\)), i.e., the slow mode is stably produced by pure collision twists, consistent with the "near-coboundary small-angle perturbation" mechanism.
\end{remark}
