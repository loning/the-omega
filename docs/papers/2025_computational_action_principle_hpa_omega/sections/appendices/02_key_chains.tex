\section{Key derivation chains (summary)}
\label{app:chains}

\subsection{The \texorpdfstring{$\Omega$}{Omega} action and field equations (minimal sector)}

The minimal CAP sector is gravity plus Fisher amplitude plus matter:
\begin{equation}
  S_{\Omega}
  =\int \dd^{4}x\,\sqrt{-g}\,
  \left[
    \frac{R-2\Lambda}{16\pi G}
    -\lambda_{F}\, g^{\mu\nu}(\nabla_{\mu}\chi)(\nabla_{\nu}\chi)
    -V(\chi^{2})
    +\mathcal{L}_{m}
  \right].
\end{equation}
Metric variation yields
\begin{equation}
  G_{\mu\nu}+\Lambda g_{\mu\nu}=8\pi G\left(T_{\mu\nu}^{(m)}+T_{\mu\nu}^{\mathrm{info}}\right),
  \qquad
  T^{\mathrm{info}}_{\mu\nu}
  = 2\lambda_{F}\left(\nabla_{\mu}\chi\nabla_{\nu}\chi-\frac12 g_{\mu\nu}(\nabla\chi)^2\right)
  -g_{\mu\nu}V(\chi^2).
\end{equation}
Variation in $\chi$ yields
\begin{equation}
  2\lambda_{F}\square\chi-\frac{\dd V}{\dd\chi}=0,
  \qquad
  \frac{\dd V}{\dd\chi}=2\chi\frac{\dd V}{\dd\varrho},\quad \varrho=\chi^2.
\end{equation}

\subsection{Second-order covariant closure \texorpdfstring{$\Rightarrow$}{=>} Einstein uniqueness}

If the macroscopic continuum limit is local, diffeomorphism invariant, and second-order in the metric, then the divergence-free symmetric rank-2 tensor on the left-hand side is uniquely $G_{\mu\nu}+\Lambda g_{\mu\nu}$ (up to coupling), so the gravitational skeleton must be Einsteinian \cite{Lovelock1971}.

\subsection{Discrepancy source \texorpdfstring{$\Rightarrow$}{=>} Poisson phase potential \texorpdfstring{$\Rightarrow$}{=>} Newton limit}

Theorem~\ref{thm:ostrowski_dk_cap} provides a finite-depth quantitative bound on mismatch accumulation $E_N$ in terms of continued-fraction data. In CAP, a regulated continuum limit of the corresponding mismatch is packaged into a coarse-grained density $\sigma$, and one assumes it sources a phase potential:
\begin{equation}
  \Delta\Phi = 4\pi\kappa_{\Phi}\sigma,
  \qquad
  \mathbf{P}_{\Phi}=-\nabla\Phi.
\end{equation}
For an isolated defect with quantized flux, spherical symmetry enforces the exterior harmonic solution $\Phi=-M/r$, hence $\mathbf{P}_{\Phi}=-M\hat{\mathbf{r}}/r^2$. Appendix~\ref{app:newtonian_limit} explains why the Poisson operator is the correct weak-field closure (Newtonian limit of GR) and how the $\Omega$ action sources it through an effective energy density.


