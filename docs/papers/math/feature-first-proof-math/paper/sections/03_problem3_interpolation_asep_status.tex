\section{Problem 3: Interpolation ASEP/Macdonald Stationary Law}

\textbf{Latest Reviewed Version:} Q3-R6 (FAIL).

\textbf{Current Status.}
The submitted version closes a \emph{conditional} statement (assuming nonnegative normalized starred weights), but does not close the original unconditional Q3.

\textbf{Submission Record.}

\textbf{Q3-V6 (user submission).}
Claimed theorem: assuming
\[
Z=P^*_{\lambda}(x;1,t)>0,\qquad w(\mu)=F^*_{\mu}(x;1,t)\ge 0\ \forall\mu\in S_n(\lambda),
\]
and
\[
\pi(\mu)=\frac{w(\mu)}{Z},
\]
there exists a Markov chain with stationary distribution $\pi$ (constructed by a lazy adjacent-swap Metropolis--Hastings kernel).

Core structure in the submission:
\begin{itemize}
\item Uses BDW identity $P^*_{\lambda}=\sum_{\mu\in S_n(\lambda)}F^*_{\mu}$ at $q=1$ for normalization.
\item Defines symmetric proposal kernel via adjacent transpositions.
\item Applies MH acceptance ratio $\min(1,w(\nu)/w(\mu))$ on positive-weight states.
\item Treats zero-weight states separately and proves detailed balance casewise.
\end{itemize}

\textbf{Review Outcome (Q3-R6).}
For the original problem statement, still \textbf{FAIL}: positivity assumptions are added but not derived from the problem data/literature bridge.

\textbf{Tracking Rule.}
Each newly submitted user version is first appended in this section, then reviewed in `reviews/problem3_review.md`.
