\paragraph{Audit summary (protocol horizon illustration).} \AuditTag Rows are a deterministic illustration only. Comparison family: budgets $I_{\mathrm{obs}}\in\{64, 1024, 1000000\}$ (bits) and objects mapped to fixed protocol resolutions. The verdict uses a declared margin rule: \texttt{black-hole-like} iff $I_{\mathrm{prot}}(m,n)\ge 16\,I_{\mathrm{obs}}$, \texttt{resolvable} iff $I_{\mathrm{prot}}(m,n)\le I_{\mathrm{obs}}/2$, otherwise \texttt{marginal}.