\paragraph{Multi-$k$ stability (k=3,5,10,20).}
Stop-class differences in $\overline{U}_{\mathrm{before/after}}$ across window sizes:
Before-window: effects vary with $k$.
After-window: effects vary with $k$ (some pairs lose significance).
