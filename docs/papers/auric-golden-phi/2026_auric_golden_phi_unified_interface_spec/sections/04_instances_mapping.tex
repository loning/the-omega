\section{分篇论文的接口映射（实例化位置）}

本节把每篇论文定位到统一接口的哪些模块/模板上。标注只给“接口位置”，不复述各自证明细节。

\subsection{动力系统与诱导重整化线}

\begin{itemize}
  \item \texttt{2026\_torus\_translation\_window\_induced\_renormalization\_.../}
  \begin{itemize}
    \item 主要覆盖：模板 T1（返回时间谱/两值退化）、并扩展到 Möbius 更新、二次无理数周期点与高维闭包维数。
    \item 作为其它论文的“动力系统底座”：提供可复用的算术—重整化更新口径。
  \end{itemize}
\end{itemize}

\subsection{读出—稳定化—纤维（离散核）线}

\begin{itemize}
  \item \texttt{2026\_golden\_existence\_state\_phi\_pi\_e\_.../}
  \begin{itemize}
    \item 主要覆盖：统一对象 \S\ref{def:XmZ}--\ref{def:fiber} 与模板 T3（$2^m\to F_{m+2}$），并以“$\varphi$--$\pi$--$e$ 通道”组织可证伪输出。
  \end{itemize}

  \item \texttt{2026\_zeckendorf\_fold\_observation\_kernel\_single\_head\_.../}
  \begin{itemize}
    \item 主要覆盖：模板 T3 + 模板 T4（闭包/固定点现实）；
    \item 增加可逆微观动力学 $U$（Feistel 单头扫描），给出“可逆本体—不可逆观测”的最小离散原型与 $m=6$ 全枚举可验证谱。
  \end{itemize}
\end{itemize}

\subsection{超空间几何接口（cut-and-project）线}

\begin{itemize}
  \item \texttt{2026\_finite\_resolution\_hyperspace\_cut\_project\_.../}
  \begin{itemize}
    \item 主要覆盖：几何实现（cut-and-project） + 环因子符号化（模板 T2） + 稳定化核（模板 T3） + SMB/KS 时间箭头（模板 T5）；
    \item 给出 Levi/闭包视角下的可复现协议。
  \end{itemize}

  \item \texttt{2026\_golden\_oriented\_six\_dim\_superspace\_ontology\_.../}
  \begin{itemize}
    \item 主要覆盖：IUCr 词典口径的 6D superspace/接受域/原子面与本接口的对接；
    \item 将“观察者—时空对偶”表述为算子—像关系，并把信息论时间与 Levi 图作为协议层输出。
  \end{itemize}

  \item \texttt{2026\_six\_dimensional\_golden\_orientation\_cut\_project\_.../},
  \texttt{2026\_static\_phase\_lattice\_irrational\_cut\_project\_.../}
  \begin{itemize}
    \item 主要覆盖：以 6D 本体/静态相位格为起点的协议化读出体系；稳定扇区/折叠与“未来=未达分辨率深度”的表述端口。
  \end{itemize}
\end{itemize}

\subsection{紧群扫描与算术接口线}

\begin{itemize}
  \item \texttt{2026\_compact\_group\_scan\_projection\_readout\_resolution\_folding/}
  \begin{itemize}
    \item 主要覆盖：读出协议（Weyl 对扫描、三值存在逻辑） + 模板 T3（分辨率折叠）；
    \item 提供“计算—几何字典”和 Hecke 骨架作为可审计接口端口。
  \end{itemize}

  \item \texttt{2026\_compact\_abelian\_group\_scan\_readout\_renormalization\_.../}
  \begin{itemize}
    \item 主要覆盖：紧阿贝尔群平移、Pontryagin 对偶可观测、Sturmian/Fibonacci 重整化与指数完备化常数 $e$ 的结构位置；
    \item 给出与 HPA 的协议层对齐陈述。
  \end{itemize}

  \item \texttt{2026\_golden\_ratio\_driven\_scan\_projection\_generation\_.../}
  \begin{itemize}
    \item 主要覆盖：递归地址化与“概念可定义性”的生成规则；以 Zeckendorf 折叠作为稳定算术骨架与 $\zeta$ 接口。
  \end{itemize}
\end{itemize}

