\begin{remark}[Cross-layer homology and ``superstructure residual'': assembling \texorpdfstring{$r_2,\rho_{3,3,5}$}{r2,rho}, SVD, covariance, and harmonic characteristic signatures into auditable diagnostics]\label{rem:arity-335-cross-layer-diagnostics}
Beyond the comparison between \(r_2\) and \(\rho_{3,3,5}\) discussed above, the secondary summary output of \(((3,3,5))\) also provides two groups of ``cross-layer assembled characteristic signatures'' directly related to the mechanism:
\begin{itemize}
  \item \textbf{Low-rank residual:} Flattening the constant tensor \(C^{(3,3,5)}_{a,b,c}\) into \(T\in\RR^{5\times 9}\) and taking singular values \(s_1\ge\cdots\ge s_5\), then
  \(
  \varepsilon_{\mathrm{extra}}
  :=1-\frac{s_1^2+s_2^2+s_3^2}{\sum_i s_i^2}
  \)
  gives the residual energy scale ``exceeding what three modes can carry'';
  \item \textbf{Bias spectrum vs.\ rate spectrum:} The Fourier two-harmonics \(A^{(1)},A^{(2)}\) of the constant tensor along the \(c\in\ZZ/5\) axis satisfy \(\max|A^{(2)}|/\max|A^{(1)}|\approx 1\), yet the worst slow mode is realized by \(j_3=\pm 1\) (rather than \(\pm 2\)), so the ``constant-term amplitude'' and ``exponential error rate'' must be reported in parallel;
  \item \textbf{Strong coupling and GRH diagnostic:} The covariance prediction for \((\chi,\nu,\xi)\) (\(\nu\) being the negative carry, \(\xi\) the collision) gives \(\mathrm{Corr}(\nu,\xi)\approx 0.85\), and \(\rho_{3,3,5}/\sqrt{\lambda}>1\) indicates that this mode-axis twist is far from the square-root spectral bound (the kernel-version GRH does not hold), with the error residing in the exponential decay interval rather than the square-root interval.
\end{itemize}
The above scalar characteristic quantities are reproduced in one step from the same JSON output by the script \texttt{scripts/exp\_arity\_335\_cross\_layer\_diagnostics.py}:
\input{sections/generated/eq_arity_335_cross_layer_diagnostics}
\end{remark}

