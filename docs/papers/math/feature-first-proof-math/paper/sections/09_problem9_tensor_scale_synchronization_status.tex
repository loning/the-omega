\section{Problem 9: Tensor Block Scaling Synchronization}

\textbf{Latest Reviewed Version:} Q9-R3 (FAIL).

\textbf{Current Status.}
Substantial improvement over Q9-R2: the repeated-index generic-rank gap is addressed. However, the only-if direction still leaves an uncovered index regime in the final factorization step.

\textbf{Submission Record.}

\textbf{Q9-V3 (user submission).}
Main upgrades relative to Q9-R2:
\begin{itemize}
\item Adds a full case split in the generic rank lemma for nonconstant triples, including repeated-index patterns.
\item Keeps block-consistent rigidity lemma with 3-row camera coupling.
\item Uses degree-5 minor constraints in modes 1--4 and derives mode-wise separability equations.
\end{itemize}

\textbf{Review Outcome (Q9-R3).}
Still \textbf{FAIL}: final extension to all non-identical quadruples is not fully justified for the regime with constant trailing triple $(\beta,\gamma,\delta)=(b,b,b)$ and $\alpha\neq b$.

\textbf{Tracking Rule.}
Each newly submitted user version is first appended in this section, then reviewed in `reviews/problem9_review.md`.
