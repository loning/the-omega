Out-of-sample validation across three independent buckets (no use of the control-boundary identification objective). Bucket B1 (RefSeq human mRNA): AUC for discriminating terminal-stop class $\mathrm{UGA}$ vs $\mathrm{UAA}$ using $\overline{U}_{\mathrm{before}}(k=10)$ on $n_\mathrm{UGA}=20000$ and $n_\mathrm{UAA}=20000$ ORFs; score $=|\mathrm{AUC}-0.5|$. Bucket B2 (GenBank \texttt{transl\_except}): AUC for discriminating recoding sites vs CDS-deduplicated terminal stops using $\overline{U}_{\mathrm{before}}(k=10)$ on $n_1=773$ and $n_0=640$; score $=|\mathrm{AUC}-0.5|$. Bucket B3 (NCBI \texttt{gc.prt}): mean number of boundary-hit stop codons per translation table under the encoding. Across the 24 encodings, $\mu^\ast$ ranks 14/24 (B1), 3/24 (B2), and 2/24 (B3), giving conservative tail probabilities (uniform encoding prior) $p_\mathrm{B1}=0.5833$, $p_\mathrm{B2}=0.1250$, $p_\mathrm{B3}=0.1667$. Combining the three p-values with Fisher's statistic yields $p_\mathrm{comb}=0.1250$ under the exact encoding-null (enumeration over 24 encodings).
