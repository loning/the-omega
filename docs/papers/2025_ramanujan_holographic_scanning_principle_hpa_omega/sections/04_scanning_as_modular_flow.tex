\section{Scanning as modular flow: from rotation algebras to modular dynamics}
\label{sec:scanning_as_modular_flow}

This section explains how the scan algebra (O6) can be viewed as a minimal cross-section of modular dynamics. The main point is structural: the Weyl relation singles out the irrational rotation algebra (a noncommutative torus), while the modular generators $S,T$ provide canonical geometric symmetries whose cusp action supplies a scale-inversion template.

\subsection{The rotation algebra \texorpdfstring{$A_\alpha$}{A\_alpha} as the minimal scan closure}
\label{sec:rotation_algebra}

The $C^*$-algebra generated by unitaries $U,V$ satisfying
\begin{equation}
UV=\e^{2\pi\iu\alpha}VU,\qquad \alpha\notin\QQ,
\end{equation}
is the (irrational) rotation algebra $A_\alpha$ \cite{Rieffel1981,Connes1994}. In the scan interpretation:
\begin{itemize}
  \item $U$ encodes the tick iteration (time as an integer counter),
  \item $V$ encodes the phase/readout coordinate.
\end{itemize}
This is the smallest closed algebraic setting in which scanning and phase readout coexist while remaining intrinsically noncommutative.

\subsection{\texorpdfstring{$T$}{T}-translation and phase periodicity}
\label{sec:T_translation}

On $\HH$, the modular translation $T:\tau\mapsto\tau+1$ induces
\begin{equation}
q = \e^{2\pi \iu \tau} \mapsto \e^{2\pi \iu (\tau+1)} = q,
\end{equation}
so the $q$-coordinate is $T$-periodic. At the same time, the real part shifts by an integer:
\begin{equation}
x := \Re\tau \pmod 1 \quad \Rightarrow \quad x \mapsto x+1 \equiv x.
\end{equation}
This identifies a canonical boundary phase coordinate $x\in\RR/\ZZ$ compatible with modular periodicity. The scan orbit of O5,
\begin{equation}
x_t = x_0 + t\alpha \pmod 1,
\end{equation}
is then an irrational translation on the same circle: it respects the same quotient structure but selects an irrational slope (minimality and unique ergodicity are standard; see e.g.\ \cite{Weyl1916,KatokHasselblatt1995}).

Figure~\ref{fig:golden_rotation_equidistribution} provides a concrete visualization of the golden-branch rotation and the induced equidistribution that underlies window-readout frequencies.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\linewidth]{fig03_golden_rotation_equidistribution}
\caption{Golden-branch rotation $x_t=x_0+t\alpha\ (\mathrm{mod}\,1)$ with $\alpha=\varphi^{-1}$. Left: a prefix of the orbit on $\RR/\ZZ$ together with the canonical window $W=[0,1-\alpha)$. Right: an empirical histogram over equal bins illustrating equidistribution.}
\label{fig:golden_rotation_equidistribution}
\end{figure}

\subsection{\texorpdfstring{$S$}{S}-inversion, cusp equivalence, and a scale-exchange template}
\label{sec:S_inversion}

The modular inversion $S:\tau\mapsto -1/\tau$ exchanges deep and shallow regions of the cusp. Indeed, if $\Im\tau$ is large (near $\iu\infty$), then $\Im(-1/\tau)$ is small (near $0$). Since $0\sim\infty$ as cusps on $X(1)$ (Section~\ref{sec:modular_group}), $S$ supplies a canonical endpoint identification on the arithmetic quotient.

\begin{remark}[interpretation-layer template: ``semantic wormhole'']
Mathematically, $S$ is an involution that exchanges deep and shallow cusp regions and realizes an endpoint identification in the modular quotient. In an HPA--$\Omega$ interpretation, this can be used as a template for a non-local \emph{semantic wormhole}: a symmetry channel that relates coarse (IR) and fine (UV) descriptions without traversing all intermediate scan ticks in a naive linear parametrization. This language is interpretive (Layer~2) and is not required for the Layer~0/1 closure.
\end{remark}

\subsection{Geodesic flow on the modular surface and continued fractions}
\label{sec:modular_geodesic_cf}

A key bridge in this constitution is that \emph{continued fractions are not an arbitrary coding choice}: they arise canonically from the dynamics of the modular surface. Let
\begin{equation}
M := \PSL_2(\ZZ)\backslash \HH
\end{equation}
be the modular surface (an orbifold of finite hyperbolic area). The geodesic flow on $M$ admits a classical symbolic coding in which the digit sequence of a continued fraction is the itinerary of a geodesic with respect to a natural cross-section. Concretely, the Gauss map
\begin{equation}
G(\xi) = \left\{\frac{1}{\xi}\right\},\qquad \xi\in(0,1),
\end{equation}
is the base map of a standard suspension model for the modular geodesic flow \cite{Series1985,KatokHasselblatt1995}.

\begin{theorem}[Series: modular geodesic flow as a suspension over the Gauss map]
\label{thm:series_suspension_gauss}
There exists a Poincar\'e cross-section $\Sigma\subset T^1M$ for the geodesic flow and a measurable identification of $\Sigma$ with $(0,1)$ such that the first-return map on $\Sigma$ is (conjugate to) the Gauss map $G$. Moreover, the geodesic flow on $M$ is measurably isomorphic to the suspension flow over $G$ with roof function
\begin{equation}
r(\xi)=-2\log \xi.
\end{equation}
In particular, the continued-fraction digits of $\xi=[0;a_1,a_2,\ldots]$ arise as the symbolic itinerary of the corresponding modular geodesic under successive returns.
\end{theorem}

\begin{theorem}[Gauss measure and digit law]
\label{thm:gauss_digit_law}
The Gauss map $G(\xi)=\{1/\xi\}$ preserves the probability measure
\begin{equation}
\dd\mu(\xi)=\frac{1}{\log 2}\,\frac{\dd \xi}{1+\xi},\qquad \xi\in(0,1),
\end{equation}
and is ergodic with respect to $\mu$ \cite{IosifescuKraaikamp2002}. Writing $\xi=[0;a_1,a_2,\ldots]$, the first digit satisfies
\begin{equation}
\mu(a_1=k)=\log_2\!\left(1+\frac{1}{k(k+2)}\right)\qquad (k\ge 1).
\end{equation}
In particular, for $\mu$-almost every $\xi$ the digit frequencies along the orbit are governed by this law (Birkhoff theorem).
\end{theorem}

\begin{theorem}[Gauss--Kuzmin exponential convergence (classical)]
\label{thm:gauss_kuzmin_convergence}
Let $\nu$ be a probability measure on $(0,1)$ that is absolutely continuous with respect to Lebesgue measure with a density of bounded variation. Then there exist constants $C>0$ and $0<\rho<1$ such that for all $n\ge 0$,
\begin{equation}
\sup_{x\in(0,1)}\left|\nu\!\left(G^n(\xi)\le x\right)-\mu\!\left((0,x]\right)\right|
\le C\,\rho^n,
\end{equation}
where $\mu$ is the Gauss invariant measure from Theorem~\ref{thm:gauss_digit_law} (so $\mu((0,x])=\log_2(1+x)$). Equivalently, digit statistics relax exponentially fast to the Gauss digit law along typical continued-fraction tails. The optimal $\rho$ is known as the Gauss--Kuzmin--Wirsing constant, numerically $\rho\approx 0.30366$; see \cite{IosifescuKraaikamp2002}.
\end{theorem}

\begin{remark}[quantitative relaxation-to-equilibrium (audit point)]
Theorem~\ref{thm:gauss_kuzmin_convergence} upgrades the digit law from an ``almost-everywhere'' statement to a \emph{finite-time} relaxation guarantee for a broad class of initial distributions. We include a reproducible toy fit for the decay of a digit-law distance under $G^n$ (Appendix~\ref{app:quant_fits_gauss_kuzmin}, Experiment~13).
\end{remark}

\begin{remark}[constitution-level consequence]
This modular-geodesic origin provides a strict mother-space justification for the continued-fraction/Ostrowski module used in Section~\ref{sec:canonical_coding}: the digits $\{a_n\}$ are canonical dynamical invariants of modular geodesics, and Ostrowski numeration is the corresponding canonical integer-time coordinate system.
\end{remark}


