Composition-adjusted controls for recoding-site context (primary window radius $k=10$).

Within-CDS GC+dinuc NN (Control-C), before-window: diff -1.6410 (CI$_{95\%}$=[-1.7601,-1.5219], paired $p<10^{-300}$, sign-flip $p=0.0005$; n=1656).

Within-CDS GC+dinuc NN (Control-C), after-window: diff -1.8191 (CI$_{95\%}$=[-1.9646,-1.6737], paired $p<10^{-300}$, sign-flip $p=0.0005$; n=1656).

GC+dinuc NN to CDS-deduplicated terminal-stop pool, before-window: diff -3.3025 (paired $p<10^{-300}$, sign-flip $p=0.0005$; n=1565).

GC+dinuc NN to CDS-deduplicated terminal-stop pool, after-window: diff NA (paired $p=NA$, sign-flip $p=NA$; n=0).

Stratified (GC$\times$CpG) recoding vs terminal, before-window: overall diff -0.3850 (perm $p=1.0000$).

Stratified (GC$\times$CpG) recoding vs terminal, after-window: overall diff NA (perm $p=NA$).

Stratified (GC$\times$TA) recoding vs terminal, before-window: overall diff -1.8732 (perm $p=0.7066$).

Stratified (GC$\times$TA) recoding vs terminal, after-window: overall diff NA (perm $p=NA$).
