\section{Closed computable work packages (interface deliverables)}
\label{app:next_steps}

This appendix summarizes the interface work packages that are realized \emph{within this paper} as explicit computable deliverables.
They are not premises for any theorem-level folding statement; they are protocol-layer closures and audit objects with deterministic scripts and generated tables.

\subsection{Functorial refinement of the field-level labeling map}
The labeling map $\mathcal{L}_{\mathrm{SM}}$ at $(m,n)=(6,3)$ is closed in Section~\ref{sec:sm_labeling_closure}.
Its canonical uplift/refinement under window growth is made explicit by prefix projection and deterministic refinement indices in Appendix~\ref{app:functorial_refinement}, together with generated lift-multiplicity and refinement-audit tables.

\subsection{Mixing matrices as holonomy of protocol connections}
Finite protocol connections and holonomy diagnostics are constructed explicitly in Section~\ref{sec:protocol_connections_holonomy}.
The resulting bounded-complexity closures for CKM/PMNS magnitudes and their induced matrix reconstructions are recorded in Sections~\ref{sec:couplings_cp} and \ref{sec:pmns_neutrino_closure}, with unitarity and CP-odd invariants reported in generated tables.

\subsection{Resolution flow and running couplings}
The discrete uplift/coarse-graining flow is fixed as the protocol flow law in Definition~\ref{def:protocol_flow_step}.
Its scale dictionary is provided by the Fibonacci resolution coordinate and the RG conversion rule in Proposition~\ref{prop:rg_dictionary_r}, while the thresholded staircase $m_{\mathrm{eff}}(\mu)$ is fixed by Corollary~\ref{cor:resolution_selection}.

\subsection{Mass/inertia as protocol cost and latency (interface)}
The closed mass-spectrum template in Section~\ref{sec:mass_spectrum_closure} is complemented by a matching-layer delay and lapse dictionary in Section~\ref{app:time_mass_delay}, including Wigner--Smith delay as an operational proxy and standard GR/SR reference relations used only at the interface layer.
Appendix~\ref{app:mass_flow_under_uplift} additionally records a minimal uplift mass-flow construction that pools intrinsic high-$m$ invariants inside lift fibers $\mathrm{Ext}_m(u)$ (CAP representative vs.\ free-energy representative), yielding reproducible pooled depths $\widehat r_{\mathrm{CAP}}(u;m)$ and $\widehat r_{\mathrm{FE}}(u;m)$ in a fixed audit window.

\subsection{External neutrino audit channels (Match/Audit only)}
The minimal PMNS neutrino closure is oscillation-focused and does not close Majorana phases.
To make the falsifiability interface explicit without importing new premises, Appendix~\ref{app:neutrino_external_audit_channels} records external audit-channel definitions (0$\nu\beta\beta$, $\Sigma m_\nu$, $m_\beta$, sterile/$\Delta N_\mathrm{eff}$), a generated external-input ledger, and deterministic proxy checks that compare the interface spectra to recorded bounds.

\subsection{Protocol-to-continuum error control (interface)}
The $\chi(x)$ reconstruction protocol in Section~\ref{app:chi_reconstruction_protocol} and the overhead-to-gravity weak-field dictionary in Section~\ref{app:overhead_to_gravity_closure} together define a concrete pipeline from discrete protocol statistics to continuum representative fields such as $\Phi$ and $\rho_{\mathrm{eff}}$.
To make this pipeline auditable as a quantitative interface deliverable, Appendix~\ref{app:protocol_to_continuum_error_control} records explicit error-control bounds and propagation formulas:
concentration bounds for bounded folding-derived statistics, log-ratio perturbation bounds for $\widehat\chi$, and truncation/noise-amplification bounds for the finite-difference operators (needed for $\chi'$ and $\Delta\chi$), together with the resulting uncertainty propagation to $\widehat\gamma$ and $\widehat\rho_{\mathrm{eff}}$.


