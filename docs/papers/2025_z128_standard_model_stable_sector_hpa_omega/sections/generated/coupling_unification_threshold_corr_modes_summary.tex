\paragraph{Threshold correlation-mode audit summary (bounded).}
\AuditTag We evaluate a bounded correlated threshold-mode family under scheme \texttt{Decouple\_QCD\_nf} with $n=10$ using two-loop gauge-gauge running. Each mode is parameterized by a single signed amplitude $a\in\{-0.25,0,0.25\}$ in the $r$ coordinate and yields an optimized mismatch $\min_r E_\infty(r)$ and its minimizing $r_\star$. The best row in the declared family achieves $E_\infty\approx 0.804$ at $r_\star\approx 89.250$ (mode \texttt{stratified\_heavy}, $|a|=0.250$).
