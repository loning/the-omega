\section{Problem 6: Existence of Large $\epsilon$-Light Sets}

\textbf{Latest Reviewed Version:} Q6-R1 (FAIL).

\textbf{Current Status.}
Partial progress only. The draft correctly proves a universal upper barrier and a linearization lemma, but does not close the main existence statement.

\textbf{Submission Record.}

\textbf{Q6-V1 (user submission).}
Main delivered claims:
\begin{itemize}
\item A sharp universal upper bound candidate: any universal constant must satisfy $c\le 1/2$ (matching-graph obstruction).
\item A reduction lemma: for all $S\subseteq V$, 
\[
L_S\preceq \frac12\sum_{u\in S}L_u,
\]
with corollary that $\sum_{u\in S}L_u\preceq 2\varepsilon L$ implies $S$ is $\varepsilon$-light.
\item Explicit statement that the global existence step (absolute $c>0$ for all graphs/all $\varepsilon$) is not proved.
\end{itemize}

\textbf{Tracking Rule.}
Each newly submitted user version is first appended in this section, then reviewed in `reviews/problem6_review.md`.
