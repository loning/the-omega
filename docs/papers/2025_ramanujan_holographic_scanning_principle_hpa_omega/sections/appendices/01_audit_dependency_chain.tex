\section{Logical closure and dependency chain (audit table)}
\label{app:audit_dependency_chain}

To make the argument auditable, we list the dependency chain explicitly. Items marked ``interpretation layer'' are not used as premises.

\subsection{Layering axioms (A0; accepted as constitutive)}
\label{app:a0}

\begin{itemize}
  \item O1--O4: ontological container (static global state, finite information, causally local discrete update, holographic map).
  \item O5--O6 and R1: scan--projection readout, induced measures, Weyl pair, and a fixed finite-part convention.
  \item Stage choice: adopt the level-$1$ modular quotient $X(1)$ as a minimal arithmetic-geometric stage under the selection criteria of Section~\ref{sec:why_x1_selection} (with alternatives left open).
\end{itemize}

\subsection{From axioms to the minimal scan model (A1; standard)}
\label{app:a1}

\begin{itemize}
  \item Weyl pair $\Rightarrow$ rotation algebra closure (Section~\ref{sec:rotation_algebra}).
  \item Covariant model $\Rightarrow$ circle rotation scan $x_t=x_0+t\alpha \pmod 1$ (O5/O6).
\end{itemize}

\subsection{From scan to discrete coding (A2; standard)}
\label{app:a2}

\begin{itemize}
  \item Irrational rotation + interval window $\Rightarrow$ Sturmian mechanical word (Section~\ref{sec:sturmian_words}).
  \item Continued fraction $\Rightarrow$ Ostrowski numeration; golden branch $\Rightarrow$ Zeckendorf degeneration (Section~\ref{sec:ostrowski}--\ref{sec:zeckendorf}).
  \item Denjoy--Koksma + Ostrowski block decomposition $\Rightarrow$ finite-$N$ discrepancy bounds controlled by digit sums (Appendix~\ref{app:ostrowski_discrepancy}).
\end{itemize}

\subsection{Arithmetic-geometric mother space (A3; standard definitions and a minimal choice)}
\label{app:a3}

\begin{itemize}
  \item Minimal selection criteria for $X(1)$ and explicit alternatives (Section~\ref{sec:why_x1_selection}).
  \item $\PSL_2(\ZZ)$ action on $\HH$, fundamental domain, cusp identification (Section~\ref{sec:modular_group}).
  \item Modular-form $q$-expansion at cusps and Hecke algebra structure (Sections~\ref{sec:modular_forms_qexp}--\ref{sec:why_primes}).
\end{itemize}

\subsection{Interpretation-layer mappings (A4; not premises)}
\label{app:a4}

\begin{itemize}
  \item ``discrete matter $=$ $q$-coefficients'' is a readout-interface hypothesis;
  \item ``$S$-inversion as wormhole/entanglement template'' is a semantic analogy;
  \item Hecke correspondences as symmetry-preserving coarse-graining are a canonical operator family once the modular stage and cusp interface are adopted (Section~\ref{sec:hecke_protocol_origin}); interpreting Hecke dynamics as physical renormalization requires additional dynamical closure beyond the constitution.
\end{itemize}

The constitution-level dependency chain may be summarized as
\begin{equation}
\begin{aligned}
&\text{scan--projection (O5/O6)}
\;\wedge\;
\text{cusp interface ($q$-expansion)}
\;\wedge\;
\text{Hecke/prime skeleton}
\\
&\Rightarrow\;\text{canonical discrete readout with arithmetic rigidity.}
\end{aligned}
\end{equation}


