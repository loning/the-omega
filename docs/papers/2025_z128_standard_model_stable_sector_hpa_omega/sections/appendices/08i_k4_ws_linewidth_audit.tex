\section{K4 WS--linewidth calibration audit (externally falsifiable interface)}
\label{app:k4_ws_linewidth_audit}

\AuditTag This appendix records a minimal interface calibration audit that can be falsified by a single measured resonance.
Given phase-shift points $\delta(E)$ one may form a phase-derived delay proxy $\tau_{\mathrm{phase}}(E)\approx 2\,\mathrm{d}\delta/\mathrm{d}E$ (single-channel convention), while a single-resonance linewidth $\gamma$ provides a complementary proxy $\tau_{\gamma}\approx 4/\gamma$ in the same proxy units.
We compare these two proxies as a bounded audit check.

\subsection{Generated artifact}
\begin{table}[h]
\centering
\small
\begin{tabular}{l l l r r r r r r r}
\toprule
dataset & $x$ & unit & $x_0$ & $\gamma$ & $k$ & $\widehat{\tau}_{\mathrm{phase}}$ & $\widehat{\tau}_{\gamma}$ & sgn & $e$ \\
\midrule
\input{sections/generated/k4_ws_linewidth_rows.tex}%
\end{tabular}
\caption{WS--linewidth calibration rows. $k$ is the (bounded) local-fit window radius in samples. sgn is the sign of $\widehat{\tau}_{\mathrm{phase}}$. $e$ is the absolute log mismatch on magnitudes $e=\lvert\log(\lvert\tau_{\mathrm{phase}}\rvert/\lvert\tau_{\gamma}\rvert)\rvert$.}
\end{table}

\begin{table}[h]
\centering
\small
\begin{tabular}{r l l l l r r r r r}
\toprule
\# & dataset & transform & $y$ & unit & $k$ & $\widehat{\tau}_{\mathrm{phase}}$ & $\widehat{\tau}_{\gamma}$ & sgn & $e$ \\
\midrule
\input{sections/generated/k4_ws_linewidth_coord_rows.tex}%
\end{tabular}
\caption{Optional coordinate-transform report (non-selective): when $x=T_{\mathrm{lab}}$ (NN-OnLine) and the resonance metadata is given in the same abscissa units, we additionally report two transparent hypotheses for $y=E_{\mathrm{cm,kin}}$ (NR/REL) and transform both $\widehat{\tau}_{\mathrm{phase}}$ and $\widehat{\tau}_{\gamma}$ consistently via the local Jacobian.}
\end{table}

\input{sections/generated/k4_ws_linewidth_summary.tex}

