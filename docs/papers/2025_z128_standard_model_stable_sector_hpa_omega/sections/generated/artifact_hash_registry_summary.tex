\paragraph{Audit summary (artifact hash registry).} \AuditTag We write a JSON dictionary that records, for each generator step, the script hash, the transitive local dependency fingerprint, and the content hashes of expected outputs. This provides a content-addressable cache index independent of mtimes. File: \texttt{sections/generated/artifact_hash_registry.json}.