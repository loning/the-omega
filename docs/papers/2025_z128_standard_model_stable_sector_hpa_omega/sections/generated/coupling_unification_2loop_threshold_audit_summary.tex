\paragraph{Two-loop + threshold-uncertainty audit summary (bounded).}
\AuditTag We run a two-loop gauge-running dictionary in the $r$ coordinate under a bounded named-threshold registry, with a bounded discrete family $\alpha_3^{-1}(\mu_Z)=n\pi^2$ for $1\le n\le 50$. At the central threshold anchors (no perturbation), the overall winner is scheme \texttt{Decouple\_QCD\_nf} with $n=10$, achieving $E_\infty\approx 0.830$ at $r_\star\approx 89.250$. For each scheme we additionally report a bounded threshold-location perturbation band (step $\Delta r\in\{-0.25,0,0.25\}$ at $(H,t,b,c,s)$).
