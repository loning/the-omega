\noindent\AuditTag Weak-field Laplacian scaling check on the periodic unit square: $\chi(x,y)=\sin(2\pi x)\sin(2\pi y)$ so $\Delta\chi=-8\pi^2\chi$. We compare the periodic central-difference Laplacian $\Delta_h$ to the exact $\Delta$ under a deterministic bounded additive noise $|\eta|\le \epsilon$ (fixed RNG seed). The truncation-bound constant uses the explicit $C h^2$ form from Theorem~\ref{thm:laplacian_truncation}, and the noise term uses Corollary~\ref{cor:laplacian_noise_amplification} with $d=2$.