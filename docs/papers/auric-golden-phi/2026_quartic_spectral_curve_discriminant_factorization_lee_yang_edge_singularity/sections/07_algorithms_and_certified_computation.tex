\section{Computational aspects}\label{sec:algo}

\subsection{Evaluating \texorpdfstring{$Z_m(y)$}{Z\_m(y)} via the order-\texorpdfstring{$4$}{4} recurrence}

For any fixed $y\in\CC$, the recurrence \eqref{eq:recurrence} evaluates $Z_m(y)$ in $O(m)$ arithmetic operations after initializing $Z_0,\dots,Z_3$.
When evaluating $Z_m$ at many points (e.g.\ along a contour in the complex plane), the same linear-time computation can be performed independently at each sample point.

These computations are reproducibility-oriented and numerical only: all visual diagnostics are empirical evidence and are not used as input to theorems in the analytic part of the paper.

\subsection{Localization of the branch set and the branch point \texorpdfstring{$\yLY$}{y\_LY}}

The branch locus $\Branch$ is explicitly determined by Theorem~\ref{thm:disc-factor}.
The points $y=0$ and $y=1$ are rational and hence exact.
The branch point $\yLY$ is the unique real root of the cubic $c(y)$ in \eqref{eq:cubic}.
An interval isolation is obtained by any rational interval $(\alpha,\beta)$ such that $c(\alpha)c(\beta)<0$; one explicit witness is
\[
\alpha=-\frac{113446}{100000},\qquad
\beta=-\frac{113445}{100000}.
\]
Indeed
\[
c(\alpha)<0,\qquad c(\beta)>0.
\]
Standard bisection then yields arbitrarily fine rational enclosures.

\subsection{Zero localization}

For each fixed $m$, $Z_m(y)$ is a polynomial in $y$ with integer coefficients.
Two standard zero-localization routes are available:
\begin{enumerate}
  \item \textbf{Coefficient route.} Propagate the recurrence in the polynomial ring $\ZZ[y]$ to obtain explicit coefficients of $Z_m(y)$, and apply a polynomial root solver to locate the zeros.
  \item \textbf{Sampling route.} Sample $Z_m(y)$ on a closed contour $\Gamma\subset\CC$, estimate the number of enclosed zeros via the argument principle, and refine $\Gamma$ to localize zero clusters.
\end{enumerate}
Both routes can be compared with the algebraic boundary $\Branch$, providing a consistency check on the zero-cloud geometry predicted by the Beraha--Kahane--Weiss framework (Section~\ref{sec:zeros}).

For reproducibility, the repository contains the following scripts in the local \texttt{scripts} directory:
\begin{itemize}
  \item \texttt{scripts/coeff\_nonneg\_check.pl}: exact finite-range verification of nonnegative coefficients of $Z_m(y)$ in $\ZZ[y]$ (supports Theorem~\ref{thm:coeff-nonneg}; a reproducibility certificate is also recorded in Appendix~\ref{app:degeneracies});
  \item \texttt{scripts/branch\_root\_certificate.pl}: exact integer certificates for both branch-cubic and non-bifurcating equimodular data:
        \begin{itemize}
          \item verifies the branch-cubic sign flip at $\alpha,\beta$, yielding \(\yLY\in(-1.13446,-1.13445)\);
          \item optionally checks \(p_{\mathrm{eq}}(-160092/10^6)<0<p_{\mathrm{eq}}(-160091/10^6)\), yielding
                \(\yEQ\in(-160092/10^6,\,-160091/10^6)\);
          \item with \texttt{--check-sturm} gives a full Sturm certificate that \(p_{\mathrm{eq}}\) has exactly one real root;
          \item with \texttt{--check-coprime} verifies \(\operatorname{Res}_y(c,p_{\mathrm{eq}})\neq0\).
        \end{itemize}
  \item A minimal reproducibility command set is
      \begin{verbatim}
perl scripts/coeff_nonneg_check.pl 5000
perl scripts/branch_root_certificate.pl
perl scripts/branch_root_certificate.pl --check-equimodular --check-sturm
perl scripts/branch_root_certificate.pl --check-equimodular --check-coprime
      \end{verbatim}
\end{itemize}
In addition, the same directory can be used to regenerate the discriminant factors and the branch point isolation:
\begin{itemize}
  \item $c(y)=256y^3+411y^2+165y+32$ and $y_{\mathrm{LY}}$ are checked using exact rational arithmetic in $\QQ$;
  \item numerator/denominator cancellations at $y\in\{-1,0,1\}$ are verifiable directly from `N(t,y)` and `D(t,y)`;
  \item numerical coefficient data can be regenerated for any target $(m,k)$ grid for external verification of Theorem~\ref{thm:coeff-nonneg};
  \item $y_{\mathrm{LY}}$ and all 12 audit figures are generated by
        \begin{verbatim}
node scripts/generate_audit_figures.js
        \end{verbatim}
        and rendered from \texttt{figs/data/*.dat}.
\end{itemize}
\FloatBarrier
\subsection{Reproducible visual diagnostics (12 figures)}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.9\linewidth,
  height=5.2cm,
  xlabel={$y$},
  ylabel={$c(y)$},
  xmin=-2.5, xmax=1,
  grid=both
]
\addplot[blue, thick] table[x index=0,y index=1,col sep=space] {figs/data/fig01_branch_cubic_values.dat};
\addplot[black, dotted] coordinates {(-2.5,0) (1,0)};
\addplot[red, only marks, mark=*, mark size=2pt] coordinates {(-1.134452,0)};
\end{axis}
\end{tikzpicture}
\caption{Branch cubic profile $c(y)=256 y^3+411y^2+165y+32$ on the real axis (numerical evidence).}
\label{fig:branch-cubic-real}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={$\Re\,y$},
  ylabel={$\Im\,y$},
  axis equal image,
  grid=both
]
\addplot[only marks,blue, mark=*, mark size=2.0pt] table[x index=0,y index=1,col sep=space] {figs/data/fig02_branch_roots_complex.dat};
\end{axis}
\end{tikzpicture}
\caption{Complex roots of the branch cubic $c(y)$ (numerical evidence).}
\label{fig:branch-cubic-roots}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.9\linewidth,
  height=5.2cm,
  xlabel={$y$},
  ylabel={$\log |\lambda_i(y)|$},
  grid=both,
  legend style={at={(1.02,1)},anchor=north west}
]
\addplot[blue, thick] table[x index=0,y index=1,col sep=space] {figs/data/fig03_root_moduli_real.dat};
\addplot[red, thick] table[x index=0,y index=2,col sep=space] {figs/data/fig03_root_moduli_real.dat};
\addplot[green!60!black, thick] table[x index=0,y index=3,col sep=space] {figs/data/fig03_root_moduli_real.dat};
\addplot[orange, thick] table[x index=0,y index=4,col sep=space] {figs/data/fig03_root_moduli_real.dat};
\legend{$\log|\lambda_1|$,$\log|\lambda_2|$,$\log|\lambda_3|$,$\log|\lambda_4|$}
\end{axis}
\end{tikzpicture}
\caption{Logarithmic root moduli on the real axis (numerical evidence).}
\label{fig:root-moduli-real}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.9\linewidth,
  height=5.2cm,
  xlabel={$y$},
  ylabel={root real / imag parts near $\yLY$},
  legend style={at={(1.02,1)},anchor=north west},
  grid=both
]
\addplot[blue, thick] table[x index=0,y index=1,col sep=space] {figs/data/fig04_local_collision_zoom.dat};
\addplot[blue, thick, dashed] table[x index=0,y index=2,col sep=space] {figs/data/fig04_local_collision_zoom.dat};
\addplot[red, thick] table[x index=0,y index=3,col sep=space] {figs/data/fig04_local_collision_zoom.dat};
\addplot[red, thick, dashed] table[x index=0,y index=4,col sep=space] {figs/data/fig04_local_collision_zoom.dat};
\legend{$\Re r_1$,$\Im r_1$,$\Re r_2$,$\Im r_2$}
\end{axis}
\end{tikzpicture}
\caption{Local colliding pair of spectral roots as $y$ varies across $\yLY$ (numerical evidence).}
\label{fig:local-collision}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.9\linewidth,
  height=5.0cm,
  xlabel={$y$},
  ylabel={gap in $\log$ moduli},
  ymajorgrids=true,
  xminorgrids=true,
  grid style={dashed,gray!30}
]
\addplot[blue, thick] table[x index=0,y index=3,col sep=space] {figs/data/fig05_dominance_gap_real.dat};
\addplot[red, thick] table[x index=0,y index=4,col sep=space] {figs/data/fig05_dominance_gap_real.dat};
\addplot[black,dashed] coordinates {(-3,0) (0,0)};
\legend{$\log|\lambda_1|-\log|\lambda_2|$,$\log|\lambda_2|-\log|\lambda_3|$}
\end{axis}\end{tikzpicture}
\caption{Dominant/subdominant gap on the negative real axis (numerical evidence).}
\label{fig:dominance-gap}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={Re($y$)},
  ylabel={Im($y$)},
  axis equal image,
  grid=both
]
\addplot[only marks,blue, mark=*, mark size=0.8pt] table[x index=0,y index=1,col sep=space] {figs/data/fig06_zero_cloud_m12.dat};
\end{axis}
\end{tikzpicture}
\caption{Zero cloud for $Z_{12}(y)$ (numerical evidence).}
\label{fig:zero-cloud-m12}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={Re($y$)},
  ylabel={Im($y$)},
  axis equal image,
  grid=both
]
\addplot[only marks,blue, mark=*, mark size=0.8pt] table[x index=0,y index=1,col sep=space] {figs/data/fig07_zero_cloud_m20.dat};
\end{axis}\end{tikzpicture}
\caption{Zero cloud for $Z_{20}(y)$ (numerical evidence).}
\label{fig:zero-cloud-m20}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={Re($y$)},
  ylabel={Im($y$)},
  axis equal image,
  grid=both
]
\addplot[only marks,blue, mark=*, mark size=0.8pt] table[x index=0,y index=1,col sep=space] {figs/data/fig08_zero_cloud_m28.dat};
\end{axis}\end{tikzpicture}
\caption{Zero cloud for $Z_{28}(y)$ (numerical evidence).}
\label{fig:zero-cloud-m28}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={Re($y$)},
  ylabel={Im($y$)},
  axis equal image,
  grid=both
]
\addplot[only marks,blue, mark=*, mark size=0.8pt] table[x index=0,y index=1,col sep=space] {figs/data/fig09_zero_cloud_m36.dat};
\end{axis}\end{tikzpicture}
\caption{Zero cloud for $Z_{36}(y)$ (numerical evidence).}
\label{fig:zero-cloud-m36}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={Re($y$)},
  ylabel={Im($y$)},
  axis equal image,
  grid=both
]
\addplot[blue, thick, unbounded coords=jump] table[x index=0,y index=1,col sep=space] {figs/data/fig10_eq12_contour.dat};
\end{axis}\end{tikzpicture}
\caption{Tracked-root equimodular contour from the local-1/2 gap (numerical evidence).
}
\label{fig:eq-contour-12}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={Re($y$)},
  ylabel={Im($y$)},
  axis equal image,
  grid=both
]
\addplot[blue, thick, unbounded coords=jump] table[x index=0,y index=1,col sep=space] {figs/data/fig11_eq13_contour.dat};
\addplot[red, thin, dashed, unbounded coords=jump] table[x index=0,y index=1,col sep=space] {figs/data/fig12_eq23_contour.dat};
\end{axis}\end{tikzpicture}
\caption{Tracked-root equimodular contours from gaps $1$-$3$ and $2$-$3$ (numerical evidence).}
\label{fig:eq-contour-13-23}
\end{figure}
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth,
  height=4.8cm,
  xlabel={Re($y$)},
  ylabel={Im($y$)},
  axis equal image,
  grid=both
]
\addplot[blue, thick, unbounded coords=jump] table[x index=0,y index=1,col sep=space] {figs/data/fig12_eq23_contour.dat};
\end{axis}\end{tikzpicture}
\caption{Tracked-root equimodular contour from gap $2$-$3$ (numerical evidence).}
\label{fig:eq-contour-23}
\end{figure}

