\paragraph{BH6 sweep diagnostic (audit).} \AuditTag This fragment reports a finite sweep that treats interior degrees of freedom as folding-fiber information beyond coarse stable labels. For each $(m,\\texttt{mode})$, it reports the interior ambiguity proxy $\\sum_i\\log_2 g_m(W_i)$ together with whether exact reconstruction from the exterior record succeeds in the queue-equivalent single-stream model. This is an audit-level diagnostic of the algebraic pattern ``interior as reconstructible subalgebra of the exterior record''.