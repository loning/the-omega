\label{subsec:bdry-tower-zeck-gut-part1}
\paragraph{Endpoint \texorpdfstring{$(1,1)$}{(1,1)} fiber as the ``boundary word'' layer}
Under the golden mean syntax (forbidding adjacent \(11\)), the set of stable types of length \(m\) is \(X_m\), and the endpoint fiber
\[
X_m^{\mathrm{bdry}}
:=\{u\in X_m:\ u_1=1,\ u_m=1\}
\]
has its cardinality rigidly forced by Fibonacci numbers. Specifically, Corollary \ref{cor:parry-golden-three-levels} gives
\[
|X_m^{\mathrm{bdry}}|
=\#\{u\in X_m:u_1=1,u_m=1\}
=F_{m-2}.
\]
Therefore each resolution \(m\) carries a discrete ``boundary mode number'' \(d(m):=|X_m^{\mathrm{bdry}}|\).

\paragraph{Boundary word three-layer tower: the \(12\) gauge bosons of the SM}
At the \(m=6\) anchoring interface, the \(18\oplus 3\) decomposition of \(X_6\) provides three boundary connectivity class discrete labels;
if one promotes the gauge degrees of freedom from a ``single-layer structure'' to a ``cross-resolution tower of modes,'' then the SM boson count
\(\,1+3+8=12\,\)
emerges in this framework as a strict Fibonacci boundary counting decomposition:
\input{sections/generated/eq_boundary_tower_sm12_en}
This step does not merely repeat the ``\(SU(3)\times SU(2)\times U(1)\) three-factor'' conclusion, but upgrades the interface to the \emph{boson counting level}:
the gauge bosons of \(U(1)\), \(SU(2)\), and \(SU(3)\) reside respectively in the \(m=4,6,8\) boundary layers,
with the boundary words themselves serving as discrete label candidates.

\begin{remark}[Origin of color symmetry as a closed-layer envelope: basis mixing redundancy of three-copy Hilbert spaces]\label{rem:color-as-three-copy-envelope}
The preceding paragraph provides a discrete anchor for ``color'' boson counting at the \(m=8\) boundary layer.
If the closed-layer mechanism further satisfies: the internal state space decomposes into three disjoint copies
\[
\mathcal{H}=\mathcal{H}_q\oplus\mathcal{H}_q\oplus\mathcal{H}_q,
\]
and the observable statistics are insensitive to basis changes among these three copies,
then the redundancy group of ``copy mixing'' is naturally \(U(3)\); removing the overall phase yields the continuous envelope \(SU(3)\).
In the compilation framework of this paper, similar ``copy juxtaposition'' has already appeared in the form of type lifting (e.g., the \(\mathrm{LIFT}_{G^{\times q}}\) juxtaposition lifting of the moment kernel),
thereby tracing the origin of the continuous envelope symmetry back to a trackable discrete copy structure.
\end{remark}

\paragraph{The \texorpdfstring{$18\oplus 3$}{18+3} anchoring of Fold6 forces a \texorpdfstring{$B_3/C_3$}{B3/C3} unification seed}
At the \(m=6\) anchoring interface, in addition to the \(18\oplus 3\) decomposition \(X_6=X_6^{\mathrm{cyc}}\sqcup X_6^{\mathrm{bdry}}\),
the interior of \(X_6^{\mathrm{cyc}}\) also exhibits a strict \(6+12\) two-layer fingerprint (stratified by Hamming weight):
\input{sections/generated/eq_fold6_b3c3_seed_en}
If one interprets the three endpoint fibers \(X_6^{\mathrm{bdry}}\) as three commuting connection directions (Cartan-like), and the \(18\) closed words \(X_6^{\mathrm{cyc}}\) as root directions (charged/root-like),
then the count \((3+18)=21\) is isomorphic to the Cartan/root decomposition of a rank-\(3\) simple Lie algebra, thereby locking the candidate unification Lie type to \(B_3\) or \(C_3\) (which are long/short root exchange dual types).
More strongly, the \(6+12\) cyclic fingerprint is consistent with the ``two root lengths'' structure of the \(B_3/C_3\) root system: one class of length \(6\), the other of length \(12\).
\par
The corresponding discrete symmetry kernel is the Weyl group
\[
W(B_3)\cong W(C_3)\cong (\ZZ_2)^3\rtimes S_3,\qquad |W|=48,
\]
where \(S_3\) can be read as the permutation of three channels/axes, and \((\ZZ_2)^3\) corresponds to sign reflections along each commuting direction (charge mirrors).
\par
To avoid ``manual pairing,'' the script \texttt{scripts/exp\_fold6\_b3c3\_seed.py} outputs root dictionary tables in both normalizations (long/short root exchange) (Tables \ref{tab:fold6_b3c3_root_dictionary_b3} and \ref{tab:fold6_b3c3_root_dictionary_c3}; see the appendix), and writes the JSON certificate \texttt{artifacts/export/fold6\_b3c3\_seed.json}.

\begin{corollary}[Weyl rigidity two-orbit compression law: any Weyl-invariant statistic collapses to two scalars]\label{cor:fold6-weyl-two-orbit-compression}
Take the \(B_3\) normalized root dictionary from Table \ref{tab:fold6_b3c3_root_dictionary_b3}:
\(\psi_B:X_6^{\mathrm{cyc}}\to \Phi(B_3)\).
Let \(\Phi_{\mathrm{short}},\Phi_{\mathrm{long}}\subset\Phi(B_3)\) denote the short and long root sets respectively, and define their preimage blocks in the cyclic sector:
\[
X_6^{\mathrm{short}}:=\psi_B^{-1}(\Phi_{\mathrm{short}}),\qquad
X_6^{\mathrm{long}}:=\psi_B^{-1}(\Phi_{\mathrm{long}}),
\qquad
X_6^{\mathrm{cyc}}=X_6^{\mathrm{short}}\sqcup X_6^{\mathrm{long}}.
\]
Then this partition is consistent with the fingerprint stratification \(\mathrm{wt}=1\) vs.\ \(\mathrm{wt}\neq 1\), and satisfies
\(|X_6^{\mathrm{short}}|=6\), \(|X_6^{\mathrm{long}}|=12\) (see Table \ref{tab:fold6_b3c3_root_dictionary_b3} and the snippet \texttt{\detokenize{sections/generated/eq_fold6_b3c3_seed.tex}}).
\par
Let \(f:X_6^{\mathrm{cyc}}\to\RR\) be a \textbf{Weyl-invariant} observable: for any \(u,v\in X_6^{\mathrm{cyc}}\), if there exists \(w\in W(B_3)\) such that
\(\psi_B(v)=w\cdot \psi_B(u)\), then \(f(u)=f(v)\).
Then \(f\) takes constant values on the two Weyl orbits:
\[
f_{\mathrm{short}}:=f(u)\ (u\in X_6^{\mathrm{short}}),\qquad
f_{\mathrm{long}}:=f(u)\ (u\in X_6^{\mathrm{long}}),
\]
and the cyclic average satisfies the strict identity
\[
\frac{1}{|X_6^{\mathrm{cyc}}|}\sum_{u\in X_6^{\mathrm{cyc}}} f(u)
=
\frac{6}{18}\,f_{\mathrm{short}}+\frac{12}{18}\,f_{\mathrm{long}}
=
\frac13 f_{\mathrm{short}}+\frac23 f_{\mathrm{long}}.
\]
\end{corollary}

\begin{proof}
In the standard root system model of \(B_3\), the Weyl group \(W(B_3)\cong (\ZZ_2)^3\rtimes S_3\) acts as ``signed coordinate permutations'' on \(\RR^3\), and hence acts transitively on the short root set \(\{\pm e_i\}\) and the long root set \(\{\pm e_i\pm e_j\}\) respectively; therefore \(\Phi(B_3)\) decomposes into exactly two Weyl orbits \(\Phi_{\mathrm{short}}\) and \(\Phi_{\mathrm{long}}\) with cardinalities \(6\) and \(12\).
By the bijectivity of the dictionary \(\psi_B\), pulling back the two-orbit decomposition to \(X_6^{\mathrm{cyc}}\) yields \(X_6^{\mathrm{cyc}}=X_6^{\mathrm{short}}\sqcup X_6^{\mathrm{long}}\) with the same orbit sizes.
Weyl-invariance means \(f\) takes a constant value on each orbit, so the summation splits into two blocks, yielding the stated averaging formula. \qed
\end{proof}

\begin{theorem}[Four-step shift uplift isomorphism]\label{thm:boundary-shift4-uplift-isomorphism}
For any \(n\ge 2\), the map
\[
\iota_n:X_n\to X_{n+4}^{\mathrm{bdry}},\qquad \iota_n(v)=10\,v\,01
\]
is a canonical bijection, whose inverse is the deletion of the first two and last two bits. In particular, when \(n=6\),
\input{sections/generated/eq_boundary_uplift_m10_isomorphism_en}
\end{theorem}
\begin{proof}
Under the golden mean syntax (forbidding adjacent \(11\)), any boundary word \(b\in X_{n+4}^{\mathrm{bdry}}\) satisfies \(b_1=b_{n+4}=1\), hence necessarily \(b_2=b_{n+3}=0\), so \(b\) is uniquely written as \(b=10\,v\,01\) with \(v\in X_n\).
Thus \(\iota_n\) is a bijection, and its inverse is the deletion of the first two and last two bits. \qed
\end{proof}

\begin{theorem}[\texorpdfstring{$(B_3/C_3)$}{(B3/C3)} rigid four-block splitting of root directions]\label{thm:boundary-m10-b3c3-rigid-four-block-split}
Take the cyclic sector \(X_6^{\mathrm{cyc}}\) at \(m=6\) and the \(B_3\) normalized root dictionary from Table \ref{tab:fold6_b3c3_root_dictionary_b3}:
\(\psi_B:X_6^{\mathrm{cyc}}\to\Phi(B_3)\).
Then the \(m=10\) boundary layer \(X_{10}^{\mathrm{bdry}}\) can be canonically labeled as ``\(18\) root directions \(\,+\,3\) Cartan directions'': for any \(b\in X_{10}^{\mathrm{bdry}}\), let \(v=\iota_6^{-1}(b)\in X_6\); if \(v\in X_6^{\mathrm{cyc}}\) then assign the root vector \(\psi_B(v)\); if \(v\in X_6^{\mathrm{bdry}}\) then mark it as a Cartan direction.
The Cartan directions correspond precisely to the three words of \(X_6^{\mathrm{bdry}}\); and the \(18\) root directions split rigidly as
\[
18=6\ (\mathrm{short})\ +\ 6\ (A_2(+))\ +\ 6\ (A_2(-)).
\]
More specifically, the long root set splits into two disjoint \(A_2\) root systems
\[
\Phi_{\mathrm{long}}=\Phi_{A_2(+)}\sqcup\Phi_{A_2(-)},\qquad
\Phi_{A_2(+)}=\{\pm(e_1+e_2),\pm(e_1+e_3),\pm(e_2+e_3)\},
\]
\[
\Phi_{A_2(-)}=\{\pm(e_1-e_2),\pm(e_1-e_3),\pm(e_2-e_3)\}.
\]
The criterion depends only on the sign structure ``two nonzero components have the same/opposite sign,'' and thus introduces no additional choices; consequently \(X_6^{\mathrm{cyc}}\) further splits rigidly into
\[
X_6^{\mathrm{cyc}}=X_6^{\mathrm{short}}\ \sqcup\ X_6^{A_2(+)}\ \sqcup\ X_6^{A_2(-)},
\qquad 18=6+6+6,
\]
and this partition is pushed forward to \(X_{10}^{\mathrm{bdry}}\) via the uplift isomorphism of Theorem \ref{thm:boundary-shift4-uplift-isomorphism}.
The explicit word table, categories, and root vectors are generated by the script \texttt{\detokenize{scripts/exp_boundary_uplift_m10_b3c3_dictionary.py}} and summarized in Table \ref{tab:boundary_uplift_m10_dictionary}, thereby providing a finite, item-by-item verifiable labeling certificate.
Therefore the \(m=10\) boundary interface is fixed as the four-block structure
\(
6\ (\text{short})\ +\ 6\ (A_2(+))\ +\ 6\ (A_2(-))\ +\ 3\ (\text{Cartan})
\),
supplying hard targets for subsequent spectral gap alignment or block coupling analysis.
\end{theorem}
\input{sections/generated/tab_boundary_uplift_m10_dictionary_en}

\paragraph{Isotropy point unification criterion and CAP-II constant combination calibration}
Remark \ref{rem:sp6-minimal-continuous-envelope} has identified the isotropy scan of \(\Sigma(\theta)=\nabla^2P(\theta)\) as an auditable entry point for the \(C_3=\mathrm{Sp}(6)\) continuous envelope.
Combining the uplift isomorphism of Theorem \ref{thm:boundary-shift4-uplift-isomorphism} with the root direction partition of Theorem \ref{thm:boundary-m10-b3c3-rigid-four-block-split} and this scan yields a stronger unification criterion: at a certain ``unification point'' \(\theta^*\), the effective lengths of root vectors should depend only on root type (short/long), not on the specific direction; therefore near \(\theta^*\), the \(21\) boundary modes at \(m=10\) should exhibit the simplest splitting
\[
12\ (\text{long roots, comprising }A_2(+)\oplus A_2(-))\ \oplus\ 6\ (\text{short roots})\ \oplus\ 3\ (\text{Cartan}),
\]
and under the metric \(g=\Sigma(\theta)^{-1}\) satisfy
\[
R_{ls}(\theta):=\sqrt{\frac{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{\mathrm{long}}}}{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{\mathrm{short}}}}}
\approx \sqrt{2},\qquad
R_{pm}(\theta):=\sqrt{\frac{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{A_2(+)}}}{\langle\|\alpha\|^2_g\rangle_{\alpha\in\Phi_{A_2(-)}}}}
\approx 1.
\]
The script \texttt{\detokenize{scripts/exp_sp6_isotropy_root_split_ratio.py}} directly reads the isotropy scan output
\texttt{\detokenize{artifacts/export/sync_kernel_weighted_sp6_isotropy_scan.json}} and computes \(R_{ls},R_{pm}\), thereby grounding the above criterion in verifiable numerical output.
\par
Furthermore, combining Remark \ref{rem:lapse-modulated-tau-mix}'s external time rescaling proxy \(N=\kappa_0/\kappa\) (equivalently taking the primary scalar variable \(u:=\log(\kappa/\kappa_0)=-\log N\)) with the CAP-II Fisher-amplitude variable (denoted \(\chi_F\) to avoid confusion with the arity-charge \(\chi\) of this paper),
if one sets \(\rho=\rho_0 e^{p u}\) and \(\chi_F=\sqrt{\rho}\), then
\[
|\nabla\chi_F|^2=\rho_0\,\frac{p^2}{4}\,e^{p u}\,|\nabla u|^2.
\]
At the isotropy point \(\Sigma(\theta^*)\simeq\sigma^2 I\), the second-order fluctuations yield a quadratic form scale read off from \(\sigma^2\), thereby providing a computable constant combination calibration:
\(
\lambda_F\,\rho_0\,\frac{p^2}{4}\ \approx\ \sigma^2
\)
(\(\sigma^2\) is given by the eigenvalues of \(\Sigma(\theta^*)\)). This directly binds the ``unification criterion'' and the effective scale of CAP-II to the same auditable object.

\paragraph{\texorpdfstring{$C_6$}{C6} orbit rigidity: from intrinsic cyclic rotation partition to a Pati--Salam-type Levi skeleton candidate}
The preceding paragraph uses the \(18\oplus 3\) counting interface of \(\,X_6=X_6^{\mathrm{cyc}}\sqcup X_6^{\mathrm{bdry}}\) to lock in the rank-\(3\) simple Lie type seed.
The same window \(m=6\) also carries an intrinsic symmetry action induced solely by the \emph{cyclic structure}, independent of ``root length'': let
\[
\rho(u_1u_2u_3u_4u_5u_6):=u_2u_3u_4u_5u_6u_1,
\]
then \(\rho\) generates the \(C_6\) cyclic rotation action.
Since \(X_6^{\mathrm{cyc}}\) is equivalent to the set of closed words satisfying ``no adjacent \(11\)'' and \(u_1u_6=0\), the \(C_6\) action is closed on \(X_6^{\mathrm{cyc}}\).
The orbit decomposition under this action is rigid at window-6:
\input{sections/generated/eq_window6_c6_orbit_decomposition_en}
\input{sections/generated/tab_window6_c6_orbit_decomposition_en}
From the perspective of permutation representations, the orbit multiset \(\{1,2,3,6,6\}\) also uniquely determines the \(C_6\)-module isomorphism type of the complex permutation module \(\CC[X_6^{\mathrm{cyc}}]\) and its linear character multiplicities (i.e., the Fourier signature of cyclic rotation):
\input{sections/generated/eq_window6_c6_character_decomposition_en}
Therefore, for any observable \(f:X_6^{\mathrm{cyc}}\to\CC\), the available dimensions of its six eigenmodes \(\chi_k\) under cyclic rotation \(\rho\) are rigidly locked by the vector \((m_k)\).
\par
This Fourier signature is determined by the orbit data, and hence constitutes a rigid invariant independent of the \(B_3/C_3\) root dictionary (weight stratification and the \(|W|=48\) orbit verification) described above: any generator alignment or observation channel alignment must simultaneously satisfy both classes of constraints.
\par
Note that the last line of the above gives a \(15\oplus 3\) subdivision by orbit size threshold, and the \(3\)-dimensional small block supports only the \(k\in\{0,3\}\) Fourier modes; however, to construct a dictionary aligned orbit-by-orbit with the Pati--Salam adjoint block, the following paragraph adopts a different \(15\oplus 3\) subdivision anchored by the \emph{unique three-element orbit}.
Therefore the cyclic sector not only provides the \(18\)-dimensional ``root-type'' interface but also forces an auditable set of \(C_6\)-invariant blocks at the orbit level.
Let \(\mathcal{O}(w):=\{\rho^k(w):k=0,1,\dots,5\}\subseteq X_6^{\mathrm{cyc}}\).
By Table \ref{tab:window6_c6_orbit_decomposition}, there is a unique three-element orbit \(\mathcal{O}(\texttt{001001})\), and the union of the remaining four orbits forms a \(15\)-dimensional block:
\[
X^{\mathrm{cyc},3}_6:=\mathcal{O}(\texttt{001001}),\qquad
X^{\mathrm{cyc},15}_6:=\mathcal{O}(\texttt{000000})\sqcup \mathcal{O}(\texttt{010101})\sqcup \mathcal{O}(\texttt{000001})\sqcup \mathcal{O}(\texttt{000101}).
\]
Then \(|X^{\mathrm{cyc},15}_6|=15\), \(|X^{\mathrm{cyc},3}_6|=3\).
Combined with \(|X_6^{\mathrm{bdry}}|=3\), one obtains a three-block dimensional skeleton derived entirely from the intrinsic \(m=6\) action:
\[
21=15\oplus 3\oplus 3.
\]
Under the minimal framework allowing only compact semisimple Lie algebras as continuous envelopes, this dimensional partition locks the candidate Levi skeleton to
\[
\boxed{\;\mathfrak g_{6}^{\mathrm{Levi}}\ \cong\ \mathfrak{su}(4)\oplus\mathfrak{su}(2)_L\oplus\mathfrak{su}(2)_R\;}\qquad(\dim=15+3+3=21),
\]
i.e., a Pati--Salam-type unification skeleton (here as a purely structural candidate, without introducing additional referents).
Under this skeleton, the orbit dimensions from Table \ref{tab:window6_c6_orbit_decomposition} can be organized into an ``adjoint block dictionary'':
\begin{itemize}
  \item The \(1\)-dimensional orbit (\texttt{000000}) can be aligned with the \(\mathfrak{u}(1)\) direction in \(\mathfrak{su}(4)\) that commutes with \(\mathfrak{su}(3)\);
  \item The \(2\)-dimensional orbit (\texttt{010101}) aligns with the Cartan subalgebra of \(\mathfrak{su}(3)\) (\(\dim=2\));
  \item The two \(6\)-dimensional orbits align respectively with the off-diagonal step directions of \(\mathfrak{su}(3)\) (\(\dim=6\)) and the bridging directions of
  \(\mathfrak{su}(4)/(\mathfrak{su}(3)\oplus\mathfrak{u}(1))\) (\(\mathbf{3}\oplus\bar{\mathbf{3}}\), \(\dim=6\));
  \item The remaining \(3\)-dimensional orbit in the cyclic sector (\texttt{001001}) aligns with an \(\mathfrak{su}(2)_L\) adjoint block; the boundary sector's \(3\) then corresponds to the \(\mathfrak{su}(2)_R\) adjoint block.
\end{itemize}
Furthermore, the internal \(1\oplus 2\oplus 6\oplus 6\) orbit sizes of the \(15\)-dimensional block precisely align at the ``Cartan/step/bridge'' dimensional level with the adjoint decomposition of
\(
\mathfrak{su}(4)\supset \mathfrak{su}(3)\oplus \mathfrak{u}(1)
\),
providing auditable targets for distinguishing the two unification seeds (\(B_3/C_3\) vs.\ Levi-\(SU(4)\times SU(2)\times SU(2)\)) at higher resolution using twisted spectra/anomaly signatures.

\paragraph{\texorpdfstring{$9+6+3+3$}{9+6+3+3} four-block decomposition: upgrading the \texorpdfstring{$21$}{21}-dimensional interface to a Pati--Salam boson counting seed}
The weight histogram of the window-6 cyclic sector is \(\{0:1,1:6,2:9,3:2\}\), and the boundary sector is the triple \(X_6^{\mathrm{bdry}}\).
Therefore, \emph{without introducing any additional structure}, \(X_6\) is also forced into a finer, fully enumerable four-block decomposition:
\[
X_6
=X^{(2)}_6\ \sqcup\ X^{(1)}_6\ \sqcup\ X^{(L)}_6\ \sqcup\ X^{(R)}_6,
\qquad
21=9+6+3+3,
\]
where
\(
X^{(2)}_6=\{w\in X_6^{\mathrm{cyc}}:\mathrm{wt}(w)=2\}
\),
\(
X^{(1)}_6=\{w\in X_6^{\mathrm{cyc}}:\mathrm{wt}(w)=1\}
\),
\(
X^{(L)}_6=\{w\in X_6^{\mathrm{cyc}}:\mathrm{wt}(w)\in\{0,3\}\}
\),
\(
X^{(R)}_6=X_6^{\mathrm{bdry}}
\);
the explicit word tables for the four blocks are given in Table \ref{tab:window6_patisalam_9633_partition}.
\input{sections/generated/tab_window6_patisalam_9633_partition_en}
\par
Treating these \(21\) stable types as ``gauge generator label candidates,'' this four-block counting aligns block by block at the pure dimensional level with the Pati--Salam boson decomposition
\[
21=(8+1)+6+3+3
\]
: \(9\) corresponds to the non-tuple counting interface of \(\mathfrak{su}(3)\oplus\mathfrak{u}(1)\), \(6\) corresponds to the bridging direction (\(\mathbf{3}\oplus\bar{\mathbf{3}}\)) counting interface of
\(\mathfrak{su}(4)/(\mathfrak{su}(3)\oplus\mathfrak{u}(1))\), and the two groups of \(3\) provide discrete anchors for two \(\mathfrak{su}(2)\) adjoint blocks (following the convention boundary=\(R\): \(X^{(R)}_6\) serves as the \(\mathfrak{su}(2)_R\) anchor, \(X^{(L)}_6\) as the other \(\mathfrak{su}(2)\) anchor).
Note that this four-block decomposition is compatible with the orbit-Levi skeleton above (since \(15=(9+6)\)), but it uses weight-layer alignment; therefore its word-to-generator alignment dictionary is generally not required to coincide with the orbit-alignment dictionary.
The emphasis here is that the conclusion is at the \emph{discrete counting and labeling level} of forcing; to promote it to a ``closed Lie algebra structure theorem'' still requires specifying a computable bracket source within the protocol framework of this paper and auditing the closure (see \S\ref{subsec:group-unification-audit-pointers} for the parallel verifiable interfaces of \(C_6\) orbit decomposition and the \(21=15\oplus 3\oplus 3\) Levi skeleton candidate).

\paragraph{The \texorpdfstring{$\delta=34$}{delta=34} double-layer covering of the boundary sector: intrinsic \texorpdfstring{$\ZZ_2$}{Z2} sheet parity}
Under the dyadic readout (the binary interval folding \(\mathrm{Fold}^{\mathrm{bin}}_6\) of Appendix \ref{app:fold-multiplicity}),
the window-6 uplift offset set is a hard arithmetic object, and types with last bit \(1\) are forced into exactly two layers:
\input{sections/generated/eq_fold6_bin_uplift_delta_set_en}
Since boundary words satisfy \(u_6=1\) (more strongly \(u_1=u_6=1\)), each \(w\in X_6^{\mathrm{bdry}}\) carries a canonical double-layer covering
\(
(\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w)=\{V_6(w),\,V_6(w)+34\}
\)
(with difference strictly \(34=F_9\)), inducing an unambiguous \(\ZZ_2\) grading (sheet parity):
\[
\pi_{\mathrm{sheet}}(V_6(w)):=0,\qquad \pi_{\mathrm{sheet}}(V_6(w)+34):=1.
\]
This \(\ZZ_2\) grading arises from the hard constraints of the dyadic readout and uplift offsets, and therefore cannot be recovered from the \(C_6\) orbit data or the weight stratification of the \(B_3/C_3\) root dictionary; it provides an independent discrete selection channel at the protocol level, which can be used in parallel with subsequent coupling/projection gate audit conditions.
Furthermore, if one views the double-layer preimage of the boundary sector under the dyadic uplift as a \emph{lifted boundary seed}
\[
\widetilde X_6^{\mathrm{bdry}}
:=\bigsqcup_{w\in X_6^{\mathrm{bdry}}}(\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w),
\]
then \(\pi_{\mathrm{sheet}}\) gives a canonical double-layer decomposition
\[
\begin{aligned}
\widetilde X_6^{\mathrm{bdry}}&=\widetilde X^{\mathrm{bdry}}_{6,+}\ \sqcup\ \widetilde X^{\mathrm{bdry}}_{6,-},\\
\widetilde X^{\mathrm{bdry}}_{6,+}&:=\{n\in\widetilde X_6^{\mathrm{bdry}}:\ \pi_{\mathrm{sheet}}(n)=0\},\\
\widetilde X^{\mathrm{bdry}}_{6,-}&:=\{n\in\widetilde X_6^{\mathrm{bdry}}:\ \pi_{\mathrm{sheet}}(n)=1\},\\
|\widetilde X^{\mathrm{bdry}}_{6,\pm}|&=3.
\end{aligned}
\]
Thus under the parity-graded framework, the window-6 seed not only carries the \(21=15\oplus 3\oplus 3\) dimensional skeleton but is also accompanied by an intrinsic \(3_+\sqcup 3_-\) double-layer channel (given by the lift structure, not determined by orbit/weight structure).
The explicit preimage pairs of the three boundary words are given in Table \ref{tab:fold6_boundary_sheet_pairs}.
\input{sections/generated/tab_fold6_boundary_sheet_pairs_en}
As a cross-resolution robustness check, under the same dyadic folding framework, the uplift structure of the boundary sector is promoted from the two-layer at \(m=6\) to three layers at \(m=7,8\) (the corresponding \(\delta\) combinations are shown in Table \ref{tab:foldbin_boundary_lift_m6_m8}).
Therefore the \(\ZZ_2\) sheet parity of window-6 is a hard structural fact \emph{specific to the minimal window} under this readout; to extend it as a cross-\(m\) residual selection law requires additional protocol gates/projection constraints to re-lock the double-layer covering at higher resolutions (this requirement is itself auditable and falsifiable).
\input{sections/generated/tab_foldbin_boundary_lift_m6_m8_en}

\paragraph{Geometric stabilizer of window-6: the unique surviving \texorpdfstring{$\ZZ_2$}{Z2} under \texorpdfstring{$\mathrm{Aut}(Q_6)$}{Aut(Q6)}}
The \(\delta=34\) double-layer covering and sheet parity described above are first and foremost an \emph{arithmetic} fact forced by the dyadic uplift.
To interface this fact with the geometric symmetries of the underlying microstates, we view \(\mathrm{Fold}^{\mathrm{bin}}_6\) as a labeling function on the vertex set of the \(6\)-dimensional hypercube, and introduce the following ``geometric stabilizer'' under hypercube graph automorphisms.\par
Let \(\Omega_6=\{0,1\}^6\) be the vertex set of the \(6\)-dimensional hypercube, and let \(Q_6\) be its graph structure with Hamming distance \(1\).
Its graph automorphism group is
\(
\mathrm{Aut}(Q_6)\cong (\ZZ_2)^6\rtimes S_6
\).
Define the binary encoding
\[
\mathrm{int}_6(\omega_1\cdots\omega_6):=\sum_{k=1}^6 \omega_k\,2^{6-k}\in\{0,\dots,63\},
\]
and restrict the binary interval folding (Definition \ref{def:fold-bin}) to the vertex set:
\[
F_6:\Omega_6\to X_6,\qquad F_6(\omega):=\mathrm{Fold}^{\mathrm{bin}}_6\bigl(\mathrm{int}_6(\omega)\bigr).
\]
Define its \textbf{geometric stabilizer}
\[
\mathrm{Stab}_{\mathrm{geo}}(F_6)
:=\bigl\{\sigma\in \mathrm{Aut}(Q_6):\ F_6\circ\sigma=F_6\bigr\}.
\]
Note: this group is a subgroup of \(\mathrm{Aut}(Q_6)\), \emph{distinct from} the fold-gauge group
\(\mathcal{G}^{\mathrm{bin}}_6=\mathrm{Aut}(\mathrm{Fold}^{\mathrm{bin}}_6)\) (Definition \ref{def:fold-bin-gauge-group}) generated by arbitrary permutations within fibers.\par

\begin{theorem}[Geometric rigidity and the unique nontrivial symmetry of window-6]\label{thm:foldbin6-geo-stabilizer-z2}
The geometric stabilizer \(\mathrm{Stab}_{\mathrm{geo}}(F_6)\) is a group of order \(2\).
Besides the identity, the unique nontrivial element can be taken as the involution
\(\sigma_{\mathrm{geo}}\in\mathrm{Aut}(Q_6)\), given in coordinates by ``swap bits \(1\) and \(5\) and simultaneously complement both'':
\input{sections/generated/eq_foldbin6_geo_stabilizer_en}
That is,
\(
\mathrm{Stab}_{\mathrm{geo}}(F_6)=\{\mathrm{id},\sigma_{\mathrm{geo}}\}\cong\ZZ_2
\).
\end{theorem}
\begin{proof}
\(\mathrm{Aut}(Q_6)\) is a finite group with \(|\mathrm{Aut}(Q_6)|=2^6\cdot 6!=46080\), so \(\mathrm{Stab}_{\mathrm{geo}}(F_6)\) can be completely verified by finite exhaustion.
The script \texttt{\detokenize{scripts/exp_foldbin6_geo_stabilizer.py}} enumerates \(\mathrm{Aut}(Q_6)\) and filters for elements satisfying \(F_6\circ\sigma=F_6\), outputting the audit certificate
\texttt{\detokenize{artifacts/export/foldbin6_geo_stabilizer.json}} and the generated snippet for the main text
\texttt{\detokenize{sections/generated/eq_foldbin6_geo_stabilizer.tex}}.
The exhaustive certificate shows the stabilizer has size \(2\), with the nontrivial element being unique and equal to the stated involution. \qed
\end{proof}

\begin{corollary}[Geometric origin of \texorpdfstring{$34=100010_2$}{34=100010_2}: half-cube support and \texorpdfstring{$\pm 34$}{+-34} offset]\label{cor:foldbin6-geo-mask-34}
Let \(\sigma_{\mathrm{geo}}\) be as in Theorem \ref{thm:foldbin6-geo-stabilizer-z2}.
Then for any \(\omega=(\omega_1,\dots,\omega_6)\in\Omega_6\),
\[
\omega_1\neq \omega_5\ \Longrightarrow\ \sigma_{\mathrm{geo}}(\omega)=\omega,
\qquad
\omega_1=\omega_5\ \Longrightarrow\ \mathrm{int}_6\!\bigl(\sigma_{\mathrm{geo}}(\omega)\bigr)-\mathrm{int}_6(\omega)\in\{\pm 34\}.
\]
In other words, the nontrivial action of \(\sigma_{\mathrm{geo}}\) is supported precisely on the half-cube \(\{\omega:\omega_1=\omega_5\}\), and carries the mask integer \(34=2^5+2^1=100010_2\) in binary encoding.
Therefore the \(\delta=34\) uplift/double-layer covering in window-6 (described above) is not merely an arithmetic tail offset, but also the integer fingerprint carried by the unique surviving nontrivial geometric symmetry of \(\mathrm{Aut}(Q_6)\).
\end{corollary}
\begin{proof}
For \(\sigma_{\mathrm{geo}}(\omega_1,\omega_2,\omega_3,\omega_4,\omega_5,\omega_6)=(1-\omega_5,\omega_2,\omega_3,\omega_4,1-\omega_1,\omega_6)\), proceed coordinate by coordinate.
If \(\omega_1\neq\omega_5\), then \((\omega_1,\omega_5)\in\{(0,1),(1,0)\}\); substituting gives \((1-\omega_5,1-\omega_1)=(\omega_1,\omega_5)\), so the point is fixed.
If \(\omega_1=\omega_5\), then both bits are simultaneously flipped, and the binary weights are \(2^5\) and \(2^1\), so the encoding difference is necessarily \(\pm(2^5+2^1)=\pm 34\).
\qed
\end{proof}

\paragraph{\texorpdfstring{$F_9$}{F9} gateway locking: \texorpdfstring{$34$}{34} is simultaneously the window-6 uplift offset and the \texorpdfstring{$m=6\to 8$}{m=6->8} resonance gap}
Table \ref{tab:fibonacci_lie_resonance_ladder} gives the next stable type dimension resonance point as \(m=8\), with
\(
|X_6|=21,\ |X_8|=55
\).
Therefore the gap dimension is strictly
\[
|X_8|-|X_6|=55-21=34=F_9.
\]
Combined with the hard constraint \(w_6=1\Rightarrow \Delta(w)=\{0,34\}\) from above, one obtains an auditable ``gateway'' fact:
the boundary sector (more strongly, the endpoint fiber \(X_6^{\mathrm{bdry}}\subset\{w:w_6=1\}\)) can only be paired and lifted via \(\delta=34\) under the dyadic uplift,
and \(\delta=34\) is precisely the dimensional gap from \(m=6\) to the next resonance layer \(m=8\).
This grounds any interpretive semantics such as ``right-handed/high-energy/broken'' (should the reader choose to adopt them) in a purely discrete lock: the available set of \(\delta\) and the resonance gap coincide at the same integer \(34\).

\paragraph{The \texorpdfstring{$4\to 3\to 2$}{4->3->2} collapse of available tail offsets: discrete breaking switch induced by resolution truncation}
For any \(w\in X_6\), define the uplift-delta set and its cardinality
\[
\Delta(w):=\{\,n-V_6(w): n\in (\mathrm{Fold}^{\mathrm{bin}}_6)^{-1}(w)\,\},\qquad
\Xi(w):=|\Delta(w)|.
\]
Under the finite-domain truncation \(n\in\{0,\dots,63\}\) of window-6, \(\Xi(w)\) is forced to collapse to three discrete values \(\{4,3,2\}\), and the trigger condition depends only on the last bit \(w_6\) and the threshold \(V_6(w)\le 8\) (Table \ref{tab:fold6_bin_uplift_choice_collapse} gives the full word-table partition):
\input{sections/generated/tab_fold6_bin_uplift_choice_collapse_en}
\par
Note that these three numbers \(4,3,2\) exactly match the minimal internal dimensions of the fundamental representations of \(SU(4),SU(3),SU(2)\).
Therefore ``unification and breaking'' can be read in this framework as a prototype of a purely combinatorial mechanism: \emph{resolution truncation causes a reduction in available tail offsets (collapse of internal label state counts)},
without the need to write in an external continuous order parameter a priori; and \(\Xi(w)\) itself can serve as an auditable discrete order parameter (see \S\ref{subsec:group-unification-audit-pointers} for the audit chain of window-6 \(C_6\) orbits and dyadic uplift, and its JSON certificate \texttt{\detokenize{artifacts/export/window6_c6_orbit_patisalam_seed.json}}).

\begin{remark}[Three-channel potential--response symplectic phase space and the \texorpdfstring{$C_3=\mathrm{Sp}(6)$}{C3=Sp(6)} continuous envelope]\label{rem:sp6-minimal-continuous-envelope}
Under the vector potential framework of Appendix \ref{app:vector-potential}, given a three-dimensional potential parameter \(\theta=(\theta_1,\theta_2,\theta_3)\), the pressure
\(
P(\theta)=\log\rho(B(\theta))
\)
can be entirely read off from the spectral radius of a finite matrix.
For each channel \(i\in\{1,2,3\}\), define the response (density/current)
\[
J_i(\theta):=\partial_{\theta_i}P(\theta),
\]
then \((\theta;J)\in\RR^6\) gives a natural ``potential--response phase space'' carrying the standard symplectic form
\[
\omega:=\sum_{i=1}^{3}\dd\theta_i\wedge \dd J_i.
\]
The minimal continuous linear group that allows three-channel mixing while preserving the conjugate structure (i.e., preserving \(\omega\)) is
\[
\mathrm{Aut}(\omega)=\mathrm{Sp}(6),
\]
which corresponds to \(C_3\) (the ``symplectic side'' of the long/short root dual type) in the \(B_3/C_3\) discrete unification seed of this paper.
More importantly, this continuous envelope aligns precisely in generator counting with the discrete \(m=6\) anchor:
\[
\dim\mathfrak{sp}(6)=3(2\cdot 3+1)=21=|X_6|.
\]
Therefore, when interpreting the \(21\)-dimensional counting interface at \(m=6\) as the minimal continuous envelope of a rank-\(3\) unification algebra, \(\mathrm{Sp}(6)\) is a candidate intrinsically forced by the ``potential--response'' coordinate structure, rather than an external guess.
An auditable numerical check of this envelope (via the ``single-scale/isotropy'' degree scan of \(\nabla^2P(\theta)\) with \((u,v,w)=(e^{\theta_1},e^{\theta_2},e^{\theta_3})\)) is given in Appendix Remark \ref{rem:sync-kernel-3d-sp6-isotropy-scan} and Table \ref{tab:sync_kernel_weighted_sp6_isotropy_scan}.
\end{remark}

\paragraph{The $9+12$ Cartan decomposition of \texorpdfstring{$\mathrm{Sp}(6)$}{Sp(6)}: the \texorpdfstring{$U(3)$}{U(3)} compact core and rigid alignment with SM=12}
In this section, \(\mathrm{Sp}(6)\) appears in the context of the real symplectic group \(\mathrm{Sp}(6,\RR)\), whose maximal compact subgroup is \(U(3)\), with the standard Cartan-type decomposition
\[
\mathfrak{sp}(6,\RR)=\mathfrak{u}(3)\oplus \mathfrak{p},\qquad \dim\mathfrak{u}(3)=9,\ \dim\mathfrak{p}=12.
\]
Therefore, without introducing any new parameters, \(\dim\mathfrak{sp}(6)=21\) comes with a rigid \(9+12\) dimensional splitting:
the \(9\)-dimensional compact core can be interpreted as the three-channel basis mixing/channel rotation degrees of freedom,
and the \(12\)-dimensional noncompact complement aligns in counting with the SM boson count \(1+3+8=12\) from the boundary three-layer tower.
Combined with the rigid fingerprint ``\(21-12=9\)'' from Remark \ref{rem:fib-lie-extra-9},
this \(U(3)\) compact core provides an intrinsic candidate for ``hidden symmetry/family-type mixing,''
and supplies a natural \(9\) vs.\ \(12\) channel partition for subsequent spectral gap alignment block checks (see \S\ref{subsec:group-unification-spectral-alignment}).

\paragraph{Zeckendorf--GUT signature: unification algebra \texorpdfstring{$\leftrightarrow$}{<->} resolution layer combination}
For any candidate unification Lie algebra \(\mathfrak g\), let \(D:=\dim\mathfrak g\) (i.e., the total number of gauge bosons).
Write the Zeckendorf unique decomposition as
\[
D=\sum_{k\in S}F_k,\qquad S\ \text{contains no adjacent indices},
\]
and define the \textbf{resolution signature}
\[
\Sigma(\mathfrak g):=\{\,m=k+2:\ k\in S\,\}.
\]
Since \(F_k=|X^{\mathrm{bdry}}_{k+2}|\), this is equivalent to
\[
D=\sum_{m\in\Sigma(\mathfrak g)}|X_m^{\mathrm{bdry}}|,
\]
and by Zeckendorf uniqueness, \(\Sigma(\mathfrak g)\) is also the \emph{unique} (redundancy-free activation) resolution layer set.
If one interprets ``observable gauge modes'' as a subtower of boundary layers \(X_m^{\mathrm{bdry}}\) activated under the protocol kernel/projection gates,
then \(\Sigma(\mathfrak g)\) provides a falsifiable dictionary: one need only determine within the adopted auditable protocol which \(m\) participate in the observable action to place the observation into the corresponding unification signature.

\input{sections/generated/tab_zeckendorf_gut_signatures_en}

\begin{corollary}[Discrete correspondence between the three tail offsets of the window-\(6\) uplift and the top Zeckendorf terms of GUT]\label{cor:fold6-tail-offsets-gut-top-terms}
At window \(m=6\), the uplift preimage of any \(w\in X_6\) (under finite-domain truncation or dyadic readout) can only superpose three types of nonzero offsets on the Zeckendorf value
\(
V_6(w)=\sum_{k=1}^{6}w_kF_{k+1}
\):
\[
\Delta_{\mathrm{tail}}(6)\subseteq \{F_8,F_9,F_{10}\}=\{21,34,55\}
\]
(this set can be verified item by item from the dyadic uplift enumeration certificate; see \S\ref{subsec:group-unification-audit-pointers}.)
On the other hand, from Table \ref{tab:zeckendorf_gut_signatures},
\[
24=F_8+F_4,\qquad 45=F_9+F_6+F_4,\qquad 78=F_{10}+F_8+F_3
\]
show that the highest Fibonacci terms in the Zeckendorf--GUT decompositions of \(SU(5),SO(10),E_6\) are \(F_8,F_9,F_{10}\) respectively.
Therefore, retaining only the ``top trigger term,'' the first uplift at window-\(6\) possesses a natural \textbf{discrete three-branch} structure:
choosing \(\,F_8/F_9/F_{10}\,\) respectively aligns with the top term of the \(SU(5)/SO(10)/E_6\) type candidate.
\end{corollary}

\paragraph{Minimal prediction (discrete jumps, not continuously tunable)}
Once one accepts the interpretation ``gauge bosons = boundary word tower mode numbers,'' the SM signature is
\(\Sigma_{\mathrm{SM}}=\{4,6,8\}\).
Then ``gauge modes beyond the SM'' are no longer continuously tunable parameters but must make discrete jumps via Fibonacci boundary layers:
the minimal upward jump is to \(m=9\) (adding \(F_7=13\) modes) or \(m=10\) (adding \(F_8=21\) modes).
The \(21\) modes at \(m=10\) allow a \(21-(8+1)=12\) remainder splitting isomorphic to the \(SU(5)\) heavy boson count,
providing a direct target for subsequently operationalizing the ``projection/compensation from higher boundary tower to the lower three-tower'' as an operator-level closure.

\paragraph{Family number \(N_f\) and the hard correspondence with the window-6 uplift three-branch: \(F_8/F_9/F_{10}\)}
Taking the standard counting of \(15\) per generation \(SU(5)\)-type matter block as external input, define the total count
\(
M(N_f):=15N_f
\).
Then for \(N_f\in\{2,3,4\}\), the highest Zeckendorf term of \(M(N_f)\) is successively \(F_8,F_9,F_{10}\) (Table \ref{tab:window6_family_uplift_lock}).
Combined with the ``window-6 uplift discrete three-branch'' correspondence of Corollary \ref{cor:fold6-tail-offsets-gut-top-terms},
the family number \(N_f\) is promoted at the window-6 level to a \emph{branch selection parameter}:
in particular, \(N_f=3\) precisely selects the \(F_9=34\) branch, thereby discretely locking in the \(SO(10)\) top-term-aligned branch at window-6 as the natural unification candidate.
\input{sections/generated/tab_window6_family_uplift_lock_en}
\par
The key point of this conclusion is: it translates \(N_f\) from a traditional external input into a discrete selection quantity auditable by the Zeckendorf--uplift structure within this framework.

\paragraph{Three-channel singlet slots of the window-6 boundary sector: the rigid completion \(45+3=3\times 16\)}
The boundary sector cardinality of window-6 is rigidly fixed by the golden syntax as \(|X_6^{\mathrm{bdry}}|=F_4=3\).
Therefore at \(N_f=3\), the standard \(SU(5)\) matter count \(15N_f=45\) and the boundary channel count give a discrete completion identity (generated by script):
\input{sections/generated/eq_window6_family_singlet_completion_en}
Here \(16\) can be read as the dimension of the \(SO(10)\) spinor representation (which under the embedding \(SU(5)\subset SO(10)\) decomposes as \(15\oplus 1\)),
so this identity provides an auditable slot interpretation: the ``extra one \(SU(5)\) singlet'' per generation (e.g., a right-handed neutrino-type channel) is naturally anchored in the boundary sector, rather than being a free addition from the cyclic/bulk stable states.
\par
\emph{Testable predictions:}
\begin{enumerate}
  \item If the observable coupling is required to be compatible with the sheet parity \(\pi_{\mathrm{sheet}}\) grading defined above, then bulk-boundary mixing terms must be realized via the \(\delta=34\) sheet flip, yielding a discrete selection rule (``\(\delta\)-parity'') requiring no continuous fine-tuning.
  \item Without lifting to higher windows, the number of independent boundary channels is strictly \(3\); therefore any construction requiring more than \(3\) independent singlet channels must enter resolution \(m\ge 7\) and accept the complete rewriting of the uplift-tail structure (e.g., the dyadic lift at \(m=7,8\) is no longer a two-layer covering, see Table \ref{tab:foldbin_boundary_lift_m6_m8}).
\end{enumerate}

\paragraph{The \(1+8+12\) discrete splitting within \texorpdfstring{$X_6$}{X6}: the \texorpdfstring{$SU(5)$}{SU(5)} light/heavy boson dictionary}
From the uplift-choice threshold of the dyadic folding (Table \ref{tab:fold6_bin_uplift_choice_collapse}), define
\[
S_{\mathrm{low}}:=\{w\in X_6:\ w_6=0,\ V_6(w)\le 8\},
\]
then \(|S_{\mathrm{low}}|=9\), and the unique zero word \(\texttt{000000}\) together with the remaining \(8\) nonzero words automatically gives a
\(
X_6=1\oplus 8\oplus 12
\)
discrete splitting:
\[
\begin{aligned}
X_6^{\mathrm{zero}}&:=\{\texttt{000000}\},\\
X_6^{\mathrm{light}}&:=\{\texttt{000010},\ \texttt{000100},\ \texttt{001000},\ \texttt{010000},\\
&\qquad \texttt{010100},\ \texttt{100000},\ \texttt{100100},\ \texttt{101000}\},\\
X_6^{\mathrm{heavy}}&:=\{\texttt{000001},\ \texttt{000101},\ \texttt{001001},\ \texttt{010001},\ \texttt{010101},\\
&\qquad \texttt{001010},\ \texttt{010010},\ \texttt{100010},\ \texttt{101010},\ \texttt{100001},\\
&\qquad \texttt{100101},\ \texttt{101001}\}.
\end{aligned}
\]
Via \(|X_{10}^{\mathrm{bdry}}|=|X_6|\) and the four-step shift (Remark \ref{rem:fib-lie-resonance-shift-by-4}),
the standard embedding \(u\mapsto 10\,u\,01\) lifts this dictionary back to the \(m=10\) boundary layer,
thereby upgrading ``\(21-(8+1)=12\)'' from a counting target to an enumerable light/heavy boson discrete dictionary.

\begin{corollary}[The \(21+3\) counting closure of \texorpdfstring{$SU(5)$}{SU(5)}: from the \(m=6\) unification seed to \(24\) slots]\label{cor:su5-21-plus-3-closure}
From Table \ref{tab:zeckendorf_gut_signatures}, the Zeckendorf decomposition of \(SU(5)\) is \(24=F_8+F_4\), with resolution signature
\(\Sigma(SU(5))=\{10,6\}\).
Using the four-step uplift isomorphism \(X_n\simeq X_{n+4}^{\mathrm{bdry}}\), this decomposition equivalently gives a stable layer counting closure:
\[
24=|X^{\mathrm{bdry}}_{10}|+|X^{\mathrm{bdry}}_{6}|=|X_{6}|+|X_{2}|=21+3.
\]
The stable layer \(|X_6|=21\) already carries the \(B_3/C_3\) unification seed and its rigid \(9+12\), \(1+8+12\) partitions, and therefore intrinsically contains the slot structure of ``light sector \((8+1)\) and heavy remainder \(12\)'' at the counting and partition level;
while \(|X_2|=3\) provides a minimal three-mode supplementary block isomorphic in dimension to the adjoint of \(\mathfrak{su}(2)\).
Therefore, without taking \(SU(5)\) as a prior principal group, the \(24\) boson count of \(SU(5)\) can be understood as: a minimal \(+3\) closure (``append closure'') on top of the rank-\(3\) seed at \(m=6\), without altering the intrinsic \(21\)-dimensional partition structure of that seed.
\end{corollary}

\paragraph{The $8+4$ two-tier structure of the heavy remainder \(12\) and entropy weight primitives}
Using the definition of the dyadic uplift count \(\Xi(w)=|\Delta(w)|\) (equivalently \(d^{\mathrm{bin}}_6(w)\)) from above,
Table \ref{tab:fold6_bin_uplift_choice_collapse} immediately gives:
\[
X_6^{\mathrm{heavy}}=X_{6,2}^{\mathrm{heavy}}\sqcup X_{6,3}^{\mathrm{heavy}},
\qquad |X_{6,2}^{\mathrm{heavy}}|=8,\ |X_{6,3}^{\mathrm{heavy}}|=4,
\]
where
\(
X_{6,2}^{\mathrm{heavy}}=\{w\in X_6^{\mathrm{heavy}}:w_6=1\}
\)
corresponds to \(\Xi(w)=2\), and
\(
X_{6,3}^{\mathrm{heavy}}=\{w\in X_6^{\mathrm{heavy}}:w_6=0,\ V_6(w)>8\}
\)
corresponds to \(\Xi(w)=3\).
Therefore the heavy bosons are not a single tier but intrinsically split into \(8\oplus 4\) two tiers.
Introducing the entropy weight
\[
h(w):=\log \Xi(w)\in\{\log 2,\log 3,\log 4\},
\]
this two-tier structure is directly encoded as a discrete primitive; within the cyclic subset \(X_6^{\mathrm{cyc}}\),
\(h(w)\) further cuts out a \(9\oplus 4\oplus 5\) three-tier grading (corresponding to \(\Xi=4,3,2\); still enumerated from Table \ref{tab:fold6_bin_uplift_choice_collapse}),
providing an auditable channel for refining the \(B_3/C_3\) root-like directions into a ``weighted root system'' and serving as partition weights for spectral gap alignment (see \S\ref{subsec:group-unification-spectral-alignment}).

\paragraph{Dual-scale unification selection law from the bin-fold degeneracy spectrum: SM=12 and \texorpdfstring{$\dim=55$}{dim=55} locked via two routes}
For the binary interval folding \(\mathrm{Fold}^{\mathrm{bin}}_m:\{0,\dots,2^m-1\}\to X_m\) (Definition \ref{def:fold-bin}),
denote the degeneracy (fiber size)
\[
d^{\mathrm{bin}}_m(w):=\bigl|\mathrm{Fold}^{\mathrm{bin}}_m{}^{-1}(w)\bigr|,\qquad w\in X_m.
\]
Under this framework, one can state a set of interfaced conclusions that depend not on externally imposed ``decomposition aesthetics'' but solely on the enumerable degeneracy spectrum:
\begin{enumerate}
  \item \textbf{(Non-artificial normalization of the SM: \(12=8+4\) read off from the rigid stratification at \(m=6\))}
  By Corollary \ref{cor:fold-bin-m6-fiber-hist}, at \(m=6\) one has \(d^{\mathrm{bin}}_6(w)\in\{2,3,4\}\), with
  \(\#\{d^{\mathrm{bin}}_6=2\}=8\), \(\#\{d^{\mathrm{bin}}_6=3\}=4\), \(\#\{d^{\mathrm{bin}}_6=4\}=9\).
  Define the low-degeneracy (most rigid) sector
  \[
  X^{\mathrm{rig}}_6:=\{w\in X_6:\ d^{\mathrm{bin}}_6(w)\le 3\},
  \]
  then
  \[
  \boxed{\ |X^{\mathrm{rig}}_6|=8+4=12\ }.
  \]
  Thus ``the observable gauge degree of freedom count of the SM is \(12\)'' can, without invoking the boundary three-layer tower decomposition,
  be directly read off from the rigid degeneracy spectrum of the bin-fold (independently of the \(1+3+8\) boundary tower counting interface described earlier in this section).

  \item \textbf{(Dark capacity completion for the unification dimension anchor: \(12+(64-21)=55\))}
  Define the ghost microstates capacity at \(m=6\):
  \[
  H^{\mathrm{bin}}_6:=2^6-|X_6|
  =\sum_{w\in X_6}\bigl(d^{\mathrm{bin}}_6(w)-1\bigr)
  =43.
  \]
  Let
  \[
  D^{\mathrm{bin}}_{\mathrm{GUT}}:=|X^{\mathrm{rig}}_6|+H^{\mathrm{bin}}_6,
  \]
  then one obtains a unification dimension candidate locked entirely by the \(m=6\) degeneracy spectrum:
  \[
  \boxed{\ D^{\mathrm{bin}}_{\mathrm{GUT}}=12+43=55\ }.
  \]
  For dimensional comparison, \(55\) simultaneously equals \(\dim\mathfrak{so}(11)\) and \(\dim\mathfrak{sp}(10)\);
  therefore, if the framework is ``ghost capacity must be absorbed by post-unification generators,'' then \(\dim=55\) constitutes a directly falsifiable candidate anchor.
  \item \textbf{(Unification mechanism: two-level discrete resolution lift, not continuous breaking parameters)}
  Define the two-level rigid lift (tail-fixed) embedding
  \[
  \iota_m:X_m\hookrightarrow X_{m+2},\qquad
  \iota_m(w_1,\dots,w_m):=(w_1,\dots,w_m,0,1).
  \]
  This lift satisfies: \(\iota_m\) is a canonical injection, and its image falls in the subset with tail forced to \(01\),
  hence has minimal tail variability in the sense that ``last bit \(1\) forces the preceding bit to \(0\)''.
  Correspondingly, unification/breaking can be formulated as a discrete resolution flow: the lift \(\iota_m\) and truncation (deleting the last two bits) are one-sided inverses of each other,
  thereby placing the mechanism of ``high-energy unified state \(\to\) low-energy reduced state'' onto computable lift--truncation operators.
  \item \textbf{(High-resolution degeneracy spectrum selector: locking \(55\) again at \(m=10\))}
  For \(m\in\{8,10,12\}\), the bin-fold degeneracy takes only three discrete values, and the histogram at the type level is rigidly fixed as
  \[
  \begin{aligned}
  m=8:\ &\{3:21,\,5:11,\,6:23\},\\
  m=10:\ &\{5:55,\,8:52,\,9:37\},\\
  m=12:\ &\{8:144,\,12:85,\,13:148\},
  \end{aligned}
  \]
  with verifiable output in Table \ref{tab:fold_bin_degeneracy_spectrum_m6_m12}.
  In particular, at \(m=10\) the minimal degeneracy sector
  \[
  X^{\min}_{10}:=\Bigl\{w\in X_{10}:\ d^{\mathrm{bin}}_{10}(w)=\min_{u\in X_{10}} d^{\mathrm{bin}}_{10}(u)\Bigr\}
  \]
  satisfies
  \[
  \boxed{\ |X^{\min}_{10}|=55\ }.
  \]
  Thus \(\dim=55\) simultaneously appears via two independent readout routes:
  one from the ``SM low-degeneracy reduction \(12\)'' and ghost capacity \(43\) completion at \(m=6\),
  the other from the high-resolution most-rigid sector count at \(m=10\).
  \input{sections/generated/tab_fold_bin_degeneracy_spectrum_m6_m12_en}
\end{enumerate}




