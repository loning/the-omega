% (User input window)
% 2026-02-15 19:56:58（Asia/Singapore）
% According to an internal note dated 2026-02-14.

\begin{remark}[\texorpdfstring{$q=4$}{q=4} Minimal Five-Mode Opening: Difference-Mode Truncation and Hankel Rank First Alignment]\label{rem:pom-q4-minimal-five-mode-opening}
Under the "output-consistent + conjugate-pairing" symmetric quotient compilation regime, the net phase bias \((a-b)\) admissible range is truncated to
\(-\lfloor q/2\rfloor\le a-b\le \lfloor q/2\rfloor\),
thus typically one need only track \(2\lfloor q/2\rfloor+1\) difference modes to close the recurrence (see Remark \ref{rem:pom-closure-dim-mode-trunc} and the mode coordinate regime in Theorem \ref{thm:pom-moment-resonance}).
Hence \(q=4\) is the minimal order for this mode count to reach \(5\) (first appearance of \(\pm 2\) second-order difference channel).
\par
Simultaneously, under the verified integer-coefficient recurrence regime, the minimal recurrence order for fourth-order collision moment is \(d_4=5\) (Table \ref{tab:fold_collision_moment_spectrum_k2_8}), equivalently its Hankel rank/minimal realization dimension is also precisely \(5\) (Corollary \ref{cor:pom-moment-minreal}).
Thus the criticality of \(q=4\) can be strictly attributed to "second-order difference channel first becomes excitable and fully visible in sequence semantics", rather than an accidental numerical phenomenon.
\end{remark}

\begin{remark}[Arithmetic Entropy Barrier: Hankel Determinant Simultaneously Controls Volume Potential and \texorpdfstring{$p$}{p}-adic Recoverability]\label{rem:pom-hankel-arithmetic-entropy-barrier}
Let integer sequence \((a_n)\) have finite Hankel rank \(d\), and take invertible Hankel principal blocks
\[
H_0:=(a_{i+j})_{0\le i,j\le d-1},
\qquad
H_1:=(a_{i+j+1})_{0\le i,j\le d-1}.
\]
Under the minimal realization regime of Theorem \ref{thm:hankel-rank-minimal}, one can take a minimal linear realization such that the state transition matrix satisfies
\[
\boxed{\;
A=H_0^{-1}H_1=\frac{\mathrm{adj}(H_0)\,H_1}{\det(H_0)}\;}
\]
thus all rational denominators of the minimal realization are uniformly controlled by \(\det(H_0)\) in the divisibility sense.
Therefore for prime \(p\), its \(p\)-adic valuation
\[
v_p(\det(H_0))
\]
is not only the arithmetic shadow of bulk volume potential \(-\log|\det(H_0)|\), but also yields the minimal \(p\)-adic precision threshold required to unambiguously recover this minimal realization in \(\mathbb{Z}_p\) (denominator must be invertible).
\par
In Fold's Hankel witness blocks, this "arithmetic entropy axis" can be directly read out: Remark \ref{rem:pom-hankel-stiffness-p5} has recorded
\(
\det(H_{k=5})=2^{13}\cdot 3^5\cdot 5^7
\),
thus \(v_5(\det(H_{k=5}))=7\) forms a hard threshold at \(k=5\); it parallels the volume potential \(\log|\det(H_{k=5})|\) in the same table, jointly determining the total information budget for "auditable reconstruction".
\end{remark}

