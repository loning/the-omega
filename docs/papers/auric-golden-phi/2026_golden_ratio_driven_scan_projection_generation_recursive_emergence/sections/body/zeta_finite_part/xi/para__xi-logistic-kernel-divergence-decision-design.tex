% (User input window)
% 时间：2026-02-15 22:45:58（Asia/Singapore）
%
% This paragraph organizes the translation family of logistic scale kernels into a set of closed dictionaries that can be directly embedded in tomography and discriminant theory:
% First give some core \(f\) - a fully explicit expression of the divergence and its chain of local constants;
% The multi-category MAP decision, optimal layout, mutual information and Fisher loss induced by this kernel are then written into auditable formulas.

in proposition \ref{prop:xi-bulk-curvature-logistic-kernel-decomposition} In the Scale Curvature interface, depth atoms appear as a translation family of logistic kernels.
Therefore, the translation family \(\{\phi_a\}_{a\in\RR}\)'s divergence, decision error and information geometric constant can be regarded as the prototype ledger of "scale visibility threshold".

\begin{proposition}[Divergence dictionary of scale curvature kernel:\texorpdfstring{$1/4$}{1/4}、\texorpdfstring{$4$}{4}、\texorpdfstring{$1/3$}{1/3} homologous sealing]\label{prop:xi-logistic-divergence-dictionary}
make
\[
\phi(s)=\frac{1}{4\cosh^{2}(s/2)}=\frac{e^{s}}{(1+e^{s})^{2}},
\qquad
\phi_a(s):=\phi(s-a),
\qquad
\Delta:=b-a.
\]
Then for any \(a,b\in\RR\) The following divergences all have completely closed forms:
\begin{enumerate}
  \item \textup{(total variation distance)}
  \[
  \|\phi_a-\phi_b\|_{\mathrm{TV}}
  :=\frac12\int_{\RR}|\phi_a(s)-\phi_b(s)|\,\dd s
  =\tanh\!\Bigl(\frac{|\Delta|}{4}\Bigr).
  \]
  \item \textup{(Bayesian minimum error rate for binary equal priors)}
  \[
  P_{\mathrm e}(\Delta)
  =\frac{1-\|\phi_a-\phi_b\|_{\mathrm{TV}}}{2}
  =\frac{1}{1+e^{|\Delta|/2}}.
  \]
  \item \textup{(Hellinger Affinity vs. Squared Hellinger Distance)}
  \[
  \int_{\RR}\sqrt{\phi_a(s)\phi_b(s)}\,\dd s
  =\frac{|\Delta|}{2\sinh(|\Delta|/2)},
  \qquad
  H^{2}(\phi_a,\phi_b)
  :=1-\int_{\RR}\sqrt{\phi_a\phi_b}
  =1-\frac{|\Delta|}{2\sinh(|\Delta|/2)}.
  \]
  \item \textup{(Strict symmetry of Kullback--Leibler divergence)}
  \[
  D_{\mathrm{KL}}(\phi_a\|\phi_b)
  :=\int_{\RR}\phi_a(s)\log\frac{\phi_a(s)}{\phi_b(s)}\,\dd s
  =|\Delta|\coth(|\Delta|/2)-2,
  \qquad
  D_{\mathrm{KL}}(\phi_a\|\phi_b)=D_{\mathrm{KL}}(\phi_b\|\phi_a).
  \]
  \item \textup{（\(\chi^2\) Divergence and Rényi-\(2\) divergence)}
  \[
  \chi^2(\phi_a\|\phi_b)
  :=\int_{\RR}\frac{(\phi_a-\phi_b)^2}{\phi_b}\,\dd s
  =\frac{4}{3}\sinh^{2}(|\Delta|/2),
  \qquad
  D_2(\phi_a\|\phi_b)
  :=\log\!\int_{\RR}\frac{\phi_a(s)^2}{\phi_b(s)}\,\dd s
  =\log\!\Bigl(\frac{2\cosh|\Delta|+1}{3}\Bigr).
  \]
\end{enumerate}
and in \(|\Delta|\downarrow 0\) when there is a unified local quantization expansion
\[
\|\phi_a-\phi_b\|_{\mathrm{TV}}=\frac{|\Delta|}{4}+O(|\Delta|^{3}),
\qquad
H^{2}(\phi_a,\phi_b)=\frac{\Delta^{2}}{24}+O(\Delta^{4}),
\qquad
D_{\mathrm{KL}}(\phi_a\|\phi_b)=\frac{\Delta^{2}}{6}+O(\Delta^{4}),
\]
Thus a strict local proportionality law appears
\[
D_{\mathrm{KL}}(\phi_a\|\phi_b)=4\,H^{2}(\phi_a,\phi_b)+O(\Delta^{4}),
\qquad
\|\phi_a-\phi_b\|_{\mathrm{TV}}\sim\frac{|\Delta|}{4}.
\]
This constant chain fixes subsequent scale visibility thresholds to the same set of ineliminable universal constants.
\end{proposition}
\begin{proof}
Due to translation invariance, we only need to take \(a=0\)、\(b=\Delta>0\)。
remember \(F(s)=\int_{-\infty}^{s}\phi(u)\dd u=(1+e^{-s})^{-1}\) is the logistic distribution function.
because \(\phi\) is an even function with respect to \(0\) single peak,\(\phi(s)\) and \(\phi(s-\Delta)\) has the only intersection point \(s=\Delta/2\), so
\[
\|\phi-\phi_{\Delta}\|_{\mathrm{TV}}
=\int_{\Delta/2}^{\infty}\bigl(\phi(s-\Delta)-\phi(s)\bigr)\dd s
=F(\Delta/2)-F(-\Delta/2)
=\tanh(\Delta/4),
\]
and obtained by the binary Bayes formula \(P_{\mathrm e}(\Delta)\)。
\par
Affinity for Hellinger, write \(\phi(s)=e^{s}(1+e^{s})^{-2}\) and substitute \(x=e^{s}\)（\(\dd s=\dd x/x\))have to
\[
\int_{\RR}\sqrt{\phi(s)\phi(s-\Delta)}\,\dd s
=e^{-\Delta/2}\int_{0}^{\infty}\frac{1}{(1+x)(1+e^{-\Delta}x)}\,\dd x
=\frac{\Delta}{2\sinh(\Delta/2)}.
\]
right \(D_{\mathrm{KL}}\), similarly substituted to get
\[
D_{\mathrm{KL}}(\phi\|\phi_{\Delta})
=\int_{0}^{\infty}\frac{\Delta+2\log(1+e^{-\Delta}x)-2\log(1+x)}{(1+x)^{2}}\,\dd x.
\]
right
\(
I(\alpha):=\int_{0}^{\infty}\frac{\log(1+\alpha x)}{(1+x)^{2}}\,\dd x
\)
Can be obtained by making partial points
\(
I(\alpha)=\alpha\int_{0}^{\infty}\frac{1}{(1+x)(1+\alpha x)}\dd x
=\frac{\alpha\log(1/\alpha)}{1-\alpha}
\)；
Pick \(\alpha=e^{-\Delta}\) and use \(I(1)=1\) have to
\[
D_{\mathrm{KL}}(\phi\|\phi_{\Delta})
=\Delta+2(I(e^{-\Delta})-I(1))
=\Delta\coth(\Delta/2)-2,
\]
Thus the symmetry is given by \(\Delta\mapsto-\Delta\) is obtained by accident.
\par
right \(\chi^2\) and \(D_2\),Depend on
\(
\chi^2(\phi\|\phi_{\Delta})=\int \phi^2/\phi_{\Delta}-1
\), similarly substituted to get
\[
\int_{\RR}\frac{\phi(s)^2}{\phi(s-\Delta)}\,\dd s
=e^{\Delta}\int_{0}^{\infty}\frac{(1+e^{-\Delta}x)^{2}}{(1+x)^{4}}\,\dd x
=\frac{e^{\Delta}+1+e^{-\Delta}}{3}
=\frac{2\cosh\Delta+1}{3},
\]
The last step uses beta points
\(
\int_{0}^{\infty}\frac{x^{m}}{(1+x)^{4}}\dd x=B(m+1,3-m)
\)
（\(m=0,1,2\)）。
therefore
\(
\chi^2(\phi\|\phi_{\Delta})=\frac{2}{3}(\cosh\Delta-1)=\frac{4}{3}\sinh^{2}(\Delta/2)
\)
and \(D_2=\log(1+\chi^2)\)。
Local expansion is given by \(\tanh\)、\(\sinh\)、\(\coth\) can be obtained directly from the Taylor expansion of . Certification completed.
\end{proof}

% (User input window)
% 时间：2026-02-15 23:09:12（Asia/Singapore）

\begin{proposition}[Hellinger--Gram volume potential: square root embedded closed-form kernel and collision divergence law]\label{prop:xi-logistic-hellinger-gram-determinant}
inheritance proposition \ref{prop:xi-logistic-divergence-dictionary} and take the depth spectrum \(\sigma_1,\dots,\sigma_\kappa\in\RR\) are different.
make
\[
\psi_j(s):=\sqrt{\phi(s-\sigma_j)}\in L^2(\RR),
\qquad
G^{(1/2)}_{jk}:=\langle \psi_j,\psi_k\rangle=\int_{\RR}\sqrt{\phi(s-\sigma_j)\phi(s-\sigma_k)}\,\dd s.
\]
but \(G^{(1/2)}\) is positive definite, and its core has a completely closed form
\[
\boxed{\;
G^{(1/2)}_{jk}=\frac{|\sigma_j-\sigma_k|}{2\sinh\!\bigl(|\sigma_j-\sigma_k|/2\bigr)}\;}
\qquad(1\le j,k\le\kappa),
\]
The diagonal takes the continuous extension value \(G^{(1/2)}_{jj}=1\)。
Define volume and entropy potential
\[
V_{1/2}:=\sqrt{\det G^{(1/2)}},
\qquad
S_{1/2}:=-\log\det G^{(1/2)}.
\]
When a scale collision occurs \(\sigma_j=\sigma_\star+\varepsilon t_j\) and \(\varepsilon\downarrow 0\)（\(t_j\) when each pair is different), there is a universal Vandermonde divergence law
\[
\boxed{\;
S_{1/2}(\boldsymbol{\sigma})
=\kappa(\kappa-1)\log\frac{1}{\varepsilon}
+2\sum_{1\le j<k\le \kappa}\log\frac{1}{|t_k-t_j|}
-\log\Bigl(\mathfrak H_\kappa\prod_{j=0}^{\kappa-1}\frac{1}{(j!)^{2}}\Bigr)
+o(1).\;}
\]
in \(\mathfrak H_\kappa\) is the theorem \ref{thm:xi-hellinger-gram-collision-hankel-constant} The universal Hankel constant defined (depends only on \(\kappa\) and spectral density \(w(\xi)=\pi^2\mathrm{sech}^2(\pi\xi)\)）。
Among them, the coefficient of the two-body logarithm term is always \(2\), independent of spectral point weights and other configuration details.
\end{proposition}
\begin{proof}
positive reason \(G^{(1/2)}\) of Gram means get it directly.
closed form proposition \ref{prop:xi-logistic-divergence-dictionary} No. \textup{(3)} The bar (Hellinger affinity closed form) is taken \(a=\sigma_j\)、\(b=\sigma_k\) is available immediately.
\par
Asymptotically for collisions, theorem \ref{thm:xi-hellinger-gram-collision-hankel-constant} gives a more precise asymptotic of the determinant:
\[
\det G^{(1/2)}
=\varepsilon^{\kappa(\kappa-1)}
\Bigl(\prod_{j=0}^{\kappa-1}\frac{1}{(j!)^{2}}\Bigr)\,
\mathfrak H_\kappa\,
\prod_{1\le j<k\le\kappa}(t_k-t_j)^{2}\,\bigl(1+o(1)\bigr).
\]
Taking the negative logarithm gives us the expansion shown. Certification completed.
\end{proof}

\input{para__xi-hellinger-kernel-spectral-rkhs-loggas-toeplitz}

\begin{proposition}[Multi-category optimal decision under logistic noise: nearest neighbor rule and explicit average error rate]\label{prop:xi-logistic-multiclass-map-error}
make \(\kappa\ge 2\), and take the ordered depth logarithmic spectrum
\(
\sigma_1<\sigma_2<\cdots<\sigma_\kappa
\)。
Let the discrete input \(J\sim\mathrm{Unif}\{1,\dots,\kappa\}\), the observation channel is
\[
S=\sigma_J+U,
\qquad
U\sim\phi.
\]
Then the optimal MAP (equivalent to maximum likelihood) decision under equal priors is the nearest neighbor rule
\[
\widehat J(S)\in\arg\min_{1\le j\le\kappa}|S-\sigma_j|,
\]
Its decision boundary is the adjacent midpoint \((\sigma_j+\sigma_{j+1})/2\)。
And the equal prior average Bayes minimum error rate has a strictly closed form
\[
\boxed{\;
P_{\mathrm e}^{(\kappa)}
=\mathbb P(\widehat J(S)\neq J)
=\frac{2}{\kappa}\sum_{j=1}^{\kappa-1}\frac{1}{1+\exp\!\bigl((\sigma_{j+1}-\sigma_j)/2\bigr)}\; }.
\]
Therefore multi-category separability is completely determined by the adjacent interval \(\Delta_j:=\sigma_{j+1}-\sigma_j\) is determined by the logistic tail term.
\end{proposition}
\begin{proof}
because \(\phi\) is an even function and in \([0,\infty)\) strictly decreases on, so for fixed \(s\) maximize \(j\mapsto \phi(s-\sigma_j)\) is equivalent to minimizing \(j\mapsto |s-\sigma_j|\), thereby obtaining the nearest neighbor decision and midpoint boundary.
\par
make \(\Delta_j=\sigma_{j+1}-\sigma_j\)。
when \(J=j\)（\(1<j<\kappa\)), the misjudgment event is equivalent to
\(
U\ge \Delta_j/2
\)
or
\(
U\le -\Delta_{j-1}/2
\)；
endpoint class \(j=1,\kappa\) has only one side of the tail.
Symmetry gives
\(
\mathbb P(U\le -x)=\mathbb P(U\ge x)=1/(1+e^{x})
\)（\(x\ge 0\)），
right \(j\) and counting the tail contribution twice for each interval gives the closed form shown. Certification completed.
\end{proof}

% (User input window)
% 时间：2026-02-16 02:05:41（Asia/Singapore）

\begin{corollary}[Reversible reconstruction of depth intervals from local boundary masses: one-to-one correspondence between error profiles and interval profiles]\label{cor:xi-logistic-boundary-mass-inverts-spacing}
in proposition \ref{prop:xi-logistic-multiclass-map-error} Under the setting of , let the adjacent interval \(\Delta_j:=\sigma_{j+1}-\sigma_j>0\)。
Define local boundary mass (one-sided tail probability)
\[
e_j:=\mathbb P\bigl(U\ge \Delta_j/2\bigr)=\frac{1}{1+e^{\Delta_j/2}}\in(0,1/2).
\]
Then the mapping \(\Delta_j\mapsto e_j\) is strictly monotonic and explicitly invertible:
\[
\boxed{\;
\Delta_j=2\log\Bigl(\frac{1-e_j}{e_j}\Bigr).\;}
\]
So once the tail mass of each adjacent cut is observable \((e_j)_{j=1}^{\kappa-1}\), depth interval sequence \((\Delta_j)\) can be restored point by point losslessly; and
\(
P_{\mathrm e}^{(\kappa)}=\frac{2}{\kappa}\sum_{j=1}^{\kappa-1}e_j
\)
The total error rate is given as a linear summary of the local boundary quality.
\end{corollary}
\begin{proof}
Depend on \(\mathbb P(U\ge x)=1/(1+e^{x})\)（\(x\ge 0\)) get directly \(e_j=(1+e^{\Delta_j/2})^{-1}\), and the inversion formula is obtained by algebraic deformation.
The last formula is a proposition \ref{prop:xi-logistic-multiclass-map-error} The closed form of the error is in the notation \(e_j\) under rewriting. Certification completed.
\end{proof}

\begin{corollary}[Optimal depth layout under fixed span: isometric error minimum solution and necessary span law]\label{cor:xi-logistic-optimal-spacing-span-law}
in proposition \ref{prop:xi-logistic-multiclass-map-error} Under the setting of , let the total span
\[
L:=\sigma_\kappa-\sigma_1=\sum_{j=1}^{\kappa-1}\Delta_j.
\]
then there is a strict lower bound
\[
\boxed{\;
P_{\mathrm e}^{(\kappa)}
\ \ge\
\frac{2(\kappa-1)}{\kappa}\cdot\frac{1}{1+\exp\!\bigl(L/(2(\kappa-1))\bigr)}\; }.
\]
And the equal sign holds if and only if
\(
\Delta_1=\cdots=\Delta_{\kappa-1}=L/(\kappa-1)
\), that is, the equidistant arrangement of the depth logarithmic spectrum is the only error minimum solution.
So if required \(P_{\mathrm e}^{(\kappa)}\le \varepsilon\in(0,1)\), then there must be
\[
\boxed{\;
L\ \ge\ 2(\kappa-1)\log\!\Bigl(\frac{2(\kappa-1)}{\kappa\varepsilon}-1\Bigr)
=2(\kappa-1)\log\frac{1}{\varepsilon}+O(\kappa)\qquad(\varepsilon\downarrow0).\;}
\]
\end{corollary}
\begin{proof}
by proposition \ref{prop:xi-logistic-multiclass-map-error}，
\(
P_{\mathrm e}^{(\kappa)}=\frac{2}{\kappa}\sum_{j=1}^{\kappa-1}g(\Delta_j)
\)
in \(g(x)=(1+e^{x/2})^{-1}\)。
right \(x>0\),function \(g\) can be written as \(g(x)=\bigl(1+e^{x/2}\bigr)^{-1}\) and satisfy \(g''(x)>0\), so it is strictly convex.
Obtained from Jensen's inequality
\(
\frac{1}{\kappa-1}\sum_{j}g(\Delta_j)\ge g(L/(\kappa-1))
\)，
Thus the error lower bound is obtained; the strictness and equal sign conditions give uniqueness.
The span law is solved by the lower bound \(L\) get. Certification completed.
\end{proof}

\begin{proposition}[Entropy difference representation of mutual information and low-resolution quadratic law: constant \texorpdfstring{$1/6$}{1/6} The rigidity appears]\label{prop:xi-logistic-mutual-information-low-resolution}
in proposition \ref{prop:xi-logistic-multiclass-map-error} Under the setting of , let the output mixing density
\[
p(s)=\frac{1}{\kappa}\sum_{j=1}^{\kappa}\phi(s-\sigma_j),
\]
and define differential entropy in terms of natural logarithm \(h(f):=-\int_{\RR}f\log f\)。
Then the mutual information satisfies the strict identity
\[
\boxed{\;I(J;S)=h(p)-h(\phi)=h(p)-2.\;}
\]
and in low-resolution regimes (e.g. let the centered input \(X:=\sigma_J-\frac{1}{\kappa}\sum_{j}\sigma_j\) and \(\mathbb E[X^{2}]\) is small enough) there exists a quadratic approximation law
\[
\boxed{\;
I(J;S)=\frac{1}{6}\,\Var(\sigma_J)+O\!\bigl(\mathbb E[|X|^{4}]\bigr).\;}
\]
where the coefficient \(1/6\) by the core \(\phi\) Fisher information constant \(\mathcal I_{\mathrm{loc}}(\phi)=1/3\) is mandatory and independent of \(\kappa\) and specific configuration.
\end{proposition}
\begin{proof}
The additive noise channel gives \(I(J;S)=h(S)-h(U)=h(p)-h(\phi)\)。
right \(h(\phi)\), as substitution \(x=e^{s}\) have to \(\phi(s)\dd s=\dd x/(1+x)^{2}\) and
\(
\log\phi(s)=\log x-2\log(1+x)
\)；
Depend on \(x\mapsto 1/x\) can be obtained by the symmetry of
\(
\int_{0}^{\infty}\frac{\log x}{(1+x)^{2}}\dd x=0
\),and
\(
\int_{0}^{\infty}\frac{\log(1+x)}{(1+x)^{2}}\dd x=1
\), so \(h(\phi)=2\)。
\par
The quadratic approximation law belongs to the small perturbation expansion of additive noise channels: when the input scale approaches zero,
\(
I(J;S)=\frac{\mathcal I_{\mathrm{loc}}(\phi)}{2}\Var(\sigma_J)+O(\mathbb E[|X|^4])
\)。
For the logistic kernel,\(\phi'(u)/\phi(u)=-\tanh(u/2)\), so
\(
\mathcal I_{\mathrm{loc}}(\phi)=\mathbb E[\tanh^{2}(U/2)]
\)。
make \(T=\tanh(U/2)\in(-1,1)\), then substitute \(u=2\,\operatorname{artanh}(t)\) gives
\[
\phi(u)\,\dd u=\frac{1}{4\cosh^{2}(u/2)}\,\dd u=\frac12\,\dd t,
\]
thereby \(T\sim\mathrm{Unif}(-1,1)\) and \(\mathcal I_{\mathrm{loc}}(\phi)=\mathbb E[T^{2}]=1/3\), get the coefficient \(1/6\). Certification completed.
\end{proof}

\begin{corollary}[Fano-type computable lower bound derived from explicit mean error rate]\label{cor:xi-logistic-fano-lower-bound-from-error}
in proposition \ref{prop:xi-logistic-multiclass-map-error} Under the settings, there are
\[
I(J;S)\ \ge\ \log\kappa\ -\ h_{\mathrm b}\!\bigl(P_{\mathrm e}^{(\kappa)}\bigr)\ -\ P_{\mathrm e}^{(\kappa)}\log(\kappa-1),
\]
in \(h_{\mathrm b}(u)=-u\log u-(1-u)\log(1-u)\) is the binary entropy function.
\end{corollary}
\begin{proof}
By Fano's inequality. Certification completed.
\end{proof}

\begin{proposition}[Strict loss of positional Fisher information: certificates of deep dispersion given by posterior variance]\label{prop:xi-logistic-fisher-information-deficit}
Still take
\(
p(s)=\frac{1}{\kappa}\sum_{j=1}^{\kappa}\phi(s-\sigma_j)
\), and define the location family \(p_\theta(s)=p(s-\theta)\) Fisher information
\[
\mathcal I_{\mathrm{loc}}(p):=\int_{\RR}\frac{(p'(s))^{2}}{p(s)}\,\dd s.
\]
make \(J\sim\mathrm{Unif}\{1,\dots,\kappa\}\)、\(S=\sigma_J+U\)（\(U\sim\phi\)), and note
\[
T:=\tanh\!\Bigl(\frac{S-\sigma_J}{2}\Bigr),
\qquad
m(S):=\mathbb E[T\mid S].
\]
Then there is a strict identity
\[
\boxed{\;
\mathcal I_{\mathrm{loc}}(p)=\mathbb E[m(S)^{2}]
=\frac{1}{3}-\mathbb E\!\bigl[\Var(T\mid S)\bigr]
\ \le\ \frac{1}{3}\; }.
\]
And the equal sign holds if and only if \(\sigma_1=\cdots=\sigma_\kappa\)。
therefore \( \frac{1}{3}-\mathcal I_{\mathrm{loc}}(p)\) is the irreversible information loss introduced by scale curvature mixing and gives a fit-free auditable dispersion certificate in the form of conditional variance.
\end{proposition}
\begin{proof}
Depend on \(\phi'(x)=-\tanh(x/2)\phi(x)\) have to
\[
p'(s)=-\frac{1}{\kappa}\sum_{j=1}^{\kappa}\tanh\!\Bigl(\frac{s-\sigma_j}{2}\Bigr)\phi(s-\sigma_j).
\]
therefore
\(
-p'(s)/p(s)=\mathbb E[T\mid S=s]=m(s)
\),thereby
\(
\mathcal I_{\mathrm{loc}}(p)=\mathbb E[m(S)^{2}]
\)。
And because \(T=\tanh(U/2)\), same as above substitution \(u=2\,\operatorname{artanh}(t)\) have to \(T\sim\mathrm{Unif}(-1,1)\), so \(\mathbb E[T^{2}]=1/3\)(that is \(\mathcal I_{\mathrm{loc}}(\phi)=1/3\)），
Decomposed by total variance
\(
\mathbb E[T^{2}]=\mathbb E[m(S)^{2}]+\mathbb E[\Var(T\mid S)]
\)
Obtain the identity and upper bound shown.
If the equal sign holds, then \(\Var(T\mid S)=0\) almost everywhere, forcing \(T\) for \(S\) is a determining function;
Under finite mixing with a uniform prior, this is equivalent to the same translation parameters for all. Certification completed.
\end{proof}

\begin{remark}[One-dimensional collapse of information geometry: taking \(\mathrm{TV}\) is the elementary parameterization of coordinates]\label{rem:xi-logistic-tv-coordinate-parametrization}
make \(v:=\|\phi_a-\phi_b\|_{\mathrm{TV}}\in[0,1)\)。
by proposition \ref{prop:xi-logistic-divergence-dictionary} There is strict inversion
\[
|\Delta|=4\,\operatorname{artanh}(v)=2\log\!\Bigl(\frac{1+v}{1-v}\Bigr).
\]
and
\[
H^{2}(\phi_a,\phi_b)
=1-\frac{(1-v^{2})\,\operatorname{artanh}(v)}{v},
\qquad
D_{\mathrm{KL}}(\phi_a\|\phi_b)
=2\frac{1+v^{2}}{v}\operatorname{artanh}(v)-2,
\]
\[
\chi^{2}(\phi_a\|\phi_b)
=\frac{16}{3}\frac{v^{2}}{(1-v^{2})^{2}},
\qquad
D_{2}(\phi_a\|\phi_b)
=\log\!\bigl(1+\chi^{2}(\phi_a\|\phi_b)\bigr).
\]
Therefore several core divergences of this translational family can be represented by a single scalar coordinate \(v\) is fully parameterized.
\end{remark}

\begin{remark}[Fisher--Explicitization of Rao metric and quadratic approximation deviation function]\label{rem:xi-logistic-fisher-rao-and-curvature}
scale kernel translation family \(\{\phi_a\}\) where the Fisher information is constant \(\mathcal I_{\mathrm{loc}}(\phi)=1/3\), so the Fisher--Rao metric is in the parameter \(a\) is a constant coefficient,
The Fisher--Rao distance between any two points satisfies
\[
d_{\mathrm{FR}}(\phi_a,\phi_b)=\sqrt{\mathcal I_{\mathrm{loc}}(\phi)}\,|a-b|=\frac{|\Delta|}{\sqrt3}.
\]
and by the proposition \ref{prop:xi-logistic-divergence-dictionary} The closed form definable deviation function of
\[
\Psi(\Delta):=D_{\mathrm{KL}}(\phi_a\|\phi_b)-\frac{1}{2}d_{\mathrm{FR}}(\phi_a,\phi_b)^{2}
=|\Delta|\coth(|\Delta|/2)-2-\frac{\Delta^{2}}{6},
\]
Its in \(\Delta\to 0\) is satisfied when \(\Psi(\Delta)=O(\Delta^{4})\), and characterizes the deviation of KL from the Fisher--Rao quadratic form on the nonlinear visible scale.
\end{remark}

\begin{corollary}[Boundary quality representation for multi-depth discrimination: adjacent cut decomposition of average error rate]\label{cor:xi-logistic-boundary-mass-error}
in proposition \ref{prop:xi-logistic-multiclass-map-error} Under the setting of \(\Delta_j=\sigma_{j+1}-\sigma_j\)。
Then the average error rate has the strictly bounded mass form
\[
P_{\mathrm e}^{(\kappa)}
=\frac{2}{\kappa}\sum_{j=1}^{\kappa-1}\mathbb P\!\Bigl(U\ge \frac{\Delta_j}{2}\Bigr),
\qquad
\mathbb P(U\ge x)=\frac{1}{1+e^{x}}\quad(x\ge 0).
\]
Therefore, the multi-class error under the logistic scale kernel does not have high-dimensional interference terms and is only given by the linear superposition of boundary tail masses induced by adjacent intervals.
\end{corollary}
\begin{proof}
by proposition \ref{prop:xi-logistic-multiclass-map-error} The derivation of , misjudgment is equivalent to noise falling into the outer tails of both sides of the adjacent midpoint cut, and is transformed into the sum of the probabilities of one-sided tails due to symmetry. Certification completed.
\end{proof}

\begin{remark}[\texorpdfstring{$4$}{4} Divergence representation of order closing threshold]\label{rem:xi-logistic-fourth-order-closure}
proposition \ref{prop:xi-logistic-divergence-dictionary} Expand the display with small intervals:\(\mathrm{TV}\) The first term is linear (coefficient \(1/4\)),and \(H^{2}\) and \(D_{\mathrm{KL}}\) The first non-trivial term is quadratic;
and \(D_{\mathrm{KL}}=4H^{2}+O(\Delta^{4})\) locks the two to the same local geometry within fourth-order accuracy.
So when simultaneously focusing on boundary cuts (by \(\mathrm{TV}\) and the entropy potential fluctuation (controlled by \(H^{2}\)、\(D_{\mathrm{KL}}\), the fourth-order term becomes the smallest closed order.
\end{remark}

\begin{corollary}[Depth spectrum design law targeting information visibility: optimal configuration shrinks to isometric family]\label{cor:xi-logistic-depth-design-law}
in fixed \(\kappa\) and total span \(L\), so that the proposition \ref{prop:xi-logistic-multiclass-map-error} The average error rate of \(P_{\mathrm e}^{(\kappa)}\) The smallest configuration is realized uniquely by the isometric spectrum:
\[
\sigma_j=\sigma_1+\frac{j-1}{\kappa-1}L\qquad(1\le j\le\kappa).
\]
And this configuration also makes the corollary \ref{cor:xi-logistic-fano-lower-bound-from-error} The given lower bound on mutual information reaches the maximum value.
\end{corollary}
\begin{proof}
by inference \ref{cor:xi-logistic-optimal-spacing-span-law}, under a fixed span \(P_{\mathrm e}^{(\kappa)}\) is uniquely realized by Jensen's equality condition;
And Fano's lower bound is \(P_{\mathrm e}^{(\kappa)}\) decreases monotonically, so the same configuration also maximizes the lower bound. Certification completed.
\end{proof}

