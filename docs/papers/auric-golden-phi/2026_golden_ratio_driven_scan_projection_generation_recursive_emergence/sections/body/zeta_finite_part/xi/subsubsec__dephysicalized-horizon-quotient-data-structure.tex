\paragraph{Horizon as a "quotient data structure": Structured representation of externally visible algebra, equivalence classes of fibers and volumes/holograms}Theorem \ref{thm:dephys-pw-nohair} rules out additional continuous registers in parallel with phase; in this caliberThe "horizon" can be understood as the quotient structure induced by the externally visible algebra:The external readout relies only on an accessible operator algebra (or visible \(\sigma\)-algebra), while a large number of internal degrees of freedom are pressed into the same external equivalence class fiber.The following conclusions are used to align the four types of interpretive semantics of "hairless-area law-holographic-zero knowledge"; they are not used as any derivation premises.
\begin{definition}[External visible algebra and external equivalence relationship]\label{def:dephys-ext-observable-algebra-equivalence}Let \(\mathcal{H}_{\mathrm{bulk}}\) be the total Hilbert space including “internal and external” degrees of freedom, and let\(\mathcal{A}_{\mathrm{ext}}\subseteq \mathcal{B}(\mathcal{H}_{\mathrm{bulk}})\)
is an \(\ast\)-algebra (externally measurable algebra) accessible to external observers.Let the density operator state space be\[
\mathcal{D}(\mathcal{H}_{\mathrm{bulk}})
:=\{\rho\succeq 0:\ \operatorname{Tr}\rho=1\}.
\]
Define external equivalence relations for \(\rho,\tilde\rho\in\mathcal{D}(\mathcal{H}_{\mathrm{bulk}})\)\begin{equation}\label{eq:dephys-ext-equivalence}
\boxed{\;
\rho\sim_{\mathrm{ext}}\tilde\rho
\quad\Longleftrightarrow\quad
\operatorname{Tr}(\rho A)=\operatorname{Tr}(\tilde\rho A)\ \ (\forall\,A\in\mathcal{A}_{\mathrm{ext}}).\;}
\end{equation}
And note \(\mathrm{St}(\mathcal{A}_{\mathrm{ext}})\) as the state space on \(\mathcal{A}_{\mathrm{ext}}\):\[
\mathrm{St}(\mathcal{A}_{\mathrm{ext}})
:=\{\omega:\mathcal{A}_{\mathrm{ext}}\to\CC:\ \omega\ge 0,\ \omega(I)=1\}.
\]
\end{definition}

\begin{proposition} [Horizon as a “quotient data structure”: equivalence-like fibers induced by external observable algebra]\label{prop:dephys-horizon-quotient-data-structure}Under the settings that define \ref{def:dephys-ext-observable-algebra-equivalence}, there is a natural quotient mapping (restriction mapping)\begin{equation}\label{eq:dephys-ext-quotient-map}
\boxed{\;
\pi_{\mathrm{ext}}:\ \mathcal{D}(\mathcal{H}_{\mathrm{bulk}})\twoheadrightarrow \mathrm{St}(\mathcal{A}_{\mathrm{ext}}),
\qquad
(\pi_{\mathrm{ext}}(\rho))(A):=\operatorname{Tr}(\rho A)\; }.
\end{equation}
And for any \(\rho,\tilde\rho\in\mathcal{D}(\mathcal{H}_{\mathrm{bulk}})\), we have\[
\boxed{\;
\rho\sim_{\mathrm{ext}}\tilde\rho
\quad\Longleftrightarrow\quad
\pi_{\mathrm{ext}}(\rho)=\pi_{\mathrm{ext}}(\tilde\rho).\;}
\]
Therefore for any \(\sigma\in \mathrm{St}(\mathcal{A}_{\mathrm{ext}})\), its fiber\(
\pi_{\mathrm{ext}}^{-1}(\sigma)
\)
Can be understood as an "externally indistinguishable set of internal configurations" (external equivalence class).\end{proposition}

\begin{proof}
According to the definition of \eqref{eq:dephys-ext-quotient-map}, \(\pi_{\mathrm{ext}}(\rho)\) is exactly the linear functional induced by \(\rho\) on \(\mathcal{A}_{\mathrm{ext}}\);And \(\pi_{\mathrm{ext}}(\rho)=\pi_{\mathrm{ext}}(\tilde\rho)\) is equivalent to having\(\operatorname{Tr}(\rho A)=\operatorname{Tr}(\tilde\rho A)\), that's exactly \eqref{eq:dephys-ext-equivalence}.Certification completed.\end{proof}

\begin{remark}[Same type as fold push forward: probability push forward under commutative external algebra]\label{rem:dephys-ext-quotient-as-pushforward}When \(\mathcal{A}_{\mathrm{ext}}\) is commutative and identifiable as the function algebra \(L^\infty(X)\) on some finite set \(X\),\(\mathrm{St}(\mathcal{A}_{\mathrm{ext}})\) is equivalent to the probability simplex \(\Delta(X)\).At this time, \(\pi_{\mathrm{ext}}\) degenerates into the push-forward operator of "microscopic distribution \(\mapsto\) macroscopic distribution",Exactly the same type as the readout operator \(P_m=(\Fold_m)_*\) in section \ref{subsec:pom-information-ledger}:The external equivalence class of fibers corresponds to a collection of microscopic configurations under the same macroscopic readout, while the ledger three-term decomposition gives the strict identity (theorem \ref{thm:pom-kl-ledger}) of "visible entropy + fiber capacity \(-\) residual information".\end{remark}

\begin{proposition}[Capacity law of area saturation: the maximum logarithmic scale of the horizon fiber is controlled by the boundary area]\label{prop:dephys-horizon-fiber-capacity-area-law}Interpret the amount of external inaccessible information as the logarithm of the "number of effective states" of the external equivalence class fiber,Then the area law of semiclassical black hole thermodynamics\(
S_{\mathrm{BH}}\propto A
\)
It can be understood as: given the external interface \(\mathcal{A}_{\mathrm{ext}}\) and the given boundary geometry (area \(A\)),Typical fibers of \(\pi_{\mathrm{ext}}\) have the maximum allowed logarithmic scale, and black holes reach a saturation upper bound (maximum entropy object) under this constraint.In Planck units (\(G=\hbar=c=k_{\mathrm B}=1\)),The Bekenstein-Hawking entropy formula gives the normalized saturation bound\begin{equation}\label{eq:dephys-bh-entropy-area-quarter}
\boxed{\;
S_{\mathrm{BH}}=\frac{A}{4}\;}
\end{equation}
(See \cite{Bekenstein1973BlackHolesEntropy,Hawking1975ParticleCreation}).\end{proposition}

\begin{theorem}[Structural expression of the holographic principle: the horizon is the boundary encoding of the minimum sufficient statistics]\label{thm:dephys-holography-sufficient-statistic}Suppose there is a bounded Hilbert space \(\mathcal{H}_{\partial}\) with coded equidistant embeddings\(
\mathcal{E}:\mathcal{H}_{\mathrm{code}}\hookrightarrow \mathcal{H}_{\partial}
\)
(\(\mathcal{H}_{\mathrm{code}}\subseteq\mathcal{H}_{\mathrm{bulk}}\) is a certain code subspace), so that for any externally visible operator \(A\in\mathcal{A}_{\mathrm{ext}}\):There exists a boundary operator \(A_{\partial}\in\mathcal{B}(\mathcal{H}_{\partial})\) that satisfies\begin{equation}\label{eq:dephys-holography-reconstruction}
\boxed{\;
\langle\psi, A\psi\rangle_{\mathcal{H}_{\mathrm{bulk}}}
=
\langle\mathcal{E}\psi, A_{\partial}\mathcal{E}\psi\rangle_{\mathcal{H}_{\partial}}
\qquad(\forall\,\psi\in\mathcal{H}_{\mathrm{code}}).\;}
\end{equation}
Then \(\mathcal{E}\) realizes a holographic structure with "boundary sufficient to reconstruct external computable physical quantities" under the code subspace caliber:Externally observable data are not in principle approximate summaries but rather encoded as boundaries of minimal sufficient statistics.Under the intuition of "one degree of freedom per Planck area", equation \eqref{eq:dephys-bh-entropy-area-quarter} states that the maximum amount of information that can be encoded is proportional to the boundary area (see \cite{tHooft1993DimensionalReductionQuantumGravity,Susskind1995WorldAsHologram}).\end{theorem}

\begin{remark}[A verifiable model for AdS/CFT: Boundary theory i.e. explicit encoders]\label{rem:dephys-adscft-explicit-encoder}In the AdS/CFT duality, the boundary conformal field theory is equivalent to the bulk gravity theory in the relevant limit, so that the boundary states can be considered as complete encoders of the bulk states (\cite{Maldacena1999LargeN}).The framework further fixes the "area/4" constant in a geometric form of the entanglement entropy:The entanglement entropy for the boundary subregion \(A\) is given by the area of ​​the extremal surface in the volume in the classical limit\[
S(A)=\frac{\mathrm{Area}(\gamma_A)}{4G_N},
\]
That is Ryu--Takayanagi formula (\cite{RyuTakayanagi2006HolographicEntanglementEntropy}).Therefore, the meaning of "horizon as data structure" is explicitly realized in this type of theory and is no longer just an analogy.\end{remark}

\begin{proposition}[Rigidization of zero-knowledge analogy: external verification is equivalent to the determination of quotient space predicates]\label{prop:dephys-zero-knowledge-quotient-factorization}Make the external validator \(V\) accessible only to \(\mathcal{A}_{\mathrm{ext}}\) and output the Boolean value \(V(\rho)\in\{0,1\}\);That is, assume that \(V\) is constant for the external equivalence class:\[
\rho\sim_{\mathrm{ext}}\tilde\rho\ \Longrightarrow\ V(\rho)=V(\tilde\rho).
\]
Then \(V\) must be factored through quotient mapping \(\pi_{\mathrm{ext}}\): there is a predicate\(
v:\mathrm{St}(\mathcal{A}_{\mathrm{ext}})\to\{0,1\}
\)
make\begin{equation}\label{eq:dephys-verification-factorization}
\boxed{\;
V=v\circ \pi_{\mathrm{ext}}.\;}
\end{equation}
Therefore any "verifiable statement" externally is essentially a predicate of an external equivalence class and cannot differentiate between microstates within the same fiber;If the microstate is regarded as the witness \(w\) and the externally visible data is regarded as the statement \(x\), then \eqref{eq:dephys-verification-factorization} gives the physical correspondence of zero-knowledge semantics.\end{proposition}

\begin{proof}
Define function \(\overline V([\rho]) := V(\rho)\) on business set \(\mathcal{D}(\mathcal{H}_{\mathrm{bulk}})/{\sim_{\mathrm{ext}}}\);This definition is well defined by the "external equivalence class constant" assumption.From the proposition \ref{prop:dephys-horizon-quotient-data-structure}, the commercial set can be identified as the image of \(\pi_{\mathrm{ext}}\)\(\mathrm{St}(\mathcal{A}_{\mathrm{ext}})\), and \(\pi_{\mathrm{ext}}\) fibers are exactly equivalent classes.Therefore, let \(v(\sigma):=\overline V([\rho])\) (including \(\sigma=\pi_{\mathrm{ext}}(\rho)\)) get \(V=v\circ\pi_{\mathrm{ext}}\).Certification completed.\end{proof}

\begin{remark}[Dynamic Data Structure Perspective: Horizon as Random Encoder/Scrambler and Information Recycling Threshold]\label{rem:dephys-horizon-scrambler-recovery-threshold}If it is further assumed that the dynamics within the black hole approximate rapid mixing, then the incoming information is non-locally diffused into the event horizon and radiation degrees of freedom in a short time;After the evaporation process is deep enough, the newly injected information can be recovered relatively quickly from the radiation.This phenomenon can be accurately formulated as: under the assumption of "fast mixing + unit evolution", the black hole achieves near-optimal quantum erasure channel coding,And it presents the information release threshold behavior of "first hiding, then highlighting" (Hayden--Preskill mirroring mechanism; see \cite{HaydenPreskill2007BlackHolesMirrors}).\end{remark}

\begin{proposition} [Position with respect to coefficient \texorpdfstring{XPH0X}{1/4}: boundary—normalization constant of thermodynamic consistency]\label{prop:dephys-one-quarter-normalization}The coefficient \(1/4\) in semiclassical black hole thermodynamics is not an adjustable parameter.The First Law of Black Hole Mechanics (\cite{BardeenCarterHawking1973FourLaws})\[
dM=\frac{\kappa}{8\pi G}\,\dd A+\Omega\,\dd J+\Phi\,\dd Q
\]
With Hawking Temperature (\cite{Hawking1975ParticleCreation})\(
T=\frac{\hbar\kappa}{2\pi}
\)
Compatible thermodynamic identification\(
dM=T\,\dd S+\Omega\,\dd J+\Phi\,\dd Q
\)
jointly forced to give\[
\boxed{\;
\dd S=\frac{1}{4G\hbar}\,\dd A,\qquad
S=\frac{A}{4G\hbar}+(\text{constant})\;}\]
This gives \(S_{\mathrm{BH}}=A/4\) of \eqref{eq:dephys-bh-entropy-area-quarter} in Planck units.Its mathematical position is the same as the role of the endpoint/horizon normalization constant in this article: the constant assumes the sealing of units and normalization, aligning the boundary geometric quantities with the additive logarithmic potential without introducing new internal degrees of freedom.(See Note \ref{rem:dephys-cayley-scale-gauge-four} for the explanation that normalization changes only the additive zeros, and Proposition \ref{prop:xi-defect-entropy-hyperbolic-area-law-4pi} for the exact similarity of the \(4\pi\) alignment mechanism).\end{proposition}

\paragraph{Horizon sufficiency and indexed capacity: recoverability criterion of conditional expectation caliber}In order to upgrade the above "quotient data structure" from the equivalence class description at the set theory level to the auditable operator algebra caliber,The following uses the setting of \(\sigma\)-finite von Neumann algebraic embedding and faithful normal conditional expectations,And the information loss of "external compression" is measured by Araki--Umegaki relative entropy (\cite{Araki1976RelativeEntropyVonNeumann}).This caliber strictly equates "external verification = zero-knowledge predicate" to recoverability in a sufficient statistical sense (Petz sufficiency theorem; \cite{Petz1986SufficientSubalgebrasRelativeEntropy,Petz2003MonotonicityRelativeEntropyRevisited}),And lock the worst-case logarithmic scale of "fiber capacity" to the logarithm of a finite exponent (\cite{PimsnerPopa1986EntropyIndexSubfactors,Hiai1991MinimumIndexSubfactorsEntropyII}).
\begin{definition}[Externalized channels and fixed point state space]\label{def:dephys-conditional-expectation-externalization-map}Let \(\mathcal{A}_{\mathrm{ext}}\subseteq \mathcal{A}_{\mathrm{bulk}}\) be the \(\sigma\)-finite von Neumann algebraic embedding,And letLet \(\mathcal{S}(\mathcal{A})\) be the overall normal state on \(\mathcal{A}\).\par
Define externalization operators on state space (idempotent projection)\begin{equation}\label{eq:dephys-ext-externalization-Pi}
\boxed{\;
\Pi:\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\to \mathcal{S}(\mathcal{A}_{\mathrm{bulk}}),
\qquad
\Pi(\rho):=\rho\circ E.\;}
\end{equation}
and define its fixed point set\[
\mathrm{Fix}(\Pi):=\{\rho\in\mathcal{S}(\mathcal{A}_{\mathrm{bulk}}):\ \Pi(\rho)=\rho\}.
\]
Since \(E\) is identical on \(\mathcal{A}_{\mathrm{ext}}\), the constraint mapping \(\rho\mapsto \rho|_{\mathcal{A}_{\mathrm{ext}}}\) is naturally identical on \(\mathrm{Fix}(\Pi)\) to \(\mathcal{S}(\mathcal{A}_{\mathrm{ext}})\).\end{definition}

\begin{proposition}[Horizon sufficiency theorem: "Zero-knowledge type" necessary and sufficient conditions for external verification]\label{prop:dephys-horizon-sufficiency-zero-knowledge}Under the settings that define \ref{def:dephys-conditional-expectation-externalization-map},Let the predicate \(V:\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\to\{0,1\}\) satisfy external invariance:\[
\rho|_{\mathcal{A}_{\mathrm{ext}}}=\rho'|_{\mathcal{A}_{\mathrm{ext}}}
\ \Longrightarrow\ 
V(\rho)=V(\rho').
\]
Then there is a unique predicate \(v:\mathrm{Fix}(\Pi)\to\{0,1\}\) such that\begin{equation}\label{eq:dephys-zero-knowledge-factorization-by-Pi}
\boxed{\;
V=v\circ\Pi.\;}
\end{equation}
In particular, any "verifiable statement" externally can only depend on the externalized state \(\Pi(\rho)\) (equivalently on \(\rho|_{\mathcal{A}_{\mathrm{ext}}}\)), and cannot distinguish microstates within the same external equivalence class of fibers.\end{proposition}

\begin{proof}
Note that for any \(\rho\in\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\) and any \(A\in\mathcal{A}_{\mathrm{ext}}\), we have\(
\Pi(\rho)(A)=\rho(E(A))=\rho(A)
\), so \(\Pi(\rho)|_{\mathcal{A}_{\mathrm{ext}}}=\rho|_{\mathcal{A}_{\mathrm{ext}}}\).The external invariance is therefore equivalent to the constant value of \(V\) on the fiber of \(\Pi\).Let \(v(\sigma):=V(\rho)\), where \(\sigma=\Pi(\rho)\in\mathrm{Fix}(\Pi)\);This definition is defined by constantness and clearly satisfies \(V=v\circ\Pi\).The uniqueness comes from the fact that \(\Pi\) is identical on the image: if \(V=v_1\circ\Pi=v_2\circ\Pi\), then taking \(\rho=\sigma\) for any \(\sigma\in\mathrm{Fix}(\Pi)\) gives \(v_1(\sigma)=v_2(\sigma)\). Certification completed.\end{proof}

\begin{theorem}[Horizon sufficiency: Relative entropy fidelity \(\Longleftrightarrow\) Restorability]\label{thm:dephys-petz-sufficiency-equivalences}Under the settings that define \ref{def:dephys-conditional-expectation-externalization-map},Let \(\mathcal{F}\subset\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\) be a family of normal states.The following propositions are equivalent:\begin{enumerate}
\item[(i)] \(E\) is sufficient to\item[(ii)] For any \(\rho,\sigma\in\mathcal{F}\), the Araki--Umegaki relative entropy satisfies the fidelity equation  \begin{equation}\label{eq:dephys-relative-entropy-sufficiency-equality}
  \boxed{\;
  D(\rho\|\sigma)=D(\rho\circ E\ \|\ \sigma\circ E)\;}
  \end{equation}
(where \(D\) is the relative entropy on the von Neumann algebra; \cite{Araki1976RelativeEntropyVonNeumann});\item[(iii)] There is a completely normal unitary mapping (restoration operator) \(R:\mathcal{A}_{\mathrm{ext}}\to\mathcal{A}_{\mathrm{bulk}}\), such that for all \(\rho\in\mathcal{F}\)  \begin{equation}\label{eq:dephys-recovery-map-identity-on-family}
  \boxed{\;
  \rho=\rho\circ R\circ E.\;}
  \end{equation}
\end{enumerate}
This equivalence strictly refines the "horizon as verification interface" into a recoverability criterion in a sufficient statistical sense (Petz's sufficiency theorem).\end{theorem}

\begin{proof}[Points of Proof]From the relative entropy data processing inequality (\cite{Petz2003MonotonicityRelativeEntropyRevisited}), for any \(\rho,\sigma\) there is always\(
D(\rho\|\sigma)\ge D(\rho\circ E\|\sigma\circ E)
\)。
Petz's theory describes its equality conditions, and gives the necessary and sufficient conditions (\cite{Petz1986SufficientSubalgebrasRelativeEntropy}) for equality if and only if there is a recovery operator such that \(\rho=\rho\circ R\circ E\).Therefore, Certification completed.\end{proof}

\begin{corollary} [Fully Statistical \(\Rightarrow\) Universal Zero-Knowledge Simulation: Recovery-Induced Channel Factorization] \label{cor:dephys-universal-zk-simulation-factorization}Under the setting of theorem \ref{thm:dephys-petz-sufficiency-equivalences},If there is a recovery operator \(R:\mathcal{A}_{\mathrm{ext}}\to\mathcal{A}_{\mathrm{bulk}}\) such that\(\rho=\rho\circ R\circ E\) holds for all \(\rho\in\mathcal{F}\),Then for any output von Neumann algebra \(\mathcal{C}\) and any normal quantum channel\(\Gamma_*:\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\to\mathcal{S}(\mathcal{C})\)
(i.e. normal, affine and induced by some normal fully positive trace-preserving map in Schr\"odinger caliber),There is a normal quantum channel\(\widetilde\Gamma_*:\mathcal{S}(\mathcal{A}_{\mathrm{ext}})\to\mathcal{S}(\mathcal{C})\)
Such that a universal factorized identity is established on the family of states \(\mathcal{F}\)\begin{equation}\label{eq:dephys-universal-zk-factorization-on-family}
\boxed{\;
\Gamma_*(\rho)=\widetilde\Gamma_*(\rho\circ E)\qquad(\forall\,\rho\in\mathcal{F}).\;}
\end{equation}
And an explicit simulator construct is desirable:\[
\boxed{\;
\widetilde\Gamma_*(\sigma):=\Gamma_*(\sigma\circ R)\qquad(\sigma\in\mathcal{S}(\mathcal{A}_{\mathrm{ext}})).\;}
\]
Therefore, "the externally verifiable output on \(\mathcal{F}\) is completely determined by the external like \(\rho\circ E\)" is not only a quotient space predicate closure at the set theory level,Rather, it is a general zero-knowledge simulation law strictly aligned with Petz-type recovery (sufficient statistics) in the completely positive category.\end{corollary}

\begin{proof}
For any \(\rho\in\mathcal{F}\), by assuming \(\rho=\rho\circ R\circ E\) we get\[
\widetilde\Gamma_*(\rho\circ E)
=\Gamma_*\bigl((\rho\circ E)\circ R\bigr)
=\Gamma_*(\rho),
\]
You will get \eqref{eq:dephys-universal-zk-factorization-on-family}.From the normality and affineness of \(\sigma\mapsto \sigma\circ R\) and \(\Gamma_*\), it can be seen that \(\widetilde\Gamma_*\) is still a normal quantum channel. Certification completed.\end{proof}

\begin{corollary}[full state family recoverable \(\Rightarrow\) ordinary vision: \(\mathrm{Ind}(E)=1\)]\label{cor:dephys-full-state-recovery-forces-trivial-index1}Suppose there exists a normal completely unitary mapping \(R:\mathcal{A}_{\mathrm{ext}}\to\mathcal{A}_{\mathrm{bulk}}\) such that\begin{equation}\label{eq:dephys-left-inverse-recovery-map}
\boxed{\;R\circ E=\mathrm{id}_{\mathcal{A}_{\mathrm{bulk}}}.\;}
\end{equation}
Then there must be \(\mathcal{A}_{\mathrm{bulk}}=\mathcal{A}_{\mathrm{ext}}\) and \(E\) is an identity mapping;In particular, \(\mathrm{Ind}(E)=1\).\end{corollary}

\begin{proof}
From \eqref{eq:dephys-left-inverse-recovery-map}, we know that \(E\) is injective.However, \(E\) is a conditional expectation, so it is identical and idempotent on its image \(\mathcal{A}_{\mathrm{ext}}\); if \(\mathcal{A}_{\mathrm{ext}}\subsetneq \mathcal{A}_{\mathrm{bulk}}\), then \(E\) must have a non-zero nuclear direction, making it impossible to inject.Therefore, there can only be \(\mathcal{A}_{\mathrm{bulk}}=\mathcal{A}_{\mathrm{ext}}\) and \(E=\mathrm{id}\).By definition \ref{def:dephys-pimsner-popa-index}, \(\mathrm{Ind}(\mathrm{id})=1\). Certification completed.\end{proof}

\begin{definition}[Pimsner--Popa limited index]\label{def:dephys-pimsner-popa-index}Under the above settings, the conditional expectation \(E\) is said to have a finite Pimsner--Popa index. If there is a constant \(\Lambda\in(1,\infty)\), it holds for all \(x\in\mathcal{A}_{\mathrm{bulk}}\)\begin{equation}\label{eq:dephys-pimsner-popa-inequality}
\boxed{\;
E(x^\ast x)\ \ge\ \Lambda^{-1}\,x^\ast x.\;}
\end{equation}
Define its index as\[
\boxed{\;
\mathrm{Ind}(E):=\inf\{\Lambda:\\]
\end{definition}

\begin{theorem}[Horizon index formula: the maximum information loss is equal to the logarithm of the finite index]\label{thm:dephys-index-max-relative-entropy-loss}Under the settings defining \ref{def:dephys-pimsner-popa-index}, if \(E\) has finite exponent \(\mathrm{Ind}(E)\in(1,\infty)\),Then the maximum relative entropy loss that can be caused by externalization satisfies the exact extreme value formula\begin{equation}\label{eq:dephys-index-max-entropy-loss}
\boxed{\;
\sup_{\rho,\sigma\in\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})}
\Bigl[D(\rho\|\sigma)-D(\rho\circ E\ \|\ \sigma\circ E)\Bigr]
=\log \mathrm{Ind}(E).\;}
\end{equation}
\(\log\mathrm{Ind}(E)\) therefore gives a normalized logarithmic scale of the "horizon fiber capacity": it does not depend on the concrete state, but is uniquely determined by the external interface and embedded structure.\end{theorem}

\begin{proof}[Points of Proof]Monotonicity of relative entropy gives the integrand expression to be nonnegative.In the case of finite exponentials, the upper bound and reachability of the above formula are given by Pimsner-Popa's exponential relative entropy estimation and its extreme value construction;See the "Exponential \(\leftrightarrow\) Relative Entropy" theorem chain for \cite{PimsnerPopa1986EntropyIndexSubfactors,Hiai1991MinimumIndexSubfactorsEntropyII}. Certification completed.\end{proof}

\begin{proposition}[Hard upper bound of hidden label capacity: relative exchange subdimension is capped by exponential]\label{prop:dephys-relative-commutant-dimension-index-cap}Let \(N\subset M\) be the finite exponential subfactor inclusion, and let \(E:M\to N\) be the standard (trace preserving) conditional expectation.If the Jones index is finite \([M:N]=\mathrm{Ind}(E)<\infty\), then the relative commutator \(N'\cap M\) is a finite-dimensional \(C^\ast\)-algebra and satisfies the upper bound of dimensionality\begin{equation}\label{eq:dephys-relative-commutant-dimension-bound}
\boxed{\;\dim(N'\cap M)\ \le\ \mathrm{Ind}(E).\;}
\end{equation}
Therefore, the cardinality of any family of pairwise orthogonal non-zero projections \(\{p_j\}\subset N'\cap M\) satisfies\(
|\{p_j\}|\le \mathrm{Ind}(E)
\)。
Under the semantics of "external interface is \(N\)", this conclusion compresses the number of discrete tags that are externally indistinguishable but internally distinguishable into a hard upper bound uniquely controlled by the horizon index.\end{proposition}

\begin{proof}[Points of Proof]The finite exponent contains the finite Jones dimension givingIts upper bound on dimensionality is derived from dimensionality estimates in Pimsner--Popa basis and basic construction (see \cite{PimsnerPopa1986EntropyIndexSubfactors}'s Exponential/Basic Construction link).The cardinality of the orthogonal projection family does not exceed the algebraic dimension, so the inference holds. Certification completed.\end{proof}

\input{subsubsec__dephysicalized-horizon-quotient-data-structure__fold-hidden-label-cap-sqrt}

\begin{proposition}[Operator version of the zero-knowledge simulation theorem: exact simulatability and exponential lower bounds of conditional expectations]\label{prop:dephys-operator-zk-simulation-index-lower-bound}Under the settings that define \ref{def:dephys-conditional-expectation-externalization-map},For any externally measurable process (i.e. any POVM/inspection that only relies on \(\mathcal{A}_{\mathrm{ext}}\)), there is an \textbf {accurate simulator}: its input is only externally visible state\(
\sigma:=\rho|_{\mathcal{A}_{\mathrm{ext}}}\equiv \Pi(\rho)|_{\mathcal{A}_{\mathrm{ext}}}
\)，
The external statistical distribution of the output is exactly the same as under real \(\rho\).\par
Furthermore, ifThen its minimum auxiliary dimension \(d_{\min}\) satisfies\[
\boxed{\;d_{\min}^2\ \ge\ \mathrm{Ind}(E)\;}
\]
The logarithmic scale of the minimum auxiliary resources thus has an insurmountable lower bound \(\tfrac12\log\mathrm{Ind}(E)\).\end{proposition}
\begin{proof}[Points of Proof]The first part is an equivalent rewriting of the definition layer: any expectation of \(M\in\mathcal{A}_{\mathrm{ext}}\) satisfies\(
\rho(M)=\rho(E(M))=\Pi(\rho)(M)
\), so external statistics only rely on \(\sigma=\rho|_{\mathcal{A}_{\mathrm{ext}}}\).\par
The second part can be given by the count of "base/certificate elements" in finite-dimensional implementations: if \(E\) has a Stinespring representation implemented as an \(d\)-dimensional auxiliary system, then up to \(d^2\) operator elements can be constructed to form a set of finite certificate bases that implement \(E\);And the finite exponent \(\mathrm{Ind}(E)\), being the minimized magnitude of the Pimsner--Popa constant, cannot exceed the "square size" of any such finite certificate.Therefore \(\mathrm{Ind}(E)\le d^2\), take the minimum \(d=d_{\min}\) to get the conclusion. Certification completed.\end{proof}

\input{subsubsec__dephysicalized-horizon-quotient-data-structure__fold-stinespring-sqrt-law}

\begin{corollary}[Chain Rule: Exponential Additivity and Information Loss Additivity of Nested Horizons]\label{cor:dephys-index-chain-rule}Let \(\mathcal{A}_0\subseteq \mathcal{A}_1\subseteq \mathcal{A}_2\) and there is a conditional expectation\(
E_{21}:\mathcal{A}_2\to\mathcal{A}_1
\)、\(
E_{10}:\mathcal{A}_1\to\mathcal{A}_0
\), and let \(E_{20}:=E_{10}\circ E_{21}\).If all three are finite exponents, then\[
\boxed{\;
\mathrm{Ind}(E_{20})=\mathrm{Ind}(E_{21})\,\mathrm{Ind}(E_{10}),\qquad
\log\mathrm{Ind}(E_{20})=\log\mathrm{Ind}(E_{21})+\log\mathrm{Ind}(E_{10}).\;}
\]
And for any \(\rho,\sigma\in\mathcal{S}(\mathcal{A}_2)\) there is a chain identity of relative entropy loss\[
\boxed{\;
D(\rho\|\sigma)-D(\rho\circ E_{20}\ \|\ \sigma\circ E_{20})
=\Delta_{21}(\rho,\sigma)+\Delta_{10}(\rho\circ E_{21},\sigma\circ E_{21}),\;}
\]
Among them \(\Delta_{ab}(\cdot,\cdot):=D(\cdot\|\cdot)-D(\cdot\circ E_{ab}\ \|\ \cdot\circ E_{ab})\).\end{corollary}

\begin{proof}
Exponential multiplicativeness is a standard property expected of finite exponential conditions (see \cite{PimsnerPopa1986EntropyIndexSubfactors,Hiai1991MinimumIndexSubfactorsEntropyII}).The chain identity is directly expanded by substitution and term-by-term elimination of \(E_{20}=E_{10}\circ E_{21}\). Certification completed.\end{proof}

\begin{proposition}[Commutative square matrices and mutual information defects: decidable compatibility of two externalization processes]\label{prop:dephys-commuting-square-mutual-information-defect}Let \(\mathcal{A}_{\mathrm{ext}}^{(1)},\mathcal{A}_{\mathrm{ext}}^{(2)}\subseteq\mathcal{A}_{\mathrm{bulk}}\) be two external subalgebras,And let \(E_1,E_2\) be the corresponding faithful normal condition expectation.If it satisfies commuting square compatibility\[
E_1E_2=E_2E_1=:E_{12},
\]
Then for any \(\rho,\sigma\in\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\) there is a nonnegative defect identity\begin{equation}\label{eq:dephys-mutual-information-defect}
\boxed{\;
\Delta_{1}(\rho,\sigma)+\Delta_{2}(\rho,\sigma)-\Delta_{12}(\rho,\sigma)\ \ge\ 0,\;}
\end{equation}
Among them \(\Delta_i(\rho,\sigma):=D(\rho\|\sigma)-D(\rho\circ E_i\ \|\ \sigma\circ E_i)\).The equality sign holds if and only if the two externalization processes do not introduce additional unrecoverable losses to each other on \((\rho,\sigma)\) (equivalently: the relative entropy is equal to the two-step externalization at the same time; see the equality condition characterization of \cite{Petz2003MonotonicityRelativeEntropyRevisited}).\end{proposition}

\begin{proof}[Points of Proof]This inequality is an equivalent expression of the monotonicity of relative entropy and the strong subadditivity structure under commuting-square constraints;\(E_{12}\) can be viewed as a co-refinement of two steps of coarse-graining, and Petz's equality condition is used to translate "no additional loss" into a recoverability condition. Certification completed.\end{proof}

\begin{theorem}[Möbius--Markov uniqueness: Poisson semigroup is the only covariant flow of horizon coarse-graining]\label{thm:dephys-mobius-markov-unique-poisson}Let \((T_t)_{t\ge 0}\) be a strong continuous Markov semigroup acting on the integrable density on \(\RR\), satisfying:\begin{enumerate}
\item[(i)] covaries with the boundary affine Möbius automorphism (\(x\mapsto ax+b\), \(a>0\)) of \(\mathbb{H}\);\item[(ii)] maintains the Lebesgue measure and satisfies the strong maximum principle;\item[(iii)] is a homogeneous first-order operator whose generator is radially isotropic in the frequency domain: there is a constant \(c>0\) such that its Fourier symbol is \(c|\xi|\).\end{enumerate}
Then \(T_t\) is consistent with the Poisson semigroup: for any \(f\in L^1(\RR)\)\begin{equation}\label{eq:dephys-poisson-semigroup-fourier}
\boxed{\;
\widehat{T_t f}(\xi)=e^{-t|\xi|}\,\widehat f(\xi)\qquad(\xi\in\RR),\;}
\end{equation}
Thus \(T_t f=P_t*f\), where \(P_t(x)=\frac{1}{\pi}\frac{t}{x^2+t^2}\) is a Poisson core.Therefore, under the constraint of "taking the horizon as the only continuous interface", coarse-grained dynamics is unique under the requirements of covariance and Markov properties.\end{theorem}

\begin{proof}
By the strong continuous Markov semigroup and the homogeneous sign assumption of (iii), \(T_t\) must be\(
\widehat{T_t f}(\xi)=e^{-t c|\xi|}\widehat f(\xi)
\) semigroup of multipliers.By normalizing (i)–(ii) (preserving the measure/probabilistic kernel mass) one can take \(c=1\), giving \eqref{eq:dephys-poisson-semigroup-fourier}.Invert the Fourier transform of \(e^{-t|\xi|}\) to obtain the Poisson kernel \(P_t\), so \(T_t f=P_t*f\). Certification completed.\end{proof}

\begin{corollary}[Stieltjes inversion of scale entropy curves: depth spectra uniquely recoverable on univariate orbits]\label{cor:dephys-stieltjes-inversion-scale-entropy}Under the limited point defect aperture, the scale entropy curve\[
S_{\mathrm{def}}(t):=\sum_j m_j\,\frac{\delta_j}{t+1+\delta_j}\qquad(t>0)
\]
is a Stieltjes function, and its complex analytic continuation has a simple pole at \(t=-1-\delta_j\) and a residual of \(m_j\delta_j\);Therefore, the value of \(S_{\mathrm{def}}(t)\) on any set of \(t>0\) containing infinitely many points uniquely determines the multiset \(\{(\delta_j,m_j)\}\) (merging atoms of the same depth, ignoring permutations).Furthermore, its derivative chain gives a perfectly positive moment chain\[
\boxed{\;
\frac{(-1)^n}{n!}\,S_{\mathrm{def}}^{(n)}(0)
=\sum_j m_j\,\delta_j(1+\delta_j)^{-n-1}\qquad(n\ge 0).\;}
\]
This conclusion is consistent with the complete monotonicity/pole identification of the proposition \ref{prop:xi-defect-entropy-scale-fingerprint-stieltjes} and elevates it to a unique formulation of recovery in the sense of Stieltjes inversion.\end{corollary}

\begin{corollary}[Coarse-grained resolvable lower bound: Poisson flow down \texorpdfstring{XPH0X}{L1} convergence controlled by second-order central moments]\label{cor:dephys-poisson-tv-bound-variance}Let the normalized profile density \(h:=H/\|H\|_{L^1(\RR)}\) be the finite Cauchy--Poisson mixture, and let \(h_t:=P_t*h\) (\(P_t\) be the Poisson kernel).Denote the center of gravity of \(h\) as \(\bar\gamma\), and give the second-order central moment with depth weight\(
\mathrm{Var}(\gamma)
\)。
Then there is an absolute constant \(C>0\) such that when \(t\to\infty\)\[
\boxed{\;
\|h_t-\mathrm{Cauchy}(\bar\gamma,t)\|_{L^1(\RR)}
\ \le\ C\,\frac{\mathrm{Var}(\gamma)}{t^2}\;}
\]
Among them \(\mathrm{Cauchy}(\bar\gamma,t)(x)=\frac{1}{\pi}\frac{t}{(x-\bar\gamma)^2+t^2}\).This inequality is asymptotically compatible with the consistent \(O(t^{-2})\) of proposition\end{corollary}

\begin{proof}[Points of Proof]By the Taylor expansion of the proposition \ref{prop:xi-poisson-coarsegraining-nohair}, the difference between \(h_t\) and a single Cauchy kernel determined by \((\|H\|_1,\bar\gamma)\) is consistent on \(x\)- toThen integrate \(x\in\RR\) and use the consistent bound of \(\int_{\RR}\frac{t}{(x^2+t^2)}\,dx=\pi\) and the second derivative of \(t^{-3}\) to get the estimate of \(L^1\). Certification completed.\end{proof}

\begin{theorem}[Impotent monad characterization of horizon data structure: recycling closures as information projections]\label{thm:dephys-idempotent-monad-i-projection}Under the setting of defining \ref{def:dephys-conditional-expectation-externalization-map}, \(\Pi(\rho)=\rho\circ E\) satisfies \(\Pi^2=\Pi\), thereby defining an idempotent projection on the state space;Its fixed point set \(\mathrm{Fix}(\Pi)\) is exactly the entire set of "externally visible states".And for any \(\rho\in\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\) and any \(\sigma\in\mathrm{Fix}(\Pi)\), the relative entropy satisfies the strict Pythagoras decomposition\begin{equation}\label{eq:dephys-relative-entropy-pythagoras}
\boxed{\;
D(\rho\|\sigma)=D(\rho\|\Pi(\rho)) + D(\Pi(\rho)\|\sigma).\;}
\end{equation}
Therefore \(\Pi(\rho)\) is the unique information projection (\(I\)-projection) of \(\rho\) on the set \(\mathrm{Fix}(\Pi)\):The invisible degrees of freedom are exactly encapsulated as the logarithmic potential of \(D(\rho\|\Pi(\rho))\), while all externally verifiable predicates depend only on the quotient data \(\Pi(\rho)\).\end{theorem}

\begin{proof}[Points of Proof]From \(E^2=E\) we get \(\Pi^2=\Pi\), and \(\sigma\in\mathrm{Fix}(\Pi)\) is equivalent to \(\sigma=\sigma\circ E\).The Pythagoras decomposition \eqref{eq:dephys-relative-entropy-pythagoras} can be viewed as the orthogonal decomposition of relative entropy on the "information projection" induced by conditional expectations:It is essentially equivalent to the chain formula of relative entropy and the "sufficient statistics/recoverability" structure of \(E\) (see the discussion of the equality condition and quantum Markov structure of \cite{Petz2003MonotonicityRelativeEntropyRevisited}).In the case of finite dimensions (density matrices), this decomposition can also be directly verified by the orthogonal decomposition of the log-likelihood; the general von Neumann algebra case is given by the modular operator formulation of Araki relative entropy. Certification completed.\end{proof}

\begin{proposition}[The unique entropy of idempotent recycled monads: the characterization axiom of \texorpdfstring{XPH0X}{D(rho||Pi(rho))}]\label{prop:dephys-idempotent-monad-entropy-axiomatic-uniqueness}Let \(\Pi\) be the externalized idempotent projection defined by \eqref{eq:dephys-ext-externalization-Pi}.Assume that the recovery cost functional \(C:\mathcal{S}(\mathcal{A}_{\mathrm{bulk}})\to[0,\infty)\) satisfies the following axiom family:\begin{enumerate}
\item \textbf{monad invariance:} \(C(\rho)=0\iff \rho\in\mathrm{Fix}(\Pi)\);\item \textbf{additivity:} Externalizing tensor product \(\Pi_{12}:=\Pi_1\otimes\Pi_2\) has\item \textbf{Monotonicity:}For any quantum channel \(\mathcal{N}\) that exchanges with\item \textbf{\(I\)-Projective decomposition:}There exists a constant  \[
  \boxed{\;
  D(\rho\|\sigma)=c^{-1}C(\rho)+D(\Pi(\rho)\|\sigma)\;}
  \]
(Same type as \eqref{eq:dephys-relative-entropy-pythagoras}).\end{enumerate}
Then \(C\) must be the only scaling of the relative entropy projection potential:\[
\boxed{\;
C(\rho)=c\,D\bigl(\rho\ \|\ \Pi(\rho)\bigr)\;}
\]
where the proportionality constant \(c\) only reflects the logarithmic base/unit selection.\end{proposition}
\begin{proof}[Points of Proof]Take \(\sigma=\Pi(\rho)\in\mathrm{Fix}(\Pi)\) from item (iv) to get\(
D(\rho\|\Pi(\rho))=c^{-1}C(\rho)
\), which is \(C(\rho)=cD(\rho\|\Pi(\rho))\).The remaining axioms and theorems The structural compatibility (monotonicity, additivity) of \ref{thm:dephys-idempotent-monad-i-projection} is given by the standard properties of relative entropy. Certification completed.\end{proof}

\begin{corollary}[Multi-period accumulation exponential lower bound: linear growth of linkable residuals from two sources]\label{cor:dephys-multiplicity-index-gap-linear-cmi}Under the setting of proposition \ref{prop:dephys-commuting-square-mutual-information-defect},Suppose there is a finite index conditional expectation \(E_i:\mathcal{A}_{\mathrm{bulk}}\to \mathcal{A}_{\mathrm{ext}}^{(i)}\) (\(i=1,2\)) and a finite index expectation \(E_0\) on its intersection \(\mathcal{A}_{\mathrm{ext}}^{(0)}:=\mathcal{A}_{\mathrm{ext}}^{(1)}\cap\mathcal{A}_{\mathrm{ext}}^{(2)}\), and let the index gap\[
\Gamma:=\log\frac{\mathrm{Ind}(E_1)\mathrm{Ind}(E_2)}{\mathrm{Ind}(E_0)}\ \ge\ 0.
\]
Consider the \(n\) phase independently repeating (tensorized) systems \(\mathcal{A}_{\mathrm{bulk}}^{\otimes n}\) and \((\mathcal{A}_{\mathrm{ext}}^{(i)})^{\otimes n}\).Then by the multiplication of exponents\(
\mathrm{Ind}(E_i^{\otimes n})=\mathrm{Ind}(E_i)^n
\)
The gap is enlarged to \(n\Gamma\).Furthermore, under the conditional mutual information caliber of Appendix \SXPH1X (corollary \ref{cor:op-algebra-cmi-lower-bound-by-index-gap}), there is a linear lower bound for the conditional mutual information of any \(\varphi\)-holding state family\[
\boxed{\;
I\bigl((\mathcal{A}_{\mathrm{ext}}^{(1)})^{\otimes n}:(\mathcal{A}_{\mathrm{ext}}^{(2)})^{\otimes n}\,\big|\,(\mathcal{A}_{\mathrm{ext}}^{(0)})^{\otimes n}\bigr)
\ \ge\ n\Gamma.\;}
\]
Therefore, as long as \(\Gamma>0\) is used, any externalization that attempts to simultaneously achieve "two sources are completely unlinkable" and "the interface is completely restored" in multi-period accumulation will encounter indelible linear accumulation leakage; the only avoidance channel is exponential saturation \(\Gamma=0\).\end{corollary}

\begin{corollary}[Exponential discrimination template for anonymous two-source completeness: exponential saturation \texorpdfstring{XPH0X}{<->} conditional independence \texorpdfstring{XPH1X}{<->} recoverable]\label{cor:dephys-two-source-anonymy-completeness-template}The settings of the inference\(
E_0=E_1E_2=E_2E_1
\)
And all three are \(\varphi\)-maintaining conditional expectations of a certain faithful regular state \(\varphi\).Then the equivalent template can be obtained by inferring \ref{cor:op-algebra-cmi-lower-bound-by-index-gap} from the appendix:\[
\boxed{\;
\Gamma=0
\quad\Longleftrightarrow\quad
I_\varphi(\mathcal{A}^{(1)}_{\mathrm{ext}}:\mathcal{A}^{(2)}_{\mathrm{ext}}\mid\mathcal{A}^{(0)}_{\mathrm{ext}})=0.\;}
\]
When the above equivalence holds, "the conditional independence of the two source interfaces under the cross-visibility factorTherefore, for any external channel that only relies on the external image in this state family, its output can be simulated by the universal factorization of the external image (inference \ref{cor:dephys-universal-zk-simulation-factorization}), which can be regarded as a strict zero-knowledge expression of "anonymous two-source completeness".\end{corollary}

\begin{corollary}[Rényi flatness: the worst loss under finite folding horizon is independent of order \texorpdfstring{XPH0X}{alpha}]\label{cor:dephys-renyi-flatness-fold-commutative}Let \(\Omega\twoheadrightarrow X\) be the finite exchange folding, and let \(E:\CC^{\Omega}\to\CC^{X}\) be the conditional expectation uniformly averaged over the fibers.Record the maximum fiber multiplicity \(M:=\max_{x\in X}d(x)\), then \(\mathrm{Ind}(E)=M\) (the exchange caliber of proposition/inference \ref{prop:op-algebra-index-extremal-entropy-loss-maxfiber}).For any \(\alpha\in[1,\infty]\), let \(D_\alpha\) be the sandwiched Rényi relative entropy (in the exchange case), then the flat extreme value law is established\[
\boxed{\;
\sup_{\rho}D_\alpha(\rho\mid\rho\circ E)=\log M=\log\mathrm{Ind}(E),\qquad(\forall\,\alpha\in[1,\infty]).\;}
\]
And the supremum is realized by the "point state on the largest fiber": taking \(a\in\Omega\) to make \(d(\Fold(a))=M\), then for \(\rho=\delta_a\) we have\(
D_\alpha(\rho\mid\rho\circ E)=\log M
\)
Established simultaneously for all \(\alpha\in[1,\infty]\).\end{corollary}

\begin{proof}
Infer \ref{cor:op-algebra-renyi-loss-spectrum-capped-by-index} from the appendix, for any \(\rho\) and any \(\alpha\)\(
D_\alpha(\rho\mid\rho\circ E)\le \log\mathrm{Ind}(E)=\log M
\)。
On the other hand, for \(a\), it belongs to the largest fiber, and for \(\rho=\delta_a\), \(\rho\circ E\) is a uniform state on the fiber;In the exchange case, directly substitute the definition of Rényi divergence to get\(
D_\alpha(\delta_a\mid \mathrm{Unif}_{F^\star})=\log M
\)
(irrelevant to \(\alpha\)), so that the upper bound is reachable and gives a supremum bound. Certification completed.\end{proof}

\endinput

