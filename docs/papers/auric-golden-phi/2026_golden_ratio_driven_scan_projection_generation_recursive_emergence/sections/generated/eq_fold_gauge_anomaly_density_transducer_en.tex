% Auto-generated by scripts/exp_fold_gauge_anomaly_density_transducer.py
\begin{proposition}[Closed form for fold gauge discrepancy density: \st=\frac49$]\label{prop:fold-gauge-anomaly-density-49}
Under the uniform baseline $\omega\sim\mathrm{Unif}(\Omega_{m+1})$, let $\bar G_m=\mathbb E[G_m(\omega)]$. Then the limit
\[
g_\ast:=\lim_{m\to\infty}\frac{\bar G_m}{m}
\]
exists and satisfies the exact identity
\[
\boxed{\;g_\ast=\frac49\;}.
\]
\end{proposition}

\begin{proof}[Proof (auditable)]
We use Berstel's 4-state unambiguous normalization transducer (\cite{ITA_2001__35_6_491_0}, Figure~1) to describe Zeckendorf normalization. This transduction is synchronous (bitwise input/bitwise output), and the bitwise mismatch depends only on the output bit at the current position. Let $ denote the reachable state set of the input NFA driven by prefix \cdots x_{i-1}$ (subset determinization after forcing leading \/bin/zsh$), and let $ denote the co-reachable state set of the reverse input NFA driven by suffix \{i+1}x_{i+2}\cdots$ (subset determinization starting from the terminal state set). Unambiguity guarantees: for any triple $, the chosen output bit is uniquely determined. Under IID Bernoulli$ input, $ and $ each induce finite-state Markov chains; moreover, $ depends only on the past and $ only on the future, so they are independent at the cut point. By the ergodic theorem, $\bar G_m/m$ converges to the stationary expectation on this finite synchronous kernel.
Subset determinization yields: the prefix chain has two states $\{a,b\},\{c,d\}$, with transitions \(\{a,b\}\xrightarrow{0}\{a,b\},\ \{a,b\}\xrightarrow{1}\{c,d\},\ \{c,d\}\xrightarrow{0}\{a,b\},\ \{c,d\}\xrightarrow{1}\{a,b\}\), hence stationary distribution $. Similarly, the suffix chain has three states $\{a,c\},\{a,d\},\{b,c\}$, with transitions \(\{a,d\}\xrightarrow{0}\{a,d\},\ \{a,d\}\xrightarrow{1}\{a,c\},\ \{a,c\}\xrightarrow{0}\{a,d\},\ \{a,c\}\xrightarrow{1}\{b,c\},\ \{b,c\}\xrightarrow{0}\{a,c\},\ \{b,c\}\xrightarrow{1}\{b,c\}\), hence uniform stationary distribution $. Enumerating each $ case for its unique output bit and weighting by the above probabilities yields the exact identity \st=4/9$.
More precisely, mismatches occur only in the following five cases: \=1$ and \in\{(\{a,b\},\{a,c\}),(\{c,d\},\{a,c\}),(\{c,d\},\{a,d\})\}$, as well as \=(\{a,b\},\{b,c\})$ (where mismatch occurs for both \=0,1$). Thus
\[
g_\ast=\frac12\Bigl(\frac23\cdot\frac13+\frac13\cdot\frac13+\frac13\cdot\frac13+2\cdot\frac23\cdot\frac13\Bigr)=\frac49.
\]
All the above subset-chain states, stationary distributions, and output uniqueness can be reproduced and cross-audited via the script \texttt{scripts/exp\_fold\_gauge\_anomaly\_density\_transducer.py}.
\end{proof}
