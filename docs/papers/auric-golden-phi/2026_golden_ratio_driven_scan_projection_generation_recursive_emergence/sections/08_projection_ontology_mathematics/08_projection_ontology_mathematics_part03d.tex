对 \(\gamma\ge 0\) 与 \(\varepsilon>0\)，定义“近似层”计数
$$
N_m(\gamma,\varepsilon):=\Bigl|\bigl\{x\in X_m:\ |\gamma_m(x)-\gamma|\le \varepsilon\bigr\}\Bigr|.
$$
定义纤维多重度的（上）谱函数
$$
f(\gamma):=\lim_{\varepsilon\downarrow 0}\ \limsup_{m\to\infty}\ \frac{1}{m}\log N_m(\gamma,\varepsilon).
$$
\end{definition}

\begin{proposition}[碰撞谱的几何含义：纤维指数的指数矩母函数]\label{prop:pom-fiber-index-cgf}
沿用 \(\pi_m(x)=d_m(x)/2^m\) 的输出分布记号，并把 \(\gamma_m(X)\) 视为在 \(X_m\) 上以 \(\pi_m\) 抽样的随机变量。
则对任意整数 \(q\ge 2\) 有严格恒等式
\[
\boxed{\;
\mathbb{E}_{\pi_m}\!\left[\exp\!\bigl((q-1)m\,\gamma_m(X)\bigr)\right]
=\frac{S_q(m)}{2^m}\; }.
\]
从而
\[
\frac{1}{m}\log \mathbb{E}_{\pi_m}\!\left[\exp\!\bigl((q-1)m\,\gamma_m(X)\bigr)\right]
=\frac{1}{m}\log S_q(m)-\log 2.
\]
特别地，若存在 Perron 主尺度 \(S_q(m)=\Theta(r_q^m)\)（例如由 moment-kernel 实现给出），则极限累积母函数在整点 \(t=q-1\) 上满足
\[
\lim_{m\to\infty}\frac{1}{m}\log \mathbb{E}_{\pi_m}\!\left[e^{t m\,\gamma_m(X)}\right]
=\log r_{t+1}-\log 2,
\qquad
h_{t+1}=(t+1)\log 2-\log r_{t+1}.
\]
\end{proposition}
\begin{proof}
按定义展开并用 \(\pi_m(x)=d_m(x)/2^m\)：
\[
\mathbb{E}_{\pi_m}\!\left[\exp\!\bigl((q-1)m\,\gamma_m(X)\bigr)\right]
=\sum_{x\in X_m}\pi_m(x)\,e^{(q-1)\log d_m(x)}
=\sum_{x\in X_m}\frac{d_m(x)}{2^m}\,d_m(x)^{q-1}
=\frac{1}{2^m}\sum_{x\in X_m}d_m(x)^q
=\frac{S_q(m)}{2^m}.
\]
取对数并除以 \(m\) 得第二式。最后一段由 \(S_q(m)=\Theta(r_q^m)\) 的指数尺度与 \(h_q=\log(2^q/r_q)\) 的定义直接推出。证毕。
\end{proof}

\begin{remark}[大偏差/多分形反演（作为可检验目标）]\label{rem:pom-fiber-index-ldp}
命题 \ref{prop:pom-fiber-index-cgf} 表明：碰撞谱 \(q\mapsto h_q\) 等价于纤维指数 \(\gamma_m\) 的指数矩母函数在整点的增长率。
若进一步能把 \(q\)-重 moment-kernel 的“整数参数”推广为实参数 \(t\ge 0\) 的加权核，从而得到一个凸函数
\(
\Lambda(t):=\lim_{m\to\infty}\frac{1}{m}\log \mathbb{E}_{\pi_m}[e^{t m\,\gamma_m(X)}]
\)
并满足标准的可微/本质严格凸条件，则 Gartner--Ellis 定理给出 \(\gamma_m\) 的大偏差率函数
\[
I(\gamma)=\sup_{t\ge 0}\bigl(t\gamma-\Lambda(t)\bigr).
\]
此时 \(I\)（从而纤维厚度谱）可由碰撞谱“反演”得到，构成“从 \(h_q\) 指纹回推纤维几何”的可证伪计算目标。
\end{remark}

\subsubsection{典型空间损失常数：均值率与方差率}\label{subsec:pom-fiber-log-moments}
把
\[
L_m:=\log d_m(X),\qquad X\sim \pi_m,\ \ \pi_m(x)=\frac{d_m(x)}{2^m},
\]
视为“对数纤维体积”的随机变量。
定义其低阶累积量密度为
\[
\mu_m:=\frac{1}{m}\mathbb{E}_{\pi_m}[L_m],
\qquad
\sigma_m^2:=\frac{1}{m}\mathrm{Var}_{\pi_m}(L_m).
\]
若极限累积母函数
\(
\Lambda(t)=\lim_{m\to\infty}\frac{1}{m}\log \mathbb{E}_{\pi_m}[e^{tL_m}]
\)
在 \(t=0\) 的邻域可二次可微，并且可交换“取极限/求导”（标准热力学形式下常见），则有
\[
\mu=\Lambda'(0)=\lim_{m\to\infty}\mu_m,\qquad
\sigma^2=\Lambda''(0)=\lim_{m\to\infty}\sigma_m^2.
\]
\begin{remark}[碰撞谱 \texorpdfstring{$\leftrightarrow$}{<->} 典型损失：\texorpdfstring{$\Lambda(t)=\tau(t+1)-\log2$}{Lambda(t)=tau(t+1)-log2}]\label{rem:pom-lambda-tau-shift}
把 \(S_q(m)\) 视为实参数 \(q\in\mathbb{R}\) 的分配函数
\[
S_q(m):=\sum_{x\in X_m} d_m(x)^q
\]
（当 \(q\in\mathbb{Z}_{\ge 2}\) 时与本文的碰撞矩一致）。
则对任意固定 \(m\) 与任意 \(t\in\mathbb{R}\)，由 \(\pi_m(x)=d_m(x)/2^m\) 直接展开得严格恒等式
\[
\mathbb{E}_{\pi_m}[e^{tL_m}]
=\sum_{x\in X_m}\frac{d_m(x)}{2^m}\,d_m(x)^t
=\frac{S_{t+1}(m)}{2^m}.
\]
因此若记
\[
\tau_m(q):=\frac1m\log S_q(m),
\qquad
\tau(q):=\lim_{m\to\infty}\tau_m(q)\quad(\text{在极限存在处}),
\]
则
\[
\frac1m\log \mathbb{E}_{\pi_m}[e^{tL_m}]=\tau_m(t+1)-\log2,
\qquad
\Lambda(t)=\tau(t+1)-\log2.
\]
并且在 \(q=1\) 处（无需取极限）有精确导数恒等式
\[
\mu_m=\tau_m'(1),
\qquad
\sigma_m^2=\tau_m''(1).
\]
在可交换“取极限/求导”的假设下，上式取 \(m\to\infty\) 给出 \(\mu=\tau'(1)\) 与 \(\sigma^2=\tau''(1)\)。
\end{remark}

\begin{corollary}[零参数审计夹逼：\texorpdfstring{$\mu$}{mu} 被 \texorpdfstring{$(r_0,r_1,r_2)$}{(r0,r1,r2)} 两端夹死]\label{cor:pom-mu-secant-audit}
对每个固定 \(m\)，函数 \(q\mapsto \tau_m(q)=(1/m)\log S_q(m)\) 为凸函数，故其在 \(q=1\) 处的导数满足 secant 夹逼
\[
\boxed{\;
\tau_m(1)-\tau_m(0)\ \le\ \mu_m\ \le\ \tau_m(2)-\tau_m(1).\;}
\]
在 Fold 的口径下 \(S_1(m)=\sum_x d_m(x)=2^m\)、\(S_0(m)=|X_m|=F_{m+2}\)，从而得到可直接审计的有限 \(m\) 夹逼
\[
\boxed{\;
\log2-\frac1m\log F_{m+2}\ \le\ \mu_m\ \le\ \frac1m\log S_2(m)-\log2.\;}
\]
若进一步 \(r_2=\lim_{m\to\infty}S_2(m)^{1/m}\) 存在（由二体碰撞核 \(A_2\) 精确给出），并记端点谱常数
\(
r_0:=\lim_{m\to\infty}|X_m|^{1/m}=\varphi
\)
与
\(
r_1:=2
\)，
则取 \(m\to\infty\) 得到极限版“零参数”审计区间
\[
\boxed{\;
\log\frac{2}{r_0}\ \le\ \mu\ \le\ \log\frac{r_2}{2}\;}
\qquad(\text{即 }\ \log(2/\varphi)\le\mu\le\log(r_2/2)).
\]
\end{corollary}
\begin{proof}
由于 \(S_q(m)=\sum_x e^{q\log d_m(x)}\)，函数 \(q\mapsto \log S_q(m)\) 为 log-sum-exp，因此凸；除以 \(m\) 保持凸性。对凸函数在点 \(q=1\) 应用 secant 夹逼即得第一条。其余陈述把 \(S_0,S_1\) 的端点等式代入并取极限即可。证毕。
\end{proof}
表 \ref{tab:fold_fiber_log_moments_mu_sigma} 给出通过模 \(F_{m+2}\) 的 residue DP 精确计算得到的 \((\mu_m,\sigma_m^2)\) 数值，作为“典型空间损失密度与热涨落强度”的可审计常数接口。

\input{sections/generated/tab_fold_fiber_log_moments_mu_sigma}

\begin{proposition}[Legendre 上界：谱由 moment 压力控制]\label{prop:pom-spectrum-legendre-upper}
对任意 \(q\ge 2\) 与任意 \(\gamma\ge 0\)，有上界
$$
f(\gamma)\ \le\ \inf_{q\ge 2}\bigl(\tau(q)-q\gamma\bigr).
$$
特别地，在 \(\tau(q)=\log r_q\) 的情形下，
$$
f(\gamma)\ \le\ \inf_{q\ge 2}\bigl(\log r_q-q\gamma\bigr).
$$
\end{proposition}

\begin{proof}
固定 \(q\ge 2\) 与 \(\gamma,\varepsilon\)。对任意满足 \(|\gamma_m(x)-\gamma|\le\varepsilon\) 的 \(x\)，有
$$
d_m(x)^q=\exp(qm\gamma_m(x))\ \ge\ \exp\bigl(qm(\gamma-\varepsilon)\bigr).
$$
因此
$$
S_q(m)=\sum_{x\in X_m} d_m(x)^q
\ \ge\ N_m(\gamma,\varepsilon)\,\exp\bigl(qm(\gamma-\varepsilon)\bigr),
$$
即
$$
\frac{1}{m}\log N_m(\gamma,\varepsilon)\ \le\ \frac{1}{m}\log S_q(m)\ -\ q(\gamma-\varepsilon).
$$
取 \(m\to\infty\) 的 \(\limsup\) 并再令 \(\varepsilon\downarrow 0\)，得
$$
f(\gamma)\le \tau(q)-q\gamma.
$$
对所有 \(q\ge 2\) 取下确界即得结论。证毕。
\end{proof}

\begin{remark}[多重分形形式（作为可检验目标）]\label{rem:pom-spectrum-formalism}
命题 \ref{prop:pom-spectrum-legendre-upper} 给出严格的上界：moment 压力曲线 \(\tau(q)\) 的凸共轭控制了纤维多重度的（上）谱。
在标准热力学形式条件下（例如存在与 \(\tau\) 对偶的 Gibbs 结构并具有足够的准乘性），往往可得到相反方向的不等式从而实现等号，即“谱 \(f\) 与压力 \(\tau\) 互为 Legendre 变换”。
在本文框架中，这一等号可被视为一条可证伪的结构目标：它把“空间损失是一条谱”转化为“\(\{r_q\}\) 族决定谱形”的可计算命题。
\end{remark}

\begin{proposition}[Paley--Zygmund 下包络：仅由 \texorpdfstring{$(r_q,r_{2q})$}{(rq,r2q)} 给出“厚尾”指数下界]\label{prop:pom-spectrum-pz-lower}
取任意整数 \(q\ge 2\)。令 \(X\) 在 \(X_m\) 上均匀分布，并令非负随机变量
\[
Y:=d_m(X)^q.
\]
则对阈值 \(\theta=\tfrac12\) 的 Paley--Zygmund 不等式给出
\[
\#\Bigl\{x\in X_m:\ d_m(x)\ge \Bigl(\tfrac12\,\frac{S_q(m)}{|X_m|}\Bigr)^{1/q}\Bigr\}
\ \ge\ \frac14\cdot\frac{S_q(m)^2}{S_{2q}(m)}.
\]
从而若极限 \(r_k=\lim_{m\to\infty}S_k(m)^{1/m}\) 在 \(k=q,2q\) 处存在，并且 \(\lim_{m\to\infty}\frac1m\log|X_m|=\log\varphi\)，则定义
\[
\gamma_q:=\frac{\log r_q-\log\varphi}{q},
\qquad
\underline f(q):=2\log r_q-\log r_{2q},
\]
有严格下界
\[
\boxed{\;
\lim_{\varepsilon\downarrow 0}\ \liminf_{m\to\infty}\ \frac1m
\log \#\bigl\{x\in X_m:\ \gamma_m(x)\ge \gamma_q-\varepsilon\bigr\}
\ \ge\ \underline f(q)\; }.
\]
\end{proposition}
\begin{proof}
按均匀抽样 \(X\sim \mathrm{Unif}(X_m)\) 展开期望：
\[
\mathbb{E}[Y]=\frac{1}{|X_m|}\sum_{x\in X_m}d_m(x)^q=\frac{S_q(m)}{|X_m|},
\qquad
\mathbb{E}[Y^2]=\frac{S_{2q}(m)}{|X_m|}.
\]
对任意非负 \(Y\) 与 \(\theta\in(0,1)\)，Paley--Zygmund 不等式给出
\[
\mathbb{P}\!\left(Y\ge \theta\,\mathbb{E}Y\right)\ \ge\ (1-\theta)^2\frac{(\mathbb{E}Y)^2}{\mathbb{E}[Y^2]}.
\]
取 \(\theta=\tfrac12\) 并代入上式得到
\[
\mathbb{P}\!\left(Y\ge \tfrac12\,\mathbb{E}Y\right)\ \ge\ \frac14\cdot\frac{S_q(m)^2}{|X_m|\,S_{2q}(m)}.
\]
把概率乘以 \(|X_m|\) 即得到计数下界：
\[
\#\Bigl\{x\in X_m:\ d_m(x)^q\ge \tfrac12\,\frac{S_q(m)}{|X_m|}\Bigr\}
\ \ge\ \frac14\cdot\frac{S_q(m)^2}{S_{2q}(m)},
\]
等价改写即得命题第一条不等式。
\par
对第二条，取对数并除以 \(m\)，并令 \(m\to\infty\)：
右端给出
\[
\liminf_{m\to\infty}\frac1m\log\!\left(\frac14\cdot\frac{S_q(m)^2}{S_{2q}(m)}\right)
=2\log r_q-\log r_{2q}=\underline f(q),
\]
而左端的阈值满足
\[
\frac1m\log\Bigl(\Bigl(\tfrac12\,\frac{S_q(m)}{|X_m|}\Bigr)^{1/q}\Bigr)
=\frac{1}{q}\Bigl(\frac1m\log S_q(m)-\frac1m\log|X_m|+\frac1m\log\tfrac12\Bigr)
\to \gamma_q.
\]
因此对任意 \(\varepsilon>0\)，当 \(m\) 足够大时左端集合包含于 \(\{\gamma_m(x)\ge \gamma_q-\varepsilon\}\)，从而得到所述的 \(\varepsilon\downarrow 0\) 的 \(\liminf\) 下界。证毕。
\end{proof}

\begin{corollary}[间歇性缺口 \texorpdfstring{$\delta(q)$}{delta(q)}：从表格数值可直接审计的“尾部变薄”指标]\label{cor:pom-spectrum-intermittency-deficit}
在命题 \ref{prop:pom-spectrum-pz-lower} 的设定下，
\[
\underline f(q)\ \le\ \log\varphi,
\]
并可定义非负缺口
\[
\boxed{\;
\delta(q):=\log\varphi-\underline f(q)
=\log\varphi+\log r_{2q}-2\log r_q\ \ge\ 0.\;}
\]
此外，命题 \ref{prop:pom-spectrum-legendre-upper} 在特殊点
\(
\gamma=\gamma_q=(\log r_q-\log\varphi)/q
\)
处的上界读出为
\[
f(\gamma_q)\ \le\ \log r_q-q\gamma_q=\log\varphi,
\]
从而 \(\underline f(q)\) 给出“靠近 \(\gamma_q\) 的高多重度尾部”所必然包含的指数级稳定类型下界，而 \(\delta(q)\) 量化了该下界相对最大可能指数 \(\log\varphi\) 的亏损。
\end{corollary}
\begin{proof}
由 Cauchy--Schwarz（或 Hölder）不等式
\[
\Bigl(\sum_{x\in X_m}d_m(x)^q\Bigr)^2\le |X_m|\sum_{x\in X_m}d_m(x)^{2q}
\]
得 \(S_q(m)^2\le |X_m|S_{2q}(m)\)。取对数并除以 \(m\)，再令 \(m\to\infty\)，得到
\(
2\log r_q-\log r_{2q}\le \log\varphi
\)，即 \(\underline f(q)\le\log\varphi\) 与 \(\delta(q)\ge 0\)。
其余陈述由命题 \ref{prop:pom-spectrum-pz-lower} 与 \(\gamma_q\) 的定义直接推出。证毕。
\end{proof}

\begin{remark}[可复核读出：\texorpdfstring{$q\le 10$}{q<=10} 的下包络点与两侧包络图]\label{rem:pom-spectrum-pz-numerics}
对 \(q\le 10\)，由表 \ref{tab:fold_collision_renyi_spectrum_mod_dp_q20} 的 \((r_q,r_{2q})\) 可直接计算
\(
(\gamma_q,\underline f(q),\delta(q))
\)（无需任何新 DP）。
脚本 \texttt{scripts/exp\_fold\_collision\_multifractal\_envelope\_pz\_q10.py} 会从该表格片段读出 \(r_q\) 并生成派生表 \ref{tab:fold_collision_multifractal_envelope_pz_q10} 与对比图 \ref{fig:fold-collision-multifractal-envelope-pz}。
例如由表中 \(r_2,r_4,r_{10},r_{20}\) 得
\(
\delta(2)\approx 0.0108
\)、
\(
\delta(10)\approx 0.0804
\)，显示高阶矩探针下“厚尾保证指数”随 \(q\) 增大而显著下降。
\input{sections/generated/tab_fold_collision_multifractal_envelope_pz_q10}
\input{sections/generated/fig_fold_collision_multifractal_envelope_pz_q10}
\end{remark}

\begin{proposition}[有限 \texorpdfstring{$m$}{m} 的“典型窗平台”：由 \texorpdfstring{$(\mu_m,\sigma_m^2)$}{(mu,sigma2)} 给出指数级下界]\label{prop:pom-spectrum-finite-m-plateau}
令 \(w_m(x)=d_m(x)/2^m\) 为均匀微态的折叠推前（定义 \ref{def:pom-wm-baseline}；在本小节亦记作 \(\pi_m\)），令 \(X\sim w_m\)，并记
\(
\gamma_m(X)=(1/m)\log d_m(X)
\)。
则对任意 \(\varepsilon>0\)，Chebyshev 不等式给出
\[
\boxed{\;
w_m\!\bigl(|\gamma_m(X)-\mu_m|>\varepsilon\bigr)\ \le\ \frac{\sigma_m^2}{m\varepsilon^2}\; }.
\]
因此集合
\(
A_m(\varepsilon):=\{x\in X_m:\ |\gamma_m(x)-\mu_m|\le \varepsilon\}
\)
满足计数下界
\[
\boxed{\;
|A_m(\varepsilon)|
\ \ge\
\left(1-\frac{\sigma_m^2}{m\varepsilon^2}\right)
\exp\!\bigl(m(\log 2-\mu_m-\varepsilon)\bigr)\; }.
\]
上式右端只依赖于表 \ref{tab:fold_fiber_log_moments_mu_sigma} 的 \((\mu_m,\sigma_m^2)\)（以及常数 \(\log 2\)），因此提供了一个完全可审计的有限分辨率“平台保证”：在固定窗口宽度 \(\varepsilon\) 下，存在指数级多的稳定类型其 \(\gamma_m\) 落在 \(\mu_m\) 附近。
\end{proposition}
\begin{proof}
由 \(\mathrm{Var}(\log d_m(X))=m\sigma_m^2\) 与 Chebyshev 得
\[
w_m\bigl(|\log d_m(X)-\mathbb{E}\log d_m(X)|>m\varepsilon\bigr)
\le \frac{m\sigma_m^2}{m^2\varepsilon^2}
=\frac{\sigma_m^2}{m\varepsilon^2},
\]
除以 \(m\) 即得到第一式。
\par
对第二式，注意对任意 \(x\in A_m(\varepsilon)\)，有
\(
\log d_m(x)\le m(\mu_m+\varepsilon)
\)，从而
\(
w_m(x)=d_m(x)/2^m\le \exp(m(\mu_m+\varepsilon))/2^m
\)。
于是
\[
w_m(A_m(\varepsilon))
\le |A_m(\varepsilon)|\cdot \frac{\exp(m(\mu_m+\varepsilon))}{2^m}.
\]
结合第一式的下界 \(w_m(A_m(\varepsilon))\ge 1-\sigma_m^2/(m\varepsilon^2)\) 并移项即得。证毕。
\end{proof}

\begin{remark}[同一个凸共轭的两种读出：\texorpdfstring{Legendre 谱上界 \(\leftrightarrow\) Chernoff 尾界}{Legendre<->Chernoff}]\label{rem:pom-legendre-chernoff-same-dual}
命题 \ref{prop:pom-spectrum-legendre-upper}（按计数测度的几何谱上界）与命题 \ref{prop:pom-quantile-chernoff}--推论 \ref{cor:pom-quantile-dual-optimizer}（按输出测度 \(\pi_m\) 的尾部控制）本质上由同一个凸对偶核给出。
令
\[
\Phi(q):=\tau(q)-\log 2,
\qquad
\Lambda(t):=\Phi(t+1)=\tau(t+1)-\log 2\quad(t\ge 1),
\]
则 Chernoff 率函数上界为
\[
I(\gamma)=\sup_{t\ge 1}\bigl(t\gamma-\Lambda(t)\bigr)
\]
（推论 \ref{cor:pom-quantile-dual-optimizer}）。
而 Legendre 上界可改写为
\[
\begin{aligned}
\inf_{q\ge 2}\bigl(\tau(q)-q\gamma\bigr)
&=\log 2-\gamma+\inf_{t\ge 1}\bigl(\Lambda(t)-t\gamma\bigr)\\
&=\log 2-\gamma-\sup_{t\ge 1}\bigl(t\gamma-\Lambda(t)\bigr)
=\log 2-\gamma-I(\gamma).
\end{aligned}
\]
因此二者共享同一个凸共轭 \(\sup_{t\ge 1}(t\gamma-\Lambda(t))\)：差别仅来自基准测度（计数 vs.\ \(\pi_m\)）引入的 \(-\log 2\) 归一化与 \(q\leftrightarrow t=q-1\) 的平移。
\par
在可微/严格凸的扩展口径下（例如把 \(q\) 视作实参数并延拓 \(\tau\)），最优矩阶 \(q^\star(\gamma)\)（无论用于谱上界还是用于尾界预算）满足同一条“支撑线条件”
\[
\gamma\ \in\ \partial \tau\bigl(q^\star(\gamma)\bigr),
\]
即 \(\tau\) 在 \(q^\star\) 处的次梯度命中 \(\gamma\)；这与压力的离散凸性（命题 \ref{prop:pom-pressure-convexity}）一起，把“选 \(q\)”从经验旋钮升级为由凸几何决定的对偶坐标。
\end{remark}

\subsubsection{在线延迟的不可约下界}\label{subsec:pom-delay-lb}
\begin{proposition}[单趟在线延迟 $3$ 的最小性]\label{prop:pom-delay-min}
任何实现 $\Fold_\infty:\{0,1,2\}^{\ZZ}\to X_\infty$ 的单趟在线转导器，其最小延迟满足
\[
\delta_{\min}(\Fold_\infty)\ge 3.
\]
结合定理 \ref{thm:online-delay-fold}，可得 $\delta_{\min}(\Fold_\infty)=3$。
\end{proposition}
\begin{proof}
在小节 \ref{subsec:online-delay} 的在线核中，输出满足
\(
e=\lfloor \varphi R(s)+d\varphi^{-3}\rfloor
\)，其中 $s$ 为长度 $3$ 的同步状态、$d\in\{0,1,2\}$ 为当前输入。取状态 $s=002$（其属于状态集 $Q$），则
\[
R(s)=2\varphi^{-3},\qquad
e=\left\lfloor 2\varphi^{-2}+d\varphi^{-3}\right\rfloor.
\]
当 $d=0$ 时 $e=0$，当 $d=1$ 时 $e=1$，故该位输出确实依赖于“向前第 $3$ 位”输入。任何延迟 $2$ 的单趟在线机在输出此位时尚未读到该输入，必产生冲突，从而不可能实现 $\Fold_\infty$。证毕。
\end{proof}

\begin{remark}[空间缺陷与时间缺陷的正交性]\label{rem:pom-space-time-orth}
最大纤维多重度给出“值投影”的最坏空间缺陷（定理 \ref{thm:pom-max-fiber}），而单趟在线延迟给出“步骤投影”的不可约时间缺陷（命题 \ref{prop:pom-delay-min}）。两者来源于不同的投影门，因而互不替代：增大空间并不能把 $\delta_{\min}$ 压到 $2$，而压低延迟也无法消除 $d_m$ 的纤维多重度。
\end{remark}

\begin{definition}[空间—时间—谱三指纹]\label{def:pom-fingerprint}
对实现规范投影 $P_Z$ 的归一化核 $K$，记其分辨率截断为 $K_m:\Omega_m\to X_m$，并令
\(
d^{(K)}_m(x):=|K_m^{-1}(x)|
\)。
定义：
\[
S_K(m):=\max_{x}\log d^{(K)}_m(x)\quad(\text{空间指纹}),
\qquad
T_K:=\delta_{\min}(K)\quad(\text{时间指纹}),
\]
并以
\[
\Pi_K:=\{\zeta_K,\ \log\mathfrak{M}_K,\ \rho/\lambda,\ \text{Dirichlet--Mertens 常数张量等}\}
\]
作为谱指纹族（见第 \ref{sec:zeta-finite-part} 节）。三者分别量化“值投影损失”“在线不可并行延迟”与“机制谱细节”，在同一投影框架下互相正交。
\end{definition}

\begin{remark}[Fold 的三指纹]\label{rem:pom-fold-fingerprint}
对 Zeckendorf 归一化核 $K=\Fold_\infty$，由定理 \ref{thm:pom-max-fiber} 得 $S_K(m)=\log D_m$，由命题 \ref{prop:pom-delay-min} 得 $T_K=3$，而 $\Pi_K$ 可由 10/40 状态同步核的 $\zeta$、$\log\mathfrak{M}$ 与 Dirichlet--Mertens 张量显式抽取（见第 \ref{sec:zeta-finite-part} 节与附录 \ref{app:sync-kernel-basic}、\ref{app:real-input-40-abel}）。因此“空间(值)$\times$时间(步骤)$\times$谱(机制)”形成可比较的统一量化框架。
\end{remark}

\begin{theorem}[统一闭环：在线核—碰撞矩核—\texorpdfstring{$\zeta$/primitive}{zeta/primitive}—几何维数谱]\label{thm:pom-unified-closure}
以 $K=\Fold_\infty$ 为规范投影核。则下述链路在同一套“有限核 $\Rightarrow$ 谱 $\Rightarrow$ 轨道/有限部 $\Rightarrow$ 几何谱”的接口下闭合：
\begin{enumerate}
  \item \textbf{（在线核与时间缺陷）} $\Fold_\infty$ 存在单趟在线同步实现（附录 \ref{app:sync-kernel-basic} 给出显式 10 状态核），并且最小延迟刚性为 $\delta_{\min}=3$（命题 \ref{prop:pom-delay-min}）。
  \item \textbf{（任意阶 moment-kernel 与熵率存在）} 对任意整数 $q\ge 2$，$q$-重碰撞矩 $S_q(m)$ 可由有限状态非负整数核 $A_q$ 计数（命题 \ref{prop:pom-moment-kernel-exists}），从而 $S_q$ 为 \(\mathbb N\)-有理级数并满足有限阶整系数递推；其 Perron 根 $r_q=\rho(A_q)$ 给出指数尺度 $S_q(m)=\Theta(r_q^m)$，并诱导投影熵率
  \[
  h_q=\log\!\left(\frac{2^q}{r_q}\right)
  \]
  的存在性与闭式接口（定义 \ref{def:pom-moment-pressure}）。
  \item \textbf{（闭合维数的内禀不变量）} 在已核验范围 $q\le 8$，序列 $S_q$ 的最小线性实现维数由 Hankel 秩锁定：\(\mathrm{rank}(H)\ge 2\lfloor q/2\rfloor+1\)（定理 \ref{thm:pom-moment-hankel}），并与递推阶数一致从而在该范围内取等号（推论 \ref{cor:pom-moment-minreal}）。
  \item \textbf{（共振/坍缩的机制判别）} 当递推阶数低于 \(2\lfloor q/2\rfloor+1\) 时，差分模态满足线性约束并导致 Hankel 秩下降（定理 \ref{thm:pom-moment-resonance}）；在 \(q=9,\dots,16\) 的已核验递推中该共振被触发。
  \item \textbf{（几何化：Rényi 维数谱）} 当 $h_q$ 存在时，输出测度的 Rényi 维数谱由 $h_q$ 闭式决定：
  \[
  D_q=\frac{h_q}{(q-1)\log\varphi}
  \]
  （命题 \ref{prop:pom-renyi-dimension-spectrum}）。
  \item \textbf{（机制核的周期点闭式与 primitive 抽取）} 在真实算术输入约束下，同步核与输入记忆合成 40 状态机制核 $M$（附录 \ref{app:real-input-40-abel}；构造见命题 \ref{prop:real-input-40-essential-20} 起），其 $\zeta$-函数为显式有理式并具有谱因子分解（命题 \ref{prop:real-input-40-fib-tensor} 与推论 \ref{cor:real-input-40-spectrum-decomp}）。因此周期点计数 $a_n=\Tr(M^n)$ 具有闭式表达（同上证明段落），primitive 轨道数则由 Möbius 反演
  \[
  p_n=\frac{1}{n}\sum_{d\mid n}\mu(d)\,a_{n/d}
  \]
  直接抽取，从而把“prime-orbit/有限部常数/Dirichlet--Mertens 张量”等谱对象纳入同一可计算接口（第 \ref{sec:zeta-finite-part} 节与附录群）。
\end{enumerate}
\end{theorem}

\subsubsection{可复算 commuting package：真实输入 $40$ 状态核}\label{subsec:real-input-40-commuting-package}
\paragraph{对象：真实输入的确定性 skew-product}
令黄金均值移位（禁词 $(11)$）的邻接矩阵
\[
A=\begin{pmatrix}1&1\\[2pt]1&0\end{pmatrix},\qquad \rho(A)=\varphi.
\]
把两条合法 Zeckendorf 输入轨道 $(x,y)\in X\times X$ 并入同步核状态，得到
\(
\Sigma:=Q\times\{0,1\}^2
\)
（$|Q|=10,|\Sigma|=40$）。
每步读入 $(x_k,y_k)\in\{0,1\}^2$（受黄金均值守卫约束），令 $d_k=x_k+y_k\in\{0,1,2\}$，
并用 10 状态核的确定性转移 $s_{k+1}=\delta(s_k,d_k)$ 更新纤维坐标 $s\in Q$；
从而得到一个 $40$ 状态 SFT，其邻接矩阵记为 $M$（详见附录 \ref{app:real-input-40-abel}）。

\begin{proposition}[确定性扩张不增熵：$\rho(M)=\varphi^2$]\label{prop:real-input-40-entropy}
真实输入 $40$ 状态核的拓扑熵满足
\[
h_{\mathrm{top}}(\Sigma)=h_{\mathrm{top}}(X\times X)=2\log\varphi,
\qquad
\rho(M)=e^{h_{\mathrm{top}}(\Sigma)}=\varphi^2.
\]
\end{proposition}
\begin{proof}
基系统为输入对语法 $X\times X$，其邻接矩阵为 $A\otimes A$，故 $h_{\mathrm{top}}(X\times X)=\log\rho(A\otimes A)=2\log\varphi$。
另一方面，给定任意基系统符号序列 $\{(x_k,y_k)\}$，则 $d_k=x_k+y_k$ 唯一确定，因而纤维更新 $s_{k+1}=\delta(s_k,d_k)$ 亦唯一确定。
因此对任意长度 $n$ 的基系统路径，从每个初始纤维态 $s_0$ 出发至多抬升出一条 $\Sigma$-路径，总路径数仅乘以常数 $|Q|$，指数增长率不变。
故 $h_{\mathrm{top}}(\Sigma)=h_{\mathrm{top}}(X\times X)$，从而 $\rho(M)=\varphi^2$。证毕。
\end{proof}

\paragraph{谱—迹—Euler 乘积的闭式桥包}
附录 \ref{app:abel-mertens-universal} 与 \ref{app:real-input-40-abel} 给出该核的完全因子分解：
\[
\boxed{\;
\det(I-zM)=(1-z)^2(1+z)^3(1-3z+z^2)(1+z-z^2).\;}
\]
因此除零特征值外的非零谱（按重数计）为
\[
\sigma(M)\setminus\{0\}=\{1,1,\,-1,-1,-1,\,\varphi^2,\,\varphi^{-2},\,-\varphi,\,\varphi^{-1}\}.
\]
对应周期迹（周期点计数）有全闭式
\[
\boxed{\;
a_n:=\Tr(M^n)=
2+3(-1)^n+\varphi^{2n}+\varphi^{-2n}+\varphi^{-n}+(-\varphi)^n.\;}
\]
primitive 轨道数（按轨道计）由 Witt/Möbius 反演严格抽取：
\[
\boxed{\;
p_n=\frac1n\sum_{d\mid n}\mu(d)\,a_{n/d}.\;}
\]
并且 Euler 乘积（prime-like 侧）与迹侧严格对齐：
\[
\zeta_M(z)
=\exp\!\left(\sum_{n\ge 1}\frac{a_n}{n}z^n\right)
=\prod_{\gamma\ \mathrm{primitive}}\frac{1}{1-z^{|\gamma|}}
=\frac{1}{\det(I-zM)}.
\]

\paragraph{Abel/有限部常数：主极点与留数闭式}
由 $\rho(M)=\varphi^2$ 得主极点
\[
z_\star=\rho(M)^{-1}=\varphi^{-2}.
\]
对应留数常数与 Abel 有限部常数具有闭式读出（附录 \ref{app:real-input-40-abel}）：
\[
\boxed{\;
C=\lim_{z\to z_\star}(1-\varphi^2 z)\,\zeta_M(z)=\frac{47+21\sqrt5}{100}\approx 0.9395742753,\;}
\]
\[
\boxed{\;
\log\mathfrak{M}=\log C+\sum_{m\ge 2}\frac{\mu(m)}{m}\log\zeta_M(z_\star^{\,m})
\approx -0.2543126619.\;}
\]

\paragraph{Dirichlet/模分层：arity-charge 的有限维 Fourier 反演}
定义 arity-charge（附录 \ref{app:real-input-40-arity-charge}）
\[
\chi(s,d):=e-\mathbf{1}_{\{d=2\}}\in\{-1,0,1\},
\]
并以 $q^{\chi}$ 对边加权得到矩阵族 $M(q)$。
令
\(
c_n(q):=\Tr(M(q)^n)
\)
与 primitive 生成多项式
\[
P_n(q):=\sum_{\substack{\gamma\ \mathrm{primitive}\\|\gamma|=n}}q^{\chi(\gamma)}.
\]
则有加权 Witt/Möbius 反演：
\[
c_n(q)=\sum_{d\mid n}d\,P_d\!\left(q^{n/d}\right),
\qquad
P_n(q)=\frac{1}{n}\sum_{d\mid n}\mu(d)\,c_{n/d}\!\left(q^{d}\right).
\]
对模 $m$ 的分层计数
\(
N_{n,r}^{(m)}:=\sum_{k\equiv r\ (\mathrm{mod}\ m)}[q^k]P_n(q)
\)
可由离散傅里叶反演直接写成
\[
\boxed{\;
N_{n,r}^{(m)}=\frac{1}{m}\sum_{j=0}^{m-1}\zeta_m^{-jr}P_n(\zeta_m^j),
\qquad \zeta_m=e^{2\pi i/m}.\;}
\]
例如模 $2$ 时
\(
N_{n,0}^{(2)}=(P_n(1)+P_n(-1))/2,\,
N_{n,1}^{(2)}=(P_n(1)-P_n(-1))/2
\)，并且
\[
P_{10}(q)=54q^5+334q^4+634q^3+410q^2+79q+2,
\qquad
(N_{10,0}^{(2)},N_{10,1}^{(2)})=(746,767).
\]

\paragraph{时间复杂度轴：负携带势的三次压力方程与大偏差端点}
把“串行冲突/携带活跃度”作为可观测势引入后，压力与大偏差仍保持可审计的有限维闭式。
例如负携带势取（附录 \ref{app:vector-potential}）
\[
Q_-:=\{0\overline{1}2,\,1\overline{1}2,\,01\overline{1},\,11\overline{1}\},
\qquad
\phi(s\to t):=\mathbf{1}_{\{t\in Q_-\}},
\]
令 $u=e^\theta$，则主特征值 $\lambda(u)$ 满足三次方程
\[
\boxed{\;
\lambda^3-(u+2)\lambda^2+(u-2)\lambda+3u=0,\qquad u=e^\theta,\;}
\]
从而压力 $P(\theta)=\log\lambda(e^\theta)$ 被该三次方程的最大实根完全锁定，并且
\[
P'(0)=\frac18,\qquad P''(0)=\frac{25}{128},
\qquad
I(0)=\log\frac{3}{1+\sqrt3},\quad I(1)=\log 3.
\]

\paragraph{多维势：张量化观测仍保持“有限矩阵—行列式—压力/大偏差”闭环}
对一般向量势
\(
\boldsymbol{\varphi}(e)=(\varphi_1(e),\dots,\varphi_d(e))
\)
定义加权矩阵族
\[
B(\boldsymbol{\theta})_{ij}=\sum_{e:i\to j}\exp\!\bigl(\langle\boldsymbol{\theta},\boldsymbol{\varphi}(e)\rangle\bigr),
\qquad
P(\boldsymbol{\theta})=\log\rho(B(\boldsymbol{\theta})),
\]
则
\(
\zeta(z;\boldsymbol{\theta})=\det(I-zB(\boldsymbol{\theta}))^{-1}
\)
给出与标量势同型的可审计接口，且大偏差率函数由 Legendre 变换
\(
I(\alpha)=\sup_{\boldsymbol{\theta}}(\langle\boldsymbol{\theta},\alpha\rangle-(P(\boldsymbol{\theta})-P(0)))
\)
读出（见附录 \ref{app:vector-potential} 的二维/三维闭式示例）。

\subsection{新结论簇 I：熵饱和与最大熵提升}\label{subsec:pom-entropy-maxent}
\begin{definition}[窗口值与外表值]\label{def:pom-window-value}
对 $\omega=\omega_1\cdots\omega_m\in\Omega_m$，定义窗口值
\[
N(\omega):=\sum_{k=1}^m \omega_k F_{k+1}.
\]
对 $x=x_1\cdots x_m\in X_m$，定义外表值
\[
V_m(x):=\sum_{k=1}^m x_k F_{k+1}.
\]
\end{definition}

\begin{lemma}[单隐藏位分解]\label{lem:pom-one-bit}
对任意 $\omega\in\Omega_m$，存在唯一 $b(\omega)\in\{0,1\}$ 使
\[
N(\omega)=V_m(\Fold_m(\omega)) + b(\omega)\,F_{m+2}.
\]
\end{lemma}
\begin{proof}
由 $\sum_{k=1}^m F_{k+1}=F_{m+3}-2$ 得 $0\le N(\omega)<F_{m+3}$。因此 $N(\omega)$ 的 Zeckendorf 表示中不可能出现权重 $F_{m+3}$，其非零位仅可能到 $F_{m+2}$。由 $\Fold_m$ 的定义，$\Fold_m(\omega)$ 给出前 $m$ 位，余下唯一可能的高位即 $F_{m+2}$，于是存在唯一 $b(\omega)\in\{0,1\}$ 使等式成立。证毕。
\end{proof}

\begin{proposition}[纤维自由度的二层分解]\label{prop:pom-fiber-decompose}
对任意 $x\in X_m$，记 $I(x)$ 为 $x$ 中可执行逆代换 $100\mapsto 011$ 的局部位点集合（以长度 $3$ 窗口计），并要求所选位点两两不相邻以避免重叠。则任意 $\omega\in\Fold_m^{-1}(x)$ 可视为"一个余量位 $b\in\{0,1\}$"与"一组不相邻逆代换位点"的组合；因此
\[
d_m(x)\le 2\cdot \#\{\text{路径图 }P_{|I(x)|}\text{ 的独立集}\}
\le 2F_{\lfloor m/2\rfloor+2}.
\]
\end{proposition}
\begin{proof}
由引理 \ref{lem:pom-one-bit}，给定 $\omega$ 的超出前 $m$ 位的信息至多是一位 $b$。其余自由度来自逆代换 $100\mapsto 011$ 的选择：当两次逆代换发生重叠时会产生非二元符号，因此合法选择只能来自一条线性链上的不相邻位点集合。路径图独立集计数即为 Fibonacci 数，从而得到上述上界。证毕。
\end{proof}

\begin{theorem}[$B_m$ 的闭式计数]\label{thm:pom-hidden-bit-count}
对任意 $m\ge 1$，令
\[
B_m:=\{\omega\in\Omega_m: b(\omega)=1\}.
\]
则
$$
\boxed{\ |B_m|=\left\lfloor \frac{2^m}{3}\right\rfloor\ }
$$
等价地
$$
|B_m|=
\begin{cases}
\dfrac{2^m-1}{3}, & m\ \text{为偶数},\\[6pt]
\dfrac{2^m-2}{3}, & m\ \text{为奇数}.
\end{cases}
$$
\end{theorem}
\begin{proof}
由引理 \ref{lem:zeckendorf-range-bijection} 知 $V_m(X_m)=\{0,1,\dots,F_{m+2}-1\}$。故由引理 \ref{lem:pom-one-bit} 的分解
$$
N(\omega)=V_m(\Fold_m(\omega))+b(\omega)F_{m+2}
$$
可得
$$
b(\omega)=1\quad\Longleftrightarrow\quad N(\omega)\ge F_{m+2}.
$$
下面对最高位 $\omega_m$ 分类计数。

\paragraph{(1) 若 $\omega_m=0$}
则
$$
N(\omega)\le \sum_{k=1}^{m-1}F_{k+1}=F_{m+2}-1,
$$
故不可能满足 $N(\omega)\ge F_{m+2}$，从而该分支对 $B_m$ 无贡献。

\paragraph{(2) 若 $\omega_m=1$}
写
$$
N(\omega)=F_{m+1}+\omega_{m-1}F_m+\sum_{k=1}^{m-2}\omega_kF_{k+1}.
$$
阈值为 $F_{m+2}=F_{m+1}+F_m$，故 $N(\omega)\ge F_{m+2}$ 等价于
$$
\omega_{m-1}F_m+\sum_{k=1}^{m-2}\omega_kF_{k+1}\ \ge\ F_m.
$$
再对 $\omega_{m-1}$ 分类：
\begin{itemize}
  \item 若 $\omega_{m-1}=1$，条件自动成立；其余 $m-2$ 位任意，贡献 $2^{m-2}$ 个。
  \item 若 $\omega_{m-1}=0$，条件化为
  $$
  \sum_{k=1}^{m-2}\omega_kF_{k+1}\ge F_m.
  $$
  这与“长度 $m-2$ 的同型阈值问题”一致：右端正是 $F_{(m-2)+2}=F_m$，因此该分支贡献 $|B_{m-2}|$ 个。
\end{itemize}
综上得到递推
$$
|B_m|=2^{m-2}+|B_{m-2}|\qquad(m\ge 3),
$$
并由直接枚举得初值 $|B_1|=0$、$|B_2|=1$。

解递推：若 $m=2k$，则
$$
|B_{2k}|=1+\sum_{j=1}^{k-1}2^{2j}=\frac{2^{2k}-1}{3}.
$$
若 $m=2k+1$，则
$$
|B_{2k+1}|=\sum_{j=0}^{k-1}2^{2j+1}=\frac{2^{2k+1}-2}{3}.
$$
两式统一为 $|B_m|=\lfloor 2^m/3\rfloor$。证毕。
\end{proof}

\begin{corollary}[极限密度与“非均匀的一比特”]\label{cor:pom-hidden-bit-entropy}
在 $\Omega_m$ 上取均匀分布，则
$$
\mathbb{P}[b(\omega)=1]=\frac{|B_m|}{2^m}
=\frac{1}{3}-\frac{\varepsilon_m}{3\cdot 2^m},
$$
其中 $\varepsilon_m=1$ 当 $m$ 为偶数、$\varepsilon_m=2$ 当 $m$ 为奇数。因而
$$
\lim_{m\to\infty}\mathbb{P}[b(\omega)=1]=\frac{1}{3},\qquad
\lim_{m\to\infty}\mathbb{P}[b(\omega)=0]=\frac{2}{3}.
$$
\end{corollary}

\begin{corollary}[全局分支质量守恒与子集偏置约束]\label{cor:pom-branch-mass-law}
对每个 \(x\in X_m\) 与 \(b\in\{0,1\}\)，定义纤维内的隐藏位分裂计数
\[
d_{m,b}(x):=\#\{\omega\in\Omega_m:\ \Fold_m(\omega)=x,\ b(\omega)=b\},
\]
并记纤维总多重度 \(d_m(x)=d_{m,0}(x)+d_{m,1}(x)\) 以及纤维内的分支-\(1\) 质量比例
\[
p_1(x):=\frac{d_{m,1}(x)}{d_m(x)}\in[0,1].
\]
令 \(\omega\sim\mathrm{Unif}(\Omega_m)\)、\(X:=\Fold_m(\omega)\)，并写输出分布为 \(\pi_m(x):=\mathbb{P}(X=x)=d_m(x)/2^m\)。
则有精确平均恒等式
\[
\boxed{\;
\mathbb{E}_{X\sim\pi_m}\bigl[p_1(X)\bigr]
=\mathbb{P}[b(\omega)=1]
=\frac{|B_m|}{2^m}
=\frac{\lfloor 2^m/3\rfloor}{2^m}. \;}
\]
因此
\[
\left|\mathbb{E}_{\pi_m}[p_1]-\frac13\right|\le 2^{-m}.
\]
并且对任意子集 \(S\subseteq X_m\) 满足 \(\pi_m(S)>0\)，有\emph{守恒型}约束（严格恒等式）
\[
\boxed{\;
\mathbb{E}_{\pi_m}[p_1\mid X\in S]-\mathbb{P}[b=1]
=-\frac{\pi_m(S^c)}{\pi_m(S)}
\left(\mathbb{E}_{\pi_m}[p_1\mid X\in S^c]-\mathbb{P}[b=1]\right). \;}
\]
即任何“特殊集合”内部的系统性偏置只能以跨集合的重分配形式出现，不能在全局上净产生/净消去隐藏位质量。
\end{corollary}
\begin{proof}
对任意 \(x\in X_m\)，在事件 \(\{X=x\}\) 上 \(\omega\) 在纤维 \(\Fold_m^{-1}(x)\) 内条件均匀，故
\(
\mathbb{P}[b=1\mid X=x]=d_{m,1}(x)/d_m(x)=p_1(x)
\)。
由全期望公式得
\[
\mathbb{E}_{X\sim\pi_m}[p_1(X)]=\mathbb{P}[b=1]=|B_m|/2^m,
\]
再用定理 \ref{thm:pom-hidden-bit-count}（以及 \(|\lfloor 2^m/3\rfloor-2^m/3|\le 1\)）得到误差界。
\par
对任意 \(S\subseteq X_m\)，再用全期望公式
\(
\mathbb{P}[b=1]=\pi_m(S)\mathbb{E}[p_1\mid X\in S]+\pi_m(S^c)\mathbb{E}[p_1\mid X\in S^c]
\)，
移项并除以 \(\pi_m(S)\) 即得守恒恒等式。证毕。
\end{proof}

\begin{theorem}[一步可判别与右解析性]\label{thm:pom-right-resolving}
对任意 $m\ge 2$ 与任意 $v\in V_m=\{0,1\}^{m-1}$，有
\[
\Fold_m(v0)\neq \Fold_m(v1).
\]
因此标记图 $G_m$（定义 \ref{def:G_m}）是右解析（right-resolving）的：同一顶点出发的两条出边具有不同标记。
\end{theorem}
\begin{proof}
记 $\omega_0=v0$、$\omega_1=v1$。则 $N(\omega_1)-N(\omega_0)=F_{m+1}$. 若 $\Fold_m(\omega_0)=\Fold_m(\omega_1)$，由引理 \ref{lem:pom-one-bit} 得
\[
N(\omega_1)-N(\omega_0)\in\{0,F_{m+2}\},
\]
与 $F_{m+1}\neq F_{m+2}$ 矛盾。故两者标记不同，右解析性随之成立。证毕。
\end{proof}

\begin{theorem}[一步可判别与左解析性]\label{thm:pom-left-resolving}
对任意 $m\ge 2$ 与任意 $v\in V_m=\{0,1\}^{m-1}$，有
\[
\Fold_m(0v)\neq \Fold_m(1v).
\]
因此对任意固定的长度 $m-1$ \emph{后缀}，在其左侧补 $0/1$ 的读出结果可一步判别；换言之，$\Fold_m$ 对“首位未知”的二选一也是严格可判别的。
\end{theorem}
\begin{proof}
记 $\omega_0=0v$、$\omega_1=1v$。则 $N(\omega_1)-N(\omega_0)=F_2=1$。
若 $\Fold_m(\omega_0)=\Fold_m(\omega_1)$，由引理 \ref{lem:pom-one-bit} 得
\[
N(\omega_1)-N(\omega_0)\in\{0,F_{m+2}\},
\]
而当 $m\ge 2$ 时 $F_{m+2}\ge 3$，与 $N(\omega_1)-N(\omega_0)=1$ 矛盾。故两者读出不同。证毕。
\end{proof}

\begin{theorem}[熵饱和：折叠不改变信息率]\label{thm:pom-entropy-saturation}
对任意 $m\ge 2$，像空间 $Y_m=\Phi_m(\{0,1\}^{\ZZ})$ 满足
\[
h_{\mathrm{top}}(Y_m)=\log 2.
\]
\end{theorem}
\begin{proof}
由于 $\Phi_m$ 是滑动块因子，必有 $h_{\mathrm{top}}(Y_m)\le \log 2$。另一方面，$G_m$ 每个顶点有两条出边，总长度为 $n$ 的路径数为 $2^{m-1}\cdot 2^{n}$。由定理 \ref{thm:pom-right-resolving}，在固定起点下标记序列决定唯一路径，因此长度 $n$ 的标记词数至少为 $2^{n}$。于是
\[
\log 2\le h_{\mathrm{top}}(Y_m)\le \log 2,
\]
结论成立。证毕。
\end{proof}

\begin{corollary}[二轴复杂度分离律：熵率不降，但降分辨率的最小重写支持为线性尺度]\label{cor:pom-two-axis-separation}
在序列级，折叠因子像 \(Y_m\) 的信息率不损失：对任意 \(m\ge 2\)，\(h_{\mathrm{top}}(Y_m)=\log 2\)（定理 \ref{thm:pom-entropy-saturation}），并且对任意输出轨道 \(y\in Y_m\) 有有限对一缺陷上界 \( |\Phi_m^{-1}(y)|\le 2^{m-1}\)（注 \ref{rem:pom-finite-defect}）。
\par
但在跨分辨率降阶时，把朴素微态截断 \(r_{m+1\to m}\) 修正为折叠一致的 coarse-graining \(\widetilde r_{m+1\to m}\)（小节 \ref{subsec:fold-gauge-anomaly}）所需改动支持规模由规范差 \(G_m(\omega)\) 给出（定义 \ref{def:fold-gauge-anomaly}），并满足：
\begin{enumerate}
  \item \textbf{最坏情形：}对任意 \(m\ge 1\)，存在输入 \(\omega^\star(m)\in\Omega_{m+1}\) 使 \(G_m(\omega^\star)=m\)（定理 \ref{thm:fold-gauge-anomaly-max}），即纠偏可触及 \(\Theta(m)\) 位；
  \item \textbf{均匀基线：}在审计窗口 \(m\le 20\) 的精确枚举中（表 \ref{tab:fold_gauge_anomaly_stats}），\(\bar G_m/m\) 已上升到约 \(0.4176\)；并且该线性尺度可被封口为闭式常数密度定律：
  \[
  \lim_{m\to\infty}\frac{\bar G_m}{m}=\frac{4}{9}
  \]
  （定理 \ref{thm:fold-gauge-anomaly-density}）。因此在均匀微态基线下，降一阶分辨率的“典型重写支持规模”满足 \(\bar G_m\sim (4/9)m\)。
\end{enumerate}
因此在本文体系内，“每步信息率（熵率）”与“降一阶分辨率所需的最小重写支持规模”构成两条正交轴：折叠的多对一并不意味着熵率压缩；它把代价迁移为跨尺度一致性所需的全局支持重写量。
\end{corollary}

\begin{remark}[有限缺陷：从纤维多重度到有限部常数]\label{rem:pom-finite-defect}
熵饱和表明折叠在信息率意义下不压缩；其“丢失”体现为\textbf{有限缺陷}而非指数尺度。具体而言，给定标记序列 $y\in Y_m$，其前像路径数由起始顶点选择给出，故
\[
|\Phi_m^{-1}(y)|\le |V_m|=2^{m-1}.
\]
更强地，该“有限缺陷”可被提升为一个显式的缺陷寄存器：令
\[
\mathcal{D}_m:=V_m=\{0,1\}^{m-1}.
\]
定义映射
\[
\Psi_m:\{0,1\}^{\ZZ}\to Y_m\times \mathcal{D}_m,\qquad
\Psi_m(s):=\bigl(\Phi_m(s),\,s_0s_1\cdots s_{m-2}\bigr).
\]
则 $\Psi_m$ 为单射：给定 $y=\Phi_m(s)$ 与缺陷寄存器 $\delta:=s_0\cdots s_{m-2}$，可用定理 \ref{thm:pom-right-resolving} 逐步恢复所有 $t\ge 0$ 的后继比特（由窗口末位的二选一可判别），并用定理 \ref{thm:pom-left-resolving} 逐步恢复所有 $t<0$ 的前驱比特（由窗口首位的二选一可判别），从而恢复整条输入序列 $s$。
因此每条 $y\in Y_m$ 的前像数至多为 $|\mathcal{D}_m|=2^{m-1}$，并且“折叠丢失”可被理解为一个有限的初态/横截面寄存器，而非熵率损失。

这种有限缺陷可由纤维多重度 $d_m(x)$ 与条件期望 $E_m$ 的块平均结构刻画（命题 \ref{prop:fold-conditional-expectation}），并与 Abel/Hadamard 有限部常数形成互补的“常数级指纹”（定义 \ref{def:abel-finite-part} 及相关附录接口）。因此折叠的“丢失”属于有限分辨率残差，而非熵率损失。
\end{remark}

\begin{remark}[纤维平均与有限部常数的同一接口]\label{rem:pom-em-logm}
窗口尺度上，$E_m$ 通过 $d_m$ 对纤维做平均，压缩的是一次投影的不可观测自由度；轨道尺度上，Abel/Hadamard 有限部常数
\[
\log\mathfrak{M}(\theta)
=
\log C(\theta)
\;+\;
\sum_{k\ge 2}\frac{\mu(k)}{k}\log\zeta_\theta\!\bigl(z_\star(\theta)^k\bigr)
\]
（定义 \ref{def:abel-finite-part} 与命题 \ref{prop:abel-finite-part-mobius-zeta}）压缩的是 primitive 轨道乘积在无限尺度下的有限部漂移。因而 $E_m$ 与 $\log\mathfrak{M}$ 是同一“信息丢失接口”的微观/宏观两种尺度：前者是窗口纤维平均，后者是沿循环重复观测后的重整化常数；其导出的 $\mathsf{M}(\theta)=\gamma+\log\mathfrak{M}(\theta)$ 与 $P(\theta)$ 共同形成主尺度与有限部的双指纹（见 \ref{subsec:chi-ldp}）。
\end{remark}

\begin{definition}[推前与纤维均匀提升]\label{def:pom-maxent-lift}
把 $\mathcal{A}_m\simeq\mathbb{C}^{\Omega_m}$、$\mathcal{B}_m\simeq\mathbb{C}^{X_m}$ 识别为有限集上的函数代数（见命题 \ref{prop:fold-pushforward-lift}）。对任意分布 $\pi\in\Delta(X_m)$，定义纤维均匀提升
\[
\widetilde\pi(a):=\frac{\pi(\Fold_m(a))}{d_m(\Fold_m(a))},\qquad a\in\Omega_m.
\]
则 $(\Fold_m)_*\widetilde\pi=\pi$。
\end{definition}

\begin{theorem}[最大熵提升的唯一性]\label{thm:pom-maxent-lift}
固定任意 $\pi\in\Delta(X_m)$，在所有满足 $(\Fold_m)_*\mu=\pi$ 的分布 $\mu\in\Delta(\Omega_m)$ 中，Shannon 熵
\[
H(\mu):=-\sum_{a\in\Omega_m}\mu(a)\log\mu(a)
\]
的最大值唯一且由 $\widetilde\pi$ 取得。并且
\[
H(\widetilde\pi)=H(\pi)+\mathbb{E}_{\pi}[\log d_m(X)].
\]
\end{theorem}
\begin{proof}
固定 $x\in X_m$ 时，纤维 $\Fold_m^{-1}(x)$ 上的总质量为 $\pi(x)$。在该纤维内，熵在“均匀分配”处唯一取最大值（严格凹性）。对各纤维求和得到全局最大值与唯一性。直接代入 $\widetilde\pi$ 得
\[
H(\widetilde\pi)
=-\sum_{x\in X_m}d_m(x)\frac{\pi(x)}{d_m(x)}\log\!\left(\frac{\pi(x)}{d_m(x)}\right)
=H(\pi)+\sum_{x\in X_m}\pi(x)\log d_m(x).
\]
证毕。
\end{proof}

\begin{definition}[热力学缺陷与空间投影标量]\label{def:pom-kappa}
对任意分布 $\pi\in\Delta(X_m)$，定义
\[
\kappa_m(\pi):=\mathbb{E}_{\pi}[\log d_m(X)].
\]
由定理 \ref{thm:pom-maxent-lift}，$\kappa_m(\pi)=H(\widetilde\pi)-H(\pi)$，因此 $\kappa_m$ 给出“折叠纤维被压掉的自由度”的可观测标量刻画。
\end{definition}

\begin{definition}[自然基准分布：均匀微态的折叠推前]\label{def:pom-wm-baseline}
令 $u_{\Omega_m}$ 表示 $\Omega_m=\{0,1\}^m$ 上的均匀分布（$u_{\Omega_m}(a)\equiv 2^{-m}$）。
定义 $X_m$ 上的自然基准分布
\[
w_m:=(\Fold_m)_*u_{\Omega_m},\qquad
w_m(x)=\frac{d_m(x)}{2^m}\quad(x\in X_m).
\]
它刻画了“均匀微态”在折叠后的输出分布（亦即把纤维多重度归一化为概率权重）。\footnote{为避免与第 \ref{sec:experiments} 节 Parry(PF) 基线的 $\pi_m$ 记号混淆，这里用 $w_m$ 表示该分布；在碰撞矩/R\'enyi 指纹小节中它也常被记为 $\pi_m(x)=d_m(x)/2^m$。}
\end{definition}

\begin{proposition}[\texorpdfstring{$\kappa_m(\pi)$}{kappa} 的 KL 精确分解]\label{prop:pom-kappa-kl-decomposition}
对任意 $\pi\in\Delta(X_m)$，令 $w_m$ 为定义 \ref{def:pom-wm-baseline} 的自然基准分布。则有严格恒等式
\[
\boxed{\;
\kappa_m(\pi)=m\log 2\;-\;H(\pi)\;-\;D_{\mathrm{KL}}(\pi\|w_m)\; }.
\]
因此 $\kappa_m(\pi)\le m\log 2-H(\pi)$，且当且仅当 $\pi=w_m$ 时取等号。
\end{proposition}
\begin{proof}
由 KL 定义与 $w_m(x)=d_m(x)/2^m$，
\[
D_{\mathrm{KL}}(\pi\|w_m)
=\sum_{x\in X_m}\pi(x)\log\frac{\pi(x)}{d_m(x)/2^m}
=\sum_x\pi(x)\log\pi(x)-\sum_x\pi(x)\log d_m(x)+m\log 2
=-H(\pi)-\kappa_m(\pi)+m\log 2.
\]
移项即得恒等式；不等式与取等号条件由 $D_{\mathrm{KL}}\ge 0$ 与 $D_{\mathrm{KL}}(\pi\|w_m)=0\Leftrightarrow \pi=w_m$ 得到。证毕。
\end{proof}

\begin{proposition}[折叠的“近正则性”：\texorpdfstring{$D_{\mathrm{KL}}(w_m\|U_m)$}{KL(w||U)} 的温和线性上界]\label{prop:pom-wm-kl-to-uniform}
令 $U_m$ 为 $X_m$ 上的均匀分布（$U_m(x)\equiv 1/|X_m|=1/F_{m+2}$），并令平均纤维多重度为
\(
\overline d_m:=2^m/|X_m|=2^m/F_{m+2}
\)。
则
\[
\boxed{\;
D_{\mathrm{KL}}(w_m\|U_m)\ \le\ \log\frac{D_m}{\overline d_m}
\;=\;\log\!\left(\frac{D_m\,F_{m+2}}{2^m}\right)\; }.
\]
并且由注 \ref{rem:pom-defect-gap}，
\[
D_{\mathrm{KL}}(w_m\|U_m)\ \le\ m\log\!\left(\frac{\varphi^{3/2}}{2}\right)+O(1),
\qquad \log(\varphi^{3/2}/2)\approx 0.0286.
\]
\end{proposition}
\begin{proof}
注意
\(
\frac{w_m(x)}{U_m(x)}=\frac{d_m(x)/2^m}{1/|X_m|}=\frac{d_m(x)}{\overline d_m}
\)，
故 $\sup_x \frac{w_m(x)}{U_m(x)}=D_m/\overline d_m$。由一般不等式
\(
D_{\mathrm{KL}}(P\|Q)\le \log \sup_x \frac{P(x)}{Q(x)}
\)
立得第一式；第二式由注 \ref{rem:pom-defect-gap} 的 $D_m/\overline d_m=\Theta((\varphi^{3/2}/2)^m)$ 取对数得到。证毕。
\end{proof}

\begin{theorem}[谱--信息夹逼：熵缺口由 \texorpdfstring{$(D_m,\mathsf{Col}_m,\mathcal Z_m)$}{(D,Col,Z)} 控制]\label{thm:pom-spectral-information-sandwich}
令 \(w_m=(\Fold_m)_*u_{\Omega_m}\) 为均匀微态的折叠推前（定义 \ref{def:pom-wm-baseline}），令 \(U_m\) 为 \(X_m\) 上的均匀分布（命题 \ref{prop:pom-wm-kl-to-uniform}）。
定义到均匀的熵缺口（自然对数，单位 nats）
\[
\mathrm{gap}_m^{(\mathrm{nat})}
:=\log|X_m|-H(w_m)
=D_{\mathrm{KL}}(w_m\|U_m),
\qquad |X_m|=F_{m+2}.
\]
再定义折叠碰撞概率
\[
\mathsf{Col}_m:=\sum_{x\in X_m}w_m(x)^2
=\mathbb{P}\bigl(\Fold_m(\omega)=\Fold_m(\eta)\bigr),
\qquad \omega,\eta\stackrel{\mathrm{iid}}{\sim}\mathrm{Unif}(\Omega_m).
\]
则有如下\emph{可审计}的双向夹逼：
\[
\boxed{\;
2\left(\frac{D_m}{2^m}-\frac{1}{F_{m+2}}\right)^2
\ \le\
\mathrm{gap}_m^{(\mathrm{nat})}
\ \le\
\log\!\bigl(F_{m+2}\,\mathsf{Col}_m\bigr)
\ \le\
\log\!\bigl(F_{m+2}-|\mathcal Z_m|\bigr).\;}
\]
其中 \(D_m=\max_{x\in X_m}d_m(x)\) 为最大纤维多重度（见小节 \ref{subsec:pom-max-fiber}），\(\mathcal Z_m\) 为附录 \ref{app:fold-multiplicity} 中定义 \ref{def:fold-spectrum-zero-set} 的谱零点集合。
把上式除以 \(\log 2\) 即得到比特单位的缺口 \(\mathrm{gap}_m^{(2)}=\mathrm{gap}_m^{(\mathrm{nat})}/\log 2\)。
\end{theorem}
\begin{proof}
\textbf{(1) 下界（Pinsker + 最大点差）.}
令 \(F:=F_{m+2}\)。
由 Pinsker 不等式（自然对数口径）
\(
D_{\mathrm{KL}}(w_m\|U_m)\ge 2\,D_{\mathrm{TV}}(w_m,U_m)^2
\)，
其中
\(
D_{\mathrm{TV}}(P,Q):=\frac12\sum_x|P(x)-Q(x)|
\)。
又因为
\[
D_{\mathrm{TV}}(w_m,U_m)
=\sum_{x:\,w_m(x)>1/F}\bigl(w_m(x)-1/F\bigr)
\ge \max_x\bigl(w_m(x)-1/F\bigr),
\]
而 \(\max_x w_m(x)=D_m/2^m\)，故得到
\(
D_{\mathrm{KL}}(w_m\|U_m)\ge 2(D_m/2^m-1/F)^2
\)。
\par
\textbf{(2) 上界（Rényi-2/碰撞上界）.}
二阶 Rényi 熵（collision entropy）满足
\(
H(w_m)\ge H_2(w_m):=-\log\sum_x w_m(x)^2=-\log \mathsf{Col}_m
\)，
因此
\[
\mathrm{gap}_m^{(\mathrm{nat})}
=\log F-H(w_m)
\le \log F-H_2(w_m)
=\log\bigl(F\,\mathsf{Col}_m\bigr).
\]
最后由附录 \ref{app:fold-multiplicity} 的定理 \ref{thm:fold-collision-zero-reduction}，
\(
\mathsf{Col}_m\le 1-|\mathcal Z_m|/F
\)，
从而 \(F\mathsf{Col}_m\le F-|\mathcal Z_m|\)，得到最右端上界。证毕。
\end{proof}

\begin{corollary}[最大纤维达到者的指数稀有性（均匀基线）]\label{cor:pom-maxfiber-rare-uniform}
在 $U_m$（$X_m$ 上均匀分布）下，有
\[
\mathbb{P}_{U_m}\!\bigl(d_m(X)=D_m\bigr)
\le \mathbb{P}_{U_m}\!\bigl(d_m(X)\ge D_m\bigr)
\le \frac{\overline d_m}{D_m}
=\Theta\!\left(\left(\frac{2}{\varphi^{3/2}}\right)^m\right).
\]
换言之，尽管 $D_m$ 随 $m$ 增长，达到最大纤维的稳定类型在 $X_m$ 中所占比例指数趋零。
\end{corollary}
\begin{proof}
在 $U_m$ 下，
\(
\mathbb{E}_{U_m}[d_m(X)]=\frac{1}{|X_m|}\sum_{x\in X_m}d_m(x)=\frac{2^m}{|X_m|}=\overline d_m
\)。
对非负随机变量 $d_m(X)$ 用 Markov 不等式得
\(
\mathbb{P}_{U_m}(d_m(X)\ge D_m)\le \overline d_m/D_m
\)；
最后一条 $\Theta$ 渐近由注 \ref{rem:pom-defect-gap} 给出。证毕。
\end{proof}

\begin{remark}[窗口缺陷与有限部常数的同型]\label{rem:pom-kappa-logm}
窗口尺度上，$\kappa_m(\pi)$ 是一次折叠投影的对数纤维体积缺陷；轨道尺度上，$\log\mathfrak{M}(\theta)$ 是 primitive 轨道乘积在无限尺度下的 Möbius--Abel 正则化有限部（定义 \ref{def:abel-finite-part}）。二者分别给出微观/宏观的“纤维缺陷”接口，为跨尺度缺陷泛函的对齐提供了统一口径。
\end{remark}

\begin{remark}[与条件期望的对偶性]
纤维均匀提升 $\widetilde\pi$ 与条件期望 $E_m$ 构成对偶接口：$E_m$ 在观测量层面是“纤维平均”，而 $\widetilde\pi$ 在概率层面是“纤维均匀化”。二者分别给出折叠的算子化与信息论刻画。
\end{remark}

\begin{proposition}[全局 KL 缺陷恒等式：偏离最大熵提升的精确距离]\label{prop:pom-kl-defect-identity}
固定任意 \(\pi\in\Delta(X_m)\)，并令 \(\widetilde\pi\in\Delta(\Omega_m)\) 为其纤维均匀提升（定义 \ref{def:pom-maxent-lift}）。
则对任意满足 \((\Fold_m)_*\mu=\pi\) 的 \(\mu\in\Delta(\Omega_m)\)，有严格恒等式
\[
\boxed{\;
D_{\mathrm{KL}}(\mu\|\widetilde\pi)=H(\widetilde\pi)-H(\mu)\;}
\]
特别地，由定义 \ref{def:pom-kappa} 与定理 \ref{thm:pom-maxent-lift}，恒有
\[
H(\widetilde\pi)=H(\pi)+\kappa_m(\pi),
\qquad
D_{\mathrm{KL}}(\mu\|\widetilde\pi)=H(\pi)+\kappa_m(\pi)-H(\mu),
\]
从而 \(\kappa_m(\pi)\) 可被视为“在宏观分布 \(\pi\) 固定时，微观层面可被压掉的最大熵缺陷”。
\end{proposition}

\begin{proof}
由 KL 定义
\[
D_{\mathrm{KL}}(\mu\|\widetilde\pi):=\sum_{a\in\Omega_m}\mu(a)\log\frac{\mu(a)}{\widetilde\pi(a)}
=-H(\mu)-\sum_{a\in\Omega_m}\mu(a)\log\widetilde\pi(a).
\]
而由 \(\widetilde\pi(a)=\pi(x)/d_m(x)\)（其中 \(x=\Fold_m(a)\)），并用 \((\Fold_m)_*\mu=\pi\) 得
\[
\sum_{a\in\Omega_m}\mu(a)\log\widetilde\pi(a)
=\sum_{a\in\Omega_m}\mu(a)\bigl(\log\pi(\Fold_m(a))-\log d_m(\Fold_m(a))\bigr)
=\sum_{x\in X_m}\pi(x)\log\pi(x)-\sum_{x\in X_m}\pi(x)\log d_m(x).
\]
对 \(\widetilde\pi\) 直接展开熵可得
\[
H(\widetilde\pi)
=-\sum_{a\in\Omega_m}\widetilde\pi(a)\log\widetilde\pi(a)
=-\sum_{x\in X_m}\pi(x)\log\pi(x)+\sum_{x\in X_m}\pi(x)\log d_m(x).
\]
故 \(-\sum_a\mu(a)\log\widetilde\pi(a)=H(\widetilde\pi)\)，代回即得
\(
D_{\mathrm{KL}}(\mu\|\widetilde\pi)=H(\widetilde\pi)-H(\mu)
\)。
证毕。
\end{proof}

\begin{remark}[延迟归一化推迟律]\label{rem:pom-lazy-fold}
若中间演化 $U_t$ 在观测层不区分折叠纤维，即对任意 $g\in\mathcal{A}_m$ 有 $g\circ U_t\in\mathcal{B}_m$ 等价于 $g=\bar g\circ\Fold_m$（命题 \ref{prop:pom-fiber-invariance}），则
\[
E_m\circ U_t\circ E_m=E_m\circ U_t.
\]
因此在此类“纤维不区分”的演化段内，多次 $P_Z$ 可合并为末端一次，实现规范投影的推迟执行。
\end{remark}

\subsection{新结论簇 II：商余作为平移群胚横截面}\label{subsec:pom-division-groupoid}
\begin{definition}[平移群胚与基本域]\label{def:pom-translation-groupoid}
固定整数 $b>0$，令 $\mathbb{Z}$ 作用于 $\mathbb{Z}$：
\[
k\cdot a:=a-kb.
\]
其轨道即同余类 $a\bmod b$。取基本域
\[
\mathcal F_b:=\{0,1,\dots,b-1\},
\]
并定义投影
\[
\Pi_b(a):=\text{唯一满足 }a\equiv r\pmod b\text{ 且 }r\in\mathcal F_b\text{ 的 }r。
\]
\end{definition}

\begin{theorem}[商余作为横截面投影]\label{thm:pom-division-section}
对任意 $a\in\mathbb{Z}$，存在唯一 $(q,r)\in\mathbb{Z}\times\mathcal F_b$ 使
\[
a=qb+r,\qquad r=\Pi_b(a).
\]
其中 $q$ 是轨道坐标，$r$ 是沿平移轨道投影到横截面的像。
\end{theorem}
\begin{proof}
