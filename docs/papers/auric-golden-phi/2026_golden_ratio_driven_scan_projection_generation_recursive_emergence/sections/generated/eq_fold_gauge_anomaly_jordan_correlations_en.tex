% Auto-generated by scripts/exp_fold_gauge_anomaly_jordan_correlations.py \paragraph{规范差逐位过程全相关闭式and Jordan 指纹}\label{par:fold-gauge-anomaly-jordan} 命题 \ref{prop:fold-gauge-anomaly-pressure} 已将逐位失配指示 $g_t=\mathbf 1\{X_t\neq Y_t\}$ 压力写为$P_G(\theta)=\log\rho(A_\theta)$ 并给出显式 $4\times 4$ 矩阵 $A_\theta$.取 $\theta=0$ 得 $A_0$, 对 Perron 根 $\rho(A_0)=1$ 作 Parry 归一化, 可进一步we obtain该逐位过程谱指纹, 完整自协方差序列and有限长度方差完全闭式. \begin{lemma}[Parry 归一化核and失配边势]\label{lem:fold-gauge-anomaly-parry-kernel} 在状态次序 $(a,b,c,d)$, let $A_0:=A_\theta|_{\theta=0}$. 取 Perron 根 $\rho(A_0)=1$ 左右本征向量 \[ r=(2,1,2,1)^\top,\qquad \ell=(2,2,1,1)^\top,\qquad \ell^\top r=9, \] 并Definition Parry 转移核and平稳分布为 \[ P_{ij}:=\frac{(A_0)_{ij}\,r_j}{r_i},\qquad \pi_i:=\frac{\ell_i r_i}{\ell^\top r}. \] then $P$ and $\pi$ 具有完全有理闭式:  \[ P=\begin{pmatrix}\frac12&\frac14&0&\frac14\\0&0&1&0\\\frac12&\frac12&0&0\\1&0&0&0\end{pmatrix},\qquad\pi=\Bigl(\frac49,\frac29,\frac29,\frac19\Bigr). \] 并且 $A_\theta$ 中携带 $e^\theta$ 边恰对应 $g_t=1$ 失配边势: 在 $P$ 上仅三条转移 $(a\!\to\! b),(b\!\to\! c),(c\!\to\! a)$ 取值 $1$, 其余转移取值 $0$. \end{lemma} \begin{theorem}[Jordan 指纹]\label{thm:fold-gauge-anomaly-jordan-fingerprint} 上述 $4$ 态核 $P$ 谱为 $\{1,\tfrac12,-\tfrac12\}$, 且特征值 $-\tfrac12$ 代数重数为 $2$, 几何重数为 $1$, 从而存在大小为 $2$ Jordan 块. 因此 $P^k$ 衰减项一般包含 $k(-\tfrac12)^k$ 广义特征贡献: 对任意中心化可加观测量, 其相关衰减在一般位置呈现 $k2^{-k}$ 级多项式修正(并伴随 $(-1)^k$ 振荡), 而非纯指数. \end{theorem} \begin{theorem}[自协方差核and生成函数完全闭式]\label{thm:fold-gauge-anomaly-autocov-closed} 在平稳分布 $\pi$, 逐位失配指示满足 \[ \mathbb E[g_t]=\frac49,\qquad \mathrm{Var}(g_t)=\frac{20}{81}. \] 并且对任意 $k\ge 1$, 自协方差具有闭式 \[ \mathrm{Cov}(g_t,g_{t+k})=2^{-k}\!\left(\frac18+(-1)^k\!\left(\frac{7}{648}+\frac{k}{108}\right)\right). \] 其协方差生成函数为显式有理函数:  \[ \sum_{k\ge 0}\mathrm{Cov}(g_t,g_{t+k})\,z^k=\frac{20}{81}+\frac{z}{16(1-z/2)}-\frac{7z}{1296(1+z/2)}-\frac{z}{216(1+z/2)^2}. \] \end{theorem} \begin{theorem}[有限长度方差完全闭式]\label{thm:fold-gauge-anomaly-finite-var-closed} let $G_m:=\sum_{t=1}^{m} g_t$.then在平稳初值有完全闭式 \[ \mathrm{Var}(G_m)=\frac{118}{243}m-\frac{40}{81}+\frac{243-(-1)^m(2m+3)}{486\cdot 2^m}. \] 因此 $\mathrm{Var}(G_m)-\frac{118}{243}m$ 偏差不仅为 $O(1)$, 而且其剩余项精确为 $O(m2^{-m})$(并带显式振荡项). \end{theorem} \begin{corollary}[Edgeworth 一阶项auditable封口]\label{cor:fold-gauge-anomaly-edgeworth1} let $Z_m:=\frac{G_m-\frac49 m}{\sqrt{\frac{118}{243}m}}$.在中心极限定理尺度上, 其一阶 Edgeworth 近似系数可写为完全闭式:  \[ \mathbb P(Z_m\le x)=\Phi(x)+\frac{1}{6\sqrt m}\,\gamma_1^{(G)}\,(1-x^2)\varphi(x)+O(m^{-1}),\qquad\gamma_1^{(G)}=\frac{P_G^{(3)}(0)}{(P_G''(0))^{3/2}}=\frac{-\frac{1174}{2187}}{\left(\frac{118}{243}\right)^{3/2}}. \] 其中 $\Phi,\varphi$ are the ceiling values of标准正态分布函数and密度, $P_G^{(3)}(0)$ and $P_G''(0)$ 由命题 \ref{prop:fold-gauge-anomaly-pressure} 给出. \end{corollary} \begin{proof}[Proof sketch(auditable)] 引理 \ref{lem:fold-gauge-anomaly-parry-kernel} 由 $A_0$ Perron 本征向量and Parry 归一化直接计算we obtain.定理 \ref{thm:fold-gauge-anomaly-jordan-fingerprint} 特征多项式分解and $-\tfrac12$ 处 Jordan 块由有限维有理线性代数给出.定理 \ref{thm:fold-gauge-anomaly-autocov-closed} 通过在平稳链上把 $g_t$ 视为边势并利We use $P$ 谱/Jordan 分解, we obtain自协方差生成函数有理表达并逐项抽取系数.定理 \ref{thm:fold-gauge-anomaly-finite-var-closed} 由$\mathrm{Var}(G_m)=m\,\mathrm{Var}(g_t)+2\sum_{k=1}^{m-1}(m-k)\mathrm{Cov}(g_t,g_{t+k})$and上一条协方差闭式求和we obtain.以上全部代数化简and一致性校验可由The script \texttt{scripts/exp\_fold\_gauge\_anomaly\_jordan\_correlations.py} 一键复算. \end{proof} 