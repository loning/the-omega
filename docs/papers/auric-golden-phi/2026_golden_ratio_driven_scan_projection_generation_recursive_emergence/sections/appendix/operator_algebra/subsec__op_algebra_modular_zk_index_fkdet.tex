\subsection{Zero-knowledge horizon in modular theory caliber: Takesaki criterion, finite exponential bounds and non-commutative determinants \texorpdfstring{XPH0X}{zeta}}\label{subsec:op-algebra-modular-zk-index-fkdet}This section defines the measure theory of the main text "Horizon Zero Knowledge (Relatively Independent Extension)" (definition \ref{def:xi-horizon-perfect-zero-knowledge} and theorem \ref{thm:xi-horizon-perfect-zk-equivalences})Upgrade to the von Neumann algebra and Tomita--Takesaki module theory framework, and point out:The zero knowledge of the horizon can be strictly equivalently characterized as the existence of anFurthermore, under the inclusion of finite exponential sub-factors, the Pimsner-Popa inequality accurately quantifies the "number of indistinguishable witnesses within the fiber" with the Jones index, thereby giving an irreducible lower bound for the soundness error under zero-knowledge conditions.Finally, the Fuglede--Kadison determinant is introduced as the non-commutative definition of the "horizon \(\zeta\) function", and its zero-point correspondence with the visible spectrum resonance is given.
\paragraph{Settings: Volume Algebra, Visible Sub-Algebra, and Transcription Channel}Let \(\mathcal{M}\) be a finite von Neumann algebra, and \(\varphi\) be its faithful normal state;Let \(\mathcal{N}\subset \mathcal{M}\) be the "horizon visible" subalgebra (visibility factor).Let\[
\Phi:\mathcal{M}\to\mathcal{T}.
\]
At this caliber, an emulator that "relies only on visible factors" would be a channel that only reads \(\mathcal{N}\).
\begin{definition}[Perfect zero-knowledge horizon of operator algebra]\label{def:op-algebra-horizon-perfect-zk}We call the transcription channel \(\Phi:\mathcal{M}\to\mathcal{T}\) relative to the number of visible subalgebras \(\mathcal{N}\) \textbf{perfect zero knowledge},If there exists an \(\varphi\)-preserving conditional expectation \(E:\mathcal{M}\to\mathcal{N}\) with a regular fully positive mapping \(S:\mathcal{N}\to\mathcal{T}\) such that\begin{equation}\label{eq:op-algebra-zk-factorization}
\boxed{\;\Phi=S\circ E.\;}
\end{equation}
At this time, \(E\) can be regarded as a "horizon coarse-graining", while \(S\) is a simulator that "only relies on visible factors".\end{definition}

\begin{remark}[Correspondence to the definition of measure theory in the text]\label{rem:op-algebra-zk-vs-measure}When \(\mathcal{M}=L^\infty(\Omega,\mu)\), \(\mathcal{N}=L^\infty(Y,\nu)\) and \(E\) are taken as the conditional expectation \(\mathbb{E}[\cdot\mid Y]\),\eqref{eq:op-algebra-zk-factorization} degenerates into the “transcriptional core dependent only on \(y\)” that defines \ref{def:xi-horizon-perfect-zero-knowledge};Takesaki's criterion gives: In the non-commutative setting, the expected existence of the \(\varphi\)-holding condition is controlled by the mold flow invariance (Theorem \ref{thm:op-algebra-takesaki-criterion}).\end{remark}

\paragraph{Takesaki invariance and state-preserving conditions are expected to exist}\begin{theorem}[Takesaki criterion: module group invariance \texorpdfstring{XPH0X}{<->} \(\varphi\)-preserving conditional expectation]\label{thm:op-algebra-takesaki-criterion}Let \(\mathcal{M}\) be the von Neumann algebra, \(\varphi\) its faithful normal state, and \(\sigma_t^\varphi\) the corresponding modular automorphism group.For inclusion \(\mathcal{N}\subset\mathcal{M}\), the following propositions are equivalent:\begin{enumerate}
\item exists \(\varphi\)-preserved normal condition expectation \(E:\mathcal{M}\to\mathcal{N}\), that is, \(E\) is \(\mathcal{N}\)-bimodular and \(\varphi\circ E=\varphi\);\item \(\mathcal{N}\) remains unchanged under mold flow:  \begin{equation}\label{eq:op-algebra-modular-invariance}
  \boxed{\;\sigma_t^\varphi(\mathcal{N})=\mathcal{N}\qquad(\forall t\in\RR).\;}
  \end{equation}
\end{enumerate}
And when the above conditions are met,Relative to \(\mathcal{N}\)’s “perfect zero-knowledge transcription channel”\[
\bigl\{\,\Phi:\mathcal{M}\toXPH1X\\]
\end{theorem}

\begin{proof}
\((1)\Leftrightarrow(2)\) is the core criterion in Takesaki’s conditional expectation theory;In the framework of coarse-graining of general states, see Accardi--Cecchini's systematic expression \cite{AccardiCecchini1982ConditionalExpectationsTakesaki},Also harking back to Takesaki's original work \cite{Takesaki1972ConditionalExpectationsVNA}.The last characterization is directly given by the "factor closure" semantics that define \ref{def:op-algebra-horizon-perfect-zk}: given \(E\), the post-synthesis of any \(S\) results in a zero-knowledge channel; the reverse is obtained by \(\Phi=S\circ E\).Certification completed.\end{proof}

\begin{remark}[Zero-knowledge operator algebraic equivalent expression]\label{rem:op-algebra-zk-as-fixed-subalgebra}Theorem \ref{thm:op-algebra-takesaki-criterion} shows that: "horizon zero knowledge" can be equivalently regarded as\(\mathcal{N}\) is the modular group invariant subalgebra of \((\mathcal{M},\varphi)\), so there is a state-preserving conditional expectation \(E\).Therefore, zero knowledge is not an additional probability theory assumption, but an \emph {modular theoretical invariant of visible subalgebra};This provides a non-commutative source for the text's juxtaposition of "visible closures" with "functional equation-type symmetries" such as \eqref{eq:xi-hzom-detailed-balance}.\end{remark}

\paragraph{Pimsner--Popa index and the upper bound of the number of indistinguishable witnesses}Below, when \(\mathcal{M}\) is the \(\mathrm{II}_1\) factor and \(\mathcal{N}\subset\mathcal{M}\) is the finite exponential subfactor,The "indistinguishable fiber size on the horizon" is accurately quantified as the Jones index using the Pimsner--Popa inequality.
\begin{theorem}[Pimsner--Popa Inequalities and Indistinguishable Witness Number Boundaries]\label{thm:op-algebra-pimsner-popa-witness-bound}Let \(\mathcal{M}\) be the factor of \(\mathrm{II}_1\), \(\mathcal{N}\subset\mathcal{M}\) be the subfactor, and there exists a trace-preserving normal condition expectation \(E:\mathcal{M}\to\mathcal{N}\) with finite Jones exponent \([\mathcal{M}:\mathcal{N}]<\infty\).Then there is an optimal constant\[
\lambda=[\mathcal{M}:\mathcal{N}]^{-1}
\]
Make \(x\in\mathcal{M}_+\) have\begin{equation}\label{eq:op-algebra-pimsner-popa}
\boxed{\;E(x)\ \ge\ \lambda\,x.\;}
\end{equation}
In addition, if there are \(k\) pairwise orthogonal projections \(\{p_i\}_{i=1}^k\subset\mathcal{M}\) satisfying\begin{equation}\label{eq:op-algebra-indist-projections}
\boxed{\;E(p_i)=k^{-1}1_{\mathcal{N}}\qquad(\forall i),\;}
\end{equation}
then there must be\begin{equation}\label{eq:op-algebra-k-index-bound}
\boxed{\;k\ \le\ [\mathcal{M}:\mathcal{N}].\;}
\end{equation}
\end{theorem}

\begin{proof}
The characterizations of \eqref{eq:op-algebra-pimsner-popa} and \(\lambda=[\mathcal{M}:\mathcal{N}]^{-1}\) are basic results of Pimsner--Popa, see \cite{PimsnerPopa1986EntropyIndexSubfactors}.For \eqref{eq:op-algebra-k-index-bound}:Take \(x=p_i\) from \eqref{eq:op-algebra-pimsner-popa} and combine it with \eqref{eq:op-algebra-indist-projections} to get\(
k^{-1}1_{\mathcal{N}}=E(p_i)\ge \lambda p_i
\)，
So \(k^{-1}\ge\lambda\) is \(k\le \lambda^{-1}=[\mathcal{M}:\mathcal{N}]\). Certification completed.\end{proof}

\begin{corollary}[Exponential lower bound for zero-knowledge soundness (an unimprovable scale)]\label{cor:op-algebra-soundness-lower-bound-index}Under the setting of theorem \ref{thm:op-algebra-pimsner-popa-witness-bound},If the validator's visible interface is restricted to \(\mathcal{N}\) (that is, all its decisions rely only on \(E(x)\in\mathcal{N}\)),Then any strategy facing "in-fiber indistinguishable" witness sets such as \eqref{eq:op-algebra-indist-projections} cannot reduce the worst-case error rate toUnder \([\mathcal{M}:\mathcal{N}]^{-1}\).More precisely, if the indistinguishable set reaches the maximum size \(k=[\mathcal{M}:\mathcal{N}]\), then the worst-case indistinguishability rate is at least \(1/k\), so there is a scale lower bound\[
\boxed{\;\varepsilon_{\mathrm{sound}}\ \gtrsim\ [\mathcal{M}:\mathcal{N}]^{-1}.\;}
\]
\end{corollary}

\begin{corollary}[Near Zero Knowledge—Soundness—Unbreakable Tradeoffs at Fiber Scale]\label{cor:op-algebra-nearzk-soundness-fiber-tradeoff}Under the setting of theorem \ref{thm:op-algebra-pimsner-popa-witness-bound}, suppose there are \(k\) pairwise orthogonal projections \(\{p_i\}_{i=1}^k\subset\mathcal{M}\) that satisfy\[
E(p_i)=k^{-1}1_{\mathcal{N}}\qquad(1\le i\le k),
\]
Thus completely indistinguishable at the horizon factor \(\mathcal{N}\).Consider an arbitrary (interactive or non-interactive) proof system whose transcribed random element is denoted by \(\tau\).If the system satisfies:\begin{enumerate}
\item \(\varepsilon\)-Soundness: The worst-case forgery success rate does not exceed \(\varepsilon\);\item \(\delta\) - Near-zero knowledge: There exists a simulation distribution  \(d_{\mathrm{TV}}(\mathsf{Law}(\tau\mid p_i),S)\le \delta\)；
\end{enumerate}
then there must be hard constraints\[
\boxed{\;\varepsilon\ \ge\ \frac{1-\delta}{k}.\;}
\]
Equivalently, if \(\varepsilon<1/k\) then there must be \(\delta\ge 1-k\varepsilon\).\end{corollary}
\begin{proof}[Points of Proof]By \(\delta\)-near zero knowledge, for any verification predicate \(\mathsf{Acc}=\mathsf{Acc}(\tau)\in[0,1]\) we have\(
\bigl|\mathbb{E}[\mathsf{Acc}\mid p_i]-\mathbb{E}_S[\mathsf{Acc}]\bigr|\le \delta
\)。
Therefore, if there is some \(i\) that makes \(\mathbb{E}[\mathsf{Acc}\mid p_i]\) close to \(1\), then a simulator without access to invisible fibers can push the acceptance rate to \(1-\delta\), so that the soundness error cannot be smaller than \(1-\delta\).On the other hand, if the verifier tries to simultaneously suppress the acceptance rate on \(k\) mutually exclusive fibers with indistinguishable horizons, its worst-case error rate is at least the lower bound of the average error rate under the uniform prior; combined with the scale constraint \(k\) of indistinguishable fibers (theorem \ref{thm:op-algebra-pimsner-popa-witness-bound}), we get \(\varepsilon\ge(1-\delta)/k\).The strict derivation can be regarded as the direct application of "TV-stable lower bound for multiple hypothesis testing" under the indistinguishable fiber diameter in this section. Certification completed.\end{proof}

\begin{proposition}[Exponential multiplicative properties of cross horizons: universal inequalities of finite exponential conditional expectations]\label{prop:op-algebra-index-submultiplicativity-commuting-square}Let \(\mathcal{A}\) be a finite von Neumann algebra and \(\mathcal{B}_1,\mathcal{B}_2\subseteq\mathcal{A}\) be a subalgebra.Suppose there is a formal condition of fidelity expected\(
E_i:\mathcal{A}\to\mathcal{B}_i
\)
(\(i=1,2\)) vs.\(
E_{12}:\mathcal{A}\to\mathcal{B}_1\cap\mathcal{B}_2
\), and assume that all three have finite Pimsner--Popa exponents.If the condition of commuting square is further satisfied\[
\boxed{\;E_1E_2=E_2E_1=E_{12},\;}
\]
Then there is always an exponential multiplicative inequality\[
\boxed{\;\mathrm{Ind}(E_{12})\ \le\ \mathrm{Ind}(E_1)\,\mathrm{Ind}(E_2).\;}
\]
And in the sense of "swapped square matrix and minimal" (that is, the above inequality takes the equal sign), the exponential gap of the cross interface is completely closed by the product of the exponentials of the two interfaces.\end{proposition}
\begin{proof}
According to the Pimsner--Popa inequality, there is an optimal constant \(\lambda_i=\mathrm{Ind}(E_i)^{-1}\) such that for all positive elements \(x\in\mathcal{A}_+\) there is \(E_i(x)\ge \lambda_i x\).Under the exchange square matrix \(E_{12}=E_1E_2\), then for \(x\ge 0\) we have\[
E_{12}(x)=E_1(E_2(x))\ \ge\ \lambda_1E_2(x)\ \ge\ \lambda_1\lambda_2\,x.
\]
Therefore \(\mathrm{Ind}(E_{12})^{-1}\ge \lambda_1\lambda_2=\mathrm{Ind}(E_1)^{-1}\mathrm{Ind}(E_2)^{-1}\), we get the inequality. Certification completed.\end{proof}

\begin{corollary}[Exponential discrimination of independence of two sources: lower bound of conditional mutual information into logarithmic exponential gap]\label{cor:op-algebra-cmi-lower-bound-by-index-gap}Under the setting of proposition \ref{prop:op-algebra-index-submultiplicativity-commuting-square}, take a faithful regular state \(\varphi\) so that \(E_1,E_2,E_{12}\) are all \(\varphi\)-maintained.Remember \(\mathcal{B}_0:=\mathcal{B}_1\cap\mathcal{B}_2\) and order\(
I_\varphi(\mathcal{B}_1:\mathcal{B}_2\mid\mathcal{B}_0)
\)
is the (non-commutative) conditional mutual information relative tothen there is a strict lower bound\[
\boxed{\;
I_\varphi(\mathcal{B}_1:\mathcal{B}_2\mid\mathcal{B}_0)
\ \ge\
\log\frac{\mathrm{Ind}(E_1)\mathrm{Ind}(E_2)}{\mathrm{Ind}(E_{12})}
\ \ge\ 0.\;}
\]
And if and only if the inclusion forms a minimal commutative square matrix (i.e.,\end{corollary}
\begin{proof}[Points of Proof]From strong subadditivity we get \(I_\varphi(\mathcal{B}_1:\mathcal{B}_2\mid\mathcal{B}_0)\ge 0\).On the other hand, under the limited exponential caliber, the Pimsner--Popa constant is the optimal exponential certificate of the "indistinguishable witness number"; juxtaposing it with the combination of the exchange square matrix \(E_{12}=E_1E_2\) can lower the gap of the conditional mutual information into the exponential gap \(\log\frac{\mathrm{Ind}(E_1)\mathrm{Ind}(E_2)}{\mathrm{Ind}(E_{12})}\).When the exchange square matrix is ​​extremely small, this gap disappears if and only if the two sources are conditionally independent at \(\varphi\) with respect to the cross-visibility factor \(\mathcal{B}_0\), and thus \(I_\varphi=0\). Certification completed.\end{proof}

\begin{proposition}[Realization of exponential extreme values ​​and extreme state structure: maximum information loss is achieved by maximum fiber]\label{prop:op-algebra-index-extremal-entropy-loss-maxfiber}In the finite-dimensional exchange caliber of appendix \ref{app:op-algebra-fold-subalgebra}, take the folding-induced conditional expectation \(E_m:\mathcal{A}_m\to\mathcal{B}_m\) (proposition \ref{prop:fold-conditional-expectation}), and record the maximum fiber multiplicity \(D_m:=\max_{x\in X_m}d_m(x)\).Then the Pimsner--Popa index satisfies\[
\boxed{\ \mathrm{Ind}(E_m)=D_m\ }.
\]
Let \(\sigma_m\) be the uniform state on \(\Omega_m\) (i.e., the probability vector \(\sigma_m(a)=|\Omega_m|^{-1}\)), and let \(\rho_{m,a}\) be the pure state \(\delta_a\) for anyUnder the identification of taking the relative entropy as the KL divergence, there is a strict identity\[
\boxed{\;
D_{\mathrm{KL}}(\rho_{m,a}\|\sigma_m)\;-\;D_{\mathrm{KL}}(\rho_{m,a}\circ E_m\ \|\ \sigma_m\circ E_m)
=\log d_m(\Fold_m(a))\;}
\]
Thus the maximum information loss satisfies\[
\boxed{\;
\sup_{a\in\Omega_m}\Bigl(D_{\mathrm{KL}}(\rho_{m,a}\|\sigma_m)-D_{\mathrm{KL}}(\rho_{m,a}\circ E_m\|\sigma_m\circ E_m)\Bigr)
=\log D_m=\log\mathrm{Ind}(E_m).\;}
\]
And the equal sign is if and only if \(a\) belongs to a certain largest fiber; in this case, \(\rho_{m,a}\circ E_m\) is a uniform state on the largest fiber (uniquely determined by the fiber average structure of \(E_m\), and is coorbital under the action of the fiber substitution group \(\mathcal{G}_m\), see the definition of \ref{def:fold-gauge-group}).\end{proposition}
\begin{proof}
According to the definition of Pimsner--Popa index, \(\mathrm{Ind}(E_m)^{-1}\) is equal to all satisfying\(
E_m(x)\ge \lambda x
\) The supremum of the constant \(\lambda>0\) (for all \(x\in(\mathcal{A}_m)_+\)).For the minimal projection \(P_a\) (of which \(x=\Fold_m(a)\)) we have\(
E_m(P_a)=d_m(x)^{-1}\widetilde P_x
\)
(Proposition \ref{prop:fold-conditional-expectation}), and \(P_a\le \widetilde P_x\).Therefore for any \(\lambda\le 1/D_m\) we have\[
E_m(P_a)=\frac{1}{d_m(x)}\widetilde P_x\ \ge\ \frac{1}{D_m}\widetilde P_x\ \ge\ \frac{1}{D_m}P_a.
\]
From linearity and positive cone generation (\(\mathcal{A}_m\cong \CC^{|\Omega_m|}\)), it can be generalized to any positive element, thus \(\mathrm{Ind}(E_m)\le D_m\).On the other hand, if \(a\) belongs to the largest fiber (\(d_m(\Fold_m(a))=D_m\)), then\(
E_m(P_a)=D_m^{-1}\widetilde P_x
\)
It can be seen that any \(\lambda\) that satisfies \(E_m(P_a)\ge \lambda P_a\) must not exceed \(1/D_m\), so the optimal constant is exactly \(1/D_m\), that is, \(\mathrm{Ind}(E_m)=D_m\).\par
Relative entropy identity: from \(\rho_{m,a}=\delta_a\) and \(E_m(P_{a'})=d_m(\Fold_m(a'))^{-1}\widetilde P_{\Fold_m(a')}\)\[
(\rho_{m,a}\circ E_m)(P_{a'})
=\rho_{m,a}\!\left(E_m(P_{a'})\right)
=\begin{cases}
1/d_m(x),& \Fold_m(a')=x,\\
0,& \text{otherwise},\end{cases}
\qquad x:=\Fold_m(a),
\]
That is, \(\rho_{m,a}\circ E_m\) is the uniform state on fiber \( \Fold_m^{-1}(x)\).In the same way,\[
(\sigma_m\circ E_m)(P_{a'})
=\sigma_m\!\left(E_m(P_{a'})\right)
=\frac{1}{d_m(\Fold_m(a'))}\cdot \sigma_m\!\left(\widetilde P_{\Fold_m(a')}\right)
=\frac{1}{d_m(\Fold_m(a'))}\cdot \frac{d_m(\Fold_m(a'))}{|\Omega_m|}
=\frac{1}{|\Omega_m|},
\]
Therefore \(\sigma_m\circ E_m=\sigma_m\).then\[
D_{\mathrm{KL}}(\rho_{m,a}\|\sigma_m)=\log|\Omega_m|,
\qquad
D_{\mathrm{KL}}(\rho_{m,a}\circ E_m\ \|\ \sigma_m)=\log\frac{|\Omega_m|}{d_m(x)},
\]
Subtract\(
D_{\mathrm{KL}}(\rho_{m,a}\|\sigma_m)-D_{\mathrm{KL}}(\rho_{m,a}\circ E_m\|\sigma_m\circ E_m)=\log d_m(x)
\)。
Taking the supremum of \(a\) gives the maximum value \(\log D_m=\log\mathrm{Ind}(E_m)\), and the equal sign is if and only if \(a\) belongs to the largest fiber. Certification completed.\end{proof}

\paragraph{Maximum relative entropy version horizon capacity: \texorpdfstring{XPH0X}{log Ind(E)} as the exact cap of the worst zero-knowledge loss}Proposition \ref{prop:op-algebra-index-extremal-entropy-loss-maxfiber} gives the "maximum fiber \(\Longleftrightarrow\) maximum loss" template under the KL (Shannon) relative entropy caliber.In the following the interface is promoted to the max-relative entropy and Rényi families: they characterize the "worst case" distinguishable loss in an institutional sense and are still completely sealed by the same exponential constant.
\begin{definition}[max-relative entropy]\label{def:op-algebra-max-relative-entropy}Let \(\mathcal{A}\) be the von Neumann algebra, and \(\rho,\sigma\) be the normal state above it.Define maximum relative entropy\begin{equation}\label{eq:op-algebra-dmax-def}
\boxed{\;
D_{\max}(\rho\|\sigma)
:=\log\inf\{\lambda>0:\ \rho\le \lambda\,\sigma\}\;}
\end{equation}
The order relationship is understood as a forward cone: \(\rho\le \lambda\sigma\) means \(\rho(x)\le \lambda\sigma(x)\) is true for all \(x\in\mathcal{A}_+\).\end{definition}

\begin{proposition}[Maximum relative entropy version horizon capacity: \texorpdfstring{XPH0X}{log Ind(E)} is equal to the worst zero-knowledge loss]\label{prop:op-algebra-dmax-capacity-equals-log-index}Let \(\mathcal{B}\subseteq\mathcal{A}\) be a finite von Neumann algebra containing,And \(E:\mathcal{A}\to\mathcal{B}\) is a faithful normal conditional expectation with finite Pimsner--Popa index \(\mathrm{Ind}(E)\).Define max-loss for any normal state \(\rho\)\[
L_{\max}(\rho):=D_{\max}(\rho\|\rho\!\circ\!E).
\]
Then there is always a unified upper bound\begin{equation}\label{eq:op-algebra-dmax-bound-by-index}
\boxed{\;
L_{\max}(\rho)\le \log\mathrm{Ind}(E).\;}
\end{equation}
And the supremum satisfies the exact extreme value formula\begin{equation}\label{eq:op-algebra-dmax-sup-equals-log-index}
\boxed{\;
\sup_{\rho\in\mathcal{S}(\mathcal{A})}L_{\max}(\rho)=\log\mathrm{Ind}(E),\;}
\end{equation}
Among them, \(\mathcal{S}(\mathcal{A})\) represents the overall normal state on \(\mathcal{A}\).\end{proposition}
\begin{proof}
By the finite exponent assumption, the Pimsner--Popa inequality gives: for all \(x\in\mathcal{A}_+\)\(
E(x)\ge \mathrm{Ind}(E)^{-1}x
\)。
For any normal state \(\rho\) acting on this inequality we get\[
(\rho\!\circ\!E)(x)=\rho(E(x))\ \ge\ \mathrm{Ind}(E)^{-1}\rho(x),
\]
That is, \(\rho(x)\le \mathrm{Ind}(E)\,(\rho\!\circ\!E)(x)\) holds for all \(x\ge0\),Therefore \(\rho\le \mathrm{Ind}(E)\,(\rho\!\circ\!E)\).From defining \eqref{eq:op-algebra-dmax-def} we get \eqref{eq:op-algebra-dmax-bound-by-index}.\par
To prove \eqref{eq:op-algebra-dmax-sup-equals-log-index}, take any \(c<\mathrm{Ind}(E)\).If there is \(L_{\max}(\rho)\le \log c\) for all normal states \(\rho\), then \(\rho\le c(\rho\!\circ\!E)\) from \eqref{eq:op-algebra-dmax-def} is true for all \(\rho\),Equivalent to \(\rho(x)\le c\,\rho(E(x))\), which holds for all \(x\in\mathcal{A}_+\) and all normal \(\rho\).From the normal positive functional separation order (\(x\le y\) if and only if \(\rho(x)\le \rho(y)\) holds for all normal states \(\rho\)), it follows that \(x\le cE(x)\) holds for all \(x\ge0\),That is, \(E(x)\ge c^{-1}x\), and thus \(\mathrm{Ind}(E)\le c\), a contradiction.Therefore, for each \(c<\mathrm{Ind}(E)\) there must be some \(\rho\) such that \(L_{\max}(\rho)>\log c\), so\(\sup_{\rho}L_{\max}(\rho)\ge \log\mathrm{Ind}(E)\)。
Combined with \eqref{eq:op-algebra-dmax-bound-by-index} we get the equal sign. Certification completed.\end{proof}

\begin{corollary}[Rényi horizon loss spectrum: monotonic interpolation from Shannon to min-entropy and exponential capping]\label{cor:op-algebra-renyi-loss-spectrum-capped-by-index}Under the setting of proposition \ref{prop:op-algebra-dmax-capacity-equals-log-index},Take \(\alpha\in[1,\infty]\), let \(D_\alpha\) be the sandwiched Rényi relative entropy (\(\alpha=1\) takes Umegaki relative entropy, \(\alpha=\infty\) takes \(D_{\max}\)), and define the horizon loss spectrum\[
L_\alpha(\rho):=D_\alpha(\rho\|\rho\!\circ\!E).
\]
Then for each fixed \(\rho\), the function \(\alpha\mapsto L_\alpha(\rho)\) is monotonic and does not decrease, and satisfies the unified cap\[
\boxed{\;
0\le L_\alpha(\rho)\le \log\mathrm{Ind}(E)\qquad(\forall \alpha\in[1,\infty]).\;}
\]
And the limit \(\lim_{\alpha\to\infty}L_\alpha(\rho)=D_{\max}(\rho\|\rho\!\circ\!E)\) exists.\end{corollary}
\begin{proof}
The monotonicity and limit of sandwiched Rényi relative entropy for parameter \(\alpha\) is its standard property, so that \(\alpha\to\infty\) converges to \(D_{\max}\)\(
L_\alpha(\rho)\le L_\infty(\rho)=D_{\max}(\rho\|\rho\!\circ\!E)
\)。
Then combine it with the upper bound \eqref{eq:op-algebra-dmax-bound-by-index} of proposition \ref{prop:op-algebra-dmax-capacity-equals-log-index} to get it. Certification completed.\end{proof}

\begin{corollary}[Extremal templates for finite exchange folding: maximum loss is achieved by "single-point microstates on the largest fiber"]\label{cor:op-algebra-commutative-dmax-extremizers-maxfiber}In the finite exchange aperture of appendix \ref{app:op-algebra-fold-subalgebra}, let \(\mathcal{A}=\CC^{\Omega}\), \(\mathcal{B}=\CC^{X}\), and letRecord fiber multiplicity \(d(x)=|\Fold^{-1}(x)|\) and \(D:=\max_{x\in X}d(x)=\mathrm{Ind}(E)\).Then for any normal state \(\rho\) we have\[
D_{\max}(\rho\|\rho\!\circ\!E)\ \le\ \log D.
\]
And \(D_{\max}(\rho\|\rho\!\circ\!E)=\log D\) if and only if there is a certain maximum fiber point \(x^\star\) (\(d(x^\star)=D\)) and a certain \(\omega^\star\in\Fold^{-1}(x^\star)\) such that\[
\rho(\omega)=0\qquad\bigl(\forall \omega\in\Fold^{-1}(x^\star)\setminus\{\omega^\star\}\bigr).
\]
In this case, the coarse-grained state \(\rho\!\circ\!E\) must be uniformly distributed on the fiber \(\Fold^{-1}(x^\star)\) (the mass is uniformly distributed as \(\rho(\Fold^{-1}(x^\star))\)), so "maximum fiber + fiber homogenization (after coarse-graining)" becomes the structural template for maximum loss realization.\end{corollary}
\begin{proof}
Identify \(\rho\) as the probability vector \(p\) over \(\Omega\).For any \(\omega\in\Omega\) let \(x=\Fold(\omega)\), and record the total fiber mass \(P_x:=\sum_{\omega'\in\Fold^{-1}(x)}p(\omega')\).Then the probability vector \(q\) corresponding to \((\rho\!\circ\!E)\) is constant on the fiber:\(
q(\omega)=P_x/d(x)
\)。
Therefore the pointwise ratio satisfies\[
\frac{p(\omega)}{q(\omega)}=\frac{d(x)\,p(\omega)}{P_x}\ \le\ d(x)\ \le\ D,
\]
The inequality uses \(p(\omega)\le P_x\).Therefore, the point state of \(p\le Dq\) is established, which is equivalent to \(\rho\le D(\rho\!\circ\!E)\), so\(
D_{\max}(\rho\|\rho\!\circ\!E)\le \log D
\)。
\par
And \(D_{\max}(\rho\|\rho\!\circ\!E)=\log D\) is equivalent to the existence of some point \(\omega^\star\) such that \(\frac{p(\omega^\star)}{q(\omega^\star)}=D\).From the above formula, we know that this requires \(d(\Fold(\omega^\star))=D\) and \(p(\omega^\star)=P_{x^\star}\), that is, all the mass on the largest fiber is concentrated at a single point \(\omega^\star\).Finally, the uniformity of XPHOX on that fiber is given by the average structure of the fiber expected by the conditions. Certification completed.\end{proof}

\paragraph{Fuglede--non-commutative definition of Kadison determinant and "horizon \texorpdfstring{XPH0X}{zeta}"}The following determinant is a non-commutative generalization of the Fredholm determinant \(\det(I-\mathcal{K}_s)\) defined in the text as "protocol \(\zeta\)".
\begin{definition}[Fuglede--Kadison determinant and non-commutative \texorpdfstring{XPH0X}{zeta}]\label{def:op-algebra-fk-determinant-zeta}Let \((\mathcal{A},\tau)\) be a finite von Neumann algebra with a faithful normal trace.Define the Fuglede--Kadison determinant for any element \(X\in\mathcal{A}\)\begin{equation}\label{eq:op-algebra-fkdet}
\boxed{\;\Delta_\tau(X):=\exp\bigl(\tau(\log|X|)\bigr)\ \in\ [0,\infty),\qquad |X|:=(X^\ast X)^{1/2}.\;}
\end{equation}
Among them, when \(|X|\) has a non-trivial core, \(\tau(\log|X|)=-\infty\) is understood according to the standard convention and \(\Delta_\tau(X)=0\) is taken.Given a family of "horizon shift elements" \(K_s\in\mathcal{A}\) that are analytically dependent on \(s\), define a non-commutative \(\zeta\)-type object\begin{equation}\label{eq:op-algebra-nc-zeta}
\boxed{\;
Z_{\mathcal{A}}(s)\ :=\ \Delta_\tau(I-K_s)^{-1}.
}
\end{equation}
\end{definition}

\begin{proposition}[Multiplicative properties, matrix degeneracy and zeros as visible spectral resonances]\label{prop:op-algebra-fkdet-properties}Under the settings defining \ref{def:op-algebra-fk-determinant-zeta}:\begin{enumerate}
\item \(\Delta_\tau\) is strictly multiplicative: \(\Delta_\tau(XY)=\Delta_\tau(X)\Delta_\tau(Y)\);\item When \(\mathcal{A}=M_n(\CC)\) and \(\tau=\frac{1}{n}\Tr\), there is  \[
  \Delta_\tau(X)=|\det(X)|^{1/n};
  \]
\item If \(\Delta_\tau(I-K_s)=0\), then \(I-K_s\) is non-reversible and is equivalent to  \begin{equation}\label{eq:op-algebra-resonance}
  \boxed{\;1\in\mathrm{Spec}_{\mathcal{A}}(K_s),\;}
  \end{equation}
The "zero/pole anomalies" of \(Z_{\mathcal{A}}(s)\) can thus be interpreted as resonance collisions in the visible spectrum.\end{enumerate}
\end{proposition}

\begin{proof}
Multiplicative and matrix degenerate properties originate from the basic theory of Fuglede-Kadison determinants, see \cite{FugledeKadison1951DeterminantsTraceFiniteFactors}.In clause (3), \(\Delta_\tau(X)=0\) iff \(X\) is irreversible (equivalent to \(0\in\mathrm{Spec}(X)\) in limited circumstances);Take \(X=I-K_s\) to get \eqref{eq:op-algebra-resonance}. Certification completed.\end{proof}

\begin{remark}["visible multiplicity" and principal divergence coefficient of non-commutative \texorpdfstring{XPH0X}{zeta}]\label{rem:op-algebra-nc-zeta-visible-multiplicity}Under the definition of \ref{def:op-algebra-fk-determinant-zeta}, let \(s_0\) satisfy \(1\in\mathrm{Spec}_{\mathcal{A}}(K_{s_0})\).If it is further assumed that \(1\) is an isolated spectral point in \(\mathrm{Spec}_{\mathcal{A}}(K_{s_0})\), the Riesz projection can be defined by the holomorphic functional calculus\[
E_{s_0}:=E_{K_{s_0}}(\{1\})\ \in\ \mathcal{A},
\]
Together\(
m_{\mathrm{vis}}(s_0):=\tau(E_{s_0})
\)
It is the \emph{visible multiplicity} of spectral point \(1\).\begin{enumerate}
\item \textbf{(matrix degenerate)} when \(\mathcal{A}=M_n(\CC)\) and \(\tau=\frac{1}{n}\Tr\),  \(Z_{\mathcal{A}}(s)=|\det(I-K_s)|^{-1/n}\)。
If \(\det(I-K_s)\) has zero point order \(m_0\) at \(s_0\), then  \[
  \log Z_{\mathcal{A}}(s)
  =\frac{m_0}{n}\log|s-s_0|^{-1}+O(1)
  =m_{\mathrm{vis}}(s_0)\log|s-s_0|^{-1}+O(1),
  \]
Among them \(m_{\mathrm{vis}}(s_0)=\tau(E_{s_0})=m_0/n\).\item \textbf{(general finite factor)} In the case of general finite factors,It can be regarded as the intrinsic substitution of "pole order": when \(s\to s_0\) triggers \(\Delta_\tau(I-K_s)\to 0\),The logarithmic divergent principal term of \(\log Z_{\mathcal{A}}(s)=-\tau(\log|I-K_s|)\) is controlled by this trace weight,Thus, the "zero/pole anomaly" is promoted to an auditable spectral projection trace invariant.\end{enumerate}
\end{remark}

\paragraph{Intrinsic source of zero-knowledge modular scattering and functional equations}Under the framework of modular theory, if there are two complementary horizon coarse-grainings (incoming/outgoing) and both are the same reference state,Their relative mode realizations can then be considered as "scattering", with the critical line pure phaseliness enforced by the dual reciprocity.
\begin{proposition}[Reflection duality of mode scattering determinants and critical line pure phase properties]\label{prop:op-algebra-modular-scattering-phase}Under the setting of theorem \ref{thm:op-algebra-takesaki-criterion}, further assume that there are two \(\varphi\)-preserving conditional expectations\(
E_{\mathrm{in}},E_{\mathrm{out}}:\mathcal{M}\to\mathcal{N}
\)
(Corresponding to the two visible interfaces of incident/exit), and the reversible "scattering operator family" \(S(s)\) and its scattering determinant \(\varphi_{\mathrm{sc}}(s)\) induced by the relative mode mutual adjoint can be defined under a certain traceability/regularization caliber.If the structure carries the mode dual reciprocity of \(s\leftrightarrow 1-s\), then the scattering determinant satisfies the functional equation\begin{equation}\label{eq:op-algebra-scattering-fe}
\boxed{\;\varphi_{\mathrm{sc}}(s)\,\varphi_{\mathrm{sc}}(1-s)=1,\;}
\end{equation}
Therefore, on the critical slice \(s=\tfrac12+\mathrm{i}t\) there must be\begin{equation}\label{eq:op-algebra-scattering-unit-modulus}
\boxed{\;\bigl|\varphi_{\mathrm{sc}}(\tfrac12+\mathrm{i}t)\bigr|=1.\;}
\end{equation}
\end{proposition}

\begin{remark}[Isotype with finite volume hyperbolic scattering]\label{rem:op-algebra-scattering-selberg-analogy}In the standard spectral theory of finite volume hyperboloids (including cusp), the scattering matrix determinant satisfies the functional equation of the same type as \eqref{eq:op-algebra-scattering-fe},And coupled with the completion expression of Selberg \(\zeta\); related discussion and quantitative distribution properties can be found in \cite{GarunkstisSimenas2017SelbergAValues}.This proposition abstracts it as: \(\varphi\)-maintaining coarse-grained dual-channel structure\end{remark}

\paragraph{Poisson boundary entropy and zero-knowledge iteration criterion}When the radiation/evaporation process is viewed as an iteration of a regular Markov action, the Poisson bound is the limiting algebra of "long-term visible statistics";A boundary entropy of zero corresponds exactly to the fact that the visible statistics can be completely pulled back by the state-preserving condition expectation, so that a global simulator exists.
\begin{theorem}[Poisson boundary entropy is zero \texorpdfstring{XPH0X}{<->} state holding condition is expected to exist]\label{thm:op-algebra-poisson-boundary-entropy}Let \(\mathcal{M}\) be the \(\mathrm{II}_1\) factor, andAnd let \(\mathcal{A}\) be the Poisson boundary algebra related to \(\Phi\) (including \(\mathcal{M}\subset\mathcal{A}\)).For a suitable boundary state \(\zeta\), record the boundary entropy as \(h_\Phi(\mathcal{M}\subset\mathcal{A},\zeta)\).then there is strict equivalence\begin{equation}\label{eq:op-algebra-poisson-entropy-zero}
\boxed{\;
h_\Phi(\mathcal{M}\subset\mathcal{A},\zeta)=0
\ \Longleftrightarrow\
\exists\\;}
\end{equation}
Therefore, the entire visible statistics produced by long-term iterations can be considered to be pulled back by \(\mathcal{E}\) if and only if the boundary entropy is zero,and can be generated by a global simulator relying only on\end{theorem}

\begin{proof}
This is the standard conclusion of "entropy zero \(\Leftrightarrow\) boundary trivial/expected recovery" in Poisson's boundary theory;See \cite{DasPeterson2022PoissonBoundariesII1} in System construction and entropy theory on \(\mathrm{II}_1\) factor Poisson boundaries.Certification completed.\end{proof}

\paragraph{Bayesian inverse \texorpdfstring{XPH0X}{=} simulator}The "existence of the simulator" is completely translated from the language of probability theory into "whether the non-commutative Bayesian inversion is definable", and its existence is controlled by the modular group criterion.
\begin{definition}[Non-commutative Bayesian inverse]\label{def:op-algebra-bayesian-inverse}Let \(F:\mathcal{N}\hookrightarrow\mathcal{M}\) be the inclusion mapping, \(\varphi\) be the upper reference state of \(\mathcal{M}\) and denote it as \(\xi:=\varphi\circ F\).It is said that \(F\) has an \textbf {non-commutative Bayesian inverse} \(F^\sharp:\mathcal{M}\to\mathcal{N}\). If \(F^\sharp\) is a regular complete positive mapping,and satisfies in the sense of \(\xi\)\begin{equation}\label{eq:op-algebra-bayes-inverse}
\boxed{\;F^\sharp\circ F=\mathrm{id}_{\mathcal{N}},\qquad \xi\circ F^\sharp=\varphi.\;}
\end{equation}
\end{definition}

\begin{theorem}[Bayesian inverse existence and modular group criterion; category closure of zero-knowledge decomposition]\label{thm:op-algebra-bayes-inverse-zk}Under the settings that define \ref{def:op-algebra-bayesian-inverse},The existence of the Bayesian inverseOnce \(F^\sharp\) exists, then for any normal fully positive transcription channel \(\Phi:\mathcal{M}\to\mathcal{T}\), the following propositions are equivalent:\begin{enumerate}
\item has a regular completely positive mapping \(S:\mathcal{N}\to\mathcal{T}\) such that  \begin{equation}\label{eq:op-algebra-zk-via-bayes-inverse}
  \boxed{\;\Phi = S\circ F^\sharp\;}
  \end{equation}
(i.e. \(\Phi\) factors through \(F^\sharp\) in a categorical sense and is thus perfectly zero-knowledge relative to \(\mathcal{N}\));\item \(\Phi\) are unchanged under the "visible closure" projection \(F\circ F^\sharp:\mathcal{M}\to\mathcal{M}\):  \begin{equation}\label{eq:op-algebra-zk-closure-fixed}
  \boxed{\;\Phi=\Phi\circ F\circ F^\sharp.\;}
  \end{equation}
\end{enumerate}
When the equivalence condition holds, \(S\) in the factorized \(\Phi=S\circ F^\sharp\) is unique and is given by\begin{equation}\label{eq:op-algebra-zk-simulator-unique}
\boxed{\;S=\Phi|_{\mathcal{N}},\qquad S(y)=\Phi(F(y))\ (y\in\mathcal{N}).\;}
\end{equation}
\end{theorem}

\begin{proof}
See \cite{GiorgettiEtAl2023BayesianInversionModularGroup} for the correspondence between Bayesian inverses and modular groups, and a generalization of the Takesaki/Accardi--Cecchini results.\par
\((1)\Rightarrow(2)\): If \(\Phi=S\circ F^\sharp\), then\(
\Phi\circ F\circ F^\sharp=S\circ (F^\sharp\circ F)\circ F^\sharp=S\circ F^\sharp=\Phi
\), get \eqref{eq:op-algebra-zk-closure-fixed}.\par
\((2)\Rightarrow(1)\): If \(\Phi=\Phi\circ F\circ F^\sharp\), define \(S:\mathcal{N}\to\mathcal{T}\) as \(S:=\Phi\circ F\).but\(
S\circ F^\sharp=\Phi\circ F\circ F^\sharp=\Phi
\), get \eqref{eq:op-algebra-zk-via-bayes-inverse}.\par
Uniqueness: if \(\Phi=S\circ F^\sharp\), then for any \(y\in\mathcal{N}\) there is \(y=F^\sharp(F(y))\), so\(
S(y)=S(F^\sharp(F(y)))=\Phi(F(y))
\), that is, \(S=\Phi|_{\mathcal{N}}\) (formula \eqref{eq:op-algebra-zk-simulator-unique}).Certification completed.\end{proof}

\begin{corollary}[Bayesian inverse-induced zero-knowledge channel reflecton and category isomorphism]\label{cor:op-algebra-zk-reflector-bayes}Under the setting of theorem \ref{thm:op-algebra-bayes-inverse-zk}, let it be seen that the closure idempotent\[
K:=F\circ F^\sharp:\mathcal{M}\to\mathcal{M}.
\]
For any transcription algebra \(\mathcal{T}\), define a zero-knowledge class (as a subset of the homomorphic set)\[
\mathrm{ZK}_{\mathcal{N}}(\mathcal{M},\mathcal{T})
:=\left\{\Phi:\mathcal{M}\toXPH1X\\]
Then restriction/post-synthesis gives natural bijection\[
\mathrm{CP}(\mathcal{N},\mathcal{T})\ \xrightarrow{\ \cong\ }\ \mathrm{ZK}_{\mathcal{N}}(\mathcal{M},\mathcal{T}),
\qquad
S\ \longmapsto\ S\circ F^\sharp,
\]
Its inverse mapping is \(\Phi\mapsto \Phi|_{\mathcal{N}}\).Equivalently, the presynthesized functor \((-) \circ F^\sharp:\mathrm{CP}(\mathcal{N},\mathcal{T})\to \mathrm{CP}(\mathcal{M},\mathcal{T})\) is fully faithful, and its essential image is exactly \(\mathrm{ZK}_{\mathcal{N}}(\mathcal{M},\mathcal{T})\);Thus in the category (von Neumann algebras, normal complete positive maps), \(\mathrm{ZK}_{\mathcal{N}}(\mathcal{M},\mathcal{T})\) gives a reflecton subcategory of \(\mathrm{CP}(\mathcal{M},\mathcal{T})\) (represented by the fixed-point class of homomorphism sets).\end{corollary}
\begin{proof}
According to the equivalence of theorem \ref{thm:op-algebra-bayes-inverse-zk}: \(\Phi=\Phi\circ K\) if and only if there exists \(S\) such that \(\Phi=S\circ F^\sharp\),And get the uniqueness of \(S=\Phi|_{\mathcal{N}}\) from \eqref{eq:op-algebra-zk-simulator-unique},Thus the reciprocal correspondence and full fidelity are obtained. Certification completed.\end{proof}

\begin{corollary}[Zero-knowledge post-synthetic ideal, convex closure and tensor closure]\label{cor:op-algebra-zk-ideal-tensor-closure}Under the setting of theorem \ref{thm:op-algebra-bayes-inverse-zk}, for any transcription algebra \(\mathcal{T}\):\begin{enumerate}
\item\item \textbf{(convex combined closure)} If \(\Phi_i\in\mathrm{ZK}_{\mathcal{N}}(\mathcal{M},\mathcal{T})\), \(p_i\ge 0\), \(\sum_i p_i=1\), then \(\sum_i p_i\Phi_i\in\mathrm{ZK}_{\mathcal{N}}(\mathcal{M},\mathcal{T})\).\end{enumerate}
Furthermore, let \(F':\mathcal{N}'\hookrightarrow\mathcal{M}'\) also have the Bayesian inverse \((F')^\sharp\), and let \(K':=F'\circ (F')^\sharp\).If \(\Phi\in\mathrm{ZK}_{\mathcal{N}}(\mathcal{M},\mathcal{T})\), \(\Phi'\in\mathrm{ZK}_{\mathcal{N}'}(\mathcal{M}',\mathcal{T}')\),Then its space tensor product satisfies\[
\Phi\bar\otimes \Phi'\in \mathrm{ZK}_{\mathcal{N}\bar\otimes \mathcal{N}'}(\mathcal{M}\bar\otimes \mathcal{M}',\mathcal{T}\bar\otimes \mathcal{T}').
\]
And the above closure properties can be equivalently restated as "factorization is invariant through Bayesian inverse/visible closure idempotence" (theorem \ref{thm:op-algebra-bayes-inverse-zk}).\end{corollary}
\begin{proof}
Obtained from \(\Phi=\Phi\circ K\)\(
\Psi\circ\Phi=\Psi\circ\Phi\circ K
\), so the post-synthetic closure is obtained; the convex combination closure is obtained directly from linearity.The tensor closure is given by\[
(\Phi\bar\otimes\Phi')\circ (K\bar\otimes K')
=(\Phi\circ K)\bar\otimes(\Phi'\circ K')
=\Phi\bar\otimes\Phi'
\]
roll out.The equivalent statement follows immediately from (1)$\Leftrightarrow$(2) of theorem \ref{thm:op-algebra-bayes-inverse-zk}. Certification completed.\end{proof}

\paragraph{Selberg determinant expression and Riemann resonance in arithmetic scattering}\begin{remark}[\texorpdfstring{XPH0X}{zeta}-ratio structure and zero-knowledge instability frequency in scattering poles]\label{rem:op-algebra-selberg-zeros-as-resonances}In the spectral theory of finite volume hyperboloids (including cusp), Selberg \(\zeta\) can be expressed as a combination of the Laplacian determinant and the scattering matrix determinant,And satisfy the coupled functional equations \(Z(s)\varphi_{\mathrm{sc}}(s)=\eta(s)Z(1-s)\) and \(\varphi_{\mathrm{sc}}(s)\varphi_{\mathrm{sc}}(1-s)=1\)(See note \ref{rem:op-algebra-scattering-selberg-analogy}).Under the geometric data related to the arithmetic symmetry group, the ratio structure of \(\zeta(2s)\) and \(\zeta(2s-1)\) appears in the scattering determinant,The zeros of the Riemann \(\zeta\) thus appear as poles/resonances in the scattering object.Therefore, if the "critical line pure phase" of the proposition \ref{prop:op-algebra-modular-scattering-phase} is used as the unitarity requirement for zero-knowledge stability,Then all resonances can only appear in a critical manner; any hypothetical non-critical resonance will introduce an amplitude anomaly of \(|\varphi_{\mathrm{sc}}(\tfrac12+\mathrm{i}t)|\neq 1\),This gives a scalable differentiator (zero-knowledge instability frequency).\end{remark}

\paragraph{Exponent--Determinant--Unified control law of zero point (template)}\begin{proposition} [Indices as co-controlled quantities: simulatability, zero-free bands and forgery advantages] \label{prop:op-algebra-index-determinant-zero-control}Let \(\mathcal{N}\subset\mathcal{M}\) be the finite exponential subfactor containing and existing trace-preserving conditional expectationAnd its "visible spectrum resonance" is statistically read out by horizon verification through the degradation of \(\Delta_\tau(I-K_s)\).Then the Jones index \([\mathcal{M}:\mathcal{N}]\) serves as the common control variable for the following three types of constraints at the same time:\begin{enumerate}
\item\item\item \textbf{(Out-of-band zero point\end{enumerate}
Therefore, the index can be interpreted as the "horizon entropy capacity" or as the joint control quantity of "\(\zeta\)-spectral stability" and "protocol soundness limit".\end{proposition}

\endinput

