\subsection{Time scale and readout stability of spectral gap driving}\label{subsec:pom-spectral-gap}
\begin@@ZXQPH1QXZ@@[Readout time scale controlled by spectral gap]\label{cor:pom-spectral-gap-time}
Assume $M_K(u)$ is original, the main eigenvalue is $\lambda_1(u)$, and the maximum modulus of the remaining eigenvalues ​​is $\lambda_2(u)$. By theorem \ref{thm:kernel-weighted-prime-orbit},
\[
B_n(u)=\frac{\lambda_1(u)^n}{n}+O\!\left(\frac{|\lambda_2(u)|^n}{n}\right),
\]
So the relative error satisfies
\[
\left|\frac{nB_n(u)}{\lambda_1(u)^n}-1\right|
\le C\left(\frac{|\lambda_2(u)|}{\lambda_1(u)}\right)^n.
\]
Given the target accuracy $\varepsilon\in(0,1)$, satisfy
\[
n\ \ge\ \frac{\log(C/\varepsilon)}{\log\!\bigl(\lambda_1(u)/|\lambda_2(u)|\bigr)}
\]
This ensures that the relative error does not exceed $\varepsilon$. If the nuclear RH/GRH condition (definition \ref{def:kernel-rh}) is further satisfied, $\lambda_1(u)^{1/2}$ can be used instead of $|\lambda_2(u)|$, thus obtaining the square root attenuation scale (corollary \ref{cor:kernel-rh-sqrt-error}).
\end{corollary}

\begin@@ZXQPH1QXZ@@[Self-duality and complete duality]\label{rem:pom-duality-even}
If the synchronization core satisfies the self-dual relationship of \ref{app:kernel-self-dual} in the appendix, then the completed $\zeta$ function will be strictly dual on the $\theta$ variable. Then its odd-order derivative to $\theta$ disappears at $\theta=0$, which imposes symmetry constraints on the fluctuation statistics of the cost potential; the corresponding adjustability can only be achieved by destroying the self-dual structure.
\end{remark}

\subsubsection{Endogenous time correlation: correlation length scale given by the spectral gap (no external input)}

This section further explains the "spectral gap" from prime-orbit error control to \textbf{time-dependent attenuation scale}. The emphasis here is on "no external input": the stochasticity considered comes from the Parry/equilibrium measure of the kernel itself (i.e., the stationary Markov structure induced by the finite-dimensional transport operator), rather than from external noise or external driving.

\begin@@ZXQPH2QXZ@@[Spectral gap \texorpdfstring{$\Rightarrow$}{=>} Time-dependent exponential decay (auditable)]\label{prop:pom-time-corr-gap}
Let $B_\theta$ be a family of tilt transfer matrices for finite-state synchronization kernels (e.g., one-dimensional tilts to the output potential $e$), and assume that for a given $\theta$ the matrix $B_\theta$ is primitive. remember
\(
\lambda_1(\theta)=\rho(B_\theta),\quad
\Lambda(\theta)=\max\{ |\lambda|:\ \lambda\in\mathrm{spec}(B_\theta),\ |\lambda|<\lambda_1(\theta)\}
\), and let
\[
\rho(\theta):=\frac{\Lambda(\theta)}{\lambda_1(\theta)}\in[0,1),
\qquad
g(\theta):=-\log \rho(\theta)\in(0,+\infty).
\]
Then for any observable $F,G$ that depends on a finite window (after being transformed into a single-time function by block promotion), there is a constant $C_{F,G}(\theta)<\infty$ under the Parry/equilibrium measure of $B_\theta$ such that for all $n\ge 0$
\[
\bigl|\mathrm{Cov}(F,\,G\circ\sigma^n)\bigr|\ \le\ C_{F,G}(\theta)\,\rho(\theta)^n.
\]
Therefore it can be
\(
\tau(\theta):=1/g(\theta)
\)
Interpreted as "correlation time", and $\tau_{1/2}(\theta):=\log 2\,/\,g(\theta)$ can be interpreted as half-life (the time for the exponential envelope to decay to $1/2$).
\end{proposition}

\begin{proof}
For the original non-negative matrix $B_\theta$, the Perron--Frobenius theory gives the unique main eigenvalue $\lambda_1(\theta)>0$ and the corresponding left and right positive eigenvectors. By the standard construction of the Parry measure (Doob transformation) the covariance can be written as
\(
\mathrm{Cov}(F,G\circ\sigma^n)=\langle f,\,\mathcal{T}_\theta^n g\rangle
\)
in the form, where $\mathcal{T}_\theta$ is the normalized transfer operator (spectral radius $1$), and its non-trivial spectral radius is $\rho(\theta)$. For the centered function, the main eigenmode disappears and only the spectral contribution remains, thus obtaining an exponential decay bound. All quantities are finite-dimensional linear algebra objects and can be directly audited by matrix spectra and vector inner products.
\end{proof}

\input{sections/generated/fig_sync_kernel_time_correlation}

\subsection{Conclusion Cluster IV: Exponential scale saturation and constant level fingerprint}\label{subsec:pom-lambda-saturation}
\begin@@ZXQPH1QXZ@@[Exponential scale input saturation]\label{prop:pom-lambda-saturation}
For deterministic, fully read-in synchronized kernels, the Perron roots $\lambda$ are primarily determined by the input skeleton, while the kernel orientation only contributes smaller mode spectral corrections. Specifically, the 40-state real input kernel satisfies $\rho(M)=\rho(A)=\varphi^2$ (proposition \ref{prop:real-input-40-skeleton-factor}), and the 10-state synchronization kernel satisfies $\lambda=3$ (appendix determinant decomposition of \ref{app:sync-kernel-basic}). So $\log\lambda$ is closer to a saturation scale of "input branch entropy" than a fine-grained fingerprint of mechanism differences.
\end{proposition}

\begin@@ZXQPH1QXZ@@[Three types of fingerprints distinguishing mechanism]\label{rem:pom-mechanism-fingerprint}
When the main scale $\lambda$ is saturated, the real distinction between the operation mechanism comes from the constant level and the distorted spectral layer:
\begin{itemize}
\item \textbf{Finite part curve $\log\mathfrak{M}(\theta)$:} is recorded side by side with $P(\theta)=\log\rho(M_\theta)$ on the same slice to characterize constant-level drift (see the comparison note in \ref{subsec:chi-ldp}).
\item \textbf{Dirichlet--Mertens constant tensor:} Gives a constant-level bias to primitive track counts stratified by congruence classes (see \ref{subsec:arity-dirichlet-mertens-tensor} and related appendices).
\item \textbf{Worst twist ratio $\rho/\lambda$:} controlled by a Chebotarev-type error on the ordered projected mixed time scale (theorem \ref{thm:kernel-chebotarev-exp} and corollary \ref{cor:pom-order-mixing}).
\end{itemize}
These three types of quantities are directly complementary to $\lambda$ and constitute the auditable fingerprint of the "operation mechanism"; therefore, when $\lambda$ is locked by the input branch, the difference between cores must fall on $\log\mathfrak{M}$ and finite interfaces such as Dirichlet--Mertens tensors.
\end{remark}

\subsection{RH-sharp spectral boundary of real input 40-state kernel and toy RH}\label{subsec:pom-rh-sharp}
\begin@@ZXQPH1QXZ@@[RH-sharp/Ramanujan spectrum world]\label{thm:pom-rh-sharp}
Determinant decomposition of real input 40-state kernel
\[
\det(I-zM)=(1-z)^2(1+z)^3(1-3z+z^2)(1+z-z^2)
\]
(See appendix \ref{app:real-input-40-finite-rh}) gives a non-zero spectrum
\[
\mathrm{spec}(M)\setminus\{0\}
=\{\lambda,\ -\sqrt{\lambda},\ 1^{(2)},\ (-1)^{(3)},\ \lambda^{-1},\ \lambda^{-1/2}\},
\qquad \lambda=\varphi^2.
\]
Therefore, all non-zero eigenvalues ​​$\nu$ except $\lambda$ satisfy
\[
|\nu|\le \sqrt{\lambda}=\varphi,
\]
And only one non-trivial eigenvalue reaches the bounds: $\nu_2=-\sqrt{\lambda}$.
\end{theorem}

\begin@@ZXQPH1QXZ@@[Optimality of Square Root Oscillation]\label{cor:pom-rh-sharp-an}
Assume $a_n=\Tr(M^n)$, then
\[
a_n=\lambda^n+(-\sqrt{\lambda})^n+O(1),
\]
And the error term cannot be improved to $o(\lambda^{n/2})$. Equivalently, the dynamical version of the "Chebyshev square root error" reaches critical optimality on this kernel.
\end{corollary}

Structural decomposition of \begin@@ZXQPH5QXZ@@[\texorpdfstring{$\zeta$}{zeta}: input product kernel \texorpdfstring{$\times$}{x} \((-z)\) Twisted golden factor \texorpdfstring{$\times$}{x} Ordinary period factor]\label{prop:pom-zeta-factorization-40}
Let the golden mean first-order Markov skeleton matrix be
$$
A:=
\begin{pmatrix}
1&1\\
1&0
\end{pmatrix},
$$
And let \(M\) be the adjacency matrix of the real input 40-state kernel (Appendix \ref{app:real-input-40-abel}).
Then there is an exact algebraic decomposition
$$
\det(I-zM)
=(1-z)^2(1+z)\cdot \det\!\bigl(I-z(A\otimes A)\bigr)\cdot \det(I+zA).
$$
Equivalently,
$$
\zeta_M(z)
=\zeta_{A\otimes A}(z)\cdot \zeta_A(-z)\cdot \frac{1}{(1-z)^2(1+z)},
$$
Where \(\zeta_B(z):=1/\det(I-zB)\) is the \(\zeta\)-function of finite subshift.
And the last "trivial period factor" \(\frac{1}{(1-z)^2(1+z)}\) only corrects short periods: at the primitive (Witt/Möbius) coefficient level it is only non-zero at \(n=1,2\) (see corollary \ref{cor:real-input-40-space-twist-boundary-split} for the item-by-term formula).
\end{proposition}
\begin{proof}
The eigenvalues ​​of matrix \(A\) are \(\varphi\) and \(-\varphi^{-1}\), so the eigenvalues ​​of \(A\otimes A\) are
\(\{\varphi^2,-1,-1,\varphi^{-2}\}\), thus
$$
\det\!\bigl(I-z(A\otimes A)\bigr)=(1-\varphi^2 z)(1+z)^2(1-\varphi^{-2}z)=(1+z)^2(1-3z+z^2).
$$
at the same time
\(
\det(I+zA)=1+z-z^2
\)。
Multiply the above two equations with \((1-z)^2(1+z)\) to get
$$
(1-z)^2(1+z)^3(1-3z+z^2)(1+z-z^2),
$$
Consistent with the \(\det(I-zM)\) factorization of theorem \ref{thm:pom-rh-sharp}. Certification completed.
\end{proof}

\begin@@ZXQPH2QXZ@@[hidden \texorpdfstring{$(-A)$}{(-A)} factor: structural source of toy RH critical line lattice]\label{rem:pom-minusA-factor-critical-line}
The third factor of the proposition \ref{prop:pom-zeta-factorization-40} can be further rewritten as
$$
\det(I+zA)=\det\!\bigl(I-z(-A)\bigr),
$$
thereby
$$
\boxed{\;
\det(I-zM)
=(1-z)^2(1+z)\cdot \det\!\bigl(I-z(A\otimes A)\bigr)\cdot \det\!\bigl(I-z(-A)\bigr).
\;}
$$
Equivalently, at the \(\zeta\)-function level,
$$
\boxed{\;
\zeta_M(z)
=\frac{1}{(1-z)^2(1+z)}\cdot \zeta_{A\otimes A}(z)\cdot \zeta_{-A}(z),
\qquad
\zeta_{-A}(z):=\frac{1}{\det(I-z(-A))}=\frac{1}{\det(I+zA)}.
\;}
$$
This rewriting completely strips out the source of toy RH's "critical line lattice": because the Perron root of \(-A\) is \(-\varphi\), and the principal scale is \(\lambda=\varphi^2\), so the only eigenvalue reaches the boundary
\(
-\sqrt{\lambda}=-\varphi
\)
It is not an accidental resonance from the complex structure of the \(40\) state, but caused by the inevitable embedding of the sign flip factor \((-A)\)} of the \textbf{golden mean adjacency operator \(A\).
Therefore the non-trivial pole sequence (theorem \ref{thm:pom-toy-rh}) in the open band \(0<\Re(s)<1\) can be regarded as the direct image of \(\det(I-z(-A))=0\) in Dirichlet coordinates \(z=\lambda^{-s}\).
\end{remark}

\begin@@ZXQPH1QXZ@@[Permutation lift perspective: Ramanujan/RH-sharp as a “new eigenvalue bound”]\label{rem:pom-permutation-lift-ramanujan}
Appendix \ref{prop:real-input-40-skeleton-factor} gives: the real input \(40\) The state kernel can be written as the block matrix of \(4\times 4\), the non-zero blocks are all induced by deterministic transfer \(10\times 10\) The permutation matrix \(B^{(d)}\), and the constant fiber subspace
\(
U=\RR^{4}\otimes \mathrm{span}\{\mathbf{1}\}
\)
Invariant under the action of \(M\) and \(M|_{U}\simeq A\otimes A\).
This is exactly \textbf{permutation lift (coverage)} in graph language: the base graph is given by \(A\otimes A\), each base edge carries a permutation (the local kernel is induced by \(d\in\{0,1,2\}\)), so that the adjacency operator \(M\) that lifts the graph is assembled on the fiber (see \cite{StarkTerras1996GraphCoverings,TerrasZetaGraphs} and figure lift A review of spectral theory).
\par
From this lift perspective, the spectrum of \(M\) is naturally divided into two categories:
\begin{itemize}
\item \textbf{Old spectrum (base/horizontal spectrum):} comes from the action on \(U\), that is, the spectrum of the base graph \(A\otimes A\);
\item \textbf{New spectrum (vertical/fiber orthogonal spectrum):} comes from the action of \(U^\perp=\RR^{4}\otimes \mathbf{1}^\perp\), the entire content of which is carried by the "longitudinal residual factor" (appendix \ref{prop:real-input-40-skeleton-factor} of \(\det_{\mathrm{vert}}\)).
\end{itemize}
Therefore the Ramanujan/RH-sharp radius bound of theorem \ref{thm:pom-rh-sharp}
\(
\max_{\mu\neq\lambda}|\mu|\le \sqrt{\lambda}
\)
It can be understood as the unified suppression bound of \textbf{new eigenvalue} of lift: the spectral radius of all vertical modes is suppressed within \(\sqrt{\lambda}\), and the saturation of the intrinsic kernel is only forced by the hidden factor \((-A)\) (note \ref{rem:pom-minusA-factor-critical-line}).
This gives a structural pipeline for the "nuclear version of RH/GRH": translating the spectral boundary problem of RH/GRH into spectral engineering of lift/coverage (constructing/excluding near-coboundary distortions to make the new spectrum controllable), which is the same type as the Ramanujan graph and the new spectrum control problem of lift (see the representative work of \cite{BiluLinial2006LiftsDiscrepancy,Friedman2008AlonSecondEigenvalue,LinialPuder2010WordMapsLifts}).
\end{remark}

\begin@@ZXQPH3QXZ@@[Additive decomposition of finite part constants: input \texorpdfstring{$\oplus$}{+} twisted \texorpdfstring{$\oplus$}{+} trivial]\label{cor:pom-logM-split-40}
Decomposition of \(\zeta\) along the proposition \ref{prop:pom-zeta-factorization-40}
\(
\zeta_M=\zeta_{\mathrm{in}}\zeta_{\mathrm{twist}}\zeta_{\mathrm{triv}}
\), where the main pole \(z_\star=1/\lambda=\varphi^{-2}\) only comes from \(\zeta_{\mathrm{in}}\).
So the Abel/Hadamard finite part constant satisfies \textbf{exact additive decomposition}
\[
\log\mathfrak{M}_{\mathrm{tot}}
\;=\;
\log\mathfrak{M}_{\mathrm{in}}
\;+\;
\log\mathfrak{M}_{\mathrm{twist}}
\;+\;
\log\mathfrak{M}_{\mathrm{triv}},
\]
Among them, \(\log\mathfrak{M}_{\mathrm{in}}\) is the finite part constant of \(\zeta_{\mathrm{in}}\), and the “finite part contribution” of the analytic factor \(f\in\{\zeta_{\mathrm{twist}},\zeta_{\mathrm{triv}}\}\) is defined as
\[
\log\mathfrak{M}_f
:=
\log f(z_\star)
\;+\;
\sum_{k\ge 2}\frac{\mu(k)}{k}\log f(z_\star^k).
\]
The corresponding numerical split is shown in the table \ref{tab:real_input_40_finite_part_split} (script \texttt{scripts/exp\_real\_input\_40\_finite\_part\_split.py} can be reproduced).
\end{corollary}

\begin{proof}
According to the theorem \ref{thm:abel-mertens-universal}, for any \(\zeta\)-function with simple main pole \(z_\star\) we have
\(
\log\mathfrak{M}=\log C+\sum_{k\ge 2}\mu(k)\log\zeta(z_\star^k)/k
\)。
Here \(\zeta_M=\zeta_{\mathrm{in}}\zeta_{\mathrm{twist}}\zeta_{\mathrm{triv}}\), and \(\zeta_{\mathrm{twist}},\zeta_{\mathrm{triv}}\) resolves to non-zero at \(z=z_\star\), so the principal residue satisfies
\(
C_{\mathrm{tot}}=C_{\mathrm{in}}\zeta_{\mathrm{twist}}(z_\star)\zeta_{\mathrm{triv}}(z_\star)
\)。
Then substitute \(\log\zeta_M=\log\zeta_{\mathrm{in}}+\log\zeta_{\mathrm{twist}}+\log\zeta_{\mathrm{triv}}\) into the tail of Möbius and group by factors to get the additive decomposition formula; the "finite part contribution" to the analytic factor \(f\) is exactly
\(
\log f(z_\star)+\sum_{k\ge2}\mu(k)\log f(z_\star^k)/k
\)。
Certification completed.
\end{proof}

\begin@@ZXQPH1QXZ@@[Algebraic closed form of main pole and residue]\label{rem:pom-logM-split-40-exact}
There is a completely closed form on this kernel:
\[
z_\star=\lambda^{-1}=\varphi^{-2}=\frac{3-\sqrt5}{2},
\qquad
C_{\mathrm{in}}=\frac{3}{10}+\frac{7\sqrt5}{50},
\qquad
\zeta_{\mathrm{triv}}(z_\star)=1+\frac{2\sqrt5}{5},
\qquad
\zeta_{\mathrm{twist}}(z_\star)=\frac{1+\sqrt5}{4},
\]
thereby
\(
C_{\mathrm{tot}}=\frac{47+21\sqrt5}{100}
\)。
These closed forms, together with the splitting constants of \(\log\mathfrak{M}\), give verifiable values ​​in the table \ref{tab:real_input_40_finite_part_split}.
\end{remark}

\input{sections/generated/tab_real_input_40_finite_part_split}

\begin@@ZXQPH1QXZ@@[Lucas--square closed form]\label{prop:pom-lucas-square}
Let Lucas count
\(
L_n:=\varphi^n+(-\varphi^{-1})^n
\)。
but
\[
a_n=L_n^2+(-1)^n(L_n+1)+2.
\]
\end{proposition}
\begin{proof}
From the closed form of appendix \ref{app:real-input-40-abel}
\(
a_n=L_{2n}+(-1)^nL_n+2+3(-1)^n
\)
It can be obtained by directly simplifying the identity $L_{2n}=L_n^2-2(-1)^n$.
\end{proof}

\begin@@ZXQPH1QXZ@@[Lucas closed form of odd prime length primitive]\label{prop:pom-primitive-prime-lucas}
Let \(p\) be an odd prime number, and \(L_n\) be a Lucas number. Then the number of primitive tracks of the real input 40-state kernel satisfies
$$
p_p=\frac{L_p(L_p-1)}{p}.
$$
\end{proposition}
\begin{proof}
Inversion by Möbius (definition \ref{def:pom-proj-prim}) has for primes \(p\)
\(
p_p=\frac{a_p-a_1}{p}
\)。
From the proposition \ref{prop:pom-lucas-square} and \(p\) is an odd number (so \((-1)^p=-1\)) we get
\(
a_p=L_p^2-L_p+1
\), and \(a_1=1\).
Substitute to get \(p_p=\frac{L_p^2-L_p}{p}\). Certification completed.
\end{proof}

\begin@@ZXQPH2QXZ@@[Lucas prime congruence: \texorpdfstring{$L_p\equiv 1\pmod p$}{Lp=1 mod p}]\label{lem:pom-lucas-modp}
For any prime number \(p\), the Lucas number satisfies \(L_p\equiv 1\ (\mathrm{mod}\ p)\).
\end{lemma}
\begin{proof}
Let \(p\neq 5\) (when \(p=5\) directly calculates \(L_5=11\equiv 1\pmod 5\)). In the ring \(\mathbb{Z}[\sqrt5]\),
From the binomial expansion and \(\binom{p}{k}\equiv 0\pmod p\) (\(1\le k\le p-1\)) we get
$$
(1+\sqrt5)^p+(1-\sqrt5)^p\equiv 2+(\sqrt5)^p+(-\sqrt5)^p\pmod p.
$$
When \(p\) is an odd prime number, the last two terms on the right cancel, so
\(
(1+\sqrt5)^p+(1-\sqrt5)^p\equiv 2\pmod p
\)；
When \(p=2\) direct verification is also established. And from the definition \(2^p L_p=(1+\sqrt5)^p+(1-\sqrt5)^p\) and Fermat’s little theorem \(2^{p-1}\equiv 1\pmod p\) we get
\(
2^p\equiv 2\pmod p
\), so \(L_p\equiv 1\pmod p\). Certification completed.
\end{proof}

\begin@@ZXQPH2QXZ@@[Primary Wieferich fingerprint (\texorpdfstring{$\nu_p$}{nu_p})]\label{def:pom-lucas-nu-p}
For odd prime numbers \(p\), define
\[
\nu_p:=v_p(L_p-1)\in\ZZ_{\ge 1},
\]
Among them, \(v_p(\cdot)\) is \(p\)-forward assignment.
\end{definition}

\begin@@ZXQPH1QXZ@@[Complete integer criterion for primitive degeneration]\label{cor:pom-lucas-nu-p-criterion}
Let \(p\) be an odd prime number, and follow the notation of the proposition \ref{prop:pom-primitive-prime-lucas}.
but
\[
v_p(p_p)=\nu_p-1.
\]
Therefore the "degradation intensity" of the prime layer is fully controlled by a single integer \(\nu_p\): for any \(k\ge 1\),
\[
p^{k}\mid p_p
\qquad\Longleftrightarrow\qquad
\nu_p\ge k+1.
\]
In particular, \(p\mid p_p\) is equivalent to \(p^2\mid(L_p-1)\) if and only if \(\nu_p\ge 2\).
\end{corollary}
\begin{proof}
From lemma \ref{lem:pom-lucas-modp} we get \(L_p\equiv 1\pmod p\), so \(v_p(L_p)=0\) and \(\nu_p\ge 1\).
On the other hand, the proposition \ref{prop:pom-primitive-prime-lucas} gives
\(
p_p=L_p(L_p-1)/p
\),therefore
\[
v_p(p_p)=v_p(L_p)+v_p(L_p-1)-1=\nu_p-1.
\]
Therefore, \(p^k\mid p_p\) is equivalent to \(v_p(p_p)\ge k\), and then we get \(p^k\mid p_p\Longleftrightarrow \nu_p\ge k+1\) from the above formula.
\end{proof}

\begin@@ZXQPH1QXZ@@[Integerness and Wieferich-type rare events]\label{rem:pom-lucas-wieferich}
By inference \ref{cor:pom-lucas-nu-p-criterion}, the integerity and rare degeneration of the prime-length layer can be expressed by
\(
\nu_p=v_p(L_p-1)
\)
Unified classification: \(\nu_p\ge 1\) is always true, and \(\nu_p\ge 2\) (equivalent to \(p\mid p_p\)) corresponds to the Wieferich-type rare event interface of the Lucas sequence.
Thus \(\nu_p\) can be considered an auditable fingerprint of "prime-length primitive arithmetic degeneration", and \(\nu_p=2,3,\dots\) as a natural gradation of anomaly strength.
\end{remark}

\begin@@ZXQPH1QXZ@@[primitive parity split]\label{cor:pom-prim-odd-even}
Note $p_n$ as the number of primitive tracks, then
\[
p_n=\frac{\lambda^n}{n}+\frac{\bigl((-1)^n-\mathbf{1}_{2\mid n}\bigr)\lambda^{n/2}}{n}
+O\!\left(\frac{\lambda^{n/3}}{n}\right).
\]
Among them, the $\lambda^{n/2}$ term in the even-numbered case exactly cancels out between the $d=1,2$ terms in the Möbius inversion (see appendix \ref{app:real-input-40-finite-rh}).
\par
In particular, when \(n\) is an odd number, the above formula becomes
\[
p_n=\frac{\lambda^n}{n}-\frac{\lambda^{n/2}}{n}+O\!\left(\frac{\lambda^{n/3}}{n}\right),
\]
When \(n\) is an even number, the \(\lambda^{n/2}\) level explicit oscillation term completely disappears; the lower order oscillation term related to the bounded eigenvalue \(-\sqrt{\lambda}\) is shown in the note \ref{rem:pom-prim-even-lower-osc}.
\end{corollary}

\begin@@ZXQPH1QXZ@@[Lower-order oscillations of even-length orbits]\label{rem:pom-prim-even-lower-osc}
Write $n=2m$, use $a_{m}=\lambda^{m}+(-\sqrt{\lambda})^{m}+O(1)$ to get further
\[
p_{2m}=\frac{\lambda^{2m}}{2m}-\frac{(-\sqrt{\lambda})^{m}}{2m}
+O\!\left(\frac{\lambda^{2m/3}}{m}\right),
\]
Indicates that the first explicit term size of an even-length primitive is $\lambda^{n/4}$.
\end{remark}

\begin@@ZXQPH1QXZ@@[Vertical lattice in Dirichlet logarithmic coordinates]\label{lem:pom-dirichlet-vertical-lattice}
Take \(\lambda>1\) and let \(z=\lambda^{-s}\), \(r=\lambda z=\lambda^{1-s}\).
Given \(r_0\in\CC^\times\), the solution of the equation \(r=r_0\) forms a vertical lattice on the \(s\)-plane:
\[
s=1-\frac{\log|r_0|}{\log\lambda}-\,\mathrm{i}\,\frac{\arg(r_0)+2\pi k}{\log\lambda},
\qquad k\in\ZZ,
\]
Among them \(\arg(r_0)\in(-\pi,\pi]\).
Therefore the imaginary part spacing is constant
\[
\boxed{\ \Delta\Im(s)=\frac{2\pi}{\log\lambda}\ }.
\]
In the special case \(r_0<0\), \(\arg(r_0)=\pi\), the above formula becomes
\[
s=1-\frac{\log|r_0|}{\log\lambda}-\,\mathrm{i}\,\frac{(2k+1)\pi}{\log\lambda},
\qquad k\in\ZZ.
\]
\end{lemma}
\begin{proof}
Got by \(r=\lambda^{1-s}\)
\(
(1-s)\log\lambda=\log r_0
\), and write the multivalued logarithm as
\(
\log r_0=\log|r_0|+\mathrm{i}\,(\arg(r_0)+2\pi k)
\)(\(k\in\ZZ\)), solve \(s\) to get.
\end{proof}

\begin@@ZXQPH1QXZ@@[Dirichlet pole lattice and toy RH]\label{thm:pom-toy-rh}
Take the Dirichlet variables $z=\lambda^{-s}$ and $r=\lambda z=\lambda^{1-s}$. Then all the poles of $\widehat{\zeta}(s)=\zeta_M(\lambda^{-s})$ are given by
\[
r\in\{1,\ \pm\lambda,\ \lambda^2,\ \varphi^3,\ -\varphi\}
\]
given and satisfied
\[
\Re(s)\in\left\{1,\tfrac12,0,-\tfrac12,-1\right\}.
\]
Among them $r=-\varphi$ produces a critical line lattice
\(
s=\tfrac12+\frac{(2k+1)\pi i}{\log\lambda}
\),
It is the only pole sequence within the open band $0<\Re(s)<1$ (see appendix \ref{app:real-input-40-finite-rh}).
\end{theorem}

\begin@@ZXQPH1QXZ@@[Logarithmic image mechanism of critical line (toy RH)]\label{rem:pom-toy-rh-critical-line-log}
According to the lemma \ref{lem:pom-dirichlet-vertical-lattice}, any negative pole point \(r_0<0\) generates a vertical lattice line with a fixed real part under Dirichlet logarithmic coordinates
\(
\Re(s)=1-\frac{\log|r_0|}{\log\lambda}
\)。
Under the standard normalization \(\lambda=\varphi^2\) in this article, take \(r_0=-\varphi=-\sqrt{\lambda}\) to get
\(
\Re(s)=\tfrac12
\), so the "critical line" can be regarded as the algebraic ratio image of the negative pole point under logarithmic mapping; for the nuclear decomposition explanation of its structural origin, see note \ref{rem:pom-minusA-factor-critical-line}.
\end{remark}

\begin@@ZXQPH1QXZ@@[Second- and third-order cumulative quantity closed form of output pressure]\label{prop:pom-pressure-cumulants}
Let $u=e^{\theta}$, $\lambda(u)=\rho(M(u))$, $P(\theta)=\log\lambda(e^\theta)$, and use
\(
G(\lambda,u)=0
\)
Portraying $\lambda(u)$ (see appendix \ref{app:real-input-40-zeta-u}). but
\[
P''(0)=\frac{1791}{200}-\frac{3983}{1000}\sqrt5,
\qquad
P'''(0)=\frac{1416369}{5000}-\frac{126691}{1000}\sqrt5,
\]
Thus $P''(0)\approx 0.0487412456$, $P'''(0)\approx -0.0158881374$.
\end{proposition}
\begin{proof}
Do the implicit function derivative of $G(\lambda(u),u)=0$, and use the chain rule of $u=e^\theta$ to convert the $u$-derivative into the $\theta$-derivative; substitute $u=1,\lambda=\varphi^2$ to get it.
\end{proof}

\begin@@ZXQPH1QXZ@@[zero temperature limit constant]\label{cor:pom-pressure-zero-temp}
From the appendix \ref{app:real-input-40-zeta-u} we can get $\theta\to+\infty$
\[
P(\theta)=\frac{\theta}{2}+\log c+o(1),
\]
Among them, $c=\sqrt{y_{\max}}$ and $y_{\max}$ are the maximum positive roots of the equation $y^4-6y^3+9y^2-y-1=0$.
\end{corollary}

\input{sections/generated/eq_real_input_40_output_potential_puiseux_asymptotics}

\subsection{Bridge from prime number shadow to prime number register}\label{subsec:pom-prime-bridge}
\paragraph{Twisting and decomposition of finite axis beam}
For the family of twisted matrices $M_{K,\rho}(u)$ with the twisted $\zeta/L$-function of the finite group label $G$, the proposition \ref{prop:kernel-artin-factorization} gives an Artin-type factorization. This decomposition shows that the finite axial bundle is essentially an "auditable fibrosis" with $\widehat G$ as the spectral coordinate system.

\paragraph{Irreducibility of additional modular axes}
The proposition \ref{prop:arity-335-n2-nondegenerate} proves that the third axis $N_2(\gamma)\bmod 5$ cannot be eliminated by the first two axes, so the introduction of this modular axis will bring irreducible new degrees of freedom. This fact solidifies "adding tracts is not relabeling" as an auditable conclusion at the nuclear level.

\begin@@ZXQPH1QXZ@@[Limit diameter of prime register fiber]\label{def:pom-profinite}
Projective limit of modulo-taking system $\{\ZZ/m\ZZ\}_{m\ge 1}$
\[
\widehat{\ZZ}:=\varprojlim_m \ZZ/m\ZZ\ \cong\ \prod_{p\in\mathbb{P}}\ZZ_p,
\]
And take it as the limit object of "infinite prime axis bundle". This limit unifies all finite mode axial bundles into the same fiber coordinate system, thus providing a structural definition for the "prime register" (see \cite{Apostol1976} on CRT and p-ary decomposition).
\end{definition}

\begin@@ZXQPH2QXZ@@[Aligned with the true caliber of the Dirichlet character: \texorpdfstring{$\widehat{\ZZ}^\times$}{Zhat^x}]\label{rem:pom-profinite-units}
In analytic number theory, the Dirichlet role acts on the unit group $(\ZZ/m\ZZ)^\times$, whose natural limit object is
\[
\widehat{\ZZ}^{\,\times}:=\varprojlim_m (\ZZ/m\ZZ)^\times\ \cong\ \prod_{p\in\mathbb{P}}\ZZ_p^\times.
\]
This article uses $\widehat{\ZZ}$ as unified coordinates at the "congruence axis beam" level in order to bring all finite mode residue readouts into the same fibrosis framework; when strict alignment with Dirichlet/Artin's role language is required, just limit the readouts to the unit part within the framework (and correspondingly replace the role base with the dual coordinates of $\widehat{\ZZ}^{\,\times}$), and you can get a "full character family" caliber that is closer to the real GRH.
\end{remark}

\begin@@ZXQPH1QXZ@@[Dirichlet type congruent axis beam (unit version)]\label{def:pom-dirichlet-axis-units}
For each modulus \(m\ge 1\), take
\[
G_m:=(\ZZ/m\ZZ)^\times,
\]
And use the natural projection \(G_{m_2}\to G_{m_1}\) (when \(m_1\mid m_2\)) to form an inverse system; its inverse limit is
\[
G_\infty:=\varprojlim_m G_m=\widehat{\ZZ}^{\,\times}.
\]
Under this caliber, the finite layer character set \(\widehat{G_m}\) is equivalent to the Dirichlet character family "modulo \(m\)" (strictly speaking, it is its restriction on the unit group; by convention, extending it to a function on \(\ZZ/m\ZZ\) and taking \(0\) at the non-unit, you can get the standard Dirichlet character).
\end{definition}

\begin@@ZXQPH1QXZ@@[Finite word stability: finite layer reduction of infinite congruence axes]\label{prop:pom-finite-modulus-stability}
Under the Dirichlet axis beam diameter that defines \ref{def:pom-dirichlet-axis-units}, let \(w\) be any finite projection word (\(1\)-morphism).
remember
\[
\mathsf@@ZXQPH0QXZ@@(w):=\{\,m\ge 1:\ w\ \text{A certain limited layer is explicitly called syntactically}\ G_m=(\ZZ/m\ZZ)^\times\ \text{or its character/twisted door}\,\},
\]
And order
\[
m_\ast:=\mathrm@@ZXQPH0QXZ@@\,\mathsf@@ZXQPH1QXZ@@(w)\qquad(\mathsf@@ZXQPH2QXZ@@(w)=\varnothing\ \text{time taken}\ m_\ast:=1).
\]
but:
\begin{enumerate}
\item \textbf{(finite layer closure)} \(w\) All semantic dependencies in the congruence/role direction can be closed on a single finite layer \(G_{m_\ast}\): any Dirichlet role channel appearing in \(w\) can be regarded as a certain \(\chi_\ast\in\widehat{G_{m_\ast}}\) limit (pull along \(G_{m_\ast}\to G_m\)).
\item \textbf{(verification reduction)} Therefore, under the audit caliber with "full role scan/worst channel" as input (such as \(\sup_{\chi\neq 1}\rho(M_{m,\chi}(u))\) or finite part constant comparison on a fixed grid), the verification of \(w\) can be reduced to the verification of the finite set \(\widehat{G_{m_\ast}}\) Exhaustive inspection.
\end{enumerate}
This "infinite register" is implemented into a certifiable pro-object: \(G_\infty\), which is embodied as an incrementally expandable axis bundle, but any finite implementation must terminate and complete verification on a certain finite layer.
\end{proposition}
\begin{proof}
Since \(w\) is a finite word, only a limited number of modulus levels can appear grammatically, so \(\mathsf{Mod}(w)\) is a finite set and \(m_\ast\) is well defined.
For any \(m\in\mathsf{Mod}(w)\) we have \(m\mid m_\ast\), so there is a natural projection \(G_{m_\ast}\to G_m\); which induces a pullback on the Pontryagin dual
\(
\widehat{G_m}\hookrightarrow \widehat{G_{m_\ast}}
\)。
So all finite layer role/distortion channels appearing in \(w\) can be uniformly promoted to channels on \(\widehat{G_{m_\ast}}\), and the congruence/role semantics of \(w\) are closed on that finite layer. Since \(\widehat{G_{m_\ast}}\) is a finite set, the exhaustive "full character verification" is terminated within a finite number of steps. Certification completed.
\end{proof}

\begin@@ZXQPH1QXZ@@[Local finite Dirichlet distortion and tensorization closure]\label{cor:pom-local-finite-dirichlet-action}
Under the setting of the proposition \ref{prop:pom-finite-modulus-stability}, the Dirichlet distortion channel of any finite projection word \(w\) is structurally \textbf{locally finite}:
\begin{enumerate}
\item \textbf{(finite layer factorization)}For \(w\), there is a closed layer \(m_\ast\) such that all twisted gates/channels that semantically require enumeration (such as the \(\mathrm{Tw}_\chi\) caliber of \ref{rem:twist-commuting-cube}) can be considered Restrictions on roles on \(\widehat{G_{m_\ast}}\); thus any Full Role Scan/Worst Pass audit is factored by the finite group \(G_{m_\ast}\) and terminated in a finite step.
\item \textbf{(tensorized closure)}If the closed class generated by direct sum, tensor product, direct factor and finite composite is taken for finite multiple words \(\{w_i\}_{i=1}^{\ell}\), then its Dirichlet dependency is still closed in a single finite layer; the closed layer can be taken as
  \(m_\ast=\mathrm{lcm}\bigl(m_\ast(w_1),\dots,m_\ast(w_\ell)\bigr)\)。
\end{enumerate}
This conclusion implements the termination of "infinite congruence/role axis bundle" into an auditable finite principle: infinite registers only exist as inverse limit semantics, and any finite implementation must be verified on the finite layer.
\end{corollary}

\begin@@ZXQPH1QXZ@@[Tannakian caliber cyclotomic quotient (optional viewing angle)]\label{rem:pom-cyclotomic-quotient-tannakian}
Organize the finite core/matrix family with Dirichlet twist structures and their intertwiners (e.g. the \(\mathbf{End}_R\) caliber of the appendix proposition \ref{prop:witt-bridge-functorial}) into a \(\QQ\)-linear rigid tensor category \(\mathcal{C}\), and let there be a faithful tensor fiber functor \(\omega:\mathcal{C}\to\mathrm{Vect}_{\QQ}\).
Then the "role direction" of the tensor automorphism group \(\mathrm{Aut}^{\otimes}(\omega)\) is induced by the twist gate \(\mathrm{Tw}_\chi\); by deducing the local finiteness of \ref{cor:pom-local-finite-dirichlet-action}, this direction is factorized on each object via a certain finite layer \(G_{m_\ast}\), thus obtaining the inverse limit
\(
G_\infty=\varprojlim_m G_m
\)
The natural full homomorphism (cyclotomic quotient) of
\[
\mathrm{Aut}^{\otimes}(\omega)\twoheadrightarrow G_\infty.
\]
This perspective is only used to explain the categorization source of "the twisted channel is not an external label, but a representation theoretic decomposition forced by the structure" (see also the Big-Witt caliber of the appendix theorem \ref{thm:witt-dirichlet-twist}); all auditable conclusions in the text only rely on the finite layer closure of the proposition \ref{prop:pom-finite-modulus-stability} and its subsequent certificate chain.
\end{remark}

\begin@@ZXQPH1QXZ@@[Dirichlet character distortion spectrum: nuclear version of GRH's "Dirichlet-like" form]\label{cor:pom-dirichlet-grh-twisted-radius}
Take \(G_\infty=\widehat{\ZZ}^{\,\times}\) in the proposition \ref{prop:pom-profinite-artin-hilbert}, and assume a one-step cocycle
\(
g:E(K)\to \widehat{\ZZ}^{\,\times}
\)
A group expansion kernel is given with a "unit label". but:
\begin{enumerate}
\item \textbf{(finite layer aperture)} For each \(m\), read the label to the finite layer \(G_m=(\ZZ/m\ZZ)^\times\), and define the distortion matrix for each Dirichlet character \(\chi\in\widehat{G_m}\)
  \[
  M_{m,\chi}(u):=M_\chi(u),
  \]
where \(M_\chi(u)\) is given by the entry formula of the proposition \ref{prop:pom-profinite-artin-hilbert} (where the group operation is multiplication).
\item \textbf{(GRH-type equivalent expression)} Let’s record the Perron root of the ordinary character channel as
  \(
  \lambda_1(u):=\rho(M_{m,1}(u))
  \)。
Then the nuclear version of GRH’s “unified square root bounds for all characters”
  \[
  \sup_{\chi\in\widehat{G_m},\ \chi\neq 1}\rho\!\left(M_{m,\chi}(u)\right)\ \le\ \lambda_1(u)^{1/2}
  \]
It is of the same type as the operator spectrum radius inequality at point 3 of the proposition \ref{prop:pom-profinite-artin-hilbert}; and is structurally aligned layer by layer with the square root error of the shape of the nuclear version of Chebotarev/prime number theorem (definition \ref{def:kernel-rh} and corollary \ref{cor:kernel-rh-sqrt-error}).
\end{enumerate}
\end{corollary}

\begin@@ZXQPH0QXZ@@[From finite twist to infinite register]
Finite group twisting provides an auditable spectral decomposition of "finite mode axis bundles"; while the irreducibility of additional axes means that the decomposition strictly increases the degrees of freedom as the number of modes increases. Therefore, the limit aperture of $\widehat{\ZZ}$ is structurally compatible with the prime register fiber $\mathcal{P}$: the former is "the completeness of the modular axis bundle" and the latter is "the linearization of the axis bundle of multiplicative coordinates".
\end{remark}

\begin@@ZXQPH1QXZ@@[Inverse limit of congruence axis and role diagonalization]\label{rem:pom-congruence-invlimit}
Treating the finite congruence axis $\{\ZZ/m\ZZ\}_{m\ge 1}$ as a projection system, then its inverse limit is given
\(
\widehat{\ZZ}\cong\prod_{p}\ZZ_p
\)；
The corresponding role base
\(
\chi_{k/m}(n)=e^{2\pi i kn/m}
\)
are natural coordinates on the Pontryagin dual. From this, "finite congruence axis + phase diagonalization + distortion $\zeta$" forms an auditable construction path, and the limit is the strict implementation of "infinite prime register fiber".
\end{remark}

\begin@@ZXQPH1QXZ@@[profinite role diagonalization: Artin decomposition on Hilbert space and operatorization of nuclear version of GRH]\label{prop:pom-profinite-artin-hilbert}
Let \(K\) be a finite kernel (directed graph/Markov kernel), the vertex set be \(V\), and let \(\kappa:E(K)\to\ZZ\) be a one-step additive potential (such as carry/collision/output potential).
Take the profinite congruence axis \(G_\infty:=\widehat{\ZZ}\) (or its unit part \(\widehat{\ZZ}^{\,\times}\), see note \ref{rem:pom-profinite-units}), and let
\(
g:E(K)\to G_\infty
\)
It is a one-step congruential cocycle (that is, each edge is marked as a certain residue increment; the reading of the finite module \(m\) is the synthesis \(G_\infty\to\ZZ/m\ZZ\)).
in Hilbert space
\[
\mathcal{H}:=\CC^{V}\otimes L^2(G_\infty)
\]
(\(G_\infty\) Take the Haar measure) and define the "group expansion adjacency operator"
\[
\bigl(\mathcal{M}_u f\bigr)(j,h)
:=\sum_{e:i\to j} u^{\kappa(e)}\, f\!\bigl(i,\ h\cdot g(e)\bigr),
\qquad u>0,
\]
Where \(f\in\mathcal{H}\) is regarded as the square integrable function on \(V\times G_\infty\), and \(h\mapsto h\cdot g(e)\) is a right multiplication translation.
but:
\begin{enumerate}
\item \textbf{(Fourier decomposition and block diagonalization)} Since \(G_\infty\) is a compact commutative group, there is a role orthonormal basis decomposition
  \[
  L^2(G_\infty)\cong \bigoplus_{\chi\in\widehat{G_\infty}}\CC\,\chi,
  \]
And under this decomposition \(\mathcal{M}_u\) becomes a direct sum in a unitary equivalent way
  \[
  \mathcal{M}_u\ \cong\ \bigoplus_{\chi\in\widehat{G_\infty}} M_\chi(u),
  \]
where each block \(M_\chi(u)\) is a finite-dimensional warped matrix on \(\CC^{V}\):
  \[
  (M_\chi(u))_{i\to j}:=\sum_{e:i\to j} u^{\kappa(e)}\,\chi\!\bigl(g(e)\bigr).
  \]
\item \textbf{(Operator readout of the worst distortion spectrum radius)} Let the ordinary character be \(1\), and let
  \(
  \mathcal{H}_0:=\CC^V\otimes\mathrm{span}\{1\}
  \)
and \(\mathcal{H}_\perp:=\mathcal{H}_0^\perp\).
but
  \[
  \rho\!\left(\mathcal{M}_u|_{\mathcal{H}_\perp}\right)
  =\sup_{\chi\neq 1}\rho\!\left(M_\chi(u)\right),
  \qquad
  \rho\!\left(\mathcal{M}_u|_{\mathcal{H}_0}\right)=\rho\!\left(M_1(u)\right)=:\lambda_1(u).
  \]
\item \textbf{(Operatorization of the nuclear version of GRH)} If the nuclear version of GRH (the isotype condition that defines \ref{def:kernel-rh}) is established on the profinite role family, that is
  \(
  \sup_{\chi\neq 1}\rho(M_\chi(u))\le \lambda_1(u)^{1/2}
  \),
Then there is equivalently the operator spectral radius inequality
  \[
  \rho\!\left(\mathcal{M}_u|_{\mathcal{H}_\perp}\right)\ \le\ \lambda_1(u)^{1/2}.
  \]
\end{enumerate}
\end{proposition}
\begin{proof}
For the axis bundle of finite module \(m\), the proposition \ref{prop:kernel-artin-factorization} gives the determinant factorization of the finite group version, which is essentially the Fourier/role decomposition of the regular representation.
For the compact commutative group \(G_\infty\), the Pontryagin dual \(\widehat{G_\infty}\) is a discrete commutative group, and the roles form an orthonormal basis in \(L^2(G_\infty)\); the right-multiplied translation \(T_{g(e)}:f(h)\mapsto f(h\cdot g(e))\) is diagonalized on this basis, and the eigenvalue is \(\chi(g(e))\).
Therefore, \(\mathcal{M}_u\) is reduced to a finite-dimensional block \(M_\chi(u)\) on a point-by-point basis under the Fourier basis of \(L^2(G_\infty)\), thus obtaining the direct sum decomposition. The spectral radius identity is given by the spectrum of the direct sum operator being the union of the individual block spectra. Certification completed.
\end{proof}

\begin@@ZXQPH1QXZ@@[Finite layer lifting system and fiber measurement]\label{def:pom-lifted-system-m}
For the finite congruence axis $G_m$ of module $m$, define its \textbf{finite layer lifting system} as the product dynamic system $(\Sigma \times G_m, \sigma_m)$, where $\Sigma$ is the base state space and $\sigma_m$ is the kernel $K$ with congruent cocycle $g$ induced lifting transfer.
Define its stationary measure as \textbf{raised Parry measure}:
\[
\widetilde\mu_m := \mu_{\mathrm{Parry}} \otimes \mathrm{Haar}_{G_m},
\]
Among them, $\mu_{\mathrm{Parry}}$ is the Parry measure on the base system, and $G_m$ is the normalized Haar measure.
Define \textbf{zero-mean fiber observable space} as
\[
L^2_0(G_m) := \left\{ f \in L^2(G_m) : \int_{G_m} f(g) \, d\mathrm{Haar}(g) = 0 \right\},
\]
And embed it naturally as a function on the boost system $F(x, g) = f(g)$.
\end{definition}

\begin@@ZXQPH1QXZ@@[Congruent fiber related attenuation rate = worst distortion ratio]\label{thm:pom-fiber-corr-decay}
Fixed $u>0$. Let $f \in L^2_0(G_m)$ be the zero-mean fiber observable. Define its time-related function as
\[
C_f(n) := \mathbb{E}_{\widetilde\mu_m} [f(g_0) \overline{f(g_n)}],
\]
Where $g_n$ is the fiber coordinate after $n$ step transfer. Note $\lambda_1(u) := \rho(M_{m,1}(u))$ as the main eigenvalue of the base system. but:
\begin{enumerate}
\item \textbf{(exponential decay bound)} The correlation function satisfies
    \[
    |C_f(n)| \le \|f\|_2^2 \cdot \eta_m(u)^n,
    \]
Where $\eta_m(u)$ is \textbf{worst distortion ratio}:
    \[
    \eta_m(u) := \max_{\chi \in \widehat{G_m},\, \chi \neq 1} \frac{\rho(M_{m,\chi}(u))}{\rho(M_{m,1}(u))}.
    \]
\item \textbf{(role decomposition formula)} Let $f = \sum_{\chi \neq 1} \hat{f}(\chi) \chi$ be the Fourier expansion of $f$. Let $\mathcal{L}_\chi$ be the normalized twist transfer operator, whose matrix element is $(\mathcal{L}_\chi)_{ij} = \sum_{e:i\to j} \frac{h_j}{\lambda_1(u) h_i} \chi(g(e))$, where $h$ is the right positive eigenvector of $M_{m,1}(u)$. Then the correlation function has an exact role superposition representation:
    \[
    C_f(n) = \sum_{\chi \neq 1} |\hat{f}(\chi)|^2 \langle \pi, \mathcal{L}_\chi^n \mathbf{1} \rangle,
    \]
Among them, $\pi$ is the stationary distribution of the base system (satisfying $\pi \mathcal{L}_1 = \pi$), and $\mathbf{1}$ is the full $1$ vector.
\item \textbf{(Scale distinction: horizontal vs vertical)}
The state layer correlation attenuation (proposition \ref{prop:pom-time-corr-gap}) is determined by the spectral gap $\rho = \Lambda/\lambda_1$ of the basis matrix, which characterizes the basis space mixing velocity;
The fiber layer correlation attenuation is determined by the twist ratio $\eta_m(u)$, which describes the "de-correlation" speed of congruent labels in the path evolution.
The two are independent structural objects: the former comes from the internal spectral structure of $M_1(u)$, and the latter comes from the relative modulus ratio of $M_\chi(u)$ and $M_1(u)$.
\end{enumerate}
\end{theorem}

\begin{proof}
The path cocycle is recorded as $g(e_1 \dots e_n) := g(e_1) \cdots g(e_n)$, and its multiplication sequence is consistent with the right translation $h \mapsto h \cdot g(e)$.
Since the lifting measure $\widetilde\mu_m = \mu_{\mathrm{Parry}} \otimes \mathrm{Haar}_{G_m}$ is a product measure, and the fiber transfer $g_n = g_0 \cdot g(e_1 \dots e_n)$ only depends on the basis path, the relevant function can be written as:
\[
C_f(n) = \int_{G_m} \int_{\Sigma} f(g_0) \overline{f(g_0 \cdot g(e_1 \dots e_n))} \, d\mu_{\mathrm{Parry}} \, d\mathrm{Haar}(g_0).
\]
Using the translation invariance of the Haar measure and the orthogonality of the role $\int \chi(g) \overline{\chi'(g)} dg = \delta_{\chi,\chi'}$, substitute $f = \sum \hat{f}(\chi) \chi$ to get:
\[
C_f(n) = \sum_{\chi \neq 1} |\hat{f}(\chi)|^2 \int_{\Sigma} \overline{\chi(g(e_1 \dots e_n))} \, d\mu_{\mathrm{Parry}}.
\]
The integral term is exactly the $n$ step transfer expectation $\langle \pi, \mathcal{L}_\chi^n \mathbf{1} \rangle$ of the normalized twist operator $\mathcal{L}_\chi$.
Since $\rho(\mathcal{L}_\chi) = \rho(M_{m,\chi}(u))/\lambda_1(u)$, we get $|\langle \pi, \mathcal{L}_\chi^n \mathbf{1} \rangle| \le \rho(\mathcal{L}_\chi)^n$ from the spectral radius formula.
Taking $\eta_m(u) = \max_{\chi \neq 1} \rho(\mathcal{L}_\chi)$ and using the Parseval identity $\sum |\hat{f}(\chi)|^2 = \|f\|_2^2$, we get the exponential decay bound.
Since $f \in L^2_0(G_m)$, the coefficient of the trivial character $\chi=1$ is $\hat{f}(1) = 0$, the correlation is entirely determined by the non-trivial twist channel.
\end{proof}

\begin@@ZXQPH1QXZ@@[Numerical certificate of congruent fiber-related attenuation]\label{rem:pom-fiber-corr-decay-certificate}
Theorem @ZXQPH9QXZ@@ The exponential decay bound is governed by the worst twist ratio $\eta_m(u)$. Table \ref{tab:real-input-40-lifted-chain-eta-envelope} gives numerical certificates under the modular specification $((3,3,5))$, where the third axis is $N_2 \bmod 5$ (collision count). The certificate comes from the output of the script \verb|scripts/exp_real_input_40_lifted_chain_correlation.py| via the transfer matrix for all non-trivial characters $\chi \neq 1$ $M_{m,\chi}(u)$ Calculate the spectral radius and determine the worst distortion role $j^\star=(0,0,4)$ and its corresponding ratio $\eta_m \approx 0.9506$. The maximum envelope value in the table $\max_n |C_f(n)|/\eta_m^n \approx 1.0027$ is reached at $n=2$, which verifies the tightness of the theoretical bounds.

\input{sections/generated/tab_real_input_40_lifted_chain_eta_envelope}
\end{remark}

\begin@@ZXQPH1QXZ@@[Contrast: Ihara--Isomorphic version of Ramanujan mechanism]\label{rem:pom-profinite-ihara-ramanujan}
The structure of the proposition \ref{prop:pom-profinite-artin-hilbert} is completely the same type as the “graph plate RH \(\Leftrightarrow\) Ramanujan spectral boundary” of graph theory Ihara-\(\zeta\):
Role decomposition diagonalizes the operators on the cover/axis bundle into twisted blocks, and the "square root bound" is the unified spectral radius control for all non-trivial blocks.
The nuclear version of RH/GRH (definition \ref{def:kernel-rh}) in this article transplants this mechanism to the auditable scenario of "primitive orbit counting/explicit formula error" (see note \ref{rem:kernel-graph-rh}).
\end{remark}

\begin@@ZXQPH1QXZ@@[Prime length as parallel structure probe]\label{rem:pom-prime-length-probe}
On the global kernel of the tensor assembly, the trace sequence satisfies $a_n^{\mathrm{glob}}(u)=a_n^{\mathrm{flow}}(u)^m$, and the primitive spectrum is given by the Witt/Möbius inversion (corollary \ref{cor:par-add-global-lift-endpoints}). When $n=\ell$ is a prime number,
\[
p_\ell^{\mathrm{glob}}(u)=\frac{1}{\ell}\Bigl(a_\ell^{\mathrm{flow}}(u)^m-a_1^{\mathrm{flow}}(u^\ell)^m\Bigr),
\]
Therefore, the primitive term of prime length directly isolates the difference between "irreducible cycle" and "power repeating cycle". This difference is highly sensitive to parallel tensorization and serves as an unbiased probe of hidden parallelism and structural decomposition.
\end{remark}

\subsection{Projection Word Regular Form and Reading Hierarchy Tower}\label{subsec:pom-normal-form}
\begin@@ZXQPH1QXZ@@[One-shot normal form of semi-ring closure]\label{thm:pom-one-fold-normal-form}
Let $t$ be any item generated by $\oplus,\otimes,0_X,1_X$, and the variable takes the value $X_\infty$. Then there exists a polynomial
\(
P\in\NN[x_1,\dots,x_k]
\)
Such that for any $c_1,\dots,c_k\in X_\infty$,
\[
t(c_1,\dots,c_k)=Z\!\left(P\bigl(\mathrm{Val}(c_1),\dots,\mathrm{Val}(c_k)\bigr)\right).
\]
Therefore, within the semi-ring closure, the canonical projection $P_Z$ can be called once uniformly at the end, while the intermediate steps can be completely left at the process level.
\end{theorem}
\begin{proof}
From the theorem \ref{thm:add-definitional} and \ref{thm:mul-definitional}, $\mathrm{Val}$ gives the semicyclic isomorphism of $(X_\infty,\oplus,\otimes)$ and $(\NN,+,\times)$. Any term $t$ corresponds to a polynomial $P$ on $\NN$; then
\(
\mathrm{Val}(t(c_1,\dots,c_k))=P(\mathrm{Val}(c_1),\dots,\mathrm{Val}(c_k))
\)。
Then draw the conclusion from $Z=\mathrm{Val}^{-1}$. Certification completed.
\end{proof}

\begin@@ZXQPH1QXZ@@[Program: Read the Hierarchy Tower]\label{rem:pom-hyper-iteration}
Theorem \ref{thm:pom-one-fold-normal-form} states that within a semiring closure "the canonical projection only needs to be terminated once". Further, a readout hierarchy tower can be constructed along the lines of the theorem \ref{thm:composition-two-layer}: the $0$ layer reads out one iteration corresponding to the synthesis of $\circ$, and the $1$ layer uses "addition iteration" to correspond to multiplication; the $k$ layer is recursively defined in the same way. The "operation" of the layer is the iterative readout of the $k{-}1$ layer. This hierarchical tower does not introduce new primitives at the process level, but transfers complexity to the improvement of the readout level.
\end{remark}
